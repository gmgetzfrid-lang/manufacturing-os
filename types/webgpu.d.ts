// WebGPU is not yet in TypeScript's bundled DOM library, so pull in the
// official community typings globally rather than listing them under
// compilerOptions.types (which would suppress every other @types package).
/// <reference types="@webgpu/types" />
