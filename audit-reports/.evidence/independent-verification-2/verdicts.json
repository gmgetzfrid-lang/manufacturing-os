[
  {
    "id": "ALOG-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/schema.sql:52-59 defines the table with `data JSONB NOT NULL DEFAULT '{}'` and no `value` column; lib/capabilityPolicy.ts:173-176 `.from(\"org_configurations\").select(\"value\")` and :231 `{ org_id, key: \"capability_policy\", value: input.policy, ... }`; supabase/migrations/20260901_db_hard_enforcement.sql:44-45 `SELECT value INTO v_val FROM org_configurations WHERE org_id = p_org AND key = 'capability_policy';`. Every other reader uses `data`: lib/orgBranding.ts:23-24 `.select(\"data\")`, lib/ticketRouting.ts:49-50 `.select(\"data\")`, app/(protected)/admin/requests/page.tsx:79 `.select('data')`.",
    "note": "Repo-wide search confirms the absence: no `ALTER TABLE org_configurations`, no `ADD COLUMN value`, no rename anywhere under supabase/ — the only `value JSONB` in the tree is an unrelated table in 20260920_per_user_keys_real_limits.sql:25. The read path fails silently (supabase-js returns an error object, `data?.value` is undefined, raw={} so DEFAULTS apply); the save path throws at capabilityPolicy.ts:234 `if (error) throw new Error(error.message)`; and org_capability_allows() — whose body is only planned at execution time — raises `column \"value\" does not exist` on every call, which is exactly what gates the document_holds INSERT/UPDATE policies (20260901:94,99,101) and the checkout force-release trigger (:115). CRITICAL is correct."
  },
  {
    "id": "ALOG-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260813_acl_close_gaps_and_audit_scope.sql:86-90 `WITH CHECK (user_id = auth.uid() AND (org_id IS NULL OR org_id IN (SELECT my_org_ids())))` explicitly admits NULL; supabase/schema.sql:1084-1085 `FOR SELECT USING (org_id IN (SELECT my_org_ids()))` — `NULL IN (...)` is never true, so such a row is unreadable by any authenticated query. Both viewers narrow further: app/(protected)/admin/audit/page.tsx:129 `.eq(\"org_id\", activeOrgId)` and app/(protected)/activity/page.tsx:103 `.eq(\"org_id\", activeOrgId)`. The optional-orgId signature is real: lib/audit.ts:6 `orgId?: string;` and :22 `org_id: entry.orgId || null`, reached through lib/documentOrigin.ts:26-43 whose input declares `orgId?: string | null` and passes `orgId: input.orgId ?? undefined`.",
    "note": "The write-only-sink mechanism is exactly as described. Worth flagging as the finding itself concedes: the one live caller does supply it — components/documents/OriginSection.tsx:48 passes `orgId` — so today this is a latent trap in the signature rather than an active data-loss bug, which is why MEDIUM (not higher) is the right level."
  },
  {
    "id": "ALOG-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/admin/audit/page.tsx:46-83 `ACTION_STYLE` has exactly 36 keys, and :271-281 builds the entire action dropdown from it: `...Object.keys(ACTION_STYLE).sort().map((k) => ({ value: k, label: prettyAction(k) }))`, applied server-side at :137 `if (actionFilter !== \"ALL\") q = q.eq(\"action\", actionFilter)`. The KPI at :192 is `const deletes = [...byAction.entries()].filter(([k]) => /DELET|ARCHIVE|REVERS|FORCE/.test(k)).reduce(...)`.",
    "note": "The counts are exact, not rhetorical: a scan of every action-name literal in the tree (`action:` plus the `type:` unions that lib/audit.ts's helpers forward) yields precisely 110 distinct actions against 36 filterable. The unreachable set includes CAPABILITY_POLICY_CHANGED (capabilityPolicy.ts:239), NODE_ACL_CHANGED (PermissionDrawer.tsx:292), ACCESS_RECERTIFIED, DATA_PURGE, DATA_RESTORE, ESIGNATURE_CAPTURED, FOLDER_DELETED and STORAGE_ORPHANS_PURGED. The KPI regex genuinely errs both ways — it misses DATA_PURGE (a real destruction) while counting TICKET_ARCHIVE_RESTORE and ARCHIVE_PRODUCE_CANCELED (a restore and a cancel) because both contain \"ARCHIVE\"."
  },
  {
    "id": "ALOG-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/permissions/CapabilityPolicyEditor.tsx:35-43 `load` runs once per mount (`useEffect(() => { void load(); }, [load])`) and seeds both `policy` and `baseline`; :81-83 saves the whole grid from that snapshot — `const stored = await loadCapabilityPolicy(activeOrgId); await saveCapabilityPolicy({ orgId, policy: { caps: policy, grants: stored.grants ?? [] }, ... })`. lib/capabilityPolicy.ts:159-160 `const CACHE_TTL_MS = 60_000;` and :170 `if (hit && Date.now() - hit.at < CACHE_TTL_MS) return hit.policy;` — so that re-read can be up to a minute stale. saveCapabilityPolicy:227 `const before = await loadCapabilityPolicy(input.orgId)` goes through the same cache, and :228-233 is a bare `.upsert(..., { onConflict: \"org_id,key\" })` with no version or updated_at precondition. addUserGrant:263-275 and revokeUserGrant:286-293 do the mirror-image read-modify-write, writing back `caps: current.caps ?? {}` from the cached read.",
    "note": "Every limb verified: last-writer-wins on the full `caps` object in both directions (grid save clobbers grants, grant save clobbers caps), and the audit `before` at :245 is whatever the 60-second cache held, which need never have been the row's actual prior state. Note this is currently masked by ALOG-1 — nothing persists at all — but the concurrency defect is independent of it."
  },
  {
    "id": "ALOG-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/admin/permissions/page.tsx:13-16 asserts \"The old read/write/admin role matrix is GONE: it wrote columns (libraries.read_access/write_access/admin_access) that no policy, trigger, or query ever read ... Confirmed safe to remove: no org data depended on it.\" That is falsified by app/(protected)/documents/page.tsx:45-51 `function computeCanRead(lib, role) { const readAccess = lib.readAccess; if (readAccess === \"ALL\") return true; ... return readList.includes(role) || visibleTo.includes(role); }` and :166 `const visible = isController ? libs : libs.filter((l) => l._canRead);`. The columns are still written on every save: admin/libraries/page.tsx:105-107 `write_access: config.writeAccess ?? [], admin_access: config.adminAccess ?? [], read_access: config.readAccess ?? \"ALL\", visible_to: config.visibleTo ?? []`, fed by LibraryWizard.tsx:269-271, and again by lib/libraryCollections.ts:129-131. supabase/schema.sql:75-77 still declares all three.",
    "note": "The console's absence claim is wrong: `read_access` (with `visible_to`) is read on every load of the documents home page and does gate which library cards a non-controller sees. The finding's 'two of them' is loose — the second column doing the filtering is `visible_to`, which is not one of the three the console names — but that does not weaken it, because the genuinely inert pair is `write_access`/`admin_access`, which /admin/libraries and LibraryWizard still present as upload/admin restrictions and no enforcement path ever consults. Both halves of the trap are therefore live."
  },
  {
    "id": "ALOG-14",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/permissions/PermissionsExplorer.tsx:14 fixes the columns as `[\"Admin\",\"DocCtrl\",\"Manager\",\"Supervisor\",\"DraftingSup\",\"Engineer 1-4\",\"Drafter\",\"Requester\",\"Staff*\",\"Contractor\",\"Auditor\",\"Viewer\"]` and :19-80 is a hand-written `m:` string per row, rendered verbatim by app/(protected)/admin/permissions/page.tsx:148 `<PermissionsExplorer />`. Six rows contradict the code: (1) `{ cap: \"Audit log\", m: \"yyyy------y-\" }` vs the ungated app/(protected)/activity/page.tsx:99-117 that renders the same audit_logs table to every role; (2) `{ cap: \"User management (invite, roles)\", m: \"y-y---------\" }` — DocCtrl marked '—' and Manager marked '✓' — vs app/api/admin/create-user/route.ts:62 `if (!callerMember || ![\"Admin\", \"DocCtrl\"].includes(callerMember.role as string)) return ... 403`, which admits DocCtrl and rejects Manager; (3) `{ cap: \"Release stale checkouts (/admin/holds)\", m: \"y-yy--------\" }` vs admin/holds/page.tsx:32 `const ADMIN_ROLES = new Set([\"Admin\",\"Manager\",\"Supervisor\",\"DocCtrl\"])`; (4) `{ cap: \"Operational scope\", m: \"y-yy--------\" }` vs admin/scope/page.tsx:36 with the same four-role set including DocCtrl; (5) `{ cap: \"Equipment / asset admin pages\", m: \"y-yy--------\" }` vs admin/assets/page.tsx:56 `const ADMIN_ROLES = [\"Admin\",\"DocCtrl\",\"Manager\",\"Supervisor\"]`; (6) `{ cap: \"Access recertification reviews\", m: \"yycccccccccc\", cond: \"If library owner\" }` vs documents/[libraryId]/page.tsx:3372-3379 where the entry is `{isController && ...}` — Admin/DocCtrl only, the owner never sees it.",
    "note": "Right, and the drift runs in both directions — DocCtrl is understated on four rows while Manager is overstated on user management. Only the row count is off: the array holds 51 rows, not 52, which is immaterial to the claim. The one hedge in the file, the `warn` field flagging known gaps, is used on exactly one row (\"Create / edit / refresh work packages\"), so none of the six drifted rows carries any warning."
  },
  {
    "id": "ALOG-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/accessRecert.ts:106-112 — `await supabase.from(\"libraries\").update({ last_recertified_at: now, ... }).eq(\"id\", input.libraryId);` then `await supabase.from(\"access_recertification_events\").insert({...});` then `await logAuditAction({...}).catch(() => {});` — no destructuring of `error` on either write, and the function still `return { grantCount, nextDate }`. components/documents/AccessRecertModal.tsx:64-66 wraps it in `try { await recertifyAccess(...); ... } finally { setBusy(false); }` — no catch, no error state. RLS: supabase/migrations/20260821_access_recert.sql:39-44 `CREATE POLICY \"access_recert_events_member\" ON access_recertification_events FOR ALL USING (EXISTS (... status = 'active'))`, duplicated by 20260819_orphan_tables_backfill.sql:223-238 which builds `<table>_member_all ... FOR ALL TO authenticated` over the same table.",
    "note": "All three limbs hold. `FOR ALL` with a plain active-member predicate means any member — Viewer, Contractor, the departed contractor being reviewed — can INSERT a forged attestation or DELETE a real one; there is no reviewer restriction at the data layer and no UPDATE/DELETE-blocking policy. The UI entry point is gated (page.tsx:3372 `{isController && ...}`, isControllerRole = Admin|DocCtrl only, permissions.ts:18-20), which notably excludes the library *owner* the module header names as a reviewer — so the gate is both wrong-tight in the UI and wrong-loose in the database."
  },
  {
    "id": "ALOG-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/admin/libraries/page.tsx:147-162 `confirmDelete` is `await supabase.from(\"libraries\").delete().eq(\"id\", libraryToDelete.id!)` followed only by local state pruning — no audit write. Cascades: supabase/schema.sql:92 `library_id UUID NOT NULL REFERENCES libraries(id) ON DELETE CASCADE` (collections), :117 same on document_sets, :133 same on documents, :322 `record_id UUID NOT NULL REFERENCES documents(id) ON DELETE CASCADE` (document_versions). DeleteSafetyModal.tsx:47-52 renders only \"permanently delete the {libraryName} library configuration. Documents inside this library may become orphaned or inaccessible if not migrated first.\"",
    "note": "Confirmed by absence too: grep across the repo finds no LIBRARY_DELETED/LIBRARY_CREATED/LIBRARY_UPDATED audit action and no trigger on `libraries` (the only migrations touching it add columns and indexes). The modal takes no count props at all — its whole prop surface is {isOpen,onClose,onConfirm,libraryName,isLoading} — so it structurally cannot preview what is destroyed, and 'may become orphaned' actively understates a two-level ON DELETE CASCADE. The legal-hold triggers in 20260826_legal_hold_delete_guard.sql fire per-row on documents/document_versions, so they stop only flagged rows."
  },
  {
    "id": "ALOG-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/admin/users/page.tsx:123-156 `persistRoles` — `await supabase.from('org_members').update({ roles: cleaned, role: headline }).eq('id', member.id)` with only optimistic-UI and revert handling; :163-186 `handleRemoveMember` — `await supabase.from('org_members').delete().eq('id', member.id)`; app/api/admin/create-user/route.ts:127-170 — the insert/update of org_members and the users upsert end at `return NextResponse.json({ uid: userId })` with no audit call anywhere in the file. app/(protected)/admin/settings/page.tsx:103-118 `saveNumbering` updates `orgs` with no audit; app/(protected)/admin/libraries/page.tsx:110-129 create/update of libraries likewise.",
    "note": "The absence claim checks out repo-wide: a grep for ROLE_GRANT/ROLE_CHANGE/MEMBER_ADDED/MEMBER_REMOVED/USER_CREATED-style actions returns nothing, and the only migrations touching org_members with triggers are 20260831's last-admin guards (trg_prevent_last_admin_update/_delete), which block but do not record. `addRole`/`removeRole` (:159-161) both funnel into persistRoles, so the whole grant/revoke surface is unrecorded."
  },
  {
    "id": "ALOG-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/activity/page.tsx:99-117 — the component destructures only `{ activeOrgId }` from useRole() and goes straight to `supabase.from(\"audit_logs\").select(\"*\").eq(\"org_id\", activeOrgId).order(\"timestamp\", {ascending:false}).limit(limit)`; there is no `activeRole`, no canRead, and no gate anywhere in the file (the directory holds only page.tsx and loading.tsx). Contrast app/(protected)/admin/audit/page.tsx:27 `const ADMIN_ROLES = new Set([\"Admin\",\"Manager\",\"Supervisor\",\"DocCtrl\",\"Auditor\"])`, :94 `const canRead = !!activeRole && ADMIN_ROLES.has(activeRole)`, :196-208 the \"Admin-class roles only\" refusal card. Both pages sit in the same strip: ViewTabs.tsx:99-102 `ACTIVITY_VIEWS = [{ Activity, /activity }, { Audit log, /admin/audit }]`. Backstop RLS is permissive: schema.sql:1084-1085 `CREATE POLICY \"audit_logs_org_access\" ON audit_logs FOR SELECT USING (org_id IN (SELECT my_org_ids()))`.",
    "note": "The only role filtering is cosmetic and does not cover the scenario: Sidebar.tsx:239 lists Activity in `workAll`, and :245-249 strips it only for `activeRole === 'Viewer' || activeRole === 'Contractor'` — a Maintenance, Operations, Safety, HR, Accounting or Drafter member keeps the link, and even Viewer/Contractor can reach /activity by URL since nothing server-side or in RLS stops the read."
  },
  {
    "id": "ALOG-6",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "app/(protected)/admin/audit/page.tsx:107-108 `const [range, setRange] = useState<\"24h\"|\"7d\"|\"30d\"|\"all\">(\"7d\"); const [limit, setLimit] = useState(200);`, :131 `.limit(limit)`, :226-234 `onClick={() => exportAuditCsv(filtered, docMeta)}`, and :436 `const header = [\"Timestamp\",\"Action\",\"Resource Type\",\"Resource ID\",\"Resource Label\",\"User Email\",\"User Role\",\"Details JSON\"];` — no Metadata column, even though the row type carries it (:39, :143).",
    "note": "The scope half is true — the export serialises the in-memory `filtered` array only. But two mitigations the finding omits lower it: the button carries `title=\"Download the currently-visible audit rows as a CSV\"` (:232) and the toolbar shows `Showing {filtered.length} of {rows.length}` beside a `Load 200 more` control (:294-297), so the truncation is disclosed in the UI even if not inside the file. More importantly the `metadata` half is vacuous: no writer in the repo ever populates it — lib/audit.ts:27 `metadata: entry.metadata || null` is the only assignment, zero callers of logAuditAction pass a `metadata` key, and none of the ~30 direct `from(\"audit_logs\").insert(...)` sites set one — so the column is uniformly NULL and dropping it loses nothing. LOW."
  },
  {
    "id": "ALOG-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/audit.ts:5-14 declares `userEmail?`, `userRole?` and `timestamp?: string`; lib/audit.ts:17-31 writes `user_email: entry.userEmail || null, user_role: entry.userRole || null` and contains no `timestamp:` key at all — the declared field is silently dropped. supabase/schema.sql:781-793 `timestamp TIMESTAMPTZ DEFAULT NOW()` — a default, not a trigger. supabase/migrations/20260813_acl_close_gaps_and_audit_scope.sql:84-90 constrains only `user_id = auth.uid() AND (org_id IS NULL OR org_id IN (SELECT my_org_ids()))`, leaving user_email, user_role and timestamp entirely caller-supplied.",
    "note": "Confirmed on every limb. The display side makes the forgery consequential: app/(protected)/admin/audit/page.tsx:403 renders `{row.userEmail || row.userId}{row.userRole ? ` (${row.userRole})` : \"\"}` — it prefers the self-declared email over the RLS-pinned user_id — and app/(protected)/admin/holds/page.tsx:94-98 shows the pattern of the caller handing its own `userEmail`/`activeRole` in as the recorded identity. Because nothing forces `timestamp`, a hand-rolled insert through the same permissive INSERT policy can backdate or postdate a row."
  },
  {
    "id": "ALOG-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "All nine sites discard the outcome. Service-role routes wrap the insert in `try { await sb.from(\"audit_logs\").insert({...}); } catch { /* best-effort */ }` — purge/route.ts:173-184, restore/apply/route.ts:129-139, shed/commit/route.ts:113-120, ticket-shed/commit/route.ts:197-204, ticket-shed/restore/route.ts:210-217, archive-cancel/route.ts:64-71 — and each then returns `NextResponse.json({ ok: true, ... })`. orphans/route.ts:48-53 and lib/capabilityPolicy.ts:238-246 use `.then(() => undefined, () => undefined)`. components/permissions/PermissionDrawer.tsx:291-302 is the same `.then(() => undefined, () => undefined)`, sitting immediately after the ACL write at :283-284 which *is* checked (`if (error) throw new Error(error.message)`).",
    "note": "Worse than stated: supabase-js resolves rather than rejects on a database error, so the `try/catch` at the seven route sites cannot catch an RLS or constraint rejection either — the error object is simply never destructured. The PermissionDrawer path also supplies its own failure mode: `user_id: auth.user?.id ?? null` (:295) writes NULL when the session lookup returns nothing, which the 20260813 policy's `user_id = auth.uid()` check rejects — silently, after the ACL change has already committed."
  },
  {
    "id": "ALOG-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/permissions/CapabilityPolicyEditor.tsx:24 `const TOKENS = [\"*\",\"Admin\",\"DocCtrl\",\"Manager\",\"Supervisor\",\"DraftingSupervisor\",\"Engineer\",\"Drafter\",\"Requester\",\"Viewer\",\"Contractor\",\"Auditor\"];` — 12 entries, iterated as the grid's columns at :145 `{TOKENS.map((t) => { const on = (policy[d.id] ?? []).includes(t); ...`. types/schema.ts:5-46 lists 19 real roles in `Role`/`ALL_ROLES`; the five with no column are exactly Accounting, Safety, HR, Maintenance, Operations.",
    "note": "The arithmetic is exact — 19 roles, 12 tokens (one of which is the `*` wildcard, not a role), Engineer-1..4 collapsed to one `Engineer` token by design (roleTokenMatches, capabilityPolicy.ts:130), leaving the five named roles with no cell. lib/capabilityPolicy.ts:86-88 confirms holds.open/holds.release default to `[\"*\"]`, so an admin who narrows them from Everyone has no way to re-add Operations or Safety through this grid; the only escape hatches are a per-person grant or hand-editing the JSON. MEDIUM is right."
  },
  {
    "id": "AREA-1",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "HIGH",
    "evidence": "20260605_rls_policies_new_tables.sql:25-29 `ALTER TABLE assets ENABLE ROW LEVEL SECURITY; ... CREATE POLICY \"assets_member_all\" ON assets FOR ALL TO authenticated USING (EXISTS (SELECT 1 FROM org_members WHERE org_id = assets.org_id AND uid = auth.uid() AND status = 'active')) WITH CHECK (same)` — the only policy on assets in the repo. lib/assets.ts:172-220 `createAsset`/`updateAsset`/`deleteAsset` all use the browser client (`supabase.from(\"assets\").delete().eq(\"id\", id)`), and no API route writes assets (only graph/shape, projects/checklist, flows/read, knowledge/ask read them). admin/assets/page.tsx:56 `const ADMIN_ROLES = [\"Admin\", \"DocCtrl\", \"Manager\", \"Supervisor\"];` and :348-352 render \"Only Admin / Doc Control / Manager / Supervisor roles can create or edit assets\" — a claim enforced only by React.",
    "note": "The claim is correct in every particular; the banner promises an authority boundary that the database does not hold. Severity is one notch high: the migration's own header states the design (\"Role-based authorization ... is handled in application code, not RLS — RLS just prevents cross-tenant data access\") and the identical FOR-ALL pattern is app-wide (schema.sql:1080 `tickets_org_access ON tickets FOR ALL`, document_assets, asset_photos, asset_types), so this is intra-tenant privilege escalation by an authenticated active member with no tenant-isolation break — HIGH, not CRITICAL."
  },
  {
    "id": "AREA-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Repo-wide grep for `unit_id` finds NO writer for assets.unit_id — lib/assets.ts:36 declares `unit_id: string | null;` and lib/orgGraph.ts:256 reads it (`if (a.unit_id) addEdge(\"asset:\"+a.id, \"unit:\"+a.unit_id, \"unit\")`), but no insert/update anywhere sets it. documents.unit_id (orgGraph.ts:259) has exactly one writer, lib/documentLifecycle/common.ts:181 `unit_id: input.unitId ?? null`, reached only from split.ts:114 `unitId: copyScope ? (source.unitId ?? null) : null` and merge.ts:123 `scopeIfAllAgree(sources, \"unitId\")` — pure copy-forward, so it can never become non-null from nothing.",
    "note": "Both unit models are live: migration 20260606:52-70 creates `units` (fed by /admin/scope) while codebook units get their own `cbunit:<code>` nodes at orgGraph.ts:196-201. Also confirmed the graph's connect path treats them as interchangeable — graph/page.tsx:319-322 `n.id.startsWith(\"unit:\") ? (n.sub ?? null)` resolves a legacy unit row to its free-text `code` column and writes that string as a process-flow unit ref, which is a different namespace from the codebook code the flow panel matches on."
  },
  {
    "id": "AREA-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "admin/assets/page.tsx:1310-1314 `useEffect(() => { if (asset?.code) return; // existing explicit code: never overwrite silently` — changing the unit on an existing coded asset never recomputes the site code; :1382-1384 then writes `unit_code: unitCode || null, code: siteCode.trim() || null` as independent fields with no cross-validation. Automated writers are all fill-blanks-only: equipmentBridgeServer.ts:243-253 selects `.is(\"unit_code\", null)` and only sets `patch.code` `if (!blank.get(id) && s.code)`; assetCategorize.ts:51-56 `if (!a.unit_code && a.code)`; page.tsx:1014-1017 `code: r.asset.code ?? tagToCode(...)`.",
    "note": "Confirmed — no reconciliation, no warning, no validation anywhere. Mild mitigation worth noting: the two fields sit side by side in the same grid (page.tsx:1498-1512, 'Operating unit' select next to a free-text 'Site code' input), so the contradiction is at least visible to whoever is editing; it is simply never enforced or flagged."
  },
  {
    "id": "AREA-2",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "20260609_phase1_normalization.sql:186-190 `CREATE POLICY \"document_assets_member_all\" ON document_assets FOR ALL TO authenticated USING (EXISTS (... org_members ... status = 'active')) WITH CHECK (same)`. graph/page.tsx:356-362 inserts links straight from the client, and the page never reads a role — `const { activeOrgId, uid, userEmail } = useRole();` at :60 destructures no `activeRole`, and the Connect affordance at :871-873 is gated only on node kind (`[\"doc:\",\"asset:\",\"cbunit:\",\"unit:\"].some(pre => selected.id.startsWith(pre))`). admin/assets/page.tsx:1353 `await supabase.from(\"document_assets\").delete().eq(\"document_id\", documentId).eq(\"asset_id\", asset.id)`.",
    "note": "Confirmed — and worse than stated in one respect: the graph's Connect button has no role check at all, so link creation by a Contractor needs no console. Severity trimmed to MEDIUM because most of this table is machine-regenerable: the `documents_resync_assets` trigger (same migration, :85-119) rebuilds every `source='jsonb_sync'` row on any touch of `documents.asset_tags`, and `assets_backfill_documents` (:127-140) re-links on asset insert; only `source='manual'` rows are unrecoverable, so \"permanently delete every link\" overstates the blast radius."
  },
  {
    "id": "AREA-3",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "First half confirmed: 20261017_process_flows.sql:50-56 `CREATE POLICY process_flows_insert ON process_flows FOR INSERT WITH CHECK (EXISTS (... org_members ... status = 'active') AND created_by = auth.uid())` — no controller test on INSERT (unlike UPDATE/DELETE at :57-66, which require `is_org_controller(org_id) OR created_by = auth.uid()`), and lib/processFlows.ts:52-70 `createManualFlow` hardcodes `status: \"confirmed\", origin: \"manual\"` from the browser client, reached from an ungated graph Connect. Second half refuted: flows/read/route.ts:86-88 builds `settled` as `` `${f.from_kind}:${f.from_ref}>${f.to_kind}:${f.to_ref}` `` and :148-150 skips on `` `${from.kind}:${from.id}>${to.kind}:${to.id}` `` — the key is DIRECTIONAL.",
    "note": "Any active member really can write a CONFIRMED flow, and the Connect UI really has no role gate — that stands. But \"every such row permanently blinds the AI PFD reader to that pair\" is false as written: because the settled key encodes direction, a backwards hand-drawn flow (V-102→E-101) does not suppress the model's correct reading (E-101→V-102), which is still proposed for review; suppression only occurs for the identical ordered pair, where re-proposing an already-recorded flow would be pointless. The finding's headline harm scenario therefore does not occur."
  },
  {
    "id": "AREA-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/equipmentBridgeServer.ts:188-193 `const bridge = await loadBridgeConfig(admin, String(doc.library_id ?? \"\")); const targetKey = bridge?.targetColumnKey; if (!targetKey) { throw new Error(\"No equipment column mapped for this library — pick one in the sweep dialog first.\"); }` — this throw precedes the entire discovery block at :198-234 that inserts assets. The automatic path is gated identically at :128 `if (bridge?.autoApply && bridge.targetColumnKey)` inside a `try { } catch { /* stays pending ... */ }`. The config has no default: 20260928_site_codebook.sql:114 `ALTER TABLE libraries ADD COLUMN IF NOT EXISTS equipment_bridge JSONB;` (null), written only by the sweep dialog (EquipmentSweepModal.tsx:157 `<select value={bridge.targetColumnKey ?? \"\"} ... <option value=\"\">— pick a column —</option>`) via /api/equipment-bridge:102. That modal is mounted in exactly one place: documents/[libraryId]/page.tsx:4685.",
    "note": "Confirmed end to end, including the unreachability: AreaKnowledgePanel's four-step order-of-operations (:314-350) links to /documents, /plot-plans and the knowledge wizard, and its Step 4 is \"Link equipment files to assets — Open any asset below\" — it never mentions the equipment column mapping, and nothing on /admin/assets renders or links to EquipmentSweepModal. A P&ID bound to an area creates zero assets until someone finds the sweep dialog on the library page."
  },
  {
    "id": "AREA-5",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "flows/read/route.ts:57-71: `supabaseAdmin.from(\"assets\").select(\"id, tag\").eq(\"org_id\", orgId).eq(\"archived\", false).limit(4000)` — no `.order()` — then `assets.slice(0, 300).forEach((a, i) => roster.push({ ref: `A${i+1}`, ... }))`. Units are NOT capped: `units.forEach((u, i) => roster.push(...))`. The route body is `{ orgId?: string; knowledgeDocumentId?: string; pages?: number[] }` (:30) — no unit parameter. UnitOpsPanels.tsx:250-252 mounts `<ReadFlowsModal orgId={orgId} onClose={...} onDone={...} />` with no unitCode, and :389 posts `body: JSON.stringify({ orgId, knowledgeDocumentId: kdocId, ...(pages.length > 0 ? { pages } : {}) })` — even though the panel itself receives `unitCode` at :105 and filters its display by it at :127-128.",
    "note": "Both halves verified, including that the launching panel demonstrably knows the unit and drops it. Severity trimmed to MEDIUM: the failure mode is omission, not fabrication — the roster is the grounding allowlist, so an unreachable asset yields no flow rather than a wrong one, everything the reader does emit lands as `status: \"proposed\"` for human accept/dismiss (route.ts:158, UnitOpsPanels.tsx:163), and orgs at or below 300 assets are unaffected."
  },
  {
    "id": "AREA-6",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "graph/page.tsx:226-237 handles namespaced focus ids — `const id = focusParam.includes(\":\") ? focusParam : `doc:${focusParam}`; const node = view.nodes.find(n => n.id === id); if (node) { setSelected(node); setHighlight({ ids: [id], nonce: Date.now() }); }` — selection only, no view scoping. orgGraph.ts:198-202 mints the `cbunit:${u.code}` nodes it would match. Repo-wide grep for `/graph?focus=` returns exactly two call sites: RelationshipGraph.tsx:100 (`doc`) and admin/assets/page.tsx:1602 (`asset:${asset.id}`) — no `cbunit:` link anywhere. UnitOpsPanels.tsx:176-178 is the area's only graph affordance: `<Link href=\"/graph\" ...>Process lens →</Link>`, unparameterised.",
    "note": "The unit-scoped pivot genuinely does not exist and the focus handler only selects rather than scopes — both true. Two corrections: \"the deep link ... is used by nothing\" is imprecise, since admin/assets/page.tsx:1602 (a location the finding itself cites) uses the `asset:` form; and the impact is a missing navigation affordance with no correctness, security or data consequence, which is LOW rather than MEDIUM."
  },
  {
    "id": "AREA-7",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "Mechanisms confirmed: AssetCsvImportModal.tsx:22-27 `const CANONICAL_FIELDS = [{key:\"tag\"...},{key:\"description\"},{key:\"location\"},{key:\"type\"}]` and :129-133 `await createAsset({ orgId, tag, description, location, typeId, createdBy: actorUserId })` — no unitCode, no code. assetCategorize.ts:48-54 `if (!a.unit_code && a.code) { const decoded = codeToTag(a.code, book); ...}` — the unit-filing branch requires `code`, which CSV never sets, so `filedToUnits` is necessarily 0. codebook.ts:149-160 `tagToCode` opens `if (!unitCode) return null;` — it consumes a unit, it cannot derive one. BUT admin/assets/page.tsx:969-1085 is `UnassignedAssignPanel`, headed \"File this equipment under its operating area\", which per-asset and in bulk runs `await updateAsset(r.asset.id, { unit_code: r.unitCode, code: r.asset.code ?? tagToCode(r.asset.tag, r.unitCode, book) ?? null }, userId)` (:1014-1019), pre-selecting the unit from linked-drawing numbers via `parseDrawingNumber` (:986-999). It is rendered at :411.",
    "note": "Every named mechanism checks out, but the headline — \"a master equipment list can never be filed into operating areas\" — is false. The same page the finding cites ships a dedicated bulk unit-assignment panel that files imported assets and back-fills the site code, with drawing-derived suggestions. The residual defect is throughput and discoverability (the panel shows `assets.slice(0, 50)` at a time, so 3,000 rows means ~60 passes) plus the missing CSV column, which is LOW."
  },
  {
    "id": "AREA-8",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "AreaKnowledgePanel.tsx:191-196 `const knowledgeDone = !!status?.boundLibrary && status.sources.length > 0 && !needsReview;` / `const drawingsDone = status ? (status.boundLibrary ? status.counts.ready + status.counts.pending > 0 : status.suggestions.some((s) => s.docCount > 0)) : false;`; :332 `state={... (flowCount > 0 ? \"done\" : \"todo\")}`; :347 `state={... docLinkCount > 0 ? \"done\" : \"todo\"}`.",
    "note": "The thresholds are exactly as claimed — every step flips to the emerald tick at a count of 1 (step 2 too, at one watched source). One factual correction: step 1 is NOT satisfied by a folder name alone; suggestFoldersForUnit (lib/areaKnowledge.ts:56-63) name-matches, but drawingsDone additionally requires `s.docCount > 0`, and knowledge-status/route.ts:88-93 counts only AI-READABLE documents rolled up the folder ancestry — so at least one real readable drawing must be filed. Impact is a setup-checklist honesty/UX issue with no data loss or security consequence, so MEDIUM reads high; LOW fits."
  },
  {
    "id": "AREA-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/assets.ts:124-134 — `let q = supabase.from(\"assets\").select(\"*\").eq(\"org_id\", params.orgId); ... const { data, error } = await q.order(\"tag\", { ascending: true });` with no `.range()`/`.limit()`; the only two callers are admin/assets/page.tsx:115 and plot-plans/[id]/page.tsx:49. Every derived count reads that array: page.tsx:162-170 `unitCounts`, :216-235 `unitSummaries` (`count: inUnit.length`), :240-247 `unknownUnits`, :261-266 `areaAssetIds` (fed to AreaKnowledgePanel as `unitAssetIds`).",
    "note": "Confirmed, including the internal corroboration that a 1,000-row PostgREST ceiling is real in this deployment: AreaKnowledgePanel.tsx:117-119 comments 'Chunked over EVERY asset (no 1,000-asset display cap)'. The contrast with the setup navigator is also real — setup/page.tsx:57-59 uses `.select(\"id\", { count: \"exact\", head: true })`, which is unaffected by the row cap, so the two surfaces disagree. No pagination, no truncation flag, and no error anywhere."
  },
  {
    "id": "ASK-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/knowledge.ts:510-517 — `.from(\"knowledge_questions\").select(\"id, library_id, question, answer, user_name, created_at, citations\").eq(\"org_id\", orgId).textSearch(...)`: org scope only, no library filter, no ACL. Backed by migration 20260911_knowledge_ai.sql:147-150 `CREATE POLICY knowledge_questions_select ... USING (EXISTS (SELECT 1 FROM org_members WHERE org_id = knowledge_questions.org_id AND uid = auth.uid() AND status = 'active'))` — every active member. The stored payload is verbatim source text: ask/route.ts:1645 `quote: truncateSafe(c.content, 1600)`, persisted at :1739-1743.",
    "note": "Confirmed end to end. The republish is not theoretical: knowledge/[id]/page.tsx:1705-1708 rebuilds the answer object with `citations: (Array.isArray(pa.citations) ? pa.citations : []) as KnowledgeCitation[]`, and the renderer prints those quotes verbatim (page.tsx:678-690 `<blockquote>{c.quote}</blockquote>` plus a 'Copy quote' button). The route's own per-asker ACL filter (route.ts:157-187, `excludedDocIds`) governs retrieval only and has no counterpart on the memory path."
  },
  {
    "id": "ASK-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "grep for `semanticUsed` in ask/route.ts returns exactly three lines: `272: let semanticUsed = false;`, `568: semanticUsed = semantic.length > 0;`, `1774: retrieval: semanticUsed ? \"hybrid\" : \"keyword\",`. Round 2 at :701-706 computes `const [more, semantic2] = await Promise.all([...runSemantic(plan.queries.slice(0, 2))]);` and `const meaning = [...semantic, ...semantic2];`, which is fused into `chunks` at :707-718 — but `semanticUsed` is never reassigned.",
    "note": "Confirmed. Round-2 semantic hits can supply the passages that become citations while the response still reports `retrieval: \"keyword\"`. The module header (:14-19) and the response comment (:1770-1773) both call this flag the invariant that must never lie — the miss is exactly the case they describe, only in the honest-but-understated direction."
  },
  {
    "id": "ASK-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/knowledgeText.ts:622 `.map(({ chunk }) => ({ ...chunk, content: chunk.content.slice(0, maxChars) }));` (mergeRetrievedRRF) and :640 `.map((c) => ({ ...c, content: c.content.slice(0, maxChars) }));` (mergeRetrieved) — plain .slice, while truncateSafe at :603-606 exists precisely for this. app/api/knowledge/ask/route.ts:1643 `quote: truncateSafe(c.content, 1600),` is a no-op on an already-1600-char string (`if (s.length <= n) return s;`). Insert at route.ts:1739-1742 swallows the failure: `questionId = (r.data?.id as string | undefined) ?? null;` and the only retry is `if (r.error?.code === \"PGRST204\" || /mode|missing_docs|thread_id/.test(r.error?.message ?? \"\"))`, which a Postgres unicode-escape error never matches.",
    "note": "Confirmed end-to-end: lib/knowledgeIngest.ts:141/362 sanitize stored chunk text with sanitizeStorageText, which KEEPS valid surrogate PAIRS (knowledgeText.ts:22-24), so an astral-dense chunk reaches retrieval intact and the RRF slice is the first cut that can land mid-pair. Narrowing worth noting (does not refute): chunkProse caps prose chunks at target=1400 < 1600, so only TABLE chunks (chunkPageText allows up to target*2 = 2800, knowledgeText.ts:106-107) are long enough to be truncated at all — the bug needs an astral-dense table chunk. MEDIUM is right."
  },
  {
    "id": "ASK-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "ask/route.ts:945-954 `.from(\"knowledge_page_entities\").select(...).in(\"library_id\", allLibIds).in(\"kind\", TAG_ENTITY_KINDS ...).order(\"document_id\", { ascending: true }).limit(20000)` feeding :1056-1057 \"DRAWING FACTS — computed deterministically from EVERY sheet's extracted tags. TRUST these for counts and totals\" and :1066 \"next free ${c.prefix}-${c.nextNumber}\". lib/knowledgeEntityKinds.ts:33 `export const TAG_ENTITY_KINDS: readonly EntityKind[] = [\"equipment\", \"ref\", \"opc\", \"self\"];` — every kind ENTITY_KINDS:26 lists, so the filter excludes nothing today.",
    "note": "Confirmed, and the second half is the sharper point: the guard test (entityKindGuard.test.ts:46-66) asserts only `namesKinds = /\\.(in|eq)\\(\\s*[\"']kind[\"']/` on bulk reads — naming all four kinds passes it while providing zero headroom under the same 20,000 cap. Nothing anywhere compares `entRows.length` to the limit or sets a truncation flag, so a 20,000-row read is indistinguishable from a complete one and the census is presented as exact."
  },
  {
    "id": "ASK-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/ai/providerCall.ts:93-101 — `AiCallResult` is `{ text, webSources, liveWeb, usage }`: no stop_reason, no truncation flag. providerCall.ts:217-226 reads `data.stop_reason === \"max_tokens\"` ONLY inside `if (!text.trim())`, so a truncated-but-non-empty answer returns via :231 `return { text, webSources, liveWeb: !!webSearch, usage };` with the signal discarded. OpenAI's `finish_reason` is never read at all (:260-264 destructures only `choices[0].message.content` and `usage`). The answer call is ask/route.ts:1535-1542 `maxTokens: 4000` (and again at :1556-1563), and the result is persisted verbatim at :1739-1743.",
    "note": "Confirmed. The provider reports the truncation and the wrapper throws it away; the route then serves, stores in knowledge_questions, and exposes for thumbs-up rating an answer it cannot distinguish from a complete one — and via ASK-1's memory path that truncated answer is later replayed to teammates as the team's record."
  },
  {
    "id": "ASK-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "ask/route.ts:1369-1383 loads legend chunk text and :1390-1393 concatenates it into `standing` (`P&ID LEGEND / DECODER SHEETS (owner-attached — authoritative ...):\\n${legendBlock}`), which :1527 splices into `baseAnswerSystem` — the SYSTEM prompt. Same for `drawingFacts`, which at :1098 embeds raw extracted OPC text `box ${b.tag}: \"${(b.raw ?? \"\").slice(0, 60)}\"`. Retrieved passages go in the user turn at :1530 `const answerUser = ...PASSAGES:\\n\\n${passages}...`. The only text hygiene in the pipeline is lib/knowledgeText.ts:17-31 `sanitizeStorageText`, which drops lone surrogates and C0 control bytes — nothing else.",
    "note": "Confirmed, including the absence claim: a repo-wide case-insensitive grep across app/ and lib/ for 'injection', 'ignore previous', 'untrusted' returns zero hits. No delimiting, no instruction-stripping, no 'treat document text as data' directive anywhere in the ~150-line answer system prompt."
  },
  {
    "id": "ASK-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "ask/route.ts:89-94 takes `body.history` and only type-checks and truncates it (`.slice(-4).map((t) => ({ question: t.question.slice(0, 500), answer: t.answer.slice(0, 1200) }))`); :97-100 turns it into `CONVERSATION SO FAR (the question may refer back to it):` which reaches the model at :1530 and also steers query generation at :350-356. `threadId` (:95-96) is validated only as a UUID-shaped string — grep shows its sole other use is :1743 `thread_id: threadId` on the insert. Nothing reads knowledge_questions to verify the claimed turns exist.",
    "note": "Confirmed: no server-side reconstruction of the thread, no ownership check on threadId, and the resulting answer lands in the org-wide record (:1739-1743) that lib/knowledge.ts:504-527 later replays to every member. A client can fabricate an authoritative-sounding prior turn and have the poisoned answer persisted under a real user's name."
  },
  {
    "id": "ASK-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/knowledge.ts:491-494 `parseNeedPrompt` lifts everything after `**Need:**` straight out of model text; knowledge/[id]/page.tsx:1759-1761 passes it to `<NeedCard prompt={need.prompt} …>`, which at :596-597 renders the app's own chrome line 'I need a value from you to run this calculation' directly above `<p …>{prompt}</p>` and a free-text `<Textarea>` (:600-606). Clarify is the same shape: route.ts:686-693 returns `plan.clarify` verbatim and page.tsx:1753-1756 → ClarifyCard :557-566 renders `{prompt}` and maps `options` to `<button>`s. knowledgeText.ts:230-237 validates only that the question is a non-empty ≤300-char string and there are ≥2 ≤60-char options.",
    "note": "Confirmed. Neither card marks its content as model-authored, and both are reachable from document text given ASK-4 — the calc protocol at route.ts:1442-1450 explicitly instructs the model to emit a bare `**Need:** …` line, which is exactly the channel an injected instruction would use."
  },
  {
    "id": "ASK-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "ask/route.ts:253-263 is the only cap check and it runs before any provider call (`if (capUsd > 0 && monthSoFar.spentUsd >= capUsd) return bad(...)`); spend is recorded only afterwards via `meter()` (:279-280). Input assembly has no token accounting: :818-850 whole-document mode admits `WHOLE_DOC_CHAR_BUDGET = 170_000` characters (~42k tokens, per its own comment) on top of 14 governing + 8 reference chunks, plus :605-635 proven-ground pushes, :759-788 pull-by-name, :903-919 graph hops, a 6,000-char legend block (:1375), drawingFacts, and up to MAX_DEEP_READ_PAGES = 6 page images.",
    "note": "Substance confirmed, with one wording caveat: the answer call does carry a token budget — `maxTokens: 4000` at :1538 — it is the INPUT that is unbounded, which is what the summary actually describes. The cap bypass is real and worse than 'a single ask': because the check is read-then-call with no reservation, concurrent asks all pass the same pre-flight."
  },
  {
    "id": "ASK-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "ask/route.ts:1369-1374 — `await supabaseAdmin.from(\"knowledge_chunks\").select(\"document_id, page, content\").in(\"document_id\", usable).order(\"page\", { ascending: true }).limit(40)`. No `.eq(\"org_id\", orgId)`, no `.in(\"library_id\", …)`, and supabaseAdmin is the service role, so RLS does not backstop it. `usable` is only `legendDocIds.filter((id) => !excludedDocIds.has(id))` (:1367), and excludedDocIds is built solely from mirrors inside THIS org's library set (:163-187) — a foreign id passes through untouched. Up to 6,000 characters (:1375 `let budgetLeft = 6000;`) then land in the SYSTEM prompt via :1390-1393 and :1527.",
    "note": "Confirmed as stated. Both id sources are themselves org-scoped today (library.ai_features on an org-checked row at :110-114; codebookServer.ts:31 `legend_doc_ids` from `codebook_config … .eq(\"org_id\", orgId)`), so reaching it needs a bad id written into config rather than an unauthenticated request — but the read itself has no tenant scope, which is out of line with every other read in this route."
  },
  {
    "id": "ASK-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Keyword half, ask/route.ts:409-411: `const limit = (lib.tier === \"governing\" ? 10 : 6) * 3;` with the comment 'Over-fetch 3× the slot count: AI-excluded documents are filtered HERE, after the database already applied its LIMIT', then :431-432 `.filter((c) => !excludedDocIds.has(c.document_id)).slice(0, lib.tier === \"governing\" ? 10 : 6)`. Semantic half, :483-489: `supabaseAdmin.rpc(\"semantic_search\", { … p_limit: lib.tier === \"governing\" ? 12 : 6, p_model: corpusModel })` — no multiplier — with the identical post-hoc filter at :496 `if (excludedDocIds.has(row.document_id)) continue;`.",
    "note": "Confirmed: the exact fix documented on the keyword path was never applied to the vector path, so an ACL-restricted asker's meaning list is silently thinned before it reaches the fusion at :544-547 — and reciprocal-rank fusion over a 2-item list against a full keyword list gives the meaning half almost no influence."
  },
  {
    "id": "AUTHZ-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/workflow.ts:200-228: inside `if (canActAsRequester)`, the `needsEngineerApproval && !isEng` branch pushes only 'Send for Engineer Final Approval', and then at line 222 — unconditionally, outside the if/else — `actions.push({ label: 'Approve with Minor Correction', action: 'approve_minor_correction', requiresComment: true })` with the comment 'Available to every requester tier by design.' lib/ticketTransitions.ts:230-234 `case \"approve_minor_correction\": updates.status = \"PENDING_IFC\"; updates.deliverable_rev = issuedRevLabel(ticket.revisionCount);` — byte-identical outcome to approve_draft_ifc (221-224). The server gate at app/api/tickets/workflow-action/route.ts:96-103 validates against `WorkflowEngine.getActions(...)`, i.e. against the same list that offers the button, so it authorizes the bypass rather than blocking it.",
    "note": "Right as written. The only distinguishing artifact is the history label 'Approved with minor correction — issued Rev N'; the status, the issued rev, and the notification classification (ticketTransitions.ts:344-347 groups it with the approvals) are identical, so the engineer gate is one click wide for exactly the users it targets."
  },
  {
    "id": "AUTHZ-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/admin/requests/page.tsx:65 `if (activeRole && !['Admin', 'DocCtrl'].includes(activeRole)) router.push('/dashboard');` vs supabase/migrations/20260818_followups_rls.sql:23-31 `is_org_assign_drafters` = `role IN ('Admin','Manager','Supervisor','DraftingSupervisor') OR roles && ARRAY[...]` and :72-82 `WHEN 'drafting' THEN is_org_assign_drafters(org_id)` on INSERT/UPDATE. lib/roleCapabilities.ts:53 `DocCtrl: [\"doc_control\", \"manage_org_config\", \"view_requests\", \"create_requests\"]` — no assign_drafters.",
    "note": "Confirmed in both directions: DocCtrl reaches the page but is denied by the RLS restrictive policy on the upsert (page.tsx:97-100 upsert, caught by the generic `catch` that only shows \"Failed to save configuration.\"), while Manager/Supervisor/DraftingSupervisor satisfy the DB policy but are redirected out of the page. No other writer of key='drafting' exists (only lib/ticketRouting.ts:49 reads it), so there is no alternate path that rescues DocCtrl."
  },
  {
    "id": "AUTHZ-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/tickets/workflow-action/route.ts:36-37 declares `redlineAttachment?: TicketAttachment | null; finalAttachment?: TicketAttachment | null;` and :143-144 passes them through untouched: `redlineAttachment: body.redlineAttachment ?? undefined, finalAttachment: body.finalAttachment ?? undefined`; lib/ticketTransitions.ts:171 `if (input.redlineAttachment) currentAttachments = [...currentAttachments, input.redlineAttachment];` and :281 `if (input.finalAttachment) currentAttachments = [...currentAttachments, input.finalAttachment];`, then :293 `updates.attachments = currentAttachments;`.",
    "note": "No guard anywhere in the route validates the attachment's `url` key against the ticket/org, nor overrides `uploadedBy` with the authenticated actor — the route validates the ACTION and the referenced people (lines 112-131) but never the attachment object. app/api/storage/download-url/route.ts:35-46 would block signing a key under a different org's prefix, so the bytes of a cross-org key are not readable, but the forged record (filename, someone else's `uploadedBy`, another ticket's key) is written to the ticket and displayed as the IFC package."
  },
  {
    "id": "AUTHZ-12",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "app/api/verify-ticket/route.ts:50-52 `.select(\"id, ticket_id, title, unit, status, deliverable_rev, revision_count, last_modified\").eq(\"id\", ticketId)` with no `archived_at` filter, and :91-103 returns `unit: t.unit ?? null, ... ticketStatus: t.status ?? null, lastActivityAt: t.last_modified ?? null`. The handler is unauthenticated (no auth header read anywhere in the file) and no rate limiter exists — there is no middleware.ts in the repo and no lib/rateLimit* module.",
    "note": "Every literal sub-claim checks out: internal workflow status, unit and last-activity timestamp are returned forever to any holder of the ticket UUID, unrated and with no archived/retired check (unlike app/api/verify/route.ts:88 which computes `docRetired` from status). Severity lowered to LOW: exposure is a handful of low-sensitivity fields for one ticket, gated behind an unguessable UUID that the org itself printed on a deliverable it handed to that party — no files, URLs, or people are returned."
  },
  {
    "id": "AUTHZ-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/schema.sql:1080-1081 `CREATE POLICY \"tickets_org_access\" ON tickets FOR ALL USING (org_id IN (SELECT my_org_ids()));` — the only policy on `tickets` in the entire repo (grep for \"ON tickets\" across supabase/ returns only indexes and the search_tsv trigger). app/(protected)/requests/page.tsx:279-299 picks the scope client-side (`.eq('requester_id', uid)` for everyone else), and app/(protected)/requests/[id]/page.tsx:913 `supabase.from('tickets').select('*').eq('id', ticketId).single()` with only a client-side org comparison at :917.",
    "note": "Confirmed: no restrictive policy, no per-role or requester-scoped policy, and no trigger constrains ticket reads. A Contractor's own session token reads every ticket row in the org via PostgREST, and even inside the UI any member can open any ticket by URL because the detail fetch is unscoped."
  },
  {
    "id": "AUTHZ-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/schema.sql:1079-1081 `CREATE POLICY \"tickets_org_access\" ON tickets FOR ALL USING (org_id IN (SELECT my_org_ids()));` — FOR ALL, USING only, no WITH CHECK and no column or role predicate. No other policy or trigger on tickets exists: grepping `ON tickets` across supabase/ yields only indexes and `tickets_search_tsv_trg`, and 20260901_db_hard_enforcement.sql hardens holds, checkouts and documents but never tickets. No `REVOKE ... ON tickets` exists (schema.sql:393-394 revoke only archive_settings/archives), so `authenticated` keeps default UPDATE. Client-side writes prove the path is live: requests/page.tsx:620 `supabase.from('tickets').update({ priority: 1, ... })` and requests/[id]/page.tsx:1010-1014 `supabase.from('tickets').update({ attachments: [...], history: [...] })`.",
    "note": "Confirmed. The server-side enforcement in /api/tickets/workflow-action is real but entirely optional — the anon-key REST endpoint accepts a direct PATCH of status/deliverable_rev/requester_role/attachments from any active member of the org."
  },
  {
    "id": "AUTHZ-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/requests/new/page.tsx:309 `requester_role: activeRole` on a client-side insert; supabase/schema.sql:410 `requester_role TEXT,` — no CHECK, no FK, no default, and the tickets INSERT is governed by the same org-only policy (schema.sql:1079-1081). lib/ticketTransitions.ts:65 reads it back verbatim (`requesterRole: row.requester_role`), and lib/workflow.ts:78 `const needsEngineerApproval = requiresEngineerApproval(ticket.requesterRole);` is the sole input to the fork at 200. requiresEngineerApproval (37-43) returns false via `isEngineerRole` (18-20) which is `role.includes(\"Engineer\")` — so the free-text 'Engineer-4' clears the gate and line 212-217 offers 'Approve (Issue for Construction)' straight to the requester.",
    "note": "Confirmed — grep shows every writer of requester_role is client-side (requests/new/page.tsx:309, CheckInPanel.tsx:250, transitionIn.ts:315); no API route or DB constraint ever validates it against org_members.role. Note this is partly subsumed by AUTHZ-2 (with a blanket write policy the field can also just be UPDATEd after the fact), but it stands independently as an unvalidated authority input."
  },
  {
    "id": "AUTHZ-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/storage/resolve/route.ts:59-64 is the entire authorization: `if (orgId) { ... .from(\"org_members\").select(\"uid\").eq(\"org_id\", orgId).eq(\"uid\", user.id).eq(\"status\", \"active\") ... if (!m) return 403 }`, followed at 81-84 by `getSignedUrl(... GetObjectCommand ...)` with `{ expiresIn: 3600 }`. Its sibling app/api/storage/download-url/route.ts performs the same membership check at 36-46 and then, at 55-90, resolves the key to its document and runs `canDiscover(...)` for private/hidden docs (403 at 88), and at 95-111 enforces explicit `acl_index.deny.download` for users/roles/teams (403 at 109). None of that appears anywhere in resolve.",
    "note": "Confirmed by direct comparison — resolve has no document lookup for authorization at all (its document_versions read at 32-38 only fetches archived_at/archive_id), so a hidden/private document or one with an explicit download deny is signable through it by any active member."
  },
  {
    "id": "AUTHZ-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/admin/ticket-shed/commit/route.ts:136-139 `const keysFor = (t) => (Array.isArray(t.attachments) ? t.attachments : []).map((a) => (a?.url || \"\").toString()).filter(Boolean);` → line 186 `const keysToDelete = freeIds.flatMap((id) => keysByTicket.get(id) ?? []);` → line 193 `deleteR2Keys(keysToDelete)` issuing `DeleteObjectsCommand` (line 79) with no prefix validation and no assertSafeStorageKey. The restore path does exactly the guarding that is missing: restore/route.ts:101 `const prefix = `orgs/${orgId}/`;` and 140-144 `const allowedKeys = new Set((... stub.attachments ...).map((a) => (a?.url || \"\").toString()).filter((k) => k && k.startsWith(prefix)));` with the comment 'The only keys we may write are the ones the LIVE stub records, under our prefix.'",
    "note": "Confirmed, and the attachments array is member-writable by the blanket tickets policy plus the client write at requests/[id]/page.tsx:1010-1014. The deletes are irreversible (DeleteObjects) and the admin sees only a `keysDeleted` count in the audit row (line 202), so a planted key destroying another tenant's — or the org's own controlled document's — binary is invisible."
  },
  {
    "id": "AUTHZ-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/capabilityPolicy.ts:72-73 `{ id: \"ticket.requester_review\", ... defaultRoles: [\"Requester\"] }` and 141-155 `policyAllows` matches purely on role tokens/uid grants — it takes no ticket argument, so it cannot be per-ticket. lib/workflow.ts:74 `const canActAsRequester = isRequesterIdentity || allows('ticket.requester_review');` gates the whole PENDING_REVIEW block (199-234: approve_draft_ifc / approve_minor_correction / request_revision) and the FINAL_DRAFT close block (319-323).",
    "note": "Correct as stated — the identity check `isRequesterIdentity` is ORed with, not ANDed to, the role capability, so the role grants org-wide requester authority over every ticket. Worth noting the workflow-action route re-derives the same getActions server-side (route.ts:96), so this is enforced authority, not just UI."
  },
  {
    "id": "AUTHZ-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/capabilityPolicy.ts:171-178 destructures only `{ data }` — the PostgREST `error` is never inspected — so a failed query yields `const raw = (data?.value ...) ?? {}` and an empty `caps`; lines 193-195 `} catch { return {}; // defaults apply }` do the same for a thrown timeout. Line 148 then resolves every capability through `policy?.caps?.[cap] ?? DEFAULTS[cap]`, i.e. back to `{ id: \"ticket.direct_approve\", ..., defaultRoles: [\"Engineer\"] }` (74-75). Consumed unguarded at app/api/tickets/workflow-action/route.ts:95-96 `const capPolicy = await loadCapabilityPolicy(ticket.orgId, supabaseAdmin); const allowed = WorkflowEngine.getActions(ticket, callerRole, caller.id, capPolicy);`.",
    "note": "Confirmed, and worse than described: the failure result is written into the module cache at line 190-191 (`cache.set(orgId, { at: Date.now(), policy })` with CACHE_TTL_MS = 60_000), so one bad read pins the widened default authority for a full minute of requests. Contrast the DB-side twin org_capability_allows, which fails CLOSED (`ELSE '[]'::jsonb`, 20260901_db_hard_enforcement.sql:55)."
  },
  {
    "id": "AUTHZ-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/schema.sql:521-560: `LANGUAGE plpgsql SECURITY DEFINER SET search_path = public` (526) with `GRANT EXECUTE ON FUNCTION post_ticket_comment(UUID, JSONB, UUID[], UUID[]) TO authenticated, service_role;` (560). The insert takes identity straight from the payload: `COALESCE((p_comment->>'authorUid')::uuid, auth.uid()), p_comment->>'user', p_comment->>'role', ... COALESCE((p_comment->>'date')::timestamptz, NOW())` (544-551), and the JSONB thread gets the caller's blob verbatim: `comments = COALESCE(comments,'[]'::jsonb) || jsonb_build_array(p_comment)` (554). The trusted values only exist in the route the attacker skips — app/api/tickets/comment/route.ts:75-84 builds `user: callerEmail, role: callerRole, date: now, authorUid: caller.id` from the verified session before calling the RPC at 101-106. The 20260810_archive_invariants.sql:12-66 redefinition is identical.",
    "note": "Confirmed: the function's only checks are ticket existence, not-archived, and active membership in the ticket's own org (534-536), none of which constrain the author fields. Impersonation is same-org only (v_org comes from the ticket row, not the payload), which is the ceiling on the blast radius — HIGH rather than critical is right."
  },
  {
    "id": "AUTHZ-9",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/workflow.ts:65 `const allows = (cap) => policyAllows(policy, cap, userRole, null, userId);` — extraRoles is hard-null on the ticket path; app/api/tickets/workflow-action/route.ts:88 `const callerRole = (member.role as Role) ?? \"Viewer\";` (only the headline column is selected/used for the caller); lib/workflow.ts:116 `: allows('ticket.eng_review') || isManagement;` for the no-engineer-assigned case; lib/serverAuth.ts:57 `if (!allowedRoles.includes(role || \"\")) return { error: \"Insufficient role\", status: 403 };`. BUT the absolute claim is false: lib/holds.ts:99-100 `const extra = (member?.roles as string[] | null) ?? []; if (!policyAllows(policy, cap, role, extra, uid))` and components/permissions/ViewAsSimulator.tsx:128 `policyAllows(policy, d.id, who.role, who.roles, who.uid)` both pass extraRoles.",
    "note": "The substance holds for the ticket/admin surfaces — additive roles are ignored there — but \"never passed by any caller\" is refuted by lib/holds.ts:100 (production hold capability check) and the View-As simulator, and workflow-action/route.ts:124-130 does honor `roles` when validating an engineer pick (after which the assigned-engineer identity path at workflow.ts:115 grants the authority anyway). The residual defect is fail-closed (granted authority is withheld, never widened), so LOW; its worst effect is the simulator showing a capability the real check denies."
  },
  {
    "id": "BILL-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/stripe/webhook/route.ts:152-162 — `switch (s) { case \"active\": ... case \"paused\": return \"paused\"; default: return \"trialing\"; }`. `incomplete` is not a case, so it falls to `default` → \"trialing\". :55-66 then writes `subscription_status: status, subscribed_plan: plan` where :56 is `const plan = (sub.metadata?.plan as string) || null;` and checkout/route.ts:78-81 stamps `subscription_data: { metadata: { org_id: orgId, plan: String(plan) } }`. destinations/route.ts:88 — `const allowed = plan === \"growth\" || plan === \"enterprise\" || status === \"trialing\";`.",
    "note": "Confirmed: an `incomplete` (failed-3DS/declined) subscription is persisted as trialing + subscribed_plan='growth', and the default branch also swallows any future Stripe status. One nuance the finding overstates: hasAccess() (lib/subscription.ts:51) still gates trialing on trial_ends_at, and the webhook never touches that column — but hasAccess has no live enforcement caller (see BILL-10), so the operative grant is the plan gate at destinations/route.ts:88, which the finding cites."
  },
  {
    "id": "BILL-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/subscription/SubscriptionGate.tsx:46-48 — `const ENFORCE = false;` then `if (!ENFORCE || loading || hasAccess(info)) return <>{children}</>;` — the first disjunct short-circuits every render, even though app/(protected)/layout.tsx:67 does mount `<SubscriptionGate>{children}</SubscriptionGate>`. supabase/migrations/20260713_document_publish_guard.sql:90-94 — `-- ── Subscription helper (NOT wired to any blocking policy yet) ──`.",
    "note": "Confirmed by census. `grep -rn assertOrgHasAccess` across the repo (excluding audit-reports) returns exactly one hit — the definition at lib/serverAuth.ts:78; `grep -rn org_has_active_subscription` over supabase/ + app/ + lib/ returns exactly one hit — the CREATE at 20260713_document_publish_guard.sql:96. app/api/admin/create-user/route.ts:21-72 checks bearer token, role validity, and caller role only — no subscription check and no seat cap (a grep for seat/max_users/userLimit finds no enforcement anywhere)."
  },
  {
    "id": "BILL-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/storage/upload-url/route.ts:35-55 — the only gate before `getSignedUrl(r2, new PutObjectCommand(...))` is the org_members membership lookup; no quota read. app/api/storage/multipart/route.ts:40-58 is the same shape before `CreateMultipartUploadCommand`. A repo-wide grep for `quota_bytes|quotaBytes` across app/lib/components returns only archive-settings (read/write), lib/storageAlerts.ts (alert math), and the storage page's display — no write path. Meanwhile lib/storageUsage.ts:224-227 tells admins to act \"before uploads start failing\" and storageAlerts.ts:63 says \"This workspace is at N% of its storage limit.\"",
    "note": "Confirmed: the quota is display-and-notify only, and the dedupe at storageAlerts.ts:56-59 does suppress re-alerting for seven days. Minor imprecision in the finding, not in the conclusion: the quoted \"before uploads start failing\" copy at storageUsage.ts:224-227 belongs to the *platform* R2 ceiling alert, while the per-org quota's copy is the storageAlerts.ts:63 line."
  },
  {
    "id": "BILL-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/storageAlerts.ts:26-36 computes one deployment-wide figure — `mfg_table_stats()` (20260805_storage_stats_fn.sql:13-22: `from pg_class c join pg_namespace n ... where n.nspname = 'public'`, no org filter) plus `mfg_storage_estimate()` (20261008:38-44: `sum(size) from document_versions`, `sum(file_size) from asset_photos`, `sum(file_size) from knowledge_documents`, no org filter) — ending at :36 `const usedBytes = dbBytes + r2Bytes;`. That single number is then compared to each org's own quota at :45-51: `for (const s of settings) { const quota = Number(s.quota_bytes) || 0; ... const { pct, band } = alertBand(usedBytes, quota); }`. The dashboard repeats the error at app/(protected)/admin/storage/page.tsx:768-770: `const budgetBytes = isRealQuota ? quotaBytes : SOFT_BUDGET_BYTES; const usedBytes = (stats?.db.totalBytes ?? 0) + (stats?.r2Real?.bytes ?? ...);`.",
    "note": "Confirmed, and the path is live: app/api/cron/maintenance/route.ts:121 calls `runStorageAlerts(sb)` daily. The 'exact for the common single-workspace deployment' caveat in the doc comment at storageAlerts.ts:19-24 is an acknowledgement of the bug, not a guard — nothing scopes usedBytes to s.org_id."
  },
  {
    "id": "BILL-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/storageUsage.ts:131-159 — `export async function measureR2Usage(maxPages = 500)` with `do { const res = await r2.send(new ListObjectsV2Command({ ... MaxKeys: 1000 })); ... } while (token && pages < maxPages);` — strictly sequential, no deadline parameter, no elapsed-time check. app/api/admin/storage-stats/route.ts:14 is `export const runtime = \"nodejs\";` and the file declares no `maxDuration`; :121 calls `const measured = await measureR2Usage();` (default 500 pages) inside the user-facing GET.",
    "note": "Confirmed the absence claim: grepping `maxDuration` across app/ shows ~25 routes that set it (cron/maintenance:41 = 300, data-export/run-scheduled:18 = 300) and storage-stats is not among them, and vercel.json contains only `ignoreCommand` and `crons` — no functions.maxDuration default. So the interactive path runs an unbounded 500-round-trip walk under the platform's default timeout while the cron path that calls the same function (maintenance:279-284) gets 300s."
  },
  {
    "id": "BILL-14",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20261010_signup_rate_limit.sql:34-40 — `-- Housekeeping: ... the maintenance cron can call this` / `CREATE OR REPLACE FUNCTION prune_signup_attempts() RETURNS void LANGUAGE sql AS $$ DELETE FROM signup_attempts WHERE created_at < NOW() - INTERVAL '2 days'; $$;`. app/api/auth/signup/route.ts:27-28 — `if (error) return false; // table absent / transient — fail open` and `return (count ?? 0) >= SIGNUP_MAX_PER_HOUR;` with :10 `const SIGNUP_MAX_PER_HOUR = 8;`; :20 `if (ip === \"unknown\") return false;`.",
    "note": "Confirmed on both halves. A repo-wide grep for `prune_signup_attempts` returns exactly one hit — the CREATE at :37 — and app/api/cron/maintenance/route.ts contains no rpc call to it (its only prune is `intent-prune` at :134), so the rolling-window table accumulates forever behind the `(ip, created_at DESC)` index at :28-29. The limiter fails open on any query error and on a missing/unparseable client IP, and 60-day trial orgs are minted at signup/route.ts:103-114 with no other cap."
  },
  {
    "id": "BILL-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/schema.sql:1042-1045 — `CREATE POLICY \"orgs_admin_write\" ON orgs FOR UPDATE USING (id IN (SELECT org_id FROM org_members WHERE uid = auth.uid() AND status = 'active' AND role = 'Admin'));` — no WITH CHECK, no column list. app/(protected)/admin/settings/page.tsx:108-112 proves the browser grant is live: `await supabase.from(\"orgs\").update({ ticket_prefix: ..., ticket_record_code: ..., ticket_number_pad: ... }).eq(\"id\", activeOrgId)`.",
    "note": "Confirmed by repo-wide search: no WITH CHECK clause, no column-level REVOKE on orgs, and no BEFORE UPDATE trigger on orgs anywhere in supabase/schema.sql or supabase/migrations/*.sql. SubscriptionProvider.tsx:39-53 reads those same columns straight from the client, so an org Admin can set subscription_status/subscribed_plan/trial_ends_at to anything the CHECK constraint in 20260601_billing.sql:14 permits."
  },
  {
    "id": "BILL-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/stripe/webhook/route.ts:56 — `const plan = (sub.metadata?.plan as string) || null;` (metadata is set once at checkout, checkout/route.ts:78-81, and the Customer Portal never rewrites it). :84-87 — the `customer.subscription.deleted` handler updates only `{ subscription_status: \"canceled\", stripe_subscription_id: null }` — subscribed_plan is not in the patch. app/api/data-export/destinations/route.ts:88 — `const allowed = plan === \"growth\" || plan === \"enterprise\" || status === \"trialing\";` (status is not required to be active). app/api/data-export/run-scheduled/route.ts:60-66 selects due destinations on `.eq(\"enabled\", true)` + next_run_at only — no entitlement predicate.",
    "note": "Confirmed on both halves. portal/route.ts:43-46 creates a bare billingPortal session with no metadata sync, so a portal-driven plan change leaves subscription metadata (and therefore subscribed_plan) frozen at the original value; cancellation clears the status but not the plan, and the scheduled-push cron re-checks neither."
  },
  {
    "id": "BILL-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/stripe/webhook/route.ts:61-66 `await supabase.from(\"orgs\").update({...}).eq(\"id\", orgId);` — result never destructured; same at :84-87, :102-104, :113-115. :134 `return NextResponse.json({ received: true });` is reached on every non-throwing path. The `customer.subscription.updated` arm at :50-66 unconditionally writes `subscription_status: mapStripeStatus(sub.status)` and `stripe_subscription_id: sub.id`, with no comparison against the row's existing state.",
    "note": "Confirmed. supabase-js returns PostgREST failures as an `error` field rather than throwing, so a rejected update cannot reach the catch at :129 and the handler still answers 200. A repo-wide grep for a webhook event ledger (`stripe_events`, `stripe_webhook_events`, any store of `event.id`) returns nothing in supabase/, app/, or lib/ — there is no idempotency or ordering guard, so a late `updated` overwrites a `canceled` written by an earlier-processed `deleted`."
  },
  {
    "id": "BILL-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/admin/storage-stats/route.ts:164 — `const actor = await authorizeOrgRole(req, orgId, [\"Admin\", \"DocCtrl\"]);` (orgId comes from the caller's own body), then :174-179 `await actor.admin.from(\"platform_settings\").upsert({ key: STORAGE_LIMITS_KEY, value: {...} }, { onConflict: \"key\" })` — actor.admin is the service-role client (serverAuth.ts:48), so RLS is bypassed. supabase/migrations/20260920_per_user_keys_real_limits.sql:24-30 — `CREATE TABLE IF NOT EXISTS platform_settings ( key TEXT PRIMARY KEY, ... )`, one row per key deployment-wide. Leak half: lib/storageUsage.ts:131-162 `measureR2Usage` walks the whole bucket with no org prefix, and 20260805_storage_stats_fn.sql:13-22 `mfg_table_stats()` reads `pg_class` for the entire `public` schema.",
    "note": "Confirmed on both halves. The role check only proves membership in *some* org; nothing scopes the write to a platform owner, and STORAGE_LIMITS_KEY is a single global key. The GET at :24-150 returns deployment-wide table sizes, row counts, and per-segment R2 breakdowns (with newest-object timestamps) to any Admin/Manager/DocCtrl of any tenant; loadPlatformLimits (storageUsage.ts:56-77) and runPlatformStorageAlerts then band every tenant off that one shared ceiling."
  },
  {
    "id": "BILL-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/stripe/webhook/route.ts:140-145 — `const { data } = await supabase.from(\"orgs\").select(\"id\").eq(\"stripe_customer_id\", customerId).maybeSingle(); return (data as { id?: string } | null)?.id || null;` — the `error` field is not destructured, and maybeSingle() errors (PGRST116) on more than one match, so two orgs sharing a customer id yield null and the caller's `if (!orgId) break;` (:101, :112) silently drops the invoice event. Impersonation half: portal/route.ts:30-46 reads `stripe_customer_id` off the caller's own org row and hands it straight to `stripe.billingPortal.sessions.create({ customer: customerId, ... })`. The column is client-writable per schema.sql:1042-1045, and 20260601_billing.sql:31 creates only a NON-unique index: `CREATE INDEX IF NOT EXISTS orgs_stripe_customer_idx ON orgs(stripe_customer_id) WHERE stripe_customer_id IS NOT NULL;`.",
    "note": "Confirmed — no uniqueness constraint and no server-side validation that the id belongs to this org. If anything MEDIUM is understated rather than overstated: beyond the denial path, a malicious Admin at org B who copies org A's cus_… gets a Stripe Customer Portal session for org A's customer and can view A's invoices/billing address and cancel A's subscription."
  },
  {
    "id": "BILL-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/admin/archive-settings/route.ts:43-56 — the PUT builds `patch` (including `patch.quota_bytes` at :48), upserts into archive_settings, and returns `{ ok: true }`; there is no audit_logs insert anywhere in the file. app/api/admin/storage-stats/route.ts:158-188 — the POST upserts platform_settings and returns `{ ok: true }` with no audit_logs insert either. schema.sql:367-374 shows archive_settings keeps only `quota_bytes`, `updated_at`, `updated_by` — the current value, no history.",
    "note": "Confirmed by reading both handlers end to end and by grepping supabase/schema.sql and supabase/migrations/*.sql for any trigger or audit hook on archive_settings / platform_settings — there is none. Both numbers feed the alerting math directly (lib/storageAlerts.ts:38-51 reads quota_bytes; lib/storageUsage.ts:56-77 reads the platform ceiling), so raising either silences alerts with no record of who moved it."
  },
  {
    "id": "BILL-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/stripe/checkout/route.ts:43-84 goes straight from the role check to `stripe.checkout.sessions.create({ customer: customerId, mode: \"subscription\", ... })` — the only org lookup is `.select(\"id, name, stripe_customer_id\")` at :46-50; `stripe_subscription_id` and `subscription_status` are never read, and there is no `stripe.subscriptions.list` call. app/(protected)/admin/billing/page.tsx:184 — `{isAuthorized && info?.status !== \"active\" && (` gates the plans grid, so a `past_due` org is shown Subscribe buttons that call subscribe() → /api/stripe/checkout (page.tsx:41-55).",
    "note": "The load-bearing claim — a second live subscription on the same customer, with no existing-subscription check — is confirmed. One correction to the title, not the severity: the duplicate-*customer* half only occurs under a concurrent race, since :53-67 reuses `org.stripe_customer_id` when present and only creates a customer when it is null."
  },
  {
    "id": "BILL-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/schema.sql:777 — `  user_id UUID,` in `CREATE TABLE IF NOT EXISTS audit_logs`. app/api/stripe/webhook/route.ts:68-76, :88-93, :116-121 each do `await supabase.from(\"audit_logs\").insert({ ..., user_id: \"stripe\", user_email: \"stripe-webhook\", ... });` with the result never destructured.",
    "note": "Confirmed. PostgREST rejects the literal 'stripe' with `invalid input syntax for type uuid`, supabase-js surfaces that as a returned `error` (not a throw, so the catch at :129 never fires), and nothing reads it. A repo-wide grep of supabase/migrations/*.sql finds no ALTER changing audit_logs.user_id to TEXT and no cast. Worth noting the gap is slightly wider than stated: `invoice.payment_succeeded` (:97-106) attempts no audit row at all."
  },
  {
    "id": "BKP-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/dataExport.ts:300 `let q = sb.from(table).select(\"*\").range(...)` dumps every column; lib/exportTables.ts:50-54 lists \"document_shares\", \"project_intake_links\", … \"transmittals\" as ORG_SCOPED_TABLES. The token columns are plaintext and are the whole credential: 20260623_document_shares.sql:13 `token TEXT NOT NULL UNIQUE, -- url-safe random token`; 20260902_project_intake.sql:22 `token TEXT NOT NULL UNIQUE`; 20260910_transmittal_portal.sql:21 `ALTER TABLE transmittals ADD COLUMN IF NOT EXISTS portal_token TEXT`. Resolution is by raw equality with no other check: app/api/share/resolve/route.ts:33-35 `.from(\"document_shares\").select(...).eq(\"token\", token)`, app/api/transmittal/route.ts:27-30 `.eq(\"portal_token\", token)`, app/api/intake/upload/route.ts:35-37 `.from(\"project_intake_links\")…eq(\"token\", token)` — and that intake route then WRITES (PutObject + cost_documents/document_versions insert). EXPORT_EXCLUDED_TABLES (lib/exportTables.ts:173-176) excludes ai_connections precisely because it \"holds live AI provider API keys — secrets never leave the database\"; no equivalent carve-out or redaction exists for token columns (repo-wide grep for redact/scrub/strip-token in lib/ and app/api/data-export finds nothing).",
    "note": "Confirmed on every leg: the columns exist in plaintext, `select(\"*\")` carries them into both the JSON envelope and the ZIP's tables/*.json, and possession of the string is the entire credential on three unauthenticated routes, one of them write-capable. The only counter-argument is fidelity (a restore without tokens breaks live share links) — that's a design tension, not a refutation of the exposure."
  },
  {
    "id": "BKP-10",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/clientBackup.ts:149-150 `for (const f of files) { if (opts.isCancelled?.()) break; }` exits the loop, then line 186 `await finalizePart(true)` runs unconditionally, writing lib/clientBackup.ts:129-138 `backup-report.json` whose note is `progress.errors.length > 0 ? … : \"Every file verified by SHA-256 in files-manifest.json.\"` — a cancel produces no errors, so the reassuring branch is taken. The report carries `filesPacked` but no filesTotal and no cancelled flag. HOWEVER, the same archive DOES carry the totals: line 124 `zip.file(\"data.json\", JSON.stringify(envelope, null, 2))` puts the complete envelope — every `files[]` entry plus `manifest.files.count` (lib/dataExport.ts:249-250) — into part 1, so 200 entries in files-manifest.json against 9,000 in data.json is a one-line diff.",
    "note": "The misleading report note and the missing cancellation flag are real and correctly cited. But the claim's core sting — 'nothing in the ZIP records that 8,800 files are missing' — is false: part 1's data.json holds the full file list and count. Discoverable-but-unflagged omission, so LOW rather than MEDIUM."
  },
  {
    "id": "BKP-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260605_rls_policies_new_tables.sql:140-144 `CREATE POLICY \"export_dest_member_select\" ON export_destinations FOR SELECT TO authenticated USING (EXISTS (SELECT 1 FROM org_members WHERE org_id = export_destinations.org_id AND uid = auth.uid() AND status = 'active'))` — no role restriction and no column restriction, so any active member (Viewer included) can read access_key_id_encrypted / secret_access_key_encrypted / webhook_secret_encrypted, the exact columns app/api/data-export/destinations/route.ts:55-66 blanks out (`access_key_id_encrypted: undefined, …`). Restore reinstatement: export_destinations is exported (lib/exportTables.ts:157), is NOT in SKIP_TABLES (lib/dataRestore.ts:86-93), and is in the insert order at lib/dataRestore.ts:313 — rows come back with enabled and next_run_at intact (remapRow only touches org_id and uid values). app/api/data-export/run-scheduled/route.ts:60-66 then claims them globally: `.from(\"export_destinations\").select(\"*\").eq(\"enabled\", true).not(\"next_run_at\", \"is\", null).lte(\"next_run_at\", nowIso)`, and a restored past next_run_at is immediately due.",
    "note": "Both halves check out. The credential leak is ciphertext only (AES-256-GCM, key server-side), which is why MEDIUM is the right level; the substantive risk is the reinstated destination auto-pushing the new org's full dataset to the backup owner's bucket/webhook on the next sweep."
  },
  {
    "id": "BKP-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/dataRestore.ts:336-348 `CONFLICT_TARGETS` lists six tables and `conflictTargetFor` returns `\"id\"` for everything else. I checked all 104 ORG_SCOPED_TABLES against their CREATE TABLE statements: exactly four exported tables have no `id` column AND no CONFLICT_TARGETS entry — codebook_config (20260928_site_codebook.sql:37-38 `org_id UUID PRIMARY KEY`), document_equipment_suggestions (20260928:89-101 `PRIMARY KEY (org_id, document_id)`), recently_viewed_docs (20260806_intelligence_layer.sql:101-106 `PRIMARY KEY (user_id, document_id)`), library_numbering (20260806:120-121 `library_id UUID PRIMARY KEY`). Their upsert `onConflict: \"id\"` errors (42703); the fallback `.insert(chunk)` at apply-table:79-83 / apply:104-107 only survives while no row collides, so a re-run into a workspace that already has the row fails on the PK — and in /apply that failure ABORTS everything downstream (apply/route.ts:113-122 `for (const remaining of order.slice(idx + 1)) results.push({ … error: \\`skipped: aborted after ${name} failed\\` }); break;`) with codebook_config sitting 8th in RESTORE_TABLE_ORDER. The UI advises exactly the action that fails: app/(protected)/admin/restore/page.tsx:450 \"re-run the restore (it&apos;s additive and safe)\".",
    "note": "Confirmed exactly, including the count of four; codebook_config is one-per-org so the collision is guaranteed on any restore into a configured workspace."
  },
  {
    "id": "BKP-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/data-export/run-scheduled/route.ts:106-115 calls `buildAndDeliverExport({ … exporterUserId: \"cron\", exporterEmail: \"cron@manufacturing-os\" … })` and writes only export_runs rows; there is no audit_logs insert and no alertAdminsOfExport call anywhere in the file. The one audit attempt inside lib/dataExport.ts:182-197 inserts `user_id: params.exporterUserId` = the literal string \"cron\" into supabase/schema.sql:771-783 `CREATE TABLE IF NOT EXISTS audit_logs ( … user_id UUID, …)` — a 22P02 that PostgREST returns as `{ error }` (never thrown), so the row silently never lands. By contrast the interactive path does both: app/api/data-export/run/route.ts:141-144 `await alertAdminsOfExport(auth.admin, { … })`. The plan gate is bucket-only — app/api/data-export/destinations/route.ts:81-95 `if (body.bucket) { … Cloud backup destinations (S3/R2) require the Growth plan … }` — so a webhook destination skips it, and ADMIN_ROLES on that route (line 13) includes Manager and DocCtrl. lib/exportRunner.ts:58-76 assertSafeExternalUrl only rejects non-http(s) schemes, localhost/.internal and private-range addresses.",
    "note": "Every leg confirmed, including the subtle one: even the generic DATA_EXPORT audit insert cannot succeed for a cron run because \"cron\" is not a UUID."
  },
  {
    "id": "BKP-14",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/schemaExpectations.ts:104 `{ table: \"statements\", migration: \"20260819_orphan_tables_backfill.sql\" }` — repo-wide grep for \"statements\" in supabase/ returns only prose: 20260819_orphan_tables_backfill.sql:3 `-- Reproducibility backfill: CREATE TABLE statements for 11 tables that exist` and line 7 `-- All statements are IF NOT EXISTS…`. There is no such table, so the probe at app/api/admin/schema-health/route.ts:45-51 (`supabaseAdmin.from(t.table).select(\"*\", { count: \"exact\", head: true })`) always reports it missing and lines 67-81 always return `healthy: false` with 20260819 in migrationsToRun. I diffed EXPECTED_TABLES against every real CREATE TABLE in supabase/schema.sql + supabase/migrations: exactly 24 real tables are absent — answer_skills, audit_logs, change_orders, checklist_items, collections, companies, company_events, document_sets, document_versions, download_audits, knowledge_line_traces, link_rules, metadata_templates, org_configurations, org_members, orgs, process_flows, project_checklists, punch_items, table_views, tickets, turnover_items, users, watermark_policies — including process_flows, whose only source is the newest migration 20261017_process_flows.sql.",
    "note": "Confirmed precisely, phantom and count: my own naive CREATE-TABLE scan reproduced the 'statements' artifact from that comment line, which is exactly how the file says it was generated ('Generated from supabase/migrations (CREATE TABLE scan)', lines 10-13). The panel is permanently red on a table that does not exist and silent on 20261017_process_flows.sql."
  },
  {
    "id": "BKP-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "cost_documents holds a storage key: 20260819_orphan_tables_backfill.sql:184 `file_url text` and app/api/intake/upload/route.ts:70-87 writes `const key = \\`orgs/${orgIdQ}/project-costs/${projectIdQ}/quote-…\\`` then `supabaseAdmin.from(\"cost_documents\").insert({ … file_url: key … })`. lib/dataExport.ts:316-363 `collectFilePaths` enumerates ONLY document_versions.file_url, tickets.attachments, markup_requests.shared_markup_url, asset_photos.file_url, plot_plans.image_path, libraries/collections.cover_image_url and org_configurations branding — no cost_documents. lib/storageOrphans.ts:42-88 `sources` likewise has no cost_documents entry, and lib/storageOrphans.ts:26 `const PROTECTED_PREFIXES = [\"data/\", \"exports/\"]` does not cover `orgs/…/project-costs/`, so with MIN_AGE_DAYS = 7 (line 25) the key falls through scanOrphans:135-138 into the orphan list and deleteOrphans:162-165 issues `DeleteObjectsCommand`. app/api/admin/orphans/route.ts:47 `deleteOrphans(actor.admin)` is reachable by Admin/DocCtrl (ROLES, line 20). The row is still exported (lib/exportTables.ts:93 \"cost_documents\"), so the metadata survives with a dangling file_url.",
    "note": "Both halves verified by repo-wide search: no reference source anywhere registers cost_documents.file_url, so the binaries are simultaneously absent from every backup manifest and eligible for permanent, unrecoverable deletion 7 days after upload."
  },
  {
    "id": "BKP-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "apply-table forces the boundary — route.ts:52-58: `const m = remapRow(r, idRemap); if (\"org_id\" in m) m.org_id = orgId;` plus an IMPORTABLE allowlist (line 25). /apply does neither: app/api/admin/restore/apply/route.ts:83 `let mapped = rows.map((r) => remapRow(r, idRemap));` and lib/dataRestore.ts:211-213 only rewrites org_id when the value is the backup's own: `if (k === \"org_id\" && typeof v === \"string\" && idRemap.orgId[v]) out[k] = idRemap.orgId[v];` — and the map is a single pair `{ [backupOrgId]: targetOrgId }` (dataRestore.ts:136). A row carrying org_id = B is passed through untouched and inserted at apply/route.ts:103. The table set is also unbounded: line 75 `const importable = plan.counts.tables.filter((t) => t.willImport && t.rows > 0).map((t) => t.name)` and plan.counts.tables is built from `Object.entries(env.tables)` (dataRestore.ts:146) — any table name in the uploaded envelope. Writes go through the service-role client (lib/serverAuth.ts:48 `createClient(supabaseUrl, serviceRoleKey, …)`, returned as `admin`), so RLS cannot catch it.",
    "note": "Verified by direct comparison of the two routes; the org-forcing line exists in apply-table and has no counterpart in apply. Admin of org A can write forged rows into org B, and into any table name the envelope invents."
  },
  {
    "id": "BKP-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/dataExport.ts:95-97 calls `dumpTable(sb, tbl, \"org_id\", params.orgId)` for EVERY entry of ORG_SCOPED_TABLES, and dumpTable:304 issues `q.eq(column, value)` — a 42703 on a table with no such column, thrown at line 307. I diffed all 104 ORG_SCOPED_TABLES against every CREATE TABLE in supabase/: exactly four have no org_id column — `orgs` (schema.sql:19-30, PK `id`, no org_id), `project_members` (20260527:55-65, only project_id/user_id), `curated_collection_items` (20260602:63-71, `PRIMARY KEY (collection_id, document_id)`), `access_requests` (20260819:16-24, whose own comment reads \"public sign-up requests; no org_id\"). Each lands in the catch at dataExport.ts:100-106, forcing `complete: failedTables.length === 0` → false (line 248) and note #1 at lines 211-215 \"⚠ INCOMPLETE BACKUP — 4 table(s) could not be exported\".",
    "note": "Confirmed exactly, including the count of four. project_members (project rosters) and curated_collection_items (curated collection contents) are unrecoverable customer data that no export has ever contained."
  },
  {
    "id": "BKP-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/admin/restore/apply-table/route.ts:77 and app/api/admin/restore/apply/route.ts:103 are identical: `.upsert(chunk, { onConflict: conflictTargetFor(table), ignoreDuplicates: true, count: \"exact\" })` — ON CONFLICT DO NOTHING on the backup's own primary keys (lib/dataRestore.ts:346-348 `return CONFLICT_TARGETS[table] ?? \"id\"`). There is no UPDATE path anywhere under app/api/admin/restore/ (only begin, preview, apply, apply-table; grep for onConflict/ignoreDuplicates returns only those two lines). The UI still declares success: app/(protected)/admin/restore/page.tsx:445-447 renders a green panel `<CheckCircle2 /> Records restored … Imported <b>{fmtNum(applyResult.totalInserted)}</b> record(s)`, and page.tsx:190-216 counts `plan.counts.totalRows` as \"Restore N records\" before the run.",
    "note": "Right as stated — restore is insert-only by construction, so corrupted rows that still hold their original ids are never touched and the run reports success. Note the failure can also read WORSE than described: `inserted += up.count ?? chunk.length` (line 81) falls back to the full chunk size if PostgREST returns no count, reporting thousands of 'imported' records that were all discarded."
  },
  {
    "id": "BKP-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/exportRunner.ts:257-265 `if (dest.retention_days && dest.retention_days > 0) { … await s3PurgeOlderThan({ dest, prefix: dest.prefix || \"\", keepDays: dest.retention_days }) }` — the empty-string default reaches lib/exportRunner.ts:386 `Prefix: params.prefix ? params.prefix.replace(/^\\/+|\\/+$/g, \"\") + \"/\" : undefined`, i.e. an unfiltered ListObjectsV2 over the whole bucket, and lines 389-392 queue EVERY key whose LastModified < cutoff for deletion in 1000-key `DeleteObjectsCommand` batches (lines 398-403). The UI makes Prefix optional and mislabels the effect: app/(protected)/admin/data-export/page.tsx:605 `<Field label=\"Prefix\" hint=\"Optional folder inside the bucket\">` and 664-666 `<Field label=\"Retention (days)\" hint=\"Delete older exports in your bucket\">`.",
    "note": "Confirmed: nothing constrains the purge to keys this app wrote — not the filename pattern, not a prefix requirement, not a marker object. With Prefix blank, retention deletes unrelated objects in the customer's bucket."
  },
  {
    "id": "BKP-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Client ZIP (data-export page → page.tsx:136-143 `downloadZip` → `startGlobalBackup`) writes lib/clientBackup.ts:124 `zip.file(\"data.json\", JSON.stringify(envelope, null, 2))`, plus `files/<key>` (line 170), `files-manifest.json` and `backup-report.json` (lines 128-138) — no manifest.json, no tables/ folder. The restore page rejects exactly that shape: app/(protected)/admin/restore/page.tsx:111-115 `const manifestPath = entryNames.find((p) => /(^|\\/)manifest\\.json$/i.test(p)); if (!manifestPath) throw new Error(\"No manifest.json — this doesn't look like a manufacturing-os backup ZIP.\")` and then reads tables only from `/(^|\\/)tables\\/[^/]+\\.json$/` (line 118). The server-built ZIP does match: lib/exportRunner.ts:133 `zip.file(\"manifest.json\", …)` and 158-161 `tableFolder?.file(\\`${name}.json\\`, …)` — and that one is produced by a DIFFERENT page, app/(protected)/admin/storage/page.tsx:738-762 (`POST /api/data-export/run`).",
    "note": "Two admin pages emit two archive layouts and only the storage page's is loadable by /admin/restore. Confirmed by reading both writers and the reader."
  },
  {
    "id": "BKP-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/data-export/structured/route.ts:55-57 `if (![\"Admin\", \"Manager\", \"DocCtrl\"].includes(role || \"\")) return … 403` then line 60 `runOrgExport({ supabaseUrl, serviceRoleKey, … })`; app/api/data-export/run/route.ts:17 `const ADMIN_ROLES = [\"Admin\", \"Manager\", \"DocCtrl\"]`. The export client bypasses RLS by construction — lib/dataExport.ts:18-20 comment \"uses the Supabase service-role key to bypass RLS\" and line 86 `createClient(params.supabaseUrl, params.serviceRoleKey, …)`. The bypassed policies are real: 20260708_acl_rls_enforcement.sql:85-91 `CREATE POLICY documents_acl_select ON documents AS RESTRICTIVE FOR SELECT USING (node_visible(visibility, acl_index, org_id))`, whose helper (lines 56-62) returns true only for role IN ('Admin','DocCtrl') — a Manager without a grant is denied; 20260630_scratchpad_private.sql:59-66 `CREATE POLICY \"notes_standalone_own\" … AND notes.created_by = auth.uid()`. lib/dataExport.ts:143-179 mints a 24h presigned GET per file (`getSignedUrl(r2, new GetObjectCommand(...), { expiresIn })`, expiresIn default 24*60*60 at line 85) with no stamping and no download_audits insert — the only audit written is the single DATA_EXPORT row at lines 182-197, whereas lib/downloads.ts:1-9 documents that the normal path stamps and logs \"Every download … to `download_audits`\". app/data-portability/page.tsx:71-72 advertises \"Audited service-role usage … exports are logged to your audit trail.\"",
    "note": "Verified end to end: role gate admits Manager, the export runs as service role, and neither the ACL restrictive policy nor the private-notes policy nor the stamping/download_audits path applies."
  },
  {
    "id": "BKP-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/dataExport.ts:316-363 `collectFilePaths` covers exactly seven sources (document_versions.file_url, tickets.attachments, markup_requests.shared_markup_url, asset_photos.file_url, plot_plans.image_path, libraries/collections.cover_image_url, org_configurations branding logoPath) and lib/dataExport.ts:126-133 is the comment enumerating that same closed list. Missing keys that provably exist: `source_file_key` written at lib/revisions.ts:504-507 (`sourceFileKey = srcUpload.url` → versionPayload.source_file_key, line 524) — the native CAD source; `file_key` written at lib/knowledge.ts:357-365 (`const fileKey = \\`orgs/${input.orgId}/knowledge/${input.libraryId}/…\\`` → knowledge_documents insert); and output_templates' `template_file_key`/`example_files`. The orphan collector knows about all of them — lib/storageOrphans.ts:43-47 selects `\"file_url, source_file_key\"` and `knowledge_documents … \"file_key\"`, lines 80-87 add output_templates — which proves the columns are live storage keys the export simply omits. Meanwhile lib/exportRunner.ts:474-476 README still states `- files/<storage-path> — every binary file, path-preserved`, and app/data-portability/page.tsx:64 promises \"your backed-up export contains every byte you've ever uploaded.\"",
    "note": "The sibling collector in storageOrphans.ts is the settling evidence: the same repo enumerates these keys for deletion-safety but not for backup, so the ZIP's 'every binary file' claim is false for CAD sources, knowledge PDFs and output templates."
  },
  {
    "id": "BR-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/equipment-bridge/route.ts:37-40 is the entire authorization: `.from(\"org_members\").select(\"role\").eq(\"org_id\", orgId).eq(\"uid\", user.id).eq(\"status\", \"active\").maybeSingle(); if (!member || !WRITER_ROLES.has(String(member.role))) return bad(\"Not permitted\", 403);` — no libraryId/documentId ACL evaluation anywhere in GET or POST. Contrast lib/knowledgeAccess.ts:33-34 `.select(\"role, roles\")` and :38 `const roles = new Set<string>([member.role as string, ...((member.roles as string[]) ?? [])]);`. The UI gate is documents/[libraryId]/page.tsx:3769 `{isController && (` with lib/permissions.ts:18-20 `return role === \"Admin\" || role === \"DocCtrl\";`.",
    "note": "The security claim holds and is not merely a UI/server mismatch: the route writes through supabaseAdmin, so it bypasses the DB-level ACL guards that catch the equivalent client write (supabase/migrations/20260901_db_hard_enforcement.sql:152-162 `documents_deny_write_guard ... AS RESTRICTIVE FOR UPDATE ... auth.uid() IS NULL OR ...` — service role is exempt) and 20260708_acl_rls_enforcement.sql:85-87 `documents_acl_select`. One clause of the title is overstated: `roles[]` is NOT honored by 'every other route' — app/api/codebook/import/route.ts:192, app/api/graph/mentions/route.ts:43, lib/serverAuth.ts:51, lib/projects.ts:589, lib/reviewCycles.ts:234 and lib/accessRecert.ts:160 all select only `role`. That clause is inert anyway (ignoring roles[] under-grants, it does not leak)."
  },
  {
    "id": "BR-10",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/equipmentBridgeServer.ts:31 `pages: number[]; // sheets/pages the tag appears on` is filled at :76 from the entity row's PDF page ordinal `if (Number.isFinite(e.page)) pages.add(Number(e.page));`, and stored at :214 `discovered_from: { documentId, pages: s.pages }`. The parsed sheet segment lives on a separate row family — lib/knowledgeIngest.ts:291-292 `self(tb.drawingNumber); if (tb.sheetNumber) self(`${tb.drawingNumber}-SH${tb.sheetNumber}`);` (kind 'self') — and nothing joins it to an equipment tag.",
    "note": "The technical claim is true, but 'has no data to answer from' is false. knowledge_page_entities rows carry (document_id, page), and lib/drawingText.ts:559-582 `equipmentRegisterCsv` already emits a `[\"Tag\", \"Category\", \"Occurrences\", \"Sheets\", \"First page\"]` register keyed by documentName — reachable in the product via lib/knowledge.ts:983 `action=export` and :769 `action=census`, and the ask route's show-me guarantee (app/api/knowledge/ask/route.ts:1665-1680) mints per-sheet citations from the same table. On a one-sheet-per-document library the document IS the sheet, so the owner's question is answered; the 'self' rows also make the sheet number recoverable per page by a join nobody has written yet. Real gap, but a missing enrichment rather than missing data."
  },
  {
    "id": "BR-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/equipment-bridge/route.ts:24 `export const maxDuration = 60;` against :112-124 `const { data: docs } = await supabaseAdmin.from(\"documents\").select(\"id\")...limit(2000); ... for (const t of (twins ?? [])) { try { await computeForKnowledgeDoc(supabaseAdmin, t.id); computed++; } catch { /* per-doc failures never sink the sweep */ } }` — strictly sequential, no cursor/offset parameter accepted and no elapsed-time check. Each iteration is itself several round trips (equipmentBridgeServer.ts:65, :87, :88, the :105-112 batch loop, :124, and possibly :130 auto-apply). Client: components/documents/EquipmentSweepModal.tsx:100 `if (!res.ok) throw new Error((await res.json()).error ?? \\`HTTP ${res.status}\\`);` inside a catch that only does `setError((e as Error).message)`.",
    "note": "Confirmed: nothing persists a resume point, and `computed` is only returned on the success path, so a killed invocation reports nothing about how far it got. Re-sweep re-enters at document zero."
  },
  {
    "id": "BR-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/drawingText.ts:19-24 `export const SPARSE_PAGE_MAX_CHARS = 2000;` / `export function isDrawingLikePage(pageText: string): boolean { return pageText.trim().length > 0 && pageText.length <= SPARSE_PAGE_MAX_CHARS; }`. lib/knowledgeIngest.ts:231 `} else if (isDrawingLikePage(pageText)) {` is the ONLY non-vision entity-extraction branch — a page over 2000 chars produces zero rows in knowledge_page_entities. lib/equipmentBridgeServer.ts:65-70 reads exclusively from that table (`.eq(\"kind\", \"equipment\")`), so it sees nothing; :79-82 then records the empty result: `await upsertSuggestions(admin, orgId, documentId, [], null); return { documentId, suggestions: [], autoApplied: false };`.",
    "note": "Vision does not rescue these pages: lib/drawingText.ts:609-635 `pageNeedsVision` returns false for any page over TEXTLESS_PAGE_MAX_CHARS (60) that is not 'thin' — `const thin = text.length <= THIN_PAGE_MAX_CHARS (1200); if (tagsFound >= (thin ? MIN_TAGS_THIN_PAGE : 1)) return false; if (!thin) return false;` — so a >2000-char sheet never gets the visionRead branch at knowledgeIngest.ts:214 either. The empty result is indistinguishable in the sweep modal from a genuinely tagless sheet ('0 tags'). Confirmed."
  },
  {
    "id": "BR-13",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/codebook.ts:124-126 `const m = normalizeTag(tag).match(/^([A-Z]{1,4})-(\\d{1,6})([A-Z]{0,2})$/); if (!m) return null;` is the bridge's only gate (equipmentBridgeServer.ts:74 `if (!splitTag(norm)) continue;`), and it is indeed looser than lib/drawingText.ts:63 `const EQUIPMENT_RE = /\\b([A-Z]{1,3})[-–](\\d{1,5})([A-Z]{1,2})?\\b/g` plus :66-68 `EQUIPMENT_STOP_PREFIXES = new Set([\"NO\",\"DWG\",\"REV\",\"PID\",\"DRW\",\"SHT\",\"SH\",\"PG\",\"ISO\",\"API\",\"ANSI\",\"NPS\"])`. Unknown prefixes do file untyped: equipmentBridgeServer.ts:204-209 `const t = typeForTag(s.tag, book); ... type_id: t ? typeIdByLabel.get(t.label) ?? null : null,`.",
    "note": "The consequence (CL-150 / SS-316 / CW-6 minting origin='drawing' assets with null type) is real, but the stated cause is largely inert: every row in knowledge_page_entities with kind='equipment' is written only by lib/knowledgeIngest.ts:217 and :246, both via `extractEquipmentTags`, so nothing reaches the bridge that failed the stop list — re-applying it at the registry boundary would filter out exactly zero tags, and splitTag being looser cannot admit anything the extractor rejected. What remains is a duplicate of the extractor's permissive prefix policy plus BR-8's inability to see or bulk-clean discovered assets, so LOW as a standalone finding."
  },
  {
    "id": "BR-14",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260919_knowledge_mirror_unique.sql:11-13 `CREATE UNIQUE INDEX ... ON knowledge_documents (library_id, source_document_id)` — uniqueness is per LIBRARY, so N knowledge libraries may each mirror the same controlled doc; lib/knowledgeSourceSync.ts:194-196 scopes the existing-mirror read `.eq(\"library_id\", libraryId)`, confirming per-library mirrors are the design. app/api/equipment-bridge/route.ts:118-123 `.from(\"knowledge_documents\").select(\"id\").in(\"source_document_id\", ids...)` then `for (const t of twins) await computeForKnowledgeDoc(...)` — no library filter, no ordering. 20260928_site_codebook.sql:101 `PRIMARY KEY (org_id, document_id)` — one row per document. lib/equipmentBridgeServer.ts:147-154 upserts `suggested: suggestions.map(...)` on that key, wholesale.",
    "note": "Confirmed end to end: two twins of one document exist legitimately, the sweep iterates them in unordered query order, and each overwrites the single (org_id, document_id) suggestion row. upsertSuggestions preserves only `applied` (line 145), so the 3-tag twin computing last both replaces the 40-tag proposal and flips status to 'applied' when newTags is empty (line 152)."
  },
  {
    "id": "BR-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/equipmentBridgeServer.ts:272-274 writes only the metadata blob: `.from(\"documents\").update({ metadata: { ...metadata, [targetKey]: next }, updated_at: ... })`. The join table is fed only from a different column — supabase/migrations/20260609_phase1_normalization.sql:104-110 `INSERT INTO document_assets ... FROM jsonb_array_elements(NEW.asset_tags)` on `CREATE TRIGGER documents_resync_assets_trg AFTER INSERT OR UPDATE OF asset_tags, org_id ON documents`. The asset hub reads the same other column: app/(protected)/assets/[tag]/page.tsx:55 `.contains(\"asset_tags\", [{ tag }])`; AreaKnowledgePanel.tsx:124-125 and lib/operationalGraph.ts:322-326 both read `from(\"document_assets\")`.",
    "note": "Repo-wide grep for `asset_tags` writers finds only lib/documentLifecycle/common.ts:179 and merge.ts:150 (split/merge) — nothing in the bridge path writes asset_tags, so neither the trigger nor the assets-INSERT backfill (migration :122-135) ever fires for bridge-applied tags. Confirmed."
  },
  {
    "id": "BR-3",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "lib/equipmentBridgeServer.ts:59 `if (!kdoc?.source_document_id) return null; // upload-origin docs have no column to feed`. lib/knowledge.ts:363-367 `insert({ org_id, library_id, name, file_key, file_size, created_by, created_by_name })` — no source_document_id on the upload path; only lib/knowledgeSourceSync.ts:235 `source_document_id: dcDocId,` sets it. The sweep additionally cannot reach such docs: route.ts:112-120 enumerates `from(\"documents\")` in a DC library then finds twins `.in(\"source_document_id\", ids...)`.",
    "note": "The mechanism is exactly as described and there is no compensating path. Severity lowered because the scenario's premise is wrong: the Area wizard does NOT train uploading — AreaKnowledgePanel.tsx:169/476 and UnitOpsPanels.tsx:342 all call `addKnowledgeSources(...)` (DC folder/document links), which go through knowledgeSourceSync and DO get source_document_id. The voided path is only a direct drag-drop into a knowledge library (app/(protected)/knowledge/[id]/page.tsx:1525 `addKnowledgeDocument`), and that screen never promises the registry (no 'registry'/'asset' string in the whole file). The column half of the bridge is also genuinely impossible there — only the registry-discovery half is lost."
  },
  {
    "id": "BR-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/assets/AssetCsvImportModal.tsx:22-27 `const CANONICAL_FIELDS = [{key:\"tag\"...},{key:\"description\"},{key:\"location\"},{key:\"type\"}]` — no unit or code field; :128-131 `await createAsset({ orgId, tag, description, location, typeId, createdBy: actorUserId });` omits the `unitCode`/`code` params that lib/assets.ts:177-179 accepts. Unit filing keys off code: lib/assetCategorize.ts:50-55 `if (!a.unit_code && a.code) { const decoded = codeToTag(a.code, book); if (decoded?.unitCode) unitAssignments.push(...) }`. UnitOpsPanels.tsx:46 `const plan = useMemo(() => planCategorization(assets, types, book), ...)`.",
    "note": "Confirmed: a CSV-imported asset has code NULL, so `!a.unit_code && a.code` is false for every row and plan.unitAssignments is empty — applyCategorization's `filedToUnits` (assetCategorize.ts:117-123) counts 0 while `categorized` counts the type assignments. No other automatic code-setter exists: the only tagToCode callers are admin/assets/page.tsx:1018 (UnassignedAssignPanel, human-driven), :1313 (the edit drawer, human-driven) and equipmentBridgeServer.ts:117."
  },
  {
    "id": "BR-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/equipmentBridgeServer.ts:180-182 `const alreadyApplied = new Set<string>(...row.applied...); const wanted = suggested.filter((s) => (!input.tags || input.tags.includes(s.tag)));` — alreadyApplied is never used to filter `wanted`; its only use is the union at :279 `const appliedUnion = [...new Set([...alreadyApplied, ...appliedNow])];`. :269 `const additions = wanted.map((s) => s.tag).filter((t) => !existingNorms.has(assetNorm(t)));` compares only against what is currently in the column, so a chip a human deleted is 'missing' and gets re-added. Contradicts the module's own invariant at :16-18 `* Idempotent — recompute diffs against what was already applied; re-index never duplicates chips or re-creates assets.`",
    "note": "The trigger is real and automatic: lib/knowledgeIngest.ts:452-454 `void import(\"@/lib/equipmentBridgeServer\").then((m) => m.computeForKnowledgeDoc(supabaseAdmin, doc.id))` fires on every completed (re-)index, and computeForKnowledgeDoc:128-131 then calls `applyForDocument(admin, { orgId, documentId, userId: null })` with no `tags`, so `wanted` is the full suggestion set. upsertSuggestions:145-152 preserves `applied` but that only affects the status label, not what apply re-writes."
  },
  {
    "id": "BR-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/assets.ts:196-201 `let resp = await supabase.from(\"assets\").insert({ ...base, unit_code..., code... }).select(\"*\").single(); ... if (resp.error) throw new Error(resp.error.message);` — insert only, never upsert. supabase/migrations/20260603_asset_registry.sql:46 `UNIQUE (org_id, tag_normalized)`. components/assets/AssetCsvImportModal.tsx:135-136 `catch (e) { failed.push({ row: rIdx + 2, reason: (e as Error).message }); }` propagates the raw Postgres text, rendered verbatim at :242-245 `{result.failed.slice(0, 8).map(...)}<li>Row {f.row}: {f.reason}</li>` and `{result.failed.length > 8 && <li className=\"italic\">+{result.failed.length - 8} more</li>}`.",
    "note": "Confirmed down to the '+N more' detail in the summary. Notably the codebase already has `translatePostgresError` (used at admin/assets/page.tsx:1391 and :1406) and this modal does not use it, so the raw constraint name reaches the user."
  },
  {
    "id": "BR-7",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/equipmentBridgeServer.ts:244-245 `const { data: blankRows } = await admin.from(\"assets\").select(\"id, code\").in(\"id\", ids).is(\"unit_code\", null);` then :250 `if (!blank.has(id)) continue;` — an asset whose unit_code is already set is skipped entirely, so the `patch.code = s.code` branch at :252 can never run for it.",
    "note": "The gate is exactly as described, but two of the finding's supporting claims are false. (1) The cited 'admin drawer that set only the unit' does NOT exist: app/(protected)/admin/assets/page.tsx:1311-1315 `useEffect(() => { if (asset?.code) return; const derived = unitCode ? tagToCode(tag, unitCode, book) : null; setSiteCode(derived ?? \"\"); }, ...)` auto-derives the code whenever a unit is set. (2) 'keeps code NULL forever' is refuted by that same drawer, which is a working per-asset remedy. lib/assetCategorize.ts:117-121 only sets unit_code, but its precondition (`!a.unit_code && a.code`) guarantees code is already present. The realistic residual path is narrow — a bridge-discovered asset whose tagToCode returned null because the codebook lacked the prefix at discovery time."
  },
  {
    "id": "BR-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Repo-wide grep for `discovered_from` returns exactly four non-migration hits, all writes or type declarations: lib/equipmentBridgeServer.ts:214 `discovered_from: { documentId, pages: s.pages },`, :220, :222, and lib/assets.ts:47 `discovered_from?: Record<string, unknown> | null;` — zero readers. Same for assets.origin: written at equipmentBridgeServer.ts:213 `origin: \"drawing\",` and declared at lib/assets.ts:46, but no UI reads it (the only `origin ===` comparisons in the app are on codebook_entries, documents, asset_aliases and document links). Cleanup is one row at a time: app/(protected)/admin/assets/page.tsx:1397-1404 `onDelete` → `await deleteAsset(asset.id);` behind a single-asset appConfirm. The modal's promise is components/documents/EquipmentSweepModal.tsx:263 'applying discovers them (unit + type from the Site Codebook, provenance recorded)'.",
    "note": "Confirmed by exhaustive search — the provenance is written and never surfaced or filtered on, and app/(protected)/admin/assets/page.tsx:115 loads with `listAssets({ orgId: activeOrgId, archived: false })` with no origin facet."
  },
  {
    "id": "BR-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Client: components/documents/EquipmentSweepModal.tsx:106-113 `const apply = async (documentIds: string[]) => { ... body: JSON.stringify({ action: \"apply\", orgId, documentIds }) }` — no `tags` key; the only call sites are :201 `apply(pendingRows.map((r) => r.document_id))` and :228 `apply([r.document_id])`. Server: lib/equipmentBridgeServer.ts:171-172 `/** Restrict to a subset of suggested tags (review UI); omit = all. */ tags?: string[];` and :181-182 `suggested.filter((s) => (!input.tags || input.tags.includes(s.tag)))`; route.ts:131 `const tags = Array.isArray(body.tags) ? body.tags.map(String) : undefined;` is wired and unused. DB: supabase/migrations/20260928_site_codebook.sql:103 `status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'applied', 'dismissed'))`.",
    "note": "Confirmed: no code anywhere writes status 'dismissed' to document_equipment_suggestions (grep for 'dismissed' hits only processFlows, linkProposals and unrelated UI state), and the chip renderer at :236-253 is a plain <span> with no click handler — there is no per-tag selection affordance at all."
  },
  {
    "id": "CB-1",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "lib/equipmentBridgeServer.ts:93-99 `const numberCandidates = [srcDoc.document_number, kdoc.name, srcDoc.title, srcDoc.name]...; for (const cand of numberCandidates) { const parsed = parseDrawingNumber(cand, book); if (parsed?.unitCode) { unitCode = parsed.unitCode; break; } }` — it tests `parsed.unitCode`, never `parsed.unitLabel`, which is the only thing lib/codebook.ts:229 (`out.unitLabel = book.units.find((u) => u.code === chunk)?.label ?? null`) resolves against the codebook. lib/codebook.ts:149-159 tagToCode concatenates `${unitCode}${type.code}` with no membership check. lib/equipmentBridgeServer.ts:211-212 `unit_code: s.unitCode ?? null, code: s.code` writes it straight into assets.",
    "note": "The mechanism is real and I re-derived the decode by hand against the repo's own fixture (codebook.test.ts:37-43 segments unit(2)/drawing_type(2)/size(1)/iterable/sheet): \"2024-VESSEL-LIST.PDF\" yields unitCode \"20\", size \"V\", then the iterable finds no digits and returns early — so a filename becomes an operating unit, unvalidated. Severity is overstated at HIGH: it is a data-quality defect with no attacker, it needs a configured drawing-number decoder AND a document whose document_number fails to parse, and \"permanently\" is wrong — admin/assets/page.tsx:1307 and :1383 (`unit_code: unitCode || null`) let a human re-file any asset, and page.tsx:240-244 surfaces phantom units in the unknownUnits bucket rather than hiding them."
  },
  {
    "id": "CB-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/codebook.ts:159 `return `${unitCode}${type.code}.${padded}${parts.suffix}`` — the composed code carries the TYPE code, never the prefix, while lib/codebook.ts:132-143 typeForTag maps every prefix in `t.meta.tagPrefixes` onto the same entry. The repo's own fixture registers a multi-prefix type: lib/__tests__/codebook.test.ts:25 `entry(\"equipment_type\", \"10\", \"Vessels\", [\"V\", \"D\"])`. So V-1 and D-1 both resolve to type \"10\" and both compose to \"2010.1\". codeToTag (:177) makes the loss explicit — `const prefix = (type.meta.tagPrefixes ?? [])[0]` — it can only ever return the first prefix. 20260928_site_codebook.sql:83 `CREATE INDEX IF NOT EXISTS idx_assets_org_code ON assets (org_id, code);` is non-unique, and a grep for UNIQUE on assets across supabase/migrations finds nothing on `code`.",
    "note": "Correct, and the collision is reachable with the exact codebook the project ships in its own tests. The asset rows stay distinct (they are keyed on tag_normalized), but their site identity — the thing the org navigates and prints by — is shared, and codeToTag inverts \"2010.1\" to V-1 unconditionally, so the drum is silently renamed to the vessel on every round trip."
  },
  {
    "id": "CB-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260928_site_codebook.sql:108-111 `CREATE POLICY doc_equip_sugg_write ON document_equipment_suggestions FOR ALL USING (org_id IN (SELECT org_id FROM org_members WHERE uid = auth.uid() AND status = 'active')) WITH CHECK (same)` — FOR ALL, membership only, no role predicate, in contrast with the codebook tables' own policies eight lines earlier (:66-73) which require `role IN ('Admin','DocCtrl')`. lib/equipmentBridgeServer.ts:175-179 reads `suggested` back verbatim and :205-216 inserts `tag: s.tag, unit_code: s.unitCode ?? null, code: s.code` with no re-validation (no splitTag/typeForTag gate on the stored tag).",
    "note": "The write/delete authority claim is exactly right — any active member, Viewer included, can rewrite or DELETE the proposal and the applied ledger, and applyForDocument trusts the row. One sub-claim in the summary is wrong: the automatic post-ingest path is NOT a clean trigger, because computeForKnowledgeDoc recomputes and upserts `suggested` at line 124 immediately before calling applyForDocument at :130, destroying the injection. The real vector is the confused deputy in app/api/equipment-bridge/route.ts:128-141, where a WRITER_ROLES user's Apply reads the poisoned row with no recompute."
  },
  {
    "id": "CB-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/codebook/import/route.ts:143 `.filter((r) => r.code.length > 0 && r.code.length <= 6 && r.label.length > 0 && r.label.length <= 80)` — length only, while the sibling line :140 DOES validate prefixes with `/^[A-Z]{1,4}$/.test(p)`, showing the omission is specific to `code`. app/(protected)/admin/codebook/page.tsx:194-201 `if (!draft.code.trim() || ...) return; await upsertEntry(orgId, { kind, code: draft.code, ... })` and lib/codebook.ts:350 `code: entry.code.trim()` — trim only. app/(protected)/admin/assets/page.tsx:1714-1717 AddUnitModal: `if (!c) {...}; if (existingCodes.includes(c)) {...}` then inserts — emptiness and collision, never digits. 20260928_site_codebook.sql:20 `code TEXT NOT NULL` with no CHECK.",
    "note": "Right on every point. The asymmetry is decisive: lib/codebook.ts:159 `return `${unitCode}${type.code}.${padded}${parts.suffix}`` happily emits \"CU30.22\", but codeToTag at :166 requires `/^(\\d+)\\.(\\d+)([A-Za-z]{0,2})$/` and parseDrawingNumber at :225 requires `^\\d{width}$`, so a non-numeric code is write-only — derivable, never invertible, and nothing raises."
  },
  {
    "id": "CB-4",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/roleCapabilities.ts:74-79 `Manager: 90, ... DocCtrl: 70` with the comment at :70-72 \"the highest-ranked role ... mirrored into org_members.role so ... the database RLS policies (which read `role`) reflect\" it. 20260928_site_codebook.sql:68 `... AND role IN ('Admin','DocCtrl')` — the headline column. app/(protected)/admin/codebook/page.tsx:35 `const WRITER_ROLES = new Set([\"Admin\",\"DocCtrl\"])` and :49 `canWrite = !!activeRole && WRITER_ROLES.has(activeRole)`, where RoleContext.tsx:201,213 sets `const headline = primaryRole(collection); setActiveRole(active ? headline : \"Viewer\")`. Against that, lib/knowledgeAccess.ts:38-41 `const roles = new Set([member.role, ...(member.roles ?? [])]); isController: roles.has(\"Admin\") || roles.has(\"DocCtrl\")` — additive — and app/api/area/knowledge-status/route.ts:287 `if (!principal.isController)` is the only bar on binding a unit's knowledge library. lib/codebook.ts:382-387 documents the divergence in-tree.",
    "note": "The asymmetry is exactly as described and is even acknowledged by the code's own NOTE. Severity should drop to LOW: this fails CLOSED (a lockout, not an escalation), and there is no silent-no-op variant to worry about because every codebook write surface gates on the same headline value — admin/assets/page.tsx:251 `canEditLinks = activeRole === \"Admin\" || activeRole === \"DocCtrl\"` — so the UI hides the control rather than firing an update that affects zero rows."
  },
  {
    "id": "CB-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/admin/codebook/page.tsx:209-213 `appConfirm({ title: `Remove ${e.code} — ${e.label}?`, message: \"Features stop recognizing this code. Nothing else is deleted.\" })` → :215 `await deleteEntry(e.id)`, and lib/codebook.ts:360-363 `deleteEntry` is a bare `.delete().eq(\"id\", id)`. The code is genuinely not editable: EntryRow's save (page.tsx:265-273) rebuilds only `label` and `meta.tagPrefixes` from `...entry`. Nothing references the code: 20260928_site_codebook.sql:78-83 adds `assets.unit_code TEXT` / `assets.code TEXT` as plain columns with only non-unique indexes, no FK to codebook_entries; 20261017_process_flows.sql:17-20 `from_kind ... CHECK (from_kind IN ('asset','unit')), from_ref TEXT NOT NULL, to_kind ..., to_ref TEXT NOT NULL` with the header comment at :9-12 stating endpoints are \"(kind, ref) pairs rather than hard FKs\" and `kind='unit'` holds the unit CODE.",
    "note": "Confirmed: re-typing a mistyped unit code is delete-plus-add, which cascades to nothing, and the confirm text's \"Nothing else is deleted\" is technically true but misleading — assets keep the dead unit_code (surfacing in the unknownUnits bucket at admin/assets/page.tsx:240-244) and process_flows rows keep dangling to_ref/from_ref strings that now resolve to no unit."
  },
  {
    "id": "CB-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "A repo-wide grep for `tagToCode` returns exactly three non-test call sites: lib/equipmentBridgeServer.ts:117 (compute time), app/(protected)/admin/assets/page.tsx:1018 `code: r.asset.code ?? tagToCode(r.asset.tag, r.unitCode, book) ?? null` (?? — existing code always wins), and :1313 (the manual asset editor's preview). No batch/recompute path exists. lib/equipmentBridgeServer.ts:251-252 `const patch = { unit_code: s.unitCode }; if (!blank.get(id) && s.code) patch.code = s.code;` — code is written only when it is already blank, and the whole backfill is gated on `.is(\"unit_code\", null)` (:245). lib/assetCategorize.ts:51-57 only reads `codeToTag(a.code, book)` under `if (!a.unit_code && a.code)`, and pushes a unit assignment — it never rewrites `code`. app/(protected)/admin/codebook/page.tsx:344-347 `await saveConfig(orgId, { drawingNumber..., iterableRule: { mirrorsTag: true, padTo } }, uid)` is followed only by `setSaved`/`onChanged` — no re-derivation.",
    "note": "The absence claim holds under a repo-wide search: every write of assets.code is a create-time or fill-a-blank write, so a padTo (or type-code, or prefix) change leaves every pre-existing asset carrying a code the current codebook would no longer produce, with nothing anywhere detecting the divergence."
  },
  {
    "id": "CB-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/admin/codebook/page.tsx:346 `iterableRule: { mirrorsTag: true, padTo: Math.max(0, Math.min(6, padTo)) }` — the literal `true`, in the only save on the numbering tab. lib/codebook.ts:396 `if (patch.iterableRule !== undefined) row.iterable_rule = patch.iterableRule;` then :398 `.upsert(row, { onConflict: \"org_id\" })` — whole-object replace, no merge. A repo-wide grep for `mirrorsTag` finds it in exactly four places outside tests: the type (codebook.ts:76), the default (:94), the read (:338 and codebookServer.ts:28) and the consumer (:156 `if (!mirrorsTag) return null`) — plus this one hardcoded write. There is no input, toggle, or API that can ever set it to false.",
    "note": "Both halves of the claim are true. The only way an org can hold mirrorsTag:false is an imported/restored codebook_config row, and the first Save on the Drawing numbers tab silently flips it back to true — after which tagToCode (codebook.ts:155-159) starts minting derived codes for a scheme that does not mirror the tag."
  },
  {
    "id": "CB-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/codebook.ts:137-142 `for (const t of book.equipmentTypes) { for (const p of t.meta.tagPrefixes ?? []) { const up = p.toUpperCase(); if (parts.prefix === up && up.length > bestLen) { best = t; bestLen = up.length; } } }` — the strict `>` means a second, equal-length prefix can never displace the first, and the iteration order is the load order from lib/codebook.ts:325 `.order(\"sort\").order(\"code\")`. 20260928_site_codebook.sql:31 `UNIQUE (org_id, kind, code)` constrains the CODE only; nothing constrains meta.tagPrefixes. app/(protected)/admin/assets/page.tsx:1787-1789 checks only `existingTypeCodes.some((c) => ... === tc.toLowerCase())` — a code collision — and app/(protected)/admin/codebook/page.tsx:197-201 adds the entry with no prefix check at all.",
    "note": "Accurate, including the detail that the winner is deterministic-by-sort rather than random. Both admin entry points validate the type CODE for collisions and neither looks at prefixes, so a second type claiming an in-use prefix is accepted in silence and every one of its tags is then typed and coded as the first type."
  },
  {
    "id": "CB-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/assetAliases.ts:17 `import { normalizeTag } from \"@/lib/codebook\"` and :65 `alias_normalized: normalizeTag(alias)` — the sole writer of the column (grep for `alias_normalized` returns only lines 65, 87, search.ts:55 and assets.ts:163). lib/codebook.ts:112-117 uppercases: `.toUpperCase().replace(...).replace(/\\s+/g,\"\")...`. The readers use the other one: lib/assets.ts:77-79 `export function normalizeTag(tag) { return (tag||\"\").toLowerCase().replace(/[^a-z0-9]+/g, \"\"); }`, used at assets.ts:163 `.eq(\"alias_normalized\", normalizeTag(tag))` and — via `import { normalizeTag } from \"@/lib/assets\"` at search.ts:32 — at search.ts:50-55 `const key = normalizeTag(q); ... .eq(\"alias_normalized\", key)`. No DB-side normalization exists: 20260807_link_proposals.sql:125 declares `alias_normalized TEXT NOT NULL` and :136-138 index it, with no trigger, generated column, or citext.",
    "note": "Confirmed by exhaustive grep on the column. Writes land uppercase (\"THENORTHFURNACE\"), so the two `.eq()` lookups in assets.ts and search.ts — both case-sensitive equality against a lowercase key — can never match any row. Only lib/assetAliases.ts:83 resolveAliasToAssetIds happens to use the codebook normalizer and therefore works; the other alias consumers (linkProposerServer.ts:261, mentionIndexer.ts:44, knowledge/ask/route.ts:373) sidestep the bug by reading the raw `alias` text."
  },
  {
    "id": "COST-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/costs.ts:309 `if (earnedValue !== null) { evTotal += earnedValue; evActual += spent; }` and 332 `cpi: evActual > 0 ? evTotal / evActual : null` — both sums are restricted to pinned accounts, while 326/329 sum budget and spent over ALL accounts. costSeries.ts:112-113 `const eac = budget / cpi;` is handed the whole-project budget at CostCharts.tsx:54 `budget: rollup.budget, spent: rollup.spent, cpi: rollup.cpi`. lib/projectReport.ts:58-59: `void pctIdx; // EV pinning uses milestone ids; report uses account-level rollup below` then `const rollup = computeCostRollup(accounts, entries, new Map());`.",
    "note": "Both halves hold. With the shape pinned in lib/__tests__/costs.test.ts:53-69 (cpi 1.5, budget 1500, spent 900) computeForecast returns eac = 1000 — below money already spent. And the close-out report passes an EMPTY milestone map, so every earnedValue is null, cpi is null, and projectReport.ts:139's CPI row is omitted entirely while the forecast silently falls back to run-rate/none."
  },
  {
    "id": "COST-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260906_projects_hardening.sql:158-175 creates `%I_write … FOR ALL USING (is_org_controller(org_id))` for cost_entries/cost_documents/cost_accounts/project_parties, and 20261013:304-314 adds `%I_owner_write … FOR ALL USING (user_owns_project(project_id))`; 20261013:262-266 does the same FOR ALL for change_orders. FOR ALL includes DELETE and a repo-wide grep for `FOR DELETE`/`BEFORE DELETE` finds no restrictive DELETE policy or trigger on any of the three tables — while lib/costs.ts:246-247 states \"Financial records are never deleted\" and lib/costDocs.ts:308-309 states \"Awarded/posted paper stays\".",
    "note": "Confirmed. The codebase demonstrably knows the fix — 20260814_documents_delete_controllers.sql:44 and 20260815:24-29 use `AS RESTRICTIVE FOR DELETE`, and 20260826 adds BEFORE DELETE triggers for legal holds — but none of that was applied to the money tables."
  },
  {
    "id": "COST-11",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/costDocs.ts:252-257 `await supabase.from(\"cost_documents\").update({ status: \"declined\" }).in(\"id\", …).in(\"status\", [\"draft\",\"parsed\"]).then(() => undefined, () => undefined);` — the error branch is discarded and awardQuote returns `{ ok: true }` at :263 regardless. Same swallow at :196-201 (revertDocTransition), lib/changeOrders.ts:178-188, lib/costs.ts:111-117.",
    "note": "The swallowed write is real, but the headline consequence is FALSE: the losing bids are not awardable. QuotesPanel.tsx:205 computes `const awarded = groupDocs.find((d) => d.status === \"awarded\")` over the whole RFQ group (the same group awardQuote uses to pick rivals, costDocs.ts:250-251), and both Award affordances are gated on it — `{canManage && !awarded && (…PostControls… label=\"Award\")}` at :309-314 for the bid table and `canManage && !awarded && d.status === \"parsed\"` at :356 for typed-total bids. Once one bid in the group is awarded the Award button disappears for every sibling, so no second commitment can be posted through the UI. What actually survives is cosmetic/reporting: the rivals stay labelled \"Read — awaiting your review\" forever and never register as considered-and-declined."
  },
  {
    "id": "COST-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/companies.ts:248-251 queries change_orders, turnover_items, punch_items and cost_documents by `.in(\"party_id\", partyIds)`. Of those four, only change_orders.party_id is ever written (ChangeOrdersPanel.tsx:225 `partyId: partyId || null` from the vendor <select> at :253-257). turnover_items.party_id (lib/turnover.ts:152,174) and punch_items.party_id (:253) are optional inputs both callers omit — QualityTab.tsx:512 `seedTurnoverItems({ orgId, projectId, jobKind, actor })`, :601/:604 `addTurnoverItem({ orgId, projectId, name: addName, actor })`, :644 `addPunchItem({ orgId, projectId, title, dueDate: due || null, actor })` — as does ProjectWizard.tsx:199-201. cost_documents.party_id (lib/costDocs.ts:114) is likewise omitted by the only uploader, QuotesPanel.tsx:461-466, and by the intake-portal insert at app/api/intake/upload/route.ts:83-92. For the join itself: ProjectWizard.tsx:180-186 inserts `{org_id, project_id, name, kind, trade, company_id, created_by}` — no contract_value; lib/costs.ts:132-153 builds contract_value but never company_id, and its only caller (CostsTab.tsx:542) passes no `id`, so the update branch at costs.ts:141-146 is dead code.",
    "note": "Every clause checks out on a repo-wide search. The consequence is exactly as stated: parties are found only by company_id (companies.ts:240), so awardsTotal is always 0 and the quality/bids/punch evidence slices are always empty — the scorecard structurally cannot score a company."
  },
  {
    "id": "COST-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/projects/cost-docs/route.ts:27 `const MAX_PAGES = 8;` and :97 `renderKnowledgePages(doc.file_url, Array.from({ length: MAX_PAGES }, (_, i) => i + 1), MAX_PAGES)`; lib/knowledgePageRender.ts:31 `const wanted = [...new Set(pages)].slice(0, maxPages)` and :40 `if (page < 1 || page > pdf.numPages) continue` — it knows pdf.numPages but never returns it. The model's reading is then written straight to the awardable number at :128 `patch.total_amount = quote.total` (:137 for invoices), which lib/costDocs.ts:226 and :280 use as the posting amount. The route returns `pagesRead` at :156, and the client discards it: QuotesPanel.tsx:66 `const body = (await res.json().catch(() => null)) as { error?: string } | null;`.",
    "note": "Confirmed. The only surviving trace of how much was read is the audit row's `pagesRead` (:153); no UI surface anywhere warns that a 14-page quote was read to page 8, and nothing compares pages rendered against the PDF's true page count."
  },
  {
    "id": "COST-14",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/costDocs.ts:310-318: `if (doc.status === \"awarded\" || doc.status === \"posted\") return {ok:false,…}` reads `input.doc`, the caller's snapshot, then `await supabase.from(\"cost_documents\").update({ status: \"void\" }).eq(\"id\", doc.id)` with NO `.in(\"status\", …)` filter — unlike claimDocTransition (:183-190), which re-reads the row and CAS-guards `.in(\"status\", fromStatuses)`. The callers pass stale React state: QuotesPanel.tsx:172-174 renders VoidButton when `doc.status === \"draft\" || doc.status === \"parsed\"` and :360-362 when `d.status === \"parsed\"`, both from the last fetch.",
    "note": "The stale-snapshot race is real and the file's own sibling function shows the guard it is missing. Reachable for invoices (:172-174) and typed-total quotes (:360-362); a parsed quote in the bid table has no Void affordance, which narrows the exact scenario in the summary but not the defect."
  },
  {
    "id": "COST-2",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "lib/costs.ts:306 `const spent = agg.actual + agg.adjustments;` then 316 `remaining: a.budget - spent`, 317 `overBudget: a.budget > 0 && spent > a.budget`, 330 `remaining: sum((r) => r.account.budget) - sum((r) => r.spent)` — `committed` appears in no formula. lib/projectHealth.ts:25 declares `committed: number;` in ProjectStateSnapshot and it is never read again anywhere in the file (grep '\\bcommitted\\b' in projectHealth.ts returns only line 25); the Cost part at 70-86 branches on cpi, else `s.spent / s.budget`.",
    "note": "The arithmetic claim is exactly right — a $480k award on a $500k account still reports remaining $500k, overBudget false, and a 100/100 cost health score. Downgraded to MEDIUM because 'invisible' overstates the UI: CostsTab.tsx:129-130 renders a dedicated 'Committed' stat card with '% of budget', line 150 prints 'spent · committed · budget', lines 152-153 draw the committed ghost bar, and line 356 repeats committed per account — the reader is shown the number, it is just excluded from Remaining/overBudget/health."
  },
  {
    "id": "COST-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "QuotesPanel.tsx:275 `const known = companies.find((c) => c.name.toLowerCase() === e.vendorName.toLowerCase()) ?? null;` — whole-string equality (case-folded only), feeding the 286-291 badges. A repo-wide grep for 'do_not_use' returns only display sites (QuotesPanel.tsx:289, companies/[id]/page.tsx:106-107/525, companies/page.tsx:186) plus the type at lib/companies.ts:24 — lib/costDocs.ts:211-263 awardQuote checks kind, the CAS claim and the total, and never consults company status. app/api/companies/quality-manual/route.ts:27 `const MAX_PAGES = 10;` used at line 56 `renderKnowledgePages(file.fileKey, Array.from({ length: MAX_PAGES }, (_, i) => i + 1), MAX_PAGES)`.",
    "note": "All three sub-claims verified: no code path blocks or even warns on awarding a do_not_use vendor, matching is whole-string so 'Apex Industrial Services, LLC' never resolves to 'Apex Industrial', and the rubric score is computed from at most the first 10 rendered pages (the prompt itself concedes 'Only the attached pages count')."
  },
  {
    "id": "COST-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/changeOrders.ts:162-174: approval calls `addEntry({ ..., entryType: \"commitment\", amount: co.amount, ... })` and the only other writes in decideChangeOrder are to the change_orders row itself (status/decided_* at 148-154, posted_entry_id at 185-187) — nothing touches cost_accounts.budget. lib/costs.ts:308 `const earnedValue = pct !== undefined ? a.budget * (...) : null` and 316-317 keep measuring against the unrevised budget; a repo-wide grep for revised_budget/revisedBudget returns nothing.",
    "note": "Confirmed — approved scope growth lands only on the committed side, so once the CO work completes the milestone drives EV = original budget while actuals include the CO, producing a permanent CPI < 1 and overBudget = true. The only mitigation is a controller manually editing the account budget via saveAccount (costs.ts:173-178), which is untracked against the CO; projectHealth.ts:104-113 separately penalizes CO growth, so the same money is charged twice against the score."
  },
  {
    "id": "COST-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/bidTab.ts:159 `const maxGaps = Math.max(...econ.map((e) => e.missingScope.length + e.exclusionCount), 1);` with 168-169 `const gaps = e.missingScope.length + e.exclusionCount; const coverage = (1 - gaps / maxGaps) * 100;` — with a two-bid field where one bid has a single exclusion, maxGaps = 1 and that bid's coverage is 0. lib/bidTab.ts:165-167 `const manpower = e.dollarsPerHour != null && e.dollarsPerHour > 0 && minDph != null ? (minDph / e.dollarsPerHour) * 100 : 0;` where dollarsPerHour = q.total / hours (line 139) and hours is summed from the bid's own self-reported l.hours (118-124). DEFAULT_WEIGHTS (line 63) = { price: 0.5, manpower: 0.3, coverage: 0.2 }.",
    "note": "Both mechanics verified. Padding hours lowers $/hr monotonically and therefore raises the manpower part: 2,000h vs 4,000h at the same $200k gives parts of 50 vs 100, a 15-point weighted swing, on a number the bidder writes about itself with no cross-check. The 'best value' label is advisory (tooltip at QuotesPanel.tsx:293 says 'not automatically the winner'), which caps the impact but does not change the ranking bias."
  },
  {
    "id": "COST-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20261013_project_controls_program.sql:262-266 `CREATE POLICY change_orders_write ON change_orders FOR ALL USING (is_org_controller(org_id) OR user_owns_project(project_id)) WITH CHECK (...)` — one FOR ALL policy covers INSERT and UPDATE alike. decideChangeOrder (lib/changeOrders.ts:139-161) re-reads the row and CAS-claims on `status='proposed'` but never compares `co.createdBy` to `input.actorId`, and has no amount threshold. ChangeOrdersPanel gates only on `canManage` = `isOwner || isAdmin` (app/(protected)/projects/[id]/page.tsx:134).",
    "note": "Verified by repo-wide search: no self-approval, separation-of-duties, or amount-threshold check exists anywhere for change orders (the only self-approval logic in the repo is in the tickets/workflow module). The proposer's own uid lands in both created_by (:108) and decided_by (:154) with nothing objecting."
  },
  {
    "id": "COST-7",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/companies.ts:299 `finalCostTotal: awardsTotal + approvedCoTotal` where approvedCoTotal (:285) sums EVERY approved CO regardless of reason_code; lib/companyScore.ts:111-113 `const growth = Math.max(0, (e.finalCostTotal - e.awardsTotal) / e.awardsTotal); … const score = clamp(100 - growth * 250 - gapShare * growth * 250)`. This contradicts lib/changeOrders.ts:5-8 (\"scope_gap lands on the contractor's record, design_error and owner_request land on ours\") and the tooltip at ChangeOrdersPanel.tsx:159-161.",
    "note": "The formula is exactly as described — reason_code only ever ADDS penalty (gapShare), never exempts owner_request/design_error from the base growth penalty. Severity lowered because the dimension is unreachable with real data: awardsTotal comes solely from project_parties.contract_value on rows matched by company_id (companies.ts:240), and those two columns are written by mutually exclusive paths (ProjectWizard.tsx:180-186 writes company_id but no contract_value; lib/costs.ts:132-153 writes contract_value but no company_id, and the only caller CostsTab.tsx:542 is insert-only). So `e.awardsTotal <= 0` always holds and companyScore.ts:109 short-circuits to \"No awarded work yet\" — it is a latent formula bug, not a score anyone sees today."
  },
  {
    "id": "COST-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "cost_documents.currency is read (lib/costDocs.ts:44,67) and written by the parse route (app/api/projects/cost-docs/route.ts:129 `if (quote.currency) patch.currency = quote.currency;`) but then dropped at every comparison and every posting: lib/bidTab.ts:39-55 (BidEconomics has no currency field) and :156-162 `const minTotal = Math.min(...econ.map((e) => e.total)…); const price = (minTotal / e.total) * 100` compare raw magnitudes; QuotesPanel.tsx:298 `{fmtMoney(e.total)}` and :218 `for ${fmtMoney(total)}` both fall through to fmtMoney's `currency = \"USD\"` default (lib/costs.ts:352); lib/costDocs.ts:232-242 / 286-296 call addEntry with no currency argument at all (lib/costs.ts:211-216 has no such parameter); CostsTab.tsx:489 hardcodes `currency: \"USD\"` on every account it creates.",
    "note": "Confirmed on every cited line. The one place currency survives is the invoice row's own display (QuotesPanel.tsx:154 `fmtMoney(doc.totalAmount, doc.currency ?? \"USD\")`) — the bid tab, the award confirm dialog, the commitment entry and the rollup all lose it."
  },
  {
    "id": "COST-9",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "Repo-wide grep for `source_document_id` finds no write against cost_entries — 20260908_cost_control.sql:54-59 adds the FK to the column declared at 20260819_orphan_tables_backfill.sql:213, and lib/costs.ts:226-238 (the only cost_entries insert) never lists it. `posted_entry_id` appears in exactly two places: the column at 20261013:130 and the write at lib/changeOrders.ts:186 — never in a select, and the ChangeOrder interface (lib/changeOrders.ts:25-42) has no postedEntryId field.",
    "note": "Both dead-column claims are exactly right. Severity lowered because the summary's operational premise is wrong on two counts: a generic unwind path DOES exist and is exposed in the UI (CostsTab.tsx:383-397 renders a per-entry Void calling voidEntry), and the entry does carry a human back-reference to the CO — lib/changeOrders.ts:172-173 sets `description: \"${co.coNumber} — ${co.title}\"` and `reference: co.coNumber`, both rendered at CostsTab.tsx:378-379. The real residue is that the FKs are dead and voiding the entry leaves the CO still reading \"approved\"."
  },
  {
    "id": "DACL-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260708_acl_rls_enforcement.sql:52-55 `-- Fail-safe: normal/unset visibility is open to org members. IF p_visibility IS NULL OR p_visibility = 'normal' THEN RETURN true; END IF;` — this executes before acl_index is ever consulted, and :85-91 attach node_visible as the only RESTRICTIVE SELECT policy on documents and collections. The drawer writes one row: components/permissions/PermissionDrawer.tsx:275-284 `const payload = { acl: nextAcl, acl_index: aclIndex ?? null, updated_at }; if (nodeType !== \"library\") payload.visibility = visibility; await supabase.from(table).update(payload).eq(\"id\", nodeId)` — `.eq(\"id\", nodeId)`, no descendants, and for a library not even `visibility`. Documents are stamped independently of their folder's state: app/(protected)/documents/[libraryId]/page.tsx:2448-2452 `visibility: library.defaultNewVisibility ?? \"normal\", acl: library.defaultNewAcl ?? null, acl_index: library.defaultNewAcl ? buildAclIndexFromChain(...) : null`. The app layer does what SQL does not: page.tsx:1742-1751 `canWithAclChain({ principal, action: \"read\", aclChain: buildDocChain(docRecord), defaultAllow: true })`.",
    "note": "Confirmed, including the claims of absence — I searched every migration and supabase/REMEDIATION_APPLY_ALL.sql for a trigger, function, or policy that walks the container chain for documents and found none (the only documents trigger, 20261011_collections_guard_and_trash.sql:58-59, is a move guard). Because a document's own visibility stays 'normal' even when created inside a restricted folder, node_visible returns true at line 54 before it can read the acl_index — so restricting a folder or library is enforced by React only, and a raw PostgREST select with the member's own JWT returns the rows."
  },
  {
    "id": "DACL-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "documents/page.tsx:43-51 `computeCanRead` (`return readList.includes(role) || visibleTo.includes(role);`) is applied only on the home page, at :166 `const visible = isController ? libs : libs.filter((l) => l._canRead);`. On the detail page, documents/[libraryId]/page.tsx:1433-1456 selects `read_access,visible_to` into `readAccess: data.read_access ?? \"ALL\", visibleTo: data.visible_to ?? []` and then never reads either again — a grep of the whole 1800+ line file for `readAccess|read_access|visibleTo|visible_to` returns exactly those two lines (1433, 1456). The only guards on the page are `if (!data)` → \"Library not found.\" and `if (data.org_id !== activeOrgId)` → wrong-workspace.",
    "note": "Confirmed by repo-wide search: read_access / visible_to appear in no RLS policy in supabase/migrations (grep over *.sql returns only unrelated `project_visible_to_me` hits), and (protected)/layout.tsx enforces only auth + org membership. So the columns are a home-page display filter with no server-side or route-level backing."
  },
  {
    "id": "DACL-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260609_phase1_normalization.sql:186-197 — `CREATE POLICY \"document_assets_member_all\" ON document_assets FOR ALL TO authenticated USING (EXISTS (SELECT 1 FROM org_members WHERE org_id = document_assets.org_id AND uid = auth.uid() AND status = 'active'))` and the identical `project_documents_member_all` — membership only, no join to documents, no node_visible() call. 20260708_acl_rls_enforcement.sql:84-91 adds the RESTRICTIVE acl policies to exactly two tables: `CREATE POLICY documents_acl_select ON documents ...` and `CREATE POLICY collections_acl_select ON collections ...`.",
    "note": "Confirmed the claim of absence with a repo-wide grep of supabase/migrations for document_assets/project_documents policies — the only other hit is CATCHUP_2026-05-28.sql:468-479, which creates the same membership-only policies. Both tables carry document_id, so any active member can enumerate the UUIDs and tag↔document mappings of documents whose `documents` rows RLS hides."
  },
  {
    "id": "DACL-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260708_acl_rls_enforcement.sql:69-72 — `IF (p_acl_index->'deny'->'users'->'read') ? v_uid OR (p_acl_index->'deny'->'users'->'discover') ? v_uid THEN RETURN false; END IF;` — the deny check reads `->'users'` only; `deny->roles` and `deny->teams` are never consulted. Then :78-80 `-- Any allow grant (any action) lets the row through` / `RETURN acl_subject_in_bucket(p_acl_index->'allow', v_uid, v_role, v_teams);`, and acl_subject_in_bucket (:21-39) is `bool_or(e.value ? p_role) FROM jsonb_each(p_bucket->'roles')` — it iterates EVERY action list, so an allow of `discover` alone satisfies the read gate. The app layer disagrees: lib/acl.ts:101-105 records role/team denies (`if (rule.effect === \"deny\") for (const a of actions) denied.add(a);` after `subjectMatches` handles `case \"role\"` and `case \"team\"`), and acl.ts:135 `if (denied.has(action)) return false;`.",
    "note": "Both defects are visible in the same function and the migration comment at :78 concedes the second one (\"finer read-vs-discover distinctions stay in the app layer\"), which is precisely why a PostgREST-direct read escapes them. The role/team deny omission is not documented anywhere and looks unintentional."
  },
  {
    "id": "DACL-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/storage/delete/route.ts is 44 lines end to end: token → `supabaseAdmin.auth.getUser(token)` → `const orgMatch = path.match(/^orgs\\/([0-9a-fA-F-]{36})\\//)` → an org_members `status='active'` lookup → `await r2.send(new DeleteObjectCommand({ Bucket: R2_BUCKET, Key: path }))`. There is no documents/document_versions lookup, no role check, no legal_hold read, and no assertSafeStorageKey — a grep for that helper finds it imported and called in upload-url:26, resolve:29, download-url:29 and multipart:37, and in no other file, so delete is uniquely unguarded. The comment at :26-28 even says \"Mirrors the check in download-url\" while omitting both of download-url's other gates (download-url/route.ts:29 key validation, :71-88 the private/hidden ACL discover check). 20260826_legal_hold_delete_guard.sql:29-33 and :53-56 attach BEFORE DELETE triggers to `documents` and `document_versions` only — Postgres rows, not R2 objects. lib/storage.ts:442-450 deleteFile is a thin client wrapper that hands `path` straight through.",
    "note": "Every element checks out. The legal-hold triggers are the sharpest confirmation: the header at :10-13 claims they \"close every path at once\", but they can only refuse the DB delete, so bytes destroyed through this route leave the held document row and register entry intact and the spoliation invisible. The one point worth qualifying is the storage-key gap — lib/storageKey.ts:4-11 argues traversal is not itself exploitable against R2's opaque keys, so that part is a consistency defect rather than a second vulnerability."
  },
  {
    "id": "DACL-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/d/[number]/route.ts has no authorization header read anywhere in the file and queries with the service role: :26-31 `await supabaseAdmin.from(\"documents\").select(\"id, library_id, document_number, updated_at\").filter(\"document_number\",\"not.is\",null).ilike(\"document_number\", `%${raw.replace(/[%_]/g,\"\")}%`).order(\"updated_at\", {ascending:false}).limit(25)` — no `.eq(\"org_id\", ...)`, and :34-36 falls back to `?? (rows ?? [])[0]` so a non-exact substring still returns someone's document. :44-46 `dest.searchParams.set(\"doc\", match.id); return NextResponse.redirect(dest)` puts both UUIDs in the Location header. app/api/verify/route.ts:22-39 takes `doc` from the query string with only `UUID_RE.test(docId)` and no auth, and :96-108 returns `docNumber`, `title`, `currentRev`, `docStatus`, `isCurrent`.",
    "note": "The claim is right and each route's own comment states the assumption the other one breaks. /d/[number]:6-7 says \"The target page enforces auth + RLS as always — this route only translates a number into a location; it reveals nothing\", but the redirect body is never fetched — the Location header IS the disclosure. /api/verify:5-7 rests on \"Both IDs are unguessable UUIDs that only appear ON a printed copy the org itself issued\", which /d/ falsifies by handing out real document UUIDs from any tenant to an unauthenticated caller supplying a 2-character substring."
  },
  {
    "id": "DACL-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260623_document_shares.sql:37-54 — `CREATE POLICY document_shares_org_member ON document_shares FOR ALL USING (EXISTS (SELECT 1 FROM org_members WHERE org_members.org_id = document_shares.org_id AND org_members.uid = auth.uid() AND org_members.status = 'active'))` with an identical WITH CHECK — no predicate on the referenced document at all. app/api/share/file/route.ts:40-58 then uses the service role: `const sb = createClient(supabaseUrl, serviceRoleKey, ...)` ... `.from(\"documents\").select(...).eq(\"id\", share.document_id)` and the only gates that follow are `if (share.revoked_at)` / `if (share.expires_at && ... < Date.now())`. Nothing reads documents.visibility, documents.acl_index, document_holds, or a distribution ack anywhere in the file.",
    "note": "Confirmed on every limb: RLS gates share creation on org membership only, and the file route is service-role with no ACL/legal-hold/download-deny/ack check. I looked for a compensating guard in /api/share/resolve and in ShareLinkModal and found none — the modal is pure UI, and the insert goes through PostgREST so any UI gating is bypassable. If anything the finding understates it: the WITH CHECK never verifies that document_id belongs to org_id, and share/file resolves the document by id with no org filter, so a member can also mint a link for a document in a DIFFERENT tenant."
  },
  {
    "id": "DACL-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/acl.ts:72-73 — `case \"role\": return !!ctx.role && ctx.role === (id as Role);` — a scalar compare, never a membership test against the collection. Every caller feeds it a scalar: lib/permissions.ts:9-11 `export interface Principal { uid: string; role: Role; ... }`, documents/[libraryId]/page.tsx:1650-1656 `principal = { uid: uid ?? \"\", role: activeRole, ... }`, lib/knowledgeAccess.ts:38-44 reads `role, roles` but only uses the collection for `isController`, passing `role: (member.role as Role)` on. SQL agrees: 20260708_acl_rls_enforcement.sql:56-57 `SELECT role INTO v_role FROM org_members ...`. The single exception is exactly the one named: app/api/storage/download-url/route.ts:102-107 `const heldRoles = ((mem2?.roles as string[] | null) ?? [(mem2?.role as string) ?? \"Viewer\"]); ... heldRoles.some((r) => (dl.roles?.download ?? []).includes(r))`.",
    "note": "Repo-wide search for evaluateAcl/evaluateAclChain/canDiscover callers turned up no other site that expands org_members.roles into the ACL principal, so the asymmetry is real: secondary-role allow grants are inert, and a secondary-role deny binds only on download-URL issuance. 20260722_member_roles_collection.sql:5-8 even states the design intent (\"every existing single-role check and every RLS policy reads `role`\"), which is the cause, not a refutation."
  },
  {
    "id": "DACL-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/acl.ts:170 `if (!chain.some(Boolean)) return null;` — an ACL object with `rules: []` is truthy, so a decision IS returned, with empty allowed/denied. lib/permissions.ts:39-40 `if (!decision) return defaultAllow; return decision.can(action);` then yields false for every action (acl.ts:133-137 `can` returns `allowed.has(action)` = false). PermissionDrawer.tsx:263-270 always writes that object: `const nextAcl: AccessControl = { inherit, visibility, rules: rules.map(...) };` ... `payload = { acl: nextAcl, acl_index: aclIndex ?? null, ... }`, and buildAclIndexFromRules returns null for an empty list (acl.ts:258). Meanwhile 20260708_acl_rls_enforcement.sql:52-54 `IF p_visibility IS NULL OR p_visibility = 'normal' THEN RETURN true;` — the DB still hands every row over.",
    "note": "Verified both halves and the trigger path: page.tsx:1728-1735 (folders, via canDiscover→isDiscoverable) and 1745-1751 (documents, `canWithAclChain({... defaultAllow: true})`) both flip from allow to deny the moment library.acl exists with zero rules, while Admin/DocCtrl short-circuit at permissions.ts:18-20 and see no change. The app/DB divergence is exactly as described."
  },
  {
    "id": "DACL-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/data-export/run/route.ts:18 `const ADMIN_ROLES = [\"Admin\", \"Manager\", \"DocCtrl\"];` used at :75 `const auth = await authorizeOrgRole(req, orgId, ADMIN_ROLES);` (serverAuth.ts:58 `if (!allowedRoles.includes(role || \"\")) return { error: \"Insufficient role\", status: 403 };`). structured/route.ts:55-57 repeats it: `if (![\"Admin\", \"Manager\", \"DocCtrl\"].includes(role || \"\"))`. lib/dataExport.ts:86-88 dumps with the service role (`createClient(params.supabaseUrl, params.serviceRoleKey, ...)`, `for (const tbl of ORG_SCOPED_TABLES) { const rows = await dumpTable(sb, tbl, \"org_id\", params.orgId); }`) so RLS — including documents_acl_select — never applies; lib/exportTables.ts:44 lists `\"documents\"`, and dataExport.ts:329-331 pulls every `document_versions.file_url` into the file set. Manager is not a controller: lib/permissions.ts:18-20 `return role === \"Admin\" || role === \"DocCtrl\";` and 20260708:57-60 `IF v_role IN ('Admin', 'DocCtrl') THEN RETURN true;`.",
    "note": "\"Manager\" is a real assignable role (types/schema.ts:8). The only compensating control I found is detective, not preventive, and it does not even notify the Manager tier: run/route.ts:47 `.in(\"role\", [\"Admin\", \"DocCtrl\"])` in alertAdminsOfExport, plus a 12-runs/hour rate limit at :78-85. Neither blocks the export."
  },
  {
    "id": "DACL-8",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "app/api/verify/route.ts:32-39 — service-role client, then `.from(\"documents\").select(\"id, document_number, title, name, rev, status, current_version_id, superseded_at\").eq(\"id\", docId)` with no visibility/acl_index predicate; the response at :96-108 returns `docNumber`, `title`, `currentRev`, `docStatus`. verify-hold/route.ts:29-40 and verify-package/route.ts:39-48 do the same (the package route fans one UUID out to every member sheet's label via `work_package_documents` → `documents`).",
    "note": "Factually correct — there is no visibility check on any of the three routes. But MEDIUM overstates it: these are deliberately unauthenticated QR endpoints (documented at verify/route.ts:3-13 and verify-package/route.ts:3-9), the response is metadata only (no file, no URL, no people — verify-hold:50-54 explicitly withholds notes and staff names), and every route hard-gates on a UUID_RE match plus a record_id/document cross-check (verify/route.ts:60-63). The marginal disclosure to someone who already holds the UUID is document number, title and rev status. LOW."
  },
  {
    "id": "DACL-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "documents/[libraryId]/page.tsx:1670-1677 — `for (const id of folder.pathIds) { const node = folderMap.get(id); if (node?.acl) chain.push(node.acl); }` — an ancestor whose row RLS withheld is simply absent from folderMap and silently skipped, with no marker that the chain is incomplete. The truncated chain then lands on the default-allow path at :1745-1751 `canWithAclChain({ principal, action: \"read\", aclChain: buildDocChain(docRecord), defaultAllow: true })` and, for folders, permissions.ts:120 `if (!decision) return visibility !== \"hidden\" && visibility !== \"private\";`. The inherit:false half is at lib/acl.ts:181-192: `if (!inherit || !nodeInherit) { mergedRules = []; ... }` runs BEFORE `mergedRules = mergedRules.concat(acl.rules ?? []); ... inherit = nodeInherit;`, so for chain [library{inherit:false, rules:R1}, folder{inherit:true}] the second iteration sees the carried-forward `inherit === false`, clears mergedRules, and R1 is gone.",
    "note": "Both halves reproduce by reading the code. The inherit-flag bug is a genuine off-by-one in scope: `inherit:false` on a node should clear its ANCESTORS' rules (which it does correctly on that node's own iteration) but the false value is then carried into the next node and clears that node's inherited set again, dropping the restricting node's own rules. Fails open in both cases."
  },
  {
    "id": "DCK-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "The gate exists once, in the browser: RevUpModal.tsx:218-219 `const isMinorLike = changeType === \"Minor\" || changeType === \"Correction\"; const mocRequired = (docClass === \"drawing\" || docClassUnknown) && !isMinorLike;` enforced at :261-266 `if (mocRequired && mocReference.trim().length < 3) return setError(...)`. checkinOutcomes.ts:291-302 validateCheckIn is likewise a pure client-side string-returning validator. The DB RPC validates the label and nothing else: 20260828_integrity_hardening.sql:138-141 `v_label := COALESCE(p_version->>'revision_label',''); IF btrim(v_label) = '' THEN RAISE EXCEPTION 'publish_revision: revision_label is required'; END IF;` while :147/:159 insert `moc_reference` as a plain `NULLIF(p_version->>'moc_reference','')`. revertToVersion (revisions.ts:1147-1180) validates only `if (!reason.trim()) throw new Error(\"Revert reason is required\")` and writes `moc_reference: mocReference?.trim() || null`; supersedeDocument (:1416-1462) validates only the reason and writes `supersession_moc: mocReference?.trim() || null`. Grep of supabase/*.sql for `moc` finds only column definitions and the search-tsv trigger — no CHECK, no trigger, no policy.",
    "note": "Fully confirmed, and the bypass is broader than the finding claims: publish_revision is SECURITY DEFINER and gates only on active org membership (20260828:76-83 `IF NOT v_is_member THEN RAISE EXCEPTION`), never on library publish authority — so any active member, not just an authorized publisher, can RPC a drawing-class revision into existence with moc_reference NULL. CRITICAL stands."
  },
  {
    "id": "DCK-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/checkoutEpisodes.ts:616-626 `await supabase.from(\"checkout_sessions\").update({ status: \"checked_in\", ... }).eq(\"document_id\", input.documentId).eq(\"status\", \"active\");` — the result is not destructured at all, so no error is observable. :630-640 then runs `await supabase.from(\"documents\").update({ checked_out_by: null, checked_out_by_name: null, checked_out_at: null, checkout_note: null, current_lock_id: null, active_collaborators: [] }).eq(\"id\", input.documentId);` with no conditional and no `.or()` filter. The rejection comes from 20260901_db_hard_enforcement.sql:113-119 `IF OLD.status = 'active' AND NEW.status IS DISTINCT FROM OLD.status AND OLD.user_id::text <> auth.uid()::text AND NOT org_capability_allows(OLD.org_id, 'checkout.force_release', auth.uid()) THEN RAISE EXCEPTION`.",
    "note": "Verified the documents UPDATE is not itself blocked: documents_org_access permits any org member, documents_deny_write_guard only bites on ACL denies, and neither trg_document_publish_guard (current_version_id/status only) nor documents_guard_access (visibility/acl only) fires on lock columns. So the sessions stay active while the lock is cleared. The premise also holds — capabilityPolicy.ts:89-91 marks checkout.force_release critical (Admin unremovable) but DocCtrl can be narrowed out, while CheckoutStatusCell.tsx:238 still shows the button on a hardcoded role check."
  },
  {
    "id": "DCK-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/checkoutEpisodes.ts:136 `if (code === \"42P01\" || code === \"42703\" || code === \"PGRST204\" || code === \"PGRST205\") return true;` — no check that the missing identifier is actually `checkout_episodes` / `episode_id`. lib/checkoutEpisodes.ts:157 `let episodeSchemaMissing = false;` is module-level, and once set at :206, :264 or :386 it short-circuits :197, :229, :233, :291, :321, :341 and :381. `grep -rn resetEpisodeSchemaFlag lib components app` returns only its own definition at :160 and no call site.",
    "note": "Confirmed: the flag is a process/tab-lifetime latch with no production reset path, and the error classifier is over-broad (a bare 42703/PGRST204 for any column trips it). After it flips, ensureActiveEpisode returns null, new sessions get no episode_id and postEpisodeSystemMessage drops the episode tag, silently detaching everything from the register."
  },
  {
    "id": "DCK-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20261012_doc_class_and_checkin_outcomes.sql:47-51 `-- Field-verification attestations and outcome reporting query by document + outcome (\"who last verified this P&ID against the field?\") CREATE INDEX IF NOT EXISTS checkout_sessions_outcome_idx ON checkout_sessions (document_id, outcome) WHERE outcome IS NOT NULL;` — a repo-wide grep for `outcome` across lib/components/app finds the only consumer at components/documents/CheckoutHistoryPanel.tsx:150-151 / :293, inside an expand-on-click per-episode drawer. `grep -rn \"outcome_ref\\|outcomeRef\"` returns two hits: the write at lib/checkoutEpisodes.ts:466 and a comment at :145 — zero readers. lib/checkinOutcomes.ts:115 promises \"This is the drawing's accuracy record.\"",
    "note": "No query in the tree filters or orders by `outcome`, so the index built for \"who last verified this?\" has no user; outcome_ref is write-only. The only nuance: lib/projectExport.ts:41 and lib/inbox.ts:144 do `select(\"*\")` on checkout_sessions, so the columns leave the system in a raw project dump — neither reads or renders them, which does not change the finding."
  },
  {
    "id": "DCK-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/capabilityPolicy.ts:89-91 defines `checkout.force_release ... description: \"Release another user's active checkout. Enforced at the database, which reads this policy — widen or narrow freely.\"`, and 20260901_db_hard_enforcement.sql:113-119 does read it. But `grep -rn \"force_release\\|checkout\\.force\" --include=*.ts --include=*.tsx .` finds no `policyAllows`/`can()` call for it anywhere — the gates are components/documents/CheckoutStatusCell.tsx:238 `const canAdmin = userRole === 'Admin' || userRole === 'DocCtrl';` and components/documents/InspectorPanel.tsx:156 `const isController = activeRole === 'Admin' || activeRole === 'DocCtrl';`",
    "note": "Confirmed, and the contrast the finding draws is real: lib/holds.ts:86-105 has assertHoldCapability calling `policyAllows(policy, cap, role, extra, uid)` for holds.open/holds.release, so the pattern exists and was simply never applied to the checkout surfaces. Both the widening (delegation inert) and narrowing (button still shown, DB rejects — which is DCK-10's swallowed error) failures follow directly."
  },
  {
    "id": "DCK-14",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260729_checkout_episodes.sql:87-92 `CREATE POLICY checkout_episodes_org_update ON checkout_episodes FOR UPDATE USING (EXISTS (SELECT 1 FROM org_members WHERE ... status = 'active'));` with no WITH CHECK and no restriction to still-open rows; supabase/schema.sql:1103-1104 mirrors it. lib/checkoutEpisodes.ts:352-353 `.eq(\"id\", input.episodeId).eq(\"status\", \"active\")` is the only CAS — it protects against double-close, not against a live session. lib/checkoutEpisodes.ts:485 `const sessions = await fetchActiveSessions(input.documentId);` is read before the transition is computed at :495-499, and the documents clear at :522-524 is correctly guarded by `.or(\\`checked_out_by.is.null,checked_out_by.eq.${input.userId}\\`)` while the closeEpisode call at :529-534 is not.",
    "note": "Both halves hold. The RLS gap lets any active member PATCH a closed episode's status/close_reason/closed_by — the 'sealed record' has no immutability at the DB. And the race is asymmetric exactly as described: the documents update is protected by a CAS filter, the episode close is not, so an episode can be sealed 'checked_in' while a newcomer's session is still active on the same document."
  },
  {
    "id": "DCK-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/schema.sql:1093-1095 `CREATE POLICY \"checkout_sessions_org_access\" ON checkout_sessions FOR ALL USING (org_id IN (SELECT my_org_ids()));` — permissive, USING-only, no WITH CHECK. supabase/migrations/20260831_capability_policy_and_rails.sql:99-102 `DROP TRIGGER IF EXISTS trg_checkout_release_guard ON checkout_sessions; CREATE TRIGGER trg_checkout_release_guard BEFORE UPDATE ON checkout_sessions` (re-created identically as BEFORE UPDATE in 20260901_db_hard_enforcement.sql:109-121). The guard body only bites when `NEW.status IS DISTINCT FROM OLD.status`. lib/checkoutEpisodes.ts:806-818 `if (sessions.length === 0) { await db.from(\"documents\").update({ checked_out_by: null, ... }).eq(\"id\", documentId); }`",
    "note": "Confirmed by repo-wide search: `grep -rn checkout_sessions supabase/ --include=*.sql | grep -iE 'policy|trigger|delete|grant|revoke'` returns exactly one policy and one BEFORE UPDATE trigger — no DELETE policy, no RESTRICTIVE overlay, no REVOKE. DELETE therefore never reaches the guard, and non-status UPDATEs (expires_at, user_id, purpose) on another member's row pass the `NEW.status IS DISTINCT FROM OLD.status` predicate untouched; reconcileDocumentCheckoutState then clears the lock unconditionally once the rows are gone."
  },
  {
    "id": "DCK-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/schema.sql:1067-1069 `CREATE POLICY \"documents_org_access\" ON documents FOR ALL USING (org_id IN (SELECT my_org_ids()));`. The only RESTRICTIVE UPDATE overlay is 20260901_db_hard_enforcement.sql:152-163, which gates solely on `acl_index_denies(acl_index, org_id, auth.uid(), 'write' | 'editMetadata')`. The publish RPC's lock test reads that very column: 20260828_integrity_hardening.sql:99 `IF v_doc.checked_out_by IS NOT NULL AND v_doc.checked_out_by::text <> p_actor::text ...`",
    "note": "I enumerated every trigger/policy on `documents` (`grep -n 'ON documents' supabase/migrations/*.sql supabase/schema.sql`): documents_guard_access fires only on visibility/acl/acl_index changes, trg_document_publish_guard only when current_version_id or status advances, trg_documents_move_guard on moves, trg_documents_legal_hold_delete on delete. Nothing guards checked_out_by / checked_out_by_name / current_lock_id, so any active member — including a Viewer — can write them, and doing so also satisfies publish_revision's lock branch."
  },
  {
    "id": "DCK-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/documents/CheckoutFlowModal.tsx:368-381 `const { data: lockedRow } = await supabase.from(\"documents\").update({ checked_out_by: currentUser.uid, ... }).eq(\"id\", document.id!).or(...).select(\"id\").maybeSingle();` — the `error` field is never destructured. :383-386 `if (!lockedRow) { ... await postEpisodeSystemMessage({ ... text: \\`${userName} joined the checkout — ...\\` })` and :437-442 the toast \"Joined an active checkout\". The session was already inserted at lib/checkoutEpisodes.ts:731-733 / CheckoutFlowModal.tsx:329 with no ACL guard on checkout_sessions.",
    "note": "Confirmed: an ACL 'write' deny makes documents_deny_write_guard (RESTRICTIVE FOR UPDATE) filter the row out, so the update returns zero rows and NO error — indistinguishable from losing the CAS race. The join branch returns early (`setProcessing(false); return;`) without calling reconcileDocumentCheckoutState, leaving an active checkout_sessions row on a document whose checked_out_by is still NULL. Precondition (an ACL write-deny on the folder) narrows reachability but the swallowed-error/false-message mechanism is exactly as described."
  },
  {
    "id": "DCK-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/documents/[libraryId]/page.tsx:1186-1204 `const handleForceUnlock = async (docRecord) => { ... await forceReleaseDocument({...}) }` — no logCheckoutEvent anywhere in the function. It is the only thing wired to the Inspector button (InspectorPanel.tsx:765-773 `onClick={() => onForceUnlock(selectedDoc)}`, page.tsx:4221 `onForceUnlock={handleForceUnlock}`). By contrast CheckoutStatusCell.tsx:206-217 writes `type: \"FORCE_RELEASE\"` and only then, at :223, calls `await forceReleaseDocument(...)`. lib/checkoutEpisodes.ts:600 comments \"Callers write their own audit row.\"",
    "note": "Both halves verified. `grep -rn onForceUnlock` shows a single wiring, so there is no second audit-writing path for the Inspector drawer, and the popover's audit row is committed before the release is even attempted — a rejected or partially-failed release still leaves a FORCE_RELEASE record."
  },
  {
    "id": "DCK-6",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "20260713_document_publish_guard.sql:60-66 had `IF OLD.checked_out_by IS NOT NULL AND OLD.checked_out_by::text <> v_actor::text THEN RAISE EXCEPTION 'Document is checked out by another user...'`. 20260812_per_library_publish_authority.sql:135-137 replaces it: \"An authorized publisher MAY advance past a foreign checkout ... We deliberately do NOT block on checked_out_by here.\" Neither 20260816_owner_publish_access.sql:32-88 nor 20260822_review_completion_guard.sql:21-96 (the current definition, and the last `CREATE OR REPLACE FUNCTION enforce_document_publish_guard` in the tree) mentions checked_out_by. lib/documentGuards.ts:14-17 still says \"A defense-in-depth Postgres trigger (see the matching migration) enforces the same rule at the DB layer for any path that bypasses the lib.\"",
    "note": "The factual claim is exactly right — I confirmed 20260822 is the newest redefinition and it has no lock branch (the hold branch survives, so the comment is only half-stale). Severity lowered to MEDIUM because the removal was an explicit, documented product decision, not an oversight, and the residual gap is reachable only by someone who already holds library publish authority or effective ownership (the trigger still enforces that) — i.e. exactly the population the app itself lets override the lock via DCK-8's override-with-reason path. What genuinely survives is a defense-in-depth loss plus a misleading source comment."
  },
  {
    "id": "DCK-7",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "lib/projects.ts:1069-1078 `if (sweepErr) { ... if (isMissingOutcomeSchema(sweepErr)) {...} else { console.warn(\"[autoReleaseExpiredAdHoc] sweep update failed\", sweepErr); } }` — a check_violation from the release guard is logged and dropped; the function then proceeds to :1104-1117, inserting one \"Your ad-hoc checkout auto-released\" notification per row in `data` (the pre-update fetch), unconditionally. Called from the browser at app/(protected)/checkouts/page.tsx:64 `await autoReleaseExpiredAdHoc(activeOrgId);` and app/(protected)/documents/[libraryId]/page.tsx:354 `void import(\"@/lib/projects\").then((m) => m.autoReleaseExpiredAdHoc(activeOrgId))` inside a useEffect keyed on activeOrgId.",
    "note": "All three mechanics confirmed, including that `.in(\"id\", ids)` is one statement so a single guard rejection aborts the batch and the sweeper's own expired hold survives too. Lowered to MEDIUM because the finding's \"forever\" overstates it: app/api/cron/maintenance/route.ts runs the same sweep nightly with a service-role client, and the guard's first line is `IF auth.uid() IS NULL THEN RETURN NEW; END IF;` — so the stuck sessions and the duplicate notifications are bounded by ≤24h, not permanent."
  },
  {
    "id": "DCK-8",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "supabase/migrations/20260828_integrity_hardening.sql:41-42,52 declares `p_override_lock BOOLEAN DEFAULT FALSE` and at :98-101 `IF v_doc.checked_out_by IS NOT NULL AND v_doc.checked_out_by::text <> p_actor::text AND NOT (p_override_lock OR (p_force AND v_is_controller)) THEN RETURN ... 'locked_by_other'` — `p_override_lock` alone clears the branch, with no reason parameter, no capability test, and no audit insert anywhere in the function body (I read it through to `END; $$` at :199). lib/revisions.ts:543 `p_override_lock: lockedByOther` is computed in the browser; the reason check that should accompany it lives only client-side at lib/revisions.ts:242-243 and components/documents/RevUpModal.tsx:258-263.",
    "note": "The bypass is real: a hand-crafted `POST /rest/v1/rpc/publish_revision` with p_override_lock:true skips the lock with no recorded reason and no audit row. Lowered to MEDIUM because \"no authority test\" overstates it — the RPC's `UPDATE documents SET current_version_id = ...` still fires trg_document_publish_guard (SECURITY DEFINER does not change auth.uid()), which rejects anyone lacking per-library publish authority or effective ownership. The genuine defect is accountability (missing reason + missing audit for a lock override), not unauthorized publishing."
  },
  {
    "id": "DCK-9",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "components/projects/StaleCheckoutBanner.tsx:75-82 updates checkout_sessions with `{status, ended_at, released_at, released_by, released_reason}` — no `outcome`. lib/projects.ts:389-398 (releaseAllCheckoutsForProject) writes the same five fields, no `outcome`. lib/checkoutEpisodes.ts:616-626 (forceReleaseDocument) likewise. CheckoutHistoryPanel.tsx:293 `const oc = s.outcome ? OUTCOME_LABEL[s.outcome] : null;` renders nothing when null. `grep -rn CHECKOUT_RELEASED --include=*.ts --include=*.tsx` returns exactly one hit — StaleCheckoutBanner.tsx:84, the writer; no reader anywhere. app/(protected)/activity/page.tsx:195 buckets `[\"CHECK_OUT\",\"CHECK_IN\",\"ABANDON\",\"FORCE_RELEASE\",\"DOCUMENT_CHECKOUT\",\"DOCUMENT_CHECKIN\"]` as locks — CHECKOUT_RELEASED is absent.",
    "note": "The load-bearing half is correct: three release paths write no outcome, so the register line is blank, and CHECKOUT_RELEASED has zero readers. Lowered to MEDIUM because \"an audit action nothing renders\" is overstated — lib/timeline.ts:296 `default: return r.action.replace(/_/g, \" \").toLowerCase();` does render it as \"checkout released\"; the defect is that it is mis-bucketed in the activity pulse, not invisible. Also note lib/projects.ts:1068 (the sweep, one of the cited locations) *does* write `outcome: \"auto_released\"`, so the count of genuinely-blank paths is stale-banner / project-bulk / force-release."
  },
  {
    "id": "DELIV-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260605_rls_policies_new_tables.sql:122-124 `CREATE POLICY \"email_notif_insert\" ON email_notifications FOR INSERT TO authenticated WITH CHECK (EXISTS (SELECT 1 FROM org_members WHERE org_id = email_notifications.org_id AND uid = auth.uid() AND status = 'active'));` — the only predicate is active membership; nothing constrains to_email/subject/body_html or to_user_id. send-queued/route.ts:146-152 posts `from: fromEmail, to: row.to_email, subject: row.subject, html: row.body_html` straight to Resend.",
    "note": "Confirmed: that is the only policy on the table in any migration (no UPDATE/SELECT/DELETE, no trigger, no CHECK). Partial mitigation worth recording: lib/transmittals.ts:281 already lets ANY active member (app/(protected)/transmittals/page.tsx does no role gating) queue external mail from the same sender via queueExternalEmail, so the delta an attacker gains is full control of body_html/subject rather than the ability to mail outsiders at all. Still HIGH — arbitrary HTML from the org's verified domain, with no audit row."
  },
  {
    "id": "DELIV-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "hooks/useTicketNotifications.ts:176 `listMyNotifications({ onlyUnread: true, limit: 50, orgId: activeOrgId })` feeding lib/inAppNotifications.ts:160-164 `.order(\"created_at\", { ascending: false }).limit(opts?.limit ?? 50)`; line 312 `count: items.length` — the badge counts only what was fetched. Repo-wide grep for `countUnread` returns exactly one hit, its own definition at lib/inAppNotifications.ts:176 — zero call sites.",
    "note": "Both halves check out: the cap is a hard 50 with DESC ordering (oldest unread fall off), the bell/NotificationCenter (components/notifications/NotificationCenter.tsx:76-80 `all: items.length`) consume the same truncated array, and countUnread is dead. Worth noting lib/inbox.ts:163-165 does compute a true exact unread count, so the cockpit number and the bell number can disagree once past 50 — which reinforces rather than refutes the finding."
  },
  {
    "id": "DELIV-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/cron/maintenance/route.ts:110-112 `const batch = body?.sent ?? body?.processed ?? 0; drained += batch; if (batch === 0) break;` — on an all-fail batch send-queued returns `{ processed: N, sent: 0, failed: N }` (route.ts:186), and 0 is not nullish so `??` keeps it, the loop breaks and result.errors stays empty because res.ok was true. The unconfigured path returns `sent: 0` too (route.ts:75-82).",
    "note": "Correct — `notificationsDrained: 0, errors: []` is byte-identical for 'queue empty', 'all sends failed' and 'RESEND_API_KEY unset', and the `configured:false`/`failed` fields the route does return are never read. The identical bug sits in the 6c re-drain at line 212. MEDIUM stands."
  },
  {
    "id": "DELIV-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/settings/notifications/page.tsx:40 `digest_frequency: \"immediate\",` in DEFAULTS and line 86 `.upsert({ user_id: uid, ...prefs }, { onConflict: \"user_id\" })`; supabase/schema.sql:660 and 20260529_phase_b_notifications.sql:77-78 `digest_frequency TEXT NOT NULL DEFAULT 'instant' CHECK (digest_frequency IN ('instant','hourly','daily','never'))`.",
    "note": "Confirmed by repo-wide grep: 'immediate' appears only in the page (lines 30/40/69/151) and 'instant' only in the two schema definitions — no migration ever widens the CHECK, and no mapping layer translates the two. First save from the default state raises 23514 and is surfaced raw via `setError((e as Error).message)` (line 91). The user can only save by first selecting Hourly/Daily/Never."
  },
  {
    "id": "DELIV-13",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "20260723_notifications_unify.sql:39 `CREATE POLICY notifications_own_update ON notifications FOR UPDATE USING (user_id = auth.uid());` (no WITH CHECK) and :41 `… FOR DELETE USING (user_id = auth.uid());` — same pair at 20260621:44-46 and 65-67.",
    "note": "The mechanism is real (title/body/kind/created_at/read_at are all rewritable; the row is deletable — though note Postgres reuses USING as the check when WITH CHECK is omitted, so user_id itself cannot be re-pointed at someone else, and the DELETE policy is a documented 'clear all' affordance). The consequence is overstated, which is why LOW: the obligation lives in its own table, not in notifications — lib/acknowledgments.ts:603 sets the nag cooldown on `document_acknowledgments.notified_at`, so the daily scan re-notifies after the cooldown regardless, and lib/inbox.ts builds the cockpit's pending acks/reviews/recerts from listMyPendingAcks/listMyPendingReviews/listMyDueRecerts, not from the deleted row. Only the 25-hour digest window and the bell entry are actually evadable."
  },
  {
    "id": "DELIV-14",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "public/sw.js:226 `self.addEventListener(\"push\", (event) => {` … :238 `event.waitUntil(self.registration.showNotification(title, options));`; supabase/migrations/20260804_push_subscriptions.sql:7 `create table if not exists push_subscriptions (`; supabase/schema.sql:659 `push_enabled BOOLEAN NOT NULL DEFAULT TRUE,`; lib/notify/dispatch.ts:20 `export type NotifChannel = \"inapp\" | \"email\";`. Repo-wide grep for `pushManager|vapid|web-push|applicationServerKey` returns hits ONLY in supabase/migrations/20260804_push_subscriptions.sql, supabase/REMEDIATION_APPLY_ALL.sql, lib/schemaExpectations.ts, lib/exportTables.ts, lib/dataRestore.ts, docs/ and audit-reports/ — zero in any component, hook, route or lib that performs a subscribe or a send. No file under app/api/ references push_subscriptions; package.json has no web-push dependency.",
    "note": "Confirmed on every leg: the SW receiver, the table+RLS, and the preference column all exist, and nothing anywhere calls pushManager.subscribe() or sends a push. The only in-session signal is indeed the toast at components/providers/NotificationListener.tsx:92."
  },
  {
    "id": "DELIV-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260605_rls_policies_new_tables.sql:112-115 `CREATE POLICY \"notif_prefs_own\" ON notification_preferences FOR ALL TO authenticated USING (user_id = auth.uid())`; lib/notifications.ts:53-61 selects `notification_preferences … .eq(\"user_id\", input.toUserId).maybeSingle()` then `if (prefs?.email_enabled === false) return;` — under the actor's session the recipient's row is invisible, so prefs is null and shouldSendForEvent(null) returns true (line 153).",
    "note": "Holds, and the scope qualifier is right: lib/supabase.ts:110/136 hands the browser the anon client, and every emit() producer (postPublish, holds, projects, workPackages, branches, transitionIn, revisionImpact, CheckInPanel, requests/new) runs client-side. The server paths are genuinely exempt — app/api/tickets/comment/route.ts:283-300 and workflow-action/route.ts:349-367 re-read prefs with supabaseAdmin, and the cron swaps in the service-role client (lib/supabase.ts:123) — so the finding does not overreach."
  },
  {
    "id": "DELIV-3",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "hooks/useTicketNotifications.ts:100-101 `default: return 'other';` — ack_requested/ack_overdue/review_due/review_overdue/retention_eligible/access_recert_due/effective_now/storage_alert all fall through; line 301 `tally(section, false);` increments sectionCounts.other; Sidebar.tsx reads only `sectionCounts.documents` (229), `sectionCounts.projects` (231) and `sectionCounts.requests` (235). Repo-wide grep for `sectionCounts` finds no consumer of `.other`.",
    "note": "Mechanically exact — the 'other' bucket is computed and never rendered. Severity is too high at HIGH: those same rows ARE in items[] (line 285-300), so the header bell lists them with their deep link, and the /inbox cockpit derives the underlying obligations from their own tables (lib/inbox.ts calls listMyPendingAcks/listMyPendingReviews/listMyDueRecerts/listMyDueReviews) rather than from notifications. What is lost is the per-section badge, not the trail."
  },
  {
    "id": "DELIV-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "email_notifications has RLS on with a single FOR INSERT policy (20260605_rls_policies_new_tables.sql:120-124 — confirmed the only policy on the table in every .sql in supabase/). admin/settings/page.tsx:67-73 counts with the browser anon client `supabase.from(\"email_notifications\").select(\"id\", { count: \"exact\", head: true })…` → 0 rows visible, no error → line 274-276 `{failedEmails === 0 && (… <CheckCircle2 …/> No failed deliveries.)}` always renders.",
    "note": "Correct, and worse than stated: the remedy button is dead too — requeueFailed (lines 92-94) issues an UPDATE against a table with no UPDATE policy, so it silently affects zero rows and then re-reads the same blind 0. HIGH is right for a green 'No failed deliveries' badge that is a constant, not a measurement."
  },
  {
    "id": "DELIV-5",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "app/api/tickets/comment/route.ts:264 `const link = `/requests/${ticketId}?c=${comment.id}`;` used at 310-317 in `body_text` and `<p><a href=\"${link}\">Open ticket</a></p>`; workflow-action/route.ts:313 `const link = `/requests/${ticketId}`;` used identically at 387-393. Repo-wide grep: publicOrigin() has 8 callers (viewers, docPack, share/file, physicalBridge, downloads) and none is an email route; ticketUrl (lib/notifications.ts:248) appears only in lib/__tests__/notificationsLib.test.ts:16,95.",
    "note": "Factually exact — every ticket email ships a root-relative href that cannot resolve in a mail client. Downgraded to MEDIUM: the mail still carries the actor, the ticket label and the comment/status text, and the recipient reaches the ticket by opening the app; there is no data loss or security consequence, only a broken call-to-action."
  },
  {
    "id": "DELIV-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260723_notifications_unify.sql:45-52 `CREATE POLICY notifications_org_insert ON notifications FOR INSERT WITH CHECK (EXISTS (SELECT 1 FROM org_members … AND uid = auth.uid() AND status = 'active'))` — no constraint on user_id, kind, title, link or actor_name (identical text at 20260621:52-61). NotificationListener.tsx:92-97 renders `title: row.title, message: row.body ?? \"\"` as a toast for the recipient.",
    "note": "Confirmed; the migration comment itself concedes it ('The kind/body are validated at the app layer' — no such validation exists on the write path). Nothing sanitizes link, so an off-app https:// URL is rendered by the bell exactly like an in-app one; actor_name 'System' is the same string the cron uses (app/api/cron/maintenance/route.ts:352)."
  },
  {
    "id": "DELIV-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/inAppNotifications.ts:94-97 `if (error) console.warn(\"[notify] insert failed\", error.message); } catch (e) { console.warn(\"[notify] insert threw\", e); }` — never rethrown, never retried; lib/postPublish.ts:171 `void notifyMany({…})` inside a `catch { /* pre-migration env / non-blocking */ }`; lib/projects.ts:1116-1118 `if (inserts.length > 0) await db.from(\"notifications\").insert(inserts);` with the result discarded; maintenance/route.ts:423-437 `await sb.from(\"email_notifications\").insert({…}); queued += 1;` — the counter reported as complianceEmails is incremented without inspecting the insert result.",
    "note": "The substance holds — no notification write anywhere retries, and repo-wide there is no dead-letter path for them. Two citations are off: maintenance/route.ts:355-356 actually DOES check (`const { error: insErr } = …; if (!insErr) escalated += 1;`), it just never surfaces the failure into result.errors; and lib/notify/dispatch.ts:83-84 is `resolveRecipients`/empty-guard, not an error swallow (the swallowing is one level down in notify()). MEDIUM is right."
  },
  {
    "id": "DELIV-8",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "send-queued/route.ts:90-94 `.update({ status: \"queued\", … }).eq(\"status\", \"suppressed\").gte(\"created_at\", new Date(Date.now() - 7 * 24 * 60 * 60 * 1000).toISOString())`; app/api/admin/purge/route.ts:70 and :161 `table === \"email_notifications\" ? base.in(\"status\", [\"sent\", \"suppressed\"])`, justified at line 42 as 'Outbound emails already sent or suppressed. The delivery is done' — which is false for suppressed rows, which were never delivered.",
    "note": "Accurate as written, but repo-wide grep shows NO current code path ever writes status 'suppressed' (only the recovery filter, the purge filter and the type union in types/schema.ts:1071 mention it) — the current route explicitly defers instead (lines 65-83). So the loss can only touch rows created by a build that no longer exists in this repo; the live defect is the purge misclassifying an undelivered state as delivered. LOW."
  },
  {
    "id": "DELIV-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/notifications.ts:67-75 `const { data: dupes } = await supabase.from(\"email_notifications\").select(\"id\").eq(\"to_user_id\", …)…; if (dupes && dupes.length > 0) return;` against a table whose only policy is FOR INSERT (20260605:122-124) — a SELECT under the actor's session returns an empty set with no error, so the guard can never trip from the browser.",
    "note": "Confirmed by the same RLS fact that carries DELIV-1/2/4: no SELECT policy exists on email_notifications in any migration, and the browser client is the anon client (lib/supabase.ts:110). The dedupe only functions where the shared client has been swapped to service role (the cron, lib/supabase.ts:123). dispatch.ts:67-79 does dedupe recipients, so the residual duplication is across independent producers, exactly as the finding scopes it."
  },
  {
    "id": "DIST-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/revisions.ts:1414-1537 (supersedeDocument) — I read the whole function: it notifies the checkout holder (:1502-1522) and open work packages via notifyPackagesOfRetirement (:1526-1534), and calls nothing from lib/staleCopies. `grep -rn \"nudgeStaleHolders\\|listRecallHolders\\|DistributionRecall\"` shows the only caller is the manual button in components/documents/DistributionRecall.tsx:51. lib/staleCopies.ts:76 `if (d.status === \"Archived\" || d.status === \"Superseded\" || d.status === \"Void\") continue;` drops the retired document from the holder's own \"my outdated copies\" list.",
    "note": "Both halves confirmed, and the manual escape hatch is inert too: getDocumentRecall (lib/staleCopies.ts:143-160) computes `hasCurrent: r.version_id === currentVersionId`, and supersedeDocument never changes current_version_id — so after retirement everyone who pulled the final rev reads hasCurrent=true and nudgeStaleHolders' `holders.filter((h) => !h.hasCurrent)` returns an empty set (0 nudged). The only rationale I can construct for the line-76 filter is that 'go re-download the current rev' is meaningless for a retired doc, but the effect is that the most dangerous copy in circulation is the one the system stops mentioning."
  },
  {
    "id": "DIST-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/staleCopies.ts:182-209 nudgeStaleHolders is one `await emit({...})` and `return outdated.length` — no logAuditAction, no insert into any recall table, no ack rows. components/documents/DistributionRecall.tsx:29 `const [nudgedCount, setNudgedCount] = useState<number | null>(null);` with :36 `setNudgedCount(null)` inside the useEffect keyed on `[documentId, currentVersionId]`, and :107 renders `Recall sent to {nudgedCount} … people` from that state alone.",
    "note": "Confirmed: the count is pure component state and is cleared on every remount/document switch, and no persistent recall entity is created. One softening: emit() does leave rows in `notifications` carrying `metadata: { recall: true }` (lib/staleCopies.ts:206) plus resource id, so a forensic query against that table can partially reconstruct who was pinged and when — \"the system can answer none of it\" overstates slightly. There is still no acknowledgment of destruction and no recall record proper, so MEDIUM stands."
  },
  {
    "id": "DIST-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/staleCopies.ts:20 `const RECALL_WINDOW_DAYS = 60;` and :131-141 `.gt(\"created_at\", since) ... .order(\"created_at\", { ascending: false }).limit(400)` with no count/truncation signal in the returned RecallHolder[]. The sibling honesty is real: components/documents/InspectorPanel.tsx:188-189 `const issuedCapped = list.length >= 50;` rendered at :567 as `${distSummary.issued}${distSummary.issuedCapped ? \"+\" : \"\"} issued`. DistributionRecall.tsx:73-75 renders a flat `{outdated.length} of {holders.length} outdated`.",
    "note": "Confirmed — both caps are silent and the component has no way to know it was truncated. Precision note: because the query is `created_at DESC LIMIT 400`, the 70-day-old downloader in the scenario is dropped by the 60-day window (:130), not by the row cap; the row cap independently drops earlier holders once a doc exceeds 400 pulls in 60 days. Both truncations are real and neither is surfaced."
  },
  {
    "id": "DIST-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/distributionAcks.ts:112-114 `requested_at: now` in every upserted row, then :122-124 `.upsert(rows, { onConflict: \"version_id,recipient_user_id\", ignoreDuplicates: false })` — a re-request rewrites requested_at on the existing row. The escalation clock reads exactly that column: :274 `const ageDays = (Date.now() - Date.parse(r.requested_at as string)) / 86_400_000; if (ageDays >= escalateAfterDays ...)` (escalateAfterDays default 10, :205). Handlers: DistributionAcks.tsx:83-97, :101-109, :112-124 are all `try { ... } finally { setBusy(false); }` with no catch, invoked as `onClick={() => void handleRequest()}` (:143, :208, :218).",
    "note": "Confirmed. openPicker (:70-79) loads all active members minus self and does not exclude people who already have an outstanding row, so re-ticking the same names is the frictionless path; each `void handleX()` rejection becomes an unhandled promise rejection with no error state rendered anywhere in the component. Mild mitigation the finding does not mention: a separate \"Remind the unconfirmed\" button exists (:216-222) and calls renudgeUnacked, which does NOT touch requested_at — so the destructive path is reachable but not the only one."
  },
  {
    "id": "DIST-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/staleCopies.ts:195-196 `await emit({ orgId: input.orgId, category: \"status\", kind: \"doc_superseded\", ...})`; lib/notify/dispatch.ts:52 `case \"status\": return \"ticket_status_changed\";`; lib/notifications.ts:161-163 `case \"ticket_status_changed\": case \"ticket_approved\": case \"ticket_revision_requested\": case \"ticket_closed\": return prefs.email_on_status_change !== false;`.",
    "note": "Confirmed end to end: the stale-copy recall email is gated by the ticket-status-change preference, so anyone who muted ticket churn silently loses the recall email and is left with only a bell badge. The contrast cited is also accurate — lib/distributionAcks.ts:136 uses `category: \"assignment\"`, which maps to email_on_assignment, a toggle a field user is far less likely to have disabled."
  },
  {
    "id": "DIST-14",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "supabase/migrations/20260818_followups_rls.sql:95-96 `CREATE OR REPLACE FUNCTION bump_share_access(p_share uuid) RETURNS void LANGUAGE sql SECURITY DEFINER AS $$ UPDATE document_shares ...` and :107-108 `CREATE OR REPLACE FUNCTION revup_rollback_orphan(p_version uuid, p_prev uuid) RETURNS void LANGUAGE plpgsql SECURITY DEFINER AS $$ BEGIN DELETE FROM document_versions ...` — neither carries `SET search_path`. The house pattern is documented at supabase/migrations/20261013_project_controls_program.sql:56-58: \"search_path pinned per the house SECURITY DEFINER pattern\" / `... SECURITY DEFINER SET search_path = public AS $$`. No later migration redefines either function (grep finds only these two definitions).",
    "note": "The code facts are exactly as stated, but the severity is inflated on two counts. (1) Exploitation needs a principal who can CREATE objects in a schema ahead of public on their own search_path; Supabase's `authenticated`/`anon` roles have no CREATE right on public and cannot create schemas, so there is no in-product attacker. (2) The 'against the documented house pattern' framing implies these two are outliers — `grep -rn 'SECURITY DEFINER' supabase/migrations/*.sql | grep -v search_path` returns 42 definitions, so unpinned is in fact the majority convention. Neither function makes an authorization decision from the unqualified table (one increments a counter, one deletes a version row), so a shadowed table yields no privilege escalation. Real hardening debt, LOW."
  },
  {
    "id": "DIST-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/verify/route.ts:88 `const docRetired = d.status === \"Superseded\" || d.status === \"Archived\";` / :89 `const isCurrent = !docRetired && (!versionId || versionId === d.current_version_id);` — while types/schema.ts:613 declares `export type DocumentStatus = \"Draft\" | \"Issued\" | \"Superseded\" | \"Void\" | \"Archived\" | \"Locked\";` and every other consumer includes Void: lib/aiBoundary.ts:25 `new Set([\"Superseded\", \"Void\", \"Archived\"])`, lib/staleCopies.ts:76 `if (d.status === \"Archived\" || d.status === \"Superseded\" || d.status === \"Void\") continue;`, lib/downloads.ts:62-63 `case \"Void\": return { label: \"Void\", tone: \"danger\" };`, lib/docControlRegister.ts:101 `.or(\"status.is.null,status.not.in.(Draft,Superseded,Void,Archived)\")`.",
    "note": "Confirmed. \"Void\" is a first-class status set from MetadataEditor.tsx:9 without minting a new version, so current_version_id still equals the QR's `v` (stamped as `?v=${doc.currentVersionId}` at lib/downloads.ts:100, lib/docPack.ts:105, app/api/share/file/route.ts:115). isCurrent evaluates true and app/verify/[docId]/page.tsx:64 paints `bg-emerald-600` with the headline \"CURRENT\". No guard elsewhere in the route or page compensates; the page trusts the API's isCurrent verbatim."
  },
  {
    "id": "DIST-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260825_work_packages_acks.sql:135-139 `CREATE POLICY distribution_acks_org_update ON distribution_acks FOR UPDATE USING (EXISTS (SELECT 1 FROM org_members WHERE org_members.org_id = distribution_acks.org_id AND org_members.uid = auth.uid() AND org_members.status = 'active'));` — no recipient predicate, and no WITH CHECK (so Postgres reuses USING for the check). lib/distributionAcks.ts:186-192 `acknowledge(ackId)` filters only `.eq(\"id\", ackId).is(\"acknowledged_at\", null)`.",
    "note": "Repo-wide search confirms the absence claim: `grep -rn distribution_acks supabase/ | grep -i policy` returns only the three 20260825 policies; supabase/schema.sql:1294-1309 defines the table and indexes but no policy, and no later migration hardens it. The sibling tables were hardened in 20260828_integrity_hardening.sql (doc_review_signoff_update at :229-241 and doc_ack_update at :264-277 both carry `reviewer_user_id = auth.uid()` / `assignee_user_id = auth.uid()`); distribution_acks was skipped, exactly as claimed."
  },
  {
    "id": "DIST-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Nothing anywhere writes acknowledged_at or deletes rows on supersession — `grep -rn distribution_acks lib/postPublish.ts lib/revisions.ts` returns nothing; lib/impact.ts:186-192 only *counts* them. Recipient side is version-pinned: lib/distributionAcks.ts:83-87 `getMyPendingAck` does `.eq(\"version_id\", versionId).eq(\"recipient_user_id\", userId)`, and components/documents/DistributionAcks.tsx:54 calls it with `currentVersionId`. The inbox side is not: lib/distributionAcks.ts:158-163 `listMyPendingDistributionAcks` filters only org_id + recipient + `.is(\"acknowledged_at\", null)`.",
    "note": "Confirmed and the loop is genuinely closed against the recipient: app/(protected)/inbox/page.tsx:422 renders only a Link to `/documents/${a.libraryId}?doc=${a.documentId}` (no inline confirm button), which opens the doc at Rev 5, where getMyPendingAck(Rev5) returns null so the confirm bar never renders. lib/docControlRegister.ts:117 counts every unacknowledged row regardless of version, so the register's \"N unconfirmed\" pill is permanently inflated, and scanDistributionAcks (lib/distributionAcks.ts:202-216) has no currency filter either, so the cron nags the orphan rows forever."
  },
  {
    "id": "DIST-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/acknowledgments.ts:79 `.from(\"org_members\").select(\"uid, display_name, email, role\").eq(\"org_id\", orgId).eq(\"status\",\"active\").in(\"role\", roles)` — headline column only. Contrast the notify path, lib/notify/recipients.ts:56-59: `const held = m.roles && m.roles.length > 0 ? m.roles : m.role ? [m.role] : []; if (held.some((r) => want.has(r))) out.add(m.uid);`. The additive column exists: supabase/migrations/20260722_member_roles_collection.sql:12-13 `ALTER TABLE org_members ADD COLUMN IF NOT EXISTS roles TEXT[] NOT NULL DEFAULT '{}'` with the comment \"org_members.role mirrors the highest-ranked role here\".",
    "note": "Confirmed, including the no-warning half. lib/acknowledgments.ts:85 `for (const role of roles) if (!covered.has(role)) warnings.push(...)` fires only when a role has ZERO headline holders; a role held additively by six people and headline by two produces `covered.has(role) === true`, so the ack_unsatisfiable notice at :392-405 never fires and the partial roster is silent."
  },
  {
    "id": "DIST-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260623_document_shares.sql:38 `CREATE POLICY document_shares_org_member ON document_shares FOR ALL USING (... org_members ... status = 'active') WITH CHECK (same)` — membership is the only predicate, and it is the ONLY policy on the table repo-wide. components/documents/InspectorPanel.tsx:575 `<button onClick={() => setShareOpen(true)}>` sits behind no role check (only `selectedDoc.id && selectedDoc.orgId && uid`). ShareLinkModal.tsx:32 `{ label: \"Never expires\", days: 0 }` → lib/documentShares.ts:44 `input.expiresInDays === 0 ? null : ...` writes expires_at NULL, and app/api/share/resolve/route.ts:39 only rejects when `share.expires_at &&` it is past.",
    "note": "Confirmed on every limb. lib/documentShares.ts:33-56 createShareLink writes the row and returns — no logAuditAction, no logRevisionEvent, no notify; grep for document_shares finds no cron/sweeper and no org-wide listing (listShareLinks at :58-65 is `.eq(\"document_id\", ...)` only, i.e. per-document). The ACL-bypass limb is real but narrower than stated: 20260708_acl_rls_enforcement.sql:11-14 leaves visibility 'normal'/NULL visible to all org members, so the bypass bites only private/hidden docs and requires the attacker to already know the document UUID."
  },
  {
    "id": "DIST-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/share/file/route.ts:130-140 inserts `{ org_id, document_id, version_id, user_id, user_email, created_at, expires_at, watermark_policy_id, source: stamped ? \"share_link\" : \"share_link_unstamped\" }` into download_audits. supabase/schema.sql:789-799 is the only definition of that table and its columns end at `watermark_policy_id UUID` — there is no `source`. `grep -rn download_audits supabase/` returns hits ONLY in schema.sql (789, 1022, 1090): no migration adds the column.",
    "note": "Confirmed. PostgREST rejects the unknown column, and the call is `await sb.from(...).insert(...)` inside `try { } catch { /* pre-migration column drift — never block the share */ }` — supabase-js returns the error rather than throwing, so the catch never even runs and the discarded `{ error }` is never inspected. Every share download therefore writes zero rows, and lib/staleCopies.ts:132-140 getDocumentRecall reads download_audits exclusively, so outside holders are invisible to recall."
  },
  {
    "id": "DIST-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260828_integrity_hardening.sql:264-277 (and its successor 20260830_publisher_row_management.sql:54-71): USING carries `assignee_user_id = auth.uid() OR ...Admin/DocCtrl... OR ...effective owner...`, but `WITH CHECK (EXISTS (SELECT 1 FROM org_members WHERE org_id = document_acknowledgments.org_id AND uid = auth.uid() AND status = 'active'))` — bare membership. Nothing pins status or assignee_user_id on the new row.",
    "note": "Confirmed, and the downstream consequences check out: lib/acknowledgments.ts:172 `else if (st === \"waived\") { s.waived++; }` increments neither required nor pending, so ackStatusFor (:216) returns \"complete\" once the last pending row self-waives; and lib/downloads.ts:203-206 assertAckGate matches only `.eq(\"status\", \"pending\")`, so a waived row walks through the hard gate. The legitimate path (waiveAcknowledgment, lib/acknowledgments.ts:478-490) writes waived_by/waived_reason and logs ACK_WAIVED, none of which the WITH CHECK requires. Note DRLS-1 makes this moot in the deployed state — the surviving document_acknowledgments_member_all policy grants the write outright."
  },
  {
    "id": "DIST-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/schema.sql:1090-1091 `CREATE POLICY \"download_audits_org_access\" ON download_audits FOR ALL USING (org_id IN (SELECT my_org_ids()));` — FOR ALL, no WITH CHECK (so USING is reused for INSERT/UPDATE), no role predicate, no own-row predicate. supabase/schema.sql:1031-1034 `my_org_ids()` = `SELECT org_id FROM org_members WHERE uid = auth.uid() AND status = 'active'`. `grep -rn download_audits supabase/migrations/` returns nothing — no later migration constrains it.",
    "note": "Confirmed. Contrast the sibling three lines above it, supabase/schema.sql:1086-1087, where audit_logs gets a separate `audit_logs_insert ... WITH CHECK (user_id = auth.uid())`; download_audits got no such treatment. Since lib/staleCopies.ts:33-113 and :125-180 derive both the personal stale-copy list and the per-document recall roster entirely from this table, a self-targeted DELETE erases the holder from the recall evidence base with no trace."
  },
  {
    "id": "DRLS-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "The DROPs name the wrong policies. 20260819_orphan_tables_backfill.sql:231-236 creates them by a GENERATED name — `EXECUTE format('DROP POLICY IF EXISTS %I ON %I', t || '_member_all', t)` then `CREATE POLICY %I ON %I FOR ALL TO authenticated USING (...org_members...active...) WITH CHECK (...)` with `t` drawn from ARRAY['...','document_acknowledgments', ..., 'document_review_signoffs', ...] — i.e. the policies are named `document_acknowledgments_member_all` and `document_review_signoffs_member_all`. The hardening migration drops different names: 20260828_integrity_hardening.sql:211 `DROP POLICY IF EXISTS \"doc_review_signoff_member_all\" ON document_review_signoffs;` and :250 `DROP POLICY IF EXISTS \"doc_ack_member_all\" ON document_acknowledgments;` — those are the 20260817/20260818 names (20260817_read_understood.sql:84-85, 20260818_review_before_publish.sql:91-92).",
    "note": "Confirmed, and the naming collision is the whole mechanism: `grep -rn member_all supabase/` shows the only DROPs in existence are for the doc_ack_/doc_review_signoff_ names, so the 20260819 generated-name policies survive untouched to this day. RLS permissive policies OR together, so `document_acknowledgments_member_all FOR ALL TO authenticated USING(active member) WITH CHECK(active member)` grants exactly the UPDATE and DELETE that doc_ack_update/doc_ack_delete and doc_review_signoff_update/doc_review_signoff_delete (20260828:264-281, 20260830:33-71) were written to deny. Any active member — Viewer included — can PATCH another engineer's acknowledgment row or DELETE review sign-offs. CRITICAL is correct."
  },
  {
    "id": "DRLS-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260825_work_packages_acks.sql:71-79 `CREATE POLICY work_packages_org_update ON work_packages FOR UPDATE USING (EXISTS (SELECT 1 FROM org_members ... status = 'active'));` and the matching `work_packages_org_delete` — any member may close or delete a package; 20260828_integrity_hardening.sql:285-292 `CREATE POLICY work_package_documents_org_update ON work_package_documents FOR UPDATE USING (EXISTS (SELECT 1 FROM org_members ...)) WITH CHECK (same)` — any member may move a pin. Staleness is a pure read-time comparison with no protected state: lib/workPackages.ts:106 `drifted: !!pinned && !!current && pinned !== current` and :119 `staleCount: docs.filter((d) => d.drifted).length`. refreshWorkPackage (lib/workPackages.ts:193-219) re-pins every member row to `d.current_version_id`, so one call by any member zeroes staleCount.",
    "note": "Confirmed end to end — the tripwire is derived state with no authority gate on either input. The 20260828 comment ('Refresh pack ... silently matched zero rows from the browser') shows the UPDATE policy was widened to all members specifically to make the button work, with no narrower role considered."
  },
  {
    "id": "DRLS-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Each cited definition read and confirmed to lack SET search_path: schema.sql:1031-1034 `CREATE OR REPLACE FUNCTION my_org_ids() RETURNS SETOF UUID LANGUAGE SQL SECURITY DEFINER AS $$ SELECT org_id FROM org_members WHERE uid = auth.uid() AND status = 'active'; $$;`; also doc_is_visible (20260813:34-40), my_project_ids (20260813:58-61), is_org_controller (20260814:31-40), my_team_ids (20260707:52-55), bump_share_access (20260818:95-96), can_manage_project (20260818:10-11), is_org_assign_drafters (20260818:23-24), is_org_admin (20260713:11-12), is_org_admin_or_manager (20260817:21-22). Grep for every one of those function names across supabase/ returns exactly one CREATE each — no later ALTER FUNCTION or re-definition repairs them. The house pattern is demonstrably otherwise: 20260901_db_hard_enforcement.sql:109 writes `SECURITY DEFINER SET search_path = public` on enforce_checkout_release_guard, and schema.sql:1068-1069 confirms my_org_ids is indeed the sole predicate of documents_org_access.",
    "note": "Every cited line checks out, including the claim that my_org_ids underpins the documents and document_versions policies (schema.sql:1068-1073). MEDIUM is the right level: exploitation additionally requires a principal with CREATE on a schema ahead of public in its search_path, which is not established here — the finding itself concedes that, and treats it as a house-pattern/defence-in-depth gap."
  },
  {
    "id": "DRLS-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260901_db_hard_enforcement.sql:109-121 redefines enforce_checkout_release_guard, but the only trigger binding it is 20260831_capability_policy_and_rails.sql:99-102 `DROP TRIGGER IF EXISTS trg_checkout_release_guard ON checkout_sessions; CREATE TRIGGER trg_checkout_release_guard BEFORE UPDATE ON checkout_sessions FOR EACH ROW EXECUTE FUNCTION enforce_checkout_release_guard();` — BEFORE UPDATE only, and the guard body is written entirely in terms of OLD.status/NEW.status so it has nothing to say about a DELETE. Grep of supabase/ for checkout_sessions policies/triggers returns only that trigger plus an unrelated resync trigger; the sole policy remains schema.sql:1094-1095 `CREATE POLICY \"checkout_sessions_org_access\" ON checkout_sessions FOR ALL USING (org_id IN (SELECT my_org_ids()));`, whose FOR ALL includes DELETE with no restrictive overlay. The downstream lock check the finding names is real: 20260828_integrity_hardening.sql:94-95 `IF v_doc.checked_out_by IS NOT NULL AND v_doc.checked_out_by::text <> p_actor::text`.",
    "note": "Confirmed: the guard is a strict BEFORE UPDATE, deletion is wide open, and documents.checked_out_by has no guard of its own (the same trigger census as DRLS-3 shows no trigger touching that column). Deleting the session row rather than closing it evades the force-release control entirely."
  },
  {
    "id": "DRLS-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260615_fix_missing_rls_policies.sql:71-76 `CREATE POLICY \"document_supersessions_member_all\" ON document_supersessions FOR ALL TO authenticated USING (EXISTS (SELECT 1 FROM org_members WHERE org_id = document_supersessions.org_id AND uid = auth.uid() AND status = 'active')) WITH CHECK (same)` — FOR ALL, so INSERT and DELETE are open to every member; grep confirms this is the only policy on the table. schema.sql:189-200 shows the table carries no soft-delete column and no audit of its own, so a DELETE is unrecoverable. Both writers discard the result: lib/documentLifecycle/common.ts:293 `await supabase.from(\"document_supersessions\").upsert(rows, { onConflict: \"superseded_doc_id,replacement_doc_id\" });` and lib/revisions.ts:1481 `await supabase.from(\"document_supersessions\").insert(rows);` — neither destructures `error` nor throws, so a blocked or failed write is silently reported as success.",
    "note": "All four cited claims verified verbatim, including the 'both writers ignore errors' half, which is the part most likely to have been overstated and is not."
  },
  {
    "id": "DRLS-14",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "schema.sql:144 is bare `current_version_id UUID,` — grep for `current_version_id UUID REFERENCES` / `ADD CONSTRAINT.*current_version` across supabase/ returns nothing, so there is no FK anywhere. 20260818_review_before_publish.sql:30 `ALTER TABLE documents ADD COLUMN IF NOT EXISTS pending_version_id UUID;` — likewise none. The asymmetry is confirmed: distribution_acks has `version_id UUID NOT NULL REFERENCES document_versions(id) ON DELETE CASCADE` (20260825_work_packages_acks.sql ~102) and revision_branches has `branch_version_id UUID NOT NULL REFERENCES document_versions(id) ON DELETE CASCADE` (20260823_publish_contract.sql:76), while document_acknowledgments takes `document_version_id UUID,` with no FK at all (20260817_read_understood.sql:37). The dangling-pointer consumer is real — app/api/share/resolve/route.ts:55-59 queries the id, gets nothing back, and drops into the newest-row fallback of DRLS-5.",
    "note": "Correct on every element. One more instance of the asymmetry that strengthens rather than weakens it: work_package_documents.pinned_version_id REFERENCES document_versions(id) with no ON DELETE clause defaults to NO ACTION, so a pinned version blocks deletion while an acknowledged one silently cascades — three different behaviours across four child tables pointing at the same parent."
  },
  {
    "id": "DRLS-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260818_followups_rls.sql:107-115 — `CREATE OR REPLACE FUNCTION revup_rollback_orphan(p_version uuid, p_prev uuid) RETURNS void LANGUAGE plpgsql SECURITY DEFINER AS $$ BEGIN DELETE FROM document_versions WHERE id = p_version; IF p_prev IS NOT NULL THEN UPDATE document_versions SET superseded_at = NULL WHERE id = p_prev; END IF; END; $$;` — no auth.uid(), no org_members check, no SET search_path. Repo-wide grep for the name returns only this definition plus one caller (lib/revisions.ts:826); grep for GRANT EXECUTE/REVOKE across supabase/ returns 30 hits, none naming this function, so it keeps the PostgreSQL default EXECUTE TO PUBLIC. It defeats 20260815_versions_collections_delete_controllers.sql:22-25 `CREATE POLICY document_versions_delete_controllers ON document_versions AS RESTRICTIVE FOR DELETE USING (is_org_controller(org_id))`, and no table anywhere carries FORCE ROW LEVEL SECURITY (grep returns nothing), so the definer's owner rights bypass RLS entirely.",
    "note": "Confirmed on every element: single definition, no re-definition, no GRANT/REVOKE, no authorization of any kind. Only backstop found is trg_document_versions_legal_hold_delete (20260826_legal_hold_delete_guard.sql:52-56), which is a genuine BEFORE DELETE row trigger and does fire under SECURITY DEFINER — its `SELECT legal_hold ... WHERE id = OLD.record_id` matches the real column (schema.sql:322), so it works. That protects only legally-held documents; everything else is deletable by anyone who can supply a version UUID. The one qualifier the claim glosses is that UUIDs are not enumerable, so cross-tenant use needs a harvested id (a second, unrelated FK is also a partial brake: work_package_documents.pinned_version_id REFERENCES document_versions(id) with no ON DELETE clause blocks deleting a version pinned into a work package)."
  },
  {
    "id": "DRLS-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "schema.sql:1068-1069 `CREATE POLICY \"documents_org_access\" ON documents FOR ALL USING (org_id IN (SELECT my_org_ids()));` is the only permissive write policy. I enumerated every BEFORE UPDATE trigger on documents repo-wide (grep 'CREATE TRIGGER' + 'ON documents'): exactly three fire on UPDATE — trg_document_publish_guard, documents_guard_access, trg_documents_move_guard. Each is column-scoped and none names rev/revision/document_number/effective_date: 20260822_review_completion_guard.sql:36-41 `v_advancing := (NEW.current_version_id IS DISTINCT FROM OLD.current_version_id) OR (NEW.status = 'Superseded' AND COALESCE(OLD.status,'') <> 'Superseded'); IF NOT v_advancing THEN RETURN NEW; END IF;` — a PATCH of {rev, revision, status:'Issued'} leaves current_version_id alone, so it returns immediately; 20260816_documents_access_change_guard.sql:81-101 gates only `NEW.visibility/acl/acl_index IS DISTINCT FROM OLD.*`; 20261011_collections_guard_and_trash.sql:38-62 gates only `NEW.collection_id IS DISTINCT FROM OLD.collection_id`. The only restrictive UPDATE policy, documents_deny_write_guard (20260901:152-162), bites solely on an explicit acl_index deny. 20260819_effective_date.sql:17-28 adds documents.effective_date with no trigger and no policy.",
    "note": "The claim of absence is correct and complete — nothing in the schema or any migration keeps documents.rev/revision/document_number/effective_date consistent with current_version_id, and no check constraint or sync trigger exists. Any active member can move the register label without moving the file."
  },
  {
    "id": "DRLS-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260820_retention.sql:61-64 `CREATE POLICY \"doc_disposition_events_member\" ON document_disposition_events FOR ALL USING (EXISTS (SELECT 1 FROM org_members WHERE org_id = ... AND uid = auth.uid() AND status = 'active')) WITH CHECK (same)` and 20260812_enable_rls_orphan_tables.sql:44-47 the identical FOR ALL policy on document_review_events; 20260819_orphan_tables_backfill.sql:221-238 re-issues the same `FOR ALL ... _member_all` shape in a loop over both tables. FOR ALL covers DELETE and UPDATE, so any active member can erase or rewrite a hold_placed row. Repo-wide grep for these two table names filtered to policy/restrictive/revoke returns only those three sites (plus the mirror in supabase/REMEDIATION_APPLY_ALL.sql) — no restrictive DELETE policy anywhere. Both writers set performed_by from client-supplied input with no server check: lib/retention.ts:235 `await supabase.from(\"document_disposition_events\").insert({ ... performed_by: e.actorId ?? null, ...})` and lib/reviewCycles.ts:138 the same for document_review_events.",
    "note": "Verified in both directions: member-writable (arbitrary performed_by, arbitrary action) and member-deletable. Note the contrast the finding implies is real — 20260828_integrity_hardening.sql:243/280 restricted the sibling tables document_review_signoffs and document_acknowledgments to `role IN ('Admin','DocCtrl')` for DELETE, and these two records-management trails were left out of that pass."
  },
  {
    "id": "DRLS-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/share/resolve/route.ts:43-47 selects only `id, document_number, title, name, rev, current_version_id` — nothing about status, archived_at or legal_hold, and no branch on any of them. Lines 59-71: `if (!storagePath) { const { data: latest } = await sb.from(\"document_versions\").select(\"id, file_url\").eq(\"record_id\", doc.id).or(\"review_state.is.null,review_state.eq.approved\").order(\"created_at\", {ascending:false}).limit(1); ...}` — ordered by created_at only, with no `is_branch = false` and no `superseded_at is null` filter, although document_versions.is_branch exists (schema.sql:1199) and is what publish_contract's active-label unique index keys on (20260823:70-95 / schema.sql:1209-1211). The client is created with the service-role key (route.ts:30), so RLS never applies. app/api/share/file/route.ts:60-78 duplicates the identical resolution block, so the actual byte-serving path has the same gap.",
    "note": "Both halves confirmed, and the same defect exists in the sibling /api/share/file route the finding does not cite. One qualifier the title omits: the fallback DOES exclude in-review drafts via the `review_state.is.null,review_state.eq.approved` filter, so the exposure is branch/superseded versions rather than unreviewed drafts. The 'no status/archive/hold check' half applies on the primary path too, not just the fallback, so it is if anything understated."
  },
  {
    "id": "DRLS-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260828_integrity_hardening.sql:223-226 `CREATE POLICY doc_review_signoff_insert ON document_review_signoffs FOR INSERT WITH CHECK (EXISTS (SELECT 1 FROM org_members WHERE org_id = document_review_signoffs.org_id AND uid = auth.uid() AND status = 'active'));` — org membership is the entire check; nothing constrains status, slot, reviewer_user_id or document_version_id. The same migration's UPDATE policy (229-241) carefully limits signing to `reviewer_user_id = auth.uid()` — INSERT was simply not given the equivalent. The DB gate it must satisfy counts rows, not identities: 20260822_review_completion_guard.sql:46-58 `SELECT count(*) FILTER (WHERE slot = 'primary'), count(*) FILTER (WHERE status = 'signed') INTO v_primary_reqs, v_signed FROM document_review_signoffs WHERE document_version_id = NEW.current_version_id; IF COALESCE(v_primary_reqs,0) > 0 AND COALESCE(v_signed,0) < v_primary_reqs THEN RAISE EXCEPTION ...`. The app mirrors the same forgeable arithmetic at lib/reviewControl.ts:365-367 (`requiredPrimaries = roster.filter(r => r.slot === 'primary').length; signed = roster.filter(r => r.status === 'signed').length; complete = requiredPrimaries > 0 && signed >= requiredPrimaries`).",
    "note": "Correct: inserting N rows with slot='alternate', status='signed' raises v_signed without raising v_primary_reqs, satisfying the guard while the real primaries stay pending. The only structural brake, `doc_review_signoff_unique_idx ON (document_version_id, reviewer_user_id)` (20260818_review_before_publish.sql:76-77), does not help — reviewer_user_id is a bare UUID with no FK, so distinct arbitrary values satisfy it. The precondition in the summary is accurate: the forger still needs publish authority or effective ownership to clear the role branch of the same trigger."
  },
  {
    "id": "DRLS-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260623_document_shares.sql:37-54 `CREATE POLICY document_shares_org_member ON document_shares FOR ALL USING (EXISTS (SELECT 1 FROM org_members WHERE org_members.org_id = document_shares.org_id AND org_members.uid = auth.uid() AND org_members.status = 'active')) WITH CHECK (same)` — FOR ALL, so UPDATE (revoked_at back to NULL, expires_at forward) and DELETE (erasing access_count/access_last_at) are both open to every active member. Repo-wide grep for document_shares across supabase/ returns no other policy and no restrictive overlay. Revocation itself is nothing but that column: lib/documentShares.ts:68-70 `await supabase.from(\"document_shares\").update({ revoked_at: ..., revoked_by: actorUserId }).eq(\"id\", id);`. And the resolver trusts only those two columns — app/api/share/resolve/route.ts:38-41 `if (share.revoked_at) return ... 410; if (share.expires_at && new Date(...).getTime() < Date.now()) return ... 410;`.",
    "note": "Both halves are literally true and un-mitigated. Worth recording as context rather than refutation: an ordinary member can already mint a fresh share for the same document under the same policy, so the confidentiality delta of un-revoking is small — the durable harm is defeating Document Control's revocation of a link they believe dead and destroying the access trail. That is still integrity/audit damage of the same class as DRLS-4 and DRLS-8, so HIGH stands."
  },
  {
    "id": "DRLS-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "schema.sql:1090-1091 `CREATE POLICY \"download_audits_org_access\" ON download_audits FOR ALL USING (org_id IN (SELECT my_org_ids()));` — FOR ALL with no WITH CHECK, so PostgreSQL applies the USING expression as the check: any active member may UPDATE any row in their org (including user_id, schema.sql:793 `user_id UUID NOT NULL` with no default and no FK to auth.users) and DELETE any row. Grep for download_audits across supabase/migrations/*.sql returns nothing at all, so schema.sql holds the only policy — there is no later hardening. The intended contrast is right beside it at schema.sql:1084-1087, where audit_logs was given a split policy (`FOR SELECT` plus `audit_logs_insert ... WITH CHECK (user_id = auth.uid())`) and no UPDATE/DELETE grant.",
    "note": "Confirmed. download_audits is the distribution record that /api/share/file writes to (app/api/share/file/route.ts:130-139) and that Document Control produces for PSM audits; it has strictly weaker protection than audit_logs sitting six lines above it in the same file."
  },
  {
    "id": "DRLS-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260823_publish_contract.sql:112-117 `-- Any active org member may resolve (author reconciles, DocCtrl closes out).` / `CREATE POLICY revision_branches_org_update ON revision_branches FOR UPDATE USING (EXISTS (SELECT 1 FROM org_members WHERE org_members.org_id = revision_branches.org_id AND org_members.uid = auth.uid() AND org_members.status = 'active'));` — no WITH CHECK, so the USING expression is reused as the check and nothing constrains resolved_at, resolution or resolved_by. The CHECK constraint on the column is only a value domain: `resolution TEXT CHECK (resolution IN ('merged','withdrawn'))` (20260823:88 / schema.sql:1231). The open-debt queue keys purely on the nullable column — `CREATE INDEX revision_branches_open_idx ON revision_branches(org_id, document_id) WHERE (resolved_at IS NULL)` — so stamping resolved_at is the whole of 'closing the debt'.",
    "note": "Correct, and the migration comment shows the breadth was deliberate ('any active org member may resolve') rather than an oversight — which is exactly why it reads as a design gap at MEDIUM rather than a bug. Nothing verifies that a merge actually happened."
  },
  {
    "id": "DWG-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/knowledgeSourceSync.ts:242-244 — the rev-up branch deletes chunks only: `const { error: chunkErr } = await supabaseAdmin.from(\"knowledge_chunks\").delete().eq(\"document_id\", existing.id as string);` — no knowledge_page_entities delete anywhere in the branch (the update at :247-259 sets status 'stale', pages_indexed 0, and nothing else). The re-ingest's entity cleanup is inside a guard that a zero-entity pass never enters: knowledgeIngest.ts:399-401 `if (entityRows.length > 0) { await supabaseAdmin.from(\"knowledge_page_entities\").delete().eq(\"document_id\", doc.id).gte(\"page\", from + 1).lte(\"page\", reached) ... }`. The verdict's revision then comes from the LIVE controlled document: app/api/knowledge/drawing/route.ts:417-421 `.from(\"documents\").select(\"id, rev\")...` → :433 `revision: d.source_document_id ? (revById.get(d.source_document_id) ?? \"\") : \"\"`.",
    "note": "The chain holds end to end. The stale index also defeats the 'skipped' safety valve: route.ts:434 `indexed: d.status === \"ready\" && withEntities.has(d.id)` is satisfied by the surviving Rev-C rows plus the `status: done ? \"ready\" : \"indexing\"` write at knowledgeIngest.ts:432, so drawingAuditLog.ts:101-104 files `passed`/`flagged` rather than `skipped` — under Rev D."
  },
  {
    "id": "DWG-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "route.ts:430 `sheetNumber: selfByDoc.get(d.id)?.[0] ?? d.name` versus route.ts:299 `const base = selfTags.filter((t) => !/-SH\\d+$/.test(t)).sort((a, b) => a.length - b.length)[0] ?? null;`. The list order comes from route.ts:88-95, whose only ordering is `.order(\"document_id\", { ascending: true })` — no tiebreak within a document — and route.ts:395-400 preserves that arrival order verbatim (`const list = selfByDoc.get(e.document_id) ?? []; if (!list.includes(e.tag)) list.push(e.tag);`).",
    "note": "Two different selection rules on the same array: the audit key takes element [0] raw (which can be a `-SHn` variant, since knowledgeIngest.ts:288-289 writes both `drawingNumber` and `drawingNumber-SHn` as 'self'), while the on-screen `declared` strips `-SHn` and picks the shortest. With no ORDER BY beyond document_id, a re-index that changes physical row order changes the key, and the unique index on (org, sheet_number, revision) then files the second run as a separate row rather than an update."
  },
  {
    "id": "DWG-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "route.ts:88-95 loops docs 50 at a time with `.limit(50000)` per slice; route.ts:180-186 and :278-285 do the same for chunks with `.limit(20000)`. No returned-row count is ever compared against the cap and no 'capped' flag reaches the response (contrast lib/orgGraph.ts:165-168, which pushes `truncations` when a cap is hit). DrawingIntelPanel.tsx:132 captions the result `\"Computed from every sheet's extracted tags — counts you can trust, not AI guesses.\"`",
    "note": "The cap, the absence of any detection, and the exactness promise are all as described. The ordering makes it worse than a random shortfall: `.order(\"document_id\")` means the cut lands on whole trailing documents in each 50-doc slice, so entire sheets vanish from the census and audit. Note knowledgeEntityKinds.ts:34 makes TAG_ENTITY_KINDS = all four kinds, so the `.in(\"kind\", ...)` filter narrows nothing today — every kind competes for the same 50 000."
  },
  {
    "id": "DWG-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "entityKindGuard.test.ts:28-30 exempts the file as `\"narrowed to one document+page+tag list — bounded by the caller's tags, never a slab\"`. That describes locate/route.ts:80-83 (`.eq(\"document_id\", documentId).eq(\"page\", page).in(\"tag\", tags)`) but NOT the second read at :117-122: `.from(\"knowledge_page_entities\").select(\"document_id, page, tag\").eq(\"library_id\", doc.library_id).in(\"tag\", missingHere).limit(1000)` — library-wide, no document, no page, and no kind filter, with a cap of exactly the 1000 the guard defines as bulk.",
    "note": "The exemption text is stale and the harm is reachable: with no kind filter the hits include kind='ref' rows (a neighbouring sheet's number appearing in an off-page connector note) and kind='opc'/'self' rows, and the scorer at :141-148 ranks only by same-document then page, so a mere reference can win. CitedPageViewer.tsx:449-455 then renders that as `{e.tag} is on {documentName} — jump`. Fair correction to the wording: the read IS bounded to ≤12 tag values (MAX_TAGS at locate/route.ts:35), so 'unfiltered slab' overstates it; the missing kind filter and the stale exemption are the substance and both hold."
  },
  {
    "id": "DWG-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "route.ts:13-15 promises rows `keyed by (sheet, revision) so an unrevised sheet is never re-audited`. recordAudit (route.ts:386-479) never selects from drawing_audit_logs — it goes loadVisibleEntities → auditDrawingRefs → auditOpcBoxes → `verdictsForSheets(sheets, ...)` over EVERY doc (route.ts:423 `const sheets: AuditSheet[] = docs.map(...)`) → upsert. Repo-wide grep: `sheetsNeedingAudit` (drawingAuditLog.ts:127) appears only in lib/__tests__/drawingAuditLog.test.ts; `buildRelocateUser` (drawingLocate.ts:102) has zero references anywhere outside its own definition.",
    "note": "Both dead-code claims verified by full-repo grep, and the route genuinely recomputes and rewrites all verdicts on every click. A partial mitigation the finding misses: lib/orchestrator/tools.ts:258-285 implements the skip-if-already-audited idea for the AI tool path (`check_audit_history`), so the CONCEPT exists — just not on the route whose header comment claims it. Also note verdictRows omits audited_at, so a rewrite silently keeps the original timestamp on top of the new status."
  },
  {
    "id": "DWG-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/drawingText.ts:63 `const EQUIPMENT_RE = /\\b([A-Z]{1,3})[-–](\\d{1,5})([A-Z]{1,2})?\\b/g;` with the only inner-segment guard at :79-81 `if (at >= 2 && /[-–]/.test(upper[at - 1]) && /\\d/.test(upper[at - 2])) continue;`. On the prompt's own example line number `6\"-P-1024-A1A` the char before `P` is `-` but the char before that is `\"`, not a digit — the guard does not fire and `P-1024` is emitted as equipment. And the prompt asks for exactly that string: knowledgeVision.ts:36-37 `\"- every equipment tag, line number, valve tag, instrument bubble (V-3, P-101A, PSV-2001, 6\\\"-P-1024-A1A) exactly as written;\"`. knowledgeIngest.ts:247-252 writes each hit as `kind: \"equipment\"`, and equipmentBridgeServer.ts:65-70 selects `.eq(\"kind\", \"equipment\")`, with the only filter being `if (!splitTag(norm)) continue;` (:74) — a shape test a line number passes.",
    "note": "Searched drawingText.ts / knowledgeIngest.ts / equipmentBridgeServer.ts for any line-number recognizer or suppression and found none (the sole 'line number' mention, drawingText.ts:617, is about SHX geometry). Downstream the phantoms become real asset rows: equipmentBridgeServer.ts:200-216 inserts them with `origin: \"drawing\"` unless `bridge?.createAssets === false`, under the DrawingIntelPanel.tsx:132 caption \"counts you can trust, not AI guesses\"."
  },
  {
    "id": "DWG-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/knowledgeIngest.ts:236-240 — `const view = page.getViewport({ scale: 1 }); const norm = (x, y) => ... : { nx: clamp01(x / view.width), ny: clamp01(1 - y / view.height) };` fed by the raw text-item matrix at :243-244 `const x = item.transform?.[4] ?? null; const y = item.transform?.[5] ?? null;`. The viewport transform is never applied — grep for `rotat|Rotate|viewport|CropBox|UserUnit` across knowledgeIngest.ts returns only line 236 — so on the repo's own fixtures/PID-Legend.pdf (`/Rotate 180`, `/MediaBox [0 0 1224 792]`, verified by parsing the file) the rendered page is 180° from the space the coordinates were measured in, and the correct normalization would be `1 - x/w`, `y/h`. The viewer draws these as the confident case: CitedPageViewer.tsx:508 `const approx = m.source !== \"text\";` → :519-521 text-sourced marks get the solid yellow `w-14 h-4` swipe with no tolerance box.",
    "note": "Core defect confirmed on a fixture that ships in this repo. Two caveats on the framing, neither fatal: (a) /UserUnit is immaterial here — pdf.js ignores it on both sides, and since nx/ny are ratios any uniform scale cancels, so only /Rotate and a non-zero CropBox origin actually shift the mark; (b) the node_modules/pdfjs-dist/build/pdf.mjs line citations cannot be checked in this tree — dependencies are not installed and pdfjs-dist is not a declared dependency (package.json pulls it transitively via `unpdf`)."
  },
  {
    "id": "DWG-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/drawingText.ts:291 `const OPC_BOX_RE = /\\bOPC[\\s#.:-]*(\\d{1,4})\\b/g;` is the sole producer of `kind: \"opc\"` rows (knowledgeIngest.ts:272-280 `for (const box of parseOpcBoxes(line)) { entityRows.push({ ... kind: \"opc\", tag: box, ... }) }`). The comment at drawingText.ts:288-289 asserts \"The vision prompt asks for 'OPC <n>: …' lines\" — it does not: a repo-wide grep for `OPC` over *.ts/*.tsx hits drawingText, knowledge.ts, ask/route.ts, drawing/route.ts and the tests, and never knowledgeVision.ts. The actual prompt (knowledgeVision.ts:38-40) asks for `\"every off-page connector / continuation reference with its drawing number AND sheet number ... (e.g. 'TO 025-PID-0107', 'CONT ON DWG 21-D-1105 SH 3')\"` — no box token, no `OPC` label. With no opc rows, auditOpcBoxes (drawingText.ts:697-748) returns `{ boxCount: 0, unreturned: [], noRef: [] }`, and drawingAuditLog.ts:83-86 shows those are the ONLY inputs to `broken`, so :101-104 `b.length > 0 ? \"broken_connectors\"` can never be reached.",
    "note": "The claim is exactly right, and the smoking gun is the stale comment at drawingText.ts:288-289 describing a prompt contract that knowledgeVision.ts does not implement. The panel's reassuring \"0 connector(s) with no drawing number\" (drawing/route.ts:253-258) is therefore a report on an empty set, not a clean one."
  },
  {
    "id": "DWG-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/knowledge/locate/route.ts:216-220 records usage BEFORE the refine loop runs: `await recordAskUsage({ orgId, userId: user.id, provider, model: ..., usage: out.usage, ok: true, op: \"drawingLocate\" });`. The loop at :236-238 (`const REFINE_MAX = 4; const CROP_DIVISORS = [3, 9];`) then makes up to 4×2 = 8 further `callAiModel` calls (:256-266) and only mutates the already-persisted object: :267-268 `out.usage.inputTokens += fine.usage.inputTokens; out.usage.outputTokens += fine.usage.outputTokens;`. A grep of the whole route for `recordAskUsage` returns exactly one call site (line 216) — nothing re-records after the loop. lib/ai/usageServer.ts:112-127 snapshots the numbers at call time into `est_cost_usd: estimateCostUsd(model, usage)` and inserts immediately.",
    "note": "Confirmed: the mutation on lines 267-268 is dead weight — the ai_usage_events row was written and awaited before it happens. The cap check (:185-194 `if (cap > 0 && spent.spentUsd >= cap)`) reads getMonthUsage, which is fed by those under-counted rows, so the governance layer sees roughly 1/9th of the real spend. Each refine image (outW 1400) is comparable in size to the coarse 1800px render, so the under-count is close to the order of magnitude claimed."
  },
  {
    "id": "DWG-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260929_mention_engine.sql:150-151 `CREATE UNIQUE INDEX IF NOT EXISTS drawing_audit_logs_sheet_rev_idx ON drawing_audit_logs (org_id, sheet_number, revision_code);` — no library_id column exists on the table at all (140-153). route.ts:387 `const { docs, entities, error } = await loadVisibleEntities(orgId, userId, libraryId);` computes library-scoped, and route.ts:454-456 `.from(\"drawing_audit_logs\").upsert(verdictRows(orgId, deduped, userId), { onConflict: \"org_id,sheet_number,revision_code\" })` writes org-wide with no library discriminator and no status/severity comparison against the stored row. 20260919_knowledge_mirror_unique.sql:12-13 makes the mirror index `(library_id, source_document_id)`, so the SAME controlled doc can legitimately be mirrored into several libraries.",
    "note": "Confirmed on every leg: the key is org-wide, the computation is library-wide-scoped, the upsert is unconditional, and the mirror uniqueness index explicitly permits one controlled sheet in many libraries (so both audits carry the same `rev`, hence the same key). The RANK dedup at route.ts:445-451 protects only within one batch, never against the stored row. lib/orchestrator/tools.ts:268-270 reads the record back by `(org_id, sheet_number)` alone, so the corrupted verdict is what the orchestrator reports as history."
  },
  {
    "id": "DWG-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "drawingText.ts:19-25 `export const SPARSE_PAGE_MAX_CHARS = 2000;` / `return pageText.trim().length > 0 && pageText.length <= SPARSE_PAGE_MAX_CHARS;`. knowledgeIngest.ts:231 `} else if (isDrawingLikePage(pageText)) {` gates equipment+ref extraction, and :268 `if (visionRead || isDrawingLikePage(pageText)) {` gates opc AND title-block ('self') extraction too. The vision escape at knowledgeIngest.ts:154 `if (vision?.forceAllPages || pageNeedsVision(rawPageText, tagsFromText))` cannot fire, because pageNeedsVision short-circuits for a dense page: drawingText.ts:630-632 `const thin = text.length <= THIN_PAGE_MAX_CHARS;` (1200) then `if (!thin) return false;`.",
    "note": "All four consequences follow mechanically: >2000 chars means no equipment, no ref, no opc and no 'self' rows, so the census, the reference audit, the CSV register and the equipment bridge are all empty, while chunking/search still succeed. The only vision path for such a page is the manual library-wide `forceAllPages` toggle — nothing automatic, and the drawing route's suggestion for this exact state (route.ts:199-205) tells the user it is 'normal for prose documents' rather than offering vision."
  },
  {
    "id": "DWG-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "knowledgeIngest.ts:276 `page: p, kind: \"opc\", tag: box, raw: truncateSafe(line, 160), ...`; knowledgeText.ts:166-169 truncateSafe is a hard slice. drawingText.ts:739-746 `const noRef = opcRows.filter((o) => !o.raw || extractDrawingRefs(o.raw).length === 0)` — the ONLY evidence auditOpcBoxes has is the truncated `raw` (also used at :721 for pairing). route.ts:253-258 then emits `... off-page connector(s) carry NO drawing number at all — broken by definition`, and DrawingIntelPanel.tsx:344 renders `{intel.opcNoRef!.length} connector(s) with no drawing number — broken`.",
    "note": "The truncation-before-analysis is real and the false 'broken by definition' verdict follows directly. Stronger than the finding states for text-layer sheets: knowledgeIngest.ts:129-134 only closes a line on `item.hasEOL`, so a CAD page with no EOL flags collapses to ONE line and every opc row on it keeps only the page's first 160 characters. Caveat worth recording: knowledgeVision.ts's VISION_SYSTEM never actually asks for `OPC <n>:` lines (contradicting drawingText.ts:289), so today opc rows only arise from literal 'OPC' text on the sheet — the defect is real but its blast radius depends on that."
  },
  {
    "id": "DWG-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20261007_retire_line_traces.sql:9 `DROP TABLE IF EXISTS knowledge_line_traces;` (sorts after 20261007_line_traces.sql, which created it). 20261009_trace_method.sql:16-21 `ALTER TABLE knowledge_line_traces ADD COLUMN IF NOT EXISTS method TEXT, ADD COLUMN IF NOT EXISTS turns INTEGER;` — plain `ALTER TABLE`, not `ALTER TABLE IF EXISTS`, so it raises 42P01 on a table that no longer exists.",
    "note": "Repo-wide grep confirms no later migration recreates knowledge_line_traces (only the three files above mention it), and supabase/schema.sql — the documented 'run this to set up your database' file — contains none of the post-base tables (no line_traces, drawing_audit_logs or process_flows), so a fresh environment must apply the migrations in filename order and will hard-fail at 20261009. `IF NOT EXISTS` on the columns gives no protection; the missing relation is the error."
  },
  {
    "id": "EGR-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/transmittal/route.ts:39-55 `async function fileKeyForItem(item: Item)` resolves from member-supplied JSONB with no scoping predicate: `await supabaseAdmin.from(\"document_versions\").select(\"file_url, revision_label\").eq(\"id\", item.versionId)` and, failing that, `.eq(\"record_id\", item.documentId).eq(\"revision_label\", item.rev)` — no org_id filter, no ACL call, and supabaseAdmin is service-role so RLS never runs. Lines 66-74 authorize only `const item = items.find((i) => i.documentId === fileDoc); if (!item) return bad(...403)` — the attacker controls both sides of that comparison — then hands back a 300-second presigned R2 URL. The items array is written straight from the browser client: lib/transmittals.ts:16 `import { supabase } from \"@/lib/supabase\";` and :451 `items: input.items ?? [],`, with the token minted client-side at :453 `...(issueNow ? { portal_token: makePortalToken() } : {})` (lib/transmittals.ts:385-387). The only gate on that write is 20260910_transmittal_portal.sql:40-51 `CREATE POLICY transmittals_insert ON transmittals FOR INSERT WITH CHECK (created_by = auth.uid() AND EXISTS (SELECT 1 FROM org_members WHERE org_id = transmittals.org_id AND uid = auth.uid() AND status = 'active'));` — role-blind, and it validates nothing inside items.",
    "note": "Survives, and the second resolution branch makes it easier than the summary claims: documentId + a guessable revision_label ('0','1','A') is enough, so the attacker never needs a version UUID. The portal GET also only rejects status='voided' (route.ts:61), so a self-created draft-then-issued transmittal the attacker never sends works fine, and the attacker knows its token because the client generated it. No org check exists at any layer of this path."
  },
  {
    "id": "EGR-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260623_document_shares.sql:38-54 — the policy's only predicate is `EXISTS (SELECT 1 FROM org_members WHERE org_members.org_id = document_shares.org_id AND org_members.uid = auth.uid() AND org_members.status = 'active')` in both USING and WITH CHECK; `document_id` is never correlated to `org_id`. Neither consumer re-checks: app/api/share/file/route.ts:53-57 `.from(\"documents\").select(\"id, document_number, title, name, rev, current_version_id\").eq(\"id\", share.document_id).maybeSingle()` and app/api/share/resolve/route.ts:43-48 do the identical service-role fetch with no `.eq(\"org_id\", share.org_id)`.",
    "note": "Confirmed by repo-wide search: `document_shares` appears only in 20260623_document_shares.sql and 20260818_followups_rls.sql (the latter only defines bump_share_access) — there is no trigger, CHECK, or FK pairing (org_id, document_id). The policy is FOR ALL with no role gate, so any active member including a Viewer can insert an attacker-chosen token pointing at any documents.id, and both /api/share/resolve and /api/share/file serve it with the service role, bypassing RLS entirely."
  },
  {
    "id": "EGR-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/schema.sql:789-799 defines download_audits with exactly (id, org_id, document_id, version_id, user_id, user_email, created_at, expires_at, watermark_policy_id) — no `source`. app/api/share/file/route.ts:139 inserts `source: stamped ? \"share_link\" : \"share_link_unstamped\"`, wrapped in `try { ... } catch { /* pre-migration column drift — never block the share */ }` (line 141).",
    "note": "Repo-wide grep confirms the absence: no migration in supabase/migrations/ mentions download_audits at all, so nothing ever adds `source`. PostgREST rejects the whole insert (unknown column), and supabase-js returns `{error}` rather than throwing — so the catch never even fires and the discarded error means zero external-download rows exist. lib/staleCopies.ts:40 and every other reader query only (document_id, version_id, created_at), so nothing surfaces the gap."
  },
  {
    "id": "EGR-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/storage/download-url/route.ts:144 `const expiresIn = parseInt(req.nextUrl.searchParams.get(\"expiresIn\") || \"3600\");` then :151 `const url = await getSignedUrl(r2, command, { expiresIn });` — no Math.min, no ceiling, no role check on the value. lib/storage.ts:118-130 shows the caller-supplied plumbing (`getPresignedDownloadUrl(path, expiresIn = 3600)` → `&expiresIn=${expiresIn}`).",
    "note": "Verified there is no clamp anywhere between parse and sign; SigV4's own 604800s maximum is the only bound, exactly the 7 days the finding claims. The ACL/visibility/deny checks above (lines 55-115) gate WHO may mint the URL at mint time, but they explicitly `catch { /* fail open */ }` and, more importantly, bind nothing to the resulting credential-free URL's lifetime."
  },
  {
    "id": "EGR-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/share/file/route.ts:53-57 selects `id, document_number, title, name, rev, current_version_id` with no `status` and no document_holds lookup; the only gates are share-row revoked_at/expires_at (48-51). app/api/verify/route.ts:89-90 `const docRetired = d.status === \"Superseded\" || d.status === \"Archived\"; const isCurrent = !docRetired && (!versionId || versionId === d.current_version_id);` — and types/schema.ts:613 `export type DocumentStatus = \"Draft\" | \"Issued\" | \"Superseded\" | \"Void\" | \"Archived\" | \"Locked\";`",
    "note": "Both halves confirmed and the status half is worse than stated: `Void` is a real status that docRetired does not test, so a voided document returns isCurrent:true and app/verify/[docId]/page.tsx:65,99 paints `bg-emerald-600` / \"CURRENT\". Holds are never consulted on either route — lib/documentGuards.ts:139-148 is the only hold block and it fires solely inside evaluatePublishGuard."
  },
  {
    "id": "EGR-6",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "The code facts hold: lib/documentShares.ts:68-73 `await supabase.from(\"document_shares\").update({...}).eq(\"id\", id);` with no `{ error }` destructure; lib/downloads.ts:131-146 wraps a non-throwing `.insert()` in try/catch; lib/docPack.ts:114-123 ends `.then(() => {}, () => {});`; MultiDocViewer.tsx:747 `try { await supabase.from(\"download_audits\").insert(rows); } catch (e) { console.error(e); }`. BUT the stated UI mechanism is false — components/documents/ShareLinkModal.tsx:77 `try { await revokeShareLink(id, createdBy); await refresh(); }`, and refresh() (line 48-54) re-reads from the DB via listShareLinks.",
    "note": "REFUTED in its headline mechanism: the share panel is NOT optimistic — it re-fetches after revoke, and the row is rendered from the server value (`const isRevoked = !!s.revokedAt` at line 148, showing \"revoked\" only at line 204), so a rejected UPDATE leaves the link visibly still live rather than reading as revoked. What survives is the narrower, already-covered point that every download-audit write discards its {error} (the same swallowing that makes EGR-3 silent), so I'd drop this to LOW as a code-hygiene finding."
  },
  {
    "id": "EGR-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/exportTables.ts:50-51 lists `\"document_shares\", \"project_intake_links\",` and :54 `\"transmittals\",` among the exported tables; lib/dataExport.ts:300 `let q = sb.from(table).select(\"*\").range(from, from + pageSize - 1);` — full-row, no column filter. The token columns are plaintext: document_shares.token (20260623:14 `token TEXT NOT NULL UNIQUE`), transmittals.portal_token (20260910_transmittal_portal.sql:21), project_intake_links.token (20260902_project_intake.sql:22). Destinations: lib/exportRunner.ts:277-314 POSTs `zipBytes` to `dest.webhook_url`, and the s3 branch writes to `${dest.bucket}/${fullKey}`.",
    "note": "Confirmed by absence-search: grep for redact/sanitize/scrub/token over lib/dataExport.ts and lib/exportRunner.ts finds no redaction step — the only `token` hits in exportRunner are S3 ContinuationToken pagination (lines 381-395). lib/exportTables.ts:171-181 (EXPORT_EXCLUDED_TABLES) excludes ai_connections for exactly the 'secrets never leave the database' reason, which shows the omission is an oversight rather than a policy. Export is gated to Admin/Manager/DocCtrl (app/api/data-export/structured/route.ts:55-57), but that does not constrain where the operator points the destination."
  },
  {
    "id": "EGR-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/transmittal/route.ts:71-74 `const url = await getSignedUrl(r2, new GetObjectCommand({ Bucket: R2_BUCKET, Key: file.key, ResponseContentDisposition: ... }), { expiresIn: 300 });` — a raw presigned bucket URL returned to an unauthenticated token holder, with no PDFDocument.load / applyStampToPdfDoc anywhere in the file. Contrast app/api/share/file/route.ts:107-119, which loads the bytes server-side and calls `applyStampToPdfDoc(pdfDoc, { watermarkText: \"UNCONTROLLED — SHARED COPY\", ... })` before returning them.",
    "note": "Confirmed: the transmittal portal is a second, parallel outsider egress path that hands back exactly the artifact the share/file route's header comment (lines 5-12) says was the bug it was built to fix ('the page's fallback opened the RAW UNSTAMPED file in a new tab'). The portal is more correct than share/file in one respect — it serves the pinned as-sent revision (fileKeyForItem, lines 39-54) rather than the current one — but the bytes carry no watermark, rev footer, or verify QR."
  },
  {
    "id": "EVID-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/schema.sql:1080-1081 `CREATE POLICY \"tickets_org_access\" ON tickets FOR ALL USING (org_id IN (SELECT my_org_ids()));` — FOR ALL with USING only, so UPDATE's WITH CHECK defaults to the same org test; lib/ticketTransitions.ts:139 `const newHistory = [...(ticket.history || []), historyEntry];` and :144-148 `const updates: Record<string, unknown> = { last_modified: now, history: newHistory, unread_by: newUnreadBy };` — history/status live in client-writable ticket columns. lib/projects.ts:826-841 shows a second read-modify-write of the same JSONB from the browser client.",
    "note": "I looked specifically for a rescue: there is no RESTRICTIVE policy on `tickets` in any migration (20260828_integrity_hardening, 20260831_capability_policy_and_rails and 20260901_db_hard_enforcement harden document_review_signoffs, document_acknowledgments, work_package_documents, document_holds and documents — never tickets), and the only trigger on the table is `tickets_search_tsv_trg`. The server route's WorkflowEngine enforcement is therefore entirely bypassable by a direct PATCH from any active member's own token; app/api/verify-ticket/route.ts:51-56 then serves the forged status/rev publicly. CRITICAL stands."
  },
  {
    "id": "EVID-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/evidencePack.ts:19-24 is the complete query set: documents, document_versions, document_holds, and `supabase.from(\"audit_logs\").select(\"*\").eq(\"resource_type\", \"document\").eq(\"resource_id\", documentId).order(\"timestamp\").limit(1000)`. Repo-wide grep for e_signatures / document_review_signoffs / document_acknowledgments returns no hit in lib/evidencePack.ts. Footer (line ~125): \"This pack is assembled from the immutable audit trail and revision records\". Version row (line 62): `<td>${esc(v.approved_by_name || \"—\")}</td>`.",
    "note": "Confirmed: the only four tables queried are documents/document_versions/document_holds/audit_logs, so no signature, review sign-off, acknowledgment or distribution-ack row reaches the pack; the audit section is hard-capped at 1000 rows with no truncation notice; and both callers (InspectorPanel.tsx:950, GlobalCommandPalette.tsx:205) invoke the same gatherEvidence. Signatures appear only indirectly, via the best-effort ESIGNATURE_CAPTURED audit mirror that EVID-11 shows can be dropped."
  },
  {
    "id": "EVID-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/eSignatures.ts:140-149 `await logAuditAction({...}).catch(() => { /* audit best-effort */ });`. lib/acknowledgments.ts:441 `await supabase.from(\"document_acknowledgments\").update(patch).eq(\"id\", input.rosterId);` and 443-446 the fallback `.eq(\"status\",\"pending\")` filtered update — neither destructures `error` nor checks a matched-row count. lib/reviewControl.ts:301-304 `await supabase.from(\"document_review_signoffs\").update({ status: \"signed\", signature_id: sig.id, ... }).eq(\"id\", input.signoffId);` — same, no error check. components/permissions/PermissionDrawer.tsx:302 `.then(() => undefined, () => undefined);`",
    "note": "Every cited write discards its outcome. The zero-row scenario is real: lib/acknowledgments.ts:326-338 does set status 'void' on pending rows for a superseded revision, and the fallback UPDATE at 443-446 filters on `status = 'pending'`, so a voided roster row matches nothing and the call still returns without error. maybeNotifyComplete then counts remaining pending rows and can even declare the roster complete."
  },
  {
    "id": "EVID-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/tickets/workflow-action/route.ts:163-191 commits the state change (`await baseQuery.select(\"id\").maybeSingle()` … `if (!updated) return 409`), then separately at 214-223: `await supabaseAdmin.from(\"audit_logs\").insert({ action: `TICKET_${action.action.toUpperCase()}`, ..., details: { from: ticket.status, to: newStatus, label: action.label } });` — the returned `{ error }` is never destructured or checked. lib/ticketTransitions.ts:247-250 `updates.deliverable_rev = issuedRevLabel(ticket.revisionCount)` for engineer_approve_final.",
    "note": "Confirmed: the audit insert is a separate, later, unchecked round-trip against a different table (no transaction), and its details carry only from/to/label — no deliverable_rev, no attachment id/hash. Partial mitigation: `historyEntry.action = ... issued Rev ${updates.deliverable_rev}` is written inside the same atomic tickets UPDATE, so the rev survives in the ticket's mutable history JSONB — but not in the immutable audit trail."
  },
  {
    "id": "EVID-13",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "app/api/tickets/workflow-action/route.ts:325-329 `await supabaseAdmin.from(\"notifications\").update({ read_at: new Date().toISOString() }).eq(\"resource_id\", ticketId).is(\"read_at\", null).not(\"metadata->>action\", \"is\", null);` — no user_id filter. app/api/admin/purge/route.ts:160 `t.table === \"notifications\" ? base.not(\"read_at\", \"is\", null)` where `base` already has `.lt(\"created_at\", cutoffIso)` (line 158) and MIN_DAYS = 7 (line 24).",
    "note": "The mechanism is real — the supersede UPDATE stamps read_at across every recipient's row, and a stamped row becomes purge-eligible. Two parts of the claim overstate it: rows are not 'immediately' purge-eligible (the purge also requires created_at older than the ≥7-day cutoff, plus Admin/DocCtrl auth and confirm:true), and read_at is nowhere consumed as evidence — a repo-wide grep shows it is read only by lib/inAppNotifications.ts / lib/inbox.ts to drive the unread bell badge, never by the evidence pack or any receipt surface. The cross-user stamping is also deliberate and documented in the code comment at 317-324 ('retire the old rows for everyone'). Real but low-impact: LOW."
  },
  {
    "id": "EVID-14",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260929_mention_engine.sql:150-151 `CREATE UNIQUE INDEX ... drawing_audit_logs_sheet_rev_idx ON drawing_audit_logs (org_id, sheet_number, revision_code);`. lib/orchestrator/tools.ts:544-548 `await supabaseAdmin.from(\"drawing_audit_logs\").upsert({ org_id, sheet_number, revision_code, status, audit_details: { note: args.details ?? \"\", by: ctx.userId } }, { onConflict: \"org_id,sheet_number,revision_code\" });`. app/api/knowledge/drawing/route.ts:453-455 the same upsert with the same onConflict.",
    "note": "Confirmed — one row per (org, sheet, revision) with an overwriting upsert, and no history table, so a prior broken_connectors verdict plus its audit_details defect list is destroyed by any later 'passed' write. Notably the route at 443-451 builds a RANK map so 'a clean sheet must never mask a broken one' — but that severity ranking is applied only within the incoming batch, never against the already-stored row. The orchestrator path does go through `proposal()` (tools.ts:415-426) for human confirmation, but the confirmation text is only \"Record <sheet> rev <rev> as passed\" — it never discloses that an existing broken_connectors finding will be overwritten."
  },
  {
    "id": "EVID-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260825_work_packages_acks.sql:135-139 `CREATE POLICY distribution_acks_org_update ON distribution_acks FOR UPDATE USING (EXISTS (SELECT 1 FROM org_members WHERE ... status = 'active'));` — no recipient predicate, no WITH CHECK; lib/distributionAcks.ts:186-193 `acknowledge(ackId)` = `.update({ acknowledged_at: ... }).eq(\"id\", ackId).is(\"acknowledged_at\", null)` with no recipient filter. The CREATE TABLE at :99-116 has columns `recipient_user_id, requested_by, requested_at, acknowledged_at` and no acknowledged_by; grep for `acknowledged_by` across supabase/ hits only 20260717_transmittals.sql.",
    "note": "Correct, including the pointed contrast: 20260828_integrity_hardening.sql:15-26 fixed exactly this hole for document_review_signoffs and document_acknowledgments (`reviewer_user_id = auth.uid()` / `assignee_user_id = auth.uid()`) and left distribution_acks untouched — the migration's own comment even documents the class of bug. No later ALTER adds an acknowledged_by column."
  },
  {
    "id": "EVID-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260720_e_signatures.sql:56-64 `WITH CHECK (signer_user_id = auth.uid() AND EXISTS (SELECT 1 FROM org_members WHERE org_id = e_signatures.org_id AND uid = auth.uid() AND status = 'active'))` — the ONLY insert constraint; lib/eSignatures.ts:118-133 inserts `intent, statement, signer_name, signer_role, content_hash, document_version_id` straight from the caller's arguments via the browser client. components/signatures/SignatureCaptureHost.tsx:59-61 shows the honest client supplying `signerName`/`signerRole` from local state, and components/signatures/SignaturePanel.tsx:71-72 renders those asserted strings as the signature.",
    "note": "Confirmed: there is no server route for signature capture (grep shows only client callers in lib/reviewControl.ts:296, lib/acknowledgments.ts:433, AckSection/ReviewGateSection), no check that the signer holds authority over the resource, and no server-side recomputation of content_hash (lib/revisions.ts:177-181 hashes the file in the browser). The one real constraint is that signer_user_id must equal the authenticated uid, so the underlying account is not forgeable — only the displayed name, role, intent, statement and hash are."
  },
  {
    "id": "EVID-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/documents/RevUpModal.tsx:732 `<Field label=\"Approved By\"><input value={approvedByName} onChange={(e) => setApprovedByName(e.target.value)} .../></Field>`; lib/revisions.ts:893 `approved_by_name: approvedByName?.trim() || null` on the version insert; supabase/migrations/20260828_integrity_hardening.sql:150 `NULLIF(p_version->>'approved_by_name','')` — the RPC likewise takes it from the client JSONB. Repo-wide grep for `approved_by`/`approved_at` on document_versions returns only DDL (supabase/schema.sql:338, :346; 20260526_document_version_control.sql:26) and READS (lib/revisions.ts:962, :969; app/(protected)/documents/[libraryId]/page.tsx:1897, :1904) — no write anywhere.",
    "note": "Both halves verified: the identity is free text, and the two structured columns are dead. It is then rendered as an approval stamp (components/documents/VersionHistoryPanel.tsx:395 `<SignoffBit icon={<Stamp .../>} label=\"Approved\" name={v.approvedByName} />`) and printed in the evidence pack's Approved column (lib/evidencePack.ts:61 `${esc(v.approved_by_name || \"—\")}`). The parallel real mechanism (document_review_signoffs + e_signatures) exists but is separate and does not populate or gate this field."
  },
  {
    "id": "EVID-5",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "supabase/schema.sql:1090-1091 `CREATE POLICY \"download_audits_org_access\" ON download_audits FOR ALL USING (org_id IN (SELECT my_org_ids()));` — FOR ALL includes DELETE, and no restrictive policy or trigger exists on the table. app/(protected)/requests/[id]/page.tsx:593 `void supabase.from(\"download_audits\").insert({...}).then(() => {}, () => {});` (also :675 and lib/docPack.ts:114) are fire-and-forget; components/viewers/MultiDocViewer.tsx:747 and components/assets/FileReferenceModal.tsx:189 are `try { await ... } catch (e) { console.error(e); }`.",
    "note": "The deletable-by-any-member half is exactly right. Two sub-claims are overstated: not every write is unawaited (MultiDocViewer.tsx:747, FileReferenceModal.tsx:189 and lib/downloads.ts:132 await), and not every write is a client insert (app/api/share/file/route.ts:130 writes server-side with the service-role client). Every path does swallow the outcome. Lowered to MEDIUM because the codebase already treats this table as best-effort ambient telemetry rather than a record of account — lib/staleCopies.ts:14 states \"orgs where download_audits is sparse simply show less\"."
  },
  {
    "id": "EVID-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/audit.ts:16-31: `try { await supabase.from(\"audit_logs\").insert({...}); } catch (error) { console.error(\"Failed to write audit log:\", error); }` — the PostgrestBuilder result (`{ data, error }`) is never destructured or inspected, `.throwOnError()` is never called, and the function returns void, so no caller can learn of a failure. Concrete instance verified: lib/reviewControl.ts:558 `await activateAlternate({ ..., actorId: null })` and lib/reviewControl.ts:386 `logAuditAction({ ..., userId: input.actorId ?? \"\", ... }).catch(() => {})` against `user_id UUID` (supabase/schema.sql:777).",
    "note": "Stands. The cited node_modules/@supabase/postgrest-js paths do not exist in this checkout (dependencies are not installed), but the app code settles it without them: even if the builder rejected, the catch swallows it, and since it resolves with an error object instead, the empty-string UUID insert (22P02) is dropped with no console output at all. The `.catch(() => {})` at reviewControl.ts:386 discards even that."
  },
  {
    "id": "EVID-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/distributionAcks.ts:30 `let ackSchemaMissing = false;` with :34-39 `isMissingAckSchema` returning true for `PGRST204` and `42703` as well as 42P01/PGRST205; :58 `if (ackSchemaMissing) return [];` … :66 `if (isMissingAckSchema(error)) { ackSchemaMissing = true; return []; }` and :71-72 `} catch { return []; }`; identically for getMyPendingAck at :77-96 (`return null`).",
    "note": "Confirmed, and worse than stated: `resetAckSchemaFlag()` (lib/distributionAcks.ts:31) has zero callers anywhere in the repo (not even a test), so once the latch flips it stays flipped for the life of the module instance. A PGRST204 (\"column not found in schema cache\") from post-deploy drift is indistinguishable to the UI from \"no acknowledgments requested\", and the recipient's acknowledge bar disappears because getMyPendingAck returns null."
  },
  {
    "id": "EVID-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260825_work_packages_acks.sql:99-116 — distribution_acks has only `requested_at` and `acknowledged_at`; there is no nag/reminder column and no ALTER adds one. lib/reviewControl.ts:585 `await supabase.from(\"document_review_signoffs\").update({ notified_at: new Date().toISOString() }).eq(\"id\", r.id as string);` and lib/acknowledgments.ts:507 `await supabase.from(\"document_acknowledgments\").update({ notified_at: new Date().toISOString() }).eq(\"id\", input.rosterId);` — single last-write-wins timestamps. lib/distributionAcks.ts:220-240 derives the dedupe watermark by querying the `notifications` table itself, and :257-272 pushes `notify(...)` calls with no durable record on the ack row.",
    "note": "Correct on every part. scanDistributionAcks writes nothing back to distribution_acks — nine nags leave nine notification rows and no trace on the record — and app/api/admin/purge/route.ts:40-44 lists `notifications` as a purge target (\"Bell items the recipient has already read. Disposable once read and aged\"), so the only evidence of the nagging is expressly disposable. No logAuditAction call exists on any of these reminder paths."
  },
  {
    "id": "EVID-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/notifications/send-queued/route.ts:155-167: `if (!resp.ok) { throw ... }` then `.update({ status: \"sent\", sent_at: new Date().toISOString(), ... })` — 'sent' means only that Resend's API returned 2xx; the Resend message id is not even stored. app/api/tickets/workflow-action/route.ts:382 `.filter((uid) => emailByUid.has(uid) && wantsEmail(uid))` drops opted-out recipients before the insert at :401-403, so no row is created — despite the CHECK at supabase/schema.sql:640 allowing `'suppressed'`. app/api/admin/purge/route.ts:40-44 makes `email_notifications` a purge target.",
    "note": "Verified: there is no Resend webhook or delivery-event handler anywhere under app/api (the only Resend reference in the repo is the send call itself), so nothing ever downgrades a 'sent' row on a later bounce, and a recipient with email_enabled=false leaves no evidentiary row at all — the 'suppressed' status value is unused."
  },
  {
    "id": "FLOW-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "admin/assets/page.tsx:580-583 `{unitFilter && unitFilter !== \"__unassigned\" && uid && (<FlowPanel ... unitCode={unitFilter} unitAssets={filtered} />)}` — the panel is explicitly suppressed for the unassigned bucket. UnitOpsPanels.tsx:122-127 then filters again to `mine = new Set(unitAssets.map((a) => a.id))` plus `f.from_ref === unitCode`. Repo-wide grep: `decideFlow` is imported and called only in UnitOpsPanels.tsx:30/152. flows/read/route.ts:158 writes every proposal as `status: \"proposed\"`.",
    "note": "Confirmed: the only accept/dismiss surface in the codebase lives inside FlowPanel, FlowPanel only renders for a selected real unit, and a proposal whose endpoints both have unit_code = null matches no unit's filter. AreaKnowledgePanel.tsx:106-114 also touches flows but only counts `f.status === \"confirmed\"` — read-only, no decision. assets.unit_code is a plain nullable TEXT column (20260928_site_codebook.sql:78), so the CSV-import-before-categorize path really does leave proposals unreachable."
  },
  {
    "id": "FLOW-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/graph/OrgGraph2D.tsx:202 `if (st.showArrows && (alpha > 0.3) && (e.type === \"supersession\" || e.type === \"related\" || onPath))` — 'flow' is absent. OrgGraph2D.tsx:177 `let alpha = proposed ? 0.55 : e.type === \"related\" ? 0.5 : e.type === \"library\" ? 0.1 : 0.2;` → a flow edge is 0.2, under the 0.3 gate. lib/graphSettings.ts:65 `showArrows: false`. GraphControls.tsx:213-215 hint: \"Direction on supersession and curated links.\"",
    "note": "All three barriers confirmed, and they compound: even with Arrows toggled on, a flow edge only clears alpha>0.3 when it is the hovered/selected edge (alpha 0.8), and it is still excluded by type. The unit hub's FlowPanel does print direction (UnitOpsPanels.tsx:212, ArrowRight between endpoints), but the Process lens the finding targets does not."
  },
  {
    "id": "FLOW-11",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "app/api/flows/read/route.ts:27 `const MAX_PAGES = 6;`; :75-77 `const pages = wanted.length > 0 ? wanted : Array.from({ length: Math.min(doc.page_count ?? 1, MAX_PAGES) }, (_, i) => i + 1);`; :178-180 note = \"No new flows found — the drawing may not print flow arrows between known tags, or everything visible was already decided.\" UnitOpsPanels.tsx:394-396 uses `json.pagesRead` ONLY on the proposed>0 branch and falls back to `json.note` otherwise.",
    "note": "The substantive defect stands: on the zero-result path the route returns `pagesRead` and the UI throws it away, so the user is told the drawing is at fault with no hint that only pages 1-6 were opened. But 'never reported' is overstated — UnitOpsPanels.tsx:567 places the cap in the picker itself (`placeholder=\"Pages (optional): 1-4 or 2,5,9 — up to 6 per read\"`) and shows `reads p. …` for an explicit selection, so the cap is disclosed even though the failure message ignores it."
  },
  {
    "id": "FLOW-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/flows/read/route.ts:170-171 `const { error } = await supabaseAdmin.from(\"process_flows\").insert(rows); if (error) return bad(\"Reading succeeded but writing proposals failed: ...\", 500);` — one statement, no onConflict, no per-row fallback. Contrast lib/processFlows.ts:69 `if (error && error.code !== \"23505\") throw new Error(error.message);`. supabase/migrations/20261017_process_flows.sql:36 supplies the unique constraint.",
    "note": "Confirmed, and the window is wider than the concurrency race described: the `settled` snapshot at route.ts:82-84 is `.select(...).eq(\"org_id\", orgId).limit(4000)` with no ORDER BY, so on an org past ~4000 flow rows a single reader can miss an already-decided pair and abort its own whole batch after paying for the vision call."
  },
  {
    "id": "FLOW-13",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/knowledgePageRender.ts:20 `const RENDER_WIDTH = 1400;          // readable table text, modest tokens`, used at :42-45 `renderPageAsImage(pdf, page, { width: RENDER_WIDTH, ... })`, which app/api/flows/read/route.ts:78 calls. lib/knowledgeIngest.ts:161-162 `renderPageAsImage(pdf, p, { width: 1800, // small tags stay legible`.",
    "note": "The factual contrast is exactly as stated — the flow reader reuses the deep-read width tuned for standards tables, while the ingester deliberately renders 1800px for tag legibility. Downgraded because this is a tuning constant with a plausible but unmeasured effect, not a defined failure: nothing shows the model actually misses tags at 1400px, and the reader's grounding roster (route.ts:67-71, 146-148) still blocks any tag it did not have on the roster."
  },
  {
    "id": "FLOW-14",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "Repo-wide `grep -rn process_flows` returns only: orgGraph.ts:134 (line drawing), processFlows.ts (FlowPanel/AreaKnowledgePanel), flows/read route (dedupe), setup/page.tsx:94 (a count), exportTables/dataRestore. No orchestrator tool touches it. lib/orchestrator/tools.ts:369-377 `loadLineGraph` reads `knowledge_page_entities` and builds edges from same-page co-occurrence.",
    "note": "The absence claim holds — no reasoning surface reads process_flows. Severity lowered because the finding's harm story ('asserts a connection that may not exist') is directly guarded against: tools.ts:296-300 hard-codes a `basis` string returned with every result ('This is sheet-level connectivity, not valve-by-valve line tracing') and tools.ts:352-355 adds an explicit 'Do not invent the routing between them' instruction. This is an unrealised integration, not a wrong answer being asserted."
  },
  {
    "id": "FLOW-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20261017_process_flows.sql:49-54 `CREATE POLICY process_flows_insert ON process_flows FOR INSERT WITH CHECK (EXISTS (SELECT 1 FROM org_members m WHERE m.org_id = process_flows.org_id AND m.uid = auth.uid() AND m.status = 'active') AND created_by = auth.uid());` — active membership and self-authorship only; no is_org_controller(), no status check. processFlows.ts:59-70 inserts `status: \"confirmed\", origin: \"manual\"` from the browser client. SELECT is org-wide at migration:44-47, and orgGraph.ts:286-288 renders `if (f.status !== \"confirmed\") continue; addEdge(...)`.",
    "note": "The insert policy is exactly as described, and there is no compensating UI gate: graph/page.tsx contains no role check whatsoever (grep for role/isController/canEdit returns only an unrelated /admin/proposed-links link at :745), and NodePeek.tsx:177 renders the Connect button purely on `onConnect &&`, which page.tsx:871 supplies for any doc:/asset:/cbunit:/unit: node. A Viewer's row is therefore written straight to the shared map at status='confirmed'."
  },
  {
    "id": "FLOW-3",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "admin/assets/page.tsx:56 `const ADMIN_ROLES = [\"Admin\", \"DocCtrl\", \"Manager\", \"Supervisor\"];` and :69 `const isAdmin = ADMIN_ROLES.includes(activeRole);`, passed to FlowPanel at :581. UnitOpsPanels.tsx:179-184 shows 'Read flows from a document' on `isAdmin`, and :209/:238 gate Confirm/Dismiss/Delete on the same flag. flows/read/route.ts:46-48 rejects with `!([\"Admin\", \"DocCtrl\"].includes(m.role ?? \"\"))` → 403. RLS UPDATE/DELETE (migration:56-64) require `is_org_controller(org_id) OR created_by = auth.uid()`, and is_org_controller is `role IN ('Admin','DocCtrl') OR roles && ARRAY['Admin','DocCtrl']` (20260814_documents_delete_controllers.sql:31-40). decideFlow/deleteFlow (processFlows.ts:74-88) issue no .select(), so a zero-row update returns error === null and the caller reports success.",
    "note": "The mechanism is exactly right — Manager/Supervisor get controls the server rejects, and the Confirm/Dismiss path fails with no error at all. Two things pull the severity down to MEDIUM: it fails CLOSED (no unauthorized write occurs), and 'none of them work' is overstated — the `created_by = auth.uid()` arm means a Manager can still delete or edit a flow they drew themselves on the graph; only rows created by someone else (i.e. the AI proposals) silently no-op. This is a broken-authority/UX defect, not a security or data-loss one."
  },
  {
    "id": "FLOW-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "flows/read/route.ts:58 `supabaseAdmin.from(\"assets\").select(\"id, tag\").eq(\"org_id\", orgId).eq(\"archived\", false).limit(4000)` — no ORDER BY, no unit/document scoping — then :68 `assets.slice(0, 300).forEach((a, i) => roster.push({ ref: `A${i + 1}`, ... }))`. :71 `const rosterText = roster.map(...).join(\"\\n\")` is the whole grounding set, and the system prompt at :97-99 tells the model `Use ONLY the roster handles (A1, U2, …)`, while :146-148 hard-drops any flow whose handle isn't in `byRef`.",
    "note": "Confirmed: an arbitrary, unordered 300 of up to 4000 assets, with nothing tying the roster to the unit or document being read and no warning anywhere in the response (:174-181 returns only proposed/skippedSettled/pagesRead). The failure is doubly silent — the model is simply told the rest of the plant does not exist, and the 'no new flows found' note at :179 misattributes the result to the drawing."
  },
  {
    "id": "FLOW-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "flows/read/route.ts:82-84 `.from(\"process_flows\").select(\"from_kind, from_ref, to_kind, to_ref\").eq(\"org_id\", orgId).limit(4000)` — no ORDER BY, no filter, so which 4000 rows form `settled` (:85-88) is arbitrary. The contract it is meant to enforce is a comment: processFlows.ts:72-73 `Dismissals stay as rows — the reader must never re-propose a pair a human already rejected.` and route.ts:9-10 `pairs already decided (confirmed OR dismissed) are never re-proposed`. The batch write at :170 `await supabaseAdmin.from(\"process_flows\").insert(rows)` has no onConflict, so one collision with `UNIQUE (org_id, from_kind, from_ref, to_kind, to_ref)` (migration:36) fails the whole insert → :171 `return bad(...500)`.",
    "note": "Both halves verified. Note the failure is all-or-nothing: because it is a single multi-row insert with no upsert clause, one resurrected dismissed pair discards every other valid proposal from that read and the user is charged for the AI call regardless (usage is metered at :133, before the insert)."
  },
  {
    "id": "FLOW-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20261017_process_flows.sql:17-20 `from_kind TEXT NOT NULL CHECK (...), from_ref TEXT NOT NULL, to_kind TEXT NOT NULL CHECK (...), to_ref TEXT NOT NULL` — TEXT endpoints, no FK, so no ON DELETE anything. assets.ts:216-219 `export async function deleteAsset(id: string) { const { error } = await supabase.from(\"assets\").delete().eq(\"id\", id); ... }` — no flow cleanup, and repo-wide grep finds process_flows referenced in no trigger or other migration. UnitOpsPanels.tsx:147-148 `const endpointLabel = (kind, ref) => kind === \"unit\" ? `Unit ${ref}` : (tagById.get(ref) ?? \"…\");`, fed by the batch lookup at :138.",
    "note": "Every leg holds. The graph side is worse than 'a bare ellipsis': orgGraph.ts:171 `if (a === b || !nodes.has(a) || !nodes.has(b)) return;` makes addEdge silently drop the whole flow, so the same dead row shows as an unnamed chip in the unit hub and as nothing at all on the Process lens — with no path to notice or clean it up."
  },
  {
    "id": "FLOW-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/flowsBrowse.ts:183-184 `const state: DcDocState = mirror ? (mirror.status === \"ready\" ? \"ready\" : \"indexing\")` — 'error' falls into the else branch. components/assets/UnitOpsPanels.tsx:427 `indexing: { label: \"Indexing…\", hint: \"Mirrored and being indexed — the Read button appears when it finishes (usually under a minute)...\" }`. lib/knowledgeIngest.ts:582 `.update({ status: \"error\", error: message.slice(0, 500) })` and lib/knowledge.ts:64 `error: string | null`.",
    "note": "Confirmed, and worse than stated: lib/knowledgeIngest.ts:539 selects `.in(\"status\", [\"pending\", \"stale\", \"indexing\"])`, so an 'error' doc is NEVER re-picked-up — the spinner really is forever. app/api/flows/browse/route.ts:60 selects only `id, name, library_id, page_count, status, source_document_id`, so the stored error message never even leaves the server."
  },
  {
    "id": "FLOW-8",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/orgGraph.ts:175 `const key = a < b ? `${a}|${b}|${type}` : `${b}|${a}|${type}`;` — order-insensitive. supabase/migrations/20261017_process_flows.sql:36 `UNIQUE (org_id, from_kind, from_ref, to_kind, to_ref)` is order-SENSITIVE, so both legs legitimately exist as rows. lib/orgGraph.ts:288 `addEdge(flowNodeId(f.from_kind, f.from_ref), flowNodeId(f.to_kind, f.to_ref), \"flow\")` loses the second.",
    "note": "The mechanism is exactly as claimed — two rows, one drawn edge, and two under-counted degrees. Severity is too high: because the renderer draws flows as undirected lines with no arrowhead and no direction readout (see FLOW-10, OrgGraph2D.tsx:202), one line and two lines between the same pair are visually and semantically identical today, so the user-visible loss is a link count."
  },
  {
    "id": "FLOW-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/orgGraph.ts:62 `const EDGE_CAP = 8000;`; lib/orgGraph.ts:80-83 pageRows issues `.from(table).select(select).eq(\"org_id\", orgId).range(...)` with NO `.order()`. Truncation pushes exist only for docAssets (166), projectDocs (167) and mentions (306) — flows.capped is never read. lib/processFlows.ts:41-42 `.order(\"created_at\", { ascending: true }).limit(4000)`.",
    "note": "Confirmed by repo-wide check: `grep -n truncations lib/orgGraph.ts` shows no push for `flows.capped`, so the flow cap is genuinely silent. FlowPanel (UnitOpsPanels.tsx:121, 172-174) renders `${confirmed.length} confirmed` straight off that oldest-4000 slice with no cap warning."
  },
  {
    "id": "GM-1",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "app/(protected)/graph/page.tsx:239-242 `const insights = React.useMemo(() => (view ? computeInsights(view.nodes, view.edges) : null), [view]);` where `view` (151-180) is the type-filtered slice. page.tsx:431 `{ key: \"equipment\", ... hidden: [\"unit\", \"plant\", \"project\", \"library\"] }` removes the cbunit nodes, and lib/graphInsights.ts:67-68 `.filter((n) => ORPHANABLE.has(n.type) && !degree.has(n.id))` then calls those assets orphans. page.tsx:614-616 prints \"Floating with no equipment, unit, project or link — no context yet.\"",
    "note": "Mechanism confirmed exactly, including the badge at page.tsx:581-585 rendering `insights.orphans.length` unqualified. Downgraded from HIGH because view-scoped analysis is a defensible design (the empty-state copy at page.tsx:610 already says 'everything SHOWN is tied into the web') — the actual defect is the non-empty branch's copy asserting a plant-wide fact and an unlabelled count, i.e. misleading UI rather than broken analysis."
  },
  {
    "id": "GM-10",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "supabase/migrations/20260606_operational_entity_graph.sql:112-114 adds `documents.plant_id`, `documents.unit_id`, `documents.system_id`; lib/orgGraph.ts:110 selects only `\"id, document_number, title, library_id, unit_id, sheet_number, sheet_total\"`, and GraphNodeType (orgGraph.ts:23) has no \"system\" member. graphInsights.ts:39 `edges.filter((e) => e.type !== \"library\")` confirms a library-only document is contextually invisible.",
    "note": "Factually correct — systems are a whole hierarchy level the graph never models, and documents.plant_id/system_id are read nowhere in it. But a repo-wide grep for `system_id`/`systemId` shows NO primary write path: the only writers are lib/documentLifecycle/common.ts:182 (from an `input.systemId` no UI supplies), split.ts:115 and merge.ts:124 which merely copy an existing value. No component or API route sets it, so the described scenario is essentially unreachable through the product. Downgrade to LOW (a modelling gap, not a live defect)."
  },
  {
    "id": "GM-11",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/orgGraph.ts:179-180 `nodes.get(a)!.degree += 1; nodes.get(b)!.degree += 1;` runs inside addEdge for EVERY type including `library` (orgGraph.ts:260 `addEdge(\"doc:…\",\"lib:…\",\"library\")`). components/graph/NodePeek.tsx:89 prints `{node.degree} connection…` from that field. Meanwhile graphInsights.ts:39+62-64 builds its degree map from `contextEdges` (library excluded) over the already-filtered `view.edges` (page.tsx:240), and page.tsx:645 prints `{h.degree}` from it. page.tsx:408-418 builds the peek's own list from `view.edges` too.",
    "note": "Confirmed: the peek header's number comes from the unfiltered full graph, while both the row list beneath it and the Hubs number come from the filtered context web, so the same node can show 3 in the header and 2 rows / 2 in Hubs on one screen. Genuine but cosmetic — no data is lost or wrong, only inconsistently labelled — so LOW."
  },
  {
    "id": "GM-12",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/orgGraph.ts:61-62 `const EDGE_PAGE = 1000; const EDGE_CAP = 8000;` with pageRows' `for (let from = 0; from < cap; from += EDGE_PAGE)` (orgGraph.ts:79) — 8 requests per chain, and buildOrgGraph issues SIX pageRows chains (orgGraph.ts:118-134) plus 8 single selects, i.e. up to ~56 requests / ~56k rows. app/(protected)/graph/page.tsx:115 `buildOrgGraph(activeOrgId)` runs in an effect keyed `[activeOrgId]` (page.tsx:132) with no memo. BUT page.tsx:111-114 `const cached = window.sessionStorage.getItem(snapKey); if (cached) setGraph(JSON.parse(cached) as OrgGraph);`.",
    "note": "The rebuild-on-every-mount and the row volume are real (the finding in fact understates both: six chains, not five, and ~56k rows, not 48k). Two corrections lower it: the six chains run CONCURRENTLY inside one Promise.all, not sequentially, and a stale-while-revalidate sessionStorage snapshot (page.tsx:110-122) means a returning user sees the previous map immediately rather than 'no visible progress beyond a spinner' — the spinner only appears on a first visit or an oversized (>2MB) graph. A perf/cost concern, not a correctness one: LOW."
  },
  {
    "id": "GM-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/orgGraph.ts:17-18 promises \"Every list is capped and the truncation is reported, never silent.\" Only three `.capped` flags are ever read — orgGraph.ts:166 `docAssets.capped`, :167 `projectDocs.capped`, :306 `mentions.capped`. `related.capped`, `supersessions.capped` and `flows.capped` appear nowhere (repo-wide grep for `.capped` confirms). orgGraph.ts:141-143 `.from(\"knowledge_documents\").select(\"id, source_document_id\")…limit(5000)`, then :303 `if (!docId) { unmappedMentions += 1; continue; }` and :311-313 attribute those to \"library-only documents with no controlled counterpart\".",
    "note": "Both halves confirmed and the module's own header contract at lines 17-18 is the line that settles it. The kdoc-mirror cap is the sharper defect: a mention on a mirrored PDF beyond the 5000th mirror is misreported to the user as evidence that the PDF was never brought under document control — a false explanation, not merely a missing one. Libraries and projects are also silently capped at 300 (orgGraph.ts:107-108), and plot plans at 300 (:137). MEDIUM stands."
  },
  {
    "id": "GM-14",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "supabase/migrations/20260921_drawing_entities.sql:38 `REVOKE ALL ON knowledge_page_entities FROM public, anon, authenticated;` — and the migration's own header at :15-16 states the intent: \"Service-role only: entities mirror ACL-protected documents; the drawing API filters per caller through the real ACL engine, same as ask.\" lib/orgGraph.ts:20 `import { supabase } from \"@/lib/supabase\";` (anon client) and page.tsx:115 calls buildOrgGraph client-side. Every actual reader uses supabaseAdmin (app/api/knowledge/drawing/route.ts:90, locate/route.ts:81, lib/orchestrator/tools.ts:323, lib/knowledgeTagResolve.ts:28).",
    "note": "Every factual assertion checks out — the REVOKE is real, the graph is 100% client-assembled, and a sheet edge type added to buildOrgGraph would silently return nothing. But this is a deliberate, documented ACL decision (the table mirrors per-document ACLs the anon client cannot evaluate), and no shipped feature is broken by it — the finding describes a hypothetical future feature. That is a design constraint to note, not a MEDIUM defect: LOW."
  },
  {
    "id": "GM-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/orgGraph.ts:255-256 `if (a.unit_code) addEdge(...cbunit:${a.unit_code}...); if (a.unit_id) addEdge(...unit:${a.unit_id}...)` vs :259 `if (d.unit_id) addEdge(`doc:${d.id}`, `unit:${d.unit_id}`, \"unit\")` — two families, and no code anywhere joins `cbunit:<code>` to `unit:<uuid>`. supabase/migrations/20260606_operational_entity_graph.sql:106 `ALTER TABLE assets ADD COLUMN IF NOT EXISTS unit_id UUID REFERENCES units(id)`.",
    "note": "The 'never written' half is confirmed by repo-wide search: `createAsset` (lib/assets.ts:183-203) inserts only org_id/tag/tag_normalized/type_id/description/location/library_id/created_by/updated_by plus unit_code and code; `updateAsset` (assets.ts:206) whitelists `\"tag\"|\"type_id\"|\"description\"|\"location\"|\"library_id\"|\"archived\"|\"cover_photo_id\"|\"unit_code\"|\"code\"` — no unit_id. No migration backfills it either (only the ADD COLUMN and its index). Codebook units come from `codebook_entries` (20260928_site_codebook.sql), a different table entirely, so the two violet nodes are real."
  },
  {
    "id": "GM-3",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "lib/orgGraph.ts:80-83 `supabase.from(table).select(select).eq(\"org_id\", orgId).range(from, Math.min(from + EDGE_PAGE, cap) - 1)` — no `.order()` anywhere in pageRows, yet :166 pushes \"Equipment-tag links capped — densest web shown.\" and :306 \"Mention links capped — densest web shown.\" lib/orgGraph.ts:174 `if (a === b || !nodes.has(a) || !nodes.has(b)) return;` drops every edge to a capped-out node with no counter.",
    "note": "Both halves are true: 'densest web shown' is a fabrication over an unordered PostgREST range, and edges to nodes cut by DOC_CAP/ASSET_CAP vanish uncounted. Downgraded from HIGH because the page does warn about the node caps themselves (page.tsx:737-741 renders 'Showing the 1500 most recently updated documents' / 'Showing the first 2000 equipment items'), so the user is told the map is partial — the defect is that the description of HOW it is partial is false."
  },
  {
    "id": "GM-4",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "lib/orgGraph.ts:152-155 `const optional = <T,>(res) => { if (res.error) return []; return (res.data as T[]) ?? []; };` — every error code, not just 42P01. Applied at :159-162 (assets, units, plants, projects), :216 and :275 (plot plans), :296 (kdoc mirrors). No truncation string is pushed on that path.",
    "note": "The core claim holds for assets/units/plants/projects/plot-plans, and the contrast is stark against orgGraph.ts:86-87 where pageRows narrows to `if (error.code === \"42P01\" || /does not exist/i.test(error.message)) return { rows, capped: false }; throw new Error(error.message);`. Corrected because the title is wrong about FLOWS: process_flows goes through pageRows (:133-134), which THROWS on a real error rather than swallowing it. Severity lowered accordingly — the swallowed set is smaller than claimed and the surface is an advisory map."
  },
  {
    "id": "GM-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/graphInsights.ts:79-83 `const pairKey = (a, b) => (a < b ? ... )` and `multiplicity.set(k, (multiplicity.get(k) ?? 0) + 1)` — keyed on the PAIR, ignoring `e.type`; then :142 `if (low.get(u)! > disc.get(parent)! && (multiplicity.get(pairKey(parent, u)) ?? 0) === 1)`. lib/orgGraph.ts:262 adds the `tag` edge and :304 adds the `mention` edge for the same doc/asset pair, and addEdge's key includes the type (:175) so both survive.",
    "note": "Confirmed. lib/mentionIndexer.ts:104-120 writes an entity_mentions row for every asset it finds in the text with no exclusion for already-tagged pairs, so a Bridge-tagged drawing that also names the tag reliably lands at multiplicity 2 and is silently disqualified — while page.tsx:654-656 prints 'No single-thread bridges — every big neighbourhood has redundant connections.'"
  },
  {
    "id": "GM-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260708_acl_rls_enforcement.sql:84-86 `CREATE POLICY documents_acl_select ON documents AS RESTRICTIVE FOR SELECT USING (node_visible(visibility, acl_index, org_id));` with :57-60 `IF v_role IN ('Admin', 'DocCtrl') THEN RETURN true;`. supabase/migrations/20260605_rls_policies_new_tables.sql:26-27 gives assets a plain org-member policy — equipment is visible to everyone, documents are not. lib/orgGraph.ts:109-117 reads both through the RLS-bound client.",
    "note": "Confirmed: a controller's document set is a strict superset of a viewer's, assets are identical for both, so orphan/hub/bridge output is viewer-relative by construction. Nothing on the page qualifies it — page.tsx:581-586 and 614-616 print the counts and 'no context yet' with no ACL caveat, and the truncation strip (737-741) carries only cap notices."
  },
  {
    "id": "GM-7",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/linkProposals.ts:200-203 `.eq(\"org_id\", orgId).eq(\"status\", \"pending\").limit(4000); if (error) return [];` — silent cap, silent failure. app/(protected)/graph/page.tsx:130 `.catch(() => { if (alive) setProposals([]); });`. page.tsx:748 renders `{view.ghosts.length} dashed connection…awaiting review` — the post-cap, post-filter number. BUT supabase/migrations/20260807_link_proposals.sql:28-29 `document_id UUID NOT NULL REFERENCES documents(id)… target_document_id UUID NOT NULL REFERENCES documents(id)`.",
    "note": "The truncation/error half is real: 4000 is a hard silent cap, an error resolves to an empty array indistinguishable from 'no proposals', and the on-map chip reports the drawn count as if it were the queue. The 'drawn as document↔document unconditionally' half is REFUTED — both endpoint columns are NOT NULL FKs to `documents`, so the `doc:` prefix at page.tsx:128 is correct by schema, not an unchecked assumption. Impact is a misleading count on a map that links straight to the authoritative /admin/proposed-links queue, so LOW."
  },
  {
    "id": "GM-8",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/orgGraph.ts:175-177 `const key = a < b ? `${a}|${b}|${type}` : `${b}|${a}|${type}`; if (edgeSeen.has(key)) return;` — the dedup key is order-insensitive, while supabase/migrations/20261017_process_flows.sql:36 `UNIQUE (org_id, from_kind, from_ref, to_kind, to_ref)` is ORDERED, so A→B and B→A are both legal rows.",
    "note": "The mechanism is real — two antiparallel flow rows collapse to one edge keeping only the first-seen direction — but the title's consequence and the report's own example are false. The cited distillation loop (tower→condenser, condenser→drum, drum→tower) is three DISTINCT unordered pairs and renders completely; only a true 2-node A⇄B recycle is lost. Compounding the low impact: DEFAULT_GRAPH_SETTINGS.showArrows is `false` (lib/graphSettings.ts:65), so direction is not drawn by default at all."
  },
  {
    "id": "GM-9",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/graphInsights.ts:47 `const REGION_ANCHOR_ORDER: GraphNodeType[] = [\"unit\", \"library\", \"project\", \"asset\", \"plant\", \"document\"];` — \"plot\" is absent though it is a declared GraphNodeType (lib/orgGraph.ts:23) and plot nodes are created (orgGraph.ts:216-221). graphInsights.ts:171-174 `const rank = REGION_ANCHOR_ORDER.indexOf(n.type); … if (rank < anchorRank || …) { anchor = n; anchorRank = rank; }` — indexOf returns -1, which beats every real rank including \"unit\" at 0.",
    "note": "Confirmed exactly as claimed: any plot-plan node in a component wins the anchor race outright, so the region is named after the plot plan rather than its unit. Real but purely a display-label defect on the zoomed-out map (and the Process lens hides `plot` entirely, page.tsx:430), so LOW rather than MEDIUM."
  },
  {
    "id": "GOV-1",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "HIGH",
    "evidence": "lib/ai/usageServer.ts:63 `.eq(\"op\", \"knowledgeAsk\")` inside getMonthUsage — the ONLY ledger read behind every gate. recordAskUsage writes `op: input.op ?? \"knowledgeAsk\"` (usageServer.ts:114), and all eight enforcement sites (governedCall.ts:63-69, ask/route.ts:253-262, orchestrator:105-109, flows/read:119-120, knowledge/embed:139-145, codebook/import:92-93, knowledgeEmbedDrain:89-92, knowledgeIngest:511-515) call that same getMonthUsage. Damning: usageServer.ts:109-111 comments \"vision indexing bills as knowledgeVision so the spend is visible as its own line but SHARES THE SAME CAP\" — it does not.",
    "note": "Confirmed with no compensating control anywhere: a repo-wide grep found no other ledger reader. Two corrections. The op count is TWELVE, not sixteen — checklistAssess, checklistSegment, codebookImport, drawingLocate, flowRead, graphShape, knowledgeEmbed, knowledgeVision, orchestrator, qualityManualReview, skillAssist, templateDraft. And the exposure is bounded: spend lands on the member's OWN provider key (governedCall.ts:42-48 forbids a workspace fallback), and knowledgeAsk — the highest-volume surface — is correctly metered. Budget-governance failure, not a breach: HIGH, not CRITICAL."
  },
  {
    "id": "GOV-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/ai/usage/route.ts:36 `isController: roles.has(\"Admin\") || roles.has(\"DocCtrl\")`; :107 `if (!auth.isController) return bad(\"Only Admin or Doc Control can set monthly caps.\", 403);`; :134 `if (!Number.isFinite(capUsd) || capUsd < 0 || capUsd > 10000)`. No self-target check: `targetUserId` may equal `auth.userId` (:109-116 only verifies active membership), and omitting userId sets the org default. `lib/capabilityPolicy.ts` is never imported here — its importers are archive-view, analytics, requests/[id], tickets/workflow-action, workflow.ts, holds.ts only.",
    "note": "Confirmed on every element. One nuance the finding's summary omits: an audit_logs row IS written (:157-162, action AI_CAP_CHANGED, with targetUserId and capUsd), so the change is traceable after the fact — but that is a log, not the approval/second-signature/notification the summary says is absent, and the bypass of the capability-policy layer stands."
  },
  {
    "id": "GOV-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Repo-wide grep for `ai_key_agreements` finds the gate in exactly four callers: app/api/knowledge/ask/route.ts:234, app/api/orchestrator/route.ts:89, app/api/codebook/import/route.ts:84, and lib/ai/governedCall.ts:52. It is absent from all five cited routes. flows/read/route.ts:106-107 even documents the omission: \"this route runs the same gate order inline: own key -> provider allowlist -> cap -> call -> meter\" — the agreement is simply not in that list. templates/generate/route.ts:179-202 goes conn -> ALLOWED_PROVIDERS -> cap -> call. knowledge/embed/route.ts:128-149 goes conn -> embeddingConnectionFrom -> cap. knowledge/locate/route.ts:172-194 goes conn -> ALLOWED_PROVIDERS -> cap. ai/connection/route.ts has no gate at all.",
    "note": "Confirmed. lib/ai/governedCall.ts:4-7 names the agreement as one of the five gates every feature route needs, and lib/ai/pricing.ts:29-33 states the ask route refuses to answer for anyone unsigned — yet five direct-calling routes skip it, and connection/route.ts:269 makes a provider call at key-save time before anything can have been signed. The only quibble is arithmetic: I count 8 provider-calling route files (codebook/import, ai/connection, orchestrator, flows/read, templates/generate, knowledge/ask, knowledge/locate, knowledge/embed), not 9; the numerator of 5 is exact."
  },
  {
    "id": "GOV-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/ai/keyVault.ts:17-36 `function cryptoConfigured() { const hex = process.env.EXPORT_ENCRYPTION_KEY || \"\"; return hex.length === 64; }` ... `if (!cryptoConfigured()) { console.warn(\"EXPORT_ENCRYPTION_KEY not set — storing AI provider key UNENCRYPTED. ...\"); return plain; }`. connection/route.ts:284 `...(apiKey ? { api_key: sealAiKey(apiKey), key_last4: apiKey.slice(-4) } : {})` stores the return value verbatim. openAiKey (:39-43) passes anything without the `encv1:` prefix straight through, so plaintext rows read back fine and nothing ever surfaces the degradation.",
    "note": "Confirmed, and the contradiction is in-repo: lib/serverCrypto.ts:5-7 states the design intent — \"If unset, the API endpoints refuse to save credentials — we never want plaintext secrets on disk by accident\" — while keyVault deliberately inverts it. lib/__tests__/keyVault.test.ts:31 pins the behavior (\"degrades to plaintext storage when EXPORT_ENCRYPTION_KEY is unset\"). Repo-wide grep confirms no UI, health check, or schemaExpectations entry ever reports the unconfigured state; console.warn is the only signal."
  },
  {
    "id": "GOV-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/ai/governedCall.ts:63-69 `const [monthSoFar, capUsd] = await Promise.all([getMonthUsage(orgId, userId), getCapUsd(orgId, userId)]); if (capUsd > 0 && monthSoFar.spentUsd >= capUsd) throw ...` followed at :78 by the unbounded `callAiModel`. Identical read-then-call at ask/route.ts:252-262 and orchestrator/route.ts:104-114. lib/ai/usageServer.ts:118 `await supabaseAdmin.from(\"ai_usage_events\").insert(full)` is a plain INSERT — there is no reservation row, no advisory lock, no atomic counter, and no UPDATE ... WHERE anywhere in the file.",
    "note": "Confirmed exactly: the gate is `spent >= cap`, so any request that starts at $9.99 of $10 may spend arbitrarily much, and N concurrent requests all read the same pre-spend total. The orchestrator case is worse than described — it meters once at route.ts:145-148 after the entire multi-round loop, and lib/ai/usageServer.ts:63/74 restrict getMonthUsage to `op = \"knowledgeAsk\"`, so `op:\"orchestrator\"` rows never enter the cap total on any later request either."
  },
  {
    "id": "GOV-14",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/knowledgeEmbedDrain.ts:62-64 `const marker = (lib.ai_features?.embedBuild ?? null) as { userId?: string } | null; const userId = marker?.userId; if (!userId) continue;` — the id is read straight out of the knowledge_libraries.ai_features JSONB with no membership, identity, or consent check, then used at :76 `.eq(\"user_id\", userId)` to load and spend that person's key and at :124-127 to write their metering row. lib/ai/usageServer.ts:95 `.or(`user_id.is.null,user_id.eq.${userId}`)` interpolates the same value into a PostgREST filter expression unescaped.",
    "note": "Confirmed on both halves. supabase/migrations/20260911_knowledge_ai.sql:119-122 `CREATE POLICY knowledge_libraries_write ON knowledge_libraries FOR ALL USING (is_org_controller(org_id)) WITH CHECK (is_org_controller(org_id))` means any Admin/DocCtrl can write that JSONB directly, so the marker is controller-writable, not service-role-only. The drain does respect the named user's cap (:89-95) — but it is their cap and their money, which is the finding's point."
  },
  {
    "id": "GOV-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20261016_reasoning_skills.sql:23-24 `visibility TEXT NOT NULL DEFAULT 'org'`, `enabled BOOLEAN NOT NULL DEFAULT true`; :44-49 `CREATE POLICY answer_skills_insert … WITH CHECK (EXISTS (SELECT 1 FROM org_members m WHERE m.org_id = answer_skills.org_id AND m.uid = auth.uid() AND m.status = 'active') AND created_by = auth.uid());` — active membership only, NO role predicate (contrast process_flows_update at 20261017:59 which does use `is_org_controller(org_id)`). lib/answerSkillsServer.ts:29-30 `rows.filter((r) => r.enabled && (r.visibility === \"org\" || …))` and :44-46 splice the text into the prompt; ask/route.ts:1483 and orchestrator/route.ts:119 both call loadAnswerSkillsBlock.",
    "note": "Right, and the line that settles it is the missing role check in answer_skills_insert. I looked for a guard elsewhere and there is none: lib/answerSkills.ts:87 writes through the browser's anon `supabase` client, so RLS is the entire boundary — any UI role gating is bypassable with a direct PostgREST call. A Viewer's row defaults to enabled + org-visible and lands verbatim in every teammate's system prompt on both the ask and orchestrator paths. HIGH stands."
  },
  {
    "id": "GOV-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/ai/usage/route.ts:134 `if (!Number.isFinite(capUsd) || capUsd < 0 || capUsd > 10000)` — 0 passes validation and is persisted (:151). lib/ai/usageServer.ts:100 `return Number.isFinite(cap) && cap >= 0 ? cap : DEFAULT_MONTHLY_CAP_USD;` — returns 0, not the default. Every gate then reads `if (capUsd > 0 && monthSoFar.spentUsd >= capUsd)` — governedCall.ts:66, ask/route.ts:257, orchestrator:109, flows/read:120, knowledge/embed:143, codebook/import:93, knowledgeEmbedDrain:92, knowledgeIngest:515. Meanwhile usage/route.ts:52 `const percent = capUsd > 0 ? … : 100;` and AiSettingsModal.tsx:469 `const capped = usage.percent >= 100;` → :534-537 \"Cap reached — questions are locked until the 1st\".",
    "note": "Confirmed end to end, and worse than a single-surface bug: all EIGHT enforcement sites share the `capUsd > 0` short-circuit, so $0 uncaps every AI surface simultaneously. The UI inversion is exact — capUsd 0 forces percent to 100, painting the bar rose and asserting questions are locked at the precise moment nothing is. AiSettingsModal.tsx:476 `if (!Number.isFinite(cap) || cap < 0)` lets the operator type it. HIGH stands."
  },
  {
    "id": "GOV-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/ai/usageServer.ts:65 `if (error) return EMPTY_USAGE;` where `EMPTY_USAGE = { spentUsd: 0, … }` (:30-32) — any ledger read failure resolves to $0 spent, and every gate's `spentUsd >= capUsd` then passes. lib/ai/usageServer.ts:122-126 `const { error } = await supabaseAdmin.from(\"ai_usage_events\").insert(full); if (error && (error.code === \"PGRST204\" || /column/i.test(error.message))) { await supabaseAdmin.from(\"ai_usage_events\").insert(base) … }` where `base` (:114) carries user_id/org_id/op/provider/ok but NO est_cost_usd.",
    "note": "Both variants confirmed. The pre-migration path is self-consistent with the reported symptom: rollup at :48 does `if (r.ok !== false) out.asks += 1` so the base rows inflate the visible question count, while :45 `out.spentUsd += Number(r.est_cost_usd ?? 0)` keeps the total at $0.00 — exactly the '$0.00 of $10.00 · 0%' next to a growing 'N questions' the summary describes. A spend control that fails open on its own read error is textbook; HIGH stands."
  },
  {
    "id": "GOV-5",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "lib/knowledgeEmbedDrain.ts:88-92 `const [month, capUsd] = await Promise.all([getMonthUsage(lib.org_id, userId), getCapUsd(lib.org_id, userId)]); if (capUsd > 0 && month.spentUsd >= capUsd)` — then :124-127 records its own spend as `op: \"knowledgeEmbed\"`. lib/knowledgeIngest.ts:511-515 the identical pattern, then :587-591 records `op: \"knowledgeVision\"`. Both meter into an op that lib/ai/usageServer.ts:63's `.eq(\"op\", \"knowledgeAsk\")` filters out.",
    "note": "The substance holds — two unattended crons spend a member's key against a cap that can never see their own spend, so they never self-limit no matter how many days they run. Two corrections. The title's 'structurally always reads $0' is false: getMonthUsage returns that member's knowledgeAsk spend, so a sponsor who also asks questions does eventually trip the gate. And this is not an independent defect — it is GOV-1's single root cause (usageServer.ts:63) observed on a second surface, so it should not carry HIGH on top of GOV-1's severity. MEDIUM."
  },
  {
    "id": "GOV-6",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "lib/ai/pricing.ts:22 `export const ALLOWED_PROVIDERS: readonly AiProviderId[] = [\"anthropic\", \"openai\"];` under a header stating \"ONLY Anthropic and OpenAI are allowed, for ANY scope\" (:10-11), and :24-27 PROVIDER_BLOCK_MESSAGE ends \"blocked entirely, for every scope. No exceptions.\" Yet app/api/ai/connection/route.ts:174-176 and :204-206 validate against a SEPARATE list — `if (!EMBEDDING_PROVIDER_IDS.includes(ep)) return bad(\"Embeddings provider must be Voyage AI or OpenAI.\", 400)` — never touching ALLOWED_PROVIDERS. lib/ai/embeddings.ts:141-143 `provider === \"voyage\" ? \"https://api.voyageai.com/v1/embeddings\"` posts the passage text (:139). pricing.ts:46-56 PROVIDER_AGREEMENT_NOTES has entries for anthropic and openai only, so buildAgreementText (:62-65) never names Voyage.",
    "note": "The contradiction is real and the code's own absolutist wording is what convicts it: a third provider receives indexed page text through a validation path that structurally bypasses the allowlist, and the signed agreement text is never updated to say so. Lowered from HIGH because the omission is disclosure, not concealment: the user must deliberately pick 'Voyage AI' from a labelled dropdown (embeddings.ts:31-50, hint at :42) and paste a Voyage key, so no content reaches Voyage without an explicit per-user act. MEDIUM."
  },
  {
    "id": "GOV-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/ai/connection/route.ts:145-150 `const out = await callAiModel({ provider, model, apiKey, system: \"You are a connection test...\", user: \"Connection test.\", maxTokens: 500 });` and :269-274 the same call on save. A grep for `recordAskUsage|getMonthUsage|getCapUsd|governedCall|ai_key_agreements` across the whole file returns ZERO hits — the file imports only `callAiModel`, `ALLOWED_PROVIDERS`, `embedPassages`, `sealAiKey/openAiKey`.",
    "note": "Confirmed. `action:\"test\"` is repeatable with no idempotency, no cap read, no ledger insert, and there is no middleware.ts and no rate limiter anywhere in the repo to bound it. The embedding-test path (:177-185) and the save-path embed verify (:215-228) are two more unmetered billed calls the finding does not even count, so if anything it under-states the surface."
  },
  {
    "id": "GOV-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/knowledge/locate/route.ts:216-220 `await recordAskUsage({ orgId, userId: user.id, provider, model: VISION_MODEL[provider] ?? (conn.model as string), usage: out.usage, ok: true, op: \"drawingLocate\" });` — then :236-237 `const REFINE_MAX = 4; const CROP_DIVISORS = [3, 9];` drives :255-266 a second `callAiModel(...)` per (tag, divisor), whose tokens go to :267-268 `out.usage.inputTokens += fine.usage.inputTokens; out.usage.outputTokens += fine.usage.outputTokens;`. After the loop `out.usage` is never referenced again (only `out.text` at :222, before the loop).",
    "note": "Confirmed: 1 metered coarse call + up to 4x2 = 8 unmetered refine calls, tokens accumulated into an already-written row's source object. The 8 are bounded further by the 40s budget check at :244, so the real-world miss is often smaller than 8/9 — but the structural bug (meter-before-spend) is exactly as described. Corroborates the cap-exclusion claim: lib/ai/usageServer.ts:63 filters `.eq(\"op\", \"knowledgeAsk\")`, so drawingLocate rows never count toward the cap at all."
  },
  {
    "id": "GOV-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/knowledge/ask/route.ts CitationOut is `{ n; documentId; documentName; page; section; quote; tags?; libraryName?; tier? }` with `quote: truncateSafe(c.content, 1600)` — no provenance field. The renderer at app/(protected)/knowledge/[id]/page.tsx:1089-1103 is commented \"Library citation -> expandable verbatim passage\" and prints `{c.quote}` inside `<blockquote>` unqualified. The only provenance that exists is document-level: supabase/migrations/20260922_vision_pages.sql:13 `ALTER TABLE knowledge_documents ADD COLUMN IF NOT EXISTS vision_pages INTEGER NOT NULL DEFAULT 0;`, incremented per document at lib/knowledgeIngest.ts:458-466.",
    "note": "Confirmed by repo-wide search: no migration adds any per-chunk source/vision column (only `section` in 20260914 and `tsv` in 20261007 touch knowledge_chunks), so there is nowhere to record that a given chunk came from lib/knowledgeVision.ts's haiku-tier OCR (VISION_MODEL at :26-30). The transcript flows into the normal chunk pipeline and is rendered identically to text-layer extraction."
  },
  {
    "id": "GPV-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/graph/ask/route.ts:83 `const { data: rawHits, error: askErr } = await supabaseAdmin.rpc(\"graph_ask\", {...})` — supabaseAdmin is built from `SUPABASE_SERVICE_ROLE_KEY` (lib/supabaseAdmin.ts:4-6), which is BYPASSRLS, so the `SECURITY INVOKER` on the function (20260929_mention_engine.sql:114) buys nothing. The function body has no ACL predicate at all: `FROM knowledge_chunks c JOIN knowledge_documents d ON d.id = c.document_id WHERE c.org_id = p_org_id AND c.tsv @@ websearch_to_tsquery(...)`. Compare knowledge/ask/route.ts:157-186, which builds `excludedDocIds` from `readableControlledDocIds(principal, dcIds)` and fails closed; graph/ask has no equivalent — its only auth is the membership check at :74-77, and it returns `hits: shown` (:178) with `snippet` straight to the client (rendered at app/(protected)/graph/page.tsx:796).",
    "note": "Confirmed, and the route's own header comment at :21-23 asserts the opposite (\"the corpus RPC runs SECURITY INVOKER under the caller's own RLS, so this can never widen what the person is allowed to read\"), which is precisely why it was never caught. supabase/migrations/20260917_knowledge_sources.sql:69-82 shows the chunk RLS the service role is bypassing: `knowledge_chunks_select` explicitly excludes any chunk whose document has `source_document_id IS NOT NULL`. CRITICAL is the right severity — any active member, including a Viewer with no ACL grant, gets ts_headline excerpts of controlled-document text."
  },
  {
    "id": "GPV-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/graph/page.tsx:430 `{ key: \"process\", label: \"Process\", hidden: [\"document\", \"library\", \"project\", \"plot\"], libEdges: false, title: \"Units and equipment only — the plant as flow. …\" }` — \"plant\" is not in `hidden`, so plant nodes survive (and lib/orgGraph.ts:253 `addEdge(`unit:${u.id}`, `plant:${u.plant_id}`, \"unit\")` guarantees they have degree and are not pruned by 321-325). Line 431 has the same defect: `hidden: [\"unit\", \"plant\", \"project\", \"library\"]` leaves `plot` visible under the title \"Equipment and the documents that govern it\". Line 486 `<div className=\"hidden sm:flex items-center rounded-full …\">` wraps the entire lens row.",
    "note": "Two of the four lens titles misdescribe what the lens produces, and the row is display:none below Tailwind's sm breakpoint (640px). The un-highlighting behaviour follows directly from the exact-set match at 434-437 (`l.hidden.length === settings.hiddenTypes.length && …`)."
  },
  {
    "id": "GPV-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/graph/page.tsx reads `params.get(\"focus\")` at line 63 and never writes back: the only router call is `open()` at 301-303 `router.push(`${n.href}${n.href.includes(\"?\") ? \"&\" : \"?\"}from=graph`)`. Lens taps go to state only — 489 `onClick={() => patchSettings({ hiddenTypes: l.hidden, showLibraryEdges: l.libEdges })}` — as do `setFocusId`, `setHighlight`, `setRawQuery` and `setAnswer` (65-95). components/graph/BackToGraphChip.tsx:23 `onClick={() => router.push(\"/graph\")}` carries no state. lib/graphSettings.ts:119-121 `localStorage.setItem(settingsKey(orgId), JSON.stringify(s))` is per-viewer, per-org.",
    "note": "The central claim holds: no lens, focus, search, highlight or answer ever reaches the URL, so a pasted /graph link reproduces the recipient's own saved settings and nothing of the sender's view. One nuance on the chip — its comment's promise of \"the per-org saved layout and settings restore\" IS honoured (loadSettings at page.tsx:99 plus the `posKey` restore at 187-190); what the round trip loses is the ephemeral state (focusId, selection, highlight, answer), so \"the exact map you left\" is the part that overstates."
  },
  {
    "id": "GPV-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/graph/ask/route.ts:50 declares `mode: \"evidence\" | \"answered\";` and lines 16-19 document the ANSWERED mode, but a repo-wide grep for the literal `\"answered\"` returns exactly one hit — that type declaration. Both return sites hardcode evidence: line 110 `mode: \"evidence\", question, answer: null, …` and line 175-177 `mode: \"evidence\", question, answer: null,`. The client type at page.tsx:51-57 has no `mode`/`answer` field at all. page.tsx:763 counts unconditionally: `` `${answer.hits.length} passage… · ${answer.nodeIds.length} node… lit up` ``, with no intersection against `view.nodes`. OrgGraph2D.tsx:65-66 `const pts = flyTo.ids.map((id) => sim.get(id)).filter(Boolean) …; if (pts.length === 0) return;` — filtered-out nodes are not in the sim, so the camera silently stays put.",
    "note": "Both halves confirmed. On a lens that hides `asset`, the asset nodeIds added at route.ts:152 (`nodeIds.add(`asset:${m.asset_id}`)`) are counted in the panel, are not drawable, and contribute nothing to the fly-to — with no note telling the user why."
  },
  {
    "id": "GPV-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/graph/OrgGraph2D.tsx:427-437 renders `<div ref={wrapRef} className=\"absolute inset-0 overflow-hidden\"><canvas ref={canvasRef} className=\"block select-none\" style={{ touchAction: \"none\", cursor: \"grab\" }} onPointerDown=… onDoubleClick=… /></div>` — no tabIndex, no role, no aria-label, no keyboard handler. OrgGraph3D.tsx:748-755 is the same bare `<div ref={mountRef} …>`. A grep for `Escape|keydown|onKeyDown|tabIndex|role=` across components/graph/ and the graph page returns exactly one keyboard handler, page.tsx:453 `onKeyDown={(e) => { if (e.key === \"Enter\") void runAsk(); }}` on the search input.",
    "note": "Confirmed by repo-wide search: the canvas is not focusable and exposes nothing to assistive tech, and none of the Insights (686-728 path panel, 574-683 insights, 755-815 answer, 822-854 connect) overlays has an Escape or any other key dismissal — each is closable only by its mouse-target X button."
  },
  {
    "id": "GPV-14",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/orgGraph.ts:198-203 builds cbunit nodes from `codebook?.units ?? []` using only `u.code` and `u.label`; `u.meta.links` and `u.meta.knowledgeLibraryId` are never read anywhere in the file (the only cbunit edges are lib/orgGraph.ts:255 `if (a.unit_code) addEdge(`asset:${a.id}`, `cbunit:${a.unit_code}`, \"unit\")` and the flow edge at 288). Those relationships are real and org-authored: lib/codebook.ts:29-37 `UnitResourceLink { id; label; libraryId; libraryName; folderId?; folderName? }`, lib/codebook.ts:50-53 `knowledgeLibraryId?: string`, written by `saveUnitLinks` (codebook.ts:368-380) and read by app/(protected)/admin/assets/page.tsx:232 `links: (u.meta.links ?? []) as UnitResourceLink[]` and app/api/area/knowledge-status/route.ts:54 `((unitRow.meta as { knowledgeLibraryId?: string } | null)?.knowledgeLibraryId ?? \"\").trim() || null`.",
    "note": "Confirmed — the graph's own header comment (orgGraph.ts:5-16, \"each edge is a row somewhere\") enumerates nine relationship sources and codebook unit meta is not among them, so a unit's pinned libraries and its bound knowledge shelf are stored, edited and consumed elsewhere but never drawn."
  },
  {
    "id": "GPV-2",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "lib/orgGraph.ts:37-44 `export interface GraphNode { id: string; type: GraphNodeType; label: string; sub?: string; href: string; degree: number; }` — no unitCode, unitId, plantId, or libraryId. lib/graphSettings.ts:22-52 GraphSettings offers only `hiddenTypes`, `showLibraryEdges`, `hideUnlinked`, `showProposals`, label-matched `groups` (colour only), display/force sliders and `localDepth`; there is no scope filter. The only scoping primitive in the UI is app/(protected)/graph/page.tsx:170-175, `neighborhood(focusId, buildAdjacency(edges), settings.localDepth)` — pure hop-count, exactly as the summary describes.",
    "note": "The factual claim is exact — the node record carries no scoping key and no scope control exists. I am lowering the severity because the absolute framing (\"cannot be built without changing the assembly\") is overstated for equipment: `unit`-type edges to `cbunit:<code>` are already assembled (orgGraph.ts:255), so a UI-only filter \"assets with a unit edge to cbunit:20\" is derivable from graph.edges today. It is only DOCUMENTS that genuinely need an assembly change — and that is GPV-3's finding, so GPV-2's remaining independent content is a missing-feature/UX gap, not a HIGH defect."
  },
  {
    "id": "GPV-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/orgGraph.ts:258-261 is the only place documents get a unit edge: `for (const d of docs) { if (d.unit_id) addEdge(`doc:${d.id}`, `unit:${d.unit_id}`, \"unit\"); addEdge(`doc:${d.id}`, `lib:${d.library_id}`, \"library\"); }` — `cbunit:` never appears on a doc edge. Assets get BOTH (:254-257: `if (a.unit_code) addEdge(..., `cbunit:${a.unit_code}`, \"unit\"); if (a.unit_id) addEdge(..., `unit:${a.unit_id}`, \"unit\");`). Both families are emitted as `type: \"unit\"` (:190-203), so both render under the same \"Units\" label.",
    "note": "Confirmed, including the one escape hatch I checked: the flow edges at :284-289 use `flowNodeId = (kind, ref) => kind === \"asset\" ? `asset:${ref}` : `cbunit:${ref}``, but app/api/flows/read/route.ts:67 types the roster as `kind: \"asset\" | \"unit\"` only — no document ever enters process_flows, so no doc-to-cbunit edge can arise there either. The two families are genuinely disjoint and a document is two hops from a codebook unit, always mediated by an asset."
  },
  {
    "id": "GPV-4",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "app/(protected)/graph/page.tsx:46 `document: \"Documents\", asset: \"Equipment\", unit: \"Units\"` (duplicated verbatim at components/graph/GraphControls.tsx:23) vs page.tsx:431 `{ key: \"equipment\", label: \"Equipment ↔ Docs\", hidden: [\"unit\", \"plant\", \"project\", \"library\"], ... }`. The count claim checks out: page.tsx:244-249 `for (const n of graph?.nodes ?? []) c[n.type] += 1;` — computed over `graph`, the UNFILTERED build, while the number is rendered next to the filter checkbox at GraphControls.tsx:153 alongside a map that is drawing `view.nodes`. \"Units\" covering two families is confirmed by orgGraph.ts:190-203 emitting both `unit:` and `cbunit:` as `type: \"unit\"`.",
    "note": "The substance holds, but \"three different things\" is an overcount: page.tsx:45-48 (TYPE_LABELS) and NodePeek.tsx:88-90 (`labelFor(node.type)`, fed `TYPE_LABELS[t].replace(/s$/, \"\")` at page.tsx:864) are the SAME referent — the asset node type — rendered in two places, not two meanings. The real collision is two-way: the type-filter row \"Equipment\" (hides nodes when unchecked) vs the lens button \"Equipment ↔ Docs\" (hides four OTHER types). That is a nomenclature/label defect with a stale-count wart and no data consequence, which reads LOW rather than MEDIUM."
  },
  {
    "id": "GPV-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/graph/page.tsx:226-237 the deep-link effect ends `}, [focusParam, view]);` and its body unconditionally runs `setSelected(node); setHighlight({ ids: [id], nonce: Date.now() });`. `view` is a useMemo whose deps are `[graph, settings, proposals, focusId]` (:150-181), and `patchSettings` (:133-140) does `const next = { ...prev, ...patch }` — a NEW settings object per slider tick — so every drag frame produces a new `view`, re-fires the effect, re-selects the node and stamps a fresh nonce. components/graph/OrgGraph2D.tsx:63-79 re-runs on `[flyTo, sim]` and rewrites `camTargetRef.current`; OrgGraph3D.tsx:511-527 re-flies whenever `fly.nonce !== lastFly`.",
    "note": "Confirmed end to end. The peek's own close handler (page.tsx:874 `onClose={() => { setSelected(null); setTrail([]); setHighlight(null); }}`) is undone by the very next settings change, and nothing in the component ever clears `focusParam` — it is read once from `useSearchParams()` at :63 and never written back — so hand-editing the URL really is the only escape."
  },
  {
    "id": "GPV-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/orgGraph.ts:114-117 `supabase.from(\"assets\").select(\"id, tag, description, unit_code, unit_id\").eq(\"org_id\", orgId).eq(\"archived\", false).limit(ASSET_CAP)` — no .order(). Same at :107 (libraries, limit 300), :108 (projects, limit 300), :137 (plot_plans, limit 300), :141-143 (knowledge_documents, limit 5000). The documents query is the ONE that is deterministic — :109-113 carries `.order(\"updated_at\", { ascending: false, nullsFirst: false })`. The truncation copy at :165 then asserts an order that does not exist: `truncations.push(`Showing the first ${ASSET_CAP} equipment items.`)`.",
    "note": "Confirmed — Postgres gives no ordering guarantee for LIMIT without ORDER BY, so which 2,000 of 3,400 assets land on the map can differ per query plan, per user, and per load. Two aggravating factors the finding is right to flag: page.tsx:110-114 paints a sessionStorage snapshot of a PREVIOUS non-deterministic build first, and Insights are computed over `view.nodes` (:239-241), so orphan/degree analyses are drawn from a random subset while the chip claims \"the first 2000\"."
  },
  {
    "id": "GPV-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/graph/page.tsx:319-322 `const unitCode = (n: GraphNode): string | null => n.id.startsWith(\"cbunit:\") ? n.id.slice(7) : n.id.startsWith(\"unit:\") ? (n.sub ?? null) : null;` — for a legacy node `sub` is `units.code` (lib/orgGraph.ts:192 `sub: u.code ?? undefined`). page.tsx:344 then draws the optimistic edge as `{ a: from.id /* unit:<uuid> */, b: target.id, type: \"flow\" }`, while the rebuild resolves every unit endpoint through lib/orgGraph.ts:284-285 `const flowNodeId = (kind, ref) => kind === \"asset\" ? `asset:${ref}` : `cbunit:${ref}`;` and lib/orgGraph.ts:174 `if (a === b || !nodes.has(a) || !nodes.has(b)) return;`.",
    "note": "Confirmed: the row is written with a Site-Codebook-code ref while the line is drawn on the legacy `unit:<uuid>` nodes, so on reload the edge is never on the nodes the user clicked. One correction to the summary's framing — the row is not always a phantom: if the codebook happens to hold that same code the edge is re-drawn between the twin `cbunit:` nodes and UnitOpsPanels.tsx:127 / AreaKnowledgePanel.tsx:110 (`f.from_ref === unit.code`) do resolve it; it is orphaned only when units.code has no codebook counterpart. The visible disappearance from the drawn location happens in every case."
  },
  {
    "id": "GPV-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/graph/OrgGraph2D.tsx:202 `if (st.showArrows && (alpha > 0.3) && (e.type === \"supersession\" || e.type === \"related\" || onPath)) {` — \"flow\" is absent. A repo-wide grep for `showArrows|curvedLinks` returns hits only in GraphControls.tsx, OrgGraph2D.tsx and graphSettings.ts — OrgGraph3D.tsx never reads either, and its link pass (601-633) writes only `pos.setXYZ(si*2, …)` / `pos.setXYZ(si*2+1, …)` straight LineSegments. GraphControls.tsx:215 hint reads `\"Direction on supersession and curated links.\"` EDGE_LABELS (graphTheme.ts:33-44) is imported by app/(protected)/graph/page.tsx only at line 263, inside the path-hop list — there is no legend surface anywhere.",
    "note": "Every limb of the claim checks out: flow (the one edge type whose semantics are directional, per graphTheme.ts:42 \"Feeds (process flow)\") gets no arrowhead in either renderer, 3D ignores showArrows and curvedLinks entirely, and none of the ten EDGE_RGB colours is ever explained to the user."
  },
  {
    "id": "GPV-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/graphSim.ts:291-308 `neighborhood(...): Map<string, number>` with `seen.set(nb, d)` — the doc comment at 289-290 even says \"it also reports HOW FAR each node is so the renderer can fade by distance.\" The sole caller discards it: app/(protected)/graph/page.tsx:170-171 `const near = neighborhood(focusId, buildAdjacency(edges), settings.localDepth); nodes = nodes.filter((n) => near.has(n.id));`. Neither renderer receives a depth map — OrgGraph2D.tsx:237-240 and OrgGraph3D.tsx:575-578 compute `dim` only from spotId/query/highlight. GraphControls.tsx:224 `<Section icon={Waves} title=\"Forces\">` contains, at 235-237, `<Slider label=\"Neighbourhood depth\" … hint=\"Hops shown when you focus a single node.\" />`.",
    "note": "Both halves are exactly as claimed — the hop distance is computed and thrown away, and the only control for focus depth is filed under Forces (graphSettings.ts:50-51 itself groups `localDepth` under a separate `// ── Local graph ──` heading, so even the model disagrees with the UI placement)."
  },
  {
    "id": "HAND-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/reviewControl.ts:45 `const { data } = await supabase.from(\"collections\").select(\"review_control\")...` and :48 `const { data: lib } = await supabase.from(\"libraries\").select(\"review_control\")...` — `error` is never destructured, so a failed query yields data null and line 49 returns NONE = `{ mode: \"none\" }`. components/documents/RevUpModal.tsx:197 `catch { if (alive) setReviewControl(null); }` then :210 `effectiveModeForRevUp({ control: reviewControl ?? { mode: \"none\" }, changeType })`. lib/documentLifecycle/setRevUp.ts:84 `} catch { /* unresolved policy → direct publish, as before */ }` leaving `willReview = false` → `revUpDocument(common)`.",
    "note": "Confirmed fail-open, and no DB backstop catches it: supabase/migrations/20260822_review_completion_guard.sql:53 only raises when `COALESCE(v_primary_reqs, 0) > 0`, i.e. only when a roster already exists — and a gate that failed open never creates one. The contrast in the very same effect hook settles intent: the doc-class/MOC resolution at RevUpModal.tsx:199-202 deliberately fails CLOSED (`setDocClassUnknown(true)`), while the review gate silently degrades to 'none'."
  },
  {
    "id": "HAND-10",
    "verdict": "REFUTED",
    "corrected_severity": null,
    "evidence": "RevUpModal.tsx:279 `if (willReview && !asBranch) {` — but the ONLY setter of the conflict screen is the catch at :322-327 `if (e instanceof StaleBaseError) { setConflict(e.info); setShowBranchInput(false); ...}`, and StaleBaseError is thrown only inside revUpDocument (revisions.ts:468 pre-flight, :577 RPC `stale_base`). revUpDocument is reached only via the `else` arm at :292, i.e. only when willReview is already false. submitForReview (revisions.ts:847-940) never throws StaleBaseError.",
    "note": "The scenario is unreachable: in a library with mode 'require' and a non-Minor change type, willReview is true, so doPublish(false) goes to submitForReview and the conflict screen — the only place the 'publish as an unreconciled branch' button lives (:449-490) — never renders. A branch can therefore only be published on a rev-up where the gate was already legitimately waived (mode none, Minor/Correction per reviewControl.ts:59, or publisher_choice with the box unchecked); `!asBranch` at :279 is defensive, not a gate-skip. The claim of 'no warning' is also wrong: the branch panel carries an explicit consequence block at :463-467 plus a mandatory reason (:253) and a post-publish 'Published as an UNRECONCILED BRANCH' alert (:304-309)."
  },
  {
    "id": "HAND-11",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "documentLifecycle/common.ts:178 `status: \"Issued\",` then :225-228 `update({ current_version_id: versionId ...})` with no review_control/doc_class on the insert and no onDocumentIssued/onDocumentIssuedAck call (the function ends at :248). Counter-evidence: outputTemplates.ts:233 passes `status: \"Draft\"`, not Issued; revisions.ts:386-388 `await onDocumentIssued(...); await onDocumentIssuedAck(...)` — the file-upload path DOES seed the review clock and open the ack roster; split.ts:104 `collectionId: inheritCollectionAndSet ? (source.collectionId ?? null) : null`.",
    "note": "Core point holds for split/merge only: createNewDocWithFirstVersion lands a doc at Issued with its first version current, opens no ack/review-cycle roster, and copies neither the source's document-level review_control nor doc_class override (20261012 migration columns). But three cited specifics are wrong — the template-filing path files at Draft, the upload path seeds review-cycle + ack rosters, and class/policy inheritance is resolved dynamically doc→folder→library (reviewControl.ts:40-49), so split targets that keep collection_id do inherit the folder/library policy. Creation is also documented as deliberately outside the publish guard (revisions.ts:312-315). Severity LOW."
  },
  {
    "id": "HAND-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "RevUpModal.tsx:97 `const [changeType, setChangeType] = useState<DocumentVersion[\"changeType\"]>(\"Minor\");`; :145-146 `if (remembered?.changeType) setChangeType(remembered.changeType);` (key `mfg.revup.${libraryId}` at :136), written back on every successful publish at :313 `localStorage.setItem(memoryKey, JSON.stringify({ issueType, changeType }))`; :218-219 `const isMinorLike = changeType === \"Minor\" || changeType === \"Correction\"; const mocRequired = (docClass === \"drawing\" || docClassUnknown) && !isMinorLike;`; reviewControl.ts:59 `if (input.changeType === \"Minor\" || input.changeType === \"Correction\") return \"none\";`.",
    "note": "Confirmed: a governance-relevant field defaults to the value that waives both gates and is remembered per library. The only mitigation is the amber banner at :643-652, which renders solely when `reviewControl.mode !== \"none\"` — in a library with no review policy the Minor default silently waives the PSM MOC gate with no notice at all, and nothing anywhere warns that Minor waived MOC."
  },
  {
    "id": "HAND-13",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "documentGuards.ts:14-16 claims 'A defense-in-depth Postgres trigger ... enforces the same rule at the DB layer for any path that bypasses the lib.' 20260812_per_library_publish_authority.sql:133-136: 'An authorized publisher MAY advance past a foreign checkout ... We deliberately do NOT block on checked_out_by here.' The current definition (20260822_review_completion_guard.sql:21-90) contains only the review-completion, authority and hold checks — no checked_out_by test; `grep -rn checked_out_by supabase/migrations` shows it surviving only inside publish_revision (20260828:94-99), never in the trigger.",
    "note": "Factually correct — the DB no longer backstops the checkout lock and the module comment is stale. But the removal was an explicit design decision (20260812:133-136), and the lock is still enforced for every lib path including the legacy fallback at revisions.ts:614: revUpDocument calls authorizePublish first (revisions.ts:417), which at :242 refuses a foreign checkout without an override reason and runs evaluatePublishGuard. Residual exposure is a raw PostgREST write by someone who already holds publish authority, so this is documentation drift plus a missing defense-in-depth layer — LOW."
  },
  {
    "id": "HAND-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260901_db_hard_enforcement.sql:53 `WHEN 'holds.release' THEN '[\"*\"]'::jsonb` (the shipped default when no stored policy exists) and :98-101 `CREATE POLICY document_holds_update ON document_holds FOR UPDATE USING (org_capability_allows(org_id, 'holds.release', auth.uid()))`; org_capability_allows returns TRUE on the '*' token for any active member (line ~60 `IF t = '*' THEN RETURN TRUE; END IF;`). lib/capabilityPolicy.ts:87-88 `{ id: \"holds.release\", ... defaultRoles: [\"*\"] }`. lib/documentGuards.ts:117 `const canForceHold = forcing && isController;`",
    "note": "Confirmed and internally inconsistent: documentGuards.ts:114-117 states holds are 'deliberate do-not-advance flags reserved for the controller tier' and refuses a library publisher's force, yet the same publisher can simply UPDATE released_at under the shipped '*' default. Note also that checkout.force_release is defaulted to [\"Admin\",\"DocCtrl\"] and flagged `critical: true`, while holds.release is neither — so the weaker control guards the stronger promise. lib/holds.ts:105-109 additionally fails open on a policy-lookup error."
  },
  {
    "id": "HAND-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Repo-wide grep for the only version-creating entry points shows `revUpDocument` / `submitForReview` called solely from components/documents/RevUpModal.tsx:284,293, lib/documentLifecycle/merge.ts:170 and lib/documentLifecycle/setRevUp.ts:87,90 — never from the ticket path. lib/ticketTransitions.ts:247-250 only writes `updates.deliverable_rev = issuedRevLabel(ticket.revisionCount)` onto the tickets row. app/api/tickets/workflow-action/route.ts:240-262 touches documents only to READ current_version_id for a document_intents advisory row.",
    "note": "Confirmed: no ticket transition writes documents.current_version_id or inserts a document_versions row. Corroborating dead code — `related_ticket_id` exists in supabase/schema.sql:334 and is READ at lib/revisions.ts:958 and documents/[libraryId]/page.tsx:1893, but a repo-wide grep finds no write to it anywhere. The only cross-link is advisory (lib/impact.ts:117 queries `metadata->source_document->>id`), and requests/[id]/page.tsx:577 stamps the ticket deliverable 'UNCONTROLLED COPY'."
  },
  {
    "id": "HAND-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260830_publisher_row_management.sql:34,44 — the doc_review_signoff_update USING clause admits `OR user_can_publish_on_library(d.library_id, auth.uid()::text, d.org_id)`, and its WITH CHECK (lines 47-50) requires only `EXISTS (SELECT 1 FROM org_members ... status = 'active')` — no column restriction, no reviewer-identity check. supabase/migrations/20260822_review_completion_guard.sql:48-53 counts exactly those rows: `count(*) FILTER (WHERE status = 'signed') INTO v_signed ... IF COALESCE(v_primary_reqs,0) > 0 AND COALESCE(v_signed,0) < v_primary_reqs THEN RAISE EXCEPTION`.",
    "note": "Confirmed: the guard's only input is a status column the constrained publisher may write. A grep of supabase/migrations for triggers on document_review_signoffs finds none — nothing validates that a 'signed' row carries a matching e_signatures row or that signature_id/signed_at were set by the reviewer, so a direct PATCH satisfies the completion gate."
  },
  {
    "id": "HAND-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260822_review_completion_guard.sql:24,32-34 — `v_actor uuid := auth.uid(); -- NULL for service-role / SQL console` then `IF v_actor IS NULL THEN RETURN NEW; END IF;`, i.e. the whole guard (review completion, publish authority, AND the active-hold check at 78-86) is skipped. app/api/intake/upload/route.ts:302 `const autoNow = !!docId && !!link.allow_auto_supersede && linkAuthored;` and :322-325 `await supabaseAdmin.from(\"documents\").update({ current_version_id: versionId, rev: ..., status: \"Issued\", pending_version_id: null, ... }).eq(\"id\", documentId);`",
    "note": "Confirmed on both halves. The service-role write bypasses the hold check entirely, and a grep for runPostPublishSideEffects shows callers only in lib/revisions.ts:711,1304 and lib/reviewControl.ts:499 — never the intake route, so the ack-roster recompute, retention recompute, and supersede/watcher notifications never run for an auto-superseded intake revision. The route substitutes only its own controllers/owner notification and audit row."
  },
  {
    "id": "HAND-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/reviewControl.ts:44-48 queries `collections ... .eq(\"id\", doc.collectionId)` once and then libraries — no parent walk; lib/docClass.ts:84-88 does the same single `.eq(\"id\", doc.collectionId)` lookup; components/documents/ReviewGateSection.tsx:51-54 repeats it inline; lib/reviewControl.ts:545-549 (the daily scan) resolves from `colMap.get(doc.collection_id)` only. Collections do nest: supabase/schema.sql:94 `parent_id UUID REFERENCES collections(id) ON DELETE CASCADE` with an index at :110.",
    "note": "Confirmed — every policy resolver consults exactly three levels (document, immediate collection, library) while the folder tree is arbitrarily deep, so a policy set on 'P&IDs' does not reach 'P&IDs / Unit 400 / Current'. An ancestor-chain helper already exists (lib/serverCollections.ts:52 walks `cur.parent_id` up to 200 hops) but no policy resolver calls it."
  },
  {
    "id": "HAND-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/requests/new/page.tsx:291-297 `metadata.source_document = { id: sourceDocId, document_number: sourceDocNum, title: sourceDocTitle, rev: sourceDocRev, path: sourceFileUrl }` — `rev` is a text label, no version id. components/documents/CheckInPanel.tsx:262 `source_document: { id: doc.id, document_number: ..., title: ..., rev: doc.rev ?? null, path: null }` — same shape. app/api/tickets/workflow-action/route.ts:254-255 sets the intent's `base_version_id` from `docRow.current_version_id` read at DRAFTING time, not from the ticket's source rev.",
    "note": "Confirmed: no document_versions.id is captured on the ticket, so the stale-base machinery in lib/revisions.ts:437-450 (`expectedBaseVersionId` / provenance session|declared|unverified) has nothing to compare against for a ticket-sourced change. The one version-anchored artifact, document_intents.base_version_id, is anchored to whatever was current when the ticket entered DRAFTING and expires on a TTL, so it also cannot flag the redlined Rev 3 having been superseded by Rev 4."
  },
  {
    "id": "HAND-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/viewers/FullScreenViewer.tsx:912-916 `try { const baked = await bakeMarkupIntoPdf(bytes, states); ... draftKey = await stashDraft([...]) } catch (e) { console.error(\"bake markup for drafting failed; falling back to clean original\", e); }` — draftKey stays \"\", and the description line at :922 `draftKey ? \"(marked-up sheet attached as a Source file)\" : \"\"` simply disappears, with no user-visible warning. The only mount, app/(protected)/documents/[libraryId]/page.tsx:3025-3038, passes none of `initialPageStates` / `onPageStatesChange` / `onCommit`, and a repo-wide grep finds no other caller supplying them.",
    "note": "Confirmed on both halves. Markup state lives only in the component's `pageStates` React state (FullScreenViewer.tsx:369) — there is no markup table in supabase/migrations, and markup_requests stores only a pasted `shared_markup_url`, which both resolve call sites (inbox/page.tsx:139, dashboard/widgets.tsx:795) omit entirely, so lib/markupRequests.ts:123 writes `shared_markup_url: null` even for status 'shared'."
  },
  {
    "id": "HAND-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/projects/IntakePanel.tsx:237-240 `await finalizeReviewedRevision({ orgId, documentId: p.docId, actorId: uid, actorName: userEmail ?? \"Reviewer\", requireRosterComplete: false })`. lib/reviewControl.ts:408-411 `if (input.requireRosterComplete !== false) { const { complete } = await reviewCompletionForDraft(...); if (!complete) return { published: false, reason: \"incomplete\" }; }` — the app-side gate is skipped outright. app/api/intake/upload/route.ts:330-332 sets only `pending_version_id`; a grep of that route for 'signoff'/'roster' returns nothing. supabase/migrations/20260822_review_completion_guard.sql:53 `IF COALESCE(v_primary_reqs, 0) > 0 AND ...` — with zero roster rows the DB gate never fires.",
    "note": "Confirmed: an externally submitted revision never gets a document_review_signoffs roster, the app gate is explicitly disabled, and the DB completion check is roster-conditional, so a library configured mode:'require' with two named reviewers is fully bypassed by the intake approve button. The remaining DB checks (publish authority, active hold) still apply to this path because it runs on the user's session client, not the service role — but they say nothing about who reviewed."
  },
  {
    "id": "HLD-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260822_review_completion_guard.sql:36-40 `v_advancing := (NEW.current_version_id IS DISTINCT FROM OLD.current_version_id) OR (NEW.status = 'Superseded' AND ...); IF NOT v_advancing THEN RETURN NEW; END IF;` — every non-advance UPDATE exits before the hold test at :77-86. lib/documentGuards.ts:138-148 is the only app-side hold block and lives inside evaluatePublishGuard. lib/retention.ts:151-158 disposeDocument checks only `if (await isLegalHold(input.documentId))`, never document_holds. lib/revisions.ts:1051-1118 correctRevisionLabel checks authority, sibling labels and pending review — no hold.",
    "note": "The absence claim checks out repo-wide: a case-insensitive grep for 'hold' across lib/transmittals.ts, lib/distributionAcks.ts, lib/documentShares.ts and lib/documentLifecycle/renumber.ts returns ZERO hits, and lib/downloads.ts's only hits are about the checkout holder, not document_holds. Worse than stated: even on an advancing update the DB trigger returns NEW for Admin/DocCtrl (lines 60-63) before reaching the hold check, so controllers are unblocked there too."
  },
  {
    "id": "HLD-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/holds.ts:186 `released_reason: input.releasedReason?.trim() || null` with no validation, backed by the nullable column at 20260612_phase5_holds.sql:57 `released_reason TEXT`. The confirm buttons are enabled regardless of the box: components/documents/HoldStrip.tsx:320-330 placeholder \"Resolution (optional)\" and `<button onClick={onConfirmRelease} disabled={busy}>`; app/(protected)/admin/holds/page.tsx:204-208 identical. lib/holds.ts:208 `void notifyHoldChange({...})` — fire and forget; lib/holds.ts:254 `} catch { /* best-effort */ }`; lib/holds.ts:252 `audience: { followers: true, roles: [\"Admin\", \"DocCtrl\"] }`.",
    "note": "All four sub-claims hold. The opener is only reached if they happen to be Admin/DocCtrl or have manually subscribed — lib/notify/recipients.ts:23-31 resolves followers purely from the `subscriptions` table, and the only writer is the manual WatchButton (lib/subscriptions.ts:41), so nothing auto-follows the person who placed the hold."
  },
  {
    "id": "HLD-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/timeline.ts:350-352 `const auditEvents = ((auditResult.data as AuditRow[]) ?? []).filter((r) => r.action !== \"HOLD_OPENED\" && r.action !== \"HOLD_RELEASED\").map(auditRowToEvent);` and the identical filter at :466-468, with events instead synthesized from the live rows by `holdRowsToEvents` (lib/timeline.ts:124-169) fed by the `document_holds` select at :346-349. Deletion is permitted: 20260901_db_hard_enforcement.sql:103-105 `CREATE POLICY document_holds_delete ON document_holds FOR DELETE USING (is_org_controller(org_id));` and the UPDATE policy at :98-102 allows rewriting `reason`/`notes`.",
    "note": "Confirmed: the timeline's only hold source is the mutable row, and the immutable audit twin is filtered out unconditionally. Partial mitigation the finding does not mention — the discarded HOLD_OPENED/HOLD_RELEASED audit rows still render in /admin/audit (page.tsx:69) and /activity (page.tsx:51), so the fact is recoverable org-wide, just not on the document's own timeline. MEDIUM still fits."
  },
  {
    "id": "HLD-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260822_review_completion_guard.sql:21-22 `CREATE OR REPLACE FUNCTION enforce_document_publish_guard() RETURNS trigger LANGUAGE plpgsql SECURITY DEFINER AS $$` — `grep -c search_path` on that file returns 0, likewise for 20260826_legal_hold_delete_guard.sql (both guards, :17-18 and :37-38) and 20260828_integrity_hardening.sql:53 `LANGUAGE plpgsql SECURITY DEFINER AS $$` (publish_revision). The same repo pins others, e.g. 20260901:110 `SECURITY DEFINER SET search_path = public`. supabase/schema.sql:564-588 creates document_holds with only indexes, and the RLS block at :1011-1028 lists 18 tables — document_holds is not among them, and no `document_holds` policy exists anywhere in schema.sql.",
    "note": "Every cited fact checks out. Two caveats on impact, not accuracy: the search_path vector needs a role able to CREATE a schema on the search_path, which `authenticated` normally lacks in Supabase; and any database that ran 20260612_phase5_holds.sql:79-85 has RLS + policy on document_holds, so the schema.sql gap bites only a bootstrap-from-schema.sql deployment. MEDIUM is still fair given the repo's own DIAGNOSE_sync_check.sql exists precisely because migration drift happens here."
  },
  {
    "id": "HLD-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/physicalBridge.ts:84 `const url = `${origin()}/assets/${encodeURIComponent(asset.tag)}`;` and :99 `page.drawText(\"SCAN: drawings · holds · report a problem\", ...)`. The target lives at app/(protected)/assets/[tag]/page.tsx, i.e. inside the (protected) route group whose layout gates on the RoleContext session (app/(protected)/layout.tsx:28 `const { loading, uid, userEmail, membershipState } = useRole();`), and the page's own loader no-ops without a session: `if (!activeOrgId || !tag) return;` (:45). The contrast the finding relies on is in the same file: :219-221 \"PUBLIC verify page — the person holding the folder in the field has no account; sending them to the protected app was a login wall\" and :272-275 \"the old /packages target was a login wall under the words 'SCAN BEFORE STARTING WORK'\" — both were moved to app/verify-ticket and app/verify-package; no public counterpart exists for /assets/[tag].",
    "note": "Confirmed: the two other printed artifacts were explicitly repointed to unauthenticated /verify-* pages and the equipment label was not. One detail is imprecise — there is no middleware and no explicit redirect, so an anonymous scanner gets the app shell with an empty asset page rather than a literal sign-in screen; the outcome (no drawings, no hold badge for field staff) is the same."
  },
  {
    "id": "HLD-14",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "Repo-wide grep for `expectedReleaseAt` shows the only hold-side writer is the optional parameter itself — lib/holds.ts:75 `expectedReleaseAt?: string;` / :122 `expected_release_at: input.expectedReleaseAt ?? null`. Both callers of openHold omit it: components/documents/HoldStrip.tsx:86-93 and components/documents/CheckInPanel.tsx:366-371. The only other writer, lib/documentLifecycle/common.ts:358 `expected_release_at: h.expected_release_at`, merely copies whatever the origin row held (always null). Consequently `isLate` at components/documents/HoldStrip.tsx:269 (`expectedMs !== null && nowMs > expectedMs`) and `lateDays` at app/(protected)/admin/holds/page.tsx:173-174 are permanently false. No cron touches holds: app/api/cron/{maintenance,embed-drain} contain no document_holds reference.",
    "note": "Factually correct — the '+Nd late' badge is unreachable dead UI and no escalation exists. Severity is too high: nothing is corrupted or wrongly permitted, and the same need is served by the surface the finding itself points at — /admin/holds sorts oldest-first (lib/holds.ts:289 'oldest first — biggest blockers up top') and shows a 'Longest open' KPI. That is a missing proactive nudge plus a dead optional field, i.e. LOW."
  },
  {
    "id": "HLD-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/documentLifecycle/split.ts:137-148 calls `markSupersededAndLink({...})` directly; split.ts's full import list (lines 9-21) contains no documentGuards / assertCanPublishRevision, and grep for 'authorizePublish' in split.ts and merge.ts returns nothing — unlike lib/revisions.ts:1425-1428 `const preState = await authorizePublish({...})`. common.ts:319-377 copyActiveHoldsToDoc ends each iteration with `if (!error && insertedHold) { copied++; ... }` — a rejected insert increments nothing and raises nothing; split.ts:155-170 and merge.ts:215-224 call it in step 3, after every `register({...})` compensation entry, with no register() of their own.",
    "note": "Verified: markSupersededAndLink (common.ts:253-280) only checks `updErr` on the status flip, never document_holds. One caveat worth recording — the DB trigger DOES catch this path for non-controllers (v_advancing is true when status becomes 'Superseded'), so the unguarded supersession is specific to Admin/DocCtrl actors, which is precisely the role the finding's scenario names and the normal actor for a split."
  },
  {
    "id": "HLD-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/verify/route.ts:34-38 selects only `id, document_number, title, name, rev, status, current_version_id, superseded_at`; the whole file contains no reference to document_holds, and :90 `const isCurrent = !docRetired && (!versionId || versionId === d.current_version_id);`. app/verify/[docId]/page.tsx:65 `result?.isCurrent ? \"bg-emerald-600\" : \"bg-red-600\"` and :99 `{... result.isCurrent ? \"CURRENT\" : \"DO NOT USE\"}`. app/api/verify-package/route.ts:47-74 likewise selects `id, document_number, title, name, rev, current_version_id, status` with no hold query and computes `allFresh: sheets.length > 0 && staleCount === 0`.",
    "note": "Both public field-verification surfaces are hold-blind, confirmed by full-file inspection rather than inference. The QR that lands on the hold-blind page is stamped by lib/docPack.ts:100-105 (`verifyUrl: ${publicOrigin()}/verify/${d.id}?v=${versionId}`) and lib/physicalBridge.ts:275-281, so the print that a held drawing produces actively points the field at the green answer."
  },
  {
    "id": "HLD-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/docPack.ts:50-53 `.from(\"documents\").select(\"id, org_id, document_number, title, name, rev, library_id, current_version_id, checked_out_by, checked_out_by_name, checkout_note\").in(\"id\", input.documentIds)` — checkout state is fetched, hold state is not, and there is no document_holds query anywhere in the file. docPack.ts:90-105 stamps `watermarkText: \"UNCONTROLLED — FIELD PACK\"` with a holderWarning built only from `d.checked_out_by`. app/(protected)/assets/[tag]/page.tsx:140 `result.skipped.length === 0 ? \\`Pack ready — ${result.included} drawing${...}, all current, all stamped.\\` : ...`",
    "note": "Every element of the claim verified verbatim, including the banner text. The page does render a hold badge per row (page.tsx:215 `{(holdsByDoc.get(d.id) ?? 0) > 0 && <span ...> hold</span>}`) — but that is display-only: it feeds neither the Doc Pack button's disabled state nor buildAndDownloadDocPack's input, which is `docs.map((d) => d.id)` unfiltered (page.tsx:133)."
  },
  {
    "id": "HLD-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260901_db_hard_enforcement.sql:98-102 `CREATE POLICY document_holds_update ON document_holds FOR UPDATE USING (org_capability_allows(org_id, 'holds.release', auth.uid())) WITH CHECK (org_capability_allows(org_id, 'holds.release', auth.uid()));` — row-level only, no column list. :52-53 gives the defaults `WHEN 'holds.open' THEN '[\"*\"]'::jsonb WHEN 'holds.release' THEN '[\"*\"]'::jsonb`. 20260612_phase5_holds.sql:35-38 states outright: 'Audit row is written by the application (lib/holds.ts) using the existing audit_logs flow, not by a trigger.' lib/holds.ts:173-218 releaseHold does the UPDATE then calls logHoldEvent (lib/audit.ts:129-150) client-side.",
    "note": "The absence of a trigger is confirmed by repo-wide search: the only `ON document_holds` statements in supabase/ are policies and indexes — no CREATE TRIGGER exists on that table. Because the default capability is '*', any active member (Viewer included) can PATCH released_at, released_by_name, opened_at, or set released_at back to NULL, and lib/holds.ts's own `assertHoldCapability` (lines 86-108) is app-side only and explicitly fails open on a policy-lookup error."
  },
  {
    "id": "HLD-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/retention.ts:124-131 `for (let i = 0; i < ids.length; i += 50) { await supabase.from(\"documents\").update(patch).in(\"id\", ids.slice(i, i + 50)); } await logEvent(...); await notifyHold(...); return ids.length;` — the result is never destructured and the count comes from scopeDocumentIds, not from the write. releaseLegalHold (:134-144) is byte-for-byte the same shape. disposeDocument (:151-158) `await supabase.from(\"documents\").update({ disposition_state: \"disposed\", ... }).eq(\"id\", input.documentId); await logEvent(...); return { ok: true };`",
    "note": "Confirmed — supabase-js's PostgREST builder resolves with `{ data, error }` and never rejects, so a failed or RLS-denied UPDATE produces the identical return value and the identical retention event and notification as a successful one. No compensating control exists: 20260826_legal_hold_delete_guard.sql:17-33 only fires `IF OLD.legal_hold`, so if the placing UPDATE silently failed the column is still false and the delete guard stays inert on exactly the records it was meant to protect."
  },
  {
    "id": "HLD-7",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "app/api/verify-hold/route.ts:48-61 returns `reason: (h.reason as string) ?? null` alongside `docRev` from the live documents row (:37-46 `.select(\"document_number, title, name, rev\")`), despite the inline contract at :50-53 ('a photographed hold card must not disclose staff names or free-text operator notes'). reason IS free text — 20260612_phase5_holds.sql:44-58 declares `reason TEXT NOT NULL` with no CHECK, and HoldStrip.tsx:191-206 renders an `Other…` input with placeholder 'Custom hold reason' feeding `onOpen(otherDraft)`. document_holds has no version column, so docRev cannot be the rev the hold stopped.",
    "note": "The facts hold, but the disclosure severity is overstated: lib/physicalBridge.ts:155 prints `Reason: ${input.reason}` in 13pt bold RED on the very card that carries the QR (and the notes on the next line), so anyone positioned to photograph or scan the tag can already read the sentence — the endpoint adds essentially no exposure beyond the physical artifact. The route's top-of-file contract at line 6 also states plainly 'Exposure is minimal: hold status + reason + document label', so it is only the later inline comment that reads as contradicted. The live-rev half is a genuine correctness bug but a mild one. LOW."
  },
  {
    "id": "HLD-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/admin/holds/page.tsx:32-42 `const ADMIN_ROLES = new Set([\"Admin\", \"Manager\", \"Supervisor\", \"DocCtrl\"]); ... const canRelease = !!activeRole && ADMIN_ROLES.has(activeRole);` — the Release button at :217 renders only `{canRelease && ...}`. components/documents/InspectorPanel.tsx:132-133 `const canManageAssets = activeRole === 'Admin' || activeRole === 'Manager' || activeRole === 'Supervisor' || (activeRole?.includes('Engineer') ?? false) || activeRole === 'Drafter' || activeRole === 'DocCtrl';` feeds `canEdit={canManageAssets || isOwner}` at :425 and gates the whole place-hold section at :871 `activeHoldCount === 0 && (canManageAssets || isOwner)`. Neither file imports lib/capabilityPolicy. Row link: app/(protected)/admin/holds/page.tsx:181 `href={`/documents/${meta.libraryId}`}` — no `?doc=`, while lib/holds.ts:248 and app/(protected)/assets/[tag]/page.tsx:211 both build `/documents/${libraryId}?doc=${id}`.",
    "note": "Both halves confirmed. The hardcoded lists are also NARROWER than the shipped policy default (`holds.open`/`holds.release` default to `[\"*\"]` at lib/capabilityPolicy.ts:85-88), so the UI blocks people the policy already allows; a per-person grant added via addUserGrant can never light up either button, exactly as the error text at lib/holds.ts:102-103 invites the admin to try."
  },
  {
    "id": "HLD-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260612_phase5_holds.sql:44-58 declares `org_id UUID NOT NULL REFERENCES orgs(id)` and `document_id UUID NOT NULL REFERENCES documents(id)` with no composite FK, CHECK or trigger tying the two; a repo-wide grep of `document_holds` across supabase/ finds no trigger and no constraint on the pair. SELECT keys on the hold's org: 20260901_db_hard_enforcement.sql:89-91 `EXISTS (SELECT 1 FROM org_members WHERE org_id = document_holds.org_id ...)`. Enforcement keys on the document: 20260822_review_completion_guard.sql:77-81 `SELECT EXISTS (SELECT 1 FROM document_holds h WHERE h.document_id = NEW.id AND h.released_at IS NULL)` (same shape in 20260713:70, 20260812:138, 20260816:71, 20260823:186, 20260828:107). lib/holds.ts:271-280 filters on `document_id` only. lib/documentLifecycle/common.ts:353-356 inserts `org_id: actor.orgId, document_id: targetDocId`.",
    "note": "Confirmed, and reachable: the INSERT policy (20260901:93-95) only checks `org_capability_allows(org_id, 'holds.open', auth.uid())` against the row's OWN org_id, so a member of org A can insert a hold carrying org A's id against any document UUID. Because the publish guard is SECURITY DEFINER and org-blind, org B's publish is blocked by a row org B's SELECT policy hides."
  },
  {
    "id": "HUB-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/knowledge/LibraryAiModal.tsx:86-90 `await saveLibraryAiFeatures(library.id, { clarifyFacets, visionPages, visionAllPages, decoder: decoder.trim() || undefined, legendDocIds: legendDocIds.size > 0 ? [...legendDocIds].slice(0, 3) : undefined, });` — `drawingIntel` (state at :37, checkbox at :230) is not in the object. lib/knowledge.ts:280-281 `.update({ ai_features: features })` is a whole-column replace, so the flag is erased, not merely omitted. The single consumer requires it strictly: app/(protected)/knowledge/[id]/page.tsx:1956 `{activeOrgId && library.aiFeatures?.drawingIntel === true && (<DrawingIntelPanel …/>)}`. Line 91 still fires the toast `\"Library AI setup saved — applies to the next question.\"`",
    "note": "Repo-wide grep for `drawingIntel` returns five hits and none of them writes the flag — the modal's own state, its checkbox, the type declaration in knowledge.ts:44, and the strict `=== true` gate. There is no API route, migration default, or seed that sets it, so DrawingIntelPanel is unreachable in every workspace and the ticked box is silently discarded behind a success toast."
  },
  {
    "id": "HUB-10",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "app/(protected)/intelligence/page.tsx:70 `const snapKey = \\`intel-status-${activeOrgId}\\`;` and :79 `setStatus((prev) => prev ?? { ...snap, keysKnown: true, countsKnown: true, coverageKnown: true });` — while the snapshotted fields come from a per-user query: app/api/ai/connection/route.ts:86 `.eq(\"org_id\", orgId).eq(\"user_id\", auth.userId)` → :100 `const personal = mask(…)`.",
    "note": "The key fact holds: the localStorage snapshot is keyed by org only, yet chatKey/embeddingKey are strictly per-user, so on a shared device the next signer-in gets the previous user's masked key state painted as known (no shimmer), and the `() => patch({ keysKnown: true })` failure arm at :103 leaves those stale values standing as authoritative. But RoleContext.tsx:271-278 does purge every `intel-status-*` / `schema-gaps-*` key on SIGNED_OUT, and in the normal path the stale paint is overwritten within one round-trip by the real getAiConnections result. Exposure is a 4-character key suffix plus a boolean, transient, and inside the same org — LOW."
  },
  {
    "id": "HUB-11",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "Repo-wide grep for ViewTabs returns every INTELLIGENCE_VIEWS target — app/(protected)/intelligence/page.tsx, /intelligence/skills/page.tsx, /intelligence/setup/page.tsx, /assistant/page.tsx, /knowledge/page.tsx, /graph/page.tsx, /admin/proposed-links/page.tsx — and does NOT return app/(protected)/knowledge/[id]/page.tsx. That page's only nav is the header eyebrow at :1602: `eyebrow={<button onClick={() => router.push(\"/knowledge\")} …><ArrowLeft className=\"w-3 h-3\" /> Knowledge</button>}`.",
    "note": "Factually correct — /knowledge/[id] is the one Intelligence surface with no tab strip. But it is not navigationally stranded: a labelled back-link to /knowledge sits in the header, so the cost is one extra click, not a dead end, and every other detail page in the app (documents/[libraryId], assets/[tag]) follows the same eyebrow-not-tabs convention. Consistency nit → LOW."
  },
  {
    "id": "HUB-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Three names for one `visionAllPages` checkbox: LibraryAiModal.tsx:215 `Text doesn&apos;t extract from these files — index every page as an image`; SemanticIndexPanel.tsx:165 `Turn on <b>\\u201cThese are CAD exports or scans\\u201d</b> in Library AI setup`; DrawingIntelPanel.tsx:434 `Turn on <b>Index every page with AI vision</b> in Library AI setup` (a fourth in app/api/knowledge/drawing/route.ts:212). And the routing dead end: app/(protected)/knowledge/[id]/page.tsx:1956 `{activeOrgId && library.aiFeatures?.drawingIntel === true && (<DrawingIntelPanel …/>)}`, plus DrawingIntelPanel.tsx:68 `if (census.totalDistinct === 0 && audit.totalRefs === 0 && suggestions.length === 0) return null;`.",
    "note": "Confirmed on both clauses. A grep for the exact strings shows 'These are CAD exports or scans' exists nowhere but SemanticIndexPanel's own advice text — the real label is 'Text doesn't extract from these files'. And both SemanticIndexPanel (:163 'run Rebuild index under Drawing intelligence') and EquipmentTablePanel (:47 'use the CSV export in Drawing intelligence') send the reader to a panel that is not mounted unless aiFeatures.drawingIntel === true, and which returns null when nothing was extracted — precisely the failure state that produces the advice."
  },
  {
    "id": "HUB-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/intelligence/skills/page.tsx:68-79 runs `seedBuiltinRules(activeOrgId, uid)` and `seedBuiltinAnswerSkills(activeOrgId, uid)` in a mount effect with no role check, and both seeders stamp the caller: lib/answerSkills.ts:64 / lib/linkRules.ts:76 `created_by: userId`. RLS then grants that person write authority regardless of role — supabase/migrations/20261016_reasoning_skills.sql:54-56 `CREATE POLICY answer_skills_update … USING (is_org_controller(org_id) OR created_by = auth.uid())` and 20261015_connection_skills.sql:63-65 the same for link_rules. The UI hands them the switch: skills/page.tsx:245-246 `const mine = r.created_by === uid; const mayManage = canManageOrg || mine;` feeding `toggleButton({ … mayManage, onToggle: () => setAnswerSkillEnabled(r.id, !r.enabled) })` (:263) and the delete button at :145 `{!opts.builtin && opts.mayManage && …}`. app/(protected)/layout.tsx:49-50 gates only on `membershipState`.",
    "note": "Confirmed end to end, including the exploitability the finding rests on: `mayManage = canManageOrg || mine` is duplicated at ConnectionSkillsPanel.tsx:111-112, so a Viewer who happened to be first through the door can flip the org's built-in reasoning and connection skills off (delete is blocked for built-ins in the UI by the `!opts.builtin` guard, but the RLS DELETE policy would still permit it directly)."
  },
  {
    "id": "HUB-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/intelligence/page.tsx:302-307 — the only \"Jump in\" targets are `/assistant`, `/knowledge`, `/graph`, `/intelligence/setup`; nothing points at `/setup`. A repo-wide grep for `href=\"/setup\"` / `push(\"/setup\")` across app/ and components/ returns zero hits — the route's only two entry points are components/navigation/Sidebar.tsx:243 `{ label: 'Facility setup', href: '/setup', … }`, which sits in the `admin` array built only `isAdmin ? [...] : []` and inside the section closed by default (Sidebar.tsx:143 `useState<Set<string>>(new Set(['admin']))`), and the command-palette atlas entry lib/featureAtlas.ts:111. app/(protected)/setup/page.tsx:109-188 is a genuine seven-stage ordered navigator (codebook → registry → documents → bridge → ai → connections → process), each stage with live checks and a CTA.",
    "note": "Confirmed: the one surface that answers \"what do I do first\" is unlinked from the AI front door, admin-only in the nav, and inside a drawer that starts collapsed."
  },
  {
    "id": "HUB-4",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "components/navigation/ViewTabs.tsx:104-105 `// One tool, six lenses: everything AI lives here.` above an `INTELLIGENCE_VIEWS` array of seven entries (Overview, Ask, Knowledge, Graph, Review, Skills, Setup). app/(protected)/intelligence/page.tsx:3-4 `// /intelligence — … Six tabs (Overview · Ask · Knowledge · Graph · Review · Setup)`. components/navigation/Sidebar.tsx:237 `{ label: 'Intelligence', hint: 'AI in one place — ask, knowledge, graph, link review, setup', … }`.",
    "note": "The facts are all correct, but two of the three cited locations are source comments that no user ever sees; the only user-visible defect is the Sidebar hint omitting the Skill Library. That is a copy nit, not a MEDIUM — and the Skill Library is separately discoverable via the tab row itself (ViewTabs.tsx:111) and the command palette (lib/featureAtlas.ts:99-102, aliased to \"skills\", \"skill studio\", \"reasoning skills\", …)."
  },
  {
    "id": "HUB-5",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "app/(protected)/intelligence/page.tsx:212-218 `fixText=\"Nothing indexed — the AI has nothing to read yet\" href=\"/knowledge\" cta=\"Upload documents\"` lands on app/(protected)/knowledge/page.tsx:110-118, whose empty state gives non-controllers `action={isController ? <Button onClick={() => setShowCreate(true)}>…</Button> : undefined}` — literally no action. Line 231-232 `href=\"/knowledge\" cta=\"Build index\"` is worse: the Build-index control lives one level deeper, in SemanticIndexPanel.tsx:208-212 (`<Button size=\"sm\" variant=\"secondary\" onClick={() => void build()}> … Build index`), rendered only from app/(protected)/knowledge/[id]/page.tsx:1962 and gated by `{isController && …}` at :192.",
    "note": "SURVIVES on substance for the two Knowledge-related CTAs, but the title's quantifier is false: the two key cards (page.tsx:190-204) point at `/intelligence/setup`, which renders `<KeyEditor …/>` and `<EmbeddingKeyEditor …/>` inline at setup/page.tsx:56-59 — the exact control that fixes them — and the Database CTA lands on the page hosting Database health. Two of five misroute, so LOW rather than MEDIUM."
  },
  {
    "id": "HUB-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "A repo-wide grep for `ai-instructions` returns six hits, and the only navigational ones are app/(protected)/intelligence/setup/page.tsx:68-71 `<SetupLink href=\"/admin/ai-instructions\" icon={GraduationCap} title=\"AI instructions (playbooks)\" …/>` wrapped in `{isController && …}`, app/(protected)/knowledge/[id]/page.tsx:1644 which renders only `{instructionCount > 0 && (<Link href=\"/admin/ai-instructions\" …>)}`, plus two deep-linked `?scope=` chips in EquipmentSweepModal.tsx:150 and admin/codebook/page.tsx:799. components/navigation/Sidebar.tsx:252-269 lists sixteen admin leaves and none is AI instructions; ViewTabs.tsx:106-114 has no such tab; grepping lib/featureAtlas.ts for `playbook|instruction` returns nothing.",
    "note": "Confirmed, including the chicken-and-egg the summary points at: the knowledge-library link only appears once instructions already exist, so an org with zero playbooks has exactly one discoverable path (the controller-only block on the Setup tab) and the command palette cannot find it at all."
  },
  {
    "id": "HUB-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/intelligence/page.tsx:137-143 — `void supabase.rpc(\"semantic_coverage\", …).then( (r) => { const row = (r.data?.[0] …) ?? null; if (row) patch({ …, coverageKnown: true }); }, () => undefined, );` and StatusCard at 320-330 `if (pending) { return … animate-pulse … }` with `pending={!s.coverageKnown}` at 227.",
    "note": "Confirmed. supabase-js resolves (never rejects) on an RPC HTTP 500, so a semantic_coverage failure lands in the FIRST handler with r.data null → row null → no patch → coverageKnown stays false → the Meaning-index card shimmers permanently for any non-admin (schemaGaps is only ever set under `if (isAdmin)` at :145). Both coverage failure modes (the `() => undefined` reject arm and the null-row arm) are silent; the counts path at :132-134 at least sets `error`, and both key arms set keysKnown — so 2 of 4 silent is accurate. Migration 20261014_coverage_timeout_headroom.sql:1-9 confirms this RPC really does time out in the field."
  },
  {
    "id": "HUB-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/intelligence/ConnectionSkillsPanel.tsx:74-79 `const remove = async (r: LinkRule) => { setBusyId(r.id); try { await deleteLinkRule(r.id); await refresh(); } …}` — invoked directly at :151 `onClick={() => void remove(r)}`. Versus app/(protected)/intelligence/skills/page.tsx:98-107 `removeConfirmed` → `await appConfirm({ title: \"Delete this skill?\", … tone: \"danger\" }); if (!ok) return;`.",
    "note": "Confirmed on both halves. The Review-tab panel deletes a custom skill on a single click with no confirm and no undo, while the Skills page gates the identical deleteLinkRule call behind appConfirm. The duplication is real too: both files independently import listLinkRules/seedBuiltinRules/setLinkRuleEnabled/setLinkRuleVisibility/deleteLinkRule and both call seedBuiltinRules(activeOrgId, uid) in their own effect (panel :54, page :73), and the surfaces already diverge (panel shows 4 patterns and no author; page shows 3 patterns, author line, and the Reasoning shelf)."
  },
  {
    "id": "HUB-9",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "app/(protected)/intelligence/page.tsx:217 `{s.schemaGaps !== null ? (<StatusCard … title=\"Database\" …/>) : (<StatusCard … title=\"Meaning index\" ok={s.chunksTotal > 0 ? s.chunksEmbedded > 0 : true} okText={s.chunksTotal > 0 ? `${coveragePct}% of passages embedded` : \"Builds after your first uploads\"} …/>)}`; :261 `{s.chunksTotal > 0 && s.chunksEmbedded < s.chunksTotal && !!s.embeddingKey && (…Meaning index {coveragePct}% built…)}`.",
    "note": "The mechanism is real — the ternary at :217 means an Admin whose schema-health call succeeds gets the Database card in the coverage card's slot, and ok= is true at 1/400 embedded — but 'never renders for an Admin' is literally false: schemaGaps starts null, so the Meaning-index card DOES render until /api/admin/schema-health answers, and renders permanently if that fetch is non-ok (:160 `if (!res.ok) return;`). More importantly the scenario's own admin holds the embedding key, so the row at :261-270 does render for them and states '16 of 400 passages embedded' — the coverage number is not actually withheld. Downgrade to LOW: a green check on a partially-built index whose exact percentage is printed beside it."
  },
  {
    "id": "ILIFE-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/storageOrphans.ts:42-88 — the `sources` array registers document_versions, knowledge_documents, asset_photos, tickets, markup_requests, plot_plans, libraries, collections, users, org_configurations, output_templates. cost_documents is absent. Meanwhile lib/costDocs.ts:105 `const key = \\`orgs/${input.orgId}/project-costs/${input.projectId}/${crypto.randomUUID()}-${safeName}\\`;` → :116 `file_url: key`, and app/api/intake/upload/route.ts:70 `const key = \\`orgs/${orgIdQ}/project-costs/${projectIdQ}/quote-${crypto.randomUUID()}-${safeName}\\`;` → :85 `file_url: key`.",
    "note": "Confirmed with no mitigating guard. PROTECTED_PREFIXES is only [\"data/\", \"exports/\"] (:26), so orgs/<org>/project-costs/… is a candidate; MIN_AGE_DAYS = 7 (:25); deleteOrphans re-scans and issues DeleteObjectsCommand (:157-165). The module's own comment at :39-40 states the contract this violates — 'Tables added later MUST be registered here' — and :77-79 records that exactly this bug already shipped once for output_templates."
  },
  {
    "id": "ILIFE-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/dataRestore.ts:347 `return CONFLICT_TARGETS[table] ?? \"id\";` vs. 20261017_process_flows.sql:36 `UNIQUE (org_id, from_kind, from_ref, to_kind, to_ref)`, 20260929_mention_engine.sql:59-60 `CREATE UNIQUE INDEX IF NOT EXISTS entity_mentions_unique_idx ON entity_mentions (asset_id, COALESCE(knowledge_document_id, document_id), page)`, 20260807_link_proposals.sql:69-70 `CREATE UNIQUE INDEX ... proposed_links_pair_idx ON proposed_links (document_id, target_document_id, proposer)`. apply-table/route.ts:77-83: upsert on \"id\", and on error the fallback is a plain `.insert(chunk)`.",
    "note": "True, and broader than stated — assets (org_id,tag_normalized), asset_types (org_id,name), codebook_entries (org_id,kind,code), document_assets, work_package_documents and others carry the same shape. `ON CONFLICT (id) DO NOTHING` does not absorb a secondary-unique collision, and the fallback plain insert cannot either, so the whole 500-row chunk 23505s and the client's `break` drops the rest of that table. Only mitigation found: the failure is surfaced in `failedTables`, not silently swallowed."
  },
  {
    "id": "ILIFE-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/dataExport.ts:300 `let q = sb.from(table).select(\"*\").range(from, from + pageSize - 1);` — no column list, no exclusion; knowledge_chunks is on the contract (lib/exportTables.ts:139) and 20260930_semantic_layer.sql:48-49 `ALTER TABLE knowledge_chunks ADD COLUMN IF NOT EXISTS embedding vector(1024)`. dataExport.ts:96-99 accumulates every row into `tables[tbl]`; app/api/data-export/structured/route.ts:69 `const body = JSON.stringify(envelope, null, 2);`; lib/clientBackup.ts:124 `zip.file(\"data.json\", JSON.stringify(envelope, null, 2));`.",
    "note": "Confirmed — repo-wide grep for `embedding` in dataExport.ts / exportTables.ts / exportRunner.ts / clientBackup.ts returns nothing, so no exclusion, projection or streaming exists anywhere on the export path. Every 1024-dim vector is serialized as text into one in-memory envelope, then re-stringified twice more."
  },
  {
    "id": "ILIFE-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/schemaExpectations.ts:104 `{ table: \"statements\", migration: \"20260819_orphan_tables_backfill.sql\" }` — grep over supabase/ finds no such table; the only match is the COMMENT at 20260819_orphan_tables_backfill.sql:3 (\"CREATE TABLE statements for 11 tables\"), i.e. the list was scraped from a comment. Diffing the 88 entries against every CREATE TABLE in supabase/ shows process_flows, link_rules and answer_skills absent — all three degrade silently (lib/processFlows.ts:44 `if (missing(error)) return null;`, lib/linkRules.ts:48 same). grep for `schemaExpectations|EXPECTED_TABLES` across the repo returns only lib/schemaExpectations.ts and app/api/admin/schema-health/route.ts — no test.",
    "note": "All three sub-claims verified. One nuance on the stated consequence: schema-health's `missingTable` (route.ts:24) matches only code 42P01 or /does not exist/i, and PostgREST returns PGRST205 (\"Could not find the table … in the schema cache\") for an absent table — a code this repo handles explicitly elsewhere (lib/branches.ts:65, lib/workPackages.ts:48). So depending on PostgREST version the phantom is either reported missing forever (as the finding says) or reported PRESENT — a false clean bill. Either way the list is unvalidated and the finding stands."
  },
  {
    "id": "ILIFE-13",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/knowledgeSourceSync.ts:303-309: `const { data } = await supabaseAdmin.from(\"knowledge_sources\").select(\"library_id\");` … `const libraryIds = [...new Set((data ?? []).map(...))].slice(0, maxLibraries);` with `maxLibraries = 25` (line 299) and app/api/cron/maintenance/route.ts:245 `await syncAllKnowledgeSources();` — no ORDER BY, no cursor, no rotation, and the cap is platform-wide, not per-org. REFUTING the absolute: app/api/knowledge/sources/route.ts:137-166 `if (body.action === \"dc-changed\")` runs the same full `syncKnowledgeLibrarySources` for up to 8 affected libraries, fired immediately by lib/knowledge.ts:808 `nudgeKnowledgeSources` from every upload/move/folder-move/CSV-import in the watched DC library; route.ts:180 `if (body.action === \"sync\")` is an on-demand full resync.",
    "note": "The mechanism is real — an unordered slice(0,25) with no rotation means libraries past the cut get no cron attention. But 'never sync, ever' is false: any file landing in or moving within the watched doc-control library pushes a full reconcile (ADD + rev-up refresh + REMOVE) for that library regardless of the cut, and a controller can force one. The residual gap is narrow (a rev-up in a >25th library with no other filing activity, since publish does not call nudgeKnowledgeSources), so LOW."
  },
  {
    "id": "ILIFE-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/dataRestore.ts RESTORE_TABLE_ORDER lists `\"process_flows\",` and `\"entity_mentions\", \"drawing_audit_logs\",` (≈:293/:296) while `\"knowledge_documents\", \"knowledge_chunks\", …` appear near the end (≈:327). The FKs are real: 20261017_process_flows.sql:27 `source_document_id UUID REFERENCES knowledge_documents(id) ON DELETE SET NULL,` and 20260929_mention_engine.sql:33 `knowledge_document_id UUID REFERENCES knowledge_documents(id) ON DELETE CASCADE,`.",
    "note": "Confirmed, and the code comment above process_flows is itself wrong: 'Flows may reference knowledge documents (source PFD), restored earlier' — knowledge_documents is restored ~30 entries LATER. remapRow (:200-218) only rewrites org_id and uid values; it never nulls a dangling FK and it preserves row ids, so restoring into a fresh org raises 23503 on both tables. Only an additive re-restore into an org that already holds the knowledge documents would succeed."
  },
  {
    "id": "ILIFE-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/clientBackup.ts:124 `zip.file(\"data.json\", JSON.stringify(envelope, null, 2));` plus `zip.file(\\`files/${f.path}\\`, buf)` and files-manifest.json/backup-report.json — no manifest.json, no tables/. The restore page requires exactly that: app/(protected)/admin/restore/page.tsx:113 `const manifestPath = entryNames.find((p) => /(^|\\/)manifest\\.json$/i.test(p)); if (!manifestPath) throw new Error(\"No manifest.json — this doesn't look like a manufacturing-os backup ZIP.\");` and :117 `const tablePaths = entryNames.filter((p) => /(^|\\/)tables\\/[^/]+\\.json$/i.test(p) …)`.",
    "note": "Confirmed. 'files-manifest.json' does not satisfy /(^|\\/)manifest\\.json$/ (the char before is '-'), so the Full ZIP is rejected outright. The manifest.json + tables/*.json layout is produced only by lib/exportRunner.ts:134/:159, whose zip is built for delivery destinations (buildAndDeliverExport, called only from /api/data-export/run and /run-scheduled) and is never offered as a browser download — the page's own button at :140 calls startGlobalBackup → clientBackup. Restore's UI copy ('Drop the Full ZIP (records + files)', :316/:339) therefore advertises a path that cannot work."
  },
  {
    "id": "ILIFE-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/admin/restore/page.tsx:202 `if (!res.ok) { tableFailed = true; break; }` → :207 `if (tableFailed) failedTables.push(table);` → the outer `for (const table of order)` simply continues → :210 `setApplyProgress({ phase: \"done\", … })`. The abort lives in app/api/admin/restore/apply/route.ts:115-124 `// STOP. Tables are FK-ordered parents-before-children … for (const remaining of order.slice(idx + 1)) { results.push({ … error: \\`skipped: aborted after ${name} failed\\` }); } break;`.",
    "note": "Confirmed, including the dead-route claim: a repo-wide grep for '/api/admin/restore/apply' finds only apply-table call sites (page.tsx:196) and the route's own file — nothing ever POSTs to apply/route.ts. Partial credit to the UI: failedTables IS surfaced at :450, but the advice there ('re-run the restore, it's additive and safe') does not fix an FK-ordering failure, and children of a failed parent are still inserted wherever the column is nullable."
  },
  {
    "id": "ILIFE-5",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "The no-FK fact is true: 20260917_knowledge_sources.sql:53-54 `ALTER TABLE knowledge_documents ADD COLUMN IF NOT EXISTS source_document_id UUID;` — a bare UUID, unlike source_id one line above which is `UUID REFERENCES knowledge_sources(id) ON DELETE CASCADE`. But the reconciliation the finding missed: lib/knowledgeSourceSync.ts:286-292 `// ── REMOVE mirrors whose controlled doc left the sources ──` `for (const [dcDocId, row] of existingByDcDoc) { if (wanted.has(dcDocId)) continue; … .from(\"knowledge_documents\").delete().eq(\"id\", row.id …)` — reached daily via app/api/cron/maintenance/route.ts:245 `const sync = await syncAllKnowledgeSources();` (vercel.json cron `0 3 * * *`), and on every manual source sync from /api/knowledge/sources.",
    "note": "The claim is right about the schema and wrong about permanence. A deleted controlled document drops out of `wanted`, so the next sweep deletes the mirror and knowledge_chunks/knowledge_page_entities/entity_mentions cascade on their real FKs to knowledge_documents(id). Ask is also already guarded for ordinary users — app/api/knowledge/ask/route.ts:176-183 excludes any mirror whose source_document_id is not in readableControlledDocIds, and a deleted doc can never be in it. Residual risk that keeps this alive at MEDIUM: up to ~24h of exposure; controllers bypass the ask filter entirely (lib/knowledgeAccess.ts:196 `if (principal.isController) return new Set(docIds);` returns ids of rows it never looked up); and syncAllKnowledgeSources caps at `maxLibraries = 25` taken unordered and deployment-wide, so past 25 source-linked libraries some are never swept."
  },
  {
    "id": "ILIFE-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/dataExport.ts:299 `let q = sb.from(table).select(\"*\").range(from, from + pageSize - 1);` — no .order() anywhere in dumpTable (:286-313). lib/storageOrphans.ts:94 `const { data, error } = await sb.from(table).select(select).range(from, from + 999);` — same, no .order(). And lib/dataExport.ts:248 `complete: failedTables.length === 0,`.",
    "note": "Confirmed. PostgREST emits no ORDER BY, so LIMIT/OFFSET paging has no stable ordering; a concurrent UPDATE that rewrites a tuple (the embed drain writing knowledge_chunks.embedding) moves it in heap order and can shift rows across the page boundary, dropping or duplicating them. Nothing detects it: the loop's only termination check is `rows.length < pageSize`, and `complete` is derived solely from failedTables, so a short-changed dump still reports complete: true. Same defect makes the orphan reference set incomplete — with fail-closed behaviour only for query ERRORS (:95-97), not for silently missed rows."
  },
  {
    "id": "ILIFE-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/data-export/structured/route.ts:55 `if (![\"Admin\", \"Manager\", \"DocCtrl\"].includes(role || \"\")) { return … \"Only Admin / Manager / DocCtrl can export org data\" }` — versus lib/dataExport.ts:18-20 `// The endpoint uses the Supabase service-role key to bypass RLS, so this // function MUST be called from a server context that has already // verified the caller is an org admin.` and app/api/admin/restore/apply-table/route.ts:21 / begin/route.ts:24 `const RESTORE_ROLES = [\"Admin\"];`.",
    "note": "Confirmed, and the route contradicts even itself: its own header comment at :4 says 'Caller must be an Admin or Manager', while the code also admits DocCtrl. The payload is as described — lib/exportTables.ts registers audit_logs (:130), download_audits (:63), e_signatures (:53), document_acknowledgments (:67), access_recertification_events (:71), ai_usage_events/ai_key_agreements (:160-161) — plus presigned URLs defaulting to 24h (lib/dataExport.ts:85 `const expiresIn = params.presignedUrlSeconds ?? 24 * 60 * 60;`). The one softening fact is that the UI states the policy openly (page.tsx:74/:197), so this is a deliberate widening rather than an oversight — but it still leaves read-out strictly more permissive than write-back."
  },
  {
    "id": "ILIFE-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/admin/orphans/route.ts:23-27 takes an orgId, authorizes against it, then calls `scanOrphans(actor.admin)` — orgId is never passed on; :47 `const result = await deleteOrphans(actor.admin);` likewise. lib/storageOrphans.ts:33 `collectReferencedKeys(sb)` selects each table with no `.eq(\"org_id\", …)`, and :127-128 `new ListObjectsV2Command({ Bucket: R2_BUCKET, ContinuationToken: token, MaxKeys: 1000 })` walks the bucket with no Prefix. `actor.admin` is the service-role client (lib/serverAuth.ts:48 `createClient(supabaseUrl, serviceRoleKey, …)`), so RLS never re-scopes it either.",
    "note": "Confirmed — scan and delete are both bucket-global and the authorized orgId is used only for the permission check and the audit_logs row (:50-51). Because the reference set is also global, another tenant's LIVE files are still protected; the cross-tenant blast radius is other orgs' unreferenced objects — which, given ILIFE-1, includes their vendor quotes and cost documents. The GET also leaks other tenants' storage keys (orgs/<other-org-uuid>/…) into org A's UI, which arguably makes MEDIUM if anything conservative."
  },
  {
    "id": "ILIFE-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260928_site_codebook.sql:38 `CREATE TABLE IF NOT EXISTS codebook_config ( org_id UUID PRIMARY KEY REFERENCES orgs(id)` — no id column; 20260806_intelligence_layer.sql:106 `recently_viewed_docs (... PRIMARY KEY (user_id, document_id))` and :121 `library_numbering ( library_id UUID PRIMARY KEY ...)` likewise. lib/dataRestore.ts:336-342 CONFLICT_TARGETS lists only document_favorites/curated_collection_items/team_members/ticket_number_counters/archive_settings/org_configurations, and :347 `return CONFLICT_TARGETS[table] ?? \"id\";`. All three tables are exported (lib/exportTables.ts:19,24,26) and restorable, so apply-table/route.ts:77 runs `.upsert(chunk, { onConflict: \"id\" ...})` against a table with no id column.",
    "note": "Confirmed: the three tables have no `id` column, are on the export contract, and get conflictTargetFor()==='id'. The upsert fails (42703), the fallback `insert` at route.ts:79 then hits the real PK on any re-run/merge (23505) and returns 500. Blast radius is bounded — restore/page.tsx:203 does `tableFailed = true; break;` and records the table in `failedTables` rather than aborting the restore — but the table's rows are silently not restored. No test covers conflictTargetFor (lib/__tests__/dataRestore.test.ts has no CONFLICT reference)."
  },
  {
    "id": "ING-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/knowledgeIngest.ts:102-107 snapshots the row's file_key/pages_indexed at batch start (`const obj = await r2.send(new GetObjectCommand({ Bucket: R2_BUCKET, Key: doc.file_key }))`, `const from = Number(doc.pages_indexed ?? 0);`), and the commit at 431-446 is `await supabaseAdmin.from(\"knowledge_documents\").update(docUpdate).eq(\"id\", doc.id)` with docUpdate = {page_count, pages_indexed: reached, status: done ? \"ready\" : \"indexing\", error: null, last_section} — no source_version_id / updated_at predicate, no re-read. Meanwhile knowledgeSourceSync.ts:242-260 deletes every chunk and rewrites file_key/source_version_id/source_rev/status='stale'/pages_indexed=0 on the same row. app/api/knowledge/ingest/route.ts:124-134 passes the stale snapshot straight in.",
    "note": "Confirmed: there is no optimistic-concurrency guard anywhere on the commit path — the only status check is route.ts:67 `if (doc.status === \"ready\")`, which a sync-to-'stale' row passes. Note the cron cannot race itself (maintenance/route.ts awaits sync before the drain), so the window needs a browser-driven ingest or an overlapping invocation; the mirror-image variant is equally live — if the sync lands first, the batch indexes pages 51+ of the NEW file and marks it 'ready' with pages 1-50 never indexed."
  },
  {
    "id": "ING-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/knowledge/ask/route.ts:954 `.limit(20000);` — the read is `.in(\"kind\", TAG_ENTITY_KINDS…).order(\"document_id\", { ascending: true }).limit(20000)` with no count request and no `ents.length >= 20000` check anywhere; the slab it feeds says (route.ts:1055-1057) \"DRAWING FACTS — computed deterministically from EVERY sheet's extracted tags. TRUST these for counts and totals\". lib/orchestrator/tools.ts:376 has the identical uncapped-detection `.limit(20000)`.",
    "note": "Verified: repo-wide grep for 20000/20_000 shows the only truncation flag in this path is `truncated: byTag.size > MAX_ROWS` (MAX_ROWS=400), which is about the clickable table, not the 20k row cap; the drawingFacts prose has no equivalent. app/api/knowledge/drawing/route.ts:184 and 281 share the same pattern. The claim of absence holds."
  },
  {
    "id": "ING-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Repo-wide grep for `emptyPages|empty_pages` returns exactly four hits, all inside lib/knowledgeIngest.ts: :56 (`emptyPages: number;` in IngestBatchResult), :111 (`let emptyPages = 0;` — reset per batch), :329 (`if (!pageHadText) emptyPages++;`), :470 (`done, pageCount, pagesIndexed: reached, emptyPages, visionPages,`). Zero consumers. app/api/knowledge/ingest/route.ts:172 `return NextResponse.json({ ...res, visionSkipReason, …})` does spread it over the wire, and route.ts:11-12 promises \"we count them so the UI can say '34 of 900 pages had no extractable text' instead of pretending.\"",
    "note": "Both halves confirmed: no consumer, and `let emptyPages = 0` at line 111 is inside ingestKnowledgeDocBatch so the number is per-batch, never accumulated on the document row. The one UI string that resembles the promise — app/(protected)/knowledge/[id]/page.tsx:1894 \"page(s) had no text layer — read by AI vision\" — is gated on `visionPages > 0`, so in the no-AI-key scenario it renders nothing."
  },
  {
    "id": "ING-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/knowledgeIngest.ts:462-464 accumulates: `.select(\"vision_pages\")…` then `.update({ vision_pages: Number(cur?.vision_pages ?? 0) + visionPages })`. Neither reset path clears it — app/api/knowledge/drawing/route.ts:371 `.update({ status: \"stale\", pages_indexed: 0, page_count: null, last_section: null, error: null })` and lib/knowledgeSourceSync.ts:250-257 (`status: \"stale\", error: null, pages_indexed: 0, page_count: null, last_section: null`). The sticky verdict is app/api/knowledge/drawing/route.ts:294 `: visionPages > 0 ? \"vision\"           // AI read it — SHX/scan handled`, evaluated before the `tags > 0` / `chars > 0` / `\"empty\"` branches.",
    "note": "Confirmed by repo-wide grep: `pages_indexed: 0` appears in exactly those two reset sites and neither touches vision_pages; no other code path writes the column. A rebuild that produces zero tags and zero chars still reports verdict 'vision' once status returns to 'ready'."
  },
  {
    "id": "ING-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/knowledge.ts:387 `const activeIngests = new Set<string>();` with the comment 'One driver per document per tab' — module scope, so it cannot see another tab or the server. components/providers/KnowledgeIndexIndicator.tsx:78-95 selects status in ('pending','stale','indexing') and filters only `!isIngestActive(d.id)`; lib/knowledgeIngest.ts:536-541 (`drainKnowledgeIngestQueue`) selects the same statuses server-side. app/api/knowledge/ingest/route.ts:67 returns early only `if (doc.status === \"ready\")` — no lease, no claim column. The collision is guaranteed to hard-fail by 20260920_per_user_keys_real_limits.sql:39-40 `CREATE UNIQUE INDEX IF NOT EXISTS knowledge_chunks_doc_page_seq_idx ON knowledge_chunks (document_id, page, seq)`, and knowledgeIngest.ts:389 `throw new Error(\\`chunk insert failed: ${insErr.message}\\`)` — the per-row retry above it only catches encoding errors, not 23505 — reaching route.ts:181-183 `.update({ status: \"error\", error: message.slice(0, 500) })`.",
    "note": "Confirmed in full, including the quoted failure mode. Three drivers exist (app-shell indicator, library page loop, cron drain) and nothing server-side arbitrates between them; the losing writer's error is written onto the document row, not just its own response."
  },
  {
    "id": "ING-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/knowledgeSourceSync.ts:242-243 on rev-up: `await supabaseAdmin.from(\"knowledge_chunks\").delete().eq(\"document_id\", existing.id as string);` — that is the ONLY delete in the refresh branch; repo-wide grep shows exactly two deletes of knowledge_page_entities, lib/knowledgeIngest.ts:400-402 `.delete().eq(\"document_id\", doc.id).gte(\"page\", from + 1).lte(\"page\", reached)` (page-range scoped, so pages beyond the new page count are never reached) and app/api/knowledge/drawing/route.ts:368 (the manual 'rebuild' action, whose own comment reads 'clear derived data; the page's auto-indexer re-reads everything (chunks AND entities this time)'). Survivors feed lib/equipmentBridgeServer.ts:65-70 `.from(\"knowledge_page_entities\").select(\"tag, page, kind\").eq(\"document_id\", kdoc.id).eq(\"kind\", \"equipment\")` and app/api/knowledge/ask/route.ts:945-954, whose output is labelled at :1055-1057 'DRAWING FACTS — computed deterministically from EVERY sheet's extracted tags. TRUST these for counts'.",
    "note": "Confirmed with no mitigation: the only path that clears orphaned entities is the manual rebuild button, and its comment ('this time') is itself an acknowledgement that the sync path does not. Stale tags from deleted sheets feed a census the prompt instructs the model to trust as exact."
  },
  {
    "id": "ING-4",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "lib/knowledgeText.ts:305 `const text = buf.join(\" \").trim();` — splitPageIntoSections joins a page's lines with SPACES. lib/knowledgeIngest.ts:316-321 is the only production caller: `const { segments } = splitPageIntoSections(lines, section); ... for (const c of chunkPageText(seg.text))`, so `raw` never contains \"\\n\". splitTables (knowledgeText.ts:54 `const lines = raw.split(\"\\n\");`) then has a single-element array, and the table branch requires `if (end - i >= 3)` (line 66) — unsatisfiable. Repo grep confirms chunkPageText has no other non-test caller, while lib/__tests__/knowledgeText.test.ts:314/324/339 pass newline-joined text directly, so the tests exercise a path production cannot reach. knowledgeVision.ts:49-51 does instruct the model to emit ' | '-separated rows, so the input exists and is destroyed downstream.",
    "note": "The code claim is exactly right and decisive. Severity lowered because the finding missed a live guard: the safety net splitTables' own comment claims to have retired ('the answer prompt then had to carry a standing disclaimer … needs no disclaimer') is STILL in the prompt — app/api/knowledge/ask/route.ts:1501-1502 requires a '**Check:**' line 'whenever a value comes from a table, because PDF table extraction jumbles numbers'. Answers built on jumbled tables are therefore still flagged to the reader, making this a serious quality regression rather than an unguarded wrong-value path."
  },
  {
    "id": "ING-5",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/knowledgeIngest.ts:309 `page: p, kind: \"anchor\", tag: \\`${kindWord} ${cap[2].toUpperCase()}\\`,` versus lib/knowledgeEntityKinds.ts:25-26 `/** Every kind ingestion currently writes. */ export const ENTITY_KINDS = [\"equipment\", \"ref\", \"opc\", \"self\"] as const;` and :33 TAG_ENTITY_KINDS with the same four. lib/__tests__/entityKindGuard.test.ts:30 exempts `\"lib/knowledgeIngest.ts\": \"writes rows (insert/delete), never bulk-reads them\"` — the guard only inspects reads (`const isRead = stmt.includes(\".select(\")`), so a writer inventing a kind is structurally invisible to it.",
    "note": "Factually correct on every point. Lowered to LOW because there is no live consequence: 20260925_entity_kinds.sql drops the kind CHECK entirely so 'anchor' inserts succeed, and every bulk reader already names its kinds in the QUERY (ask/route.ts:952 `.in(\"kind\", TAG_ENTITY_KINDS ...)`, equipmentBridgeServer.ts:69 `.eq(\"kind\",\"equipment\")`), so anchor rows never compete for a row cap. The only consumer of anchors, ask/route.ts:1298, filters `.eq(\"kind\", \"anchor\")`. This is a documentation/latent-hazard defect, not a current defect."
  },
  {
    "id": "ING-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/knowledgeIngest.ts:191-196: `} catch (e) { if (isTimeoutError(e)) { stoppedForTime = true; break; } // Provider hiccup: leave the page textless rather than fail the whole document; a later pass can retry it. visionLeft--; }` — no break, no flag, so the page falls through to :315 `if (!pageHadText) emptyPages++;` and :431-437 `status: done ? \"ready\" : \"indexing\", error: null`. The 'later pass' never comes: pages_indexed advances past the page and nothing re-queues it.",
    "note": "Confirmed. One wording correction: a counter DOES exist — `emptyPages` is returned at knowledgeIngest.ts:470 — but grep shows `emptyPages` appears nowhere else in lib/, app/ or components/, so it is never persisted, never displayed and never gates 'ready'. The document reaches ready with error: null and no record of which sheets the provider dropped."
  },
  {
    "id": "ING-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/knowledgeIngest.ts:123 `for (let p = from + 1; p <= to; p++) {` with the chunk rows at :316-328 tagged `page: p` and built from that page's `segments` only; lib/knowledgeText.ts:140-161 (chunkProse) computes the 160-char overlap inside one string: `start = alignStart(text, Math.max(end - overlap, start + 1));`. Nothing carries a page's tail into the next page's first chunk.",
    "note": "True by construction, and slightly understated — chunking is per SECTION SEGMENT within a page (knowledgeIngest.ts:316-321), so a provision straddling a section heading is split too. I searched for a mitigation and found none: the ask route has no neighbouring-page expansion, the orchestrator has no read-page tool, and the per-document cap of 3 in fuseTier (ask/route.ts:526-533) actively reduces the chance both sides of a page break are retrieved together."
  },
  {
    "id": "ING-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/knowledgeIngest.ts:399-402 deletes the whole batch page range first (`.from(\"knowledge_page_entities\").delete().eq(\"document_id\", doc.id).gte(\"page\", from + 1).lte(\"page\", reached)`), then the slice loop ends at :427 `if (error) break; // missing table — drawing features just stay empty` — the error is swallowed, not rethrown, so execution continues to :431-437 which writes `pages_indexed: reached, status: done ? \"ready\" : \"indexing\", error: null`.",
    "note": "Confirmed: the `break` is written for one cause (missing table) but catches every cause, including a statement timeout, after the destructive delete has already run. Chunks for the same pages are committed independently at :340-343, so the document ends up indexed and 'ready' with a permanently blank tag layer for the tail of the batch, and nothing re-runs those pages short of the manual rebuild in app/api/knowledge/drawing/route.ts:365-372."
  },
  {
    "id": "ING-9",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "app/(protected)/knowledge/[id]/page.tsx:1519-1521 `if (!/\\.pdf$/i.test(file.name)) { showToast({ type: \"error\", title: \\`${file.name}: only PDF files can be indexed.\\` }); continue; }` and :1866 `<input ... accept=\".pdf,application/pdf\" ...>` are the only checks; lib/knowledge.ts:356-361 uploads with `contentType: input.file.type || \"application/pdf\"` and inserts the row client-side. app/api/storage/upload-url/route.ts validates the key and org membership but never the content type, and app/api/knowledge/ingest/route.ts:39-134 does auth/role/vision setup only — no magic-byte or extension check before lib/knowledgeIngest.ts:105-107 `const pdf = await getDocumentProxy(bytes);`.",
    "note": "The absence claim holds — no server-side format validation exists on any leg of the path. Lowered to LOW: the outcome is a clean terminal state (route.ts:181-183 writes status 'error' with the unpdf message prefixed 'Indexing failed:'), the action is Admin/DocCtrl-only, and nothing is corrupted — the cost is one orphaned R2 object and an unhelpful error string, not data loss or a security boundary."
  },
  {
    "id": "INTK-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/intake/upload/route.ts:246-248 `const { data: owned } = await supabaseAdmin.from(\"document_versions\").select(\"id\").eq(\"record_id\", docId).eq(\"intake_link_id\", link.id).limit(1); linkAuthored = !!owned?.length;` — no filter on review_state, released_at or supersedes. The link's own FIRST submission against an ASSIGNED doc inserts exactly such a row (:303-316, review_state 'in_review', intake_link_id = link.id), so on the second submission linkAuthored is true, :257 `if (d.pending_version_id && !(link.allow_auto_supersede && linkAuthored))` no longer blocks, and :302 `const autoNow = !!docId && !!link.allow_auto_supersede && linkAuthored;` fires the :322-329 publish (`current_version_id: versionId, status: \"Issued\", pending_version_id: null`).",
    "note": "Claim confirmed, and it contradicts two explicit written promises: the route's own comment at :299-301 (\"an assigned org-authored controlled drawing ALWAYS goes through review, whatever the link's trust level\") and 20260903_intake_assignments.sql:21-22 / IntakePanel.tsx:409 (\"Their revisions of your documents always go through review, even on trusted links\"). Rejection is not even required for the bootstrap — a first submission left in_review is enough, and the reject path (IntakePanel.tsx:254-259) leaves the version row intact with intake_link_id set, so it bootstraps too."
  },
  {
    "id": "INTK-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/intake/upload/route.ts:97-99 and :360-361: recipients come from `.from(\"org_members\").select(\"uid\").eq(\"org_id\", orgId).eq(\"status\", \"active\").in(\"role\", [\"Admin\", \"DocCtrl\"])` plus the project owner — a raw `.in(\"role\", ...)` that also misses the additive `roles[]` array. The email leg inserts into email_notifications directly (:370-380, `event_type: \"watcher_activity\", status: \"queued\"`) under the comment \"Queued rows honor delivery config\" (:361), bypassing lib/notifications.ts:57-61 `if (prefs?.email_enabled === false) return; if (prefs?.digest_frequency === \"never\") return; if (!shouldSendForEvent(prefs, input.eventType)) return;` and the 60s dedupe (:66-75). app/api/notifications/send-queued/route.ts never reads notification_preferences — it selects `.in(\"status\", [\"queued\",\"failed\"])` and sends. lib/notify/dispatch.ts:3-8 documents emit() as \"THE single entry point... each honoring per-user, per-channel preferences\", and lib/transitionIn.ts:338 shows the sibling path calling `await emit({...})`.",
    "note": "Survives. Verified the one plausible refutation — that the drain honours preferences — and it does not: send-queued has no preference lookup at any point, so a user with email_enabled:false is mailed. No follower resolution (resolveFollowers is never reached), so document watchers get nothing."
  },
  {
    "id": "INTK-11",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "app/api/intake/upload/route.ts:44-46 is the only file validation — `if (!(file instanceof File) || file.size === 0)` / `file.size > MAX_BYTES`; no MIME or extension check anywhere in the route. :268-271 `ContentType: file.type || \"application/octet-stream\"` stores the caller's declared type (same at :74-77 and :163-166). app/api/storage/download-url/route.ts:146-151 signs `new GetObjectCommand({ Bucket: R2_BUCKET, Key: path })` with no ResponseContentDisposition/ResponseContentType override. app/submit/[token]/page.tsx:245 `<input type=\"file\" className=\"hidden\" onChange={...} />` has no accept filter (contrast :173 `accept=\".pdf,application/pdf\"`). documents.document_number and title are bare TEXT (schema.sql:139-140).",
    "note": "The core claim stands — no content validation, attacker-controlled Content-Type persisted, and the presigned GET adds no disposition override, so an uploaded text/html renders inline in the bucket origin. Correction: `rev` is NOT unbounded — route.ts:137 does `.slice(0, 24)` on revLabel (changeNote is capped at 2000 too); only `title` and `number` (:135-136) are written with no length cap. Severity unchanged at MEDIUM."
  },
  {
    "id": "INTK-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/projects/cost/QuotesPanel.tsx:539-545: `const token = (crypto.randomUUID() + crypto.randomUUID()).replace(/-/g, \"\").slice(0, 40); await supabase.from(\"project_intake_links\").insert({ org_id, project_id, token, company_name, purpose: \"quote\", rfq_group, created_by })` — no expires_at written and no expiry input in the create form (:602-612 offers only company + RFQ group). The rendered row (:616-632) offers exactly two actions, `RFQ (.docx)` and `Copy link` — no revoke, while the equivalent document-link UI has both an expiry field (IntakePanel.tsx:62 `const [expires, setExpires] = useState(\"\")`) and `<button onClick={() => void revoke(l)}>... Revoke` (IntakePanel.tsx:359). Audit row: `resource_id: token.slice(0, 8)` (QuotesPanel.tsx:547).",
    "note": "All three legs confirmed. The routes do honour revoked_at/expires_at (upload route :40-41), but the Costs tab never sets or clears either, so a quote link is a permanent bearer credential in practice; and INTAKE_QUOTE_LINK_CREATED stores the first 8 chars of the secret as its resource_id, which both leaks token material into audit_logs and makes the row unjoinable to the link."
  },
  {
    "id": "INTK-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Raw driver text to the anonymous caller at app/api/intake/upload/route.ts:93 `return bad(\\`Couldn't record the quote: ${qErr?.message ?? \"unknown\"}\\`, 500)`, :191 `Couldn't attach the redline: ${updErr.message}`, :294 `Couldn't create the document: ${docErr?.message ?? \"unknown\"}`, :318 `Couldn't record the submission: ${verErr?.message ?? \"unknown\"}`. Oracle at :153-157: a missing row returns `bad(\"Ticket not found.\", 404)` while an existing ticket owned by another link returns `bad(\"This link may only attach redlines to its own collision tickets.\", 403)`. Race at :231-240: `if (!collectionId) { ...insert({ org_id, library_id, name: \\`Intake — ${project.name}\\` })... await supabaseAdmin.from(\"projects\").update({ intake_collection_id: collectionId }).eq(\"id\", projectId); }` — the update's error is never read, and collections (schema.sql:90-111) carries no unique constraint on (library_id, name) or (project scope), only three plain indexes.",
    "note": "All three legs verified, including the claim of absence: there is no UNIQUE index anywhere on collections, so two concurrent first-submissions both see NULL and both insert a folder, and the unchecked projects update makes the survivor arbitrary. The ticket oracle is the weakest leg (it requires a valid ticket UUID, which is not guessable), but the error-text leak and the race stand on their own at MEDIUM."
  },
  {
    "id": "INTK-14",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "20260902_project_intake.sql:74-79 `CREATE OR REPLACE FUNCTION bump_intake_use(p_link UUID) RETURNS VOID LANGUAGE sql SECURITY DEFINER SET search_path = public AS $$ UPDATE project_intake_links SET last_used_at = NOW(), submission_count = submission_count + 1 WHERE id = p_link; $$;` — a repo-wide grep for REVOKE returns 16 hits, none of them for this function, while the sibling pattern is applied consistently elsewhere (20260930_semantic_layer.sql:119-120 `REVOKE ALL ON FUNCTION semantic_search(...) FROM public, anon; GRANT EXECUTE ... TO authenticated`). So the default PUBLIC EXECUTE grant stands and PostgREST exposes rpc/bump_intake_use, writing past the 20260913:100-101 SELECT policy and the 20260902:52-60 write policy.",
    "note": "The technical claim is correct and confirmed by repo-wide search — the REVOKE really is missing and this is an outlier against house style. Severity is overstated at MEDIUM: exploitation needs the link's *id*, a random UUIDv4 that no public surface discloses (the resolve route returns no id, and a member who can already see it under RLS is a controller/owner with write access anyway), and the blast radius is two cosmetic display columns. LOW."
  },
  {
    "id": "INTK-2",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "HIGH",
    "evidence": "app/api/intake/upload/route.ts:322-334 updates `current_version_id`/`status: \"Issued\"` and marks the prior version superseded, then goes straight to notifications — repo-wide grep shows the only callers of runPostPublishSideEffects are lib/reviewControl.ts:499-509, lib/revisions.ts:711 and :1304. lib/postPublish.ts:1-13 states the rule (\"Every path that changes a document's CURRENT revision must run the same side effects\") and :91-144 is what is skipped: notifySuperseded, work-package pin drift, revisionImpact, and the compliance clocks onDocumentIssued / onDocumentIssuedAck / recomputeRetention.",
    "note": "All three named omissions are real — no stale-copy signal to intent holders/watchers, no fresh acknowledgment roster, no review-cycle reset. Lowered from CRITICAL because the route is not silent: :337-358 notifies every Admin/DocCtrl plus the project owner in-app and :363-385 queues them email, and the publish itself is authorized (the unauthorized-publish problem is INTK-1). This is a compliance/integrity gap, strictly less severe than INTK-1. Note also that intake is not the only unpiped writer — lib/revisions.ts:384, lib/documentLifecycle/common.ts:227 and app/(protected)/documents/[libraryId]/page.tsx:2475 also set current_version_id without the pipeline, so \"a fourth writer\" undercounts."
  },
  {
    "id": "INTK-3",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "lib/transitionIn.ts:202-254 adoptDocument re-reads only `id, document_number, library_id, collection_id` (:204-208) and then unconditionally applies `patch` (:211-217) — no impact re-scan, no status/review_state check, and `if (input.newNumber) patch.document_number = input.newNumber` is never re-scanned for collision against the new number. TransitionInPanel.tsx:241-245 renders Adopt with `disabled={busy === c.docId || !destLib}` — not gated on `impact.clean` (only the bulk path filters, :113). lib/transitionIn.ts:73-80 lists candidates with only `.neq(\"status\", \"Superseded\")`, and IntakePanel.tsx:254-259 rejects by setting the VERSION's review_state and clearing pending_version_id — the documents row is untouched, so it keeps appearing.",
    "note": "Every factual clause verified, including that the row renders only label/rev/company (:184-185) with no rejected/in-review indicator. Lowered from HIGH because the panel does show the collision loudly before the click (:193-195 collapsed chip, :205-211 expanded red box), so a colliding adoption is a deliberate override rather than a blind one; and an adopted rejected sheet lands as status 'Draft' with no current_version_id (the intake route only ever set pending_version_id, :331-333), so it does not become an issued controlled revision — it squats in the library."
  },
  {
    "id": "INTK-4",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "app/api/intake/upload/route.ts:257-259 lets a trusted link-authored submission through even when `d.pending_version_id` is set, and :325 then writes `pending_version_id: null` while nothing ever touches the old version's review_state — it stays 'in_review' forever. IntakePanel.tsx:106-113 builds the queue by selecting documents `.not(\"pending_version_id\", \"is\", null)` and keeping only versions where `String(dMap.get(String(v.record_id))?.pending_version_id ?? \"\") === String(v.id)`, so the orphan drops out silently.",
    "note": "Mechanism confirmed exactly. Lowered from HIGH because this is the trusted link's OWN document publishing its own newer revision — the register ends up correct (Rev C current) and no authority is bypassed; the harm is a permanently in_review zombie row and a review item that vanishes from the controller's queue with no notice."
  },
  {
    "id": "INTK-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/intake/upload/route.ts:284-296 inserts the document with `{org_id, library_id, collection_id, name, title, document_number, status, created_by_name, updated_at}` — no uniqueness_key. lib/transitionIn.ts:211-217 builds the adoption patch as `{library_id, collection_id, updated_at}` plus `document_number` and never recomputes it either. The index is partial: 20260619_document_uniqueness_configurable.sql:39-46 `CREATE UNIQUE INDEX … ON documents(library_id, uniqueness_key) WHERE uniqueness_key IS NOT NULL AND status NOT IN ('Archived','Superseded')`, and lib/uniqueness.ts:22-42 computeUniquenessKey is what every other path calls.",
    "note": "Confirmed by repo-wide search: computeUniquenessKey is called only from app/(protected)/documents/[libraryId]/page.tsx (:2438 create, :2564/:2596 save), CsvImportModal.tsx:162-176 and BulkEditModal.tsx:69-79 — never from the intake route or from adoptDocument. A NULL key opts the row out of the index entirely, so an intake-born number can duplicate a live controlled number both before and after adoption, and renumbering during adoption leaves it NULL as well."
  },
  {
    "id": "INTK-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/exportTables.ts:51 lists \"project_intake_links\" in ORG_SCOPED_TABLES; lib/dataExport.ts:95-98 `for (const tbl of ORG_SCOPED_TABLES) { const rows = await dumpTable(sb, tbl, \"org_id\", params.orgId)` and dumpTable does `sb.from(table).select(\"*\")` (lib/dataExport.ts:300) — no column filter, no redaction anywhere in dataExport.ts/exportTables.ts/exportRunner.ts. 20260913_projects_rls_recursion_fix.sql:100-101 `CREATE POLICY project_intake_links_select ... USING (is_org_controller(org_id) OR is_project_owner(...))` and is_org_controller = `role IN ('Admin','DocCtrl') OR roles && ARRAY['Admin','DocCtrl']` (20260814:38) — Manager excluded. app/api/data-export/destinations/route.ts:13 `const ADMIN_ROLES = [\"Admin\", \"Manager\", \"DocCtrl\"]`. project_intake_links.token is TEXT NOT NULL UNIQUE (20260902:22), the live bearer credential the token-gated routes accept.",
    "note": "Confirmed on every leg: the table is exported with `select(\"*\")`, nothing masks `token`, and a Manager — who cannot SELECT the table under the current RLS policy — can create an S3/webhook destination and receive the whole org's live intake tokens on every scheduled run (run-scheduled uses the service-role key and bypasses RLS entirely). /api/data-export/structured and /run are likewise Manager-callable."
  },
  {
    "id": "INTK-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/transitionIn.ts:124 `if (candidate.number) {` gates the entire hard-collision query; app/submit/[token]/page.tsx:230 renders the field as `placeholder=\"Drawing number (optional)\"` and app/api/intake/upload/route.ts:136 `const number = String(form.get(\"number\") ?? \"\").trim() || null` accepts null. Truncation: `.ilike(\"document_number\", candidate.number) ... .limit(5)` then `.find((d) => d.status !== \"Superseded\" && d.status !== \"Archived\")` (:129-137), `.limit(200)` on document_assets (:152) and `[...byDoc.keys()].slice(0, 50)` (:161). `out.clean = !out.numberCollision && out.overlapDocs.length === 0` (:180). adoptDocument (:202-254) re-checks nothing.",
    "note": "Survives. A blank number skips the collision query outright, and the tag scan (TAG_SCAN_RE over number+title, :57-64) is the only other input, so a numberless/tagless sheet is always `clean: true` and adoptAllClean (TransitionInPanel.tsx:113-117) moves it into the controlled library with no further check. One correction to the wording: the unescaped `ilike` pattern is a real injection of LIKE metacharacters but its effect is *extra* matches (false collisions), not evasion — the evasion path is the optional number, and the `.limit(5)`-before-filter truncation is the genuine false-negative."
  },
  {
    "id": "INTK-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/intake/upload/route.ts:29-32: `try { form = await req.formData(); } catch { return bad(...) }` executes BEFORE `const token = String(form.get(\"token\")...)` and the `/^[A-Za-z0-9_-]{16,128}$/` test — the full 100 MB body is buffered before any credential check. :44-46 only checks size/instanceof; :266-271 `const bytes = new Uint8Array(await file.arrayBuffer()); await r2.send(new PutObjectCommand(...))` streams through the function. Repo-wide, rate limiting exists only in signup (20261010_signup_rate_limit.sql) — grep for rate_limit/rateLimit across app/ and lib/ returns only knowledge-embedding backoff, and there is no middleware.ts in the repo. lib/storage.ts:411-421 shows the internal path instead does `getPresignedUploadUrl(...)` + `putWithXhr(uploadUrl, ...)` direct to R2.",
    "note": "Confirmed as a claim of absence: no throttle, no per-token ceiling (bump_intake_use increments submission_count but nothing reads it as a limit), body parsed before auth, and this is the only browser-facing upload that passes bytes through the serverless function. Each replay also fans out notifications + queued email to every Admin/DocCtrl (:377-385) and an R2 object."
  },
  {
    "id": "INTK-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/intake/upload/route.ts:249-256: `const isAssigned = (((link.assigned_doc_ids as string[] | null) ?? [])).includes(docId); if (!linkAuthored && !isAssigned) return bad(...); const { data: d } = await supabaseAdmin.from(\"documents\").select(...).eq(\"id\", docId).maybeSingle();` — no `.eq(\"org_id\", orgId)`. app/api/intake/resolve/route.ts:113-115 `.from(\"documents\").select(\"id, document_number, title, name, rev, status, pending_version_id, updated_at\").in(\"id\", docIds)` — also no org filter. 20260903_intake_assignments.sql:18-19 `ADD COLUMN IF NOT EXISTS assigned_doc_ids UUID[] NOT NULL DEFAULT '{}'` — plain UUID[], no FK, no check. 20260902:51-60 write policy admits `is_org_controller(org_id) OR EXISTS (SELECT 1 FROM projects p WHERE p.id = ... AND p.owner_user_id::text = auth.uid()::text)`. components/projects/IntakePanel.tsx:217-218 updates the array straight from the browser: `await supabase.from(\"project_intake_links\").update({ assigned_doc_ids: ids }).eq(\"id\", l.id)`.",
    "note": "Survives at HIGH. Neither public route constrains the fetched document to the link's org, and the write path is a raw client-side array update that RLS opens to any project owner regardless of org role. Worse than the summary states: the upload route not only reads the foreign document, it writes to it — `.from(\"documents\").update({ pending_version_id: versionId })` (:333) — a cross-tenant write."
  },
  {
    "id": "IRLS-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260911_knowledge_ai.sql:146-150 `CREATE POLICY knowledge_questions_select ON knowledge_questions FOR SELECT USING (EXISTS (SELECT 1 FROM org_members WHERE org_id = knowledge_questions.org_id AND uid = auth.uid() AND status = 'active'));` — any active member, no visibility/ACL term. Contrast 20260917_knowledge_sources.sql:74-82, which deliberately closes the source: `AND NOT EXISTS (SELECT 1 FROM knowledge_documents d WHERE d.id = knowledge_chunks.document_id AND d.source_document_id IS NOT NULL)`. The answer derived from those very chunks is then written at app/api/knowledge/ask/route.ts:1739 `await supabaseAdmin.from(\"knowledge_questions\").insert({ … question, answer, citations, … })`.",
    "note": "Per-asker ACL filtering is real and the code says so — ask/route.ts:160-186 builds excludedDocIds via `readableControlledDocIds(principal, dcIds)` and comments \"two people can ask the same question and correctly get different answers. … Fails CLOSED\". Both readers (lib/knowledge.ts:504-527 searchAskHistory, :529-533 listKnowledgeQuestions) use the RLS-bound `supabase` client filtered only by org_id/library_id, so the stored answer text is org-wide. No later migration narrows knowledge_questions_select (grep over supabase/ confirms only ALTERs adding columns)."
  },
  {
    "id": "IRLS-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260814_documents_delete_controllers.sql:30-31 `CREATE OR REPLACE FUNCTION is_org_controller(p_org uuid) RETURNS boolean LANGUAGE sql STABLE SECURITY DEFINER AS $$` — no SET search_path, while ten other SECURITY DEFINER functions in the same tree do set it (e.g. 20260901_db_hard_enforcement.sql:29 `… SECURITY DEFINER SET search_path = public AS $$`). supabase/schema.sql:1031-1032 `CREATE OR REPLACE FUNCTION my_org_ids() RETURNS SETOF UUID LANGUAGE SQL SECURITY DEFINER AS $$` is the same omission. Authority half: 20260928_site_codebook.sql:66-68 `codebook_entries_write … AND role IN ('Admin', 'DocCtrl')`, 20260806_intelligence_layer.sql:47 `AND m.role IN ('Admin', 'DocCtrl')`, 20260807_link_proposals.sql:87 `AND m.role IN ('Admin', 'DocCtrl', 'Manager', 'Supervisor')`, 20260929_mention_engine.sql:83 same — none consult roles[].",
    "note": "Both halves verified. The consequence is documented in the app itself at lib/codebook.ts:382-387: \"the codebook RLS write policy checks only the headline role column, so a client-side update could silently affect zero rows for a member whose DocCtrl authority lives in the additive roles[] array\" — while is_org_controller (20260814:38) is precisely the function that DOES honor `roles && ARRAY['Admin','DocCtrl']`."
  },
  {
    "id": "IRLS-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20261017_process_flows.sql:49-53 `CREATE POLICY process_flows_insert ON process_flows FOR INSERT WITH CHECK (EXISTS (SELECT 1 FROM org_members m WHERE m.org_id = process_flows.org_id AND m.uid = auth.uid() AND m.status = 'active') AND created_by = auth.uid());` — no constraint on status or origin, whose column defaults at :24-25 are `status TEXT NOT NULL DEFAULT 'confirmed'` and `origin TEXT NOT NULL DEFAULT 'manual'`. lib/processFlows.ts:58-66 createManualFlow writes `status: \"confirmed\", origin: \"manual\"` through the RLS-bound `supabase` client.",
    "note": "Confirmed, including the UI path: app/(protected)/graph/page.tsx:334-346 calls createManualFlow for any asset↔asset or unit↔unit pair, and grep for role/ADMIN_ROLES in that page returns no authorization gate on Connect mode. Over PostgREST a member can also set origin='ai' directly, since only created_by is checked."
  },
  {
    "id": "IRLS-12",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "lib/schemaExpectations.ts:29-115 lists 92 tables ending at `{ table: \"work_packages\", migration: \"20260825_work_packages_acks.sql\" }` — a diff of every `CREATE TABLE IF NOT EXISTS` in supabase/migrations against that list leaves answer_skills (20261016_reasoning_skills.sql:14), link_rules (20261015_connection_skills.sql:16), process_flows (20261017_process_flows.sql:14) unprobed. Silent degrade is real: lib/answerSkillsServer.ts:61 `if (res.error) return \"\"; // pre-migration — degrade silently` and lib/processFlows.ts:33 `!!e && (e.code === \"42P01\" ...)`. app/api/admin/schema-health/route.ts iterates only EXPECTED_TABLES/EXPECTED_COLUMNS, so `healthy: missingTables.length === 0 && missingColumns.length === 0` is true with all three absent.",
    "note": "The core claim holds for answer_skills, link_rules and process_flows. The fourth name is wrong: knowledge_line_traces is a RETIRED feature, not a live one — supabase/migrations/20261007_retire_line_traces.sql:9 `DROP TABLE IF EXISTS knowledge_line_traces;` and the only remaining reference is lib/exportTables.ts:177, so its absence from EXPECTED_TABLES is correct, not a gap. Severity unchanged; the finding should say three tables, not four. (Also unprobed but outside the claim: change_orders, checklist_items, companies, company_events, project_checklists, punch_items, turnover_items.)"
  },
  {
    "id": "IRLS-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260807_link_proposals.sql:111-113 `CREATE UNIQUE INDEX IF NOT EXISTS document_related_resources_doc_target_idx ON document_related_resources (document_id, target_document_id) WHERE target_document_id IS NOT NULL;` — partial. lib/linkProposerServer.ts:405 `.upsert(rows, { onConflict: \"document_id,target_document_id\", ignoreDuplicates: true });` and :406-407 `if (error) notes.push(\"Auto-apply skipped: \" + error.message); else autoApplied = rows.length;`.",
    "note": "Confirmed: grep over supabase/ shows the partial index is the ONLY unique constraint on that pair (the table's only other key is `id UUID PRIMARY KEY` at 20260806_intelligence_layer.sql:69), so ON CONFLICT with a bare column list cannot be inferred → 42P10 on every autoApply batch, swallowed into notes with autoApplied left at 0. The codebase already knows this failure mode: lib/answerSkills.ts:56-58 says \"the unique (org_id, builtin_key) index is PARTIAL, which ON CONFLICT can't infer through the API, so an upsert here fails wholesale.\" The sibling proposed_links upsert works because proposed_links_pair_idx (20260807:68-69) is not partial."
  },
  {
    "id": "IRLS-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20261016_reasoning_skills.sql:44-49 `CREATE POLICY answer_skills_insert ON answer_skills FOR INSERT WITH CHECK (EXISTS (SELECT 1 FROM org_members m WHERE m.org_id = answer_skills.org_id AND m.uid = auth.uid() AND m.status = 'active') AND created_by = auth.uid());` — status='active' is the whole authority test. Defaults at :22-23: `enabled BOOLEAN NOT NULL DEFAULT true, visibility TEXT NOT NULL DEFAULT 'org'`. Consumption: lib/answerSkillsServer.ts:30-31 `r.enabled && (r.visibility === \"org\" || (askerId !== null && r.created_by === askerId))` over a service-role read of the whole org (`.eq(\"org_id\", orgId).limit(200)`), with no author-role check anywhere.",
    "note": "Confirmed. lib/roleCapabilities.ts:59-60 shows Requester and Accounting hold only [\"create_requests\"], and lib/answerSkills.ts:73-96 createAnswerSkill uses the RLS-bound client with no role gate (its only caller, components/intelligence/SkillStudio.tsx:97, is likewise ungated). Mitigating context, not refuting: the migration header at :9-12 states this authority model as intentional (\"any active member may author\"), and the assembled block ends with \"they never override the citation and safety rules above\" — but that is a prompt-level request, not an enforcement boundary."
  },
  {
    "id": "IRLS-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260929_mention_engine.sql:59-60 `CREATE UNIQUE INDEX IF NOT EXISTS entity_mentions_unique_idx ON entity_mentions (asset_id, COALESCE(knowledge_document_id, document_id), page);` — an EXPRESSION index. lib/mentionIndexer.ts:138-142 `.from(\"entity_mentions\").upsert(batch, { onConflict: \"asset_id,knowledge_document_id,page\", ignoreDuplicates: false }); if (error) throw new Error(\"mention index write: \" + error.message);`",
    "note": "Confirmed: grep over supabase/ shows entity_mentions_unique_idx is the table's only unique index besides `id UUID PRIMARY KEY`, and `COALESCE(knowledge_document_id, document_id)` cannot be matched by the plain column `knowledge_document_id` in ON CONFLICT → 42P10, which is thrown rather than swallowed (unlike IRLS-2). lib/mentionIndexer.ts:138 is the sole write path in the repo (grep for entity_mentions shows every other TS hit is a SELECT or DELETE, except the generic lib/dataRestore.ts restore list)."
  },
  {
    "id": "IRLS-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260605_rls_policies_new_tables.sql:26-30 `CREATE POLICY \"assets_member_all\" ON assets FOR ALL TO authenticated USING (EXISTS (SELECT 1 FROM org_members WHERE org_id = assets.org_id AND uid = auth.uid() AND status = 'active')) WITH CHECK (…same…);` and the file's own header at :12-14: \"Role-based authorization (e.g. only Admins can delete an asset) is handled in application code, not RLS.\"",
    "note": "Confirmed. The only gate is UI-side — app/(protected)/admin/assets/page.tsx:56 `const ADMIN_ROLES = [\"Admin\", \"DocCtrl\", \"Manager\", \"Supervisor\"];` — while lib/assets.ts:217-220 deleteAsset issues a bare `supabase.from(\"assets\").delete().eq(\"id\", id)` over the user's JWT. Unlike documents (20260814:41 `documents_delete_controllers … AS RESTRICTIVE FOR DELETE USING (is_org_controller(org_id))`), no restrictive policy exists on assets — grep over all migrations returns no other assets policy. Cascade impact is real: entity_mentions.asset_id and asset_aliases.asset_id are both `REFERENCES assets(id) ON DELETE CASCADE`."
  },
  {
    "id": "IRLS-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260806_intelligence_layer.sql:57-61 `ALTER TABLE knowledge_questions ADD COLUMN IF NOT EXISTS search_tsv tsvector GENERATED ALWAYS AS (…) STORED;` versus supabase/migrations/20260911_knowledge_ai.sql:91 `CREATE TABLE IF NOT EXISTS knowledge_questions (` — the table is created 105 days later in filename order.",
    "note": "Confirmed on a fresh DB: knowledge_questions does not exist in supabase/schema.sql (grep over its 1341 lines returns nothing), there is no README or ordering manifest in supabase/migrations, and lib/schemaExpectations.ts:4-5 states migrations are \"applied BY HAND in the Supabase SQL editor\" — where a multi-statement script runs as one implicit transaction, so the whole file rolls back. The knock-on is real too: 20260807_link_proposals.sql:95 does `ALTER TABLE document_related_resources ADD COLUMN…` on a table only created at 20260806:68, inside the file that just aborted."
  },
  {
    "id": "IRLS-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260917_knowledge_sources.sql:31 `source_id UUID NOT NULL,                 -- libraries.id or collections.id` (no REFERENCES) and :53-54 `ALTER TABLE knowledge_documents ADD COLUMN IF NOT EXISTS source_document_id UUID;` — compare the line immediately above it, :51-52, which DOES carry `REFERENCES knowledge_sources(id) ON DELETE CASCADE`. Cleanup is cron-only: lib/knowledgeSourceSync.ts:286-292 `for (const [dcDocId, row] of existingByDcDoc) { if (wanted.has(dcDocId)) continue; … .from(\"knowledge_documents\").delete().eq(\"id\", row.id) … }`.",
    "note": "Structural claim verified — the mirrored knowledge_documents row and its knowledge_chunks (full extracted text) outlive a deleted controlled document until the next sync. One narrowing the finding does not state: for non-controllers the ask route fails closed on orphans, because lib/knowledgeAccess.ts:201-205 finds no `documents` row for a deleted id so it never enters `readable`; but lib/knowledgeAccess.ts:196 `if (principal.isController) return new Set(docIds);` returns ALL ids unchecked, so Admin/DocCtrl answers can still cite the deleted document. Also note the cited 20261017_process_flows.sql:17-20 is the polymorphic from_kind/from_ref/to_kind/to_ref pair (correctly FK-less by design); that file's source_document_id at :27 does have a proper FK."
  },
  {
    "id": "IRLS-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260928_site_codebook.sql:108-111 `CREATE POLICY doc_equip_sugg_write ON document_equipment_suggestions FOR ALL USING (org_id IN (SELECT org_id FROM org_members WHERE uid = auth.uid() AND status = 'active')) WITH CHECK (…same…);` — FOR ALL, no role term. The trust is at lib/equipmentBridgeServer.ts:143-150: `const applied: string[] = Array.isArray(existing?.applied) ? (existing?.applied as string[]) : []; const newTags = suggestions.map((s) => s.tag).filter((t) => !applied.includes(t));` … `status: newTags.length === 0 && applied.length > 0 ? \"applied\" : \"pending\",`.",
    "note": "Confirmed: `applied` is read straight back from the member-writable row and used as the idempotence base, so a forged applied[] array makes every recompute stamp status 'applied' with zero tags ever written to the document column. applyForDocument (lib/equipmentBridgeServer.ts:174-179) reads the same row's `suggested`/`applied` with the same trust. Contrast the sibling policies in the same file at :65-73, where codebook_entries_write and codebook_config_write DO carry `AND role IN ('Admin','DocCtrl')`."
  },
  {
    "id": "IRLS-9",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "The policy is as described — supabase/migrations/20260929_mention_engine.sql:73-76 `CREATE POLICY entity_mentions_read ON entity_mentions FOR SELECT USING (EXISTS (SELECT 1 FROM org_members m WHERE m.org_id = entity_mentions.org_id AND m.uid = auth.uid() AND m.status = 'active'));` over :38 `context_snippet TEXT NOT NULL,` (\"The evidence. This column is the entire reason the table exists.\"), and lib/mentionIndexer.ts:85-121 reads knowledge_chunks with no `source_document_id` filter, writing `context_snippet: s.snippet` for mirrored ACL documents. But the write that would populate it is the one IRLS-4 proves always raises 42P10: lib/mentionIndexer.ts:138-142.",
    "note": "The RLS design flaw is real and correctly described, but the exploit path is unreachable today — by the batch's own IRLS-4, entity_mentions has never been written, so the backlinks panel (lib/mentions.ts:126-134 mentionsForAsset) returns an empty set and there are no snippets to leak. It is a latent exposure that goes live the moment IRLS-4's onConflict is fixed; LOW until then. (The one path that could seed rows independently is a backup restore — lib/dataRestore.ts:298 lists entity_mentions.)"
  },
  {
    "id": "KACL-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260911_knowledge_ai.sql:146-150 `CREATE POLICY knowledge_questions_select ON knowledge_questions FOR SELECT USING (EXISTS (SELECT 1 FROM org_members WHERE org_id = knowledge_questions.org_id AND uid = auth.uid() AND status = 'active'))` — membership only, no library or ACL predicate. app/api/knowledge/ask/route.ts:1643 stores `quote: truncateSafe(c.content, 1600)` plus `documentName` into that row (insert at :1739-1744). lib/knowledge.ts:510-516 `searchAskHistory` filters on `.eq(\"org_id\", orgId)` alone, and page.tsx:1412 `const past = await searchAskHistory(activeOrgId, q, 3)` feeds :1705-1713 which sets the past answer AND `citations: pa.citations` as the live answer.",
    "note": "Confirmed with no mitigating guard anywhere: the history row is readable by any active org member through the browser client under RLS, and it carries verbatim 1600-char quotes and document names from documents that member may be denied. The per-asker ACL filter (route.ts:157-187) runs only over live retrieval, never over replayed history."
  },
  {
    "id": "KACL-10",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "`ai_excluded` occurs 0 times in app/api/knowledge/ask/route.ts — the only filter in the retrieval fan-out is :431 `.filter((c) => !excludedDocIds.has(c.document_id))`, which is the ACL set. Enforcement is exactly the two places named: lib/knowledgeSourceSync.ts:108-115 takes a one-shot snapshot (`const aiExcluded = new Set<string>(); ... .eq(\"ai_excluded\", true)`) that is consumed much later at :164-168 `aiReadability({ ... aiExcluded: aiExcluded.has(d.id) }, true)` with no re-read before the batched inserts at :208+, and app/api/knowledge/exclusion/route.ts:59-102 sets the flag then deletes mirrors, returning `{ excluded, purged: mirrorIds.length }`. Nothing re-checks the flag between snapshot and insert, so a purge landing inside that window is undone by the in-flight sync.",
    "note": "The race and the missing retrieval-layer enforcement both hold for the knowledge ask path. \"Enforced nowhere in the retrieval path\" is over-broad, though: the orchestrator's retrieval DOES enforce it explicitly — lib/orchestrator/tools.ts:101 `.eq(\"ai_excluded\", false)` and :144-160 map excluded controlled docs to mirrors and filter graph_ask passages. Severity stays MEDIUM."
  },
  {
    "id": "KACL-11",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/knowledgeAccess.ts:77 `return visibility !== \"hidden\";` (no-ACL fallback) vs lib/permissions.ts:125 `if (!decision) return visibility !== \"hidden\" && visibility !== \"private\";`. The DB agrees with permissions.ts, not knowledgeAccess.ts: supabase/migrations/20260708_acl_rls_enforcement.sql node_visible() — `IF p_visibility IS NULL OR p_visibility = 'normal' THEN RETURN true; ... IF p_acl_index IS NULL THEN RETURN false;` applied as `CREATE POLICY documents_acl_select ON documents AS RESTRICTIVE FOR SELECT USING (node_visible(visibility, acl_index, org_id))`. app/api/storage/download-url/route.ts:71-89 also 403s the bytes of a private doc with a null acl via canDiscover. lib/acl.ts:189 confirms 'private' is a first-class chain visibility, and types/schema.ts:81 `NodeVisibility = \"normal\" | \"hidden\" | \"private\"`. Columns are genuinely distinct (supabase/schema.sql documents: `visibility TEXT DEFAULT 'normal'`, `acl JSONB`, `is_private BOOLEAN`, `scope TEXT`), so the guard at lib/knowledgeAccess.ts:210 `if ((doc.is_private || doc.scope === \"private\") && doc.created_by !== principal.uid) continue;` does not cover visibility='private'.",
    "note": "The divergence is real and unmitigated: readableControlledDocIds/containerReadable are the only readers of the ACL chain that treat 'private' as readable-by-default, and lib/knowledgeSourceSync.ts mirrors source documents with no visibility/acl filter (grep for visibility|acl|is_private|scope in that file returns nothing), so such a doc would be indexed and quoted while RLS hides the row from the browser entirely. Severity lowered to LOW because I could not find any in-app writer that produces visibility='private' with acl NULL: PermissionDrawer.tsx:265-283 always writes acl and visibility in the same payload; document creation at app/(protected)/documents/[libraryId]/page.tsx:2448-2451 does set them independently (`visibility: library.defaultNewVisibility ?? \"normal\", acl: library.defaultNewAcl ?? null`) but nothing in the codebase ever sets default_new_visibility to anything but 'normal' (LibraryWizard.tsx:273; admin/libraries/page.tsx:107 only round-trips the stored value, with no UI control). The only path that can materialize the state is the blind restore importer (app/api/admin/restore/apply-table/route.ts:77-83 upserts caller-supplied rows verbatim, and 'documents' is in the importable contract, lib/exportTables.ts:44) or a direct DB write — i.e. latent defence-in-depth, not a demonstrated live leak."
  },
  {
    "id": "KACL-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/orchestrator/tools.ts:10-12 states the rule — \"NOTHING WIDENS ACCESS... An orchestrator that can read more than the person driving it is a data leak with a friendly interface\" — and the code breaks it: find_documents (:93-105) filters only `.eq(\"org_id\", ctx.orgId)`, `.eq(\"ai_excluded\", false)`, `.neq(\"status\",\"Archived\")`; search_documents (:133-135) calls `supabaseAdmin.rpc(\"graph_ask\", { p_org_id: ctx.orgId, ... })`. graph_ask is SECURITY INVOKER (20260929_mention_engine.sql:115) whose only stated protection is \"restricted to libraries the caller can read via the existing knowledge RLS\" — but supabaseAdmin is the service role, which bypasses RLS entirely. A repo-wide grep for readableControlledDocIds/loadPrincipal returns 14 call sites, none of them under app/api/orchestrator or lib/orchestrator.",
    "note": "Confirmed. app/api/orchestrator/route.ts:61-66 checks active membership and reads `role` only; the read tools never consult role, visibility, is_private or any ACL, so a Viewer denied read on a folder gets its passages back verbatim. Only ai_excluded is honored."
  },
  {
    "id": "KACL-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/knowledgeAccess.ts:73-77 — `if (decision) { if (visibility === \"hidden\") return decision.can(\"read\") || decision.isDiscoverable(); return decision.can(\"read\"); }`. For a hidden node lib/acl.ts:139-141 makes `isDiscoverable()` collapse to `can(\"discover\")`, so `discover` alone satisfies the branch, while a normal-visibility document with the same ACL is held to `decision.can(\"read\")`. The app's own door, lib/permissions.ts:127-129, treats hidden as STRICTER: `if (visibility === \"hidden\" || visibility === \"private\") return decision.can(\"discover\")` and that is only a DISCOVER verdict, never a licence to read the text.",
    "note": "Confirmed, and the inversion is actually wider than claimed: the `visibility` argument is the document's own column while `decision.visibility` comes from the merged chain, so a doc with visibility='hidden' but a chain whose acl.visibility is 'normal' hits lib/acl.ts:142-153, where `upload`, `editMetadata` or `publish` alone also satisfy isDiscoverable() and unlock full text."
  },
  {
    "id": "KACL-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/knowledge/ask/route.ts:166-172 — `const { data: linkedDocs, error: linkErr } = await supabaseAdmin.from(\"knowledge_documents\").select(\"id, source_document_id\").in(\"library_id\", allLibIds).not(\"source_document_id\",\"is\",null);` then `if (!linkErr && linkedDocs && linkedDocs.length > 0) {`. Any error at all — not just the 42703 the comment anticipates — leaves `excludedDocIds` the empty set built at :163, and :431 `.filter((c) => !excludedDocIds.has(c.document_id))` then excludes nothing. The stated contract at :161-162 (\"Fails CLOSED: if the readable set can't be computed, linked docs are excluded\") is honored only inside the inner try/catch at :183-185. The query carries no `.range()`/`.limit()`, while the codebase's own convention elsewhere is explicit about the cap: lib/storageOrphans.ts:91 \"Page through — .range in 1000-row windows so big tables don't truncate\" and lib/orgGraph.ts:75-93 `pageRows(... cap)` returning a `capped` flag.",
    "note": "Both halves confirmed. The fail-open half is unconditional and needs no configuration to bite. The truncation half depends on the deployment's PostgREST max-rows setting, but the repo treats that cap as real everywhere else it reads a large table, and this security-critical query is the one place that neither pages nor reports truncation."
  },
  {
    "id": "KACL-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/storage/download-url/route.ts:71 `if (doc && (visibility === \"private\" || visibility === \"hidden\")) {` — the guard is entered only for those two visibilities; :76-86 then calls `canDiscover({... aclChain: [doc.acl as AccessControl | undefined], visibility })`, a DISCOVER verdict against the document's own ACL alone, with no library or folder chain. An allow-list rule on the folder (grant read to a team, visibility left 'normal') therefore never reaches any check. Mirrors expose the key: lib/knowledgeSourceSync.ts writes `file_key: version.file_url` (the same R2 key document_versions.file_url holds), and knowledge_documents SELECT is membership-only (20260911_knowledge_ai.sql:124-128).",
    "note": "Confirmed, with one nuance the finding omits: :91-111 does enforce explicit DOWNLOAD DENY entries through the chain-resolved acl_index. That closes deny-list ACLs only — an allow-list ACL produces no deny entries, so the claim's central scenario stands untouched, as does the fail-open `catch { }` at :113-115."
  },
  {
    "id": "KACL-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/knowledge/ask/route.ts:103-112 is the entire authorization: `.from(\"org_members\").select(...).eq(\"org_id\", orgId).eq(\"uid\", user.id).eq(\"status\",\"active\")` then `.from(\"knowledge_libraries\").select(\"*\").eq(\"id\", libraryId).eq(\"org_id\", orgId)` — nothing else is checked before retrieval. The table has no ACL column to check: 20260911_knowledge_ai.sql:47-56 defines knowledge_libraries as (id, org_id, name, description, created_by, created_by_name, created_at), and its SELECT policy at :114-118 is membership-only, so ids are freely enumerable. Linked libraries are then traversed at :124-132 with no further check.",
    "note": "Confirmed. Upload-origin documents have source_document_id NULL, so they never enter the linkedDocs set at :166-170 and no exclusion can apply to them — a private data-room library really is fully answerable by any active member."
  },
  {
    "id": "KACL-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260917_knowledge_sources.sql:69-82 replaces only the CHUNK policy — `CREATE POLICY knowledge_chunks_select ... AND NOT EXISTS (SELECT 1 FROM knowledge_documents d WHERE d.id = knowledge_chunks.document_id AND d.source_document_id IS NOT NULL)` — and leaves 20260911_knowledge_ai.sql:124-128 `knowledge_documents_select ... EXISTS (SELECT 1 FROM org_members ...)` untouched. lib/knowledge.ts:341-347 `listKnowledgeDocuments` does `.select(\"*\").eq(\"library_id\", libraryId)` under the caller's own client, and lib/knowledgeSourceSync.ts:47-52 `displayName` composes exactly `${number} — ${title}` into the readable `name` column.",
    "note": "Confirmed: the 20260917 lockdown is chunk-only, so every mirror's document number, title, source_rev, page_count — and file_key, which is the same R2 key KACL-5 turns into bytes — stays readable by any active org member regardless of the controlled document's ACL."
  },
  {
    "id": "KACL-8",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "app/api/knowledge/ask/route.ts:1367 DOES filter — `const usable = legendDocIds.filter((id) => !excludedDocIds.has(id));` — but excludedDocIds is built at :165-170 only from `.in(\"library_id\", allLibIds)` where allLibIds = [libraryId, ...linkedLibraries], so a legend sheet living in any OTHER knowledge library is never a member of that set and the filter passes it vacuously. The picker is org-wide by design: admin/codebook/page.tsx:470-472 searches `.from(\"knowledge_documents\").select(\"id, name\").eq(\"org_id\", orgId).ilike(\"name\", ...)` and the UI text at :487-489 says \"indexed in any knowledge library... Every knowledge library inherits them automatically\". lib/codebookServer.ts:31 then feeds `legendDocIds` into every ask, and :1369-1383 injects up to 6000 chars of raw chunk text.",
    "note": "The substance holds — a site-wide legend that mirrors a restricted controlled document is injected into every member's answers — but the title is wrong on mechanism: the per-asker filter IS applied at :1367; it is the exclusion SET that is scoped to the asked and linked libraries only. Severity stays MEDIUM (bounded to at most 3 legend docs, 6000 chars)."
  },
  {
    "id": "KACL-9",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "A repo-wide grep for `is_indexed` matches nothing outside audit-reports/ — no migration, no lib, no route. The real gatekeeper is 20260807_link_proposals.sql:159-161 `ALTER TABLE documents ADD COLUMN IF NOT EXISTS ai_excluded BOOLEAN NOT NULL DEFAULT FALSE;`, named as such at lib/aiBoundary.ts:53 `if (doc.aiExcluded) return { readable: false, reason: \"held_back\" };` and lib/schemaExpectations.ts:121. The misleading comment is real: ask/route.ts:404-408 says \"AI-excluded documents are filtered HERE, after the database already applied its LIMIT\", but the filter it introduces is :431 `.filter((c) => !excludedDocIds.has(c.document_id))`, and excludedDocIds is the per-asker ACL set built at :163-187 — `ai_excluded` appears zero times in the whole file.",
    "note": "Factually correct on every point, but it is a documentation/terminology defect with no exploit path of its own: everything it says about retrieval not consulting ai_excluded is KACL-10's first half, and the naming point corrects a mental model rather than a control. Demote to LOW (or fold into KACL-10)."
  },
  {
    "id": "LNK-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/linkProposerServer.ts:380-383 — `for (const r of priors) { if (r.status === \"pending\") continue; // refreshed below, not blocked\\n decided.add(...) }` — every non-pending status lands in one set, and lib/linkProposalLogic.ts:404-412 `filterDrafts` drops any draft whose key is in `known.decided`, with no distinction between 'dismissed' and 'stale'. 'stale' is written by publishing: lib/postPublish.ts:132-134 fires `staleProposalsForDocument(input.documentId, input.newRev)`, which at lib/linkProposals.ts:185-190 does `.update({ status: \"stale\" }).or('document_id.eq...,target_document_id.eq...').eq(\"status\",\"pending\")`. The stated contract is the opposite — admin/proposed-links/page.tsx:282-283: \"Dismissed pairs are remembered — the system won't propose them again unless a new revision brings new evidence.\"",
    "note": "Confirmed, and nothing anywhere reverses it: grep over the repo shows proposed_links.status is only ever written to 'stale' (linkProposals.ts:186, linkProposerServer.ts:463) or decided by a human, never back to 'pending'. The upsert at linkProposerServer.ts:423-425 would refresh the row, but filterDrafts removes the draft before it can reach the queue, so the publish that was supposed to re-open the pair is exactly what seals it."
  },
  {
    "id": "LNK-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "linkProposalLogic.ts:114 `const MAX_TAG_FANOUT = 40;` and :143 `if (docs.size < 2 || docs.size > MAX_TAG_FANOUT) continue;` — so a 40-document tag emits all C(40,2)=780 pairs via the :145-156 double loop, with no minimum shared-tag count (:166 `const tier = shared >= 3 ? \"strong\" : \"inferred\"`, :174 `Both reference ${tagList[0]}` for shared===1). linkProposerServer.ts:386-387 `const capped = fresh.slice(0, BATCH);` takes the first 400 in map-insertion order — no sort by tier or confidence anywhere before the write.",
    "note": "Every element checks out, including the arithmetic (35 documents → 595 pairs). filterDrafts (linkProposalLogic.ts:404-412) removes only already-linked/already-decided pairs and does no ranking, and the only ordering in the system is display-side (linkProposals.ts:75-76), applied after the unranked 400 have already been chosen."
  },
  {
    "id": "LNK-11",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "Repo-wide grep for the literal `\"semantic\"`/`'semantic'` returns exactly two hits, both type unions — linkProposalLogic.ts:20 and linkProposals.ts:11 — yet linkProposals.ts:51 advertises `semantic: \"Similar content\"` in PROPOSER_LABELS; no proposer function ever sets it. Repo-wide grep for `invalidateProposalsForRevision` returns exactly one hit, its own definition at linkProposerServer.ts:457; postPublish.ts:132-134 instead calls the browser-side twin `staleProposalsForDocument`.",
    "note": "Both absence claims verified by repo-wide search, so the finding is factually airtight — but it describes dead code and a stale label, which is LOW, not MEDIUM. Worth noting the finding actually understates one consequence it stumbled on: because postPublish routes through the client function, the UPDATE runs under the publisher's RLS and proposed_links_write (20260807:84-90) restricts writes to Admin/DocCtrl/Manager/Supervisor, so a publish by any other role silently stales nothing and returns 0 (linkProposals.ts:192)."
  },
  {
    "id": "LNK-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "linkProposerServer.ts:381 `if (r.status === \"pending\") continue; // refreshed below, not blocked` — pending rows never enter `decided`, so filterDrafts returns the identical set every pass; :386-387 `const capped = fresh.slice(0, BATCH); const { autoApply, queue } = splitByAutoApply(capped);` re-selects the same first 400; :425 re-upserts them with `ignoreDuplicates: false`; :443 `more: fresh.length > capped.length` stays true forever. The driver at proposed-links/page.tsx:107 `for (let pass = 0; pass < 12; pass++)` accumulates `proposed: (last?.proposed ?? 0) + (json.proposed ?? 0)` (:121), so the same 400 are counted 12 times.",
    "note": "Confirmed: nothing in the loop can shrink `fresh`, because the queued rows stay 'pending' and the auto-applied ones would only shrink it via `linked`, which LNK-3's 42P10 prevents in the first place. Drafts 401+ are unreachable until a human decides the first 400."
  },
  {
    "id": "LNK-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "linkProposalLogic.ts:41-43 `export function orderPair(a: string, b: string): [string, string] { return a < b ? [a, b] : [b, a]; }` — a raw UUID string compare — and approveProposal (linkProposals.ts:111-114) writes `document_id: p.document_id` unchanged. listRelatedResources filters `.eq(\"document_id\", documentId)` (relatedResources.ts:33-34) while the other endpoint only reaches listBacklinks, which selects `\"document_id, created_by_name\"` via `.eq(\"target_document_id\", documentId)` (relatedResources.ts:101-104) — no origin, no evidence.",
    "note": "The asymmetry is real and is decided by UUID ordering, so roughly half of approved links render with the provenance chip and half do not. RelatedPanel.tsx:173-183 renders the \"approved\" badge and evidence tooltip only from the listRelatedResources rows, confirming the backlink side loses exactly the evidence the feature exists to show."
  },
  {
    "id": "LNK-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/linkProposerServer.ts:28 `const BATCH = 400;` and :189-194 `.from(\"knowledge_documents\").select(\"id, source_document_id\").eq(\"org_id\", orgId).not(\"source_document_id\",\"is\",null).limit(BATCH * 2)` — 800 rows, no `.order()`. mirrorRows bounds opc_continuity (:201-230), the custom-skill text scan (:296-318) and co-citation's kdoc→source mapping (:347-348), i.e. three of the four skills; only shared_equipment reads document_assets (:236-240, capped 20000). The priors cap is :376-379 `.from(\"proposed_links\").select(\"document_id, target_document_id, status\").eq(\"org_id\", orgId).limit(20000)`, also unordered — a dismissal past that cut never enters `decided` and the pair is re-proposed. The reported figure is docRows, not mirrors: :438-439 `return { scanned: docRows.length, ...}` where docRows itself is `.limit(5000)` (:158) unordered.",
    "note": "Both named failures verified exactly as described, including the misleading `scanned` figure and the `inputs.mirroredDocs` (:197) that quietly records the 800. One over-generalization in the title: not EVERY input is unordered — the co-citation read at :333-339 does `.order(\"created_at\", { ascending: false }).limit(400)`, so its truncation is at least deterministic."
  },
  {
    "id": "LNK-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260807_link_proposals.sql:111-113 `CREATE UNIQUE INDEX IF NOT EXISTS document_related_resources_doc_target_idx ON document_related_resources (document_id, target_document_id) WHERE target_document_id IS NOT NULL;` vs linkProposerServer.ts:404-408 `.upsert(rows, { onConflict: \"document_id,target_document_id\", ignoreDuplicates: true }); if (error) notes.push(\\`Auto-apply skipped: ${error.message}\\`); else autoApplied = rows.length;`. The repo states the mechanism itself in linkRules.ts:65-68: \"the unique (org_id, builtin_key) index is PARTIAL, which ON CONFLICT can't infer through the API, so an upsert here fails wholesale\" (repeated verbatim at linkProposerServer.ts:86-88).",
    "note": "Repo-wide grep of supabase/migrations for document_related_resources unique constraints returns exactly one hit — the partial index — and the base table (20260806_intelligence_layer.sql:68-84) declares no UNIQUE, so there is no non-partial arbiter for PostgREST to infer and the DO NOTHING raises 42P10. proposeOpcContinuity:82-88 does emit tier 'provable'/confidence 1 and splitByAutoApply:420 routes it to autoApply, so the path is reachable; the only correction is that the note IS rendered to the admin (proposed-links/page.tsx:189), just alongside autoApplied:0 — 'swallowed' is slightly strong, the failure itself is real. Approval by a human uses a plain .insert with 23505 handling (linkProposals.ts:111-127) and is unaffected."
  },
  {
    "id": "LNK-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260807_link_proposals.sql:79-81 `CREATE POLICY proposed_links_read ON proposed_links FOR SELECT USING (EXISTS (SELECT 1 FROM org_members m WHERE m.org_id = proposed_links.org_id AND m.uid = auth.uid() AND m.status = 'active'));` — no document-visibility term. The only endpoint filter is client JS: linkProposals.ts:95 `return rows.filter((r) => r.doc && r.target);` and :172 `return rows.filter((r) => r.target);`.",
    "note": "The asymmetry is confirmed by 20260708_acl_rls_enforcement.sql:85-87, which puts a RESTRICTIVE `documents_acl_select ... USING (node_visible(visibility, acl_index, org_id))` on documents — so document rows ARE ACL-scoped while proposed_links is not, and the leaked evidence JSONB carries exactly the sensitive payload (drawing numbers in `Off-page connector … continues onto <ref>`, `tags` arrays from proposeSharedEquipment:173-181). listPendingPairs (:197-206) reads the same table with no filter at all."
  },
  {
    "id": "LNK-5",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "The factual core holds: linkProposerServer.ts:138-140 `const customRules = (rules ?? []).filter((r) => !r.builtin_key && r.kind === \"reference\" && r.enabled && (r.config?.patterns?.length ?? 0) > 0);` has no visibility term, and linkProposalLogic.ts:316-319 copies `rule.name` into org-readable evidence (`detail: \\`Found by the \"${rule.name}\" skill.\\``, `rule: rule.name`). The twin does filter: answerSkillsServer.ts:29-30 `rows.filter((r) => r.enabled && (r.visibility === \"org\" || (askerId !== null && r.created_by === askerId)))`.",
    "note": "The impact framing is wrong: the migration's own header (20261015_connection_skills.sql:12-14) DEFINES private as \"the author's experiment; it still runs, but its findings NEVER auto-apply and only reach the normal review queue\" — which is exactly what the code does (custom skills top out at 'strong', linkProposalLogic.ts:311), and SkillStudio.tsx:268 tells the connection-skill author \"Either way its findings only ever QUEUE for review\". So this is the declared contract, not a violated one; the residual defect is only that the shared Studio control labels it \"Just me\" with a Lock icon and that the skill's name rides into org-visible evidence."
  },
  {
    "id": "LNK-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20261015_connection_skills.sql:55-59 `CREATE POLICY link_rules_insert ... WITH CHECK (EXISTS (... m.status = 'active') AND created_by = auth.uid())` — membership only, no role term; :63-65 `link_rules_update ... USING (is_org_controller(org_id) OR created_by = auth.uid())` with no column or JSON-shape restriction, so `config` is directly PATCHable. compileSkillPatterns (linkProposalLogic.ts:254-272) rejects only empty patterns, >200 chars, and syntax errors — `(\\w+\\s?)+$` passes all three. proposeCustomReferences:292 `while ((m = re.exec(occ.text)) !== null && count < MAX_MATCHES_PER_TEXT)` bounds match COUNT, not backtracking, and runs over up to CHUNK_SCAN_CAP=2400 knowledge_chunks.",
    "note": "Confirmed end to end: createLinkRule's validation (linkRules.ts:93-94) is a client-side library call, so a direct PATCH of link_rules.config bypasses it entirely, and there is no pattern-count cap anywhere. The only gate on execution is app/api/links/propose/route.ts:37 (Admin/DocCtrl), so the Viewer plants and an Admin detonates; maxDuration=60 (route.ts:19) bounds the platform kill, not the hang."
  },
  {
    "id": "LNK-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "linkRules.ts:69-81 seeds built-ins with `created_by: userId`, and it is called unconditionally on page load for ANY member: skills/page.tsx:70-79 `useEffect(... seedBuiltinRules(activeOrgId, uid) ...)` (also ConnectionSkillsPanel.tsx:51-58). RLS then grants that member full control: 20261015_connection_skills.sql:63-65 and :68-69 both read `is_org_controller(org_id) OR created_by = auth.uid()`. The UI does hide delete for built-ins — skills/page.tsx:145 `{!opts.builtin && opts.mayManage && (` — while nothing hides it at the database.",
    "note": "The functional impact checks out: disabling opc_continuity really does stop discovery, because linkProposerServer.ts:201 gates the whole entity fetch on `builtinEnabled(\"opc_continuity\")`. One detail is worse than the finding says, not better — `mayManage = canManageOrg || mine` (skills/page.tsx:305-306) means the seeding Viewer is shown the On/Off toggle in the UI too, so no API client is even needed to disable a core detector."
  },
  {
    "id": "LNK-8",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "linkProposerServer.ts:380-383 keys the block-set on the pair alone — `if (r.status === \"pending\") continue; decided.add(\\`${r.document_id}|${r.target_document_id}\\`);` — and filterDrafts (linkProposalLogic.ts:408-411) tests `const key = \\`${d.documentId}|${d.targetDocumentId}\\`; return !known.linked.has(key) && !known.decided.has(key);`, while the index is 3-column: 20260807_link_proposals.sql:68-69 `ON proposed_links (document_id, target_document_id, proposer)`.",
    "note": "Both facts are right, but the severity assumes an accident where the code documents an intent: 20260807_link_proposals.sql:66-67 says \"Dismissals stay as rows so the engine never nags twice about the same pair\", and the proposer repeats it at :378-379 (\"a rejected pair must never come back to nag\"). It is also mostly moot in practice — mergeDrafts (linkProposalLogic.ts:388-399) already collapses every pair to a single strongest draft before filtering, so two skills can never have live proposals for one pair anyway; the third index column is vestigial rather than a violated contract."
  },
  {
    "id": "LNK-9",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "GraphShapeWizard.tsx:130-142 inserts `origin: \"user\"`, a value outside the declared set in relatedResources.ts:19-24 (`'human' typed it, 'system' applied it as provable, 'proposed' means a person approved a discovered connection`) and outside the column default `'human'` (20260807_link_proposals.sql:96). RelatedPanel.tsx:173-183 then renders `{r.origin && r.origin !== \"human\" && (... title={[r.origin === \"system\" ? \"Applied automatically — provable connection\" : \"Approved from a proposal\", ...]} ... {r.origin === \"system\" ? \"auto\" : \"approved\"}`.",
    "note": "The mislabel is exactly as claimed — 'user' falls through the `!== \"human\"` test and lands in the else branch, so it badges \"approved\" with the tooltip \"Approved from a proposal\" and no approver name. Downgrading because the impact is confined to a display chip: the row itself is honest (proposer:\"answer\", created_by/created_by_name set, evidence.rule \"Shaped from an answer\"), and a human did accept it in the wizard, so nothing fabricated an unreviewed link — it is a one-token provenance-label bug."
  },
  {
    "id": "OFF-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "public/sw.js:207-220 is the catch-all same-origin GET handler: `const res = await fetch(request); cachePut(RUNTIME_CACHE, request, res); return res; } catch (err) { const cached = await caches.match(request); if (cached) return cached; ...` and public/sw.js:116-120 `cachePut` gates only on `response.ok` — no Cache-Control, no max-age, no expiry. `/api/verify` is a plain same-origin GET (app/verify/[docId]/page.tsx:44 `const res = await fetch(\"/api/verify?${qs.toString()}\")`) and app/api/verify/route.ts:96-108 returns `NextResponse.json({... isCurrent, checkedAt ...})` with no cache headers (grep for no-store/revalidate across all four verify routes returns nothing). The SW installs on the public page because ServiceWorkerManager is mounted in the ROOT layout (app/layout.tsx:93 `<ServiceWorkerManager />`).",
    "note": "Mechanism confirmed for all four verify routes (verify, verify-hold, verify-ticket, verify-package are all uncached GETs behind the same catch-all). Two small overstatements worth recording: the error branch is not unconditionally unreachable — a first-ever offline scan gets sw.js's 503 `unavailableResponse()` and does show 'Can't verify this code'; and the page prints a stale `Checked {new Date(result.checkedAt).toLocaleString()}` (page.tsx:140-141), though at 10px in white/60 under a full-screen green CURRENT."
  },
  {
    "id": "OFF-10",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "public/sw.js:164 `} catch {` — the navigate branch alone binds no error and never calls wasAborted, unlike :195 `if (wasAborted(request, err)) throw err;` and :216 the same. So an AbortError falls to :167-171 and returns offlineHtmlResponse(), a synthetic 503 'You're offline'.",
    "note": "The inconsistency is real — two of three branches abort-check and the navigate branch does not. But the docblock half of the claim is a misreading: sw.js:22 says 'Every branch below ends in a real Response OR a rethrow', not that every branch rethrows, and hard rule 2 at :29-30 explicitly allows 'return a 503 that says what it is'. A navigation the browser itself cancelled is one it has already abandoned, so the 'fully-online user dropped onto the offline page' outcome is speculative; this is a code-hygiene defect, LOW."
  },
  {
    "id": "OFF-11",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "public/sw.js:37 `const VERSION = \"mfgos-v5\";` in a static file under public/ — grep for `sw.js` across the repo finds no build step or codegen touching it (only components/pwa/ServiceWorkerManager.tsx:33 `.register(\"/sw.js\")` and the test's readFileSync), and package.json's build is plain `next build`. So the activate sweep at :56-60 never fires on a deploy.",
    "note": "Both halves of the title are literally true, but the stated consequence — 'no device shows an update prompt' — is false: components/system/UpdatePill.tsx:21-26 polls `/api/version` every 5 minutes and on tab focus and renders 'This tab is running an old version — tap to load the update' whenever the build id diverges, and it is mounted for every signed-in page at app/(protected)/layout.tsx:61. Combined with the runtime cache being network-first (stale bytes only ever served offline), this is LOW."
  },
  {
    "id": "OFF-12",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "public/sw.js:47 `.then((cache) => cache.addAll(SHELL_ASSETS).catch(() => undefined))` — addAll is all-or-nothing and the rejection is swallowed, so one 404 among the four assets at :41 leaves SHELL_CACHE empty with no log and no state change.",
    "note": "The mechanism is right; the consequence in the title is wrong. sw.js:167-171 ends the offline navigation chain in `offlineHtmlResponse()`, a self-contained inline 503 page that needs no cache at all, so 'no offline page at all' is refuted by the very lines the finding cites; and the cache-first branch at :184-190 re-populates SHELL_CACHE with every JS/CSS/image asset on first online use, so the shell is not permanently lost either. What is actually lost is the styled /offline route and the pre-warm."
  },
  {
    "id": "OFF-13",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "public/sw.js:117 `if (!response || !response.ok || response.type === \"opaque\") return;` — the only gate; Cache-Control is never read. app/api/share/file/route.ts:145-150 returns the stamped PDF with `\"Cache-Control\": \"no-store\"`, and app/share/[token]/page.tsx:67 pulls it with `const res = await fetch(data.fileUrl)` — a same-origin GET that lands in the catch-all branch at sw.js:210-211 and is written to RUNTIME_CACHE.",
    "note": "Survives on the facts, but two of the six cited locations are unreachable: app/api/admin/shed/route.ts:204 and app/api/admin/ticket-shed/route.ts:256 are inside POST handlers (:93 and :101), and sw.js:124 `if (request.method !== \"GET\") return;` means the worker never sees them. The headline harm is also mostly pre-existing — /api/share/file is served `Content-Disposition: attachment` (:148) and the page writes it to disk via an `<a download>`, so the contractor has the PDF on the tablet either way; the genuine residual is an invisible copy that outlives share revocation. LOW."
  },
  {
    "id": "OFF-14",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/__tests__/sw.test.ts:21-22 `put: vi.fn(async () => undefined), addAll: vi.fn(async () => undefined)` — the write side is mocked and never asserted anywhere in the file; no test inspects cacheStore.put, so no test pins WHAT the worker writes. Line 64 `expect(res!.status).toBe(503); // the synthetic offline page` does lock in the bare-catch behaviour OFF-10 objects to (the fetchImpl throws a plain Error, so the abort path is never exercised).",
    "note": "The substantive gap is real — the cache-write assertions the title asks for do not exist. But 'asserts only never resolves to undefined' overstates it: :74 and :86 assert the network status and the exact cached Response identity, and :113-114 `expect(responded).toBe(false); expect(caches.match).not.toHaveBeenCalled();` is a genuine assertion about cache behaviour for RSC payloads. Test-coverage gap with no runtime consequence: LOW."
  },
  {
    "id": "OFF-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/page.tsx:72-85 `const { data: membership } = await supabase.from(\"org_members\").select(\"org_id\").eq(\"uid\", uid).eq(\"status\", \"active\").limit(1).maybeSingle(); if (membership?.org_id) { router.replace(\"/dashboard\"); } else { setAuthedEmail(userEmail); setView(\"no-workspace\"); }` — the `error` member is not destructured at all, so a network failure (postgrest-js returns `{data: null, error}` rather than throwing) is read as 'no membership'. That renders app/page.tsx:241-262: `<h1 ...>No workspace found</h1>` … 'Ask your organization's admin to add you' plus `<button onClick={handleSignOut}> Sign out`. `/` is in the SW's SHELL_ASSETS (public/sw.js:41), so the page boots offline.",
    "note": "The RoleContext locations cited are the contrast, not the bug: RoleContext.tsx:126-128 explicitly comments 'Every query THROWS on error so the retry loop below can tell \"the lookup failed\" apart from \"this account truly has no membership\"', retries 3× (169-178) and lands on `setMembershipState(\"error\")` → `MembershipErrorScreen` at app/(protected)/layout.tsx:50. The login page never got that fix, and it is the PWA's start URL."
  },
  {
    "id": "OFF-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/pwa/ServiceWorkerManager.tsx:74-79 renders 'Offline — showing cached data' purely from `navigator.onLine === false` (line 24-27) with no reference to what is actually in any Cache, and app/offline/page.tsx:14-18 promises 'Pages and data you opened recently are still available'. public/sw.js:125 `if (!isSameOrigin(request.url)) return; // never touch Supabase/R2/Stripe/fonts` is what makes both false for the protected app: the app reads its data through the cross-origin Supabase client (679 `supabase.from(` call sites across app/components/lib versus 64 same-origin `/api` fetches, most of them POST).",
    "note": "Substance holds — the pill and the offline page both assert cached data that does not exist for any Supabase-backed panel, and the pill appears regardless. One correction to the wording: 'the worker deliberately caches none of the app's data' is too absolute — public/sw.js:203-220 does cache same-origin GET API responses (that is precisely the mechanism OFF-1/OFF-5/OFF-6 rely on); it is the cross-origin data, i.e. nearly all of it, that is uncached."
  },
  {
    "id": "OFF-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "public/sw.js:43-49 calls `self.skipWaiting()` inside the INSTALL handler (`.then(() => self.skipWaiting())`), so the new worker leaves 'installed' the instant it enters it. components/pwa/ServiceWorkerManager.tsx:36-42 latches the toast on that transition — `if (worker.state === \"installed\" && navigator.serviceWorker.controller) { waitingRef.current = worker; setUpdateReady(true); }` — and by the time the user taps, `applyUpdate` (60-70) does `w.postMessage(\"SKIP_WAITING\")` (a no-op; sw.js:68-70 just calls skipWaiting again on an already-activated worker) and then `w.addEventListener(\"statechange\", () => { if (w.state === \"activated\") window.location.reload(); })` — subscribing AFTER the activated transition already fired. The `else { window.location.reload(); }` escape at line 68 is unreachable because `waitingRef.current` is non-null.",
    "note": "Confirmed: `reg.waiting` (line 45) is also normally null under install-time skipWaiting, so the statechange path is the only one that arms the toast, and it arms it with a worker that is about to activate on its own. The only way the tap works is the sub-millisecond race where the user taps while the worker is still in 'activating'."
  },
  {
    "id": "OFF-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Both share endpoints are same-origin GETs that fall under public/sw.js:207-220's network-first-then-cache handler: app/api/share/resolve/route.ts:20 `export async function GET(req)` (revocation check at 38-41 `if (share.revoked_at) return NextResponse.json({ error: \"revoked\" }, { status: 410 });`) and app/api/share/file/route.ts:31 `export async function GET(req)` (same check at 49-51). Because public/sw.js:117 caches only `response.ok` responses, the 410 is never cached but the earlier 200s are — so offline `caches.match(request)` returns last week's successful resolve, app/share/[token]/page.tsx:42-55 takes `setState(\"ok\")`, and app/share/[token]/page.tsx:67 `const res = await fetch(data.fileUrl)` (fileUrl is the same-origin `/api/share/file?token=...`, resolve/route.ts:79-81) is served the cached PDF body.",
    "note": "Confirmed, and stronger than stated: app/api/share/file/route.ts:149 sets `\"Cache-Control\": \"no-store\"`, but the Cache Storage API ignores it — sw.js's cachePut stores the response anyway. The revocation check is not merely bypassed, it is bypassed by the exact response the server marked as never-store."
  },
  {
    "id": "OFF-6",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "The caching claim is literally true: app/api/storage/download-url/route.ts:10-20 gates entirely on `const authHeader = req.headers.get(\"authorization\")` / `supabaseAdmin.auth.getUser(token)`, returns `NextResponse.json({ url })` at line 153 with no Vary and no Cache-Control, and public/sw.js:211 `cachePut(RUNTIME_CACHE, request, res)` + 214-215 `const cached = await caches.match(request); if (cached) return cached;` key on URL only. BUT the cache is consulted ONLY inside `catch (err)` — public/sw.js:210 `const res = await fetch(request);` returns a live 403 unchanged when online, so the denied Viewer in the scenario never reaches the cached entry while the origin is reachable; and when the origin is NOT reachable the presigned R2 URL is unusable, since sw.js:125 `if (!isSameOrigin(request.url)) return;` means R2 is never cached either. The URL is also issued with `expiresIn=3600` (line 151, and components/viewers/SecureDocViewer.tsx:107 requests exactly that), so a cached entry is dead R2-side after an hour.",
    "note": "Not refuted — the missing Vary and the shared cache key are real, and the sign-out at RoleContext.tsx:270-280 does not clear Cache Storage. But the exploit requires the app origin to be unreachable while R2 is reachable, within 60 minutes of the first user's fetch; that narrow window puts this at LOW, not MEDIUM."
  },
  {
    "id": "OFF-7",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "public/sw.js:213-215 `} catch (err) { const cached = await caches.match(request); if (cached) return cached;` — the cache is consulted ONLY after `await fetch(request)` throws (:210), so a cache hit is by construction an offline replay, and app/api/share/file/route.ts:130-140 `await sb.from(\"download_audits\").insert({...})` never runs for it. Same for app/api/share/resolve/route.ts:83 `await sb.rpc(\"bump_share_access\", ...)` and app/api/transmittal/route.ts:75-80 `action: \"TRANSMITTAL_PORTAL_DOWNLOAD\"`.",
    "note": "True as far as it goes — nothing in sw.js reports a cache hit back to the server — but the branch is network-first, so a replay is only possible on a device that already performed an ONLINE fetch of the identical URL, which did write the audit row; the trail can under-count, never show zero. The transmittal leg is weaker still: /api/transmittal?file= returns only `{url}` from a 300-second presigned R2 link (:71-74), so a cached copy hands back a dead URL and no download actually occurs offline. Impact is a bounded undercount, not a missing record."
  },
  {
    "id": "OFF-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/providers/RoleContext.tsx:271-279 is the whole sign-out cleanup: `if (k && (k.startsWith(\"intel-status-\") || k.startsWith(\"schema-gaps-\"))) doomed.push(k); ... window.location.replace(\"/\")` — no caches.* call. Repo-wide grep for `caches.` over app/ components/ lib/ public/ returns only public/sw.js:60 (`caches.delete(k)` inside the VERSION sweep), :119, :168-170, :186, :214 — so nothing clears Cache Storage at sign-out, and the version sweep only fires when VERSION changes.",
    "note": "Confirmed: RUNTIME_CACHE is device-wide, survives sign-out, and caches.match(request) keys on URL, so offline any later account is served the previous account's same-origin API bytes. The OAuth half is also real — app/page.tsx:92 `redirectTo: ${window.location.origin}/` makes the code-bearing `/?code=…` a navigate request cached at sw.js:161-162 — though a single-use, already-redeemed code is the least of it; the cross-account replay is what carries the MEDIUM."
  },
  {
    "id": "OFF-9",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "public/sw.js:116-120 `function cachePut(cacheName, request, response) { if (!response || !response.ok || response.type === \"opaque\") return; ... c.put(request, copy)` — no entry count, no byte budget, no timestamp, no trim. The only deletion in the file is the VERSION sweep at :59-60 `.filter((k) => !k.startsWith(VERSION)).map((k) => caches.delete(k))`. app/api/share/file/route.ts:145-149 returns `Buffer.from(outBytes)` with `Content-Type: application/pdf`, reaching cachePut via sw.js:210-211.",
    "note": "The mechanical claim is correct and repo-wide search confirms no eviction anywhere. The stated consequence is the speculative part: it needs the origin to reach hundreds of MB and the browser to run a whole-origin eviction, and the outcome is a forced re-login (lib/supabase.ts:76-77 puts the session in localStorage), not data loss or a wrong document. LOW."
  },
  {
    "id": "ORCH-1",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "HIGH",
    "evidence": "`CONTROLLER_ROLES` (tools.ts:69) is referenced in a gate exactly once for a write path — tools.ts:460-462 in checkout_document. notify_personnel (tools.ts:474-518) and log_audit_completion (tools.ts:520-556) contain no role test; the latter goes straight to tools.ts:544 `const { error } = await supabaseAdmin.from(\"drawing_audit_logs\").upsert({...}, { onConflict: \"org_id,sheet_number,revision_code\" });`. The route's only authority check is membership: execute/route.ts:41-46 `.eq(\"status\",\"active\").maybeSingle(); if (!member) return bad(...); const role = (member.role as string) ?? \"Viewer\";` — `role` is then only passed through at :58 and never tested.",
    "note": "The bypass is confirmed by its own sibling: the human path for the same write, app/api/knowledge/drawing/route.ts:349-351, refuses with `Only Admin or Doc Control can rebuild the index or record an audit.`, and drawing_audit_logs has a read policy but no write policy at all (20260929_mention_engine.sql:155-159), so service role is the only writer. Correcting CRITICAL→HIGH: the caller must still be an authenticated active member of that org, the blast radius is falsified audit-completion rows and misattributed notifications (no cross-tenant read, no destruction), and every execution is recorded with the actor at execute/route.ts:79-84."
  },
  {
    "id": "ORCH-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/orchestrator/route.ts:55-59 builds `approved` from `body.approved` and :132 passes it as ctx; tools.ts:418 `if (!href && ctx.approved.has(fp)) return null;` lets the handler fall through to the real write (notify emit at 506-515, upsert at 544-548). Only the execute route writes an audit row (execute/route.ts:79-84 `action: \"AI_ACTION_EXECUTED\"`) — there is no equivalent in app/api/orchestrator/route.ts. app/(protected)/assistant/page.tsx:95 `void run(id, trimmed, []);` is the only call site of run(), and it always passes an empty array.",
    "note": "Confirmed on both halves. `x.approved` (page.tsx:117) is accumulated purely to render the 'done' badge at page.tsx:243 and is never fed back into askOrchestrator, so the in-run approval path is dead to the UI while remaining fully live to any caller of the API — and unlike the execute route it leaves no audit_logs row. fingerprint() is exported and pure (tools.ts:73-77), so deriving the string is trivial."
  },
  {
    "id": "ORCH-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/orchestrator/tools.ts:537 `const params = { sheet_number: args.sheet_number, revision: args.revision, status };` — `details` is absent, yet it is a declared param at :530 and is written at :547 `audit_details: { note: args.details ?? \"\", by: ctx.userId }`. tools.ts:425 stores those same `params` on the PendingAction, orchestratorClient.ts:94 posts `parameters: action.parameters`, and execute/route.ts:64 calls `def.run(checked.values, ctx)` with exactly that set.",
    "note": "Confirmed: the only path the UI can take (page.tsx:107 → executeAction) round-trips the proposal's parameters, which never contained `details`, so `args.details` is undefined at :547 and every confirmed record stores `note: \"\"`. The in-run `approved` path would preserve it, but ORCH-10 establishes that path is unreachable from the product."
  },
  {
    "id": "ORCH-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20261016_reasoning_skills.sql:44-48 `CREATE POLICY answer_skills_insert ON answer_skills FOR INSERT WITH CHECK (EXISTS (... m.status = 'active') AND created_by = auth.uid());` — membership only. answerSkills.ts:82-84 is the whole validation (`if (instructions.length < 40) throw`). answerSkillsServer.ts:29-30 admits every `r.visibility === \"org\"` row regardless of author, orchestrator/route.ts:117-120 folds it into `playbook`, and loop.ts:101 injects it into the SYSTEM prompt: `playbook ? \\`\\nSITE INSTRUCTIONS\\n${playbook.trim()}\\` : \"\"`. SkillStudio.tsx:48 defaults `useState<LinkRuleVisibility>(\"org\")`.",
    "note": "Every link in the chain verified, and there is no role gate anywhere upstream — /api/orchestrator checks membership only (route.ts:62-67) and the Skills page's author affordance is shown to anyone with an org and a uid (skills/page.tsx:176). The 9000-char BLOCK_BUDGET_CHARS bounds the size, not the authority; only a controller or the author can remove the row afterwards."
  },
  {
    "id": "ORCH-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "tools.ts:21 `import { supabaseAdmin } from \"@/lib/supabaseAdmin\";` is the sole client for every read handler (find_documents :91, search_documents :132, equipment_mentions, trace_pid_lines), and check_permissions is the clearest case — tools.ts:232-249 comments \"RLS is the real gate; this reports what the caller can already see\" and then queries `supabaseAdmin.from(\"documents\")... .eq(\"org_id\", ctx.orgId).maybeSingle()`, returning `readable: true` for any org document. The sibling enforces the opposite: ask/route.ts:176-181 `const principal = await loadPrincipal(orgId, user.id); ... const readable = await readableControlledDocIds(principal, dcIds);` and fails closed in the catch at :185.",
    "note": "The claimed ACL exists and is real (20260708_acl_rls_enforcement.sql:85-87, RESTRICTIVE `node_visible(...)` on documents; knowledgeAccess.ts:189-215 evaluates library→folder→document chains), and the orchestrator honours only `ai_excluded`, never visibility/acl_index. No role gate anywhere on /api/orchestrator (route.ts:62-67 checks membership only); the caller needing their own BYO key is a billing constraint, not an authority one."
  },
  {
    "id": "ORCH-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/orchestrator/execute/route.ts:57-60 — `const ctx: ToolContext = { orgId, userId: user.id, role, approved: new Set([fingerprint(def.name, checked.values)]) };` The approval is minted from the body the client just sent. The only prior checks are auth (28-30) and `.from(\"org_members\")... .eq(\"status\", \"active\")` (41-45). `grep -rn \"proposal\" supabase/` returns nothing for orchestrator — no table stores a pending action.",
    "note": "Confirmed: there is no server-side record of what was proposed, no nonce, no expiry and no one-shot consumption, so the endpoint accepts any {tool, parameters} an active member cares to send, forever. tools.ts:425 puts `parameters` in the PendingAction the browser holds, and orchestratorClient.ts:94 posts them straight back — the client is the only 'store'. Adversarial check for a missed guard: the two executable write tools (notify_personnel tools.ts:474-518, log_audit_completion tools.ts:520-552) contain no role or origin check, so nothing downstream re-binds the call to a proposal either."
  },
  {
    "id": "ORCH-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/ai/usageServer.ts:57-64 — getMonthUsage filters `.eq(\"op\", \"knowledgeAsk\")`; app/api/orchestrator/route.ts:146-149 writes `op: \"orchestrator\"`. The cap gate at route.ts:105-115 reads that same getMonthUsage.",
    "note": "Confirmed, and broader than claimed: every non-ask op is invisible to the cap — codebookImport, flowRead, templateDraft, drawingLocate, knowledgeVision, knowledgeEmbed, graphShape, checklistAssess, skillAssist. usageServer.ts:109-110 even documents the opposite ('vision indexing bills as knowledgeVision so the spend ... shares the same cap'), which the op filter makes false. getMonthUsageByUser (70-76) carries the identical filter, so the Admin team view undercounts too. Only mitigation worth noting: spend lands on the member's own BYO key (route.ts:69-85), so the loss is the member's, not the org's."
  },
  {
    "id": "ORCH-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/orchestrator/tools.ts:93 `const { data } = await supabaseAdmin` (no `error` destructured) then :102 `.or(\\`document_number.ilike.%${q}%,title.ilike.%${q}%\\`)`; identically tools.ts:176/180 for assets. On a PostgREST 400 `data` is null and :108 `(data ?? []).map(...)` yields `matches: []` with no error key.",
    "note": "Confirmed on both counts — the model's string is interpolated into the or() filter list unescaped (a comma splits the filter list, a paren closes the group early) and the query error is discarded rather than surfaced. loop.ts:92-93 then instructs the model that an empty result means 'I have no documents mentioning that', so a malformed-filter 400 is reported to the user as an authoritative absence. Note the injection itself is contained: `.eq(\"org_id\")`, `.eq(\"ai_excluded\", false)` and `.neq(\"status\",\"Archived\")` are separate AND'd filters that an injected or()-branch cannot widen — the harm is the false negative, as the title says."
  },
  {
    "id": "ORCH-7",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "app/api/orchestrator/route.ts:105-115 reads `getMonthUsage`/`getCapUsd` and returns 402 only if `monthSoFar.spentUsd >= capUsd`; the ledger row is written at :146-149, after `runOrchestrator` returns. No reservation row, no advisory lock, no `for update` anywhere in lib/ai/usageServer.ts.",
    "note": "The mechanism is real — a plain read-then-spend with no reservation, so N concurrent runs all clear the same pre-flight. But the severity is too high for two reasons the finding did not weigh: the spend is on the member's own BYO key (route.ts:69-85), so a user racing the check overspends their own account; and for this route specifically the race is moot because ORCH-5 shows op='orchestrator' is never counted by getMonthUsage at all, so there is no cap state to race. LOW."
  },
  {
    "id": "ORCH-8",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/orchestrator/tools.ts:69 `const CONTROLLER_ROLES = [\"Admin\", \"DocCtrl\", \"Manager\", \"Supervisor\"];` vs lib/documentGuards.ts:61 `const CONTROLLER_ROLES = new Set([\"Admin\", \"DocCtrl\"]);` and lib/permissions.ts:18-20 `return role === \"Admin\" || role === \"DocCtrl\";`, matching the DB's `role IN ('Admin', 'DocCtrl')` in 20260814_documents_delete_controllers.sql:38.",
    "note": "The divergence is real and correctly cited. Severity is too high: check_permissions is explicitly advisory ('RLS is the real gate; this reports what the caller can already see', tools.ts:233-234) and grants nothing, and checkout_document always returns an href proposal (tools.ts:465-470) that never writes server-side — the real checkout runs through CheckoutFlowModal under the user's own session. The finding's scenario is also not established: permissions.ts:22-42 default-allows edit when no ACL decision exists (`if (!decision) return defaultAllow` with defaultAllow=true), so a Supervisor is not automatically refused; and the 4-role set the orchestrator uses is itself used verbatim elsewhere in the product (app/api/equipment-bridge/route.ts:26, app/api/graph/mentions/route.ts:46, 20260929_mention_engine.sql:83). The real defect is a possibly-misleading advisory answer in both directions (a Drafter with an ACL edit grant is told editable:false), not authority the product does not grant."
  },
  {
    "id": "ORCH-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/orchestrator/loop.ts:113-114 — `lines.push(\\`\\n> ${s.tool}(${JSON.stringify(s.parameters)})\\`); lines.push(s.error ? ... : clip(JSON.stringify(s.result)))` — the tool payload is concatenated into the user turn with no delimiter, no escaping and no 'this is data, not instructions' framing. The payload includes `evidence: r.context_snippet` (tools.ts:221), written from raw page text at lib/mentionIndexer.ts:114 `context_snippet: s.snippet`, and `text: String(r.snippet ?? \"\")` (tools.ts:163).",
    "note": "Confirmed: any member who can get a PDF indexed controls bytes that are later pasted verbatim into the prompt of an agent that proposes writes, and nothing in systemPrompt() (loop.ts:71-103) tells the model that tool output is untrusted. Mitigation the finding omits, which keeps it at MEDIUM rather than higher: an injected write call still lands as a PendingAction card (tools.ts:419-426) and only executes after a human posts to /api/orchestrator/execute, so the reachable harm is fabricated read answers plus a plausible-looking confirm card."
  },
  {
    "id": "ORG-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "apply/route.ts:75 `const importable = plan.counts.tables.filter((t) => t.willImport && t.rows > 0).map((t) => t.name);` where dataRestore.ts:146-149 builds that list from `for (const [name, rows] of Object.entries(env.tables)) { const skip = SKIP_TABLES[name]; tables.push({ name, rows: n, willImport: !skip, ... }) }` — the table name is whatever the CALLER put in the envelope; only the 6 SKIP_TABLES are refused. remapRow (dataRestore.ts:211) rewrites org_id ONLY when `idRemap.orgId[v]` matches, and idRemap.orgId is `{ [env.manifest.orgId]: targetOrgId }` (dataRestore.ts:136), so a row whose org_id is a VICTIM org passes through untouched. apply/route.ts:103 then writes it with `sb` = `actor.admin`, the service-role client (serverAuth.ts:19,48).",
    "note": "Confirmed, and the contrast with the sibling route is exact: apply-table/route.ts:24,38-40 gates on `IMPORTABLE` (ORG_SCOPED_TABLES + USER_SCOPED_FOR_ORG_TABLES) and :56 forces `if (\"org_id\" in m) m.org_id = orgId;` — apply/route.ts has neither line. Any Admin of any self-signup trial org can insert rows into arbitrary tables under an arbitrary org_id with RLS bypassed."
  },
  {
    "id": "ORG-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "ViewAsSimulator.tsx:59-61 `const { data } = await supabase.from(\"team_members\").select(\"team_id\").eq(\"user_id\", pick); if (alive) setTeamIds((((data ?? []) as Array<{ team_id: string }>)).map((r) => r.team_id));` — the column is `uid`, not `user_id`: 20260707_teams.sql:19-25 defines team_members(team_id, uid, org_id, added_at, added_by) and a grep of every migration for ALTER/ADD COLUMN on team_members returns nothing but the ENABLE ROW LEVEL SECURITY line, so `user_id` has never existed. The error field is destructured away, so PostgREST's 42703 is swallowed and data is null → teamIds = []. The rest of the app uses the right column (lib/teams.ts:122 `.select(\"team_id\").eq(\"uid\", uid)`), which is what makes the simulator's answer differ from real access.",
    "note": "Confirmed by repo-wide search for the column. The simulator silently reports 'no team grants' for every user, which is worst-case in the one screen built to certify access."
  },
  {
    "id": "ORG-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "OrgBrandingProvider.tsx:35-36 `setBranding(b); applyOrgPalette(b?.palette ?? null);` — no validation, straight from lib/orgBranding.ts:24-30's untyped `data.data as OrgBranding`. ThemeProvider.tsx:143-144 `const [orgPalette, setOrgPalette] = useState<Palette | null>(null); const effective = orgPalette ?? palette;` and :161 `useEffect(() => { if (hydrated) applyTheme(mode, effective); }, [hydrated, mode, effective]);` — unguarded. Every other entry point validates against the same helper at :77 `const isHex = (s: string) => /^#[0-9a-fA-F]{6}$/.test(s);` — initialPalette :117 `if (o && isHex(o.primary) && isHex(o.secondary))`, :121 `if (legacy && isHex(legacy))`, and the pre-paint script :205 `/^#[0-9a-fA-F]{6}$/.test(o.primary)`.",
    "note": "Confirmed, and slightly worse than stated: a non-string palette value makes hexToRgb's `hex.replace(\"#\",\"\")` (:59) throw inside an unguarded useEffect, which takes down the whole provider tree for every member, not just the color tokens. Write access to the branding row is Admin-only (20260713 RESTRICTIVE policies), so the realistic vectors are a hostile/careless Admin or the ORG-1 restore path — MEDIUM stands."
  },
  {
    "id": "ORG-12",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "Repo-wide grep for `prune_signup_attempts` returns two hits only: its definition at 20261010_signup_rate_limit.sql:37 and the audit report itself. Nothing in app/, lib/, vercel.json, or any migration calls it, and there is no pg_cron schedule anywhere (grep for cron.schedule/pg_cron in supabase/ is empty). The migration's own comment at :35-36 says only that \"the maintenance cron CAN call this\" — it never did.",
    "note": "The no-caller claim is true. The severity is not: the row is tiny, signup_attempts is service-role-only with no policies, the hot query is index-covered (signup_attempts_ip_time_idx), and nothing breaks — this is unbounded growth of a housekeeping table. The secondary claim 'no cron slot is available' is also overstated: /api/cron/maintenance already exists as a daily slot and the RPC could be added inside it without a new vercel.json entry."
  },
  {
    "id": "ORG-13",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "20260707_teams.sql:19-25 — team_members has `team_id REFERENCES teams(id)`, `uid REFERENCES users(id)` and `org_id REFERENCES orgs(id)` as three INDEPENDENT FKs with `PRIMARY KEY (team_id, uid)`; a grep of every migration for ALTER/FOREIGN/UNIQUE on team_members returns nothing, so no constraint ever ties org_id to teams.org_id. The reads are unscoped: 20260707:52-55 `my_team_ids() … SELECT team_id FROM team_members WHERE uid = auth.uid();` and 20260708_acl_rls_enforcement.sql:75-76 `SELECT array_agg(team_id::text) INTO v_teams FROM team_members WHERE uid = auth.uid();`. lib/teams.ts:122 getMyTeamIds is unscoped too.",
    "note": "Factually correct on every point, and the finding is honest that it is SUSPECTED. Exploitation is blocked in both directions: team_members INSERT is gated by team_members_admin_write (20260707:50-54, USING doubles as the INSERT check) to orgs where you are already Admin/Manager, and node_visible only runs inside policies that already require membership of the row's org — so a foreign team_id buys nothing without membership you already have. Latent schema-integrity debt: LOW."
  },
  {
    "id": "ORG-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "schema.sql:1013 `ALTER TABLE org_members ENABLE ROW LEVEL SECURITY;`. schema.sql:1050 created the only FOR ALL policy, and 20260817_org_members_escalation_and_config.sql:44-46 removes it: `DROP POLICY IF EXISTS org_members_write ON org_members; CREATE POLICY org_members_write ON org_members FOR INSERT WITH CHECK (...)`. A repo-wide grep of every .sql file for policies on org_members returns exactly four — org_members_read (SELECT, schema.sql:1048), org_members_write (INSERT), org_members_update (UPDATE, 20260817:32) and their REMEDIATION_APPLY_ALL duplicates. No DELETE policy exists anywhere. admin/users/page.tsx:178 `const { error } = await supabase.from('org_members').delete().eq('id', member.id); if (error) throw error;` runs on the user-JWT client, so RLS filters the row out, zero rows match, no error is returned, and :180 optimistically drops the row from the UI.",
    "note": "Verified as a claim of absence by repo-wide search. Corroborating intent: 20260831_capability_policy_and_rails.sql:73-76 installs a BEFORE DELETE trigger on org_members, i.e. deletion is expected to work, but nothing grants it. There is no service-role removal route either (app/api/admin/ has create-user but no remove/delete-user)."
  },
  {
    "id": "ORG-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Repo-wide grep for `access_requests` outside the audit report yields only: the CREATE TABLE in 20260819_orphan_tables_backfill.sql:16, request-access/route.ts:31 (its own duplicate-check) and :46 (the insert), plus lib/exportTables.ts:91 / lib/dataRestore.ts:315 (backup manifests). No admin page, API route, notification, or email reads it. The SELECT policy has no org correlation at all: 20260819:29-30 `CREATE POLICY access_requests_admin_select ON access_requests FOR SELECT TO authenticated USING (EXISTS (SELECT 1 FROM org_members WHERE uid = auth.uid() AND status = 'active' AND role = 'Admin'))` — the EXISTS is not tied to the row's org. :27 `CREATE POLICY access_requests_anyone_insert ON access_requests FOR INSERT WITH CHECK (true)`. signup/page.tsx:93-98 `await fetch('/api/auth/request-access', {...}); setRequestSent(true);` — the response is never inspected, and :236 renders \"Request Sent\".",
    "note": "All four sub-claims hold. Note an extra defect the finding did not name: the migration's table has no org_id column while the route inserts `org_id` (route.ts:47), so on a from-migrations rebuild the insert 500s — and the client still shows \"Request Sent\" because res.ok is never checked."
  },
  {
    "id": "ORG-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "admin/settings/page.tsx:25 `const ADMIN_ROLES = new Set([\"Admin\", \"DocCtrl\"]);` and :39 `const canRead = !!activeRole && ADMIN_ROLES.has(activeRole);`. saveNumbering (:103-112) writes with the user client: `const { error } = await supabase.from(\"orgs\").update({ ticket_prefix..., ticket_record_code..., ticket_number_pad... }).eq(\"id\", activeOrgId); if (error) throw error; setNumSaved(true);`. schema.sql:1042-1045 `CREATE POLICY \"orgs_admin_write\" ON orgs FOR UPDATE USING (id IN (SELECT org_id FROM org_members WHERE uid = auth.uid() AND status = 'active' AND role = 'Admin'))` — grep for policies on `orgs` across schema.sql and every migration returns only this one plus the SELECT policy, so nothing later widens it to DocCtrl (and it reads `role`, not the additive `roles[]`).",
    "note": "Confirmed: a DocCtrl's UPDATE matches zero rows, PostgREST returns no error, and the code treats no-error as success. MEDIUM is appropriate — silent data loss on the numbering scheme, no security boundary crossed."
  },
  {
    "id": "ORG-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "storage/upload-url/route.ts:33-43 is the whole authorization for an org-prefixed key: `const orgMatch = path.match(/^orgs\\/([0-9a-fA-F-]{36})\\//); if (orgMatch) { const { data: member } = await supabaseAdmin.from(\"org_members\").select(\"uid\").eq(\"org_id\", orgMatch[1]).eq(\"uid\", user.id).eq(\"status\", \"active\").maybeSingle(); if (!member) return 403 }` — membership only, no role test, and assertSafeStorageKey (lib/storageKey.ts:40-52) checks traversal/control bytes only. The DB-side control is on the config row, not the key: 20260713_branding_admin_writes.sql:20-28 restricts INSERT/UPDATE of key='branding' to is_org_admin, and its header states \"SELECT is intentionally left open so every member can read the branding\", so any member can read data.logoPath via lib/orgBranding.ts:24-30. Sidebar.tsx:373 renders `<img src={logoUrl} ...>` from that same key.",
    "note": "Confirmed: the Admin-only RLS protects the pointer, not the bytes it points at. Any active member (Viewer/Contractor included) can presign a PUT for the exact logo key and replace what every member sees."
  },
  {
    "id": "ORG-6",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "Factually exact. schema.sql:1031-1032 `CREATE OR REPLACE FUNCTION my_org_ids() RETURNS SETOF UUID LANGUAGE SQL SECURITY DEFINER AS $$` (no SET); 20260707_teams.sql:52-53 my_team_ids — same; 20260713:11-12 is_org_admin — same; 20260814:31-32 is_org_controller — same; 20260817:21-22 is_org_admin_or_manager — same; 20260708:42-43 node_visible — same. The later guards do set it: 20260831:44 `RETURNS TRIGGER LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$` (prevent_last_admin_removal) and :81 (enforce_checkout_release_guard). Across supabase/*.sql, 61 SECURITY DEFINER declarations, only 22 with SET search_path.",
    "note": "The absence is real and the inconsistency with newer code is real, but the exploit path is not reachable in this codebase: a repo-wide grep finds no GRANT of CREATE on any schema and no CREATE SCHEMA, PostgREST fixes the connection search_path, and Supabase's anon/authenticated roles cannot create a schema that precedes public. This is search_path hardening debt (defense-in-depth), not a live privilege-escalation path — LOW."
  },
  {
    "id": "ORG-7",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "The cleanup gap is real: admin/users/page.tsx:178 issues a bare `supabase.from('org_members').delete().eq('id', member.id)` and nothing else; 20260707_teams.sql:19-25 ties team_members to `uid UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE` — the auth user, not the org_members row — so removing membership cascades nothing; accessRecert.ts:69-72 resolves grant names from org_members (`nameMap.get(r.subject.id) || r.subject.id`), degrading to a raw UUID once the row is gone; ownership.ts:52-55 does the same lookup for the effective owner. BUT the triggering event cannot occur: as established in ORG-2, org_members has no DELETE policy (20260817:44-46 replaced the FOR ALL policy with FOR INSERT), and no service-role route deletes org_members (grep of app/api + lib for org_members + delete returns only storage/delete/route.ts:31, a SELECT).",
    "note": "Right about the missing prune/reassign/snapshot logic, but the finding's own scenario is unreachable today — the DELETE silently no-ops, so nothing is actually orphaned. It is latent debt that only bites once ORG-2 is fixed, so LOW rather than MEDIUM."
  },
  {
    "id": "ORG-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "request-access/route.ts is 64 lines with no limiter of any kind — it goes straight from `const { displayName, email, orgName } = await req.json();` (:6) to the `.ilike(\"name\", orgName.trim())` lookup (:16) — while signup/route.ts:19-28 defines signupRateLimited() and :39-43 returns 429. The oracle is explicit: request-access returns 404 `No organization named \"...\" was found` (:19-24) vs 200 `{ ok: true, orgName: orgRealName }` (:59) echoing the exact registered name, and signup returns 409 `An organization named \"<existing name>\" already exists` (:69-74). There is no middleware.ts anywhere in the repo, so nothing throttles it at the edge.",
    "note": "Confirmed by repo-wide search for a middleware/edge limiter (none exists). The asymmetry is exactly as described: the endpoint that was hardened is the one that already leaks less."
  },
  {
    "id": "ORG-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Layer 1: SubscriptionGate.tsx:46-48 `const ENFORCE = false;` … `if (!ENFORCE || loading || hasAccess(info)) return <>{children}</>;`. Layer 2: repo-wide grep for `assertOrgHasAccess` returns exactly one hit — its own definition at serverAuth.ts:78. Layer 3: grep for `org_has_active_subscription` returns exactly one hit — its definition at 20260713_document_publish_guard.sql:96 — and its own header says \"(NOT wired to any blocking policy yet)\". The self-grant: schema.sql:1042-1045 is a bare `FOR UPDATE USING (…role = 'Admin')` with no WITH CHECK and no column list, and grep for triggers/policies on orgs finds nothing else, so an Admin can PATCH subscription_status or stripe_customer_id directly; stripe/webhook/route.ts:138-145 resolves the target org purely by `.from(\"orgs\").select(\"id\").eq(\"stripe_customer_id\", customerId).maybeSingle()`.",
    "note": "All five sub-claims verified, including both claims of absence. MEDIUM is fair: enforcement being off is a deliberate, commented choice, so the self-grant has no effect today, but the customer-id column being tenant-writable makes another tenant's webhook resolution attacker-influenced."
  },
  {
    "id": "OS-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260621_in_app_notifications.sql:53-60 `CREATE POLICY notifications_org_insert ON notifications FOR INSERT WITH CHECK (EXISTS (SELECT 1 FROM org_members WHERE org_members.org_id = notifications.org_id AND org_members.uid = auth.uid() AND org_members.status = 'active'))` — re-created identically at 20260723_notifications_unify.sql:44-45, so no later migration tightens it. The table declares `kind TEXT NOT NULL` with no CHECK constraint (schema.sql:668). lib/inAppNotifications.ts:79 `supabase.from(\"notifications\").insert({...})` runs on the browser anon client (lib/supabase.ts:4 `const anon = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!`), invoked client-side from components/documents/EditOverlapBanner.tsx:84 and components/documents/CheckoutFlowModal.tsx:403. Repo-wide grep for `rate.?limit|throttle` finds signup_attempts and AI-embed limiters only — nothing on the notifications insert path.",
    "note": "Claim holds in full: any active member can insert unbounded rows for any co-member with attacker-chosen kind/title/body. HIGH is defensible rather than inflated because `link` is also attacker-chosen and is rendered as a live `<Link href={item.link}>` in NotificationBell.tsx:170, making this an internal phishing primitive, not just spam."
  },
  {
    "id": "OS-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/schema.sql:659 `push_enabled BOOLEAN NOT NULL DEFAULT TRUE,` and supabase/migrations/20260723_notifications_unify.sql:87 `ADD COLUMN IF NOT EXISTS push_enabled BOOLEAN NOT NULL DEFAULT TRUE;` (preceded at :84 by the comment 'Default TRUE so existing users see no behavior change'). Repo-wide grep for `push_enabled|pushEnabled` across all .ts/.tsx returns ZERO matches. app/(protected)/settings/notifications/page.tsx:24-40 defines `interface Prefs` and `const DEFAULTS: Prefs` with only email_enabled, the five email_on_* toggles and digest_frequency — no push field, so no control is rendered.",
    "note": "Confirmed exactly: column exists, defaults TRUE, has zero readers in application code, and the settings UI has no field for it. The silent-org-wide-opt-in consequence follows directly from DEFAULT TRUE plus the absent control."
  },
  {
    "id": "OS-11",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/schemaExpectations.ts:99 `{ table: \"push_subscriptions\", migration: \"20260804_push_subscriptions.sql\" },` — an unconditional entry in the expected-tables list. lib/exportTables.ts:167 `export const USER_SCOPED_FOR_ORG_TABLES = [\"notification_preferences\", \"push_subscriptions\"] as const;` and lib/dataRestore.ts:92 `push_subscriptions: \"device push registrations are machine-specific — re-established per device\",` in SKIP_TABLES both also treat the dead table as live.",
    "note": "Factually correct — the health check does demand the table of a feature with no subscriber and no sender, and export/restore carry matching dead entries. Downgraded to LOW: the consequence is a misleading admin readout and one no-op migration, with no data loss, no security exposure and no user-facing breakage."
  },
  {
    "id": "OS-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "hooks/useTicketNotifications.ts:71-95 `sectionForKind` returns `'other'` from its `default:` arm (:99-100); :246 `const sectionCounts = emptySectionCounts();` and :284 `const section = sectionForKind(n.kind);` … :302 `tally(section, false);` populate it. The complete set of sectionCounts consumers in the repo is Sidebar.tsx:229 (`.documents`), :231 (`.projects`), :235 (`.requests`) — `.other` and `.scratchpad` are read by nothing. Programmatic count over the 48-member NotificationKind union: 26 fall to 'other', plus 3 more ('task_nudge', 'task_overdue_digest', 'morning_digest') routed to the equally-unrendered 'scratchpad'.",
    "note": "Confirmed, and if anything understated — 29 of 48 kinds badge no sidebar item, not 'roughly thirty' by coincidence but because scratchpad is unrendered too. The named compliance kinds (legal_hold_placed, retention_eligible, access_recert_due) all land in 'other' as claimed."
  },
  {
    "id": "OS-2",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "public/sw.js:226-238 (push receiver) and :241-260 (notificationclick with client.navigate/openWindow) are complete; supabase/migrations/20260804_push_subscriptions.sql:7-35 creates the table plus three own-row RLS policies; supabase/schema.sql:659 `push_enabled BOOLEAN NOT NULL DEFAULT TRUE`. No subscriber or sender exists anywhere (see DELIV-14 evidence).",
    "note": "The factual core is verified. Two corrections: (a) 'every subscriber and sender was deleted' is unverifiable — the repo is a single squashed commit (`git log -- public/sw.js` returns one commit, abdee9b), so the evidence supports 'never present in this tree', not 'deleted'; (b) this is the same defect as DELIV-14, which the same audit set rates MEDIUM — a wholly-absent feature with a misleading skeleton is a discoverability/wasted-work cost, not a HIGH-severity failure, so MEDIUM is the consistent grade."
  },
  {
    "id": "OS-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Repo-wide grep for `Notification\\.requestPermission|requestPermission|Notification\\.permission` across all .ts/.tsx/.js returns ZERO matches. components/pwa/ServiceWorkerManager.tsx:30-52 registers `/sw.js` and tracks `reg.waiting`/`updatefound` only; its header comment enumerates exactly two responsibilities ('1. Offline status', '2. Update available'). app/layout.tsx:93 `<ServiceWorkerManager />` sits in the ROOT body, outside any auth gate, so it does run for anonymous marketing/login visitors.",
    "note": "Verified, including the load-bearing part — the mount point really is the unauthenticated root layout, so the naive build would prompt anonymous visitors. Only editorial quibble: 'deliberately' is unsupported by any comment in the file; the API is simply absent, not documented as excluded."
  },
  {
    "id": "OS-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/providers/NotificationListener.tsx:78-96 — the postgres_changes INSERT handler dedupes only by `seenNotifIds.has(row.id)` (per-row id, so distinct rows never collapse) and then calls `showToast({... duration: 6000 })` unconditionally. components/providers/ToastProvider.tsx:41-42 `setToasts((prev) => [...prev, { id, type, title, message, duration }]);` — unbounded append, no cap, no coalescing; each toast is removed only by its own `setTimeout(..., duration)`. components/ui/CornerDock.tsx:22-27 renders the dock as `flex flex-col ... gap-2` with no max-height or overflow, so the stack grows upward without limit.",
    "note": "Confirmed with no mitigating guard anywhere: N inserts within 6s produce N simultaneously-mounted toast cards. The 6-second figure in the claim is exact (duration: 6000 at NotificationListener.tsx:96)."
  },
  {
    "id": "OS-5",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "components/notifications/NotificationBell.tsx:106 `<Bell className=\"w-4 h-4\" />` and :119 `<Bell className={...text-slate-300 group-hover:text-white} />` — no animation class on either bell. The badge at :108 and :121 is a static `bg-orange-500 ... text-[10px] font-black` span rendering `{unread > 99 ? \"99+\" : unread}` with no transition/interpolation. Grep for `animate-|motion|keyframes` in components/notifications/ yields only NotificationBell.tsx:130 (dropdown `animate-in fade-in zoom-in-95`), :160 (`Loader2 animate-spin`) and NotificationCenter.tsx:132 (`animate-pulse` skeleton). Sidebar.tsx:510 `leaf.badgeTone === 'red' ? 'bg-red-600 animate-pulse ...'` is gated by Sidebar.tsx:130 `badgeTone: s.actionRequired > 0 ? 'red' : 'blue'` — action-required, not unread.",
    "note": "Every factual assertion checks out, including the actionRequired-not-unread coupling of the one existing pulse. Downgrading to LOW because the finding is, by its own admission, an inventory of an absent enhancement rather than a defect — nothing is broken, mis-delivered or misleading today."
  },
  {
    "id": "OS-6",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "app/(protected)/checkouts/page.tsx:483-484 `const [nudging, setNudging] = React.useState(false); const [nudged, setNudged] = React.useState(false);` inside `function OverlapCard`, with the only guard at :488 `if (nudging || nudged) return;`. Nothing is persisted — the handler at :242-269 calls `notifyMany(...)` with no dedupe key and no server-side check. The card is mounted inside a collapse: :461 `{isOpen && (` … :465-466 `{overlaps.map((o, i) => (<OverlapCard key={i} ... />))}`.",
    "note": "The title claim is true — `nudged` is component-local and a refresh erases it, and repeat inserts are unbounded (see OS-1). But the summary's specific mechanism is FALSE: `overlaps` is independent state set from findCheckoutOverlaps (page.tsx:113 `setOverlaps(ov)`), not derived from `filtered` (page.tsx:128-139), and the key is a stable index, so changing the user filter does NOT remount OverlapCard or reset `nudged`. The real cheap reset paths are a page refresh, collapsing/reopening the 'Coordination signals' panel (`{isOpen && ...}` unmounts the cards), and the index-keying itself, which mis-carries a `nudged=true` onto a different overlap if refresh() reorders the list."
  },
  {
    "id": "OS-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Repo-wide grep for `task_nudge` across all .ts/.tsx returns exactly two hits: lib/inAppNotifications.ts:35 (the type declaration) and hooks/useTicketNotifications.ts:80 (`case 'task_nudge':` → `return 'scratchpad';`). No producer exists. Grep for `scratchpad` in components/navigation/Sidebar.tsx returns ZERO matches, and the only sectionCounts reads in the entire repo are Sidebar.tsx:229 (`.documents`), :231 (`.projects`) and :235 (`.requests`) — `.scratchpad` and `.other` are never rendered. KIND_ICON in NotificationBell.tsx has `task_overdue_digest: ListChecks` but no `task_nudge` entry, so it falls through to `KIND_ICON[item.kind] ?? Bell` (:167).",
    "note": "Every leg confirmed, including the generic-Bell fallback: the kind is declared, routed to a section with no sidebar item, emitted by nobody, and iconless."
  },
  {
    "id": "OS-8",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "supabase/schema.sql:9-16 `CREATE TABLE IF NOT EXISTS users (id, email, display_name, default_org_id, created_at, updated_at)` — no last-seen column; org_members (schema.sql:33-49) likewise has only invited_at/created_at. schema.sql:680 `read_at TIMESTAMPTZ,` is per-ROW read state on notifications, not a per-user watermark. Repo-wide grep for `last_seen|last_read|last_login|last_active|last_visit` finds only resource-level columns (checkout_sessions.last_seen_at at schema.sql:287/843, project_documents.last_seen_at) — nothing per-user-per-session.",
    "note": "The substantive point holds: a 'new since your last visit' comparison has no server-side watermark, and because read_at only clears on an explicit row click or Mark-all-read, an unread-count-driven banner would indeed re-fire forever. Correction: 'nowhere anywhere' overstates it — lib/eSignatures.ts:67 `const last = user.last_sign_in_at ? ...` shows Supabase's auth.users.last_sign_in_at IS readable; it is unusable for this purpose only because it is stamped with the CURRENT sign-in, not the previous one. Downgraded to LOW: this is a missing capability for an unbuilt feature, not a defect in shipped behavior."
  },
  {
    "id": "OS-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/notify/dispatch.ts:20 `export type NotifChannel = \"inapp\" | \"email\";` — two members. dispatch.ts:2-8 header: 'fanned out to every delivery channel (in-app bell, email)'. dispatch.ts:78-124 `emit()` has exactly two branches, `if (channels.includes(\"inapp\"))` and `if (channels.includes(\"email\"))`, with `const channels = input.channels ?? [\"inapp\", \"email\"];` at :82 and a bare blank line before the closing brace where a third branch would sit. The smoking gun is dispatch.ts:42: `/** Defaults to all three. Pass a subset to force-limit a noisy event. */` — a doc comment promising THREE channels above a two-member union.",
    "note": "Verified: emit() is the declared single fan-out point and carries only bell + email. The 'all three' comment surviving against a two-member NotifChannel union is direct corroboration that a third (push) channel once occupied that seam; the deletion itself cannot be shown from history because the repo is one squashed commit."
  },
  {
    "id": "PERS-1",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "HIGH",
    "evidence": "supabase/schema.sql:1080-1081 `CREATE POLICY \"tickets_org_access\" ON tickets FOR ALL USING (org_id IN (SELECT my_org_ids()));` — no role predicate, no column restriction, and `grep -niE 'on tickets|policy .*ticket' supabase/migrations/*.sql` finds no later or RESTRICTIVE ticket policy (only indexes and the search_tsv trigger). The server route it bypasses is the real gate: workflow-action/route.ts:96-103 `const allowed = WorkflowEngine.getActions(ticket, callerRole, caller.id, capPolicy); ... if (!action) return 403`.",
    "note": "The claim is accurate: the browser holds an anon-key session (lib/supabase.ts) and the app itself already writes tickets directly from the client (requests/page.tsx:620, :639), so any active member can PATCH status, engineerapprovedat, deliverable_rev etc. straight through PostgREST, skipping WorkflowEngine and the capability policy entirely. Severity corrected to HIGH rather than CRITICAL: because a FOR ALL policy with USING and no WITH CHECK reuses USING as the write check, tenant isolation still holds (no cross-org read or write), and the attacker must be an authenticated active member of that workspace — intra-org privilege escalation, not unauthenticated or cross-tenant compromise."
  },
  {
    "id": "PERS-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "requests/[id]/page.tsx:978 `await supabase.from('tickets').update({ comments: updatedComments }).eq('id', ticketId);` (array built at :970-975 from in-memory ticket.comments), :1010-1014 `update({ attachments: [...currentAttachments, newAttachment], ..., history: [...currentHistory, historyEntry] })`, :1328 `update({ watchers: next })` — none carries a CAS predicate. Contrast the server: comment/route.ts:205-216 `casQuery = auth.readLastModified ? casQuery.eq(\"last_modified\", auth.readLastModified) : ...; if (rows.length === 0) return 409 conflict`, and schema.sql:553-558 `UPDATE tickets SET comments = COALESCE(comments,'[]'::jsonb) || jsonb_build_array(p_comment)`.",
    "note": "Confirmed exactly as described, including the stated motive: 20260726_ticket_comments.sql:3-12 says the JSONB read-modify-write is what post_ticket_comment was built to eliminate, yet three client handlers still do whole-array RMW on the same column family. A concurrent server-side comment append or workflow action is silently erased by the client's stale array."
  },
  {
    "id": "PERS-3",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "All six cited writes discard the result: requests/[id]/page.tsx:920 `supabase.from('tickets').update({ unread_by: ... }).then(() => {})`, :978, :1010-1014, :1328; requests/page.tsx:620 and :639 — every one is `await supabase...update(...)` with no `const { error } =`. Audit rows follow unconditionally at :979-983, :1016-1020, page.tsx:623-627 and :640-644. The codebase knows better: requests/new/page.tsx:323-325 comments 'IMPORTANT: supabase-js does NOT throw on a failed insert — it returns { error }. Check it explicitly.'",
    "note": "Accurate — supabase-js resolves with {error} so the surrounding try/catch cannot fire and the user sees success. Corrected to MEDIUM: two of the six cited writes (:920 unread_by, :1328 watchers) have no audit call attached, so the 'audit log written as if it succeeded' half applies to four; and because the tickets policy is fully permissive these updates rarely fail in practice, making the impact conditional silent-failure rather than routine corruption."
  },
  {
    "id": "PERS-4",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "schema.sql:1080-1081 `FOR ALL USING (org_id IN (SELECT my_org_ids()))` covers DELETE with no RESTRICTIVE overlay; schema.sql:1249 `ticket_id UUID,` in document_intents — plain column, no REFERENCES; schema.sql:495 `ticket_id UUID NOT NULL REFERENCES tickets(id) ON DELETE CASCADE` on ticket_comments; R2 keys are only ever reclaimed for ARCHIVED stubs (ticket-shed/commit/route.ts:182-193 `.not(\"archived_at\",\"is\",null)` then deleteR2Keys).",
    "note": "The RLS and orphaning facts check out, but the scenario is hypothetical and partly self-blocking: a repo-wide grep for `from('tickets').delete(` / `from(\"tickets\").delete(` returns zero hits — no UI, route, or lib ever deletes a ticket — and schema.sql:840 `linked_ticket_id UUID REFERENCES tickets(id)` has no ON DELETE clause, so Postgres RESTRICTs deletion of any ticket that has a linked checkout session. This is latent future-feature risk, not a live defect: LOW."
  },
  {
    "id": "PERS-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/ticketTransitions.ts:51-81 rowToTicket maps id…lastModified and returns `as Ticket` — there is no `metadata:` line anywhere in it (contrast requests/[id]/page.tsx:904 which does map `metadata: (r.metadata as ...)`). workflow-action/route.ts:75 `const ticket = rowToTicket(row as Record<string, unknown>);` and :231-233 `const srcDoc = (ticket.metadata as Record<string, unknown> | undefined)?.source_document ...; if (srcDoc?.id) {` — always undefined, so both the upsert (:245) and the closure delete (:264-269) are dead.",
    "note": "Confirmed dead bridge, and the `as Ticket` cast is why TypeScript never caught it. Repo-wide, workflow-action/route.ts:245 is the only writer of a source:'ticket' document_intents row (other writers in lib/intents.ts cover viewer/checkout/declared sources), so no ticket-driven edit intent is ever registered or cleared and EditOverlapBanner can never surface a drafter working a ticket's source drawing."
  },
  {
    "id": "PERS-6",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "CheckInPanel.tsx:236-268 `supabase.from(\"tickets\").insert({ org_id, ticket_id, title, description, request_type, status, priority, target_completion_at, requester_*, attachments, watchers, comments, unread_by, history, metadata })` — no `unit`. lib/transitionIn.ts:304-331 — same, no `unit`. Only requests/new/page.tsx:303 `title, description, unit,` writes it. types/schema.ts:1112 declares `unit: string;` (non-optional) while schema.sql:403 is `unit TEXT` (nullable).",
    "note": "The absence claim is correct on all three paths. Corrected to LOW: the consequence is cosmetic/searchability only — requests/page.tsx:1046 and :984 render `{ticket.unit}` as an empty chip and the text filter at :400 guards with `(ticket.unit || '')`, so nothing throws and no ticket is hidden; there is no unit facet filter. The safety escalation for an undocumented field change does not depend on unit (CheckInPanel.tsx:291-300 notifies controllers directly, priority 1)."
  },
  {
    "id": "PERS-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/audit.ts:16-30 `try { await supabase.from(\"audit_logs\").insert({...}); } catch (error) { console.error(\"Failed to write audit log:\", error); }` — the returned {error} is never destructured. 20260813_acl_close_gaps_and_audit_scope.sql:84-90 `CREATE POLICY audit_logs_insert ON audit_logs FOR INSERT TO authenticated WITH CHECK (user_id = auth.uid() AND (org_id IS NULL OR org_id IN (SELECT my_org_ids())))`. Callers pass `userId: uid || 'unknown'` (requests/[id]/page.tsx:981, requests/page.tsx:625).",
    "note": "Correct: supabase-js resolves rather than rejects, so the catch is effectively unreachable and a policy rejection (or the literal 'unknown' string failing the uuid column) is swallowed silently. Compounding it, schema.sql:1084-1085 grants only SELECT/INSERT on audit_logs, so there is no later reconciliation path — the event is simply gone with the UI showing success."
  },
  {
    "id": "PERS-8",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "schema.sql:1031-1034 `CREATE OR REPLACE FUNCTION my_org_ids() RETURNS SETOF UUID LANGUAGE SQL SECURITY DEFINER AS $$ SELECT org_id FROM org_members WHERE uid = auth.uid() AND status = 'active'; $$;` — no `SET search_path`, unlike its peers (e.g. 20260812:37 `... SECURITY DEFINER SET search_path = public AS $$`), and no migration ever redefines it. It does gate tickets (:1080), documents (:1068), libraries, collections, versions, etc.",
    "note": "The missing SET search_path is real and worth fixing (Supabase's own linter flags it). Corrected to LOW because the described exploit is not reachable from the app's threat surface: PostgREST executes no DDL and cannot issue SET, so an app user can neither create a shadowing schema/table nor alter the session search_path — and a name in `public` cannot shadow `public.org_members` anyway; it needs CREATE-schema privilege that anon/authenticated do not hold. This is hardening, not an exploitable gate bypass."
  },
  {
    "id": "PHYS-1",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "HIGH",
    "evidence": "app/api/verify/route.ts:34-38 selects `id, document_number, title, name, rev, status, current_version_id, superseded_at` — document_holds is never queried in the file — and :89-90 `const docRetired = d.status === \"Superseded\" || d.status === \"Archived\"; const isCurrent = !docRetired && (!versionId || versionId === d.current_version_id);`, which app/verify/[docId]/page.tsx:65 paints `bg-emerald-600` and :99 labels `CURRENT`. Worse than reported: \"Void\" is absent from the docRetired list, so a VOIDED document at its current version also verifies green.",
    "note": "The mechanical claim is correct — a held document scans GREEN. But 'invisible to every field scan' is false, and CRITICAL rests on a premise the codebase contradicts: holds are defined in-repo as blocks on ADVANCING a document, not on using a print (lib/documentGuards.ts:105-107 'Any active hold blocks the operation (the hold exists precisely to stop the document from advancing)'; components/ui/IsoGuidance.tsx:112-118 'no new revisions, no IFC release'), and the physical carrier for a hold is its own tag — lib/physicalBridge.ts:150 draws 'HOLD — DO NOT ADVANCE' and :164 stamps a `/verify-hold/{holdId}` QR served by app/api/verify-hold/route.ts. Real gap, HIGH not CRITICAL."
  },
  {
    "id": "PHYS-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/verify-hold/route.ts:27-32 selects a single row `.eq(\"id\", holdId).maybeSingle()` and :53-61 returns `active: !h.released_at` — no query against sibling rows on the same document_id. app/verify-hold/[holdId]/page.tsx:53-54 paints the whole screen `result?.active ? \"bg-red-600\" : \"bg-emerald-600\"` and :81-88 prints \"RELEASED\" / \"This hold has been released — this tag can come down.\" The multi-hold model is explicit: supabase/migrations/20260612_phase5_holds.sql:19-20 \"Multiple holds can be open on the same document simultaneously\", with the unique index scoped to (document_id, reason) WHERE released_at IS NULL (:61-62) — i.e. concurrent holds of different reasons are legal by construction. lib/physicalBridge.ts:170 prints the matching promise on the card: \"A released hold shows GREEN when scanned — then this tag comes down.\"",
    "note": "Confirmed — no guard anywhere adds sibling awareness; the endpoint never touches document_id except to fetch the label. The one mitigation is that each hold gets its own card, so the second card still scans red; the failure is that the green card instructs removal of a tag while the document is demonstrably still held. MEDIUM is fair."
  },
  {
    "id": "PHYS-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": ".env.example:43-46 — under the header `# ─── Misc (optional) ───`, the comment reads \"Public origin used when building absolute links server-side (falls back to VERCEL_URL on Vercel deployments).\" lib/publicOrigin.ts:17-22 has no such fallback: `const configured = (process.env.NEXT_PUBLIC_SITE_URL || \"\")...; if (configured) return configured; if (typeof window !== \"undefined\") return window.location.origin; return \"\";`. The VERCEL_URL fallback exists only in app/layout.tsx:16-18 for OG metadata. In the server route app/api/share/file/route.ts:113-116 the footer is unconditional — `footerNotice: `${label} Rev ${rev ?? \"?\"} at time of download — scan the QR to confirm it is still current.`` — while `verifyUrl: versionId && publicOrigin() ? ... : undefined` drops out, and lib/stamping.ts:246 `if (opts.verifyUrl)` then skips QR generation entirely.",
    "note": "Correct on every leg: the documentation promises a fallback the function does not implement, and the server-side stamp keeps the 'scan the QR' sentence after the QR has been suppressed. The client-side generators cited (lib/downloads.ts:95-102, lib/docPack.ts:101-106, lib/physicalBridge.ts:49-53) do survive an unset var via the window.location.origin branch — the damage is confined to server-rendered stamps, which is what the finding claims."
  },
  {
    "id": "PHYS-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/stamping.ts:118-119 `const base = page.getViewport({ scale: 1 }); const viewport = page.getViewport({ scale: RASTER_WIDTH / base.width });` — pdf.js getViewport applies /Rotate, so W/H and the four corner boxes measured at :128-139 are in ROTATED (visual) space. The draw pass at :262 takes `const { width, height } = page.getSize();` from pdf-lib, which is the raw MediaBox, and hands those straight to `placeQr({ pageW: width, pageH: height, corner: qrCorner })` (:274) — lib/stampLayout.ts:180-189 computes plateX/plateY purely from pageW/pageH — and to drawWatermark (:154-174 via fitRotatedTextSize/centerRotatedText, lib/stampLayout.ts:38-90). `grep -rn \"getRotation\" --include=*.ts --include=*.tsx` over the repo returns zero hits.",
    "note": "Confirmed: nothing in the codebase reads or compensates for page rotation, so on a /Rotate 90 sheet the corner chosen from the rotated raster is applied to transposed MediaBox coordinates and the watermark is fitted to swapped page dimensions. Note it is broader than stated — even with no ink analysis (the server path, which passes no sourceBytes and so uses FALLBACK_INK 'br'), the stamp is still drawn in unrotated space and appears sideways on the printed sheet."
  },
  {
    "id": "PHYS-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/documents/ShareLinkModal.tsx:81 `const baseUrl = typeof window !== \"undefined\" ? `${window.location.origin}/share/` : \"/share/\";` → :147 `const url = `${baseUrl}${s.token}`;` → :209 `<QrBadge value={url} size={140} caption=\"Scan to open this share link\" />`. components/viewers/FullScreenViewer.tsx:1268 `value={`${window.location.origin}/documents/${docRecord.libraryId}?doc=${docRecord.id}`}` — while the same file imports publicOrigin at :63 and uses it correctly for the stamp's verify URL at :1015-1016. lib/publicOrigin.ts:8-12 states the rule these two break verbatim.",
    "note": "Both call sites confirmed, and the file-local inconsistency in FullScreenViewer (publicOrigin at :1016, window.location.origin at :1268) makes it plainly an oversight rather than a deliberate exception. The share-link QR is the one that reaches an external party, so the preview-deploy dead-end is real; the /documents QR at :1268 targets a protected route the scanner must log into anyway, which limits the blast radius of that half."
  },
  {
    "id": "PHYS-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/requests/[id]/page.tsx:1391 `deliverableRev={ticket?.deliverableRev}` — the ticket-level current rev is handed to FileViewerModal for whatever attachment is open, and :569/:577-578 use it verbatim: `/verify-ticket/${ticketRowId}?r=${encodeURIComponent(deliverableRev)}` and `deliverable Rev ${deliverableRev} at time of printing`. app/api/verify-ticket/route.ts:82-83 `} else if (latestIssued && printedRev === latestIssued) { verdict = \"current\";` then returns the green verdict.",
    "note": "Confirmed, and there is no defence available: types/schema.ts:1038-1047 `interface TicketAttachment` carries no rev field at all, so the modal has nothing per-file to stamp with, and nothing in lib/ticketTransitions.ts removes or re-types prior Final attachments when deliverable_rev advances (:214-251 only write the new label). Printing a superseded Final produces paper and a QR that both certify the current rev. CRITICAL stands."
  },
  {
    "id": "PHYS-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/assets/[tag]/page.tsx:56 `.neq(\"status\", \"Archived\")` is the only status filter, and :132 passes `documentIds: docs.map((d) => d.id)` — every Superseded, Void and Draft doc included. lib/docPack.ts:51-54 re-selects those ids with no status or hold predicate and :77 loops them all; the success text at :139-141 is `Pack ready — ${result.included} drawing${...}, all current, all stamped.` Holds ARE loaded on the same page (:70-77, `.from(\"document_holds\")...is(\"released_at\", null)`) and never reach docPack.",
    "note": "Confirmed on every leg. The QR does partly self-correct a Superseded sheet (app/api/verify/route.ts:89), but a VOID sheet is not in that route's docRetired list, so a voided drawing in the pack scans green — the paper claim 'all current' is unqualified and unchecked. HIGH stands."
  },
  {
    "id": "PHYS-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/d/[number]/route.ts:26-32 queries `supabaseAdmin.from(\"documents\")` (lib/supabaseAdmin.ts service-role client) with only `.filter(\"document_number\",\"not.is\",null).ilike(...)` — no org filter, no session check anywhere in the file — and :44-46 `dest.searchParams.set(\"doc\", match.id); return NextResponse.redirect(dest);` puts the UUID in the Location header of an unauthenticated 307. There is no middleware.ts in the repo, so nothing gates the route.",
    "note": "Confirmed, and slightly worse than described: the `?? (rows ?? [])[0]` fallback at :36 means even a non-matching 2-character substring returns some real document's UUID, and app/api/verify/route.ts:34-38/:96-108 then returns number, title, current rev and status for any UUID with no org scoping. HIGH stands."
  },
  {
    "id": "PHYS-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/viewers/FullScreenViewer.tsx:963-967 `const liveState = docRecord && currentUserId ? determineControlState(docRecord, currentUserId) : \"uncontrolled\"; const stampNow = liveState !== \"controlled\";` and :1004-1005 `if (stampNow) { await applyStampToPdfDoc(...) }` — the watermark, the `markups are not part of the controlled revision` footer (:1013) and the verify QR (:1014-1016) all sit inside that branch. lib/downloads.ts:35 `if (doc.checkedOutBy && doc.checkedOutBy === userId) return \"controlled\";` makes the checkout holder — i.e. the person doing the redlining — take the unstamped path, filename suffix `_markup` (:1006, :1024).",
    "note": "Confirmed by the code's own words: the footer text it skips is the one asserting the markups are not the controlled revision. The 'controlled copy = raw PDF' rule is deliberate for a clean issue, but a redlined sheet is by definition not the controlled revision, so applying that rule here produces an unmarked drawing carrying non-issued linework. HIGH stands."
  },
  {
    "id": "PHYS-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/documents/VersionHistoryPanel.tsx:150-159 `const docForDownload: DocumentRecord = { ...doc, checkedOutBy: undefined }; await downloadDocumentPdf({ doc: docForDownload, versionId: v.id, ... })` — only checkedOutBy is overridden; `rev` stays the document's current rev and no `filename` is passed. lib/downloads.ts:74 `const rev = doc.rev ? \\`_Rev${doc.rev}\\` : \"\";` names the file, and :82 `parts.push(\\`Rev ${doc.rev ?? \"?\"} at time of issue — verify current revision before use.\\`)` stamps the footer, both from the CURRENT rev, while :99 `const version = ctx.versionId ?? ctx.doc.currentVersionId;` correctly encodes the historical version in the QR.",
    "note": "Confirmed exactly as claimed, including the internal contradiction the finding relies on: the same stamped page asserts the current rev in text and the printed rev in the QR. HIGH stands."
  },
  {
    "id": "PHYS-7",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "lib/physicalBridge.ts:84 `const url = `${origin()}/assets/${encodeURIComponent(asset.tag)}`;` and :99 `page.drawText(\"SCAN: drawings · holds · report a problem\", ...)`. The target lives only at app/(protected)/assets/[tag]/page.tsx (no public /assets route exists; `find app -type d` shows app/(protected)/assets/[tag] only). There is no middleware.ts anywhere in the repo, and app/(protected)/layout.tsx gates only on `uid && membershipState === \"none\"` (:49) / `\"error\"` (:50) — an anonymous visitor (uid null, membershipState 'resolving') falls straight through to line 52 and renders the full signed-in shell. The page itself then hangs: `const [loading, setLoading] = useState(true)` (:40) with `if (!activeOrgId || !tag) return;` (:44) at the top of refresh — the early return never clears the spinner, so `if (loading && docs.length === 0 && !asset) return <Spinner/>` (:95) is terminal.",
    "note": "Factually correct: the sticker's URL is inside the protected group, nothing redirects an account-less scan to a login, and the page spins forever because refresh() early-returns without clearing its initial loading=true. Severity lowered to MEDIUM — this is a dead-end for account-less scans only (signed-in staff get the real page), it discloses nothing and, unlike PHYS-10, shows no misleading information; the label also never promises 'no login needed' the way the package cover (physicalBridge.ts:279) does."
  },
  {
    "id": "PHYS-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/schema.sql:789-799 defines download_audits with exactly id, org_id, document_id, version_id, user_id, user_email, created_at, expires_at, watermark_policy_id — no `source`, `ticket_id`, `attachment_id`, `attachment_type`, `filename`, or `watermark_text`. app/api/share/file/route.ts:139 inserts `source: stamped ? \"share_link\" : \"share_link_unstamped\"` inside `try { await sb.from(\"download_audits\").insert({...}) } catch { /* pre-migration column drift — never block the share */ }` (:129-141). app/(protected)/requests/[id]/page.tsx:593-600 inserts `ticket_id, attachment_id, attachment_type, filename, ... watermark_text, source: \"drafting_print\"` with `.then(() => {}, () => {})`, and :675-684 the same shape with `source: \"drafting\"`. Repo-wide search of supabase/migrations/ and REMEDIATION_APPLY_ALL.sql returns no ALTER TABLE download_audits ADD COLUMN — the only other hit is `ALTER TABLE download_audits ENABLE ROW LEVEL SECURITY` (schema.sql:1022).",
    "note": "Confirmed on both halves: the columns exist nowhere in the schema or any migration, and every one of these callers discards the outcome (supabase-js returns `{error}` rather than throwing, so the try/catch at share/file:141 is not even reached). The paths that DO write valid rows — lib/downloads.ts:132-141, lib/docPack.ts:114-122, components/viewers/MultiDocViewer.tsx:737-747 — use only real columns, which is exactly why the distribution record contains internal pulls and nothing else."
  },
  {
    "id": "PHYS-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/requests/[id]/page.tsx:647 `watermarkText: file.type === \"Draft\" ? \"REVIEW ONLY - DO NOT DISTRIBUTE\" : \"CONTROLLED COPY\"` — fed to stampPdf, which builds the diagonal run at lib/stamping.ts:56-64 (`parts.push(opts.watermarkText)`). The same applyStampToPdfDoc call then draws lib/stamping.ts:211 `const mainText = `UNCONTROLLED COPY • Downloaded: ${formatDate(opts.timestamp)} • Do Not Distribute`;` — a hard-coded footer string with no reference to opts.watermarkText. The print path one screen up (:575) uses \"UNCONTROLLED COPY\" for the same class of file, so the two buttons disagree with each other as well.",
    "note": "Both marks land on the same page from the same call — the footer literal at stamping.ts:211 is unconditional. The document path never has this problem because lib/downloads.ts:30-36 gives a controlled copy as an unstamped pass-through and only ever stamps \"UNCONTROLLED — FOR REVIEW ONLY\" (:236); 'CONTROLLED COPY' as a watermark exists only on this drafting-portal branch. The registration half is right too: the audit row for this very download (:675-684) names columns that do not exist (see PHYS-8), so nothing records the copy at all."
  },
  {
    "id": "PKG-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/storage/upload-url/route.ts:33-45 is the entire authorization: `const orgMatch = path.match(/^orgs\\/([0-9a-fA-F-]{36})\\//); if (orgMatch) { ... .from(\"org_members\").select(\"uid\").eq(\"org_id\", orgMatch[1]).eq(\"uid\", user.id).eq(\"status\", \"active\") ... }` — no role check, no lookup of what the key means, then :47-53 signs a PutObjectCommand for exactly that key. The key is not secret: lib/storage.ts:423 `return { path, url: path, ... }`, so `document_versions.file_url` stores the raw R2 key and any org member reads it under `documents_org_access`. No object-lock/versioning: lib/r2.ts constructs a plain S3Client, and a repo-wide grep for ObjectLock/immutable/WORM finds nothing on storage. lib/docPack.ts:84-90 then fetches those exact bytes (`const res = await fetch(httpUrl); ... PDFDocument.load(bytes)`), and no read path re-checks `file_hash`.",
    "note": "Confirmed at CRITICAL. The route authorizes the org prefix and nothing else, so any active member — Viewer included — can overwrite the bytes behind an Issued, signed revision while every database fact (rev, file_hash, current_version_id, approvals) stays untouched."
  },
  {
    "id": "PKG-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/documents/VersionHistoryPanel.tsx:151-159 calls `downloadDocumentPdf({ doc: docForDownload, versionId: v.id, ... })` where `docForDownload = { ...doc, checkedOutBy: undefined }` — the document record, carrying the CURRENT `doc.rev`. lib/downloads.ts:80-88 `buildFooterNotice` uses only `doc.rev`: `parts.push(\\`Rev ${doc.rev ?? \"?\"} at time of issue — verify current revision before use.\\`)`; lib/downloads.ts:71-76 `defaultFilename` likewise: `const rev = doc.rev ? \\`_Rev${doc.rev}\\` : \"\";`. Neither function receives ctx.versionId.",
    "note": "Confirmed: no caller overrides ctx.filename for a historical revision, and the stamp options at lib/downloads.ts:228-240 pass `footerNotice: buildFooterNotice(ctx.doc, ctx.userId)` unchanged. The only thing that does honour the version is the QR (buildVerifyUrl uses `ctx.versionId ?? ctx.doc.currentVersionId`), so the scan verdict and the printed footer contradict each other on the same sheet."
  },
  {
    "id": "PKG-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/storage/download-url/route.ts:144-151 `const expiresIn = parseInt(req.nextUrl.searchParams.get(\"expiresIn\") || \"3600\"); ... const url = await getSignedUrl(r2, command, { expiresIn });` — no Math.min, no allow-list, no clamp anywhere between parse and sign. Every in-app caller passes a fixed hour (lib/docPack.ts:31-34 `&expiresIn=3600`), so nothing depends on large values.",
    "note": "Confirmed. Authorization is per-request (active membership + private/hidden ACL discover + explicit download-deny), but all of it is spent once: the minted URL carries no further authentication and survives membership revocation for its whole lifetime, up to SigV4's 7-day ceiling, and the bytes it serves are the raw unstamped original with no download_audits row. MEDIUM is a fair severity — it requires an authenticated member who already passes the ACL gate."
  },
  {
    "id": "PKG-12",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "lib/docPack.ts:77 `for (const d of docs)` with no count/byte/time bound, accumulating into the single in-memory `merged` doc (`:68`, `:109-110`) before `await merged.save()` at :150. lib/physicalBridge.ts:262-270 `input.docs.slice(0, 24).forEach(...)` then `if (input.docs.length > 24) page.drawText(\\`…and ${input.docs.length - 24} more sheets\\`)`. Cover order comes from lib/workPackages.ts:75-79 (`work_package_documents` row order) while the PDF order comes from lib/docPack.ts:51-54 `.from(\"documents\").select(...).in(\"id\", input.documentIds)` — PostgREST does not preserve the input array order. app/(protected)/packages/page.tsx:157 calls `await refreshWorkPackage(pkg.id)` BEFORE the pack is built, so a mid-way failure leaves pins claiming a print that never landed.",
    "note": "The unbounded in-memory merge, the 24-item cover truncation and the order mismatch are all real. One sub-claim is overstated and should be corrected: stamping does NOT rasterize every page of every document — lib/stamping.ts:70 `const MAX_ANALYZED_PAGES = 40;` and :112 `const pageCount = Math.min(doc.numPages, MAX_ANALYZED_PAGES);` cap ink analysis at 40 pages per document (later pages reuse the last analysis, lib/stamping.ts:268). Also app/(protected)/assets/[tag]/page.tsx:57 `.limit(500)` puts an implicit 500-document ceiling on the asset-hub path (the work-package path has none). MEDIUM stands."
  },
  {
    "id": "PKG-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "`grep -rn \"getRotation|setRotation|/Rotate\" lib/stamping.ts lib/stampLayout.ts lib/markupExport.ts` returns no rotation handling at all — the only hits are the watermark's own `rotate: degrees(ANGLE)` (lib/stamping.ts:180). Analysis is done in DISPLAY space: lib/stamping.ts:114-116 `const base = page.getViewport({ scale: 1 }); const viewport = page.getViewport({ scale: RASTER_WIDTH / base.width });` — pdf.js viewports apply the page's /Rotate. Placement is done in UNROTATED space: lib/stamping.ts:259-261 `const { width, height } = page.getSize();` (pdf-lib MediaBox dims, rotation-agnostic) feeding `placeQr({ pageW: width, pageH: height, corner: qrCorner })` at lib/stampLayout.ts:120-190.",
    "note": "Confirmed: on a /Rotate 90 sheet the corner chosen from the displayed page maps to a different physical corner once the viewer applies the rotation, and pageW/pageH are swapped relative to what the reader sees, so the plate geometry is computed against the wrong axis. lib/markupExport.ts:32-44 has the same defect (`const { width, height } = page.getSize(); ... page.drawImage(img, { x: 0, y: 0, width, height })`), so baked markup on a rotated page is drawn to the unrotated box too."
  },
  {
    "id": "PKG-14",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/schema.sql:1264-1265 `-- WORK PACKAGES + DISTRIBUTION ACKS (migration 20260825) / Cumulative snapshot; RLS policies live in the migration file.` then CREATE TABLE for work_packages (:1266), work_package_documents (:1280) and distribution_acks (:1294) with no ENABLE ROW LEVEL SECURITY and no CREATE POLICY — `grep -n \"work_packages|work_package_documents|distribution_acks\" supabase/schema.sql` returns only the CREATE TABLE/INDEX lines. The migration does what schema.sql omits: supabase/migrations/20260825_work_packages_acks.sql:57-58 `ALTER TABLE work_packages ENABLE ROW LEVEL SECURITY; ALTER TABLE work_package_documents ENABLE ROW LEVEL SECURITY;` (and :119 for distribution_acks).",
    "note": "Confirmed, and schema.sql really is the from-scratch path: docs/ARCHITECTURE.md:947 `schema.sql ← cumulative create-from-scratch reference`. Because the migration uses CREATE TABLE IF NOT EXISTS, replaying migrations after schema.sql WOULD still run the ALTER … ENABLE RLS lines, so the gap only bites a deployment bootstrapped from schema.sql alone — which is exactly the documented path. MEDIUM is right."
  },
  {
    "id": "PKG-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/verify-package/route.ts:38-61 reads the LIVE pins and compares them to the live document: `.from(\"work_package_documents\").select(\"document_id, pinned_version_id, pinned_rev_label\").eq(\"package_id\", pkgId)` then `fresh: !retired && !!r.pinned_version_id && r.pinned_version_id === (d?.current_version_id ?? null)`. Nothing records a print event — repo-wide grep for `printed_at|last_printed|print_snapshot|package_prints` returns zero hits. Both re-arming paths exist: lib/workPackages.ts:209-219 `refreshWorkPackage` updates `pinned_version_id`/`pinned_rev_label` to the doc's current values, and lib/workPackages.ts:181-189 `addDocumentToPackage` upserts `onConflict: \"package_id,document_id\"` with the current version — so re-adding an existing drawing re-pins it (components/documents/AddToPackageButton.tsx:30-43 calls it with `currentVersionId`). The QR is per-package and immutable: lib/physicalBridge.ts:275 `${origin()}/verify-package/${input.packageId}`.",
    "note": "Confirmed. The verdict page states a fact the data cannot support — app/verify-package/[packageId]/page.tsx:95 renders `${staleCount} of ${sheetCount} sheets changed since this pack was printed` from a comparison that has no knowledge of any print. Any re-pin flips already-distributed paper back to 'PACK IS CURRENT'."
  },
  {
    "id": "PKG-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/storage.ts:205-216 `makeLibraryStoragePath` = `joinPath(\"orgs\", orgId, \"libraries\", libraryId, ...folder, sanitizeFilename(filename))` — no uuid, no timestamp. app/(protected)/documents/[libraryId]/page.tsx:2409-2413 passes `filename: file.name`; lib/revisions.ts:355-361 passes `filename: `Rev0_${input.file.name || \"drawing.pdf\"}``. Every OTHER caller salts the name: lib/revisions.ts:485/873/1615 and lib/documentLifecycle/common.ts:197 all build `${stem}__rev${safeRev}__${Date.now()}.${ext}` first. The auto-rename at page.tsx:2390-2401 only mutates `document_number` (`candidate = `${original}-${n}``), never the storage key, and R2 PUTs overwrite silently (no HEAD/exists check in uploadToPath, lib/storage.ts:378-423).",
    "note": "Confirmed, and the finding is precise about which two paths are affected — the contrast with the four salted callers is what makes it a defect rather than a design. Two same-named files in one folder collapse to one object while both document rows keep pointing at it, so the older document serves the newer drawing's bytes under its own title block."
  },
  {
    "id": "PKG-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/docPack.ts:51-54 `.from(\"documents\").select(\"id, org_id, document_number, ... current_version_id, checked_out_by, ...\").in(\"id\", input.documentIds)` — no status predicate, and document_holds is never queried anywhere in the file. The only per-sheet warning is the checkout one at :95-97 `const holderWarning = d.checked_out_by && ( ` ACTIVE CHANGE IN PROGRESS: ...`)`, and the footer at :103-107 is `${label} Rev ${d.rev} at time of issue — verify current revision before use.` The caller passes everything: app/(protected)/assets/[tag]/page.tsx:51-58 selects tagged docs with only `.neq(\"status\", \"Archived\")` (Draft/Superseded/Void all pass) and :129-141 hands `documentIds: docs.map((d) => d.id)` straight to buildAndDownloadDocPack.",
    "note": "Confirmed at CRITICAL. The page even asserts the opposite of what it built — on a clean run it toasts `Pack ready — ${result.included} drawings, all current, all stamped.` for a bundle that may contain a Void sheet and a sheet under an open hold. (The cited lib/holds.ts:246 is a miscitation — that line is notification body text — but the absence of any hold query in docPack.ts is the real evidence and it holds. Partial mitigation: each sheet's own /verify QR reports docStatus, which covers Superseded/Archived but not Void and not holds.)"
  },
  {
    "id": "PKG-5",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "HIGH",
    "evidence": "20260828_integrity_hardening.sql:285-292 `CREATE POLICY work_package_documents_org_update ON work_package_documents FOR UPDATE USING (EXISTS (SELECT 1 FROM org_members WHERE org_members.org_id = work_package_documents.org_id AND org_members.uid = auth.uid() AND org_members.status = 'active')) WITH CHECK (same)` — membership only, no role, no owner. 20260825_work_packages_acks.sql:86-89 INSERT `WITH CHECK` is the identical org_members test on the row's own `org_id`; `package_id` is never joined back to `work_packages.org_id` (table def at :37-50 has only plain FKs). app/api/verify-package/route.ts:38-41 reads those rows with the service-role client and no org filter.",
    "note": "Both factual claims are true. Severity is one step too high: the app itself already grants every member exactly this power through an unguarded UI button — app/(protected)/packages/page.tsx:293-299 renders 'Refresh pins' for any viewer and lib/workPackages.ts:194-229 performs the same UPDATE — so the policy is consistent with the shipped design rather than a bypass of it. The genuinely un-mitigated part is the unbound `package_id` on INSERT (cross-org row injection into another org's pack) plus the ability to set an arbitrary pin value; HIGH."
  },
  {
    "id": "PKG-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/packages/page.tsx:153-190: `setPrinting(pkg.id); try { await refreshWorkPackage(pkg.id); ... const result = await buildAndDownloadDocPack({...}); ... } catch (e) { showToast({ type: \"error\", title: \"Couldn't print the pack\", ... }); }` — the re-pin (lib/workPackages.ts:209-219) completes before a single byte is fetched, and the catch has no compensating rollback. The build can fail wholesale after that point: lib/docPack.ts:142-148 `if (included === 0) throw new Error(... `No documents could be packed (${skipped[0].label}: ${skipped[0].reason})` ...)`, and per-document failures are swallowed into `skipped` at :132-134 while their pins have already moved.",
    "note": "Confirmed. The comment above the handler states the intended invariant — 'the paper and the database agree by construction' — which is exactly what the ordering breaks: on a failed or partial build the pins assert a print that never left the browser, and the public cover QR (PKG-2) then reads GREEN for whatever paper is actually in the field."
  },
  {
    "id": "PKG-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "The premise is real: 20260708_acl_rls_enforcement.sql:85-87 `CREATE POLICY documents_acl_select ON documents AS RESTRICTIVE FOR SELECT USING (node_visible(visibility, acl_index, org_id));` — a document can be invisible to an active member, while work_package_documents carries only the org-wide SELECT policy. Then lib/workPackages.ts:95-106 `const doc = docById.get(String(m.document_id)); const current = (doc?.current_version_id as string | null) ?? null; ... drifted: !!pinned && !!current && pinned !== current` — a missing doc gives current=null, so `drifted` is always false and staleCount (:119) never counts it. lib/workPackages.ts:210-215 `const d = byId.get(r.document_id); ... .update({ pinned_version_id: (d?.current_version_id as string | null) ?? null, pinned_rev_label: (d?.rev as string | null) ?? null })` NULLs the pin (and the UPDATE succeeds, since its policy is org-membership only). lib/docPack.ts:51-55 fetches by id and the loop at :77-83 iterates only the returned rows, so the hidden document is not merged and is not even listed in `skipped`, while the cover built at app/(protected)/packages/page.tsx:161-168 lists every member row.",
    "note": "Confirmed at HIGH — a hidden member is silently a permanent 'fresh' sheet, its pin is destroyed by the very refresh the print flow performs, and the printed cover lists a sheet the PDF does not contain with no skip warning to the operator."
  },
  {
    "id": "PKG-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/verify-package/route.ts:56 `const retired = d?.status === \"Superseded\" || d?.status === \"Archived\";` and app/api/verify/route.ts:89-90 `const docRetired = d.status === \"Superseded\" || d.status === \"Archived\"; const isCurrent = !docRetired && (!versionId || versionId === d.current_version_id);`. types/schema.ts:613 `export type DocumentStatus = \"Draft\" | \"Issued\" | \"Superseded\" | \"Void\" | \"Archived\" | \"Locked\";` and components/documents/MetadataEditor.tsx:9 lets a user set status to \"Void\". app/verify/[docId]/page.tsx:100 renders `result.isCurrent ? \"CURRENT\"` on an emerald page; app/verify-package/[packageId]/page.tsx:91 renders `result.allFresh ? \"PACK IS CURRENT\"`.",
    "note": "Confirmed: only Superseded and Archived retire a document on either public surface, so a Void (or Draft) doc whose printed/pinned version still equals current_version_id scans full-screen green. The rest of the codebase already knows better — lib/aiBoundary.ts:25 `NOT_CURRENT_STATUSES = new Set([\"Superseded\", \"Void\", \"Archived\"])` and lib/staleCopies.ts:76 both include Void. Only nuance: a pack sheet for a doc with NO version at all reads stale (fresh requires a non-null pinned_version_id), so the Void case, not the never-uploaded-Draft case, is the live one; pinned_version_id is set to current_version_id at pin/refresh time (lib/workPackages.ts:163,185,214), so a Void doc pins green."
  },
  {
    "id": "PKG-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/downloads.ts:218 `await assertAckGate(ctx);` (downloadDocumentPdf) and :262 `await assertAckGate(ctx);` (printDocumentPdf) are the ONLY two call sites — `grep -rn \"assertAckGate\"` returns exactly lines 177, 218, 262 of lib/downloads.ts. lib/docPack.ts:40-140 `buildAndDownloadDocPack` fetches, stamps, merges and downloads every document without importing lib/downloads at all (its imports are pdf-lib, supabase, stamping, intents, publicOrigin, types).",
    "note": "Confirmed and reachable from both entry points: app/(protected)/assets/[tag]/page.tsx:129-130 and app/(protected)/packages/page.tsx:159-169 both dynamic-import buildAndDownloadDocPack directly. The pack even replicates the audit/intent side-effects of a single download (lib/docPack.ts:114-133), which shows it was written to mirror downloadDocumentPdf — it just skipped the one blocking check."
  },
  {
    "id": "PM-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/projects.ts:259-317 transitionProjectStatus writes only `status`, `completed_at`, `cancelled_at`, `cancelled_reason` and then releases checkouts — nothing marks the project read-only. app/api/intake/upload/route.ts:35-41 gates solely on `link.revoked_at` and `link.expires_at`; :225-228 `.from(\"projects\").select(\"id, name, owner_user_id, intake_library_id, intake_collection_id\").eq(\"id\", projectId)` never reads `status`, and :299-334 goes on to create the version and, for a trusted link, `update({ current_version_id: versionId, rev: revLabel, status: \"Issued\", ... })`. 20261013_project_controls_program.sql:262-288 shows every project-child write policy as `USING (is_org_controller(org_id) OR user_owns_project(project_id))` with no status predicate. app/(protected)/projects/[id]/page.tsx:625-653 renders the closeout gates as advisory (\"You can complete anyway — the open items stay on the record\").",
    "note": "Survives. Verified as a claim of absence: grep across lib/, app/ and components/ finds no closed-project write guard of any kind, and the intake door never loads projects.status. A cancelled project's trusted link still auto-supersedes a controlled drawing to Issued."
  },
  {
    "id": "PM-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/projectExport.ts:13-18 — `function csvField(v) { if (v == null) return \"\"; const s = String(v); if (/[\",\\n\\r]/.test(s)) return `\"${s.replace(/\"/g, '\"\"')}\"`; return s; }`. Quoting only; no `=`/`+`/`-`/`@`/tab/CR neutralization and no leading-apostrophe prefix. Project name and description flow straight through at :70 and :74 (`csvRow([\"Name\", p.name])`, `csvRow([\"Description\", p.description ?? \"\"])`), checkout purpose at :89 (`csvRow([c.user_name, c.mode, c.purpose ?? \"\", ...])`). Callers: page.tsx:328 `exportProjectToCsv(...)` and projects/page.tsx:107 `exportAllProjectsToCsv(activeOrgId)`.",
    "note": "Correct — and note the quoting rule actively helps the payload: `=cmd|'/C calc'!A1` contains no comma/quote/newline, so it is emitted bare. The only sanitizer in the file is at :110, `String(bundle.project.name).replace(/[^a-z0-9-_ ]/gi, \"_\")`, which is applied to the download FILENAME only, not to the cell contents."
  },
  {
    "id": "PM-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Repo-wide grep for \"observer\" outside node_modules returns exactly three hits: types/schema.ts:944 (`export type ProjectMemberRole = \"owner\" | \"collaborator\" | \"observer\";`), app/(protected)/projects/[id]/page.tsx:926 (`<option value=\"observer\">Observer</option>`) and page.tsx:952, which uses it only to pick a badge colour. The only other read of a project member's role is page.tsx:943 `const isOwner = m.role === \"owner\" || m.userId === project.ownerUserId;` (controls the Remove button) and lib/evidencePack.ts:170, which prints it into a report. Permissions never consult it: page.tsx:131-134 `const isMember = members.some((m) => m.userId === uid); const canComment = isOwner || isMember || isAdmin; const canManage = isOwner || isAdmin;`, and 20260913_projects_rls_recursion_fix.sql:74-86 gates project_members writes on `is_org_controller(project_org(...)) OR is_project_owner(...)` with no role column test.",
    "note": "The claim of absence holds under a full-repo search — no RLS policy, API route, or lib function branches on collaborator vs observer. An observer can post into the regulated activity feed (project_activity_insert requires only org membership) exactly as a collaborator can."
  },
  {
    "id": "PM-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/projectReport.ts:59 — `const rollup = computeCostRollup(accounts, entries, new Map());`. In lib/costs.ts:305-307, `const pct = a.wbsMilestoneId ? milestonePct.get(a.wbsMilestoneId) : undefined; const earnedValue = pct !== undefined ? ... : null; if (earnedValue !== null) { evTotal += earnedValue; evActual += spent; }` — with an empty map every account yields null EV, so evActual stays 0 and costs.ts:333 `cpi: evActual > 0 ? evTotal / evActual : null` is unconditionally null. The report's CPI row is guarded by `d.rollup.cpi != null` (projectReport.ts:139-140), so it never prints. Separately, projectReport.ts:54-57 builds `milestonePctIndex(live.map((m, i) => ({ id: String(i), ... })))` — array position, not milestone id — then discards it at :58 `void pctIdx;`.",
    "note": "Confirmed, and the contrast is on file: components/projects/CostsTab.tsx:98 calls `computeCostRollup(accounts, entries, pctIndex)` with a real id-keyed map, and lib/projectSnapshot.ts:55 does the same — so the Costs tab and snapshot can show a CPI the printed report structurally cannot. Note the milestone query at projectReport.ts:47 doesn't even select `id`, so the index could not be keyed correctly as written."
  },
  {
    "id": "PM-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/projects/ProjectWizard.tsx:161 `if (accErr) console.warn(\"[wizard] budget lines not saved:\", accErr.message);`; :168-172 milestones insert ends `.then(() => undefined, () => undefined);` — the error is not even captured; :189/:192 `console.warn(\"[wizard] contractors not saved:\", ...)`. None of the three sets `setError`, and control falls straight through to :198-200 `await seedTurnoverItems({...}).catch(() => undefined); onCreated(); router.push(`/projects/${projectId}`);`. The catch at :202 only fires for a `createProject` throw, since every sub-insert swallows its own failure.",
    "note": "Confirmed exactly as claimed. Worth adding that the extended-fields update at :150-152 does the same for purpose/goals/success_criteria/setup_state (console.warn only) — so a wizard run can succeed visually while everything after the bare projects row is gone. cost_accounts writes are reachable for the creator (20261013:307-312 adds `cost_accounts_owner_write ... USING (user_owns_project(project_id))`), so this is a failure-path defect rather than a guaranteed one."
  },
  {
    "id": "PM-14",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "20260814_documents_delete_controllers.sql:31-40 — `CREATE OR REPLACE FUNCTION is_org_controller(p_org uuid) RETURNS boolean LANGUAGE sql STABLE SECURITY DEFINER AS $$ SELECT EXISTS (SELECT 1 FROM org_members WHERE uid = auth.uid() ...) $$;` — no `SET search_path`. This is the only CREATE of the function in the repo (grep over supabase/migrations for `FUNCTION is_org_controller` returns one hit), and it is the predicate in every cited policy: 20260906_projects_hardening.sql:62,68,76,95,124,171 and 20261013_project_controls_program.sql:238,246,264,269,274,281,286.",
    "note": "The factual claim is exactly right, and the deviation from the house convention is stark — the sibling helpers pin it (20261013:58 `... SECURITY DEFINER SET search_path = public`, whose own comment says \"search_path pinned per the house SECURITY DEFINER pattern\"; likewise 20260913:22,28,33,41). Lowering to LOW: exploitation requires a principal who can already CREATE a schema that precedes public on the role's search_path, which on a managed Supabase instance is an admin-level capability, so this is a defense-in-depth/linter-grade hardening gap (Supabase flags it as a WARN) rather than a reachable privilege escalation for any application role."
  },
  {
    "id": "PM-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260902_project_intake.sql:18-34 declares `project_id UUID NOT NULL` with no REFERENCES clause, and a grep of every migration + schema.sql for project_intake_links shows no later ALTER adding an FK (only assigned_doc_ids, purpose, rfq_group). lib/projects.ts:601-611 deleteProject nulls checkout_sessions.project_id and markup_requests.project_id, deletes milestones / project_activity / project_members, then `supabase.from(\"projects\").delete()` — project_intake_links is never touched. app/api/intake/resolve/route.ts:43-44 `const { data: project } = await supabaseAdmin.from(\"projects\").select(\"name\").eq(\"id\", link.project_id).maybeSingle();` with no null check, falling through to `projectName: (project?.name as string | null) ?? \"Project\"` (:159) and still building `items` from `assigned_doc_ids` (:105-120).",
    "note": "Survives. Both halves confirmed by repo-wide search: no FK, no cascade, no cleanup in deleteProject. The link keeps resolving and keeps listing the assigned documents' numbers/titles/revs; uploads do fail (\"Project not found.\", route.ts:227), but the org has also lost the only UI that could revoke the link, since the Intake tab lived on the deleted project page."
  },
  {
    "id": "PM-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/projectSnapshot.ts:49 `const live = msRows.filter((m) => (m.source as string | null) == null || m.source === \"manual\" || m.source === \"app\");` — 20260614_phase7_milestones.sql:60-61 constrains source to `CHECK (source IN ('manual','p6','msproject','csv'))`, so every imported row is filtered out. That `live` then feeds `milestoneCount: live.length`, `overdueMilestones`, `hasBaseline` (projectSnapshot.ts:114-121), which lib/projectHealth.ts:90 (`if (s.milestoneCount > 0)`), :174 (`if (s.milestoneCount === 0) add({ ... title: \"Add a schedule — import a file or type a few milestones\" })`) and :198 consume. lib/projectReport.ts:54 repeats the identical filter and :86 `milestones: live.map(...)`, so :146 prints `${d.milestones.length === 0 ? '<p class=\"muted\">No schedule loaded.</p>' : ...}`. ScheduleTab.tsx:122-125 states the opposite rule: \"Metrics still computed over ALL milestones — ghost rows ARE commitments from the imported schedule, so they count.\", and ScheduleImportModal.tsx:156 imports with `source: FORMAT_TO_SOURCE[parseResult.format]`.",
    "note": "Survives. The two consumers demonstrably disagree in-code, so this is an inconsistency rather than a deliberate policy: a fully imported P6 schedule shows on the Schedule tab with its overdue count while health scores it as absent, the coach permanently nags \"Add a schedule\", and the printed report asserts \"No schedule loaded.\""
  },
  {
    "id": "PM-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/projects.ts:389-399: `await supabase.from(\"checkout_sessions\").update({ status: \"checked_in\", ended_at: now, ... }).in(\"id\", ids).eq(\"status\", \"active\");` — the result is never destructured, so `error` is discarded. lib/projects.ts:8 `import { supabase } from \"@/lib/supabase\"` (the RLS-scoped browser client, so auth.uid() is the actor). 20260831_capability_policy_and_rails.sql:87-93: `IF OLD.status = 'active' AND NEW.status IS DISTINCT FROM OLD.status AND OLD.user_id::text <> auth.uid()::text AND NOT is_org_controller(OLD.org_id) THEN RAISE EXCEPTION ...` on a `BEFORE UPDATE ... FOR EACH ROW` trigger (:99-101), so one foreign row aborts the whole statement — including the actor's own sessions. RLS does not pre-filter those rows: schema.sql:1093-1094 `CREATE POLICY \"checkout_sessions_org_access\" ON checkout_sessions FOR ALL USING (org_id IN (SELECT my_org_ids()))` is org-wide. Meanwhile page.tsx:620-622 promises \"Every active checkout on this project will be released.\" and projects.ts:316 notifies \"Any active checkouts on the project were released.\"",
    "note": "Survives at HIGH. I specifically checked the refutation that RLS would silently drop the drafter's rows before the trigger fires — it does not, the policy is org-scoped FOR ALL, so the trigger sees them and raises. And no sweep repairs it later: autoReleaseExpiredAdHoc filters `.is(\"project_id\", null)` (lib/projects.ts:1044), so project-linked checkouts are the one class the maintenance cron never touches."
  },
  {
    "id": "PM-5",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "lib/projects.ts:587-593: `const { data: mem } = await supabase.from(\"org_members\").select(\"role\")...; const role = (mem as { role?: string } | null)?.role; if (role === \"Admin\" || role === \"DocCtrl\") { return ...; } throw new Error(\"Only the project owner or an admin can do this.\")` — `roles` is never selected. RLS disagrees: 20260814_documents_delete_controllers.sql:38 `AND (role IN ('Admin', 'DocCtrl') OR roles && ARRAY['Admin', 'DocCtrl']::text[])`. So does the UI: page.tsx:67-68 `const isAdmin = hasAnyRole([\"Admin\", \"DocCtrl\"]);` over the additive collection built at RoleContext.tsx:198-199 (`normalizeRoles(mem.roles, mem.role)`), with `hasAnyRole: (rs) => rs.some((r) => roles.includes(r))` (:369), and page.tsx:133-134 `const canManage = isOwner || isAdmin;`.",
    "note": "The divergence is real and confirmed, but the illustrative scenario is not reachable. org_members.role is kept mirrored to the *highest-ranked* held role — admin/users/page.tsx:129,137 `const headline = primaryRole(cleaned); ... .update({ roles: cleaned, role: headline })` — and ROLE_RANK puts DocCtrl at 70 above every Engineer level (61-64, lib/roleCapabilities.ts:74-86), so a headline-'Engineer' member holding DocCtrl additively cannot be produced. The bug bites only when a role that outranks DocCtrl is the headline — Manager (90), Supervisor (80), DraftingSupervisor (75) — plus additive DocCtrl. It is also fail-closed (a legitimate controller is blocked, no privilege is gained), so MEDIUM rather than HIGH."
  },
  {
    "id": "PM-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/projects.ts:604-611 deletes only checkout_sessions(detach), markup_requests(detach), milestones, project_activity, project_members, then `supabase.from(\"projects\").delete()`. The FK declarations do the rest: 20261013_project_controls_program.sql:117 `project_id UUID NOT NULL REFERENCES projects(id) ON DELETE CASCADE` (change_orders), :142 (project_checklists — checklist_items cascade off that at :156), :175 (turnover_items), :194 (punch_items); 20260819_orphan_tables_backfill.sql:141/162/182/205 same for project_parties, cost_accounts, cost_documents, cost_entries; 20260609_phase1_normalization.sql:69 same for project_documents. The dialog at app/(protected)/projects/[id]/page.tsx:407 says only \"This permanently removes the project and its schedule. Document checkouts are kept (just unlinked).\"",
    "note": "Confirmed on every leg. I looked for a BEFORE DELETE guard, an archive-instead-of-delete path, or a retention/legal-hold trigger on projects — none exists (20260826_legal_hold_delete_guard.sql attaches triggers only to `documents` and `document_versions`). The audit row at lib/projects.ts:612-616 carries `details: { name: p.name }` and no count of what was destroyed."
  },
  {
    "id": "PM-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260906_projects_hardening.sql:117-121 — `CREATE POLICY project_activity_insert ON project_activity FOR INSERT WITH CHECK (EXISTS (SELECT 1 FROM org_members WHERE org_id = project_activity.org_id AND uid = auth.uid() AND status = 'active'));`. No `user_id = auth.uid()`, no reference to project_id at all. 20260913_projects_rls_recursion_fix.sql:89-94 redefines only `project_activity_select` and `project_activity_delete` — it never touches the INSERT policy, so the 20260906 one stands. Rendering: components/documents/TimelineFeed.tsx:145-148 `{(event.userName || event.userEmail) && ... <span>{event.userName || event.userEmail}</span>}` — the row's own client-supplied `user_name`.",
    "note": "Verified the claim of absence repo-wide: no trigger on project_activity exists anywhere in supabase/migrations, and no later migration re-creates the insert policy. Because project_id is unchecked while org_id is the only thing validated, a member of org A can also plant a row against a project in org B (the SELECT policy then keys visibility off project_visible_to_me(project_id), so org B reads it)."
  },
  {
    "id": "PM-8",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "20260609_phase1_normalization.sql:192-197 — `CREATE POLICY \"project_documents_member_all\" ON project_documents FOR ALL TO authenticated USING (EXISTS (SELECT 1 FROM org_members WHERE org_id = project_documents.org_id AND uid = auth.uid() AND status = 'active')) WITH CHECK (same)`. FOR ALL, no visibility test, no owner/controller test. Repo-wide grep for project_documents policies returns only this and its identical twin at CATCHUP_2026-05-28.sql:475-476 — no restrictive policy narrows it. The UI gate is cosmetic: ProjectDocumentsCard.tsx:119-144 attach/detach are only rendered under `canManage` (page.tsx:461-471 passes `canManage={!!canManage}` = isOwner||isAdmin).",
    "note": "The authorization gap is real and exactly as described — any active org member, Viewer included, can INSERT or DELETE project_documents rows for any project including private ones, with no activity trail (the `doc_added`/`doc_removed` writes at ProjectDocumentsCard.tsx:111-117 only happen on the UI path). Lowering to MEDIUM because the blast radius is the link row and its provenance (source, first_seen_at), not any document: `documents` rows survive untouched, and nothing in the evidence pipeline reads project_documents (gatherProjectEvidenceState in lib/checklists.ts:238-294 sources titles from intake_collection_id, sow_document_id and turnover_items.document_id, never from project_documents)."
  },
  {
    "id": "PM-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/projects.ts:181-185 — `await supabase.from(\"projects\").update({ last_activity_at: now, updated_at: now }).eq(\"id\", input.projectId);` with no destructuring of `{ error }` at all. 20260906_projects_hardening.sql:60-65 — `CREATE POLICY projects_update_owner ON projects FOR UPDATE USING (is_org_controller(org_id) OR owner_user_id::text = auth.uid()::text)`. Sort: lib/projects.ts:204 `q = q.order(\"last_activity_at\", { ascending: false });`.",
    "note": "Confirmed, including the claim of absence: grep over supabase/ finds `last_activity_at` only at 20260527_projects_and_collaboration.sql:42 (column) and :52 (index) — no trigger and no RPC maintains it, so the client UPDATE is the only writer. For a non-owner non-controller the UPDATE matches zero rows under RLS (not an error), and the return value is discarded, so every collaborator-driven comment/attach/join leaves the timestamp stale."
  },
  {
    "id": "PR-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/orchestrator/execute/route.ts:46 `const role = (member.role as string) ?? \"Viewer\";` then :57-60 mints `approved: new Set([fingerprint(def.name, checked.values)])` — and the route itself never tests `role`. checkout_document has `if (!CONTROLLER_ROLES.includes(ctx.role))` (tools.ts:460); notify_personnel (474-518) and log_audit_completion (520-552) contain no role test at all — logAuditCompletion.run goes status-string check → proposal() → upsert.",
    "note": "Confirmed exactly as stated: 2 of 3 write tools have no role gate, the route supplies the approval itself, and the route's own comment ('this route grants confirmation, not authority', :56) is only true for checkout_document. lib/__tests__/apiRouteAuth.test.ts:132-150 exercises the end-to-end write with role 'Admin' and has no Viewer case, so nothing in the test suite would catch it. loop.ts:95-97 then feeds the forged row back to the next asker via check_audit_history."
  },
  {
    "id": "PR-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/drawingLocate.ts:34-35 'Omit any tag you cannot actually see. A guess is worse than an absence'. app/api/knowledge/locate/route.ts:282-288 caches unconditionally: `.update({ nx: pos.nx, ny: pos.ny, pos_source: \"vision\" })` for every parsed position, with no plausibility test. The refine loop crops around the coarse point itself (:247-248 `cx = ... pos.nx * base.width - cw / 2`), so a wrong neighbourhood is the search space for both passes.",
    "note": "The repo's own designed remedy is dead code: `buildRelocateUser` (lib/drawingLocate.ts:102), written for exactly the 'no pipe line-work near there → summary-row hit' case, has zero callers repo-wide. The only path that ever clears a cached vision point is a full re-ingest (lib/knowledgeIngest.ts:400-402 deletes the page's entities); no UI or API can correct one."
  },
  {
    "id": "PR-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/drawingText.ts:194 `const TB_DWG_RE = /(?:DRAWING|DWG|DRG)[.\\s]*(?:NO|NUMBER|#)[.:\\s]*([A-Z0-9][A-Z0-9\\-._]{3,24})/g;` — the guard at :190-192 only excludes the label-less form, and lib/__tests__/drawingText.test.ts:187 pins exactly that case ('CONT ON DWG 040-B-2002 SH 1' → null). A connector printed 'CONT ON DWG NO. 040-B-2002' matches. extractTitleBlock scans the WHOLE page text and takes the first hit (`drawingNumber = candidate; break;` at :209-210), and knowledgeVision.ts:38-47 asks for connectors ('CONT ON DWG 21-D-1105 SH 3') before the title-block lines. The result becomes the sheet's kind 'self' identity (lib/knowledgeIngest.ts:281-292), which auditDrawingRefs prefers over the filename (drawingText.ts:436-438) and then uses to drop refs at :487 `if (selfRefs.has(ref)) continue;` and :499 `if (targetId === docId) continue; // resolved to itself`.",
    "note": "Correct, and the gap is narrower than 'no guard' but real: the NO/NUMBER requirement stops the bare 'CONT ON DWG X' phrasing and nothing else, while 'CONT ON DWG NO. X' — ordinary on P&IDs — passes and wins by being first in the transcript. Nothing cross-checks the declared identity against the filename or against other pages of the same document."
  },
  {
    "id": "PR-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/ai/governedCall.ts:50-61 makes the agreement one of the five gates (`.from(\"ai_key_agreements\").select(\"id\")... throw new GovernedCallError(\"Accept the AI acceptable-use agreement first\", 428)`). app/api/flows/read/route.ts:106-107 says so in its own comment and then omits it: 'this route runs the same gate order inline: own key → provider allowlist → cap → call → meter' (:110-133 contains no ai_key_agreements read). app/api/knowledge/locate/route.ts:172-215 goes connection → ALLOWED_PROVIDERS → cap → callAiModel with a rendered page attached, no agreement. app/api/templates/generate/route.ts:177-202 does connection → ALLOWED_PROVIDERS → getMonthUsage/getCapUsd and nothing else before shipping row data. A repo-wide grep for ai_key_agreements lists only ask, orchestrator, codebook/import, knowledgeIngest, governedCall and the agreement route itself.",
    "note": "Confirmed by repo-wide search: none of the three routes touches ai_key_agreements, and each can be the first provider call a DocCtrl ever makes, so document pages and row data leave the workspace with no signed acceptable-use record."
  },
  {
    "id": "PR-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/bidTab.ts:190-224 validateParsedQuote checks only `if (total == null || total <= 0) throw` and never compares `total` to Σ lineItems.total; app/api/projects/cost-docs/route.ts:126-127 `patch.total_amount = quote.total;` with `status: \"parsed\"` (:123). lib/bidTab.ts:161 `const price = e.total > 0 && minTotal > 0 ? (minTotal / e.total) * 100 : 0;` and :183-184 `const top = Math.max(...); for (const s of scored) s.best = s.score === top && top > 0;`.",
    "note": "Confirmed — there is no reconciliation anywhere between the extracted bottom-line and the extracted line items, in the validator, the route, or computeBidEconomics (bidTab.ts:100-139, which only sums hours and labor totals). A misread total propagates untouched into the price part and the `best: true` stamp. Mitigation the finding does not mention: money never posts here (route.ts:8-10) and a person clicks Award, so the harm is a rigged comparison a reviewer trusts, not an automatic payment."
  },
  {
    "id": "PR-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20261016_reasoning_skills.sql:44-49 — `CREATE POLICY answer_skills_insert ... WITH CHECK (EXISTS (SELECT 1 FROM org_members m WHERE ... m.status = 'active') AND created_by = auth.uid());` — active membership only, no role predicate, and `visibility` is unconstrained by the policy. lib/answerSkillsServer.ts:29-31 `rows.filter((r) => r.enabled && (r.visibility === \"org\" || ...))` applies it to every asker; app/api/knowledge/ask/route.ts:1481-1483 folds it into `orgInstructions`, which is concatenated onto the SYSTEM prompt at :1530.",
    "note": "Confirmed. `grep -n \"role|Role|Admin|DocCtrl\" components/intelligence/SkillStudio.tsx` returns nothing — the 'Share org-wide' button (SkillStudio.tsx:265-269) has no authority check, and lib/answerSkills.ts:74-98 createAnswerSkill validates only that instructions are ≥40 chars. app/api/orchestrator/route.ts:117-120 loads the same block into the tool-driving agent. The only guard is the prose at answerSkillsServer.ts:44-45 ('they never override the citation and safety rules above'), which is an instruction, not a control."
  },
  {
    "id": "PR-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/knowledge/ask/route.ts:1055-1057 — `\"\\n\\nDRAWING FACTS — computed deterministically from EVERY sheet's extracted tags. TRUST these for counts and totals (the passages above are excerpts, never the whole picture):\"`. The tags behind it come from lib/knowledgeIngest.ts:214-217 (`if (visionRead) { for (const line of lines) { for (const hit of extractEquipmentTags(line)) ... kind: \"equipment\" ...`) where `lines` is a transcription by lib/knowledgeVision.ts:26-30 `VISION_MODEL = { anthropic: \"claude-haiku-4-5\", openai: \"gpt-4o-mini\", gemini: \"gemini-2.5-flash\" }`.",
    "note": "Confirmed: the aggregation is deterministic but its inputs are a cheap-tier OCR pass, and I read the whole DRAWING FACTS block (route.ts:1055-1100) — it carries caveats for unrecognized prefixes and unloaded references but nothing at all about vision provenance or transcription error, while explicitly telling the model to prefer the census over the passages. ask/route.ts:943-956 confirms the census reads knowledge_page_entities, the same table those vision-derived rows land in."
  },
  {
    "id": "PR-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/knowledge/ask/route.ts:1386-1389 — `` (legendBlock ? `\\n\\nP&ID LEGEND / DECODER SHEETS (owner-attached — authoritative for symbols, line codes, and abbreviations on these drawings):\\n${legendBlock}` : \"\") `` where legendBlock is built at :1369-1383 straight from `knowledge_chunks.content`, and the containing `standing` string is concatenated into `baseAnswerSystem` (:1518) which is passed as `system:` at :1530.",
    "note": "Confirmed: raw chunk text from an attached PDF lands inside the SYSTEM prompt, undelimited, under an 'authoritative' label — strictly higher privilege than the numbered passages in the user turn. lib/knowledgeText.ts:17-31 sanitizeStorageText strips only lone surrogates and C0 controls, so no instruction-bearing text is removed anywhere in the chain. Legend ids come from library AI feature settings plus the site codebook (route.ts:151-154), matching the finding's description of who can plant one; the analogous undelimited splice in lib/orchestrator/loop.ts:108-119 is correctly cited too."
  },
  {
    "id": "PR-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/templates/generate/route.ts:252-254 — `} catch { return Object.fromEntries(aiFields.map((f) => [f.tag, \"\"])) as Record<string, string>; }` — no throw, no flag, no counter. The route returns `{ documents, ... }` (:304-313) with no indication that a row was blanked.",
    "note": "Confirmed, and the same silent-blank happens without ever reaching the catch: :242 `const json = text.startsWith(\"{\") ? text : (text.match(/\\{[\\s\\S]*\\}/)?.[0] ?? \"{}\")` parses \"{}\" cleanly and :246-249 then writes \"\" for every field. The review screen (components/templates/GenerateModal.tsx:279-317) is a collapsed one-at-a-time accordion whose row shows only the filename — an empty AI field is invisible unless that document is expanded, and nothing marks which ones are empty."
  },
  {
    "id": "PR-7",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "app/api/flows/read/route.ts:162 `evidence: { docName: doc.name, confidence: Math.max(0, Math.min(1, Number(f.confidence ?? 0.5))) }` — the only use of the value; there is no threshold, no sort, no status branch. lib/processFlows.ts:26 types it out of existence for every client: `evidence: { docName?: string; note?: string } | null;`. The one render site, components/assets/UnitOpsPanels.tsx:203-205, shows only `{f.evidence?.docName && ... from {f.evidence.docName}}`. A repo-wide grep for `confidence` returns no reader of process_flows.",
    "note": "Factually correct — the score is written and never read anywhere in the repo, and settled at :150 blocks re-proposal of the pair. Severity lowered because every AI row lands status='proposed' (:158) and only a human can promote it (decideFlow), so the missing signal costs review ergonomics, never a bad edge in the topology."
  },
  {
    "id": "PR-8",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "The try/catch closes at app/api/flows/read/route.ts:138; the parse sits outside it at :140-141 `const block = extractJsonBlock(text); const parsed = block ? (JSON.parse(block) as { flows?: unknown[] }) : null;`. The meter already ran at :133 `await recordAskUsage({... op: \"flowRead\" })`. No wrapper catches it, so Next returns an opaque 500.",
    "note": "The unguarded JSON.parse after a billed call is real. Severity lowered: lib/orchestrator/protocol.ts:32-52 returns null for unbalanced output ('unbalanced — truncated output, usually a token limit'), which is the common malformed case, so only balanced-but-invalid JSON throws; the blast radius is one wasted call and a generic error for one admin, with nothing written and no state corrupted."
  },
  {
    "id": "PR-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/knowledge/ask/route.ts:1442-1450 instructs '(3) substitute and compute step by step; (4) state units, and end with a **Check:** naming the table cells to verify.' The only post-processing of `answer` is the invented-citation strip at :1573-1586 (`const invented = used.filter((n) => n < 1 || n > chunks.length);`). Greps for mathjs / evaluate( / any numeric re-computation over the answer text return nothing across app/ and lib/.",
    "note": "Confirmed by absence: nothing recomputes, range-checks, or unit-checks the model's arithmetic, and the answer is persisted verbatim (:1739-1744) and rendered like any other. The mandatory 'Check:' line (rendered as its own block at app/(protected)/knowledge/[id]/page.tsx:379) only points the reader at table cells — it does not verify the substitution, so it is not a compensating control for a units slip."
  },
  {
    "id": "PROD-1",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "The 26/48 count is exact — enumerating the NotificationKind union in lib/inAppNotifications.ts against the `case` arms of sectionForKind (hooks/useTicketNotifications.ts:71-103) yields 48 kinds, 22 mapped, 26 hitting `default: return 'other';` (:99-100), including every ack_*, review_*, legal_hold_*, retention_eligible and access_recert_due. hooks/useTicketNotifications.ts:284 `const section = sectionForKind(n.kind);` → :302 `tally(section, false);` and Sidebar.tsx:229-235 reads only .documents/.projects/.requests. The cited producer is real: lib/acknowledgments.ts:501 `orgId: input.orgId, userId: r.assignee_user_id as string, kind: \"ack_requested\",`.",
    "note": "The claim is accurate and precisely counted. Downgraded from HIGH to MEDIUM because the summary's 'the user's legal obligation to read' framing overstates the blast radius: the notification is NOT swallowed — hooks/useTicketNotifications.ts:311 exposes `count: items.length` over the full merged feed, so the bell badge, NotificationCenter, and app/(protected)/inbox/page.tsx:36 (which consumes the same `items`) all surface ack_requested. Only the per-section sidebar badge is missing, and document-level AckSection/AckPill surfaces exist independently. Consistent with OS-12, which grades the identical defect MEDIUM."
  },
  {
    "id": "PROD-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/storageAlerts.ts:60-61 `await sb.from(\"notifications\").insert({ org_id: s.org_id, user_id: a.uid, kind: \"storage_alert\", ... })` and lib/storageUsage.ts:255-256 `insert({ ... kind: \\`storage_${alert.key}\\`, ... })` — a template literal. Neither string appears in the NotificationKind union (lib/inAppNotifications.ts:10-58). The migration allows it: 20260621_in_app_notifications.sql:17 `kind TEXT NOT NULL,` with no CHECK constraint.",
    "note": "Confirmed — both bypass notify()/emit() with raw inserts, and the untyped `kind` column means they insert successfully rather than failing loudly. Downstream they fall through sectionForKind's `default: return 'other'` (useTicketNotifications.ts:100-101) and miss KIND_ICON (NotificationBell.tsx:19-44), so the generic-icon/no-badge rendering described is accurate."
  },
  {
    "id": "PROD-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "`grep -n 'notify\\|emit\\|queueEmail\\|notifyMany' lib/milestones.ts` returns zero matches across the whole 61KB file, which exports 17 functions including createMilestone:155, updateMilestone:233, applyMilestoneMoves:293, setMilestoneStatus:346, setMilestoneProgress:456, deleteMilestone:565, rebaseSchedule:1155, setBaseline:1462, clearBaseline:1504.",
    "note": "Absence verified for the whole module, not just the cited lines. lib/inbox.ts:154-158 does pull open/overdue milestones into the /inbox cockpit, so a slip is discoverable there as well as on the Schedule tab — but that is a pull surface, not a notification, so the claim stands."
  },
  {
    "id": "PROD-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "`grep -n 'notify\\|emit(\\|queueEmail\\|ack_complete' lib/transmittals.ts` returns zero matches. acknowledgeTransmittal (lib/transmittals.ts:593-620) ends at `await logAuditAction({ action: \"TRANSMITTAL_ACKNOWLEDGED\", ... })`. The portal path, by contrast, does both: app/api/transmittal/route.ts:148-157 inserts `kind: \"ack_complete\"` for `t.created_by`, then queues an email at :168-180.",
    "note": "The asymmetry is exactly as described. issueTransmittal (:557-590) calls sendTransmittalEmail, but that is the outbound message to the external recipient, not an internal bell/email row — so 'issue writes no internal notification' holds too."
  },
  {
    "id": "PROD-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Repo-wide grep for `doc_superseded` gives exactly the eight producers cited: postPublish.ts:39 (rev-up FYI), postPublish.ts:177 (work-package retirement), distributionAcks.ts:138 ('Please confirm: … tap \"I have this revision\"'), distributionAcks.ts:322 (ack re-nudge), staleCopies.ts:198, revisionImpact.ts:138, workPackages.ts:279, app/api/intake/upload/route.ts:351. The union already carries a purpose-built `ack_requested` kind at inAppNotifications.ts:42 that these ack paths do not use.",
    "note": "Confirmed, including the rendering consequence: `const actionKinds = new Set(['checkout_conflict','checkout_released','overlap_advisory','branch_open'])` (useTicketNotifications.ts:279) excludes doc_superseded, so a PSM ack obligation renders with the same GitBranch icon (NotificationBell.tsx:35) and no 'Action needed' flag as an informational rev-up notice."
  },
  {
    "id": "PROD-14",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/markupRequests.ts:47-102 — the insert, then `if (input.projectId) { await writeActivity(...) }` (line 68), then logAuditAction, then `return rowToMarkupRequest(...)`. No notify/emit anywhere in the function. The sole caller, components/documents/MarkupRequestModal.tsx:45, adds none either (grep for notify/emit in that file returns nothing). The `markup_request` kind is never emitted repo-wide — its only hits are the union (:26), NotificationBell.tsx:34, and useTicketNotifications.ts:85; lib/markupRequests.ts:148 is a `resourceType`, not a kind.",
    "note": "Claim verified including the project-feed conditional. One mitigation the finding omits: lib/inbox.ts:149-152 loads `markup_requests` where `requested_from_user_id = userId AND status = 'open'` into the /inbox cockpit, and the page can answer them (respondToMarkup), so the request is not entirely invisible — but only to someone who opens /inbox unprompted."
  },
  {
    "id": "PROD-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/auth/request-access/route.ts:46-59 — `const { error: insertError } = await supabaseAdmin.from(\"access_requests\").insert({ org_id: orgId, ... status: \"pending\", ... }); if (insertError) {...} return NextResponse.json({ ok: true, orgName: orgRealName });` — the route ends at the insert. No notify(), notifyMany(), emit(), or queueEmail() anywhere in the file.",
    "note": "Repo-wide grep for `access_requests` returns only this route plus lib/schemaExpectations.ts:31, lib/dataRestore.ts:315, lib/exportTables.ts:91 and the migration — no producer and no reader. The finding's own summary is actually too charitable: it says an admin can find it by 'navigating to the members/access screen', but no such screen exists, so the row is unreachable from the UI entirely."
  },
  {
    "id": "PROD-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/branches.ts:148 (open) `audience: { involved, roles: [\"DocCtrl\"] }` vs lib/branches.ts:213 (resolve) `audience: { involved: [branch.createdBy] }`. Grep for `branch_resolved` repo-wide hits only lib/branches.ts:207, the union at lib/inAppNotifications.ts:31, and the two UI maps (NotificationBell.tsx:39, useTicketNotifications.ts:92) — no second emitter.",
    "note": "Confirmed asymmetry, and it is worse than stated: resolveBranch passes `actorUserId: input.actorUserId`, which dispatch.ts:77 (`ids.delete(input.actorUserId)`) strips — so when the brancher resolves their own branch the recipient set is empty and emit() returns at dispatch.ts:84 before writing anything. The stale branch_open row is also not auto-reconciled: the cleanup at useTicketNotifications.ts:188-209 only touches rows carrying `metadata.status` + `metadata.action`, and announceBranchOpened's metadata is `{ branchId }` only."
  },
  {
    "id": "PROD-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Exactly 17 emit() call sites exist (distributionAcks 135/319, staleCopies 195, orchestrator/tools 507, holds 238, postPublish 36/50, projects 696, branches 138/204, revisionImpact 135, transitionIn 339, workPackages 276, checkoutEpisodes 667, requests/new 342, documents/[libraryId]/page 2303, CheckInPanel 401). Only 4 pass `followers: true` — holds.ts:252, postPublish.ts:46, postPublish.ts:60, page.tsx:2313. The remaining 13 pass only `involved`/`roles`. A grep for `projectId` inside every `audience: {` block returns zero hits.",
    "note": "The 13-of-17 count is exact, and `audience.projectId` is genuinely never supplied, which makes resolveProjectMembers (lib/notify/recipients.ts:65-72) reachable only through a branch nothing takes — its sole references are the import and the call at dispatch.ts:74. Cited lib/ticketTransitions.ts:130-132 is not an emit() site but does hardcode recipients (`newUnreadBy = [ticket.requesterId, ticket.assignedDrafterId]`), consistent with the claim."
  },
  {
    "id": "PROD-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/documents/CsvImportModal.tsx:166-196 inserts each row via `supabase.from(\"documents\").insert({...})` and then only `onImported?.(ok); nudgeKnowledgeSources(orgId, library.id!)`. The staged path declares `notifyLibrarySubscribers` at page.tsx:2300 with `kind: \"library_doc_added\", audience: { followers: true }` (2313) and calls it at page.tsx:2508. The CSV modal's handler at page.tsx:3142-3145 is `onImported={() => { setShowCsvImport(false); setDocsRefreshTick((t) => t + 1); }}`.",
    "note": "Confirmed: notifyLibrarySubscribers is a closure declared inside handleStagedUpload (page.tsx:2299-2315), so it is not even in scope for the CSV path, and the onImported handler does no notification of any kind. Library watchers get nothing from a CSV import."
  },
  {
    "id": "PROD-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "`grep -c 'notify\\|emit(\\|queueEmail'` returns 0 for every cited file: lib/changeOrders.ts, lib/costs.ts, lib/checklists.ts, lib/turnover.ts, lib/companies.ts, lib/equipmentBridgeServer.ts, lib/documentShares.ts. The one related API route, app/api/projects/checklist/route.ts, also has zero matches for notify/emit/notifications.",
    "note": "Claim of absence verified repo-wide, including the punch-item code (which lives in lib/turnover.ts and lib/companies.ts, both zero) and the checklist API route. No caller-side compensating notification was found."
  },
  {
    "id": "PROD-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "`countUnread` grep returns exactly one hit — its own definition at lib/inAppNotifications.ts:176. No emit() call site passes `channels` (grep for `channels:` across lib/app/components hits only components/viewers/PdfRevisionDiff.tsx). The 50-cap consequence is real: useTicketNotifications.ts:176 `listMyNotifications({ onlyUnread: true, limit: 50, orgId: activeOrgId })` and line 312 `count: items.length`.",
    "note": "Two of three sub-claims are exactly right and the >50-unread undercount consequence is confirmed. One correction: `resolveRecipients` is not literally callerless — it has one internal caller at lib/notify/dispatch.ts:83 (`const recipients = await resolveRecipients(input)`); what is unused is its documented external 'preview whom-would-this-notify' purpose (dispatch.ts:65-66)."
  },
  {
    "id": "PROD-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Quoted-string grep across all .ts/.tsx/.sql for each kind returns only the union declaration and the UI maps, never a producer: `task_overdue_digest` → inAppNotifications.ts:33 + useTicketNotifications.ts:81 + NotificationBell.tsx:41; `morning_digest` → :34 + :82; `task_nudge` → :35 + :80; `task_reminder` → :36 only (not even a UI map); `checkout_handoff` → :16 + useTicketNotifications.ts:87 + NotificationBell.tsx:26.",
    "note": "All five kinds confirmed emitter-free. The checkout_handoff half is also confirmed downstream: CheckInPanel.tsx:423-428 routes handoff_release through postHandoff() rather than emitting checkout_handoff, so the Lock icon at NotificationBell.tsx:26 is unreachable."
  },
  {
    "id": "PROD-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/holds.ts:252 `audience: { followers: true, roles: [\"Admin\", \"DocCtrl\"] }` — no owner term — against the union's contract at lib/inAppNotifications.ts:24 `| \"hold_opened\" // a hold was opened on a doc the user owns / is on the project for`. Ownership creates no subscription: lib/ownership.ts:setOwner writes only `owner_user_id`/`owner_name` on the row plus a one-off `owner_assigned` notify; the only writes to the `subscriptions` table are in lib/subscriptions.ts (the explicit Watch button).",
    "note": "Verified there is no back door putting an owner in the follower set — no ownership code path inserts into `subscriptions`, so resolveFollowers (recipients.ts:23-45) cannot pick the owner up. Ironically setOwner's own body text promises 'You'll receive its notifications and review reminders.'"
  },
  {
    "id": "PROJ-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "storage.ts:452-459 makeTicketAttachmentPath → `joinPath(\"orgs\", orgId, \"tickets\", ticketId, ...)`, used by uploadTicketAttachment (:462-471) and by the IFC/Final upload at requests/[id]/page.tsx:1078 and :1010 — the deliverable only ever lands as a JSONB attachment row. A repo-wide grep for `from('documents').insert` / createDocumentWithFile / createNewDocWithFirstVersion / revUpDocument shows callers only in the documents UI, split/merge/setRevUp, DocumentLinkPicker and outputTemplates — none in app/(protected)/requests or app/api/tickets. schema.sql:334 even reserves `related_ticket_id UUID` on document_versions, and grep shows it is only ever READ (revisions.ts:958, documents/[libraryId]/page.tsx:1893), never written.",
    "note": "The absence claim holds under repo-wide search: no drafting-flow path writes documents or document_versions, so the approved Final PDF gets no document number, revision chain, supersession or hold coverage — while requests/[id]/page.tsx:647 stamps that very download 'CONTROLLED COPY'. Slight overstatement in the summary: the portal does keep an autonomous deliverable rev chain (20260827_ticket_deliverable_rev.sql), a /verify-ticket QR (:636-651) and download_audits rows (:675-683), so 'no distribution record' is too strong; the controlled-document gap itself is real."
  },
  {
    "id": "PROJ-10",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "supabase/schema.sql:840 `linked_ticket_id UUID REFERENCES tickets(id),` on checkout_sessions. Repo-wide `grep -rn \"linked_ticket_id|linkedTicketId\"` over .ts/.tsx yields only reads for that table — lib/projects.ts:354 `linkedTicketId: r.linked_ticket_id as string | undefined` in rowToCheckoutSession and documents/[libraryId]/page.tsx:1968 — while every write goes to a different table (lib/projects.ts:112 projects, lib/milestones.ts:171 milestones). The two checkout inserts named in the finding confirm it: CheckoutFlowModal.tsx:317-325 and lib/projects.ts:945-965 build sessionRow with org_id/document_id/project_id/purpose/… and no linked_ticket_id.",
    "note": "Both halves check out: nothing writes checkout_sessions.linked_ticket_id, and milestones.linked_ticket_id has a writer (milestones.ts:171) whose only caller, components/projects/ScheduleTab.tsx, never passes linkedTicketId (grep finds no such property anywhere under components/). Severity lowered: this is an unwired column with no incorrect behavior — no data is lost or corrupted, a query simply returns nothing."
  },
  {
    "id": "PROJ-11",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/reviewControl.ts:55-61 `export function effectiveModeForRevUp(input: { control; changeType; relatedTicketId }) { … if (input.relatedTicketId) return \"none\"; return input.control.mode; }`. Repo-wide, related_ticket_id/relatedTicketId appears only in schema.sql:334, two row-mappers (revisions.ts:958, documents/[libraryId]/page.tsx:1893), the type (schema.ts:774) and a unit test — no INSERT/UPDATE writes it. Crucially, NEITHER call site passes the field: RevUpModal.tsx:210 `effectiveModeForRevUp({ control: reviewControl ?? { mode: \"none\" }, changeType })` and setRevUp.ts:83 `effectiveModeForRevUp({ control, changeType })`.",
    "note": "The dead column and the bypass branch both exist, so the design smell is real. But the stated mechanism is overstated: because both call sites omit relatedTicketId, stamping the column on a version would change nothing — a second, separate change (threading the version's relatedTicketId into effectiveModeForRevUp) would be required before any review gate could be skipped. Latent hazard, not a live or one-commit-away bypass — LOW."
  },
  {
    "id": "PROJ-12",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/transitionIn.ts:321-329 writes `metadata: { source_document: {...}, intake_collision: { projectId, intakeLinkId, … } }`; the only other reference to intake_collision in the repo is app/api/intake/upload/route.ts:154 `const meta = (ticket.metadata ?? {}) as { intake_collision?: { intakeLinkId?: string | null } }` — it reads intakeLinkId, never projectId. IntakePanel.tsx:283-285 `setMsg(res.ok ? \\`${candidate.label} flagged to drafting — ticket ${res.ticketNumber} is in the assignment queue.\\` : …)` — plain text, no href.",
    "note": "Both specifics are accurate (projectId is written and read by nothing; the toast carries a ticket number but no link). Severity lowered because the association is not actually lost — it is persisted on the ticket row (metadata.intake_collision.projectId) and duplicated into audit_logs details (transitionIn.ts:362), so a future surface can recover it; the ticket itself is also routed and linked for its recipients via emit(link: `/requests/${row.id}`, :344). That is strictly less harmful than PROJ-4, where the id is discarded outright."
  },
  {
    "id": "PROJ-13",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/projects.ts:112 `linked_ticket_id: input.linkedTicketId || null` is the only writer; the only caller that supplies a non-null value is convertTicketToProject (lib/projects.ts:807 `linkedTicketId: ticket.id as string`), and `grep -rn convertTicketToProject --include=*.ts --include=*.tsx .` returns exactly one line — its own definition at :781. The other three createProject call sites (ProjectWizard.tsx:128, CheckoutFlowModal.tsx:288, projects.ts:893) pass no linkedTicketId, so the chip at projects/[id]/page.tsx:308 `{project.linkedTicketId && (` can never render.",
    "note": "Confirmed — zero call sites, so the column is always NULL. Severity lowered: this is a never-invoked feature plus a conditional that never fires (no wrong data, no wrong behavior), the same class as PROJ-10."
  },
  {
    "id": "PROJ-2",
    "verdict": "REFUTED",
    "corrected_severity": null,
    "evidence": "supabase/schema.sql:906 `linked_ticket_id UUID,` on the projects table; lib/projects.ts:807 `linkedTicketId: ticket.id as string,` inside convertTicketToProject (:776-807, 'links back to the ticket so the ticket page can show the linked project'); lib/projects.ts:112 `linked_ticket_id: input.linkedTicketId || null` on createProject; app/(protected)/projects/[id]/page.tsx:308-309 `{project.linkedTicketId && (<Link href={`/requests/${project.linkedTicketId}`} ...>`; lib/milestones.ts:64/171 `linked_ticket_id` on project milestones.",
    "note": "The claim that 'the boundary does not exist in the schema' and that there is 'no field, no join row, and no query that can associate the two' is false — projects.linked_ticket_id (and project_milestones.linked_ticket_id) is a first-class association, written by the ticket→project conversion flow and rendered as a live link from the project to the request. Only the narrower reverse-direction gap is true (the request detail page never renders its linked project, and nothing queries projects by linked_ticket_id), which does not support the stated finding or its HIGH severity."
  },
  {
    "id": "PROJ-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260609_phase1_normalization.sql:194-197 `CREATE POLICY \"project_documents_member_all\" ON project_documents FOR ALL TO authenticated USING (EXISTS (SELECT 1 FROM org_members WHERE org_id = project_documents.org_id AND uid = auth.uid() AND status = 'active')) WITH CHECK (...)`. A repo-wide grep for project_documents policies returns only this file and CATCHUP_2026-05-28.sql:475-479 (identical) — 20260906_projects_hardening.sql hardens projects, project_members, project_activity, markup_requests, cost tables and project_intake_links but never mentions project_documents. The only app-side gate is client-side: ProjectDocumentsCard.tsx:222 `{canManage && (<button onClick={() => void detach(r)}` with canManage computed in the browser at projects/[id]/page.tsx:134 `const canManage = isOwner || isAdmin;`, and detach issues a bare `supabase.from(\"project_documents\").delete().eq(\"id\", r.linkId)` (:138).",
    "note": "Confirmed: no later migration, RESTRICTIVE policy, or trigger constrains project_documents (the only trigger on it is checkouts_resync_project_documents, an INSERT/UPDATE resync), so any active org member — including a Viewer and a non-member of a private project — can SELECT, INSERT and DELETE its rows. Partial mitigation worth recording: rows with source='checkout' are re-created the next time a checkout row for that project/document is inserted or updated, so only manual attachments are permanently lost."
  },
  {
    "id": "PROJ-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "CheckInPanel.tsx:56 `mySession: CheckoutSession;` (types/schema.ts:929 `projectId?: string; // nullable: ad-hoc checkouts have none`), and the ticket insert at :236-267 writes `metadata: { source_document: {...}, checkin: { episodeId: episode?.id ?? null, checkoutNumber: episode?.seq ?? null, purpose: mySession.purpose ?? null, outcome: card.outcome }, ... }` — it reads mySession.purpose one field away from mySession.projectId and never stores the project. `grep -n \"project\" components/documents/CheckInPanel.tsx` returns zero hits.",
    "note": "Confirmed — the panel holds the project id and drops it, and tickets have no project_id column at all (the only project link any ticket carries is transitionIn.ts:323 metadata.intake_collision.projectId). One indirect route the finding does not mention: the ticket does carry metadata.source_document.id, and lib/impact.ts:117 `.eq(\"metadata->source_document->>id\", documentId)` surfaces it on the document's Impact panel, which is reachable from the project's Documents card — but nothing on the project page itself shows it."
  },
  {
    "id": "PROJ-5",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "app/api/intake/upload/route.ts:285-296 inserts the documents row and :303-320 the document_versions row; the file contains no `project_documents` reference at all (grep). BUT lib/transitionIn.ts:221-227 `await supabase.from(\"project_documents\").upsert({ org_id: input.orgId, project_id: input.projectId, document_id: input.docId, source: \"manual\", ... })` — comment: \"Keep the project tracking the document after it leaves the intake folder.\"",
    "note": "The title's \"never\" is false: adoptDocument() does create the project_documents row when an intake sheet is transitioned into the controlled register. Also, submissions are not invisible on the project page — components/projects/IntakePanel.tsx renders a \"Submissions awaiting review\" queue for them. The real, narrower defect (an un-adopted intake document is absent from project_documents, hence from ProjectDocumentsCard, getProjectTimeline's linked-doc pull at timeline.ts:423-426, and loadProjectBundle at projectExport.ts:41) stands, but it is a register/export completeness gap, not a lost document — LOW."
  },
  {
    "id": "PROJ-6",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "projects/[id]/page.tsx:627-632 lists exactly four gateLines (punchOpen, turnoverRequired/Accepted, checklistOpenItems+NeedsEvidence, openChangeOrders) and :646 renders `\"You can complete anyway — the open items stay on the record and in the report.\"`. gatherProjectSnapshot (lib/projectSnapshot.ts:20-44) queries projects, cost tables, change_orders, milestones, project_checklists, turnover_items, punch_items, project_intake_links, project_members — `grep -n tickets lib/projectSnapshot.ts` returns nothing. transitionProjectStatus (lib/projects.ts:259) only asserts canManage and then releases checkouts (:300).",
    "note": "Factually confirmed — no ticket gate exists and the snapshot never queries tickets (nor could it usefully, since no ticket carries a project id). Severity is too high, though: every existing gate is advisory, not blocking (page.tsx:646 explicitly says the owner can complete anyway), so the missing gate would only add one more warning line — it removes no enforcement that exists today."
  },
  {
    "id": "PROJ-7",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/orgGraph.ts:23 `export type GraphNodeType = \"document\" | \"asset\" | \"unit\" | \"library\" | \"project\" | \"plant\" | \"plot\";` and :25-33 the GraphEdgeType union (tag/unit/library/project/related/supersession/proposed/mention/flow/plot). `grep -n ticket lib/orgGraph.ts` returns zero hits.",
    "note": "The absence is confirmed. But the header's \"every persisted relationship\" is immediately followed (lines 5-15) by an explicit enumeration of the eight relationship tables it draws, and there is in fact no persisted ticket↔document or ticket↔project relationship row anywhere to draw — every ticket link lives in a metadata JSON blob (metadata.source_document, metadata.intake_collision). A missing node type in a visualization with no underlying edge rows is a LOW-severity coverage gap, not a MEDIUM correctness defect."
  },
  {
    "id": "PROJ-8",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "ProjectDocumentsCard.tsx:95-101 searches `.from(\"documents\").select(\"id, document_number, title, name\").eq(\"org_id\", orgId).or(...)` with no status predicate, and the picker rows (:181-187) render only `{r.label}` — no status. But the register row DOES show it: :211 `{r.status && <span …>{r.status}</span>}`, and the CSV prints it too — projectExport.ts:81 `csvRow([\"Doc Number\", \"Title\", \"Rev\", \"Status\", \"Library ID\"])` with :83 emitting `d.status`.",
    "note": "The guard genuinely is missing at pick time (compare lib/impact.ts:96 and lib/transitionIn.ts:167, which both `.filter((d) => d.status !== \"Superseded\" && d.status !== \"Archived\")`). But \"no warning\" is only true of the picker: the attached row carries a SUPERSEDED badge and the export carries a Status column, so a reviewer is not handed an unlabelled superseded drawing. LOW."
  },
  {
    "id": "PROJ-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "requests/new/page.tsx:289 `if (Object.keys(customValues).length > 0) metadata.custom_categories = customValues;` and :290-297 `metadata.source_document = {...}`. On the detail page, requests/[id]/page.tsx:904 maps `metadata: (r.metadata as Record<string, unknown> | null) ?? undefined` and the only consumer in the whole 2000-line file is :1691 `const sum = (ticket.metadata?.archive_summary ?? {}) as {` — grep for source_document / custom_categories / intake_collision in that file returns nothing, and the four components in components/requests/ contain no `metadata` reference at all.",
    "note": "Confirmed. A repo-wide grep for custom_categories finds exactly two hits: the writer (requests/new:289) and a comment in admin/requests/page.tsx:361 describing where the values are stored — no reader anywhere, so admin-defined required fields are collected on every request and never displayed. metadata.source_document is at least read server-side (lib/impact.ts:117, workflow-action route:231), but not on the request page itself."
  },
  {
    "id": "QUAL-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/checklistEngine.ts:146-156 — `const proof = rule.probe(state); if (proof) { ... status: \"satisfied\" ... } else if (item.status === \"open\") { out.push({ id: item.id, status: \"needs_evidence\", addedEvidence: [] }); }`. When the proving document is gone, `proof` is null and the item's status is `\"satisfied\"`, not `\"open\"`, so the else-if is skipped and NOTHING is emitted — the item silently keeps its green. The result type cannot express a retraction: :59-63 `export interface AutoEvidenceResult { id: string; status: \"needs_evidence\" | \"satisfied\"; addedEvidence: Array<{ label: string; source: \"auto\" }> }`. Untraceable citation: the auto label is built at :123 `return `Document on file: \"${t}\"`;` and written verbatim at lib/checklists.ts:316 `if (r.addedEvidence.length > 0) patch.evidence = [...item.evidence, ...r.addedEvidence];` — `addedEvidence` has no documentId field, even though the column shape at lib/checklists.ts:51 has an optional `documentId?`.",
    "note": "Every leg holds. Line 143 (`if (item.status === \"satisfied\" && item.evidence.some((e) => e.source === \"manual\")) continue;`) confirms auto-satisfied items ARE re-swept — so the sweep sees the missing proof and still declines to retract. I also found the voiding case is worse than stated: the register query at lib/checklists.ts:258 (`.select(\"title, name, document_number\").eq(\"collection_id\", collectionId)`) filters on nothing at all, so a Voided or superseded document keeps supplying live evidence indefinitely."
  },
  {
    "id": "QUAL-10",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/evidencePack.ts:147-154 issues exactly five reads — projects, project_members, milestones, audit_logs, transmittals — and :218-228 renders exactly four sections (Team, Schedule, Transmittals, Audit trail). No query or section touches project_checklists, checklist_items, turnover_items or punch_items. MITIGATIONS: :193-194 renders every audit row's `details` JSON, which for TURNOVER_REVIEWED carries `{ itemId, name, status, note }` plus actor email and timestamp (turnover.ts:208-210); and the adjacent 'Report' button (page.tsx:348-356) runs lib/projectReport.ts:45-48,70-79,93-95, which does gather turnover progress, per-checklist satisfied/applicable counts and open punch count.",
    "note": "The structural claim holds — the pack has no quality sections. But 'contains no turnover item and no punch item' is literally false: the audit-trail section carries TURNOVER_REVIEWED / CHECKLIST_* / PUNCH_* rows with item name, status, actor and date, and a sibling one-click Report covers the quality rollups."
  },
  {
    "id": "QUAL-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/projects/QualityTab.tsx:570-581 — the action set is gated `it.status === \"open\"` (Received), `(it.status === \"received\" || it.status === \"rejected\")` (Accept), `it.status === \"received\"` (Reject), `(it.status === \"open\" || it.status === \"received\")` (Waive). An `accepted` row matches none, so it renders no controls at all; lib/turnover.ts:190-212 accepts any TurnoverStatus, so only the UI is one-way. computeTurnoverProgress (turnover.ts:225) keeps counting it: `req.filter((i) => i.status === \"accepted\" || i.status === \"waived\")`. lib/checklistEngine.ts:176 `{ key: \"ncr\", label: \"Nonconformance handling\", … }` is the ONLY occurrence of 'nonconformance' in lib/, app/, components/ or supabase/ — no NCR table, type or surface exists.",
    "note": "Confirmed by repo-wide search: no reopen/revoke path, no 'acceptance withdrawn' field, no audit action for one, and no nonconformance record anywhere despite the rubric scoring contractors on having an NCR process. Severity MEDIUM is right."
  },
  {
    "id": "QUAL-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20261013…:276-277 `WITH CHECK (is_org_controller(org_id) OR EXISTS (SELECT 1 FROM project_checklists c WHERE c.id = checklist_items.checklist_id AND user_owns_project(c.project_id)))` — the second disjunct validates the PARENT's project, never `checklist_items.org_id = c.org_id`. Read side, :256: `%I_member_read … USING (EXISTS … m.org_id = checklist_items.org_id … status='active')` keys on the row's own org_id. lib/checklists.ts:131 `org_id: input.orgId` is caller-supplied. Contrast the three sibling tables (:280-288), all checked as `user_owns_project(project_id)` on their own column.",
    "note": "Correct as written, and worse than stated: lib/exportTables.ts:104 lists checklist_items among ORG_SCOPED_TABLES 'dumped by org_id', so injected rows would be pulled into workspace B's backup. In-app exposure is narrower than the summary implies — listChecklistItems (checklists.ts:103-104) and projectSnapshot.ts:80-82 both query by checklist_id, never by org_id, so org B's UI would not surface them."
  },
  {
    "id": "QUAL-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/turnover.ts:199 `if (input.documentId !== undefined) row.document_id = input.documentId;` against its ONLY caller, components/projects/QualityTab.tsx:531 `const res = await reviewTurnoverItem({ item, status, note, actor });` — documentId is never passed. seedTurnoverItems (:150-155) and addTurnoverItem (:172-178) omit the column too. A repo-wide grep for turnover_items writers returns only turnover.ts:157, 172, 206. Consequently lib/checklists.ts:269-272 `supabase.from(\"turnover_items\").select(\"document_id\")…not(\"document_id\", \"is\", null)` can never return a row.",
    "note": "Confirmed by repo-wide search — no UI or server path anywhere attaches a document to a turnover item, so the FK at 20261013…:181 is dead and the evidence gather's turnover-document branch is structurally empty. The other read of turnover_items (QuotesPanel.tsx:577) selects only `name`."
  },
  {
    "id": "QUAL-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/checklists.ts:290 — `miChecklistComplete: checklists.some((c) => c.kind === \"mi\" && c.status === \"complete\")` — status only; the function never asks how the MI items got green or who signed. lib/checklistEngine.ts:80-83 turns that boolean into proof for any item matching `/mechanical integrity|MI review|integrity (group|manager)/i` with the label \"Mechanical-integrity checklist complete\". The gate that mints it, lib/checklists.ts:219-223, is `items.filter((i) => i.applicability !== \"na\" && i.status !== \"satisfied\" && i.status !== \"na\")` — auto-satisfied counts identically to human-satisfied. Feeding it, checklistEngine.ts:85-110 satisfies weld-log/NDE/hydrotest/MTR/as-built/P&ID items purely on `firstDocMatch` against document TITLES (:119-126).",
    "note": "The laundering chain is real and reproducible as described — title strings become item-level greens, which become a checklist status, which becomes a proof token consumed by other checklists in the project, and no code anywhere distinguishes auto from manual at either hop. One nuance on the wording: a person is technically in the chain — someone with canManage must click \"Mark complete\" (QualityTab.tsx:381-384 → setChecklistStatus) — but that click attests to nothing item-level, so the substance of the finding is unaffected."
  },
  {
    "id": "QUAL-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Same delete path as PM-6: lib/projects.ts:610 `const { error } = await supabase.from(\"projects\").delete().eq(\"id\", input.projectId);` with the confirmation at page.tsx:407 promising only \"the project and its schedule\". The cascades: 20261013_project_controls_program.sql:141-142 (project_checklists, and checklist_items via :156 `checklist_id UUID NOT NULL REFERENCES project_checklists(id) ON DELETE CASCADE`), :174-175 (turnover_items), :193-194 (punch_items). The exemption claim checks out: 20260826_legal_hold_delete_guard.sql attaches `trg_documents_legal_hold_delete` to `documents` (line 30) and `trg_document_versions_legal_hold_delete` to `document_versions` (line 54) and to nothing else; the controls tables' only protection is the RLS at :253-288, which is a write gate, not a delete guard.",
    "note": "Confirmed on all four legs, including the two claims of absence — no BEFORE DELETE trigger exists on any of project_checklists / checklist_items / turnover_items / punch_items / change_orders, and the audit row (lib/projects.ts:612-616) carries `details: { name: p.name }` with no counts. Substantially the same defect as PM-6 viewed from the quality-record side; both are accurate as written."
  },
  {
    "id": "QUAL-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20261013_project_controls_program.sql:262-288 gates every write with the same predicate, e.g. `CREATE POLICY checklist_items_write ON checklist_items FOR ALL USING (is_org_controller(org_id) OR EXISTS (SELECT 1 FROM project_checklists c WHERE c.id = checklist_items.checklist_id AND user_owns_project(c.project_id)))`, and turnover_items/punch_items/change_orders use `is_org_controller(org_id) OR user_owns_project(project_id)`. `user_owns_project` (:57-65) resolves to `p.owner_user_id = auth.uid()` — a single person. Reads are open to everyone else via the `%I_member_read` loop at :253-260. The UI mirrors it: page.tsx:68 `const isAdmin = hasAnyRole([\"Admin\", \"DocCtrl\"]);` and :133-134 `const canManage = isOwner || isAdmin;`, which is what QualityTab.tsx:371+ hides every action behind.",
    "note": "Correct, including the separation-of-duties claim of absence: nothing in lib/checklists.ts, lib/turnover.ts or the migration compares the actor against the item's creator, `created_by`, `reviewed_by`, or a required second party, and there is no signature/attestation table (grep for signature/signed_by/attest across the quality layer returns only a prose hint string in lib/turnover.ts:67). One person holding owner or Admin/DocCtrl can create the checklist, run the sweep, mark items satisfied, mark the checklist complete, and accept turnover with no second actor at any step."
  },
  {
    "id": "QUAL-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/checklists.ts:161 `if (item.manualNote) { skippedHuman += 1; continue; }` is the ONLY protection, and :167 `if (p.applicability === \"na\" && item.status !== \"na\") patch.status = \"na\";` — an item sitting at `satisfied` with auto (or even human-set-without-note) evidence is flipped to `na`. The patch at :162-168 never touches the `evidence` column, so the chips persist; QualityTab.tsx:455-464 renders `item.evidence.map(...)` unconditionally with the emerald `source === \"auto\"` styling. Dropping out of the gate: computeChecklistProgress (:339) and setChecklistStatus (:220) both start from `items.filter((i) => i.applicability !== \"na\" && i.status !== \"na\")`. The button stays live for the whole life of an open checklist — QualityTab.tsx:371 `{canManage && checklist.status === \"open\" && (` wraps it.",
    "note": "Confirmed. The one mitigation I found and weighed: the confirm dialog at QualityTab.tsx:307-309 discloses the count of proposed N/As and says \"Items you've decided by hand are never touched\" — but it never says already-satisfied, evidence-bearing items are among them, and \"decided by hand\" means manual_note specifically, so a reviewer who clicked \"✓ Satisfied\"... does get a note prompt (QualityTab.tsx:428-433 always passes promptNote), while the sweep's own greens have no note and are unprotected. Minor overstatement in the wording: the flipped row is rendered at `opacity-50` with an N/A status dot, so the stale chips are greyed rather than fully green."
  },
  {
    "id": "QUAL-6",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/checklists.ts:315 `const patch: Record<string, unknown> = { status: r.status, updated_at: new Date().toISOString() };` — no updated_by / updated_by_name, though both columns exist (20261013…:166-167) and the human path writes them (checklists.ts:192-196). lib/checklists.ts:321-323 audits only `{ checklistId, satisfied, needsEvidence }`. BUT lib/checklistEngine.ts:141 `if (item.manualNote) continue; // human decided — hands off` and QualityTab.tsx:426-431, where all three override buttons pass a non-null promptNote so `manualNote = v.trim() || \"decided by reviewer\"` is ALWAYS set.",
    "note": "The core holds: the sweep stamps updated_at with no actor, and the audit row is a bare count. But the narrated scenario is impossible — updated_by_name is written only by updateChecklistItem, whose only caller always sets manual_note, and applyAutoEvidence skips any item with manual_note; so the stale-'mreyes' misattribution cannot occur (the column stays NULL). 'Nothing says which items were auto-satisfied' is also false: every auto-green carries an evidence entry with source:\"auto\", rendered as an emerald chip titled 'Found by the evidence sweep' (QualityTab.tsx:458-459)."
  },
  {
    "id": "QUAL-7",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/turnover.ts:269 `row.closed_by = input.actor.uid;` with no closed_by_name; supabase/migrations/20261013…:191-204 punch_items DDL has `created_by_name TEXT` but no closed_by_name; lib/turnover.ts:106-119 mapPunch omits closed_by and lib/turnover.ts:41-52 PunchItem has no closedBy field; a repo-wide grep for closed_by shows lib/turnover.ts:269,272 as the only writer and no reader. The DDL columns are title/status/due_date/party_id only — no location, description or verification. MITIGATION: lib/turnover.ts:276-278 `await audit(\"PUNCH_STATUS\", …, { itemId, title, status })` with `user_id: actor.uid, user_email: actor.email` (turnover.ts:122-126).",
    "note": "The data-model half is exactly right — a punch item is title + due date + status, and closed_by is a write-only orphan. The 'actor nobody can read' half is overstated: audit_logs records PUNCH_STATUS with the actor's email, item title and timestamp, and that trail is rendered in the project evidence pack (evidencePack.ts:193-194, 227-228), so who closed an item is recoverable."
  },
  {
    "id": "QUAL-8",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/checklists.ts:102-106 `const { data } = await supabase.from(\"checklist_items\").select(\"*\")… return (((data ?? []) …` — error discarded; checklists.ts:219-221 `const items = await listChecklistItems(...); const blocking = items.filter(...); if (blocking.length > 0)` fails open on []. REFUTING THE NAMED CAUSES: 20261013…:256 creates `checklist_items_member_read … USING (EXISTS … org_members … status='active')` while the write side (`project_checklists_write`, :268-270) requires `is_org_controller(org_id) OR user_owns_project(project_id)` — both of which require an ACTIVE org membership (20260814…:33-39; 20261013…:59-64). Read is strictly broader than write.",
    "note": "The fail-open mechanism is real, but neither cited trigger is reachable. Losing Admin/DocCtrl removes the WRITE, not the SELECT — the status update would fail, not the gate. And if 20261013 were unapplied, project_checklists would not exist either, so there would be no checklist to complete. What actually survives is a transient/network error on the item read silently passing the gate."
  },
  {
    "id": "QUAL-9",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/projectHealth.ts:231 `payoff: \"Closeout is gated on acceptance; contractors are scored on it.\"` versus app/(protected)/projects/[id]/page.tsx:669 `<button onClick={handleTransition} disabled={transitionBusy}` — the only disable condition. The gate block's own comment at :625-626 reads `Warnings, not walls: the owner can complete anyway, on the record.` MITIGATION: :646-650 renders `You can complete anyway — the open items stay on the record and in the report.` beside a live `Turnover {accepted}/{required} accepted` line (:630).",
    "note": "The copy claim is accurate — the coach asserts a gate that does not exist and Confirm is never blocked. But the confirmation dialog states the true position in plain language at the exact moment of decision, so nobody reaches 'Complete' still believing the system will stop them; this is misleading marketing copy, not an operative deception."
  },
  {
    "id": "RET-1",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "HIGH",
    "evidence": "app/api/admin/shed/route.ts:50-62 `fetchCandidates` selects from document_versions only, with no join to documents and no legal_hold reference; lib/shed.ts:56-67 `isEligible` checks only file_url / archived_at / archive_id / size / superseded_at. supabase/migrations/20260826_legal_hold_delete_guard.sql:29-33 and :52-56 attach the guard as `BEFORE DELETE ON documents` / `BEFORE DELETE ON document_versions` — the shed never deletes a row, it UPDATEs archived_at (commit/route.ts:80-88) and then `r2.send(new DeleteObjectsCommand(...))` (commit/route.ts:100-106). `grep -rn \"legal_hold\" app/api/` returns nothing at all.",
    "note": "The claim is right on every mechanical point — the hold is invisible to the shed. I propose HIGH rather than CRITICAL: the two-step design captures the bytes into a produced ZIP before commit (commit refuses when the catalog note is still \"producing…\", route.ts:41-51), only superseded revisions beyond keep-N are eligible, and the version rows plus checksums survive. So this is a hold bypass that moves held evidence out of live storage into an admin's offline file, not unconditional destruction of it. The report's own 'Verifier correction' note already concedes this framing while leaving the severity at CRITICAL."
  },
  {
    "id": "RET-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/collections/delete/route.ts:62-64 moves docs to `heirParent`, then :105-107 `loadDestinationPolicies(supabaseAdmin, node.library_id, heirParent)` — the DELETED folder's own retention_policy is never read. lib/serverRetention.ts:46-49 `const until = policy ? computeRetentionUntil(...) : null; const state = !policy ? null : ...` writes retention_until=NULL, disposition_state=NULL. app/api/collections/trash/route.ts:97-100 restores only the shell (`deleted_at: null, deleted_by: null, parent_id`) — the route's own header comment at :8-10 admits \"Its former contents stayed where the delete moved them\".",
    "note": "Verified there is no compensating guard: nothing in the delete route checks retention_until, disposition_state or legal_hold before stepping documents up, and no restore path re-clocks documents. Docs carrying their OWN retention_policy are unaffected (resolveEffectiveRetentionPolicy prefers doc policy), so the finding's scoping to folder-INHERITED retention is precise. lib/retention.ts:174-179 then skips them permanently — scanRetention filters `.not(\"retention_until\",\"is\",null)`."
  },
  {
    "id": "RET-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Repo-wide, `policy.action` is read at exactly two places, both editor prefill: components/documents/RetentionPolicyModal.tsx:45 and components/documents/RetentionSection.tsx:108 (`setAction(s?.action ?? \"review\")`). It is written at RetentionSection.tsx:189 `policy: { enabled, years, basis, action }`. The only caller of disposeDocument hardcodes it: RetentionSection.tsx:94 `await disposeDocument({ documentId: doc.id, orgId, action: \"archive\", ... })`, and lib/retention.ts:155 always does `status: \"Archived\"` regardless of input.action, which only reaches an audit `detail` at :157.",
    "note": "Claim of absence confirmed by grep: no consumer in lib/, components/, app/ or any SQL branches on the field. lib/retention.ts:194 also hardcodes the notification body \"eligible for disposition review\" for every policy. A records schedule configured 'destroy' silently becomes 'archive' — over-retention, which is itself a legal exposure, so MEDIUM stands even though the behavior fails in the safe direction."
  },
  {
    "id": "RET-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/admin/shed/route.ts:157-163 writes the manifest entry as `sha256: (r.file_hash as string | null) ?? null` — r.file_hash comes from the DB SELECT at :52 — while `size: buf.byteLength` is the real captured length. Every other producer hashes the actual bytes: app/api/admin/ticket-shed/route.ts:212 `createHash(\"sha256\").update(f.buf).digest(\"hex\")`, lib/exportRunner.ts:191, lib/clientBackup.ts:171. Produce never compares the two, and shed/commit/route.ts:103 then deletes the live object.",
    "note": "Confirmed: the only sha256 in the document shed path is the copied DB value; the capture loop at :150-168 does zero verification before the irreversible commit. Partial mitigation worth recording — components/archive/BackupViewer.tsx:96-104 prefers the DB file_hash over the manifest, so a corrupted capture IS detectable at re-open time; what is permanently lost is detection at the one moment recovery was still possible, and the manifest cannot distinguish pre- from post-archive corruption."
  },
  {
    "id": "RET-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "commit stamps archived_at BEFORE deleting (route.ts:79-88), so after an R2-only failure every linked row has archived_at set. app/api/admin/archives/route.ts:51 `if (v.archived_at) s.committed++; else s.pending++;` and :80-81 `pending > 0 ? \"pending\" : committed > 0 ? \"committed\"` ⇒ status becomes \"committed\". The Reclaim button is gated at app/(protected)/admin/storage/page.tsx:1341 `{row.status === \"pending\" && (` and the routing line is page.tsx:621 `const which = row.docPending > 0 ? \"doc\" : \"ticket\";`. The banner promising the recovery is at page.tsx:505-507, after `setPendingArchive(null)` at :500.",
    "note": "The advertised recovery is genuinely unreachable for the exact failure it addresses: no row stays pending after a successful stamp, so the catalog shows \"reclaimed — the zip is the only copy\" with no Reclaim button, and if it did render, docPending===0 would route a document archive to /api/admin/ticket-shed/commit. Minor citation drift only: the mis-routing line is page.tsx:621, not 1296-1299."
  },
  {
    "id": "RET-14",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/admin/ticket-shed/restore/route.ts contains zero occurrences of \"manifest\", \"sha256\" or \"createHash\" (grep over all 220 lines); it goes straight from :151 `const bytes = await zip.files[entry].async(\"uint8array\")` to :156 `PutObjectCommand({ ... Key: k, Body: bytes ... })`. Produce did write real hashes at ticket-shed/route.ts:212 and ships them at :231 `zip.file(\"files-manifest.json\", ...)`. lib/archive.ts:118-121: `if (suffixMatch === null && (norm.endsWith(\"/\" + key) || key.endsWith(\"/\" + norm))) suffixMatch = entry;` … `return suffixMatch;`.",
    "note": "Both halves confirmed. Real guards exist and limit the blast radius — restore writes only keys the LIVE archived stub owns (:141-146 allowedKeys, prefix-checked) and never touches hot tickets (:136) — but the bytes themselves are trusted unverified against a manifest the producer already computed, and findInBackup falls back to a first-wins suffix match. The suffix branch only fires when no exact match exists, so it needs a re-laid-out or hand-edited zip; MEDIUM is right."
  },
  {
    "id": "RET-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/storage/delete/route.ts:26-40 is the entire authorization — `const orgMatch = path.match(/^orgs\\/([0-9a-fA-F-]{36})\\//); if (orgMatch) { ...org_members... .eq(\"status\", \"active\").maybeSingle(); if (!member) return 403; }` — followed at :42 by `await r2.send(new DeleteObjectCommand({ Bucket: R2_BUCKET, Key: path }));`. No role check, no assertSafeStorageKey (contrast app/api/storage/download-url/route.ts:29 `try { assertSafeStorageKey(path); } catch { ... }`), no documents/legal_hold lookup, no audit insert. `grep -rn \"deleteFile\" app lib components hooks scripts types` returns exactly one line: lib/storage.ts:442, its own definition.",
    "note": "Confirmed on all four sub-claims including the absence ones. Note the storage-key gate matters here more than anywhere: lib/storageKey.ts:6-12 documents precisely this authorize-the-first-orgs-prefix-then-act-verbatim pattern as the reason the validator exists, and this is the one route that skips it. HIGH is appropriate — an unused surface that lets a Viewer permanently destroy the bytes of a legal-hold-protected current revision while the DB row keeps reporting the document as Issued."
  },
  {
    "id": "RET-3",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "lib/accessRecert.ts:59-63 `const { data } = await supabase.from(\"libraries\").select(\"acl\")...; const allows = (acl?.rules ?? []).filter((r) => r.effect === \"allow\")` — the attested list is library-ACL allow-rules only. Enforcement is elsewhere: supabase/migrations/20260708_acl_rls_enforcement.sql:85-87 `CREATE POLICY documents_acl_select ON documents AS RESTRICTIVE FOR SELECT USING (node_visible(visibility, acl_index, org_id))`, and node_visible :54-57 `IF p_visibility IS NULL OR p_visibility = 'normal' THEN RETURN true; END IF;` plus :60-62 controllers return true unconditionally. lib/accessRecert.ts:97-114 snapshots that same list into access_recertification_events (`grants_snapshot: grants, grant_count: grants.length`) and stamps last_recertified_at regardless.",
    "note": "The mechanism is real: for a default-visibility library the effective reader set is every active org member plus all Admin/DocCtrl, and none of them appear in the attested snapshot, so the compliance record is signed against a list that is not the access list. I propose MEDIUM rather than HIGH because there is a mitigation the finding's scenario omits — components/documents/AccessRecertModal.tsx:118 renders, for an empty list, 'No explicit grants on this library (inherited / default access only).', so the reviewer is not silently told nobody has access. The defect is the accuracy of the stored attestation, with no direct data exposure."
  },
  {
    "id": "RET-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/schema.sql:1068-1070 `CREATE POLICY \"documents_org_access\" ON documents FOR ALL USING (org_id IN (SELECT my_org_ids()));` — FOR ALL, plain membership, no role and no column scoping. The only restrictive UPDATE policy is supabase/migrations/20260901_db_hard_enforcement.sql:152-162 `documents_deny_write_guard`, which passes unless an explicit acl_index deny rule names the actor. The only BEFORE UPDATE trigger on documents that gates fields is 20260816_documents_access_change_guard.sql:81-95, and it fires only on `NEW.visibility / NEW.acl / NEW.acl_index IS DISTINCT FROM OLD.*` — legal_hold, legal_hold_matter, retention_until and disposition_state are not mentioned in any trigger (`grep -rn \"legal_hold\" supabase/migrations` outside 20260826 returns only the ALTER TABLE ADD COLUMN lines in 20260820_retention.sql:31-38). supabase/migrations/20260821_access_recert.sql:39-44 and 20260820_retention.sql:59-64 both grant `FOR ALL` on the event tables to any active member.",
    "note": "Confirmed — the app-side gate is only components/documents/RetentionSection.tsx's `canManage` prop, which is client-side. Note the interaction that makes this worse than a lone integrity bug: clearing legal_hold via PostgREST also disarms the 20260826 BEFORE DELETE triggers, which are described as the enforcement that must not depend on the client."
  },
  {
    "id": "RET-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/retention.ts:126-128 `for (let i = 0; i < ids.length; i += 50) { await supabase.from(\"documents\").update(patch).in(\"id\", ids.slice(i, i + 50)); }` then `return ids.length;` — no `{ error }` destructure, no `{ count: \"exact\" }`, no `.select()`. Identical shape at :139-141 (releaseLegalHold, also returning ids.length), :157 (disposeDocument, then `return { ok: true }`), :79 and :89 (recomputeRetention), :99 (setRetentionPolicy) and :235-239 (logEvent's disposition-event insert).",
    "note": "Confirmed. supabase-js resolves an RLS-filtered UPDATE with error null and no row information, so placeLegalHold returns the count of ids it *intended* to patch, and the UI/notification (`notifyHold`, :224-225 'A legal hold was placed on N records') reports that number as fact. Contrast app/api/admin/ticket-shed/commit/route.ts:157-167, which does `{ count: \"exact\" }` and refuses to proceed on `(count ?? 0) === 0` — the correct pattern already exists in the repo."
  },
  {
    "id": "RET-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/schema.sql:1072-1074 `CREATE POLICY \"document_versions_org_access\" ON document_versions FOR ALL USING (org_id IN (SELECT my_org_ids()));` and :1080-1082 the same for tickets — both member-writable, and no trigger anywhere makes file_url or attachments immutable (`grep -rn \"ON document_versions|ON tickets\" supabase/migrations` yields only search-tsv triggers, the acl SELECT policy, the controllers-only DELETE policy and the legal-hold DELETE guard). The destructive paths then trust those values verbatim: app/api/admin/shed/commit/route.ts:96-98 `.map((v) => v.file_url)... new DeleteObjectsCommand({ Bucket: R2_BUCKET, Delete: { Objects: batch } })`, and app/api/admin/ticket-shed/commit/route.ts:136-139 `const keysFor = (t) => (Array.isArray(t.attachments) ? t.attachments : []).map((a) => (a?.url || \"\").toString()).filter(Boolean);` feeding :193 `deleteR2Keys(keysToDelete)`.",
    "note": "Confirmed, and the contrast the finding draws is exact: app/api/admin/ticket-shed/restore/route.ts:139-144 already applies the missing rule on the WRITE path — `.filter((k) => k && k.startsWith(prefix))` with the comment 'The only keys we may write are the ones the LIVE stub records, under our prefix' — while the DELETE path applies no prefix or ownership check to the same attacker-writable field. app/api/admin/shed/route.ts:150-168 likewise GETs `r.file_url` verbatim into the archive."
  },
  {
    "id": "RET-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/storageOrphans.ts:118-150 `scanOrphans(sb, maxPages)` takes no orgId and issues `new ListObjectsV2Command({ Bucket: R2_BUCKET, ContinuationToken: token, MaxKeys: 1000 })` over the whole bucket; the only key filters are `referenced.has(key)`, PROTECTED_PREFIXES ('data/', 'exports/') and MIN_AGE_DAYS. lib/storageOrphans.ts:152-177 `deleteOrphans(sb)` re-scans with that same unscoped call and deletes everything it returns. app/api/admin/orphans/route.ts:22-33 and :35-58 use orgId ONLY for `authorizeOrgRole(req, orgId, [\"Admin\",\"DocCtrl\"])` and for the audit_logs row — `await deleteOrphans(actor.admin)` is passed no org at all.",
    "note": "Confirmed. lib/serverAuth.ts:49-58 authorizes against membership of whatever org the caller names, so any Admin of any workspace — including a self-signup personal org — can sweep every tenant's objects. The reference collector does read all orgs' rows (service-role client, no org filter), so genuinely-referenced files are safe; the sharp edge is that any table missing from the sources list at lib/storageOrphans.ts:43-88 turns into cross-tenant destruction of live files, which the file's own comment at :78-80 records as having already happened once for output_templates."
  },
  {
    "id": "RET-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/revisions.ts:1174 `file_url: targetVersion.fileUrl,` (comment at 1163-1166: \"The new version row reuses the target version's file_url. We deliberately do NOT copy the file in storage\"). lib/shed.ts:56-66 isEligible tests only the ROW's own `superseded_at`/`archived_at` — never whether another live row shares the key. app/api/admin/shed/commit/route.ts:96-98 `const keys = versions.filter(...).map((v) => v.file_url)` then DeleteObjectsCommand at :103.",
    "note": "Confirmed by repo-wide search: no code path anywhere dedupes or reference-counts file_url before deletion (only supabase/migrations/20260827_file_url_index.sql indexes it, and no shed query joins on it). The old row is superseded and beyond keep-N so it is selected; deleting its key destroys the bytes the revert row — the CURRENT revision — points at. Worse, /api/storage/resolve/route.ts:32-38 orders by created_at DESC limit 1, so the lookup lands on the revert row (archived_at NULL, archive_id NULL) and line 87 returns `archived:true, missing:true, archiveId:null` — the user is told to provide an archive the UI cannot name."
  },
  {
    "id": "RET-9",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "20260826_legal_hold_delete_guard.sql:18 `RETURNS trigger LANGUAGE plpgsql SECURITY DEFINER AS $$` and :38 same — neither carries SET search_path, unlike 20260810_archive_invariants.sql:17-19 `LANGUAGE plpgsql / SECURITY DEFINER / SET search_path = public`. The shadowable statement is :42 `SELECT legal_hold INTO v_hold FROM documents WHERE id = OLD.record_id;` followed by :43 `IF COALESCE(v_hold, false) THEN`.",
    "note": "The literal claim is true and the house convention is clearly the opposite (18 of 31 SECURITY DEFINER migration files set search_path). Two corrections pull the severity down: enforce_legal_hold_delete_guard (lines 17-27) references no relation at all — only OLD.legal_hold — so search_path shadowing cannot affect it, leaving one genuinely vulnerable function, not \"both\"; and the exploit needs CREATE on a schema ahead of public, a DDL privilege that already implies the ability to DROP the trigger outright. LOW (hardening/lint-class) fits better than MEDIUM."
  },
  {
    "id": "REV-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/documents/[libraryId]/page.tsx:4238-4241 `onOpenVersion={(v) => { setSelectedVersion(v); setShowFullScreen(true); }}` with `url={selectedVersion.fileUrl}` but `document={selectedDoc}` (:3024-3032). FullScreenViewer.tsx:843 then hardcodes `versionId: docRecord.currentVersionId ?? undefined` (same at :1036; MultiDocViewer.tsx:689). lib/downloads.ts:74 `const rev = doc.rev ? \\`_Rev${doc.rev}\\` : \"\"` and :82 `Rev ${doc.rev ?? \"?\"} at time of issue`, both from the CURRENT doc; :35 `if (doc.checkedOutBy && doc.checkedOutBy === userId) return \"controlled\"` → :222-226 raw `fetch(ctx.fileUrl)` with no stamp. app/api/verify/route.ts:90 `const isCurrent = !docRetired && (!versionId || versionId === d.current_version_id);`.",
    "note": "Every limb holds: the viewer renders old bytes against the current DocumentRecord, so filename, footer, QR ?v= and the download_audits row all say Rev 5, and a checkout holder takes the Rev 2 bytes completely unstamped. Partial mitigation the finding did not credit: VersionHistoryPanel's OWN download button (lines 129-165) clones the doc with `checkedOutBy: undefined` to force the uncontrolled stamp and passes the correct `versionId: v.id` — but it still stamps `doc.rev`, so even that path labels Rev 2 bytes as Rev 5."
  },
  {
    "id": "REV-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/share/file/route.ts:42-52 selects only `id, org_id, document_id, expires_at, revoked_at, created_by` and checks only `if (share.revoked_at) ... if (share.expires_at && ...)`; :53-58 then fetches `documents ... select(\"id, document_number, title, name, rev, current_version_id\")` — `status` is not selected and never tested. app/api/share/resolve/route.ts:32-47 is identical. A repo-wide grep for `revokeShareLink` / `document_shares` returns only lib/documentShares.ts, the two share routes, ShareLinkModal.tsx:77, and the export/restore table lists — no lifecycle, supersede, archive, or split path touches shares.",
    "note": "Confirmed on both halves: no status check in either share route, and no retirement path revokes a share. supersedeDocument (revisions.ts:1454-1467) and markSupersededAndLink (documentLifecycle/common.ts:266-281) set status='Superseded' and never look at document_shares. Partial mitigation the finding does not mention: the delivered PDF is stamped 'UNCONTROLLED — SHARED COPY' with a /verify QR, and app/api/verify/route.ts:88 `const docRetired = d.status === 'Superseded' || d.status === 'Archived'` does return docStatus/isCurrent=false — but the share landing page never surfaces status, so the outsider only learns it by scanning the QR. MEDIUM stands."
  },
  {
    "id": "REV-11",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "lib/documentLifecycle/common.ts:178 `status: \"Issued\",` inside createNewDocWithFirstVersion — no authorizePublish, no effectiveReviewControlForDocument anywhere in the file. lib/revisions.ts:344 `status: input.status ?? \"Issued\"` in createDocumentWithFile, whose own doc comment (:315-317) admits 'it does NOT run the publish guard'. supabase/schema.sql:1067-1069 `CREATE POLICY \"documents_org_access\" ON documents FOR ALL USING (org_id IN (SELECT my_org_ids()))` — the publish-guard trigger is BEFORE UPDATE only, so INSERTs are ungated.",
    "note": "The core claim is correct — split (split.ts:96-134), merge (merge.ts:107-142) and DocumentLinkPicker.tsx:101-115 all materialize status='Issued' Rev-0 controlled documents with released_at set, with no publish-authority check and no review-control resolution, so a require-review library is bypassed. Two factual corrections, which is why this is CORRECTED rather than clean: (1) split/merge do NOT start the compliance clocks — onDocumentIssued/onDocumentIssuedAck/recomputeRetention appear only at revisions.ts:386-391 (createDocumentWithFile) and postPublish.ts:138-143, never in createNewDocWithFirstVersion, so the cited clock behaviour applies only to the link-a-drawing path; (2) the Split/Merge entry point is UI-gated (InspectorPanel.tsx:283 `const canManage = isController || isOwner;` at :888), so the actor is a controller or the doc's owner, not an arbitrary drafter. Severity left at MEDIUM: an owner who is not a controller can still bypass a mandatory review gate this way."
  },
  {
    "id": "REV-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/documentLifecycle/reverse.ts:116-118 `await supabase.from(\"documents\").update({ status: \"Issued\", superseded_at: null, ... }).eq(\"id\", sourceDocId);` (reverseSplit) and :212-214 the identical `status: \"Issued\"` write in reverseMerge's `for (const sId of allSourceIds)` loop — neither reads the prior status. `auditAt` is read at :99 and :205 (`const auditAt = (ev.details?.auditAt as string) ?? \"1970-01-01T00:00:00Z\";`) and a repo-wide grep for 'auditAt' returns ONLY those two read sites — nothing writes it. types/schema.ts:613 confirms 'Void' is a real status, and merge.ts:95-100 applies no status filter to sources.",
    "note": "Both halves confirmed. split.ts:93 captures `priorSourceStatus` and restoreSupersededSource (common.ts:93-119) restores it correctly — but only on the in-transaction compensation path; the user-facing reverse* functions ignore it entirely and hard-code 'Issued', so a Void or Draft source comes back as a live controlled document. auditAt is never written by logRevisionEvent/logAuditAction (lib/audit.ts:16-31 writes action/resource/details only), so summarizeDerivativeWork always scans from epoch — over-inclusive rather than dangerous, but it means the 'since the split' warning text is false."
  },
  {
    "id": "REV-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "A repo-wide grep for `applyEffectiveDate` returns exactly three sites: the definition (lib/effectiveDate.ts:43), lib/revisions.ts:646 (revUpDocument), and lib/reviewControl.ts:484 (finalizeReviewedRevision). revertToVersion spans lib/revisions.ts:1146-1312 and contains none of them; its documents UPDATE (:1256-1266 legacy / the publish_revision RPC at 20260823_publish_contract.sql:266-273 `UPDATE documents SET current_version_id, rev, revision, status, updated_at, updated_by`) never touches effective_date or effective_notified_at.",
    "note": "Confirmed. After a revert, documents.effective_date still holds the withdrawn revision's future date with effective_notified_at NULL, so scanEffectiveDates (effectiveDate.ts:56-63, `.lte(\"effective_date\", todayISO()).is(\"effective_notified_at\", null)`) fires 'Now in effect: <doc> Rev <revert-label>' on that date, and docControlRegister.ts:186-187 keeps flagging the reverted doc effectivePending. The new revert version row itself carries no effective_date (revertPayload, :1172-1185), so the version and the document disagree."
  },
  {
    "id": "REV-14",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/revisions.ts:1470-1481: comment `// Record the (old → new) join rows. Idempotent via UNIQUE constraint — we ignore duplicate-key errors so re-running the action is safe.` followed by `await supabase.from(\"document_supersessions\").insert(rows);` — a plain multi-row INSERT with no onConflict and no error destructuring. supabase/migrations/20260526_supersede_archive.sql:36 `UNIQUE (superseded_doc_id, replacement_doc_id)`. Contrast lib/documentLifecycle/common.ts:293: `await supabase.from(\"document_supersessions\").upsert(rows, { onConflict: \"superseded_doc_id,replacement_doc_id\" });`.",
    "note": "Confirmed exactly as claimed, including the false comment. A multi-row INSERT is atomic in Postgres, so one pre-existing pair rejects the whole statement; the return value is discarded, so SupersedeModal.tsx:52-77 reaches `onSuccess(...)` and `onClose()` with no error. The sibling function in the same codebase does it correctly, which settles that this is an oversight rather than intent. Mild mitigation: logRevisionEvent at revisions.ts:1483-1497 still records resolvedReplacementIds, so the audit log retains the lineage even when the join table does not."
  },
  {
    "id": "REV-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "VersionHistoryPanel.tsx:244 `{versions.map((v) => {` — listVersions (lib/revisions.ts:988-996) applies no filter, and the Revert button at :370 is gated only by `{canRevert && !isCurrent && onRevertVersion && (` — no reviewState and no isBranch test, in contrast to the label-correct button two lines up at :351 which DOES exclude `v.reviewState !== \"in_review\"`. revertToVersion (lib/revisions.ts:1146-1234) validates only reason/lock/authority and copies `file_url: targetVersion.fileUrl` (:1174). The DB gate at 20260822_review_completion_guard.sql:46-58 counts sign-offs `WHERE document_version_id = NEW.current_version_id` — the brand-new revert row, which has none, so `COALESCE(v_primary_reqs,0) > 0` is false and it passes.",
    "note": "Confirmed end to end: nothing between the button and the DB inspects the target's review_state or is_branch, and the review-completion trigger structurally cannot see it because it only ever examines the incoming current version. The in-review draft is additionally rendered with the amber \"Superseded\" badge (:280-283), since that branch is the fall-through for any non-current, non-branch row."
  },
  {
    "id": "REV-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/revisions.ts:1169 `const revertedLabel = \\`${targetVersion.revisionLabel}-revert-${Date.now()}\\`;` fed to `revision_label` at :1172 and to `rev: revertedLabel, revision: revertedLabel` on the legacy path (:1269-1270); via the RPC, 20260828_integrity_hardening.sql:186-189 `UPDATE documents SET current_version_id = v_new_id, rev = btrim(v_label), revision = btrim(v_label)`. Consumers take it verbatim: lib/downloads.ts:74 filename and :82 footer, lib/docControlRegister.ts:96 selects `rev`, VersionHistoryPanel.tsx:263 `Rev {v.revisionLabel || \"—\"}`.",
    "note": "Grep for \"revert-\" across all .ts/.tsx/.sql returns exactly one hit — the construction site. No formatter, no display strip, no migration normalizes it, so the epoch-millis string is the controlled revision identifier everywhere. The only remedy is the manual, audited label-correct button, which is after-the-fact."
  },
  {
    "id": "REV-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/downloads.ts:49-53 `export function viewerStatusBadge(doc, viewingCurrentVersion = true) { if (!viewingCurrentVersion) return { label: \"Old revision — not current\", tone: \"caution\" };`. Exhaustive grep gives exactly two call sites and neither passes the second argument: FullScreenViewer.tsx:1184 `viewerStatusBadge({ status: docRecord.status, rev: docRecord.rev ?? rev })` and MultiDocViewer.tsx:668 `viewerStatusBadge(activeEntry.doc)`. With status 'Issued' the default path returns `Controlled · Rev ${doc.rev}` with tone 'controlled' (:57), rendered emerald at FullScreenViewer.tsx:1187.",
    "note": "The purpose-built guard parameter is dead code — confirmed by repo-wide search, not inference. Mitigation worth noting but not disqualifying: the header two lines above (:1176) simultaneously prints the true `Rev {rev}` of the version being viewed, so the screen contradicts itself rather than being uniformly wrong."
  },
  {
    "id": "REV-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/reviewControl.ts:429-433 `.update({ current_version_id: pendingId, rev: baseRev, revision: baseRev, status: \"Issued\", pending_version_id: null, ... }).eq(\"id\", input.documentId).eq(\"pending_version_id\", pendingId)` — the only CAS is on pending_version_id; current_version_id is never compared to an expected base, and status is written unconditionally. supersedeDocument (lib/revisions.ts:1453-1466) sets status/superseded_at/supersession_reason and does not touch pending_version_id. No trigger blocks the transition: grep for `OLD.status` across all migrations finds only `NEW.status = 'Superseded' AND COALESCE(OLD.status,'') <> 'Superseded'` (20260822:38 and its three predecessors) — nothing guards Superseded → Issued.",
    "note": "Both consequences hold. The DB review gate cannot stop it either: 20260822:46-58 checks sign-offs on NEW.current_version_id, which is the fully-signed draft. One caveat that does not refute it — the same trigger's authority check still applies, so an auto-finalize by a reviewer without publish rights raises and is swallowed by the `catch` at reviewControl.ts:330; the finding's scenario (Doc Control / Admin, who short-circuit at `IF v_role IN ('Admin','DocCtrl') THEN RETURN NEW`) is squarely reachable."
  },
  {
    "id": "REV-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Repo-wide grep for pending_version_id writes yields exactly four clear sites: lib/revisions.ts:633 (direct publish), lib/reviewControl.ts:430 (finalize), components/projects/IntakePanel.tsx:258 (intake reject), app/api/intake/upload/route.ts:325/332. It appears nowhere in revertToVersion (lib/revisions.ts:1146-1312), supersedeDocument (:1453-1466), archiveDocument (:1325-1354), or markSupersededAndLink (lib/documentLifecycle/common.ts:267-280) / reverse.ts:104-126. The contrast is explicit at revisions.ts:622-624: \"A direct (non-branch) publish supersedes any in-review draft: clear the pending pointer and void the draft's roster, so a stale draft can never be finalized OVER this newer revision.\"",
    "note": "Claim of absence confirmed by exhaustive search. The retirement paths deliberately mirror publish for status and lineage but omit the one line publish added for exactly this hazard, so a draft surviving a split/supersede/archive/revert can still be promoted later by finalizeReviewedRevision, which (per REV-5) writes status 'Issued' unconditionally."
  },
  {
    "id": "REV-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/documents/RevUpModal.tsx:279 `if (willReview && !asBranch) {` — submitForReview is reached only in that branch; `asBranch` diverts to revUpDocument at :293. In the RPC, the branch path (20260828_integrity_hardening.sql:169-177) inserts the version and a revision_branches row and never UPDATEs documents, so the review-completion trigger — which returns early unless `NEW.current_version_id IS DISTINCT FROM OLD.current_version_id` or status flips to Superseded (20260822:37-41) — never fires. supabase/migrations/20260823_publish_contract.sql:63-65 `CREATE UNIQUE INDEX ... ON document_versions(record_id, revision_label) WHERE (superseded_at IS NULL AND is_branch = FALSE)`.",
    "note": "Both halves verified: the partial index's own predicate excludes is_branch = TRUE, so the RPC's `EXCEPTION WHEN unique_violation` duplicate-label backstop cannot fire for a branch, and a branch row can carry the identical active label '3'. MEDIUM is right — the branch is never promoted to current, it opens a revision_branches debt row, and RevUpModal:305-308 raises a loud modal saying it is NOT the current revision."
  },
  {
    "id": "REV-8",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/revisions.ts:550 `markPublishRpcMissing(); // fall through to the legacy path below` + :117-118 `let publishRpcMissingUntil = 0; function publishRpcUnavailable(){return Date.now() < publishRpcMissingUntil;}`. BUT lib/revisions.ts:453-478 runs BEFORE the RPC for every non-branch publish: `if (freshDoc && liveCurrent !== expectedBase) { ... throw new StaleBaseError(...) }`, and supabase/migrations/20260823_publish_contract.sql:63-65 `CREATE UNIQUE INDEX ... document_versions_active_label_uniq ON document_versions(record_id, revision_label) WHERE (superseded_at IS NULL AND is_branch = FALSE)`.",
    "note": "The mechanism is real — a transient PGRST202 sets a 60s flag that routes publishes to legacyRevUpAfterUpload (revisions.ts:758-835), which genuinely has no row lock and no transaction. But two of the title's claims are wrong: there IS a base check on the legacy path (the step-1b pre-flight at 453-478 runs regardless of which path follows), and it is not 'every publish' — publishRpcMissingUntil is a module-level variable in a browser-side module (lib/supabase.ts is the anon browser client; revUpDocument is only called from RevUpModal/merge.ts/setRevUp.ts), so the flag is per-tab, not per-deployment. The stated outcome ('both version rows are written') additionally requires the two drafters to pick DIFFERENT labels, since the partial unique index rejects a second active row with the same (record_id, revision_label). Residual risk is a genuine but narrow TOCTOU: the pre-flight fires before the file hash+upload, so a true concurrent race inside the upload window can still slip through."
  },
  {
    "id": "REV-9",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/effectiveDate.ts:16 `const todayISO = () => new Date().toISOString().slice(0, 10);` (UTC) vs :23-24 `const today = new Date(); today.setHours(0,0,0,0); const eff = new Date(`${effectiveDate.slice(0,10)}T00:00:00`);` (local). :45 `const suppress = !eff || eff <= todayISO();` → :49 `effective_notified_at: suppress ? new Date().toISOString() : null`. app/api/verify/route.ts:93-94 `effectiveDate.slice(0,10) > new Date().toISOString().slice(0,10)` (UTC again). components/documents/RevUpModal.tsx:751 `<input type=\"date\" ...>` supplies a LOCAL calendar date.",
    "note": "Claim verified exactly: for a UTC-5 publisher at 20:30 on 21 Aug choosing effective date 2026-08-22, todayISO() is already '2026-08-22', so suppress is true, effective_notified_at is stamped at publish, and scanEffectiveDates (:62-63) can never pick the doc up — while effectiveStatusFor() (local) still renders the 'pending' badge. lib/__tests__/effectiveDate.test.ts:5 bakes the same UTC assumption into the fixtures, so the suite would not catch it. Downgraded to LOW: the bug only bites on the exact one-day boundary (effective date == UTC-today but local-tomorrow), and the only lost artifact is one in-app 'now in effect' notification — the revision itself is served correctly and the badge/register still show it."
  },
  {
    "id": "RG-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260828_integrity_hardening.sql:223-226: `CREATE POLICY doc_review_signoff_insert ON document_review_signoffs FOR INSERT WITH CHECK ( EXISTS (SELECT 1 FROM org_members WHERE org_id = document_review_signoffs.org_id AND uid = auth.uid() AND status = 'active') );` — no constraint on status, reviewer_user_id, slot, or signature_id. The DB gate counts it: 20260822_review_completion_guard.sql:48-53 `count(*) FILTER (WHERE slot = 'primary'), count(*) FILTER (WHERE status = 'signed') ... IF COALESCE(v_primary_reqs,0) > 0 AND COALESCE(v_signed,0) < v_primary_reqs THEN RAISE`. The app gate counts it identically at lib/reviewControl.ts:365-367.",
    "note": "Verified — I looked for a later policy, a CHECK, or a trigger that would block it: the only files touching document_review_signoffs are 20260812/20260818/20260819/20260822/20260825/20260828/20260829/20260830, and none adds a status/self-row constraint on INSERT (20260830 rewrites only the UPDATE policy). A row with slot='alternate', status='signed' raises v_signed without raising v_primary_reqs, defeating both gates. The attacker cannot promote the version themselves (the publish guard's authority branch still applies to them), but they can make the gate report complete, and recordReviewSignoff's auto-finalize (reviewControl.ts:322-330) will then publish on the next genuine signature. CRITICAL is right."
  },
  {
    "id": "RG-10",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "The finding missed the guard at app/api/intake/upload/route.ts:257-259: `if (d.pending_version_id && !(link.allow_auto_supersede && linkAuthored)) { return bad(\"Your previous submission for this document is still in review — it must be approved or rejected first.\", 409); }`. The stated scenario is an ASSIGNED document (`linkAuthored = false`, route.ts:244-250), which this line rejects with 409 before any pointer is touched.",
    "note": "The headline scenario is false: an assigned document mid-review cannot have pending_version_id repointed — the route 409s. A much narrower path does survive: when `link.allow_auto_supersede && linkAuthored` the guard is skipped, `autoNow` (route.ts:302) is true, and route.ts:322-327 sets `current_version_id` + `pending_version_id: null` through supabaseAdmin, which the publish guard waves through (`IF v_actor IS NULL THEN RETURN NEW;`, 20260822_review_completion_guard.sql:32-34), leaving the old draft's roster rows 'pending' on an un-superseded version that scanReviews (lib/reviewControl.ts:518-520) keeps chasing. That requires the org to have explicitly granted auto-supersede on a vendor-authored document that an org publisher separately put into review — hence LOW, not MEDIUM."
  },
  {
    "id": "RG-11",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "components/documents/RevUpModal.tsx:97: `const [changeType, setChangeType] = useState<DocumentVersion[\"changeType\"]>(\"Minor\");` — and \"Minor\" is not even first in CHANGE_TYPES (:79-83, Major is), so it is a deliberate default. :143-145 restores it (`if (remembered?.changeType) setChangeType(remembered.changeType);`) and :311 persists it. lib/reviewControl.ts:59: `if (input.changeType === \"Minor\" || input.changeType === \"Correction\") return \"none\";`",
    "note": "Every factual element is confirmed. Downgraded because the mitigating control the finding itself quotes actually renders in exactly the risky state and names the remedy: RevUpModal.tsx:643-651 shows the amber banner whenever `reviewControl && reviewControl.mode !== \"none\" && effMode === \"none\"`, telling the publisher the gate is being skipped and to set Change Type to Major. The gate is bypassed by a bad default, but never silently — that is a defaulting/UX defect, not a MEDIUM control failure."
  },
  {
    "id": "RG-12",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "The unchecked write is real — lib/reviewControl.ts:444-449 `await supabase.from(\"document_versions\").update({ review_state: \"approved\", revision_label: baseRev, ... })` with no error destructuring, in contrast to the very next block at :451-454 which does capture `voidErr`. But the stated consequence is refuted: every content-serving path resolves current_version_id FIRST and only falls back to the review_state filter. components/viewers/MultiDocViewer.tsx:381-387: `if (doc.currentVersionId) { ... .eq(\"id\", doc.currentVersionId) ... } if (!fileUrl) { ... .or(\"review_state.is.null,review_state.eq.approved\") ... }`. Same order in components/assets/FileReferenceModal.tsx:36-43, app/api/share/file/route.ts:63-79, and app/(protected)/documents/[libraryId]/page.tsx:1919-1934.",
    "note": "'Filtered out of every latest-approved query, so viewers serve the superseded drawing' is false — the promote at :429-433 sets current_version_id, and every viewer/share path hits that pointer directly with no review_state predicate, so the correct new file is served. The real residue is a stuck record: revision_label frozen at '2A' while documents.rev says '2', review_state still 'in_review', prior rev not superseded — which hides the current revision from the listing surfaces that DO filter (components/documents/HistoryDrawer.tsx:60, lib/timeline.ts:330, components/documents/CompareRevisionsModal.tsx:60). The cited unique-index collision (20260823_publish_contract.sql:63-65) is also not the systematic trigger implied: baseRev is the NEXT label, not the outgoing rev's, so it collides only against a pre-existing un-superseded row with the same label."
  },
  {
    "id": "RG-13",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "Repo-wide search for `useRevLetters` returns exactly two hits, both declarative: types/schema.ts:209 `useRevLetters?: boolean;` and supabase/migrations/20260818_review_before_publish.sql:22 `--     useRevLetters?: bool }                            -- auto 2A/2B suffix (default true)`. lib/reviewControl.ts:156-163 `letterLabelFor(baseRev, existingDraftLabel)` takes no flag, and lib/revisions.ts:871 calls it unconditionally: `const draftLabel = letterLabelFor(baseRev, existingLabel);`",
    "note": "The absence claim is correct — zero readers. Downgraded because there is also no way for a facility to SET it and be misled: `grep -n \"useRevLetters\" components/documents/ReviewControlModal.tsx` returns nothing, so the only configuration surface for review_control never offers the option. The exposure is a dead typed field plus a misleading migration comment, not a setting that silently fails on real users."
  },
  {
    "id": "RG-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260830_publisher_row_management.sql:34-50 — the UPDATE policy's USING clause admits `user_is_effective_owner(...) OR user_can_publish_on_library(...)`, and its WITH CHECK (:48-51) is only `EXISTS (SELECT 1 FROM org_members WHERE ... status = 'active')`, so an authorized publisher may set ANY column on ANY reviewer's row, status='signed' included. components/documents/ReviewGateSection.tsx:131-132: `if (r.status === \"signed\") return <span ...><CheckCircle2 /> {r.signedAt?.slice(0,10)}</span>;` next to `{r.reviewerName || r.reviewerUserId}` at :193 — no cross-check of signature_id or of who created the signature.",
    "note": "Confirmed, and the migration's own header (:26-27, 'Signing someone else's row is still impossible for everyone without that authority') concedes the point for those who have it. One refinement that strengthens rather than weakens the claim: e_signatures is genuinely self-insert only (20260720_e_signatures.sql:56-61 `WITH CHECK (signer_user_id = auth.uid() ...)`), so the publisher cannot mint a signature in the Piping Lead's name — but calling recordReviewSignoff (reviewControl.ts:287-303) with their OWN signerUserId and the Piping Lead's signoffId attaches the publisher's e-signature to the Lead's roster row, and the roster still renders it under the Lead's name. A bare PATCH with signature_id left NULL renders identically."
  },
  {
    "id": "RG-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/reviewControl.ts:40-50: `if (doc.collectionId) { const { data } = await supabase.from(\"collections\").select(\"review_control\").eq(\"id\", doc.collectionId).maybeSingle(); folder = ...}` — one row, no parent walk, despite supabase/schema.sql:93 `parent_id UUID REFERENCES collections(id) ON DELETE CASCADE` and :96-97 `path_ids UUID[]`, `path_names TEXT[]` being available. The same single-level lookup is repeated at :545-549 (scanReviews) and components/documents/ReviewGateSection.tsx:49-55.",
    "note": "Verified as an absence: I grepped every review_control read site in lib/, app/ and components/ — all four (reviewControl.ts:40-50, reviewControl.ts:545-549, ReviewGateSection.tsx:49-55, and page.tsx's row mapper) consult only doc.collectionId, and path_ids is never used for policy resolution anywhere. ReviewControlModal.tsx:3-4 advertises configuring the policy on 'a LIBRARY or FOLDER' with no hint that only the leaf folder counts, so a policy set on 'Piping / P&IDs' silently governs nothing under 'Piping / P&IDs / Unit 12'. HIGH stands."
  },
  {
    "id": "RG-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/reviewControl.ts:364-367: `const requiredPrimaries = roster.filter((r) => r.slot === \"primary\").length; const signed = roster.filter((r) => r.status === \"signed\").length; const complete = requiredPrimaries > 0 && signed >= requiredPrimaries;` — `signed` is not filtered by slot or by identity. The database mirrors the same defect: 20260822_review_completion_guard.sql:48-53 counts `count(*) FILTER (WHERE status = 'signed')` across all slots.",
    "note": "Confirmed, including the auto-activation premise: reviewControl.ts:555-561 `if (r.slot === \"alternate\" && r.activated === false && ageDays >= timeoutDays) { ... activateAlternate(...) }`. With two primaries (Piping + I&E) and one alternate, Piping + the alternate gives signed=2 >= requiredPrimaries=2, and neither the app gate (:367) nor the trigger (:53) notices that a required discipline never signed. Completion is per-count, never per-reviewer, so there is no place the identity of the missing primary is checked."
  },
  {
    "id": "RG-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/schema.sql:1067-1069 `CREATE POLICY \"documents_org_access\" ON documents FOR ALL USING (org_id IN (SELECT my_org_ids()));` (and :1063-1065 the same for collections) — FOR ALL with no WITH CHECK, so any org member may PATCH documents.review_control, a column 20260818_review_before_publish.sql:26 adds to that very table. lib/reviewControl.ts:143-149: `await supabase.from(table).update({ review_control: input.control }).eq(\"id\", input.id); await logAuditAction({ action: input.control ? \"REVIEW_CONTROL_SET\" : ... })` — the update's error is never destructured, so the audit row is written whether or not the write landed.",
    "note": "Both halves confirmed. The publish-guard trigger cannot help: enforce_document_publish_guard returns NEW unless `v_advancing` (20260822:37-42), and a review_control-only UPDATE does not move current_version_id or status. Combined with resolveEffectiveReviewControl:34-37 (`for (const c of [docControl, folderControl, libraryControl]) if (c) return c;`) and the test that pins it (lib/__tests__/reviewControl.test.ts:26 'lets a document-level none override an inherited require'), one PATCH of `{\"mode\":\"none\"}` by any active member permanently disables the gate for that document — with no audit row at all if written directly rather than through setReviewControlPolicy."
  },
  {
    "id": "RG-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/documents/RevUpModal.tsx:193-196 `try { const c = await effectiveReviewControlForDocument({...}); if (alive) setReviewControl(c); } catch { if (alive) setReviewControl(null); }` → :210 `const effMode = effectiveModeForRevUp({ control: reviewControl ?? { mode: \"none\" }, changeType });`. Eighteen lines later, :197-203 `catch { if (alive) { setDocClass(null); setDocClassUnknown(true); } }` → :218-219 `// An UNKNOWN class (resolution failed) is treated as drawing: PSM gates fail closed.` / `const mocRequired = (docClass === \"drawing\" || docClassUnknown) && !isMinorLike;`.",
    "note": "Confirmed, and the real behaviour is worse than the finding states: effectiveReviewControlForDocument (reviewControl.ts:40-50) uses `.maybeSingle()` and never throws on a PostgREST error, so the catch at :195 is not even reached — the failed read degrades silently to `{mode:'none'}` with no null to distinguish it. Nothing downstream recovers: the review requirement is enforced ONLY by this client-side branch (RevUpModal.tsx:280-291 chooses submitForReview vs revUpDocument), and the DB completion guard only bites when roster rows already exist (20260822:53 `IF COALESCE(v_primary_reqs, 0) > 0`), which a skipped review never creates. ReviewGateSection.tsx:39-64 swallows the same errors."
  },
  {
    "id": "RG-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/reviewControl.ts:219-224: `if (upsertErr) { console.warn(...); warnings.push(...) } else { await Promise.all(primaries...notify...) }` — openReviewRoster returns void and never rethrows, so submitForReview (revisions.ts:932-937) returns success and components/documents/RevUpModal.tsx:287-290 alerts 'Reviewers have been notified. ... the moment the last required reviewer signs, the draft publishes automatically.' With zero rows, reviewControl.ts:365-367 yields requiredPrimaries=0 → complete=false forever, and 20260822_review_completion_guard.sql:53 `IF COALESCE(v_primary_reqs, 0) > 0` short-circuits so the DB gate never fires.",
    "note": "Confirmed on both consequences: the draft can never finalize (finalizeReviewedRevision returns reason 'incomplete' at :409-411, and the Publish button is disabled at ReviewGateSection.tsx:214) while the database completion guard is simultaneously inert for that version, so a direct rev-up over it is unblocked. Partial mitigation the finding omits: reviewControl.ts:243-256 does notify the owner and Admin/DocCtrl ('The reviewer roster for X has gaps: ...'), and ReviewGateSection.tsx:206-213 shows a rescue panel — but the message misattributes the cause to 'no reviewers are configured for this library' and the publisher's own alert is flatly false. MEDIUM is appropriate."
  },
  {
    "id": "RG-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/reviewControl.ts:224 filters the actor out of NOTIFICATIONS only — `primaries.filter((r) => r.uid !== input.actorId).map((r) => notify(...))` — while the roster rows themselves (lib/reviewControl.ts:204-215) are built from every resolved primary with no actor exclusion. components/documents/ReviewGateSection.tsx:82: `const mine = roster.find((r) => r.reviewerUserId === uid && r.status === \"pending\" && (r.slot === \"primary\" || r.activated));` — the submitter gets their own Sign button. lib/reviewControl.ts:367 `const complete = requiredPrimaries > 0 && signed >= requiredPrimaries;` and :324-331 auto-finalize on `complete`.",
    "note": "Repo-wide search confirms the absence: no `excludeSelf`/self-review check exists in lib/reviewControl.ts, lib/revisions.ts (submitForReview), or any migration touching document_review_signoffs (20260818/20260822/20260828/20260830). The e_signatures RLS only requires `signer_user_id = auth.uid()` and active membership (20260720_e_signatures.sql:56-60), and the 20260822 publish guard's completion test counts signed rows without regard to who created the version. A sole-Engineer roster is self-approving end to end."
  },
  {
    "id": "RG-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/signatures/SignatureCeremony.tsx:61-66: `const identityReady = reauth === \"loading\" ? false : reauth === null ? true  // no session info — server still validates the write : reauth.method === \"password\" ? password.length > 0 : reauth.fresh;` — and lib/eSignatures.ts:63-64 `const { data: { user } } = await supabase.auth.getUser(); if (!user?.email) return null;` is the branch that produces that `null`. lib/eSignatures.ts:118-134 inserts into e_signatures with only `user_agent` as provenance — no reauth method, no reauth timestamp, and the `metadata JSONB` column (20260720_e_signatures.sql:37) is never written.",
    "note": "The comment's own claim in the file header (lib/eSignatures.ts:48-52) is not enforced anywhere server-side: the RLS INSERT policy (20260720_e_signatures.sql:55-60) checks only `signer_user_id = auth.uid()` plus active membership, so any code holding the session — including the other two callers, components/signatures/SignatureCaptureHost.tsx:50 and lib/acknowledgments.ts:429 — reaches recordSignature without passing through the ceremony. The 'defaults to satisfied' and 'no record on the signature' halves are both literally true."
  },
  {
    "id": "ROUTE-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "workflow.ts:126-131 exposes 'Return with Questions' → action 'reject' at PENDING_ENG_TEAM, and ticketTransitions.ts:273-278 `case \"request_revision\": case \"reject\": case \"reject_final\": updates.status = \"REVISION_REQ\"; updates.revision_count = …` — with no unread_by assignment (unlike engineer_request_revision at :257, which sets `if (ticket.assignedDrafterId) updates.unread_by = [ticket.assignedDrafterId]`), so ticketTransitions.ts:317 `const recipients = (updates.unread_by as string[]).filter(...)` is empty and nobody is notified. ticketAttention.ts:68-69 matches REVISION_REQ only when `ticket.assignedDrafterId === uid`; the management block (:84-95) lists PENDING_ASSIGNMENT/ENG_INITIAL/REVIEW/FINAL_APPROVAL/IFC and omits REVISION_REQ; the drafter pool (:79) matches PENDING_ASSIGNMENT only. Reachability confirmed: ticketTransitions.ts:179-189 request_eng_review sets PENDING_ENG_TEAM without assigning a drafter.",
    "note": "The attention/notification hole is real and reachable — no red dot for anyone AND no notification is sent at all on this transition. Two overstatements in the framing, worth recording but not enough to refute: it does not vanish from every queue (requests/page.tsx:347 gives management a 'Revision Status' card counting exactly `t.status === 'REVISION_REQ'`, :427 styles such rows as urgent, and :873 offers a 'Revisions Required' status filter), and it is not unactionable — workflow.ts:77 `canActAsDrafter = isDrafterIdentity || allows('ticket.draft_work')` lets any Drafter act on it at :167."
  },
  {
    "id": "ROUTE-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/ticketRouting.ts:33-34 `.select(\"uid, role, display_name, email\")` (no `roles`), :79 `const byRole = (r: Role) => members.filter((m) => m.role === r);`, :90-93 `const supervisors = byRole(\"DraftingSupervisor\"); if (supervisors.length === 0) return admins;`. Contrast lib/notify/recipients.ts:48-60, which selects `\"uid, role, roles\"` and does `const held = m.roles && m.roles.length > 0 ? m.roles : m.role ? [m.role] : [];`.",
    "note": "Confirmed, and the scenario is exact: supabase/migrations/20260722_member_roles_collection.sql:12-13 adds the additive `roles` column, app/(protected)/admin/users/page.tsx:130-137 writes `{ roles: cleaned, role: primaryRole(cleaned) }`, and ROLE_RANK (lib/roleCapabilities.ts:76,78) puts Manager 90 above DraftingSupervisor 75 — so ['Manager','DraftingSupervisor'] stores headline 'Manager', byRole('DraftingSupervisor') returns [], and every drafting request falls back to the whole Admin pool."
  },
  {
    "id": "ROUTE-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/requests/new/page.tsx:353 `metadata: { request_type: requestType, priority, unit }` (no action/status); components/documents/CheckInPanel.tsx:276-283 passes no metadata at all. The server supersede filters them out: app/api/tickets/workflow-action/route.ts:329 `.not(\"metadata->>action\", \"is\", null)`. The client reconcile skips them too: hooks/useTicketNotifications.ts:188-193 `n.filter((r) => r.resourceId && r.metadata && typeof r.metadata.status === 'string' && r.metadata.action != null)`.",
    "note": "Confirmed on both halves. Creation rows carry neither metadata.action nor metadata.status, so they are excluded from the supersede UPDATE and from the staleness reconcile, and once the ticket closes it also drops out of the ticket feed (line 156 `.neq('status','CLOSED')`) so the row can no longer even be folded into a live ticket item at line 283. Only a manual click or mark-all-read retires them."
  },
  {
    "id": "ROUTE-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "ticketAttention.ts:84-95 returns true for any MANAGEMENT_ROLES holder (which includes \"DraftingSupervisor\", :26) at PENDING_IFC, and :106-108 for DocCtrl at FINAL_DRAFT and PENDING_IFC; attentionLabel(:124) renders PENDING_IFC as \"Issue the IFC package\". The workflow engine disagrees: workflow.ts:301-302 `case 'PENDING_IFC': if (canActAsDrafter) {` — and canActAsDrafter is `isDrafterIdentity || allows('ticket.draft_work')` (:77), whose default roles are `[\"Drafter\"]` only (capabilityPolicy.ts:70). workflow.ts:319 `case 'FINAL_DRAFT': if (canActAsRequester || allows('ticket.direct_approve') || isManagement)` — DocCtrl is in none of those sets (ticket.direct_approve defaults to [\"Engineer\"], ticket.manage to MGMT = Admin/Manager/Supervisor at capabilityPolicy.ts:55).",
    "note": "Confirmed, and the mismatch is wider than stated: DraftingSupervisor is in MANAGEMENT_ROLES for attention purposes but is NOT in ticket.manage's default roles, so it also gets action-required badges at PENDING_ENG_INITIAL, PENDING_REVIEW and PENDING_FINAL_APPROVAL with no matching action. The rows are recomputed from isActionRequired on every render (useTicketNotifications.ts:253-273) with no dismissal path, so the badge persists until someone else moves the ticket."
  },
  {
    "id": "ROUTE-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "(a) recipients.ts:34-41 is reached only via dispatch.ts:72 `if (input.audience.followers) tasks.push(resolveFollowers(input.resource));`, and every `followers: true` in the repo passes a document/library resource (documents/[libraryId]/page.tsx:2313, holds.ts:252, postPublish.ts:46 and :60); the sole ticket-resource emit, transitionIn.ts:344-352, uses `audience: { involved: recipients.map(...) }`. (b) ticketRouting.ts:98 `case \"PENDING_ENG_INITIAL\":` is unreachable — `grep -rn PENDING_ENG_INITIAL` finds no code that ever assigns that status (workflow.ts:47 getInitialStatus always returns 'PENDING_ASSIGNMENT' and no case in ticketTransitions.ts sets it). (c) public/sw.js:226-239 implements the push handler, but grep for `web-push|webpush|VAPID|pushManager` across the repo (excluding node_modules) returns nothing — push_subscriptions appears only in schemaExpectations.ts:99, exportTables.ts:167 and dataRestore.ts:92.",
    "note": "All three dead paths confirmed, and the migration/comment overclaims are real. One qualification on (a): ticket followers are not actually unnotified — computeTransition folds `ticket.watchers` into unread_by (ticketTransitions.ts:300-304) and returns them as recipients (:317), and lib/subscriptions.ts:9 `WatchResourceType = \"document\" | \"project\" | \"asset\" | \"library\"` has no ticket variant, so there is no way to create a ticket subscription row in the first place. That branch is redundant dead code rather than a delivery gap; (b) and (c) are unambiguous."
  },
  {
    "id": "ROUTE-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/documents/CheckInPanel.tsx:45 `import { notifyMany } from \"@/lib/inAppNotifications\";` and :276 `await notifyMany({ orgId: doc.orgId!, userIds: recipients.map((m) => m.uid), ... })` vs app/(protected)/requests/new/page.tsx:342 `await emit({ orgId: activeOrgId, category: 'assignment', ... audience: { involved: recipients.map((m) => m.uid) } })`. lib/inAppNotifications.ts:121-137 shows notifyMany only calls notify() (a `notifications` row insert); lib/notify/dispatch.ts:85 `const channels = input.channels ?? [\"inapp\", \"email\"];` and :107-128 show emit() additionally calls queueEmail per recipient.",
    "note": "Confirmed: the check-in path fans out through notifyMany (bell rows only) while the portal path and the intake-collision path (lib/transitionIn.ts:339) both use emit(), which defaults to inapp+email. Same recipient pool (resolveTicketRecipients(..., 'PENDING_ASSIGNMENT')), two different channel sets."
  },
  {
    "id": "ROUTE-5",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "Repo-wide grep for `resolveTicketRecipients` returns exactly three call sites, all creation paths: app/(protected)/requests/new/page.tsx:340, components/documents/CheckInPanel.tsx:274, lib/transitionIn.ts:337. app/api/tickets/workflow-action/route.ts:148 takes its recipients from computeTransition — `const { updates, newStatus, recipients, newComment } = computeTransition(ticket, input);` — and route.ts:310 `if (recipients.length === 0) return;`. In lib/ticketTransitions.ts the `approve_team` case (line 190-192) sets only `updates.status = \"PENDING_ASSIGNMENT\"`, so recipients fall back to line 140's `[ticket.requesterId, ticket.assignedDrafterId]`.",
    "note": "The mechanical claim is right — no transition ever consults lib/ticketRouting, so the DraftingSupervisor pool gets no bell row and no email when a ticket re-enters PENDING_ASSIGNMENT. But 'announced to nobody in that queue' overstates it: lib/ticketAttention.ts:84-93 makes PENDING_ASSIGNMENT action-required for every management/DraftingSupervisor role and :79 for every Drafter, so the ticket does appear in their sidebar badge and /inbox feed. Missing push, not missing visibility — LOW."
  },
  {
    "id": "ROUTE-6",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/ticketTransitions.ts:300 `if (Array.isArray(updates.unread_by) && (updates.unread_by as string[]).length > 0 && (ticket.watchers ?? []).length > 0)` — watchers are folded in ONLY when a personal stakeholder is already there. But the self_assign case (lines 201-207) sets no unread_by, so it falls through to line 140: `const newUnreadBy = [ticket.requesterId, ticket.assignedDrafterId].filter((id): id is string => !!id && id !== actorUid);` — for a pick-up the requester is still in that list, so unread_by is non-empty and the watchers ARE merged.",
    "note": "The general guard bug is real (a transition whose only stakeholder is the actor drops every watcher), but the headline scenario is false: on 'Pick Up Ticket' the drafter is not the requester, so unread_by = [requesterId], line 300 fires, and the watching supervisor is notified. The silent case is the narrow one where the actor is both requester and (non-)drafter — hence LOW."
  },
  {
    "id": "ROUTE-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "hooks/useTicketNotifications.ts:31 `const OPEN_TICKET_CAP = 500;` and :156 `.neq('status', 'CLOSED').order('last_modified', { ascending: false }).limit(OPEN_TICKET_CAP)` — same ordering+cap at :160, :161 (drafter assigned + PENDING_ASSIGNMENT pool) and :170 (requester view).",
    "note": "Confirmed exactly as claimed: newest-modified-first with a hard 500 cap means the least-recently-touched open tickets are the first excluded, and lib/ticketAttention.isActionRequired only ever runs over the rows that survived the cap. No compensating query exists — the feed is the only source for the badge, bell and /inbox."
  },
  {
    "id": "ROUTE-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/requests/[id]/page.tsx:244-247 `.from('org_members').select('uid, email, role').eq('org_id', activeOrgId).eq('role', 'Drafter').eq('status', 'active')`. Contrast components/requests/EngineerPickerModal.tsx:77-85, which selects `\"uid, email, role, roles\"` and filters `const held = Array.isArray(m.roles) && m.roles.length > 0 ? m.roles : [String(m.role ?? \"\")]; return held.some((r) => r.includes(\"Engineer\"));`.",
    "note": "Confirmed, and lib/roleCapabilities.ts:74-94 makes the scenario exact: Engineer-3 ranks 63 vs Drafter 50, so primaryRole(['Engineer-3','Drafter']) = 'Engineer-3' and the `.eq('role','Drafter')` filter excludes her. The self-assign fallback is also closed to her: lib/workflow.ts:65 calls policyAllows with `extraRoles = null`, so ticket.self_assign is evaluated against the headline role only. The server (route.ts:113-123) would have accepted her as an assignee — only the picker blocks it."
  },
  {
    "id": "ROUTE-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Repo-wide grep: `isPastDue`/`isNearingDue` (lib/notifications.ts:257,265) have exactly two non-test consumers, both client render paths — app/(protected)/requests/[id]/page.tsx:1758-1761 and app/(protected)/requests/page.tsx. `sla_breach_warned_at` / `sla_breached_at` (supabase/schema.sql:433-434) are never written by any code. No `category: \"sla\"` emit exists anywhere, so lib/notify/dispatch.ts:56 `case \"sla\": return \"sla_warning\";` is unreachable, while app/(protected)/settings/notifications/page.tsx:143 renders the 'SLA warnings' toggle. app/api/cron/maintenance/route.ts's scan list (lines ~155-165: review-cycles, read-understood, pre-publish-review, effective-dates, retention, access-recert, distribution-acks) contains no ticket SLA scan, and /api/cron has only `maintenance` and `embed-drain`.",
    "note": "Confirmed by absence search: the SLA clock has no server-side reader, the breach columns are dead, and the only 'sla_warning' plumbing is a preference toggle plus an unreachable dispatch branch."
  },
  {
    "id": "RT-1",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "The healing path the finding missed: useTicketNotifications.ts:223-226 wires BOTH subscriptions to the same full re-read — `.on('postgres_changes', { event: '*', ... table: 'tickets', filter: org_id=eq… }, () => { if (alive) void fetchAll(); })` and the same for `notifications` — and fetchAll (lines 151-217) re-queries tickets and `listMyNotifications({ onlyUnread: true, limit: 50, orgId })` from scratch. Separately, app/(protected)/inbox/page.tsx:118-127: `const tick = () => { if (document.visibilityState === \"visible\") void refresh({ background: true }); }; window.addEventListener(\"focus\", tick); document.addEventListener(\"visibilitychange\", tick); const id = window.setInterval(tick, 60_000);`",
    "note": "The underlying gap is real — nothing re-reads on reconnect/refocus for the persistent bell and sidebar (RoleContext.tsx:343-354 only calls `supabase.auth.getSession()`, and the TOKEN_REFRESHED branch at :288-294 re-sets uid to the same value so the hook's deps never change) — but two words of the title are wrong. 'Permanently lost' is false: because every realtime callback runs a complete fetchAll rather than applying a delta, the FIRST event to arrive after the socket rejoins reconciles the entire feed, so the stale window ends at the next org ticket/notification event. 'Nothing reconciles on refocus or visibility change' is also false for /inbox, which polls loadInbox on focus, visibilitychange and a 60s timer. Downgrade to MEDIUM: bounded staleness on the badge surfaces, not permanent data loss."
  },
  {
    "id": "RT-10",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "settings/notifications/page.tsx:24-31 — the entire Prefs interface is `email_enabled, email_on_mention, email_on_assignment, email_on_status_change, email_on_watched_activity, email_on_sla_warning, digest_frequency`; there is no in-app field. The page's own header comment (:8-9) states \"In-app bell notifications are always on — they're the persistent inbox; the email side is the opt-in noise layer\", echoed in the UI subtitle at :113. dispatch.ts:87-102 sends the `inapp` leg with no preference lookup at all, while :104-106 notes email \"already checks notification_preferences\". NotificationListener.tsx:92-97 calls showToast unconditionally.",
    "note": "The absence is real and I confirmed it repo-wide — no in-app or toast preference column, and the inapp dispatch leg is unguarded. Correcting severity because this is an explicitly documented product decision rather than a defect; its actual pain comes from RT-2 (org-wide toast fan-out), where the fix belongs."
  },
  {
    "id": "RT-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "ToastProvider.tsx:41-42 `setToasts((prev) => [...prev, { id, type, title, message, duration }]);` — pure append, no slice/cap anywhere in the file; removal is only the per-toast `setTimeout` at :45-49. CornerDock.tsx:22-27 `className=\"fixed bottom-4 right-4 z-[300] flex flex-col items-end gap-2 pointer-events-none max-w-[calc(100vw-2rem)]\"` — a max-WIDTH but no max-height and no overflow container, and ToastProvider.tsx:57-59 portals a plain `flex flex-col gap-2` of `w-80` cards into it. Burst source confirmed: postPublish.ts:36-60 issues two separate emits per document (`doc_superseded` to intent-holders, `library_doc_revised` to followers), each writing a notifications row that NotificationListener.tsx:92 toasts.",
    "note": "Confirmed. Bottom-anchored with no height bound means the stack grows upward past the viewport top with no way to scroll to it, and a user who is both an intent-holder and a library follower does receive 2 notification rows per document in a bulk rev-up."
  },
  {
    "id": "RT-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Repo-wide `grep -rn 'ALTER PUBLICATION' --include=*.sql` returns exactly 12 lines — schema.sql:1139-1147 (tickets, documents, checkout_sessions, checkout_messages, checkout_episodes, notifications, collections, org_members, libraries) plus 20260727_checkout_activity_fix.sql:51,54 and 20260729_checkout_episodes.sql:98. Neither `milestones` nor `table_views` appears. Yet ScheduleTab.tsx:108-110 subscribes `.channel(\\`milestones-${projectId}\\`).on(\"postgres_changes\", { ... table: \"milestones\", filter: \\`project_id=eq.${projectId}\\` }, ...)` and tableViews.ts:259-262 subscribes to `table_views` (`const TABLE = \"table_views\"`, line 7).",
    "note": "Confirmed by exhaustive search of every ALTER PUBLICATION in the repo; there is no CREATE PUBLICATION ... FOR ALL TABLES anywhere either, so both channels subscribe successfully and can never receive an event. ScheduleTab's 600ms debounce (:112-116) and its 'edits stream in' comment are dead code as shipped."
  },
  {
    "id": "RT-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "NotificationListener.tsx:46 `filter: \\`org_id=eq.${activeOrgId}\\`` — the only scoping on the checkout_messages subscription. RLS is org-wide, not document-wide: 20260727_checkout_activity_fix.sql:27-30 `CREATE POLICY checkout_messages_org_select ON checkout_messages FOR SELECT USING (EXISTS (SELECT 1 FROM org_members WHERE ... uid = auth.uid() AND status = 'active'))`. Second leg: NotificationListener.tsx:84 subscribes to `notifications` `filter: user_id=eq.${uid}` and toasts every INSERT (line 92-97), and activityThread.ts:153-164 writes exactly such a row (`kind: \"checkout_message\"`) via notifyMany to participants/session-holders/watchers for the SAME post. Mounted unconditionally at app/(protected)/layout.tsx:60.",
    "note": "Confirmed on both counts: any active org member receives (and is toasted for) every checkout message in the workspace, and thread participants/watchers additionally get a second toast from the notifications channel for the same post. One arithmetic correction to the scenario: line 62-63 (`isMe`) suppresses the author's own message, so each of the two draftsmen gets ~12 toasts (6 incoming x 2 channels), not 24; the org's other 38 members get 12 each from the checkout-messages leg."
  },
  {
    "id": "RT-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "useTicketNotifications.ts:141 `const channelId = useId()...` — unique per instance, so instances do NOT share a channel; :221-227 each opens `attention-${activeOrgId}-${channelId}` and both `.on(...)` handlers are bare `() => { if (alive) void fetchAll(); }` with no debounce/throttle. fetchAll pulls `.select('*')...limit(OPEN_TICKET_CAP)` with `OPEN_TICKET_CAP = 500` (:31, :156). Always-mounted consumers: NotificationCenter.tsx:55 inside CenterPanel, which the provider renders unconditionally (`<CenterPanel isOpen={isOpen} .../>`), Sidebar.tsx:124, and NotificationBell.tsx:54 via TopBar.tsx:230 — all three inside app/(protected)/layout.tsx (Sidebar :56, TopBar :58, NotificationCenterProvider :145). Two more on /dashboard (widgets.tsx:669, :1223) and one on /inbox (inbox/page.tsx:38).",
    "note": "Claim is accurate as stated. I looked specifically for a shared channel, a memoized/deduped fetch, or a debounce and found none — useId() deliberately makes the channels distinct, and each org-wide tickets event fans out to every mounted instance's unthrottled fetchAll."
  },
  {
    "id": "RT-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Repo-wide `grep -rn 'REPLICA IDENTITY' supabase/` returns exactly three lines: 20260727_checkout_activity_fix.sql:59-60 (checkout_messages, notifications) and 20260729_checkout_episodes.sql:102 (checkout_episodes). The published set is larger — schema.sql:1139-1147 adds tickets, documents, checkout_sessions, collections, org_members, libraries. The workaround is local to collections: libraryCollections.ts:419-427 adds a SECOND, UNFILTERED listener with the comment \"DELETE events carry ONLY the old row's primary key — never library_id — so the filtered listener above can not match them\". documents/[libraryId]/page.tsx:1532-1535 has only the filtered `library_id=eq.` listener, and a real hard-delete path exists at documents/[libraryId]/page.tsx:1011 `await supabase.from(\"documents\").delete().eq(\"id\", id)`.",
    "note": "Confirmed: the fix was applied once (collections) and never generalised, and a hard-delete path for documents genuinely exists. Small overstatement worth noting: a filter keyed on the PK itself (e.g. `id=eq.`) would still match a DELETE under REPLICA IDENTITY DEFAULT — but the app's only such filters are UPDATE-only (requests/[id]/page.tsx:937, documents/[libraryId]/page.tsx:1491) or on an unpublished table (tableViews.ts:261), so the practical claim holds."
  },
  {
    "id": "RT-5",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "NotificationListener.tsx:17 `isFirstRun.current = true;` is set synchronously, cleared only at :33 `isFirstRun.current = false;` after the awaited seed query (:21-27), while the handler at :49 does `if (isFirstRun.current) return;` — so anything arriving between socket join and seed completion is dropped. `processedIds` (:12) is a useRef Set with no clearing anywhere in the file.",
    "note": "Both mechanics are real, but the impact is much smaller than MEDIUM implies. (a) The seed exists to suppress backfill that Postgres CDC never sends — INSERT events are live-only — so it drops toasts without buying anything. (b) The specific scenario in the summary is self-mitigating: a handoff note also writes a notifications row (activityThread.ts:153-164) which the SECOND channel (line 80-100) toasts with no seed guard, so a participant still gets the toast. (c) The 'unbounded' Set grows by one UUID string per org checkout message — a week of heavy use is kilobytes, not a leak worth MEDIUM."
  },
  {
    "id": "RT-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/inAppNotifications.ts:10-58 declares ~52 NotificationKind values; sectionForKind (useTicketNotifications.ts:71-103) maps only 23 of them and sends the rest to `default: return 'other'` (:100-102). Sidebar.tsx renders `...badgeOf(sectionCounts.documents)`, `...badgeOf(sectionCounts.projects)` and `...badgeOf(sectionCounts.requests)` — and nothing else; `grep -rn -i scratchpad components/navigation/Sidebar.tsx` returns zero hits, so the 'scratchpad' section it computes has no nav item at all. Meanwhile useTicketNotifications.ts:284/315 folds every kind into `count: items.length`, which NotificationBell.tsx:55 badges.",
    "note": "Accurate, and if anything understated: 32 of ~52 kinds land in 'other' and 3 more in the unrendered 'scratchpad', so roughly two-thirds — not half — of kinds inflate the bell while badging nothing in the rail. ack_requested, every review_*/ack_* kind, library_doc_added/revised and effective_now are all in that unbadged set."
  },
  {
    "id": "RT-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Repo-wide `grep -rn \"pushManager|VAPID|vapid|webpush|web-push|applicationServerKey\"` over all .ts/.tsx/.js (excluding node_modules) returns ZERO hits, and `grep -rn \"requestPermission|new Notification(\"` also returns zero. public/sw.js:225-238 has a complete `self.addEventListener(\"push\", ...)` handler and :240-256 a `notificationclick` handler; ServiceWorkerManager.tsx:31-32 only calls `navigator.serviceWorker.register(\"/sw.js\")`. The only references to the table anywhere are non-functional inventory: schemaExpectations.ts:99, exportTables.ts:167, dataRestore.ts:92.",
    "note": "Confirmed by repo-wide absence search. The SW handler and the push_subscriptions table exist; there is no subscribe call, no VAPID key, and no sender, so no push can ever be delivered."
  },
  {
    "id": "RT-8",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "useTicketNotifications.ts:207 `await markManyRead(staleIds)` inside fetchAll, which is `supabase.from(\"notifications\").update({ read_at: ... }).in(\"id\", ids)` (inAppNotifications.ts:193-196) — and :225-226 subscribes the same instance to `{ event: '*', table: 'notifications', filter: user_id=eq.${uid} }` calling fetchAll on every event.",
    "note": "The write-inside-the-subscribed-read and the N-way race are real, but this is bounded, not a feedback loop: listMyNotifications is called with `onlyUnread: true` (:176) and applies `q.is(\"read_at\", null)` (inAppNotifications.ts:170), so the rows just marked read cannot come back on the echo pass, workflowRows is empty, and no second write is issued. The concurrent writes are also idempotent (same UPDATE, same rows). Net effect is one extra round of fetches per instance — a sub-case of RT-3's amplification rather than a MEDIUM defect of its own."
  },
  {
    "id": "RT-9",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "useTicketNotifications.ts:315 `totalNotifications: items.length` / :311 `count: items.length`, where `items` is the union of ticket rows pushed at :258-271 (kept when `isActionRequired` OR unread, :252-256) and notification rows at :285-300. `markAllRead: () => markAllRead(activeOrgId)` (:323) only touches the notifications table (inAppNotifications.ts:201-205), so ticket-sourced items survive it. NotificationBell.tsx:55 `const unread = count;`",
    "note": "Factually correct — Mark-all-read provably cannot drive the badge to zero while an action-required ticket exists. But the 'vocabulary has no fixed point' half is weaker than stated: every label around the number already says attention, not unread (NotificationBell.tsx:104 `${unread} need${...} attention`, :137 same, :139 'All caught up'), and the Mark-all-read button is gated on `hasNotifRows` (:87) so it disappears once the notification rows are cleared. This is a documented design union (hook header comment :12-26), not a miscount — LOW."
  },
  {
    "id": "SCHED-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/scheduleParsers.ts:247 `const outlineLevel = Number(outlineLevelRaw) || 1;` — Number(\"0\") is 0, falsy, so OutlineLevel 0 becomes 1. Parenting at :252 `if (outlineLevel > 1)` then never fires for it, and real level-1 phases also skip it, so the row ends childless at the top. Leafness is computed from CHILDREN, never from is_summary: lib/milestones.ts:657-659 `const parentIds = new Set<string>(); for (const m of milestones) { if (m.parentId) parentIds.add(m.parentId); } const isLeaf = (m) => !(m.id && parentIds.has(m.id));`; lib/executionReport.ts:121-124 `const isLeaf = (m) => !m.id || (childrenByParent.get(m.id) ?? []).length === 0;`; lib/criticalPath.ts:43 `const isLeaf = (m) => !milestones.some((c) => c.parentId === m.id);`.",
    "note": "Holds, and the CSV path proves the coercion is the bug: scheduleParsers.ts:681 `const outlineLevel = outRaw ? Number(outRaw) : null` preserves 0, and reconstructHierarchyFromOutline (:847-852) walks `for (let l = lvl - 1; l >= 0; l--)` — level 0 works correctly there. Extra unclaimed harm: the summary row is an incomplete leaf finishing at the job finish and starting at the job start, so criticalPath.ts seeds it into the chain and the backward walk terminates in one step, collapsing the critical path."
  },
  {
    "id": "SCHED-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/criticalPath.ts:56 `let frontier = leaves.filter((m) => incomplete(m) && finishMs(m) >= projFinish - slack);` — every unfinished leaf near the finish is seeded, float or not; lines 71-78 `const next = preds.filter((m) => finishMs(m) >= latestPredFinish - slack); for (const m of next) if (m.id) chain.add(m.id); frontier = next;` — plural, so parallel chains merge; line 68 `finishMs(m) >= earliestStart - 14 * 86400000` bounds the backward walk, and `if (preds.length === 0) break;` (line 70) truncates the chain at any larger gap; lines 81-84 `if (m && typeof m.durationHours === \"number\") remainingHours += m.durationHours;` sums FULL duration with no `(1 - percent)` factor.",
    "note": "All four sub-claims verified verbatim. Mitigating context worth noting: ExecutionReportView.tsx:88 does print 'heuristic — based on schedule shape, not dependency links' next to the number, so the report surface is labelled; the timeline surface (ExecutionView.tsx:723-731, 1503-1504) is not."
  },
  {
    "id": "SCHED-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "The drag path is components/projects/ScheduleTab.tsx:336 `await applyMilestoneMoves({...})`, which goes straight to lib/milestones.ts:294 `await supabase.rpc(\"apply_milestone_moves\", ...)`. The per-task breadcrumb lives only in `updateMilestone` (lib/milestones.ts:260-270 `addMilestoneNote({... kind: \"reschedule\" ...})`), which the RPC path bypasses entirely — it is reached only in the PGRST202 pre-migration fallback (lines 303-311). The single audit row at lines 321-326 ends `.then(() => undefined, () => undefined);` — errors discarded — and its payload is `details: { count: ..., ids: ...slice(0, 50) }` with no before/after dates. supabase/migrations/20260907_milestone_batch_move.sql:49-59 is a bare `FOR v_move IN ... UPDATE milestones SET planned_start_at = ..., planned_at = ... LOOP` with no note/history write.",
    "note": "Claim confirmed on every point. listMilestoneNotes (lib/milestones.ts:557-563) reads milestone_notes, which the RPC never writes, so a dragged task's activity panel genuinely shows only status changes."
  },
  {
    "id": "SCHED-12",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "Two of the finding's own examples are contradicted by the cited files. (a) The printable report DOES qualify the critical path: components/projects/ExecutionReportView.tsx:88 `<span ...>heuristic — based on schedule shape, not dependency links</span>` sits in the same row as line 86's '{n} tasks on the critical path'. (b) The percent-complete is not a task count: lib/executionReport.ts:165-167 `// Effort-weighted % that counts partial progress (a 90%-done task is 90%, not 0). ... const pctComplete = wsum > 0 ? Math.round(wpct / wsum) : 0;` with `wpct += w * lp` at line 159 where `lp = leafPercent(m)`. What does hold: lib/executionReport.ts:179-186 `const ratePerDay = tally.done / elapsedDays; ... forecastFinish = new Date(now + daysLeft * DAY)` rendered as a bare 'Forecast finish' date (ExecutionReportView.tsx:70-78), the unqualified timeline labels (ExecutionView.tsx:723-731 title=\"Highlight the unfinished tasks driving the finish date\", 1503 title=\"On the critical path — drives the finish date\"), and lib/scheduleParsers.ts:152 `The XML import is a true 1:1 copy (every date, dependency, resource, and the full hierarchy).` while parseMsProjectXml reads only `<PredecessorUID>` and discards link Type and LinkLag (all links become FS+1day via scheduleReflow.ts:352).",
    "note": "The core observation — a heuristic forecast, an unlabelled timeline 'critical path', and an over-promising '1:1 copy' message — is real, but two of the three headline items are wrong as written: the end-of-job report is the one surface that IS labelled heuristic, and pctComplete is effort-weighted and partial-aware (its degeneracy on imported schedules is SCHED-2's point about durationHours never being populated, not 'a task count'). Presentational wording only; LOW."
  },
  {
    "id": "SCHED-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/scheduleReflow.ts:88 `const DAY_MS = 86_400_000;` and line 352 `if (finish.has(pred)) req = Math.max(req, finish.get(pred)! + DAY_MS);` — the required successor start is the predecessor's finish INSTANT plus exactly 24h, so a 17:00 finish forces a 17:00 start the next day and each link adds the gap cumulatively. lib/scheduleReflow.ts:438 `const desiredStart = cursor === null ? curStart : cursor + DAY_MS;` does the same in sequenceSiblings. The behaviour is frozen by lib/__tests__/dependencies.test.ts:28-49, which asserts the whole-day rule on midnight-only fixtures (`const d = (s) => \"${s}T00:00:00.000Z\"`), so clock-time drift is invisible to the suite.",
    "note": "Confirmed. There is no lag/relationship-type field anywhere in ReflowNode (only `dependsOn: string[]`), and no calendar/working-hours awareness, so the hardcoded 24h is the only spacing rule; it is reached on every drag through ExecutionView.tsx:449-461 `withCascade`."
  },
  {
    "id": "SCHED-14",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/scheduleProgress.ts:44-50 `const d = m.durationHours; if (d != null && ... && d > 0) return d; const w = m.weight; if (w != null && ... && w > 0) return w; return 1;` — the SAME function returns hours for one task and a unit weight for its neighbour, and the results are summed into one denominator at lines 141 (`a.wsum > 0 ? Math.round(a.wpct / a.wsum) : 0`), 159-165 (`wsum += w; wpct += w * leafPercent(m)`), lib/executionReport.ts:158-159, and lib/milestones.ts:663-669 (`totalWeight += w; ... earnedValue += w * (leafPercent(m) / 100)` — the EV/SPI rollup).",
    "note": "Premise verified: no parser populates durationHours (grep of lib/scheduleParsers.ts shows only the type declaration at line 52 and `weight: 1` literals at 308/359/406/495/550), so the only source is the manual 'Work hours' field at components/projects/TaskDetailPanel.tsx:520 — exactly the 'tag a few tasks' scenario the finding describes, and nothing warns that doing so re-weights every other rollup."
  },
  {
    "id": "SCHED-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "A repo-wide grep for `durationHours` in lib/scheduleParsers.ts returns exactly one hit — line 52, the interface field declaration. Neither parseMsProjectXml (:304-320), parseP6Xml (:402-412), parseP6Xer (:546-556) nor either CSV parser ever assigns it; all four set `weight: 1`. MS Project's Work is stringified: :295-296 `const workRaw = childText(t, \"Work\"); if (workRaw) attributes.work = workRaw;`. lib/milestones.ts:981 `baseFields.duration_hours = r.durationHours ?? null;` therefore always writes NULL on import. lib/scheduleProgress.ts:44-50 then falls back to weight (1), and lib/executionReport.ts:106 `hoursOf` returns 0, so executionReport.ts pctHours `= plannedHours > 0 ? … : pctComplete`.",
    "note": "Confirmed by absence with a repo-wide search. The user-visible end of it is real: ExecutionReportView.tsx:62-68 prints a 'Work hours' card whose big number is silently pctComplete over a `0 / 0 h` sub-line. Only a hand edit in TaskDetailPanel.tsx:467 can ever populate the column."
  },
  {
    "id": "SCHED-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260614_phase7_milestones.sql:88-91 `CREATE POLICY \"milestones_member_all\" ON milestones FOR ALL TO authenticated USING (EXISTS (SELECT 1 FROM org_members … status = 'active')) WITH CHECK (same)` — the only write policy; the RESTRICTIVE guard in 20260818_followups_rls.sql:43-49 covers DELETE only, so any active member can set or clear baseline_*. lib/milestones.ts:1479-1489 fires one UPDATE per row inside Promise.all, collecting errors and returning `{ ok: errors.length === 0, count }` — a partial baseline. clearBaseline (:1504-1514) writes four NULLs with NO audit call at all, unlike setBaseline (:1497). apply_milestone_moves (20260907…:50-58) rewrites planned_start_at/planned_at with no baseline read. Repo-wide grep: 'baseline' appears in components/projects only in TaskDetailPanel.tsx:480-483, ScheduleTab.tsx:201-215, SchedulePulse.tsx:49-52 and ExecutionReportView — in ExecutionView.tsx (the Gantt) the only hits are :291,:299 about zoom fitting.",
    "note": "All four sub-claims verified. Sharpest point the finding understates: setBaseline is audited but clearBaseline is not, so any active member can erase the approved plan silently. ScheduleTab.tsx:206 promises 'Every view will then show how far the schedule drifts from it' while the timeline draws no baseline bar; drift appears only in the execution report and the task detail panel."
  },
  {
    "id": "SCHED-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260907_milestone_batch_move.sql:28-31 `IF v_uid IS NULL THEN -- service role: trusted server code\\n NULL;\\n ELSE` — the membership lookup (:33-40) and the role/owner check (:41-47) are both inside the ELSE. The body then runs unconditionally (:50-58) `UPDATE milestones SET planned_start_at = …, updated_by = v_uid WHERE id = … AND org_id = p_org AND project_id = p_project;` under `LANGUAGE plpgsql SECURITY DEFINER` (:20), so RLS does not apply. No REVOKE/GRANT on this function exists anywhere in supabase/ — the repo's other sensitive RPCs are explicitly hardened (e.g. 20260929_mention_engine.sql:133-134 `REVOKE ALL ON FUNCTION graph_ask… FROM public, anon; GRANT EXECUTE … TO authenticated;`), which this migration omits.",
    "note": "Confirmed, including the claim of absence: the anon role's default PUBLIC EXECUTE is never revoked here, unlike every comparable RPC in the repo. The practical exploit is privilege escalation rather than pure anonymous access — milestones_member_all is `TO authenticated`, so ids must come from a signed-in session, but any member without schedule-editing rights can re-issue the call with a bare anon key and skip the role check entirely."
  },
  {
    "id": "SCHED-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/scheduleReflow.ts:345 `if (isLocked(s)) continue;` guards only the successor itself; :356-359 then moves its whole subtree unguarded — `for (const t of subtreeOf(sid)) { start.set(t, start.get(t)! + delta); finish.set(t, finish.get(t)! + delta); }`. Same shape in sequenceSiblings at :435 `if (isLocked(kid)) { cursor = …; continue; }` followed by :441-445 `for (const sid of subtreeOf(kid.id)) { start.set(sid, …+ delta); finish.set(sid, …+ delta); }`. computeTreeMove does it correctly at :161-162 `if (!isLeaf(sid)) continue; if (isLocked(byId.get(sid))) continue; // actuals don't move`, against the module's contract at :47-52.",
    "note": "Holds. The cited tests confirm the gap rather than close it: every case in scheduleReflowLocks.test.ts (:56-92) locks a DIRECT successor or a DIRECT sibling — none places a completed task beneath an unlocked parent that moves. Nor does the persistence path filter: ExecutionView.tsx:449-461 withCascade merges cascade output straight into the batch with no lock check."
  },
  {
    "id": "SCHED-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "A grep for `proj_id` across lib/scheduleParsers.ts returns nothing. parseP6Xml collects globally: :349 `const wbsNodes = Array.from(doc.getElementsByTagNameNS(\"*\", \"WBS\"));` and :381 `const acts = Array.from(doc.getElementsByTagNameNS(\"*\", \"Activity\"));` — no <Project> ancestry test. parseP6Xer reads `tables.get(\"PROJWBS\")` and the TASK table whole (:479-502, :527-557); the XER's proj_id column is never selected, and the only per-row branch is `const isRoot = wProj >= 0 && /^y/i.test(r[wProj]…)` on proj_node_flag, which merely makes EACH project's root WBS a parentless top-level row.",
    "note": "Confirmed by absence. No filter, no warning, and no project picker — ScheduleImportModal contains no mention of proj_id or multiple projects. The proj_node_flag handling actively produces the described symptom: every project in the file contributes its own top-level phase to the one target project."
  },
  {
    "id": "SCHED-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/scheduleProgress.ts:55-64 `deriveSummaryStatus` — `if (t.blocked > 0) return \"blocked\"; if (t.onHold > 0) return \"on_hold\"; if (t.started > 0) return \"in_progress\"; return \"planned\";` — there is no `missed` branch, and the `Agg` type (lines 77-80) and the leaf tally (lines 111-118) carry `done/blocked/onHold/started` only, never a missed counter. `missed` IS a first-class status (types/schema.ts:426-427 `\"planned\" | \"in_progress\" | \"completed\" | \"missed\" | \"blocked\" | \"on_hold\"`), and `leafPercent` (line 35-39) sends it through `clampPercent(m.percentComplete)` → 0 for an untouched missed task, so `started` stays 0.",
    "note": "Confirmed by repo-wide search: buildProgressIndex is the only rollup used by the timeline, calendar and detail panel (ExecutionView.tsx:249, ScheduleTab.tsx:178, TaskDetailPanel.tsx:69) and none of them re-checks for missed descendants, so an all-missed phase renders 'planned, 0%'. The only escape is a missed task that had a non-zero percent_complete logged before it was missed, which flips the parent to in_progress — still not 'missed'."
  },
  {
    "id": "SCHED-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/scheduleParsers.ts:585 `id: [\"unique id\", \"uid\", \"id\", \"task id\"]` combined with `findCol` (lines 634-637) `for (const c of cands) { const i = header.indexOf(c); if (i >= 0) return i; }` — first synonym present wins, so 'Unique ID' beats 'ID'. Row keys are built from that column at line 710 `externalRef: id ? \"${refTag}:${id}\" : ...`, while predecessor tokens are taken verbatim from the Predecessors cell at 692-698 `tok.trim().match(/^\\d+/)` → `msp:<n>`, and lib/milestones.ts:1090-1096 resolves them through the same `refToId` map (`r.dependsOnExternalRefs.map((ref) => refToId.get(ref))`).",
    "note": "The only guard present is `if (iPred >= 0 && iId >= 0)` (line 686), which checks that SOME id column exists, not that it is the same numbering the Predecessors column uses — MS Project's Predecessors field displays sequential IDs, not Unique IDs. Mis-resolved refs silently produce a wrong `depends_on` edge (no error surfaces, since only unresolvable refs are dropped by the `.filter(Boolean)` at 1095)."
  },
  {
    "id": "SCHED-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/milestones.ts:904-909 `const d = new Date(plannedStartIso); ... const h = d.getUTCHours(); return (h >= 6 && h < 18) ? \"day\" : \"night\";` fed by `coerceIsoMaybe` (895-900) which only appends `Z` to a bare `YYYY-MM-DD`, and by lib/scheduleParsers.ts:912 `if (/^\\d{4}-\\d{2}-\\d{2}T/.test(trimmed)) return trimmed;` — an offset-less `2026-06-01T19:00:00` is returned unchanged and parsed as browser-local by ES spec. Written once at lib/milestones.ts:975 `baseFields.shift = shiftFromStart(plannedStartIso);`.",
    "note": "Import runs client-side (components/projects/ScheduleImportModal.tsx:154 calls importMilestonesFromParsed in a \"use client\" component), so the local zone really is the importer's browser zone. Repo-wide grep for `shift` in lib/milestones.ts confirms line 975 is the ONLY write of the column — neither updateMilestone/applyMilestoneMoves nor the RPC recompute it, so it never follows a moved task; the filter at lib/scheduleFilter.ts:104 (`!f.shifts.includes(m.shift)`) trusts the frozen value."
  },
  {
    "id": "SEM-1",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "lib/knowledgeEmbedCore.ts:114 stamps whatever is saved now: `.update({ embedding: toVectorLiteral(vectors[i + j]), embedding_model: connection.model })`. app/api/knowledge/ask/route.ts:482-489 picks the space from one arbitrary row — `.not(\"embedding\", \"is\", null).not(\"embedding_model\", \"is\", null).limit(1).maybeSingle()` with NO order by — then `const corpusModel = (stamped?.embedding_model as string | null) ?? embedding.model;` and passes it as `p_model: corpusModel` (:505-507). 20261007_rag_hardening.sql:95 `AND (p_model IS NULL OR c.embedding_model = p_model)` excludes every row under the other stamp.",
    "note": "Mechanically correct: a mixed-stamp library is filtered to one stamp per ask and which stamp wins is unordered, so it can differ between asks. Lowered to MEDIUM because it needs an admin to change the saved embedding model mid-build, keyword retrieval is unaffected (the fuse at :512+ still runs), and both the reset comment (embed/route.ts:97-107) and the Rebuild dialog (SemanticIndexPanel.tsx:113-116, 'Do it after ingestion or the embedding model changes') name the exact remedy."
  },
  {
    "id": "SEM-10",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/ai/embeddings.ts:20-21 states the contract: \"Runs on the member's own key and is metered like every other AI call.\" But embeddings.ts:208-215 `embedQuery` does `const { vectors } = await embedPassages({...}); return vectors[0];` — the `usage` half of embedPassages' return (lines 195-201) is destructured away. In ask/route.ts the only accumulator is askUsage, fed solely by `call()` (line 273-275: `askUsage.inputTokens += out.usage.inputTokens`), and line 469-477 calls `embedQuery` directly, not through `call`. `meter()` at :280 therefore writes a row whose model is the CHAT model with zero embedding tokens. Repo-wide grep: embedQuery has exactly one non-test caller (ask/route.ts:477) and no recordAskUsage anywhere near it.",
    "note": "The mechanism is exactly as claimed — up to 5 query embeddings per ask (3 at :476 `texts.slice(0,3)` plus 2 at :703 `plan.queries.slice(0,2)`) are billed by the provider and never metered. But the magnitude is negligible: a query is tens of tokens, so 10,000 query embeddings on voyage-3.5-lite is a fraction of a cent — it cannot meaningfully distort the ledger or the cap. It is a broken documented contract, not a billing problem; LOW."
  },
  {
    "id": "SEM-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "knowledgeEmbedDrain.ts:51-56 `supabaseAdmin.from(\"knowledge_libraries\").select(...).not(\"ai_features->embedBuild\",\"is\",null).limit(6)` — no `.order()`. Cap path :92-95 `if (capUsd > 0 && month.spentUsd >= capUsd) { drained.push({... note: \"monthly cap reached\" }); continue; }` — `setEmbedBuildMarker(lib.id, null)` is NOT called. Error path :113 `if (slice.error) { drained.push(...); break; }` then :133 `if (remainingAfter === 0) await setEmbedBuildMarker(lib.id, null)` never fires because remaining > 0. Contrast :83 and :71, the only two places the marker IS cleared (no key / already complete). maintenance/route.ts:293 `drainEmbedBacklog({ scopeOrgIds: null, budgetMs: 100_000 })` — the global path takes the same unordered 6.",
    "note": "All three cited defects are real, and the finding's own scenario is the one where the mitigation doesn't help: the page-load nudge (knowledge/[id]/page.tsx:1153 → embed-drain/route.ts:41-43) scopes to the caller's own orgs, so an org whose owner closed the tab depends on the daily cron, which is exactly the global unordered `.limit(6)` path. Note for completeness that the nudge does rescue any org where a member opens the app and that org holds fewer than 6 marked libraries."
  },
  {
    "id": "SEM-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "ask/route.ts:268-272 comments that semanticUsed is \"Reported to the caller so the UI can say 'keyword only' ... an answer quietly missing its best source, with no way for the reader to know why, is the worst failure this route has.\" :568 `semanticUsed = semantic.length > 0;` — one surviving vector flips it. :1774 `retrieval: semanticUsed ? \"hybrid\" : \"keyword\",`. lib/knowledge.ts:122-126 declares `retrieval?: \"keyword\" | \"hybrid\"`. Repo-wide grep for `retrieval` across app/ and components/ returns ZERO reads of the field — the only client-side hits are unrelated prose in knowledge/[id]/page.tsx:749,1015,1035,1037. The nearest coverage signal, SemanticIndexPanel.tsx:257-265 (\"Partly built — questions already use both\"), lives on the library settings panel, not on the answer.",
    "note": "Confirmed on both halves: the flag is a boolean over `semantic.length > 0`, so 3% coverage still reports \"hybrid\", and no component anywhere renders the field. The route's own comment convicts it."
  },
  {
    "id": "SEM-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "SemanticIndexPanel.tsx:35 `const CENTS_PER_1K_PASSAGES = 1;` — model-independent — feeding :174-175 `estCents`/`fullCents = Math.ceil((status.total / 1000) * CENTS_PER_1K_PASSAGES)`, rendered at :109-118 as \"re-embeds every passage on your key — roughly ${fullCents}¢\". lib/ai/pricing.ts:91 `[\"voyage-\", 0.20, 0]` with the comment at :84-90 admitting \"DELIBERATELY CONSERVATIVE PLACEHOLDERS — this figure was not read off Voyage's published price list\". Voyage's real rates are $0.02/M (voyage-3.5-lite, the default per embeddings.ts:41 `models: [\"voyage-3.5-lite\", ...]` + defaultEmbeddingModel) — a 10× overcharge — and $0.18/M for voyage-3-large. embed/route.ts:213 returns `spentThisRun: estimateCostUsd(embedding.model, usage)` from that table.",
    "note": "Both numbers confirmed and they disagree by ~7× on the same job (250k passages: 250¢ quoted vs ~$18 ledgered at 0.20/M). This is not cosmetic: the same inflated figure is what getMonthUsage/getCapUsd compare against, so a voyage build consumes a member's monthly cap ~10× faster than the real spend and can be halted by the cap path at knowledgeEmbedDrain.ts:92."
  },
  {
    "id": "SEM-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/ai/usageServer.ts:57-67 — `getMonthUsage` filters `.eq(\"op\", \"knowledgeAsk\")`, while embedding spend is written with a different op: app/api/knowledge/embed/route.ts:178-182 `recordAskUsage({... op: \"knowledgeEmbed\" })` and lib/knowledgeEmbedDrain.ts:123-127 the same. So the gate at embed/route.ts:143 `if (capUsd > 0 && monthSoFar.spentUsd >= capUsd)` and the drain's at :92 both compare embedding spend against a total that structurally excludes it.",
    "note": "Right, and understated: the same op filter also hides flowRead, drawingLocate and knowledgeVision, and getMonthUsageByUser:70-76 carries it too, so the controllers' team view never shows this spend either — directly contradicting the comment at usageServer.ts:109-111 ('bills as knowledgeVision so the spend is visible as its own line but shares the same cap'). No DB view or trigger aggregates ai_usage_events; these helpers are the whole ledger."
  },
  {
    "id": "SEM-3",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "app/api/knowledge/ask/route.ts:487-488 takes corpusModel from the stored stamp ('text-embedding-3-small'), then :503-504 embeds the query with the NEW provider: `await embedQuery(embedding.provider, corpusModel, embedding.apiKey, t)` — the comment at :488 ('same provider key serves all its models') holds only within a provider. Voyage rejects an OpenAI model id, lib/ai/embeddings.ts:102-104 turns it into an AiCallError, and :505-507's `catch { return []; }` ('degrade to keyword, never fail the ask') swallows it on every ask forever. components/knowledge/AiSettingsModal.tsx:331-338 tells the user 'Vectors already built stay in place and start working again as soon as you add a key back.'",
    "note": "The core claim holds — after a provider switch, semantic search returns zero permanently with no error surfaced, and the dialog copy is true only for re-adding the SAME provider. One sub-claim in the summary is false: the mismatched call is rejected by the provider (404 → embeddings.ts:103) before any embedding is billed, so asks do not 'pay for a Voyage query embedding'. Lowered to MEDIUM: keyword retrieval is untouched and the existing Rebuild button (SemanticIndexPanel.tsx:203) fully repairs it once someone knows."
  },
  {
    "id": "SEM-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/knowledgeEmbedCore.ts:56-60 selects the batch with no ordering and no failure column — `.is(\"embedding\", null).limit(batchSize)` — and :104-105 ends the whole slice on any non-429 provider error: `lastError = e instanceof AiCallError ? e.message : \"Embedding failed.\"; break;`. embedPassages throws for the entire batch (lib/ai/embeddings.ts:161-163 `throw friendly(provider, res.status, ...)`), so one rejected passage kills its 96. lib/knowledge.ts:902-914 stops the client loop on `last.error`, and lib/knowledgeEmbedDrain.ts:113 does the same for the background driver.",
    "note": "Confirmed by absence: there is no attempts/failed column, no per-passage retry, no skip list, and no ORDER BY, so the next Build re-fetches the same unembedded rows under the same predicate and fails at the same point. Only the 429 path is treated as recoverable (:100), and only the client shrinks its batch, and only for rate limits."
  },
  {
    "id": "SEM-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20261014_coverage_timeout_headroom.sql:20-25 counts every chunk regardless of its document's state: `SELECT COUNT(*) FROM knowledge_chunks WHERE org_id = p_org_id AND (p_library_id IS NULL OR library_id = p_library_id)` / same predicate `AND embedding IS NOT NULL`. semantic_search does not: 20261007_rag_hardening.sql:88-95 `JOIN knowledge_documents d ON d.id = c.document_id ... AND d.status IN ('ready','indexing')`. The build loop shares coverage's blindness — lib/knowledgeEmbedCore.ts:56-60 has no status join — and lib/knowledgeIngest.ts:578-583 sets `status: \"error\"` on the document while leaving every already-committed chunk in place.",
    "note": "Correct: chunks of an errored document are counted in total and embedded, are paid for on the user's key, and are then excluded from every semantic_search result. Nothing resets status='error' automatically — only a manual re-ingest (drawing/route.ts:371 sets 'stale') puts those chunks back in retrieval range."
  },
  {
    "id": "SEM-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/knowledge/ask/route.ts:394-397 builds `searchLibraries` as the asked library plus each linked one, but corpusModel is read from the ASKED library alone (:483-487, `.eq(\"library_id\", libraryId)`), and :500-507 then fans out `for (const lib of searchLibraries) ... semantic_search({ p_library_id: lib.id, p_model: corpusModel })`. 20261007_rag_hardening.sql:95 `AND (p_model IS NULL OR c.embedding_model = p_model)` drops every vector in a linked library built under a different model.",
    "note": "Confirmed — one library's stamp is imposed on all of them, so a linked reference library on any other embedding model contributes exactly zero semantic hits while still appearing to be fully indexed in its own panel. Nothing detects or reports the mismatch."
  },
  {
    "id": "SEM-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/knowledgeEmbedCore.ts:56-60 claims nothing — `.is(\"embedding\", null).limit(batchSize)` with no lease column, no `FOR UPDATE SKIP LOCKED`, no advisory lock anywhere in the embed path. Both drivers run that same loop concurrently: app/(protected)/knowledge/[id]/page.tsx:1151-1154 fires `nudgeEmbedDrain()` on mount for every viewer, app/api/cron/embed-drain/route.ts:27-46 accepts any signed-in member and runs `drainEmbedBacklog({ scopeOrgIds, budgetMs: RUN_BUDGET_MS })` for up to 240s (maxDuration 300), and lib/knowledgeEmbedDrain.ts:51-56 selects every library carrying the embedBuild stamp with no concurrency guard.",
    "note": "Confirmed by absence across the whole path — the same unembedded rows can be selected, sent, and billed by the browser build and by one drain per member who opens the library page, all charged to the stamp owner's key (drain.ts:62-63, 123-127). The duplicate writes are idempotent, so the damage is spend, not corruption."
  },
  {
    "id": "SEM-8",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "The consent stamp that the background drain requires is set in exactly one place — app/api/knowledge/embed/route.ts:154 `await setEmbedBuildMarker(libraryId, user.id)` on a human-pressed build — and cleared on completion at :199-201 and lib/knowledgeEmbedDrain.ts:129-130. A repo-wide grep for setEmbedBuildMarker/embedBuild shows no call from any ingest path, so lib/knowledgeEmbedDrain.ts:51-56 (`.not(\"ai_features->embedBuild\", \"is\", null)`) finds nothing to continue after a library reaches 100%.",
    "note": "The automation gap is real — ingesting documents never re-arms the drain, so new chunks stay keyword-only until someone presses Build again. 'Silently' is the part that does not hold: SemanticIndexPanel.tsx:171-173 and 226-229 recompute from live coverage and display '<covered> of <total> passages carry meaning vectors (70%)', the green check disappears, and the control reverts from 'Rebuild index' to 'Build index (~N¢)' (:197-211) — that is the panel telling the truth, not concealing it."
  },
  {
    "id": "SEM-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260930_semantic_layer.sql:65-66 `CREATE INDEX ... knowledge_chunks_embedding_idx ON knowledge_chunks USING hnsw (embedding vector_cosine_ops);` — one global, unpartitioned index. 20261007_rag_hardening.sql:89-96 puts five predicates ABOVE it: `WHERE c.org_id = p_org_id AND (p_library_id IS NULL OR c.library_id = p_library_id) AND d.status IN ('ready','indexing') AND c.embedding IS NOT NULL AND (p_model IS NULL OR c.embedding_model = p_model) ORDER BY c.embedding <=> p_embedding LIMIT GREATEST(1, LEAST(p_limit,100))`. Repo-wide `grep -rn \"ef_search|hnsw\"` returns exactly ONE hit — the CREATE INDEX line. No `SET hnsw.ef_search`, no iterative-scan GUC, anywhere in .sql or .ts. Callers ask for real counts: ask/route.ts:483 `p_limit: lib.tier === \"governing\" ? 12 : 6`.",
    "note": "Confirmed: filters are applied after the HNSW walk and nothing raises ef_search off its default 40, so a small tenant inside a large multi-tenant table can get a handful of rows or zero for a 12-row request. One mitigation the finding does not cite: 20261011_semantic_coverage_fast.sql:17 adds `knowledge_chunks_org_lib_idx (org_id, library_id)`, so for a very selective library the planner may pick an exact index-scan+sort plan instead — the recall loss is plan-dependent, not guaranteed. MEDIUM stands."
  },
  {
    "id": "SHR-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260623_document_shares.sql:38-54 — the only policy is `USING (EXISTS (SELECT 1 FROM org_members WHERE org_members.org_id = document_shares.org_id AND org_members.uid = auth.uid() AND org_members.status = 'active'))` with an identical WITH CHECK. It constrains document_shares.org_id and never joins documents, so a row whose document_id belongs to another org passes both clauses. The table's only FK on document_id is `REFERENCES documents(id) ON DELETE CASCADE` (:16) — org-blind. lib/documentShares.ts:46-54 inserts caller-supplied org_id and document_id verbatim. app/api/share/resolve/route.ts:43-48 and app/api/share/file/route.ts:54-58 both do `.from(\"documents\").select(...).eq(\"id\", share.document_id).maybeSingle()` with the service role and NO `.eq(\"org_id\", share.org_id)`. Repo-wide grep for document_shares in supabase/ finds no trigger or constraint enforcing org consistency (only 20260818_followups_rls.sql:97, an unrelated UPDATE).",
    "note": "Every link in the chain holds: the RLS WITH CHECK validates the attacker's own org_id while leaving document_id unvalidated, and neither public route re-checks the pairing before service-role-reading the version's storage key and streaming the bytes from R2. The UUID needed to weaponize it is handed out unauthenticated by app/d/[number]/route.ts (SHR-2). CRITICAL stands — this is cross-tenant exfiltration via a public URL."
  },
  {
    "id": "SHR-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Repo-wide grep for `access_last_ip` returns exactly one hit outside audit-reports: supabase/migrations/20260623_document_shares.sql:27 `access_last_ip TEXT` — the column is never read or written. bump_share_access (20260818_followups_rls.sql:97-102) sets only `access_count` and `access_last_at`. Neither app/api/share/resolve/route.ts nor app/api/share/file/route.ts touches req.headers at all; share/file:135 writes `user_id: (share.created_by as string | null) ?? null, // attributed to the sharer — the outsider has no account` and `user_email: null`.",
    "note": "Claim of absence confirmed by repo-wide search. The only recipient-side data captured is a count and a timestamp; the download_audits row that /api/share/file writes is attributed to the SHARER, so the distribution record cannot distinguish one recipient from four."
  },
  {
    "id": "SHR-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/share/file/route.ts:113 `footerNotice: \\`${label} Rev ${rev ?? \"?\"} at time of download — scan the QR to confirm it is still current.\\`` is unconditional; :114-116 `verifyUrl: versionId && publicOrigin() ? ... : undefined`. lib/publicOrigin.ts:17-21 returns \"\" on a server with NEXT_PUBLIC_SITE_URL unset (`if (typeof window !== \"undefined\") return window.location.origin; return \"\";`). lib/stamping.ts:246 `if (opts.verifyUrl) {` skips QR generation entirely, and :268 `const qrCorner = qrImage ? pickQrCorner(...) : null;`. .env.example:46 ships `NEXT_PUBLIC_SITE_URL=` blank under \"Misc (optional)\".",
    "note": "Confirmed with no mitigating path: nothing logs or warns when the QR is dropped, and app/share/[token]/page.tsx:141 additionally promises the recipient the 'copy is watermarked with a verify QR'. The .env.example comment even claims a VERCEL_URL fallback that publicOrigin() does not implement."
  },
  {
    "id": "SHR-12",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "supabase/migrations/20260818_followups_rls.sql:95-96 `CREATE OR REPLACE FUNCTION bump_share_access(p_share uuid) RETURNS void LANGUAGE sql SECURITY DEFINER AS $$` — no `SET search_path`. app/api/share/resolve/route.ts:83 `try { await sb.rpc(\"bump_share_access\", { p_share: share.id }); } catch { /* best-effort */ }` (supabase-js returns `{error}` rather than throwing, so the error is discarded twice over). lib/schemaExpectations.ts:63 registers `{ table: \"document_shares\", migration: \"20260623_document_shares.sql\" }` and the file contains no function/RPC checks anywhere (126 lines, zero matches for `function`/`rpc`).",
    "note": "Every factual element checks out. I lower it to LOW on impact: the real distribution record is the download_audits row written independently by app/api/share/file/route.ts:129-140, so a missing RPC degrades a convenience counter (stuck at 0) rather than destroying the audit trail; and the search_path gap is only exploitable by a role that can create objects in a schema it can prepend, which Supabase's authenticated/anon roles cannot do on `public` by default."
  },
  {
    "id": "SHR-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/schema.sql:1089-1091 `CREATE POLICY \"download_audits_org_access\" ON download_audits FOR ALL USING (org_id IN (SELECT my_org_ids()));` — FOR ALL, USING only, no WITH CHECK and no restrictive DELETE/UPDATE policy. schema.sql:1022 `ALTER TABLE download_audits ENABLE ROW LEVEL SECURITY;`. Contrast schema.sql:1085-1087, where audit_logs is deliberately `FOR SELECT` + a separate INSERT policy.",
    "note": "I grepped all of supabase/ for `download_audits`: only three hits (table DDL :789, RLS enable :1022, this policy :1090). No migration adds a restrictive policy or revokes UPDATE/DELETE, so any active org member can delete or rewrite their org's distribution records from the browser client. Claim of absence confirmed."
  },
  {
    "id": "SHR-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/d/[number]/route.ts has no session read of any kind and queries with the service role (`import { supabaseAdmin } from \"@/lib/supabaseAdmin\"` :10; lib/supabaseAdmin.ts builds the client with SUPABASE_SERVICE_ROLE_KEY). Lines 26-36: `.select(\"id, library_id, document_number, updated_at\").filter(\"document_number\", \"not.is\", null).ilike(\"document_number\", `%${raw.replace(/[%_]/g, \"\")}%`).order(\"updated_at\", { ascending: false }).limit(25)` — no org filter — then `const match = rows.find(r => normalized === norm) ?? (rows ?? [])[0]`. The result is emitted as a redirect the anonymous caller reads directly: `dest.searchParams.set(\"doc\", match.id); return NextResponse.redirect(dest)` (:45-46).",
    "note": "Confirmed, including the exact fallback the summary describes: when no candidate's normalized number matches, `?? rows[0]` silently returns the most recently updated substring hit from ANY tenant. The route's own header comment — 'this route only translates a number into a location; it reveals nothing' — is contradicted by the Location header, which carries a foreign library_id and document UUID to an unauthenticated caller."
  },
  {
    "id": "SHR-3",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "HIGH",
    "evidence": "app/api/share/file/route.ts:42-81 performs exactly four checks — token shape, `share.revoked_at`, `share.expires_at`, and file resolution — then streams. There is no read of documents.visibility, documents.acl, documents.acl_index, documents.status, or document_holds anywhere in the file (the select at :54-58 is `id, document_number, title, name, rev, current_version_id`). The internal path it is compared against does enforce two of the four: app/api/storage/download-url/route.ts:47-88 runs canDiscover() for private/hidden docs (403 'Not authorized for this document') and :92-112 evaluates `acl_index.deny.{users,roles,teams}.download` (403 'Downloading this document is denied for your account').",
    "note": "The core claim is right and HIGH is right: an ACL that explicitly denies download, and private/hidden visibility, are both enforced on the internal byte-issuing route and both absent from the share route — and RLS lets any member mint the share regardless of that ACL. The correction is factual scope: HOLDS and STATUS are not enforced on the internal download path either. Holds only block advancing a document (lib/documentGuards.ts:138-146 'Active holds block advancing', backstopped by 20260713_document_publish_guard.sql, which guards only current_version_id/Superseded transitions), and 'Void' appears in lib/downloads.ts:62-63 solely as a badge tone. So the share link does not bypass a hold/status download gate — no such gate exists anywhere."
  },
  {
    "id": "SHR-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/documents/InspectorPanel.tsx:561 gates the section on `selectedDoc.id && selectedDoc.orgId && uid` — no role test — and :574-580 renders the Share button unconditionally within it. components/documents/ShareLinkModal.tsx:27-33 lists `{ label: \"Never expires\", days: 0 }`, and lib/documentShares.ts:41-45 turns that into `input.expiresInDays === 0 ? null : ...`, matching the column comment at supabase/migrations/20260623_document_shares.sql:20 `expires_at TIMESTAMPTZ, -- null = no expiry`. The DB policy (:38-54) tests only `org_members.status = 'active'`, never role. The only read path is lib/documentShares.ts:59-66 `listShareLinks(documentId)` filtered `.eq(\"document_id\", documentId)`, whose sole caller is ShareLinkModal.tsx:51 — a repo-wide grep for document_shares/listShareLinks finds no org-wide or admin surface.",
    "note": "Confirmed: no role check in the UI or in RLS, a never-expiring option in the picker, and per-document-only listing with per-row revoke. One caveat on 'no one can ever enumerate': document_shares is in the admin data-export table list (lib/exportTables.ts:50), so an Admin can dump the rows offline — but there is no in-app view, no query surface, and no bulk revoke, so the finding's operational point stands."
  },
  {
    "id": "SHR-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/share/file/route.ts:129-141 — `try { await sb.from(\"download_audits\").insert({ org_id, document_id, version_id, user_id, user_email, created_at, expires_at, watermark_policy_id, source: stamped ? \"share_link\" : \"share_link_unstamped\" }); } catch { /* pre-migration column drift — never block the share */ }`. supabase/schema.sql:789-799 lists nine columns and `source` is not among them; no migration adds it (verified across all of supabase/migrations/ and REMEDIATION_APPLY_ALL.sql).",
    "note": "Correct, and the 'doubly swallowed' characterization is precise: supabase-js resolves with `{ error }` instead of throwing, so PostgREST's PGRST204 rejection is discarded by the ignored return value, and the bare `catch {}` would have eaten it anyway. No console.warn, no metric — the route header comment at :15-16 ('The download_audits row is written HERE') describes behavior that never occurs."
  },
  {
    "id": "SHR-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/share/resolve/route.ts:52-58 — `let versionId = doc.current_version_id; if (versionId) { const { data: v } = await sb.from(\"document_versions\").select(\"file_url\").eq(\"id\", versionId).maybeSingle(); storagePath = v?.file_url; }` — no review_state predicate. Only the fallback at :60-71 carries `.or(\"review_state.is.null,review_state.eq.approved\")`. app/api/share/file/route.ts:62-80 is the identical pair. The divergence window is real: lib/reviewControl.ts:429 promotes with `.update({ current_version_id: pendingId, ... })` and only afterwards, at :445-447, does `await supabase.from(\"document_versions\").update({ review_state: \"approved\", revision_label: baseRev, ... }).eq(\"id\", pendingId)` — the result is never destructured, so an error there is invisible.",
    "note": "The code reads exactly as claimed — the comment 'Resolve the current PUBLISHED version's file (never an in-review draft)' sits directly above the branch that does not check. MEDIUM is right: reaching it requires the unchecked relabel at reviewControl.ts:445 to fail after the promote at :429 succeeded, which is a narrow window, but the filter's absence on the primary branch means nothing else would catch it."
  },
  {
    "id": "SHR-7",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "supabase/migrations/20260623_document_shares.sql:16 `document_id UUID NOT NULL REFERENCES documents(id) ON DELETE CASCADE` — there is no version_id column, and app/api/share/file/route.ts:62-80 re-resolves `doc.current_version_id` on every request, so the link does follow rev-ups. But the footer at :113-114 is `footerNotice: `${label} Rev ${rev ?? \"?\"} at time of download — scan the QR to confirm it is still current.`` with `const rev = doc.rev` (:103), and documents.rev is written in the same statement that sets current_version_id — lib/reviewControl.ts:429 `.update({ current_version_id: pendingId, rev: baseRev, revision: baseRev, status: \"Issued\", ... })` — so on the primary branch the stamped rev IS the served version's rev.",
    "note": "The always-current behavior is real but is the documented design, not a defect: the migration header states the route 'generates a presigned download URL for the current version's storage path', and ShareLinkModal.tsx:133-135 promises only that 'Anyone with the resulting URL can open the document until it expires or you revoke it' — no revision pinning is claimed. The stamped copy also names the rev it actually contains and carries a verify QR. The 'footer rev comes from documents.rev rather than the version served' half is false on the primary path and can only diverge on the rare fallback branch (current_version_id set but its file_url null), so I'd drop this to LOW."
  },
  {
    "id": "SHR-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/share/file/route.ts:109-116 calls `await applyStampToPdfDoc(pdfDoc, { userLabel: \"shared-link\", timestamp, watermarkText, footerNotice, verifyUrl })` — no `sourceBytes`. lib/stamping.ts:257 `const ink = opts.sourceBytes ? await analyzePageInk(opts.sourceBytes) : null;` and :265 `const pageInk: PageInk = ink?.[...] ?? FALLBACK_INK;`. lib/stampLayout.ts:150-154 `FALLBACK_INK = { corners: { br: 0, bl: 1, tr: 1, tl: 1 }, topBand: 1, bottomBand: 0 }` → pickQrCorner (:131-139) returns \"br\" and pickFooterEdge(1, 0) (:144-146) returns \"bottom\".",
    "note": "Confirmed, and worse than stated: even if sourceBytes were passed, lib/stamping.ts:100 `if (typeof document === \"undefined\") return null;` makes ink analysis impossible in any server route, so every server-stamped share copy is hardcoded to bottom-right QR + bottom footer. I found no guard anywhere on this path."
  },
  {
    "id": "SHR-9",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "public/sw.js:203-213 is the catch-all branch: `const res = await fetch(request); cachePut(RUNTIME_CACHE, request, res); return res;` with the cache consulted only inside `catch (err)`. cachePut (:116-121) checks only `response.ok`, never Cache-Control. app/layout.tsx:93 `<ServiceWorkerManager />` sits in the ROOT layout, so /share/<token> is in scope; ServiceWorkerManager.tsx:30-33 registers \"/sw.js\" at default scope. Both /api/share/resolve and /api/share/file are same-origin GET fetches (app/share/[token]/page.tsx:41 and :67).",
    "note": "The caching is real, but the finding's framing overstates the consequence: the branch is NETWORK-FIRST, so an online recipient always gets the live 410 for a revoked link — the cache is served only when the fetch throws (offline). And the recipient already saved the stamped PDF to their filesystem via the anchor at page.tsx:75-81, so the SW copy adds essentially no exposure beyond the file they legitimately hold. LOW."
  },
  {
    "id": "SM-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/workflow.ts:199-228 — inside `if (canActAsRequester)`, the engineer-gate branch is `if (needsEngineerApproval && !isEng) { ... action: 'request_final_engineer_approval' ... }`, and then UNCONDITIONALLY `actions.push({ label: 'Approve with Minor Correction', action: 'approve_minor_correction', ... })` with the comment 'Available to every requester tier by design.' lib/ticketTransitions.ts:230-235 `case \"approve_minor_correction\": updates.status = \"PENDING_IFC\"; updates.deliverable_rev = issuedRevLabel(ticket.revisionCount);` — and no `engineer_approved_at` is ever set. The server is not a second gate: route.ts:96-103 accepts any action `WorkflowEngine.getActions` returns for the caller.",
    "note": "Confirmed and frozen by test: lib/__tests__/workflow.test.ts:163-171 asserts `expect(actionsOf(t, \"Viewer\", \"u-1\")).toContain(\"approve_minor_correction\")` for a Viewer-role requester whose own ticket shows 'Send for Engineer Final Approval'. The same actor the gate blocks reaches PENDING_IFC with an issued integer rev and no engineer sign-off stamp. CRITICAL sustained."
  },
  {
    "id": "SM-10",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "page.tsx:1065-1075 `else if (action.action === 'assign' || action.action === 'approve_initial') { const effectiveAction = action.action === 'approve_initial' ? { ...action, action: 'assign', label: 'Approve & Assign' } : action;` and the POST at :1163 sends `actionType: action.action`. route.ts:96-103 `const allowed = WorkflowEngine.getActions(...); const action = allowed.find(a => a.action === body.actionType); if (!action) return ... 403`, and workflow.ts:82-109 offers only `approve_initial`, `request_eng_review`, `reject` at NEW/PENDING_ENG_INITIAL — never `assign`. Rejection is therefore certain.",
    "note": "The rewrite→403 is real, but two facts cut the severity hard. (1) The states are unreachable: every ticket-insert site hardcodes the status — requests/new/page.tsx:255 `const initialStatus: TicketStatus = 'PENDING_ASSIGNMENT'`, CheckInPanel.tsx and lib/transitionIn.ts:304 both insert `status: \"PENDING_ASSIGNMENT\"` — and workflow.ts:47-52 getInitialStatus always returns PENDING_ASSIGNMENT; nothing in the codebase ever writes NEW or PENDING_ENG_INITIAL. (2) Even at NEW it is not a dead end: `request_eng_review` (requiresEngineerPick) and `reject` (requiresComment) take earlier branches at :1051-1064, are not rewritten, and are accepted — request_eng_review routes NEW→PENDING_ENG_TEAM→approve_team→PENDING_ASSIGNMENT, a working forward path."
  },
  {
    "id": "SM-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/ticketTransitions.ts:51-82 `rowToTicket` is an explicit object literal ending `createdAt: row.created_at as string, lastModified: row.last_modified as string | undefined, } as Ticket;` — there is no `metadata:` key, and the `as Ticket` cast suppresses the type error. route.ts:231-233 then reads `const srcDoc = (ticket.metadata as Record<string, unknown> | undefined)?.source_document as { id?: string } | undefined; if (srcDoc?.id) {` — always undefined, so lines 234-270 never execute. The column and its writers do exist: supabase/migrations/20260721_tickets_metadata.sql documents `metadata.source_document { id, document_number, title, rev, path }`, CheckInPanel.tsx:262 writes `source_document: { id: doc.id, ... }`, requests/new/page.tsx:291 does too, and the ticket page's own row mapper DOES map it (`metadata: (r.metadata as Record<string, unknown> | null) ?? undefined`).",
    "note": "Confirmed dead code, and the contrast with the page's own mapper — which maps metadata correctly — settles that this is an omission, not a design choice. route.ts:245/265 are the only writers/deleters of ticket-sourced `document_intents` in the repo, so no other path compensates."
  },
  {
    "id": "SM-12",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "route.ts:113-132 loops over `[body.engineer?.id, body.assignment?.id]`, checks active membership for both, then gates only the engineer: `if (ref === body.engineer?.id) { ... if (!held.some(r => r.includes(\"Engineer\"))) return ... 400 }`. No equivalent exists for `assignment`. ticketTransitions.ts:193-200 `case \"assign\": if (input.assignment) { updates.assigned_drafter_id = input.assignment.id; ... updates.status = \"DRAFTING\"; }` — no role test. workflow.ts:69-75 then grants that uid drafter authority via `isDrafterIdentity`.",
    "note": "The asymmetry is real and correctly described. But the finding's stated scenario — \"an assigner picking from a list mis-clicks and assigns to an Accounting or Contractor seat\" — is refuted by page.tsx:240-250, where AssignmentModal populates its list with `.from('org_members').select('uid, email, role').eq('org_id', activeOrgId).eq('role', 'Drafter').eq('status','active')`; only Drafters are offered, so the mis-click cannot produce a non-drafter. Exploitation requires a hand-crafted POST by someone who already holds `ticket.assign` authority, which makes this a defense-in-depth gap rather than a MEDIUM."
  },
  {
    "id": "SM-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "route.ts:104-109 validates exactly two of the three action requirements — `if (action.requiresComment && !body.comment?.trim())` and `if (action.requiresEngineerPick && !body.engineer?.id)` — and there is no `requiresFile` check anywhere in the route (grep: `requiresFile` appears only in lib/workflow.ts:8/181/313 and page.tsx:1028). ticketTransitions.ts:280-283 `case \"submit_final\": updates.status = \"FINAL_DRAFT\"; if (input.finalAttachment) currentAttachments = [...currentAttachments, input.finalAttachment];` — the attachment is optional, the status change is not. The only guard is client-side, page.tsx:1028-1034, and it merely asserts `ticket.attachments.length > 0` without checking for a Final.",
    "note": "Confirmed, and the asymmetry inside workflow.ts strengthens it: `submit_draft` at :176-182 is only offered when `ticket.attachments?.some(a => a.type === 'Draft')`, so the server enforces its file requirement structurally, while `submit_final` at :309-314 carries `requiresFile: true` with no such state guard and no server check — the one compliance-critical issue step is the unguarded one."
  },
  {
    "id": "SM-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/schema.sql:1080-1081 `CREATE POLICY \"tickets_org_access\" ON tickets FOR ALL USING (org_id IN (SELECT my_org_ids()));` — FOR ALL, no WITH CHECK, no role predicate, no column restriction. my_org_ids() (schema.sql:1031-1034) is `SELECT org_id FROM org_members WHERE uid = auth.uid() AND status = 'active'` — active membership is the only test. `status` is a plain TEXT column (schema.sql:405) with no CHECK constraint and no BEFORE UPDATE trigger (the only trigger on tickets is `tickets_search_tsv_trg`, migrations/20260610_phase2_search_completion.sql:89-90).",
    "note": "Confirmed by absence search: no second policy, no REVOKE/column grant, no status-transition trigger. The app itself proves the write path is open to the anon key — app/(protected)/requests/[id]/page.tsx:1327 `await supabase.from(\"tickets\").update({ watchers: next }).eq(\"id\", ticketId);` runs client-side under exactly this policy. The server enforcement in /api/tickets/workflow-action (route.ts:15-26, 96-103) is therefore bypassable, and with it the CAS, the audit_logs insert and the fan-out."
  },
  {
    "id": "SM-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/workflow.ts:265-274 `const canActHere = ticket.assignedEngineerId ? isAssignedEngineerIdentity || isManagement : allows('ticket.final_approve') || isManagement; if (canActHere) { actions.push({ label: 'Approve as Engineer (Issue for Construction)', action: 'engineer_approve_final', ... }) }`, where line 66 `const isManagement = allows('ticket.manage')` and lib/capabilityPolicy.ts:55/58-60 default ticket.manage to `[\"Admin\",\"Manager\",\"Supervisor\"]`. lib/ticketTransitions.ts:247-249 then writes `updates.engineer_approved_at = now;`. The server route checks an engineer credential only for the PICKED reviewer, never the actor: route.ts:124-130 `if (ref === body.engineer?.id) { ... if (!held.some((r) => r.includes(\"Engineer\"))) return ... 'The selected reviewer does not hold an Engineer role' }`.",
    "note": "Confirmed, and the misattribution is visible in the UI: app/(protected)/requests/[id]/page.tsx:1744-1748 renders `ticket.assignedEngineerName` with an 'approved' badge keyed off `ticket.engineerApprovedAt`, so a Supervisor's click displays as the assigned engineer having signed off. HIGH sustained."
  },
  {
    "id": "SM-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Produce claims rows with archive_id only — app/api/admin/ticket-shed/route.ts:139-142 `.update({ archive_id: archiveId }).in(\"id\", chunk).eq(\"org_id\", orgId).is(\"archive_id\", null).is(\"archived_at\", null)` — leaving archived_at NULL. The workflow gate reads only archived_at: app/api/tickets/workflow-action/route.ts:69-74 `if ((row as { archived_at?: string | null }).archived_at) return ... 409`, and the UI gate is likewise `ticket.archivedAt` (page.tsx:1690, 2041). Commit then selects purely by archive_id with no status/closed_at predicate: commit/route.ts:125-129 `.select(\"id, attachments, comments, history, metadata, archived_at\").eq(\"org_id\", orgId).eq(\"archive_id\", archiveId)`, and :156-166 stamps `{ comments: [], history: [], metadata: {...archive_summary}, archived_at: now }` for any row whose archived_at is still null, before deleting the attachment binaries.",
    "note": "Confirmed by absence search: nothing in the reopen path clears archive_id — the only writers of `archive_id: null` are /api/admin/archive-cancel and /api/admin/ticket-shed/restore. lib/ticketTransitions.ts:288-290 `case \"reopen_ticket\": updates.status = \"PENDING_REVIEW\";` leaves the claim intact, so a ticket reopened between produce and commit is live, in review, and still gets shredded."
  },
  {
    "id": "SM-5",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "lib/ticketTransitions.ts:288-290 `case \"reopen_ticket\": updates.status = \"PENDING_REVIEW\"; break;` — revision_count and deliverable_rev are untouched, and :315 only clears closed_at. lib/ticketTransitions.ts:106-108 `export function issuedRevLabel(revisionCount) { return String((revisionCount ?? 0) + 1); }` is what both approve paths write (:223 and :232), so a second approval on the same cycle re-emits the identical integer. app/api/verify-ticket/route.ts:84-86 `} else if (latestIssued && printedRev === latestIssued) { verdict = \"current\"; }` then answers 'current' for both prints.",
    "note": "The mechanism is real, but the narrated path is not the one that produces the collision: from the reopened PENDING_REVIEW state the only way back to the drafter is 'Request Revision', which increments revision_count (lib/ticketTransitions.ts:277) and yields Rev 2. The collision needs the approve-without-revision route — reopen, approve straight to PENDING_IFC (rev re-written as the same N), then the drafter issues a different final package via submit_final. Real but narrower than described; MEDIUM."
  },
  {
    "id": "SM-6",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "lib/ticketTransitions.ts:273-279 `case \"request_revision\": case \"reject\": case \"reject_final\": updates.status = \"REVISION_REQ\";` — and 'Return with Questions' at lib/workflow.ts:126-131 is `action: 'reject'`. lib/workflow.ts:166-194 gates every REVISION_REQ action behind `if (canActAsDrafter)`, i.e. `isDrafterIdentity || allows('ticket.draft_work')`. lib/ticketAttention.ts:62-110 has no REVISION_REQ branch for a requester (line 71-73 covers only PENDING_REVIEW/FINAL_DRAFT) and none for management (lines 84-94).",
    "note": "The dead-end is real for the requester and REVISION_REQ is genuinely absent from every role-based attention rule, but two parts of the claim are wrong: 'zero legal actions' ignores that any member holding the Drafter role satisfies allows('ticket.draft_work') and gets Save Progress / Submit Draft there (plus Admin force-close, workflow.ts:345-349), and 'appears on nobody's attention list' ignores that the reject transition puts the requester in unread_by (ticketTransitions.ts:140), which produces a bell row, an email, and an unread item in the attention feed (useTicketNotifications.ts:254-255). LOW."
  },
  {
    "id": "SM-7",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "route.ts:214-223 `await supabaseAdmin.from(\"audit_logs\").insert({ action: `TICKET_${...}`, ... details: { from: ticket.status, to: newStatus, label: action.label } });` — the result is never destructured, and supabase-js returns `{error}` rather than throwing, so a failure is silently swallowed after the status write at :163-191 already committed. BUT ticketTransitions.ts:139 `const newHistory = [...(ticket.history || []), historyEntry];` and :146 `const updates = { last_modified: now, history: newHistory, ... }` put the actor/label/role/timestamp into the SAME compare-and-set row update as the status.",
    "note": "The mechanical claim is exactly right — four unsynchronised writes, and the audit insert's error is discarded. Severity is too high: the ticket's own `history` JSONB records who acted, with what label, in what role, and when, atomically with the status change, so what a lost audit row costs is the from→to mirror in audit_logs, not the compliance record itself."
  },
  {
    "id": "SM-8",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "Repo-wide grep for CANCELED/CANCELLED across *.ts/*.tsx/*.sql returns only readers and filters — types/schema.ts:1033 `| \"CANCELED\";`, WorkflowDiagramModal.tsx:36 `{ status: \"CANCELED\", label: \"Canceled\", blurb: \"Withdrawn or returned to the requester...\" }`, ticketShed.ts:11 `TERMINAL_TICKET_STATUSES = [\"CLOSED\", \"CANCELED\"]`, ticketTransitions.ts:311 `const TERMINAL = [\"CLOSED\", \"CANCELED\"];`, plus `.not(\"status\",\"in\",'(\"CLOSED\",\"CANCELED\")')` dashboard filters. No assignment anywhere: computeTransition's switch (ticketTransitions.ts:173-291) has no case producing it, and workflow.ts:80-342 has no `case 'CANCELED'`.",
    "note": "Both halves confirmed: nothing writes CANCELED, and workflow.ts has no case for it (only the global `ticket.force_close` override at :345 would offer anything). Severity overstated — because the state is genuinely unreachable, no ticket is ever actually stranded in it; the live defect is a workflow-diagram modal advertising an exit the product does not implement, which is a docs/feature gap, not a MEDIUM data or integrity risk."
  },
  {
    "id": "SM-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Confirmed by line number in app/(protected)/requests/[id]/page.tsx: :920 `supabase.from('tickets').update({ unread_by: ... }).eq('id', ticketId)`, :978 `await supabase.from('tickets').update({ comments: updatedComments }).eq('id', ticketId)`, :1010-1014 `update({ attachments: [...currentAttachments, newAttachment], last_modified: now, history: [...currentHistory, historyEntry] }).eq('id', ticketId)`, :1328 `update({ watchers: next }).eq(\"id\", ticketId)` — every one keyed on id alone. app/api/intake/upload/route.ts:182-190 does the same read-modify-write of `attachments`/`history` with no CAS. Contrast route.ts:155-191, which does `.eq(\"status\", ticket.status)` + `.eq(\"last_modified\", String(ticket.lastModified))`, and comment/route.ts:205-216, which CASes on `last_modified` with the comment explicitly citing \"the exact split-brain post_ticket_comment was built to prevent\". supabase/schema.sql:1080 `CREATE POLICY \"tickets_org_access\" ON tickets FOR ALL` confirms the client writes actually land.",
    "note": "Accurate; if anything undercounted (five writers, not four — the intake route is a fifth). The whole-array clobber is real for comments, attachments+history and watchers, and the guard the finding says they bypass demonstrably exists on the two hardened paths."
  },
  {
    "id": "STACK-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "SemanticIndexPanel.tsx has exactly one useEffect — line 65 `useEffect(() => { void load(); }, [load]);` — with no cleanup, and `stopRef` appears only at :51 (declare), :74 (`stopRef.current = false`), :79 (pass), :87 (read) and :194 (the Stop button's onClick). No unmount handler sets it true. `grep -n CornerPortal` on the file returns zero hits, so the build has no dock presence. The loop honours only that flag: knowledge.ts:903 `if (last.done || last.error || shouldStop?.()) break;` and :915 `last = await embedBatchWithRetry(...)`, each iteration a paid embedding call.",
    "note": "Confirmed on both halves. Contrast with the sibling KnowledgeIndexIndicator, which does portal into CornerPortal and sets `alive = false` on cleanup — SemanticIndexPanel does neither, so navigating away leaves an in-browser paid loop running with the Stop button unmounted."
  },
  {
    "id": "STACK-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "MetadataStagingModal.tsx:461 `<div className=\"fixed inset-0 z-[300] bg-slate-900/60 backdrop-blur-sm ...\">` — same z-[300] as CornerDock.tsx:25, but rendered inside `children` which app/(protected)/layout.tsx:69 places AFTER `<CornerDock />` (line 65), so DOM order breaks the tie in the modal's favor. AssetPhotoUploader.tsx:140 `fixed inset-0 z-[510]` and CustomizeNodeModal.tsx:98 `fixed inset-0 z-[400]` beat it outright.",
    "note": "Confirmed, including the tail of the scenario: MetadataStagingModal.tsx:405-406 documents that \"The parent closes the modal on a clean run, but it keeps the modal open when some files failed\", and the parent at app/(protected)/documents/[libraryId]/page.tsx:2543 `throw e` is what drives that. MetadataStagingModal does not createPortal (no such import), so the DOM-order tiebreak applies."
  },
  {
    "id": "STACK-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "InspectorDrawer.tsx:42 `fixed top-0 right-0 bottom-0 z-[60] w-[640px] max-w-[92vw] lg:w-[720px]`; HistoryDrawer.tsx:161 `fixed inset-y-0 right-0 w-[600px] max-w-[94vw] ... z-[70]`; NotificationCenter.tsx:102 `fixed top-0 right-0 bottom-0 z-[241] w-[480px] max-w-[94vw]` — every one below CornerDock.tsx:25's `z-[300]`, and all three are right-edge and full-height so their bottom-right region is the dock's anchor.",
    "note": "Confirmed exactly; there is no bottom offset, right offset, or drawer-aware repositioning anywhere in CornerDock.tsx. Nothing in the drawers raises above 241, so the dock always wins."
  },
  {
    "id": "STACK-12",
    "verdict": "REFUTED",
    "corrected_severity": null,
    "evidence": "The finding's own cited block, SemanticIndexPanel.tsx:81-82, pairs every toast with a persistent inline note: `setBuildNote({ tone: \"err\", text: final.error }); showToast({ type: \"error\", title: final.error, duration: 15000 });` — and that note is rendered durably at lines 242-249 (`{buildNote && (<div className={... border-rose-300 ...}>{buildNote.text}</div>)}`). Likewise NotificationListener.tsx:65-69's toast is a mirror of a `checkout_messages` row (durable in the document thread), and lines 92-97's toast mirrors a `notifications` row that NotificationCenter.tsx:136 lists via `<AttentionFeed items={filtered} .../>`.",
    "note": "The headline (\"toasts are the ONLY channel\", \"has left no trace anywhere in the UI\") is false for all three cited sites — each toast has a durable companion surface. The specific scenario is doubly wrong: the semantic build is client-driven and requires the panel to stay mounted (\"Leave this page open and it will finish\", line 236), so returning to that tab shows the rose buildNote box. What survives is only the narrow, uncited fact that ToastProvider itself keeps no history."
  },
  {
    "id": "STACK-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "A repo-wide grep for `beforeunload` returns exactly three sites — app/(protected)/plot-plans/[id]/page.tsx:63, lib/clientBackup.ts:235 `window.addEventListener(\"beforeunload\", warnUnload)` (handler at 211-214) and its removal at 248. lib/storage.ts's uploadToPath (378-431) only calls `emitUpload({...})` into the in-memory `const uploadListeners = new Set<UploadListener>()` (line 29); UploadIndicator.tsx:19-37 holds it in `useState`. No localStorage/sessionStorage/indexedDB anywhere in lib/storage.ts, and no resumable-upload state (`uploadId` at lines 321-359 is per-call only).",
    "note": "Fully confirmed, and the asymmetry is the sharpest evidence: the backup path deliberately installs `warnUnload` with the message \"A backup is still running — leaving this tab will stop it\", while the multipart upload path — the one the finding is about — installs nothing. UpdatePill.tsx:41-42 `onClick={() => window.location.reload()}` is a one-tap, unguarded path into that loss."
  },
  {
    "id": "STACK-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/storage.ts:425 emits BEFORE the cancellation check: `emitUpload({ id, name, percent: 0, status: \"error\", error: (err as Error).message });` and only then, at :427-429, `if (err instanceof UploadCancelledError) throw err;` under the comment \"Cancellation is the user's own doing — keep it recognisable instead of wrapping it into 'Upload cancelled' prose that reads like a failure\". UploadCancelledError's message is literally `\"cancelled\"` (:62-64). UploadIndicator.tsx:56 renders `status === \"error\"` as a rose `AlertCircle`, :58-60 as the label `\"Failed\"`, and :74-76 prints the raw error text — so the card reads \"Failed\" over \"cancelled\" in red, for 7000ms (UploadIndicator.tsx:25 `const after = e.status === \"done\" ? 2500 : 7000;`).",
    "note": "Confirmed, and the contradiction is doubly explicit: MetadataStagingModal.tsx:50-52 documents the contract as \"`signal` aborts the in-flight transfers when the user presses Stop ... reports UploadCancelledError as 'the user stopped this', not as a failure\", and stopUpload (:424-427) is a real user-facing 'Stop upload' button (:768-771). storage.ts never honours that contract on the corner-indicator channel."
  },
  {
    "id": "STACK-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "KnowledgeIndexIndicator.tsx:107 `} catch { /* row is marked errored server-side; move on */ }` — swallows the throw without touching state, and `finished++` on the line above is skipped. Then :109-111 `if (alive && sawWork) { setState((s) => s ? { ...s, phase: \"done\", finished } : null); }`, which renders at :152-156 as a green `CheckCircle2` with \"Knowledge indexing caught up\" and at :196-198 `{state.finished} document{...} indexed.` — i.e. \"0 documents indexed\" under a green check. The throw is real: knowledge.ts:450-456 `if (noProgressRounds >= 3) { throw new Error(\\`Indexing stalled at page ...\\`) }`, and the server has already flipped the row (ingest/route.ts:180-183 `.update({ status: \"error\", ... })`), which also removes it from the drain query's `.in(\"status\", [\"pending\",\"stale\",\"indexing\"])` so it never returns.",
    "note": "Confirmed. The one mitigation the finding already cites is real but out of the way: knowledge/[id]/page.tsx:1931 does show `Indexing failed — {doc.error}` with a resume button, but only on that library's page — a user anywhere else in the app sees a green 'caught up' card that actively asserts the opposite of what happened."
  },
  {
    "id": "STACK-4",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "UndoToastHost.tsx:21 `className=\"fixed bottom-4 left-1/2 -translate-x-1/2 z-[280] flex flex-col items-center gap-2 pointer-events-none\"` vs BackToGraphChip.tsx:24 `className=\"fixed bottom-4 left-1/2 -translate-x-1/2 z-40 inline-flex ...\"` — byte-identical anchor (bottom-4, left-1/2, -translate-x-1/2), z-280 over z-40. Path is real: app/(protected)/graph/page.tsx:302 `router.push(`${n.href}...from=graph`)` and lib/orgGraph.ts:213 `href: `/projects/${p.id}`` reach ExecutionView.tsx:926's `<UndoToastHost .../>`.",
    "note": "The collision is exactly as described and reachable. Severity is overstated: useUndoableActions.ts:25 `const TIMEOUT_MS = 7000` caps the occlusion at 7s and caps the stack at 3 (line 39 `t.slice(-2)`), and the chip is NOT the only way back — ViewTabs.tsx:110 has a `/graph` nav entry and graph/page.tsx:299-300 states \"layout and settings persist per-org already\", so reaching /graph by any route restores the same map."
  },
  {
    "id": "STACK-5",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "CornerDock.tsx:35-48 — `const [target, setTarget] = useState<Element | null>(null); useEffect(() => { const t = setTimeout(() => setTarget(document.getElementById(DOCK_ID)), 0); ... }, []); if (!target) { return (<div className=\"fixed bottom-4 right-4 z-[300] flex flex-col items-end gap-2 pointer-events-none\">{children}</div>); }`",
    "note": "Accurate: the fallback box carries the identical `fixed bottom-4 right-4 z-[300]` coordinates as the dock (line 25), and because the lookup is deferred into a macrotask the fallback is committed and painted at least once on every mount — and CornerPortal remounts on every appearance (UploadIndicator.tsx:47 `if (list.length === 0) return null`). But the defect is a sub-100ms transient with no state, data or interaction consequence; LOW, not MEDIUM."
  },
  {
    "id": "STACK-6",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "KnowledgeIndexIndicator.tsx:88 inside the drain loop: `attempted.add(next.id); sawWork = true; setHidden(false); setState({ phase: \"working\", ... })` — unconditionally clears the `hidden` flag set by the Dismiss button (line ~163 `onClick={() => setHidden(true)}`). All of `hidden`, `minimized`, UploadIndicator's `items` and clientBackup's `currentState` are in-memory only; grep for localStorage/sessionStorage/indexedDB in lib/storage.ts returns nothing.",
    "note": "The setHidden(false) reset and the non-persistence across reload are both real. Severity is too high because the finding's own cited lines 50-55 contain the mitigation: `const [minimized, setMinimized] = useState(false)` is explicitly sticky across drain passes (\"new work must NOT re-expand a card the user deliberately tucked away\"), and the X/Dismiss button only renders in the `phase === \"done\"` state — so what gets re-shown after dismissal is a *new* card for *new* work, not a resurrection of the dismissed 'caught up' card."
  },
  {
    "id": "STACK-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "ToastProvider.tsx:63 `pointer-events-auto w-80 p-4 rounded-xl ...` (320px), UploadIndicator.tsx:51 `flex flex-col gap-2 w-72` (288px), KnowledgeIndexIndicator.tsx:150 `w-[330px] max-w-[calc(100vw-2.5rem)]`, all inside CornerDock.tsx:25 `fixed bottom-4 right-4 z-[300] ... max-w-[calc(100vw-2rem)]`; StagingTray.tsx:20 `fixed bottom-0 left-0 right-0 z-30 flex flex-col items-center pointer-events-none`.",
    "note": "Confirmed on every count: at 375px viewport a `w-80` toast is 320/375 = 85% of the width, and the dock's z-[300] sits over the tray's z-30 full-width bottom bar whose right-hand Clear/Open controls live exactly under the dock's bottom-right anchor. Only `w-[330px]` on the indexing card has a viewport-relative max-width; the toast and upload cards have none."
  },
  {
    "id": "STACK-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "BackupIndicator.tsx:27 `className=\"fixed bottom-5 left-5 z-[300] w-[340px] max-w-[calc(100vw-2.5rem)] rounded-2xl ...\"` — no CornerPortal import at all; ServiceWorkerManager.tsx:73 `<div className=\"fixed bottom-4 left-4 z-[200] flex flex-col gap-2 pointer-events-none\">` holding the \"Offline — showing cached data\" and \"Update available\" pills.",
    "note": "I checked the one thing that could refute this — that ServiceWorkerManager might not be mounted on protected pages — and it is: app/layout.tsx:93 `<ServiceWorkerManager />` in the ROOT layout, which wraps app/(protected)/layout.tsx. A 340px x ~130px card at bottom-5/left-5 z-300 fully covers a pill at bottom-4/left-4 z-200, and BackupIndicator is genuinely outside the dock."
  },
  {
    "id": "STACK-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "CornerDock.tsx:25 `fixed bottom-4 right-4 z-[300] flex flex-col items-end gap-2 pointer-events-none max-w-[calc(100vw-2rem)]` — no max-height, no overflow-y, no order/sort. ToastProvider.tsx:41 `setToasts((prev) => [...prev, { id, type, title, message, duration }])` — unbounded append. UploadIndicator.tsx:46 `const list = Object.values(items).sort((a, b) => a._t - b._t)` sorts only WITHIN the upload widget, and line 47-48 renders every entry with no cap.",
    "note": "All three legs hold. Inter-widget order is purely `createPortal` append order (i.e. whichever CornerPortal mounted last), and with `bottom-4` and no `top`/`max-h`, the column grows upward off the top of the viewport with nothing to scroll it. Notably the app already knows how to cap a stack — useUndoableActions.ts:39 `[...t.slice(-2), ...] // keep last 3` — the dock and ToastProvider just don't."
  },
  {
    "id": "TAX-1",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "Sidebar.tsx:539 `onClick={(e) => { e.preventDefault(); e.stopPropagation(); openCenter(leaf.badgeTone === 'red' ? 'action' : 'all'); }}` — the only argument is an AttnFilter, and AttentionFeed.tsx:22 defines `export type AttnFilter = \"all\" | \"action\" | \"unread\"`, which has no section axis. The panel then renders `counts.all = items.length` at NotificationCenter.tsx:77 into the header at line 116: `${counts.all} item${...} — every badge in the app counts these.` while the badge value came from Sidebar.tsx:130 `return { badge: s.total, ... }` on `sectionCounts.documents`.",
    "note": "Factually exact — I found no section-aware open() path anywhere (all five openCenter call sites pass only 'all' or 'action'), and even the 'red' branch maps a per-section actionRequired badge onto an app-wide action count. Severity is one notch high, though: nothing is lost or blocked — every item is present, navigable and additionally narrowable by AttentionFeed's KIND_GROUPS second axis (lines 64-69); the harm is a misleading count, not an unreachable item."
  },
  {
    "id": "TAX-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Repo-wide, `useTicketNotifications` is imported by exactly six files: Sidebar.tsx, NotificationBell.tsx, NotificationCenter.tsx, AttentionFeed.tsx, widgets.tsx and inbox/page.tsx — no /documents, /projects, library, folder or request page consumes it, so the `section` field on AttentionItem (useTicketNotifications.ts:112) is written and never read below the sidebar. The one act has three labels: EditOverlapBanner.tsx:79-97 `sendHeadsUp` → button 'Send heads-up'; checkouts/page.tsx:517 `{nudged ? \"Heads-up sent\" : nudging ? \"Sending…\" : \"Nudge to coordinate\"}`; lib/staleCopies.ts:182-208 `nudgeStaleHolders` behind DistributionRecall.tsx:48-56; plus lib/orchestrator/tools.ts:505-515 `kind: \"orchestrator_message\"`. `grep -ni \"nudge|heads-up|remind|poke\" app/(protected)/requests/[id]/page.tsx` returns one unrelated line (:1640, a draft-submission hint).",
    "note": "Both halves confirmed. All four nudge paths require a document_id (the orchestrator tool at tools.ts:495 loads `d.document_number` / `d.library_id` before emitting), so none is reachable from a ticket stuck at PENDING_FINAL_APPROVAL."
  },
  {
    "id": "TAX-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/storageAlerts.ts:60-62 `await sb.from(\"notifications\").insert({ ... kind: \"storage_alert\", title: band === \"crit\" ? \\`Storage critical — ${pct}% full\\` : ... })` and lib/storageUsage.ts:255-256 `kind: \\`storage_${alert.key}\\`` (keys `platform_r2` at :219 and `platform_db` at :231). None of `storage_alert`, `storage_platform_r2`, `storage_platform_db` appears in the NotificationKind union (lib/inAppNotifications.ts:10-58). The DB accepts them: 20260621_in_app_notifications.sql:17 `kind TEXT NOT NULL,` with no CHECK constraint.",
    "note": "Verified end to end — these kinds miss KIND_ICON (NotificationBell.tsx:166 falls back to `Bell`), fall through sectionForKind's default to 'other' (which no sidebar row badges), and match no KIND_GROUPS predicate, so groupOf() returns 'other' and no chip is rendered for them (AttentionFeed.tsx:144-158 only maps KIND_GROUPS)."
  },
  {
    "id": "TAX-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "public/sw.js:223-239 `/* Web Push: scheduled reminders ... (fires whether the app is open or closed) */ self.addEventListener(\"push\", ...)` plus the notificationclick handler at :241-255; supabase/migrations/20260804_push_subscriptions.sql:7-35 creates the table with three RLS policies; 20260723_notifications_unify.sql:87 `ADD COLUMN IF NOT EXISTS push_enabled BOOLEAN NOT NULL DEFAULT TRUE`. Repo-wide grep for `pushManager`, `web-push`, `webpush`, `VAPID`/`vapid` returns ZERO hits outside audit reports, and the only code references to `push_subscriptions` are lib/schemaExpectations.ts:99, lib/exportTables.ts:167 and lib/dataRestore.ts:92. ServiceWorkerManager.tsx:31-46 registers `/sw.js` and tracks the waiting worker only — no subscribe call. lib/notify/dispatch.ts:20 `export type NotifChannel = \"inapp\" | \"email\";` — push is not even a channel.",
    "note": "Confirmed with no mitigation found: nothing calls registration.pushManager.subscribe, no VAPID key exists anywhere, no sender writes to the Web Push endpoint, and settings/notifications/page.tsx's Prefs (:24-31) omits push_enabled entirely. lib/schemaExpectations.ts:99 additionally makes the health check demand a table for a feature that has no client or server half."
  },
  {
    "id": "TAX-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/checkoutEpisodes.ts:670-672 `kind: \"checkout_released\", title: \"Your checkout was force-released\"` vs app/api/cron/maintenance/route.ts:346-348 `kind: \"checkout_released\", title: \\`Checkout held ${days} days — review needed\\`, body: \\`... Nudge them or force-release if the work is done.\\`` — both land as actionRequired (useTicketNotifications.ts:279 `actionKinds` includes 'checkout_released') with the same `Lock` icon (NotificationBell.tsx:36). doc_superseded producers: lib/staleCopies.ts:198 (recall of an outdated downloaded copy), app/api/intake/upload/route.ts:351 (external intake auto-published a new rev), lib/distributionAcks.ts:138 and :322 (manual distribution-ack request / re-nudge), lib/postPublish.ts:39 and :177, lib/revisionImpact.ts:138, lib/workPackages.ts:279.",
    "note": "Confirmed, and understated if anything: doc_superseded has eight distinct producers carrying at least four unrelated meanings, and checkout_released has three (checkoutEpisodes force-release, the cron stale-checkout escalation, and lib/projects.ts:1107). Nothing downstream can tell them apart — kind is the only discriminator any icon, tone, section or future mute/push filter has."
  },
  {
    "id": "TAX-14",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/providers/BackupIndicator.tsx:27 `className=\"fixed bottom-5 left-5 z-[300] w-[340px] ...\"` sits directly over components/pwa/ServiceWorkerManager.tsx:73 `className=\"fixed bottom-4 left-4 z-[200] flex flex-col gap-2 ...\"`, which renders both the 'Offline — showing cached data' pill (:76-80) and 'Update available — tap to refresh' (:82-88). Third/fourth surfaces: CornerDock.tsx:25 `fixed bottom-4 right-4 z-[300]`, UndoToastHost.tsx:21 `fixed bottom-4 left-1/2 -translate-x-1/2 z-[280]`, UpdatePill.tsx:41 `fixed top-3 left-1/2 -translate-x-1/2 z-[100]` reading 'This tab is running an old version — tap to load the update'.",
    "note": "Confirmed including simultaneous mounting: app/(protected)/layout.tsx:61-65 mounts UpdatePill, CornerDock, BackupIndicator and KnowledgeIndexIndicator; app/layout.tsx:93 mounts ServiceWorkerManager; UndoToastHost is mounted by components/projects/ExecutionView.tsx:926 — so the Projects → Execution scenario has all of them live at once, and the two different 'new version' wordings come from two independent detectors (a waiting service worker vs. a polled /api/version build id)."
  },
  {
    "id": "TAX-2",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "useTicketNotifications.ts:100-102 `default: return 'other';`. Cross-checking app/api/cron/maintenance/route.ts:361-374 `const COMPLIANCE_KINDS = [\"review_due\", \"owner_behind\", \"ack_requested\", \"ack_overdue\", \"ack_unsatisfiable\", \"retention_eligible\", \"access_recert_due\", \"effective_now\", \"review_requested\", \"review_overdue\", \"review_complete\", \"review_alternate_activated\", \"deletion_requested\", \"doc_superseded\", \"review_invalidated\"]` against the switch at lines 71-99: 14 of the 15 hit `default` -> 'other'. Sidebar.tsx renders `badgeOf(...)` for exactly three sections — `sectionCounts.documents` (229), `sectionCounts.projects` (231), `sectionCounts.requests` (235) — so 'other' (and 'scratchpad') badge nowhere. lib/reviewCycles.ts:300 confirms the writer: `orgId, userId: uid, kind: \"review_due\"`.",
    "note": "The mechanism is real and the review_due walkthrough is correct. Two corrections: the title's \"Every\" is false — `doc_superseded` is in COMPLIANCE_KINDS and DOES map to 'documents' (line 82) — and the trail does not fully die, since these items still appear in the bell/Notification Center/`/inbox` (NotificationCenter.tsx:77 counts all items) and group under AttentionFeed's \"Documents & revisions\" matcher (line 66 matches `review`/`ack`/`effective`/`retention`). That is a routing/discoverability gap, not a dropped obligation."
  },
  {
    "id": "TAX-3",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "The tone contradiction is exact. hooks/useTicketNotifications.ts:279 `const actionKinds = new Set(['checkout_conflict', 'checkout_released', 'overlap_advisory', 'branch_open']);` -> line 287 `actionRequired: actionKinds.has(n.kind)` = true for checkout_released. But NotificationListener.tsx:90 `const isError = row.kind === \"checkout_conflict\" || row.kind === \"hold_opened\";` omits checkout_released, so line 93 `type: isError ? \"warning\" : isMention ? \"info\" : \"info\"` renders it blue/info. The two names are lib/checkoutEpisodes.ts:655 `text: `SYSTEM ALERT: checkout force-released by ${input.actorName}. All sessions ended.`` (which reaches every org member as NotificationListener.tsx:66-67's blue `\"System Alert\"`) and line 668 `title: \"Your checkout was force-released\"`.",
    "note": "Every element checks out — the system message toast, the notification-row toast, the durable bell row, the email (lib/notify/dispatch.ts:83 `const channels = input.channels ?? [\"inapp\", \"email\"]` with no override at the call site), and the info tone on an item the feed itself flags action-required. HIGH is a notch too far because no signal is lost: the victim gets a durable action-required feed row plus email; the defect is duplicate, mis-toned, mis-named noise."
  },
  {
    "id": "TAX-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "NotificationListener.tsx:46 subscribes to checkout_messages INSERTs with `filter: `org_id=eq.${activeOrgId}`` — org-scoped only — and lines 65-70 toast `title: isSystem ? \"System Alert\" : `New Message from ${data.user_name}``, `message: data.text` to every signed-in member. RLS does not narrow it: supabase/migrations/20260620_checkout_activity_thread.sql:61-69 `CREATE POLICY checkout_messages_org_select ... USING (EXISTS (SELECT 1 FROM org_members WHERE org_members.org_id = checkout_messages.org_id AND org_members.uid = auth.uid() AND org_members.status = 'active'))` — no document-access predicate. The second, differently-worded toast comes from lib/activityThread.ts:97-98 `void notifyCheckoutActivity({ ...input, episodeId })` -> lines 153-158 `kind: \"checkout_message\", title: `${input.userName} ${kindWord} ${label}``, whose notifications row re-fires through NotificationListener.tsx:92-97.",
    "note": "I specifically hunted for the guard that would refute the broadcast — a document-level RLS predicate or a client-side access check before showToast — and there is none in either place. Recipients of the durable notification get two cards with different wordings (amber warning + blue info), and every other active org member gets the raw message text regardless of library or document permission, which is the confidentiality angle that keeps this at HIGH."
  },
  {
    "id": "TAX-5",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "hooks/useTicketNotifications.ts:85 `case 'markup_request': ... return 'documents';` vs components/cockpit/AttentionFeed.tsx:66 `{ key: \"requests\", ... match: (k) => k.includes(\"ticket\") || k.includes(\"assign\") || k.includes(\"approval\") || k.includes(\"engineer\") || k.includes(\"markup\") }` — groupOf('markup_request') returns \"requests\" while sectionForKind returns 'documents'. Independent, unshared classifiers confirmed at lib/inAppNotifications.ts:10-58 (the union), useTicketNotifications.ts:71-103 (sectionForKind), :279 `const actionKinds = new Set([...])`, NotificationBell.tsx:19-44 (KIND_ICON), AttentionFeed.tsx:35-51 (attentionVisual), :63-74 (KIND_GROUPS), NotificationListener.tsx:90 `const isError = row.kind === \"checkout_conflict\" || row.kind === \"hold_opened\";`, and app/api/cron/maintenance/route.ts:361-374 (COMPLIANCE_KINDS).",
    "note": "Factually airtight — eight, not six, hand-maintained kind classifiers exist and none derives from a registry; 'review_requested' likewise hits KIND_GROUPS 'documents' (k.includes(\"review\")) while sectionForKind falls through to 'other'. Downgraded to MEDIUM because the demonstrated user-visible consequence is which filter chip a row files under (cosmetic/maintainability); no data is lost and no obligation is hidden by this finding on its own."
  },
  {
    "id": "TAX-6",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "hooks/useTicketNotifications.ts:176 `let n = await listMyNotifications({ onlyUnread: true, limit: 50, orgId: activeOrgId })`, backed by lib/inAppNotifications.ts:164 `.limit(opts?.limit ?? 50)` ordered `created_at desc`; useTicketNotifications.ts:311 `count: items.length`. Every surface (NotificationBell.tsx:54, NotificationCenter.tsx:55, widgets.tsx:668, inbox/page.tsx:36) reads the same capped hook, and `countUnread()` (lib/inAppNotifications.ts:176-185) — the only uncapped counter in the notification lib — has zero callers repo-wide.",
    "note": "The 50-row ceiling and the 'mark read → older rows page in → badge does not move' behavior are both real and confirmed. Downgraded to MEDIUM because the compliance backlog is NOT unknowable: lib/inbox.ts:249/309 computes an uncapped exact unread count, and /inbox renders dedicated, independently-sourced obligation lists — `data.reviewCyclesDueOnMe` (inbox/page.tsx:400), `data.distributionAcksPendingOnMe` (:416), `data.accessRecertsDue` (:430), plus listMyPendingAcks/listMyPendingReviews — so the actual PSM work items remain reachable outside the truncated bell feed."
  },
  {
    "id": "TAX-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "hooks/useTicketNotifications.ts:254-256 `const unread = !!uid && !!t.unreadBy?.includes(uid); if (!actionReq && !unread) continue; if (actionReq) ar++; else ur++;` → returned as `unreadCount` at :314 (tickets-with-unread-activity only). AttentionFeed.tsx:88 `{ key: \"unread\", label: \"Activity\", n: counts.unread }`, where NotificationCenter.tsx:79 defines `unread: items.filter((i) => !i.actionRequired).length` (also widgets.tsx:673-675 and inbox/page.tsx:71-73). Third meaning: lib/inAppNotifications.ts:170 `if (opts?.onlyUnread) q = q.is(\"read_at\", null);`.",
    "note": "Confirmed on every leg. `unreadCount` is dead — repo-wide grep for `unreadCount` returns only its own definition and return in the hook, no consumer; the same is true of `countUnread` and of lib/inbox.ts's `unreadNotificationCount` (declared :110, computed :249, returned :309, referenced only by a test fixture), so there is in fact a fourth orphaned 'unread' quantity the finding did not name."
  },
  {
    "id": "TAX-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "components/providers/KnowledgeIndexIndicator.tsx:181 `<button onClick={() => setHidden(true)} ... title=\"Dismiss\">` is undone by :91 `setHidden(false);` inside the drain loop, which runs on a 120s interval (:118-121 `const t = setInterval(() => { void drain(); }, POLL_MS)`), and `hidden` is component state with no persistence (:48 `const [hidden, setHidden] = useState(false)`). EditOverlapBanner.tsx:41 `const [dismissed, setDismissed] = useState<Set<string>>(new Set());` + :136 `onClick={() => setDismissed((prev) => new Set(prev).add(row.documentId))}` — in-memory only. Contrast components/ui/FirstRunHint.tsx:45 `window.localStorage.setItem(STORAGE_PREFIX + storageKey, \"1\")`.",
    "note": "Substance holds: both dismissals are ephemeral React state and the indexing card genuinely un-hides itself whenever the poll finds queued work. One imprecision worth recording — the comment at :50-55 ('new work must NOT re-expand a card the user deliberately tucked away') governs `minimized`, not `hidden`, and `minimized` is in fact never reset by the drain, so the code does not literally contradict that comment; it fails to extend the same stickiness to the Dismiss affordance."
  },
  {
    "id": "TAX-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/notify/dispatch.ts:89-103 `if (channels.includes(\"inapp\")) { await notifyMany({...}) }` — no preference read; the email leg at :105-107 explicitly notes queueEmail checks preferences, the in-app leg checks nothing. Repo-wide grep for `inapp_enabled` returns only supabase/migrations/20260723_notifications_unify.sql:86, supabase/schema.sql:658 and the migration's own drop-comment at :11 — no reader, no writer, and app/(protected)/settings/notifications/page.tsx's `Prefs` interface (:24-31) contains only email_* fields plus digest_frequency. ToastProvider.tsx:40-43 `setToasts((prev) => [...prev, { id, ... }])` with no cap, portaled into CornerDock.tsx:25 `className=\"fixed bottom-4 right-4 z-[300] flex flex-col items-end gap-2 ...\"` which has no max-height and no overflow handling.",
    "note": "Every leg verified: no in-app preference gate, no throttle, no toast cap, and the dock cannot scroll or clip a tall stack. NotificationListener.tsx:80-100 fires one showToast per realtime INSERT with no coalescing, so an N-row cron burst does produce N stacked cards."
  },
  {
    "id": "TRAIL-1",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "Repo-wide, `useTicketNotifications` is imported only by components/navigation/Sidebar.tsx:124, NotificationBell.tsx:54, NotificationCenter.tsx:55, AttentionFeed.tsx (type only), widgets.tsx:668 and inbox/page.tsx:36 — confirming no library, folder-tree, grid, inspector or projects page reads notification state. BUT the sidebar row is not the only link: Sidebar.tsx:537-544 makes the badge itself a button `onClick={... openCenter(leaf.badgeTone === 'red' ? 'action' : 'all')}`, and each feed row deep-links via useTicketNotifications.ts:293 `link: n.link || ...`, which producers populate as `/documents/${libraryId}?doc=${documentId}` (e.g. lib/staleCopies.ts:201, app/api/cron/maintenance/route.ts:349, lib/orchestrator/tools.ts:512) — and documents/[libraryId]/page.tsx:1284-1319 consumes `?doc=`, selecting the document and navigating to its folder if needed.",
    "note": "The finding's central assertion — 'the trail has exactly one link' and the user 'opens the right one by guessing' — is wrong: badge → Notification Center → row → the exact document is a working chain, and the deep-link handler even resolves the doc's folder. What genuinely survives is narrower: no ambient per-library/per-folder/per-row marker exists while browsing, so attention state is invisible unless you enter through a notification row. That is a MEDIUM wayfinding gap, not a CRITICAL dead end."
  },
  {
    "id": "TRAIL-10",
    "verdict": "REFUTED",
    "corrected_severity": null,
    "evidence": "Sidebar.tsx:532-544 — the badge is NOT part of the row's navigation: `<button type=\"button\" onClick={(e) => { e.preventDefault(); e.stopPropagation(); openCenter(leaf.badgeTone === 'red' ? 'action' : 'all'); }} title=\"See these notifications\">`. The comment above it states the intent: 'The count is a DOORWAY, not a scoreboard: clicking it opens the Notification Center showing exactly the items it counts.' app/(protected)/layout.tsx:145 wraps ProtectedContent (and therefore Sidebar) in NotificationCenterProvider, so openCenter is the real implementation, not the no-op default.",
    "note": "False as stated — the badge does not land you on /documents at all; preventDefault/stopPropagation cancel the Link and open the Notification Center, which lists the individual item with its own deep link (e.g. /documents/<libraryId>?doc=<id> from holds.ts:249 or a checkout link). Only the row itself navigates to /documents, which is the intended behavior of a nav rail, and the six views are then visible as ViewTabs (DOCUMENT_VIEWS, ViewTabs.tsx:80-87)."
  },
  {
    "id": "TRAIL-11",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "requests/page.tsx:1037-1038 `{isActionNeeded && <div className=\"w-2.5 h-2.5 bg-red-500 rounded-full mr-2\" title=\"Action Required\" />}{!isActionNeeded && isUnread && <div className=\"... bg-blue-500 ...\" title=\"New Updates\" />}` (and the grid equivalent at 1138-1146). By contrast documents/page.tsx:388-467 renders each library card with only NodeCover, name, description, Public Read/Controlled/No Access chips and an 'Open' affordance, and projects/page.tsx:199-261 (ProjectCard) renders only status/visibility/owner/due/MOC/last-activity — neither reads any notification or unread state.",
    "note": "Verified: the per-row attention dot exists only in the requests portal. Downgraded to LOW — this is the same missing-capability observation as TRAIL-5/TRAIL-6 restated as a UX-consistency complaint, and it describes an unbuilt feature rather than incorrect behavior."
  },
  {
    "id": "TRAIL-12",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "hooks/useTicketNotifications.ts:293-294 `link: n.link || (n.resourceId ? (n.resourceType === 'document' ? `/search?q=${encodeURIComponent(n.resourceId)}` : ...`. lib/search.ts:171-235 shows searchDocuments filters on library/collection/plant/unit/system/project/status and text only — applyTextSearch on search_tsv, the ILIKE fallback `.or('document_number.ilike...,title.ilike...,name.ilike...')` (line 213) and equipment-tag augmentation; `id` is never a predicate. globalSearch.ts:36 is the only path /search uses.",
    "note": "The mechanism is real — a document notification with no link deep-links to a uuid query that structurally cannot match. But the headline example is wrong: lib/revisions.ts:124 types `libraryId: string` (required) and line 699 passes it straight to announceBranchOpened, so branch_open ALWAYS has a link. The fallback is only reachable via the nullable-libraryId emitters (staleCopies.ts:186/201, distributionAcks.ts:104/141, EditOverlapBanner.tsx:22/92), hence LOW."
  },
  {
    "id": "TRAIL-13",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "hooks/useTicketNotifications.ts:256 `if (actionReq) ar++; else ur++;` sits inside the `for (const t of tickets)` loop only — the notification loop at 281-302 never touches `ar`, even though line 288 sets `actionRequired: actionKinds.has(n.kind)` on the item. NotificationCenter.tsx:76-79 computes `action: items.filter((i) => i.actionRequired).length`, and widgets.tsx:673-675 (AttentionBody) does the same, while widgets.tsx:1223 reads `actionRequiredCount` from the hook.",
    "note": "Confirmed — one hook call yields two different action counts, and a checkout_conflict/checkout_released/overlap_advisory/branch_open row is counted by the center's 'Action' tab but not by actionRequiredCount, which the Command Deck stat that opens that tab renders."
  },
  {
    "id": "TRAIL-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Counted directly: lib/inAppNotifications.ts:10-58 declares exactly 48 kinds; hooks/useTicketNotifications.ts:71-103 names 22 of them (5 → 'requests', 3 → 'scratchpad', 12 → 'documents', 2 → 'projects'), leaving exactly 26 hitting `default: return 'other';` at :100-101 — including ack_requested, ack_overdue, ack_unsatisfiable, review_requested, review_due, review_overdue, effective_now, retention_eligible, legal_hold_placed and access_recert_due. Sidebar.tsx spreads `badgeOf(...)` on exactly three rows — :229 sectionCounts.documents, :231 sectionCounts.projects, :235 sectionCounts.requests — and grep for 'scratchpad' in Sidebar.tsx returns nothing at all.",
    "note": "Verified to the number. Worse than stated: there is no Scratchpad nav item anywhere in the sidebar, so sectionCounts.scratchpad is tallied (useTicketNotifications.ts:247-250) and discarded entirely, exactly like 'other'. Severity HIGH stands — ack_requested is the audit-relevant nudge and the nav rail is the primary wayfinding surface, even though the bell total and /inbox's dedicated pending-ack list still surface the item."
  },
  {
    "id": "TRAIL-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Sidebar.tsx:539 `onClick={(e) => { e.preventDefault(); e.stopPropagation(); openCenter(leaf.badgeTone === 'red' ? 'action' : 'all'); }}` — the section identity is dropped at the call site; NotificationCenter's open signature is `open: (filter?: AttnFilter) => void` (:23) where `AttnFilter = \"all\" | \"action\" | \"unread\"` (AttentionFeed.tsx:22) with no section dimension. CenterPanel then renders `items` straight from the hook (:55) filtered only by action/unread (:81-85), and its header prints `${counts.all} item${...} — every badge in the app counts these.` (:116), where `counts.all = items.length` (:77).",
    "note": "Confirmed with no mitigating path: nothing anywhere passes an AttentionSection to the center, and its header line is affirmatively false for section badges, which count only their own section. The AttentionFeed group chips are no substitute — they use the independent groupOf() taxonomy (AttentionFeed.tsx:63-74) that per TAX-5 disagrees with sectionForKind, so a user cannot reconstruct the three Documents items by filtering."
  },
  {
    "id": "TRAIL-4",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "app/(protected)/requests/[id]/page.tsx:921-928 `if (uid) { supabase.from('notifications').update({ read_at: new Date().toISOString() }).eq('user_id', uid).eq('resource_id', ticketId).is('read_at', null).then(() => {}); }` is the only visit-clears-by-resource writer — documents/[libraryId]/page.tsx:1284-1319 handles `?doc=` with no notifications write, and a repo-wide grep for markRead/markManyRead/markAllRead across app/, components/ and hooks/ finds writers only in the hook's stale-workflow reconciler (:207) and the four notification surfaces. HOWEVER the finding's own cited line refutes its scenario: NotificationBell.tsx:89-93 `const onItemClick = async (item) => { if (item.notificationId) { try { await markRead(item.notificationId); } ... } }`, and AttentionFeed.tsx:198 `onClick={() => { if (item.notificationId) onMarkRead(item.notificationId); }}`.",
    "note": "The narrow claim is true — only /requests/[id] clears notifications by visiting the resource — but 'the Documents badge never decays' and 'User opens each of the four documents via the bell ... the badge still says 4 because reading was never recorded' are false: clicking through from the bell, the Notification Center, the Needs-You widget or /inbox marks that row read before navigating. The real residue is the asymmetry — arriving at a document by any route other than the notification row leaves it unread — which is a MEDIUM inconsistency, not a badge that can only be cleared by 'mark all read'."
  },
  {
    "id": "TRAIL-5",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "hooks/useTicketNotifications.ts:301 `tally(section, false);` is the ONLY tally call for notification-sourced items; the sole other call site is line 272 `tally('requests', actionReq)`. Sidebar.tsx:130 `return { badge: s.total, badgeTone: s.actionRequired > 0 ? 'red' : 'blue' };`. Line 288 `actionRequired: actionKinds.has(n.kind)` proves the hook knows the item IS action-required, and line 279 includes 'checkout_released'/'branch_open', which sectionForKind maps to 'documents' (lines 90-96).",
    "note": "Claim is exactly right: sectionCounts.documents.actionRequired and .projects.actionRequired are literally unreachable, so those rows can only ever badge blue ('unread FYI' per the Sidebar's own contract at line 22). Downgraded to MEDIUM because no information is lost — the count still renders and the badge button (Sidebar.tsx:537-544) opens the Notification Center, whose own counts.action (NotificationCenter.tsx:78) does classify these items correctly; only the tone and the pre-filter are wrong."
  },
  {
    "id": "TRAIL-6",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "20260621_in_app_notifications.sql:13-27 defines exactly (id, org_id, user_id, kind, title, body, link, resource_type, resource_id TEXT, actor_user_id, actor_name, metadata, read_at, created_at) — no library_id, no collection_id; the later 20260723_notifications_unify.sql:15-30 re-creates the identical column set, and no ALTER adds either column anywhere in supabase/migrations. lib/inAppNotifications.ts:140-155 (NotificationRow) and dispatch.ts's `resourceType/resourceId` pass-through carry nothing more.",
    "note": "Factually correct — the row genuinely cannot be rolled up to a library or folder without a second query, and holds.ts:232-241 only fetches library_id to build the link string, never persisting it. Downgraded to LOW: this describes a feature that was never built (container badges) rather than a defect in shipped behavior, and notifications_org_resource_idx (org_id, resource_type, resource_id) makes the extra lookup cheap and indexed."
  },
  {
    "id": "TRAIL-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "hooks/useTicketNotifications.ts:176 `listMyNotifications({ onlyUnread: true, limit: 50, orgId: activeOrgId })` against lib/inAppNotifications.ts:160-164 `.order(\"created_at\", { ascending: false }).limit(opts?.limit ?? 50)`; hooks/useTicketNotifications.ts:31 `const OPEN_TICKET_CAP = 500;` applied at line 156 `.order('last_modified', { ascending: false }).limit(OPEN_TICKET_CAP)`. Every count derives from those arrays: line 305 `return { items: out, ... }` and line 312 `count: items.length`.",
    "note": "Confirmed on both halves: newest-first ordering plus a hard limit means the OLDEST unread rows are the ones dropped, and count/sectionCounts/NotificationCenter counts are all computed from the truncated arrays (countUnread() in inAppNotifications.ts:176 does an exact head-count but no surface in the attention path calls it). No 'showing N of M' affordance exists in NotificationBell.tsx or NotificationCenter.tsx."
  },
  {
    "id": "TRAIL-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "Repo-wide grep for useTicketNotifications() finds six call sites: Sidebar.tsx:124, NotificationCenter.tsx:55, NotificationBell.tsx:54, widgets.tsx:669, widgets.tsx:1223, inbox/page.tsx:38. Each instance runs its own effect (line 143-230) with `channelId = useId()` (line 141) producing a distinct channel `attention-${activeOrgId}-${channelId}` (line 222), and both subscriptions call the same `void fetchAll()` (lines 224, 226) which re-runs the full ticket + notification fetch.",
    "note": "Confirmed, and worse than 'five': app/(protected)/layout.tsx mounts Sidebar (line 56) and NotificationCenterProvider (line 145) — whose CenterPanel calls the hook unconditionally even while closed — plus the TopBar bell, so three instances are live on EVERY protected route and five on the dashboard. The write-back at line 207 (markManyRead) does land on the `notifications` table the subscription at line 225 watches, so the self-retrigger described is real."
  },
  {
    "id": "TRAIL-9",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "hooks/useTicketNotifications.ts:188-193 selects reconcilable rows as `r.resourceId && r.metadata && typeof r.metadata.status === 'string' && r.metadata.action != null`, then line 196-197 resolves them against `supabase.from('tickets')` only. lib/holds.ts:238-251 emits hold_opened/hold_released through emit() with no `metadata` key at all, so `r.metadata` is null and the filter excludes them; dispatch.ts passes `metadata: input.metadata` straight through (undefined here).",
    "note": "Accurate: hold rows can never be reconciled, and the hook's own header comment (lines 24-25, 'stale workflow alerts ... are reconciled away — so the badge can never disagree with the portal') overstates what the code does. Downgraded to LOW because an unread-until-read inbox row is normal semantics, the count is real unread mail rather than fabricated, and hold_released is itself the resolution notice — the badge is noisy, not false."
  },
  {
    "id": "TRX-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260910_transmittal_portal.sql:40-44 — `CREATE POLICY transmittals_insert ON transmittals FOR INSERT WITH CHECK (created_by = auth.uid() AND EXISTS (SELECT 1 FROM org_members WHERE org_id = transmittals.org_id AND uid = auth.uid() AND status = 'active'));` — there is no `status = 'draft'` predicate, while the same file's header (:15-17) claims 'members create their OWN drafts'. lib/transmittals.ts:447-454 inserts `status: issueNow ? \"issued\" : \"draft\"` together with `...(issueNow ? { portal_token: makePortalToken() } : {})` in one statement. lib/roleCapabilities.ts:67 `Viewer: [],` and :66 `Contractor: [\"create_requests\"],`.",
    "note": "Confirmed by absence too: app/(protected)/transmittals/page.tsx contains no capability/role check at all (grep for hasCapability/ROLE_CAPABILITIES/activeRole=== returns nothing), `openNew` at :133 is unconditional, ViewTabs DOCUMENT_VIEWS lists /transmittals for everyone (components/navigation/ViewTabs.tsx:86), and there is no middleware.ts or protected-layout role gate. The composer's `save(true)` (page.tsx:427) passes `issueNow: issue` straight through. Any active member issues a contractual record."
  },
  {
    "id": "TRX-10",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/(protected)/transmittals/page.tsx:422-425 — `if (editing) { await updateTransmittalDraft(...); if (issue) await issueTransmittal(editing.id, actor); await onSaved(issue, { ...editing, ...fields, status: \"issued\", issuedAt: ... }) }`; `issueTransmittal` (lib/transmittals.ts:558-580) never returns the updated row, so the object handed to the sheet still carries the DRAFT's `portalToken: null`. openTransmittalSheet gates the QR/link block on `if (t.portalToken && t.status !== \"voided\")` (lib/transmittals.ts:315) and renderTransmittalSheet only emits the portal panel `${opts?.portalUrl ? ... : \"\"}` (line 188). Toast: page.tsx:311-313 `issued ? `${t?.number} issued — ${t?.recipientEmail?.trim() ? `portal link emailed to ${...} and ` : \"\"}cover sheet opened.`` — keyed on the email field being non-empty, never on sendTransmittalEmail's boolean (it returns false at lib/transmittals.ts:277 and on a queue throw at 303-306, and both call sites discard it).",
    "note": "Both halves check out. The freshly-issued row does get its token in the DB (issueTransmittal line 562), so the list's own 'Cover sheet' button prints correctly afterwards — but the sheet auto-opened at issue time, the one that goes in the project file, has no QR and no URL."
  },
  {
    "id": "TRX-11",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/transmittal/route.ts:41-44 `await supabaseAdmin.from(\"document_versions\").select(\"file_url, revision_label\").eq(\"id\", item.versionId).maybeSingle()` — no `.eq(\"org_id\", t.org_id)`; likewise 47-50 filters only on `record_id`/`revision_label`. The client is `supabaseAdmin` (line 13), and lib/r2.ts:20 `export const R2_BUCKET = process.env.R2_BUCKET_NAME!` is one bucket for every tenant. The insert policy 20260910_transmittal_portal.sql:40-44 checks only `created_by = auth.uid()` + active membership, and nothing constrains the `items` JSONB.",
    "note": "No tenancy check exists anywhere on the resolution path — the only gate is `items.find(i => i.documentId === fileDoc)` (line 67), whose contents the attacker authored. Practical exploitation needs a version UUID from the other org, which is the only thing keeping this at MEDIUM rather than higher."
  },
  {
    "id": "TRX-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/transmittal/route.ts:47-52: `.eq(\"record_id\", item.documentId).eq(\"revision_label\", item.rev).order(\"created_at\", { ascending: false }).limit(1)` — no superseded_at / is_branch / review_state predicate, despite the comment at line 37 (\"never silently the newest\"). The uniqueness backstop is scoped away from branches: 20260823_publish_contract.sql:60-67 `CREATE UNIQUE INDEX ... ON document_versions(record_id, revision_label) WHERE (superseded_at IS NULL AND is_branch = FALSE)`. Unapproved rows sharing a label are also reachable: app/api/intake/upload/route.ts:303-317 inserts with `review_state: autoNow ? \"approved\" : \"in_review\"`. Null versionId is the normal composer case — page.tsx:399 `versionId: (d.current_version_id as string) ?? null`.",
    "note": "Confirmed: when item.versionId is null the fallback hands out whatever row most recently carried that label, including an is_branch row or an in_review intake submission. The pinned path (line 41) is the only correct one."
  },
  {
    "id": "TRX-13",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "Repo-wide grep for `acknowledged_meta` returns exactly three hits: the migration (20260910_transmittal_portal.sql:12 comment, :24 the ALTER TABLE) and the single write at app/api/transmittal/route.ts:133 `acknowledged_meta: meta`. rowToTransmittal (lib/transmittals.ts:335-369) maps no such field. BUT the same values are duplicated into the audit trail at route.ts:144 `details: { number: t.number, acknowledgedBy: name, via: \"portal\", ...meta }`, and app/(protected)/admin/audit/page.tsx:407-413 renders `{JSON.stringify(row.details, null, 2)}` with :450 exporting it to CSV.",
    "note": "The column itself is genuinely write-only and neither artifact shows it — evidencePack.ts:184-190 prints only `${acknowledged_by_name} · ${acknowledged_at}` and the cover sheet's ackLine (lib/transmittals.ts:116-121) the same, and both evidence packs filter audit_logs by resource_type 'document'/'project' so the 'transmittal' row never reaches them. But 'never mapped, read, or rendered' overstates it: the IP and user agent survive in audit_logs.details and are visible and CSV-exportable on /admin/audit, so the dispute evidence is recoverable. Cosmetic/plumbing gap, not lost data — LOW."
  },
  {
    "id": "TRX-14",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/transmittals.ts:389-392 `export function transmittalPortalUrl(token: string) { const origin = typeof window !== \"undefined\" ? window.location.origin : \"\"; return `${origin}/transmittal/${token}`; }` versus lib/publicOrigin.ts:17-21 `const configured = (process.env.NEXT_PUBLIC_SITE_URL || \"\").trim()...; if (configured) return configured;`, whose header comment states the exact failure mode ('Vercel gates those behind its own login, so the scan dead-ends'). lib/downloads.ts:96-100 `buildVerifyUrl` uses `publicOrigin()`. The bad origin reaches the recipient's email through sendTransmittalEmail → line 278 `const portalUrl = transmittalPortalUrl(t.portalToken)` and the QR at line 316-319.",
    "note": "Confirmed — transmittalPortalUrl is the only URL builder in the repo that still uses window.location.origin for an externally-consumed link, and publicOrigin() exists precisely for this. Note the fix only helps where NEXT_PUBLIC_SITE_URL is set, since publicOrigin falls back to window.location.origin (publicOrigin.ts:20)."
  },
  {
    "id": "TRX-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260910_transmittal_portal.sql:54-56 — `CREATE POLICY transmittals_delete ON transmittals FOR DELETE USING (is_org_controller(org_id) OR created_by = auth.uid());` — no status predicate. The restrictive companion at 20260818_followups_rls.sql:52-58 also permits `created_by = auth.uid()` with no status test. The deferral is explicit at 20260815_versions_collections_delete_controllers.sql:15: `--   * transmittals delete   — issuer roles, draft-only`, listed under 'NOT included here'. The draft-only rule lives solely at lib/transmittals.ts:642-643: `.delete().eq(\"id\", id).eq(\"status\", \"draft\")`.",
    "note": "The legal-hold trigger cannot cover the gap: 20260826_legal_hold_delete_guard.sql attaches `BEFORE DELETE` triggers only to `documents` (:53-57) and `document_versions` (:85-89) — there is no trigger on transmittals in any migration (grep for 'transmittals' across supabase/migrations returns only the policy statements). A creator's direct DELETE on an issued or acknowledged transmittal is permitted at the database."
  },
  {
    "id": "TRX-3",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "Holds/legal hold: grep for `legal_hold|document_holds` across app/(protected)/transmittals/page.tsx, lib/transmittals.ts, and app/api/transmittal/route.ts returns nothing. The portal's resolver (app/api/transmittal/route.ts:39-55) selects `file_url, revision_label` from document_versions with no join to documents at all, and GET's only state check is `if (t.status === \"voided\")` (:62). But two sub-claims are weaker than stated: the composer search DOES filter status — page.tsx:389 `.neq(\"status\", \"Archived\")` — and the effective-date complaint contradicts the app's own model, lib/effectiveDate.ts:5-7: 'the revision is still the current controlled version (which rev is served never changes here); it simply shows \"Effective <date>\"'.",
    "note": "The core is right and unguarded: a document under a legal hold (20260820_retention.sql:31) or an open operational hold (20260612_phase5_holds.sql) can be added to a transmittal and served by the portal with no check anywhere, and the Inspector's entry point (components/documents/InspectorPanel.tsx:582-583) is an unconditional NextLink. Corrected because 'nothing checks status' is not literally true (Archived is excluded) and the effective-date item is a design choice the app applies uniformly, not a transmittal-specific defect — which removes half the stated blast radius that HIGH rested on."
  },
  {
    "id": "TRX-4",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260910_transmittal_portal.sql:21-27 adds only `portal_token TEXT`, `acknowledged_via`, `acknowledged_meta` and a unique index — no expires_at, no revoked_at, no last_used_at. Contrast supabase/migrations/20260902_project_intake.sql:28-33 (`expires_at TIMESTAMPTZ, revoked_at TIMESTAMPTZ, ... last_used_at TIMESTAMPTZ, submission_count INT`) which app/api/intake/resolve/route.ts:39-42 actually enforces (`if (link.revoked_at) ... if (link.expires_at && Date.parse(...) < Date.now())`). The transmittal loader has no equivalent: app/api/transmittal/route.ts:26-31 matches on portal_token alone, and the only kill switch is `if (t.status === \"voided\") return ... 410` (:62).",
    "note": "The export half is confirmed too: lib/exportTables.ts:54 lists `\"transmittals\"` (and :51 `\"project_intake_links\"`) among exported tables, and lib/dataExport.ts:300 pulls them with `sb.from(table).select(\"*\")` — grep for 'redact' in lib/dataExport.ts returns nothing, so live portal_token values leave in plaintext. A repo-wide grep confirms portal_token exists in exactly one migration and is never expired, rotated, or counted."
  },
  {
    "id": "TRX-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/transmittal/route.ts:71-76 hands back a raw presigned bucket URL: `const url = await getSignedUrl(r2, new GetObjectCommand({ Bucket: R2_BUCKET, Key: file.key, ... }), { expiresIn: 300 }); ... return NextResponse.json({ url });` — and app/transmittal/[token]/page.tsx:54 opens it directly (`window.open(body.url, ...)`). The app's own rule is the opposite, lib/downloads.ts:4-6: 'User holds an active checkout on the document -> CONTROLLED copy (raw PDF) / Otherwise -> UNCONTROLLED copy (stamped)'. app/api/share/file/route.ts:107-124 applies `watermarkText: \"UNCONTROLLED — SHARED COPY\"` plus a rev footer and verify QR for the analogous outside-party link, and its fallback comment (:120-123) states the invariant explicitly: 'Deliver it anyway — but through this route, never a raw bucket URL'.",
    "note": "An external recipient definitionally holds no checkout, so lib/downloads.ts:230-240 would give them the stamped copy through any in-app path; the portal is the one egress that inverts it. It also skips the distribution record — app/api/share/file/route.ts:129-141 writes a `download_audits` row, while the portal writes only an audit_logs entry (route.ts:77-82) and no download_audits row at all."
  },
  {
    "id": "TRX-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "supabase/migrations/20260910_transmittal_portal.sql:47-52 — `FOR UPDATE USING (is_org_controller(org_id) OR created_by = auth.uid()) WITH CHECK (is_org_controller(org_id) OR created_by = auth.uid())` — and :54-56 for DELETE. The `created_by` branch carries no membership test, unlike both siblings in the same file: the SELECT policy at :34-37 and the INSERT policy at :40-44 each require `EXISTS (SELECT 1 FROM org_members ... AND status = 'active')`. The contrast the finding cites is exact — supabase/migrations/20260814_documents_delete_controllers.sql:31-40 defines `is_org_controller` with `AND status = 'active'`, so only the controller half of the OR is membership-aware.",
    "note": "Confirmed by absence: grep across supabase/migrations shows no other UPDATE policy or BEFORE UPDATE trigger on transmittals, so nothing re-adds the check. Note the deactivated member loses SELECT (the read policy does test status) but PostgREST UPDATE/DELETE by id needs only the UPDATE/DELETE USING clause, so blind writes on a known id still land — and no session-revocation-on-deactivation path exists in the repo."
  },
  {
    "id": "TRX-7",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "lib/transmittals.ts:577-591: after `.eq(\"status\", \"draft\").select(\"*\").maybeSingle()`, the code guards only the email (`if (data) { await sendTransmittalEmail(...) }`) and then writes the audit row unconditionally, with `details: data ? {...} : undefined` — proving the zero-row case was anticipated and still audited as TRANSMITTAL_ISSUED. acknowledgeTransmittal (:594-619) is worse: it checks `error` only, so an RLS-blocked or status-mismatched UPDATE still reaches `logAuditAction({ action: \"TRANSMITTAL_ACKNOWLEDGED\" ... })` at :609-618. voidTransmittal (:622-639), deleteTransmittal (:642-645) and updateTransmittalDraft (:536-548) likewise never inspect row counts.",
    "note": "The mechanism is exactly as described — RLS rejections return zero rows with no error under PostgREST, so every one of these reads as success. Downgraded to MEDIUM because the register self-corrects: both handlers call `await refresh()` after the toast (app/(protected)/transmittals/page.tsx:143-145 and :164-166), which re-reads from the database, so the false state is a transient toast rather than a persistent wrong register. The durable damage is a fabricated ISSUED/ACKNOWLEDGED entry in the compliance audit log with no corresponding state change."
  },
  {
    "id": "TRX-8",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/transmittals.ts:34-40 — `export interface TransmittalItem { documentId: string; number: string; title?: string | null; rev?: string | null; versionId?: string | null }` — no hash field, and `grep -rn \"hash\" lib/transmittals.ts supabase/migrations/20260717_transmittals.sql supabase/migrations/20260910_transmittal_portal.sql` returns zero hits. Every comparable binding artifact carries one: supabase/migrations/20260526_document_version_control.sql:41 `ADD COLUMN IF NOT EXISTS file_hash TEXT;` ('Lets the audit prove that the bytes attached to this revision haven't been swapped post-hoc'), 20260720_e_signatures.sql:23 `content_hash TEXT`, and 20260817_read_understood.sql:39 `content_hash TEXT,  -- the file_hash of that version, for audit binding`.",
    "note": "The 'point-in-time SNAPSHOT' framing in the header comment (lib/transmittals.ts:8-10, echoed at 20260717_transmittals.sql:10-14) denormalizes number/title/rev but not bytes. Partially mitigated — `versionId` is usually pinned and document_versions.file_hash may recover the hash — but it is optional in the type, the portal falls back to matching on `revision_label` alone when it is absent (app/api/transmittal/route.ts:47-54), and versions created through the intake route are inserted with no file_hash at all (app/api/intake/upload/route.ts:304-316). MEDIUM is the right level."
  },
  {
    "id": "TRX-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/transmittal/route.ts:75-80 writes ONLY `supabaseAdmin.from(\"audit_logs\").insert({ action: \"TRANSMITTAL_PORTAL_DOWNLOAD\", ... })` — repo-wide grep for `download_audits` inserts returns MultiDocViewer.tsx:747, FileReferenceModal.tsx:189, requests/[id]/page.tsx:593,675, share/file/route.ts:130, docPack.ts:114, downloads.ts:132 and nothing in the transmittal portal. lib/staleCopies.ts:132-139 `getDocumentRecall` reads `.from(\"download_audits\")`, so a portal pull can never appear there. lib/transmittals.ts:404 `if (error) { if (isMissingTable(error)) return []; return []; }` swallows every error, and InspectorPanel.tsx:1059 `if (rows.length === 0) return null;` renders the trail away silently.",
    "note": "Confirmed on all three legs: the portal download is audited only to audit_logs, the recall panel (components/documents/DistributionRecall.tsx:39 → getDocumentRecall) is download_audits-only, and the compensating TransmittalTrail returns [] on any query error and then renders nothing. The trail (InspectorPanel.tsx:1076-1080 'HOLDS SUPERSEDED REV') is a real partial mitigation for external holders, which is why MEDIUM rather than higher is right."
  },
  {
    "id": "VFY-1",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/verify/route.ts:89-90 `const docRetired = d.status === \"Superseded\" || d.status === \"Archived\"; const isCurrent = !docRetired && (!versionId || versionId === d.current_version_id);` and app/api/verify-package/route.ts:56 `const retired = d?.status === \"Superseded\" || d?.status === \"Archived\";`. The three-value set is used at lib/aiBoundary.ts:25 `NOT_CURRENT_STATUSES = new Set([\"Superseded\", \"Void\", \"Archived\"])`, lib/staleCopies.ts:76 `if (d.status === \"Archived\" || d.status === \"Superseded\" || d.status === \"Void\") continue;`, lib/docControlRegister.ts:101 `.or(\"status.is.null,status.not.in.(Draft,Superseded,Void,Archived)\")`, lib/downloads.ts:62-63 `case \"Void\": return { label: \"Void\", tone: \"danger\" };`.",
    "note": "Right, and reachable: `Void` is a first-class document status (types/schema.ts:613) offered in the status dropdown at components/documents/MetadataEditor.tsx:9/394. app/verify/[docId]/page.tsx:106 only special-cases Superseded/Archived too, so a Void document renders the full-screen emerald 'CURRENT — This print matches the current revision.' CRITICAL stands."
  },
  {
    "id": "VFY-10",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "app/api/verify-hold/route.ts:29 selects `document_id` and :40 uses it (`.eq(\"id\", String(h.document_id))`) purely to fetch the doc label; the verdict at :54 is `active: !h.released_at` — a single-row test, with no query against other document_holds rows. supabase/migrations/20260612_phase5_holds.sql:18-21: 'document_holds is a per-document log, NOT a single-column flag. Multiple holds can be open on the same document simultaneously'.",
    "note": "Factually correct — the endpoint has document_id in hand and never asks about sibling holds, and multiple concurrent holds are an explicit design goal. But the card is printed per hold (lib/physicalBridge.ts:150-171 stamps one `reason` and one holdId per card) and its own footer at :168 says 'A released hold shows GREEN when scanned — then this tag comes down', so 'this tag can come down' is literally true for that tag; the other hold's card is still hanging and still scans red. Missing-context enhancement rather than a wrong verdict — LOW."
  },
  {
    "id": "VFY-11",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "app/api/verify-package/route.ts:73 `allFresh: sheets.length > 0 && staleCount === 0` — with zero members, staleCount is 0 but allFresh is false. app/verify-package/[packageId]/page.tsx:60 then paints `bg-red-600`, :91 prints 'PACK IS STALE', and :96 renders `${result.staleCount} of ${result.sheetCount} sheet${...}s changed since this pack was printed`, i.e. '0 of 0 sheets changed'.",
    "note": "The '0 of 0' red screen is real and reads as nonsense. Severity is overstated: the failure direction is fail-closed (stop work), and the same page prints an accurate corrective line inside the card — page.tsx:118-120 `{result.sheets.length === 0 && (<div ...>This package has no sheets.</div>)}` — so the crew is not told a stale sheet exists that they must go find. Copy/verdict-taxonomy defect, LOW."
  },
  {
    "id": "VFY-12",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "A repo-wide `grep -rni \"rate.?limit\" --include=*.ts --include=*.tsx` returns nothing outside app/api/auth/signup/route.ts, which has both halves: `const SIGNUP_MAX_PER_HOUR = 8;` (:10), `signupRateLimited(ip)` → 429 (:39-43), and `recordSignupAttempt(ip, String(email), \"attempt\")` (:60). All four verify routes go straight from a UUID_RE test to a service-role `createClient(...)` read and a NextResponse.json — no counter, no insert, no audit write anywhere in the file. There is no middleware.ts and next.config.ts (14 lines) declares no headers/limits.",
    "note": "Both halves confirmed. The enumeration premise also checks out: app/d/[number]/route.ts is unauthenticated, queries with `supabaseAdmin` (service role, RLS bypassed) on a cross-tenant `ilike` over document_number (:26-32) and redirects with `dest.searchParams.set(\"doc\", match.id)` (:45) — a free document-number-to-UUID oracle feeding /api/verify."
  },
  {
    "id": "VFY-13",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "app/layout.tsx:20-40 defines metadata with title/description/openGraph/keywords and no `robots` key; `find . -iname 'robots*'` returns nothing (no app/robots.ts, no public/robots.txt), no verify page exports metadata (each of the four is a bare `\"use client\"` page), and `grep -rn \"noindex|robots\"` over app/, lib/ and next.config.ts is empty. No verify route sets headers — `grep -rn \"headers|no-store|export const\"` over app/api/verify*/route.ts returns nothing.",
    "note": "Both assertions are literally true. Impact is smaller than MEDIUM: all four verify pages are client components that fetch their data from /api/verify* after hydration, so the HTML a crawler is served contains no document number, title or rev — only the URL, which by hypothesis the leaker already had; and package.json pins `\"next\": \"^16.1.0\"`, where a route handler reading `req.nextUrl.searchParams` is dynamic and is served uncached with a no-store default, so the missing hand-written Cache-Control carries little practical risk."
  },
  {
    "id": "VFY-14",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "Only one of the three cited lines over-selects anything sensitive: app/api/verify-hold/route.ts:29 `.select(\"id, document_id, reason, notes, opened_by_name, opened_at, released_at, released_by_name, released_reason\")` against its own comment at :48-52 ('must not disclose staff names or free-text operator notes'), while the response at :53-61 returns only active/reason/openedAt/releasedAt/docLabel/docRev. app/api/verify/route.ts:36 selects `id, document_number, title, name, rev, status, current_version_id, superseded_at` and :56 selects `revision_label, created_at, superseded_at, record_id` — the unreturned fields there are IDs and timestamps, not people or notes; app/api/verify-ticket/route.ts:51 withholds only `id` and `revision_count`.",
    "note": "Nothing is disclosed today — every route hand-builds its JSON, and the `h as Record<string, unknown>` cast at verify-hold:34 is the only place a spread would be easy. The claim is true for verify-hold but over-generalized to 'the verify routes', and it describes a hypothetical future refactor rather than a present defect, so LOW (defensive-coding hardening) rather than MEDIUM."
  },
  {
    "id": "VFY-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "lib/workPackages.ts:207-217 rewrites every pin to `pinned_version_id: (d?.current_version_id as string | null) ?? null, pinned_rev_label: (d?.rev as string | null) ?? null` with no record of what was printed. app/api/verify-package/route.ts:61 derives freshness solely from `r.pinned_version_id === (d?.current_version_id ?? null)`, and :73 `allFresh: sheets.length > 0 && staleCount === 0`. lib/physicalBridge.ts:275 encodes only `${origin()}/verify-package/${input.packageId}` — no printed-revision snapshot travels on the paper.",
    "note": "Confirmed: a repo-wide grep for `printed_at`/`last_printed` finds nothing, so no data anywhere records what the paper says. One correction to the wording: it is not fully 'silent' — the confirm dialog (packages/page.tsx:112-114) and the success toast (:121) both say 'then re-print the pack so the paper matches'. The tripwire is still disarmed with no technical trace, so HIGH stands."
  },
  {
    "id": "VFY-3",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "MEDIUM",
    "evidence": "app/api/verify/route.ts:90 `const isCurrent = !docRetired && (!versionId || versionId === d.current_version_id);` — with no ?v=, isCurrent collapses to `!docRetired`. lib/downloads.ts:99-101 `const version = ctx.versionId ?? ctx.doc.currentVersionId; const base = \\`${origin}/verify/${ctx.doc.id}\\`; return version ? \\`${base}?v=${version}\\` : base;`.",
    "note": "The mechanism is real and reachable (documents.current_version_id is nullable, schema.sql:144, while DocumentRecord.fileUrl is not, types/schema.ts:763, so a version-less document can still be printed). But 'unconditionally' is wrong — the docRetired guard at :89 still returns isCurrent:false for Superseded/Archived — and two of the four cited stamp sites actually GUARD against it: lib/docPack.ts:104-105 and app/api/share/file/route.ts:114-116 both emit `verifyUrl: undefined` when versionId is null rather than a bare /verify/<docId>. Only lib/downloads.ts produces the version-less URL, which narrows this to MEDIUM."
  },
  {
    "id": "VFY-4",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "app/api/verify/route.ts:93-94 `effectiveDate.slice(0, 10) > new Date().toISOString().slice(0, 10)` vs lib/effectiveDate.ts:23-27 `const today = new Date(); today.setHours(0,0,0,0); const eff = new Date(\\`${effectiveDate.slice(0,10)}T00:00:00\\`); if (eff.getTime() > today.getTime()) return \"pending\";`.",
    "note": "The divergence is real but the finding misdiagnoses the fix. effectiveStatusFor is RUNTIME-local, and this is a server route on a UTC host, so calling the canonical helper would compute the identical answer — the two only disagree because the register/badge call sites run in the browser (lib/docControlRegister.ts:187 uses the browser supabase client). A repo-wide grep for timezone/org-locale settings finds none, so there is no plant timezone to compare against; the actual defect is a one-day-boundary disagreement bounded by the UTC offset between the field page and the in-app badge. LOW."
  },
  {
    "id": "VFY-5",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/verify/route.ts:34-108 queries only `documents` and `document_versions` — `document_holds` appears nowhere in the file, and the response object (:96-108) has no hold field. Same for app/api/verify-package/route.ts:31-77 (`work_packages`, `work_package_documents`, `documents` only). The DB itself treats an active hold as a stop: supabase/migrations/20260713_document_publish_guard.sql:68-78 `-- Block: an active (unreleased) hold exists. ... RAISE EXCEPTION 'Document has an active hold; release the hold before publishing a new revision.'`.",
    "note": "Confirmed by reading both routes end to end. A hold blocks publication at the database level but is invisible to the two public endpoints the field actually scans, so a held drawing and its pack both render green while the printed hold card says stop."
  },
  {
    "id": "VFY-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/verify-hold/route.ts:48-56 — the comment reads 'a photographed hold card must not disclose staff names or free-text operator notes ... to whoever scans it', then the response returns `reason: (h.reason as string) ?? null`. supabase/migrations/20260612_phase5_holds.sql:29-35 `reason is TEXT with NO check constraint. The four directive-named reasons ... live in the UI's predefined picker; orgs can also enter free-form reasons via \"Other\".` components/documents/HoldStrip.tsx:182-206 renders the 'Other…' button and a `placeholder=\"Custom hold reason\"` input wired to `onOpen(otherDraft)`.",
    "note": "Right on both halves: the route withholds `notes` and `opened_by_name` but publishes `reason` verbatim to an unauthenticated endpoint, and `reason` is exactly the field the UI lets a controller type free text into. app/verify-hold/[holdId]/page.tsx:110-113 prints it under 'Reason' on the public page."
  },
  {
    "id": "VFY-7",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/d/[number]/route.ts:20 computes `const norm = raw.toLowerCase().replace(/[^a-z0-9]+/g, \"\");` but :30 queries with the RAW string: `.ilike(\"document_number\", \\`%${raw.replace(/[%_]/g, \"\")}%\\`)` — norm is used only for the exact-match tiebreak at :35. :36 `?? (rows ?? [])[0]` silently falls back to the newest-updated substring match.",
    "note": "Both halves confirmed against the file's own header claim at :4-5 ('punctuation- and case-forgiving, same normalization as search'). Additional unclaimed exposure: the supabaseAdmin query at :27-32 is not scoped to any org, so the substring fallback can redirect to a document belonging to a different tenant."
  },
  {
    "id": "VFY-8",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "app/api/verify-package/route.ts:73 `allFresh: sheets.length > 0 && staleCount === 0` — packageStatus and closed are returned (:69-70) but never enter the verdict; app/verify-package/[packageId]/page.tsx:60 picks the background from `result?.allFresh` alone and :91-93 prints 'PACK IS CURRENT' from the same flag.",
    "note": "The verdict-ignores-closure part is true, but 'ignores packageStatus and closed_at entirely' is not: page.tsx:97 appends `{result.closed ? \" (This package has been closed.)\" : \"\"}` to the sentence directly under the headline, so closure IS disclosed on the same screen. The green banner is also literally accurate — every sheet still is the current revision. That mitigation drops this to LOW."
  },
  {
    "id": "VFY-9",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "app/api/verify/route.ts:89-90 — `const docRetired = d.status === \"Superseded\" || d.status === \"Archived\";` / `const isCurrent = !docRetired && (!versionId || versionId === d.current_version_id);`. The version select at :56 is `.select(\"revision_label, created_at, superseded_at, record_id\")` — `released_at` (the issued marker on document_versions) is never read by this route. Reachable producer: lib/outputTemplates.ts:226-233 calls `createDocumentWithFile({... status: \"Draft\" ...})`, and lib/revisions.ts:384 then runs `.update({ current_version_id: ver.id ...})` on that same Draft document.",
    "note": "Confirmed: the only retirement test is Superseded/Archived, so a document whose status is Draft (or 'In Review', or 'Void' — all offered in components/documents/MetadataEditor.tsx:9 and BulkEditModal.tsx:32) with current_version_id set returns isCurrent:true, and app/verify/[docId]/page.tsx:99 renders 'CURRENT' on emerald with 'This print matches the current revision.' The narrated scenario is slightly off — a submit-for-review draft lands in pending_version_id, not current_version_id (lib/revisions.ts:1550, app/api/intake/upload/route.ts:330), so that particular version scans RED — but the template-filing path above produces exactly the claimed Draft-scans-green state, and Void scanning green is the same hole."
  },
  {
    "id": "WIRE-1",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "HIGH",
    "evidence": "equipmentBridgeServer.ts:190 `const targetKey = bridge?.targetColumnKey;` then :272-274 `admin.from(\"documents\").update({ metadata: { ...metadata, [targetKey]: next }, ... })` — the write lands in the metadata JSONB. The sync trigger fires only on the real column: 20260609_phase1_normalization.sql:116-119 `CREATE TRIGGER documents_resync_assets_trg AFTER INSERT OR UPDATE OF asset_tags, org_id ON documents`. A metadata-only UPDATE does not satisfy `UPDATE OF asset_tags`, so no document_assets row is written. Repo-wide grep confirms equipmentBridgeServer.ts never touches `document_assets` or `asset_tags` at all; the only writers of documents.asset_tags are documentLifecycle/common.ts:179 and merge.ts:150. Consumers therefore miss it: assets/[tag]/page.tsx:55 `.contains(\"asset_tags\", [{ tag }])` (that page never queries document_assets or metadata — grep returns nothing), and orgGraph.ts:118-119/:262 `for (const r of docAssets.rows) addEdge(\\`doc:${r.document_id}\\`, \\`asset:${r.asset_id}\\`, \"tag\")`.",
    "note": "The wiring gap is real and I could find no path that closes it — no backfill, no second write, no metadata-aware reader. Severity lowered to HIGH: nothing is corrupted or lost (the tags persist in metadata and render as chips), and one reverse surface is unaffected — assets/[tag]/page.tsx:228 also mounts MentionsPanel, which reads entity_mentions rather than document_assets. CRITICAL overstates a broken reverse index."
  },
  {
    "id": "WIRE-10",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "20261017_process_flows.sql:17-20 `from_ref TEXT NOT NULL, to_kind TEXT NOT NULL CHECK (...), to_ref TEXT NOT NULL` — no REFERENCES on either ref; lib/assets.ts:217-220 `deleteAsset` is a bare `.delete().eq(\"id\", id)` with no flow cleanup (its only caller, app/(protected)/admin/assets/page.tsx:1402, does nothing else); lib/orgGraph.ts:174 `if (a === b || !nodes.has(a) || !nodes.has(b)) return;` drops the edge with no truncation push.",
    "note": "The dangling-row and silent-drop halves are correct — nothing anywhere deletes process_flows rows when an asset dies (repo-wide grep: process_flows is touched only by lib/processFlows.ts, app/api/flows/read, lib/orgGraph.ts, exportTables/dataRestore). But the 'no index' half is wrong: `UNIQUE (org_id, from_kind, from_ref, to_kind, to_ref)` (line 36) creates a btree whose leading columns cover from-side lookups, and no query in the repo filters by ref at all (every read is `.eq(\"org_id\", …)`). Impact is also narrower than stated: once the asset is gone the hop is genuinely unrepresentable, so the residue is stale rows in exports/proposal lists rather than a wrong topology — LOW."
  },
  {
    "id": "WIRE-2",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "mentionIndexer.ts:136-142 `.upsert(batch, { onConflict: \"asset_id,knowledge_document_id,page\" })` vs the only unique index that exists, 20260929_mention_engine.sql:59-60 `CREATE UNIQUE INDEX ... entity_mentions_unique_idx ON entity_mentions (asset_id, COALESCE(knowledge_document_id, document_id), page)` — an EXPRESSION index; ON CONFLICT arbiter inference matches expressions literally, and `COALESCE(a,b)` is not `knowledge_document_id`. linkProposerServer.ts:403-406 `.upsert(rows, { onConflict: \"document_id,target_document_id\", ignoreDuplicates: true })` vs 20260807_link_proposals.sql:111-113 `CREATE UNIQUE INDEX ... ON document_related_resources (document_id, target_document_id) WHERE target_document_id IS NOT NULL` — a PARTIAL index is only a valid arbiter when the ON CONFLICT clause repeats its predicate, which PostgREST's on_conflict never emits. I checked every migration plus supabase/schema.sql: these are the ONLY unique indexes on either table. Swallowing confirmed: ingest/route.ts:157-169 wraps indexDocumentMentions in `try { ... } catch { /* mention edges are a bonus — never block ingestion */ }`.",
    "note": "Both ON CONFLICT targets raise 42P10 and both paths are non-fatal, so the mention graph and auto-applied links stay empty with a success response. One precision note: the link proposer is not fully silent — it does `notes.push(\\`Auto-apply skipped: ${error.message}\\`)` at :406 — but it still never throws and autoApplied stays 0. HIGH stands."
  },
  {
    "id": "WIRE-3",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "20260606_operational_entity_graph.sql:105-119 declares and indexes all six columns (assets.plant_id/unit_id/system_id, documents.plant_id/unit_id/system_id). Writes: repo-wide grep for `unit_id:`/`plant_id:` in INSERT/UPDATE payloads yields only documentLifecycle/common.ts:180-182 `plant_id: input.plantId ?? null, unit_id: input.unitId ?? null, system_id: input.systemId ?? null` — and its ONLY two callers are split.ts:113 `plantId: copyScope ? (source.plantId ?? null) : null` and merge.ts:122 `scopeIfAllAgree(sources, \"plantId\")`, i.e. copies of a value no primary path ever originates. assets.ts:172-202 `createAsset` builds `base` with no scope FK at all, and :206 `updateAsset` types its patch as `Partial<Pick<Asset, \"tag\"|\"type_id\"|\"description\"|\"location\"|\"library_id\"|\"archived\"|\"cover_photo_id\"|\"unit_code\"|\"code\">>` — the three FKs are excluded. Grepping `unitId` across components/ and app/ returns only admin/scope/page.tsx:88-90, which builds the hierarchy itself; no UI ever files a document or asset into it. Reads confirmed at orgGraph.ts:256-260 and search.ts:179-181/540-543.",
    "note": "Confirmed dead scaffold. The only write path copies from a field that is always null, so assets.unit_id and documents.unit_id are null in every real row — which makes /admin/scope decorative, the search scope filters silently empty, and every `unit:`/`plant:` node degree-0 and then deleted by orgGraph.ts:321-325. HIGH stands."
  },
  {
    "id": "WIRE-4",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "orgGraph.ts:23 `export type GraphNodeType = \"document\" | \"asset\" | \"unit\" | \"library\" | \"project\" | \"plant\" | \"plot\";` — seven types, and buildOrgGraph (:99-144) queries only units, plants, libraries, projects, documents, assets, plot_plans plus five edge tables. No ticket, hold, checkout, work-package, milestone, transmittal, system, revision or person is ever a node. But the finding's worked example is wrong: graph/page.tsx:430 defines the Process lens as `hidden: [\"document\", \"library\", \"project\", \"plot\"]` — plot plans are HIDDEN in Process, not shown, and its title \"Units and equipment only — the plant as flow\" describes exactly what it renders (plus `flow` edges from process_flows, loaded at orgGraph.ts:133). Plot plans leak into the Equipment lens instead (:431 `hidden: [\"unit\",\"plant\",\"project\",\"library\"]` against the title \"Equipment and the documents that govern it\").",
    "note": "The countable core is true — the graph has seven node types and no lifecycle entity is among them — but the narrative that convicts the lenses is misattributed: the Process lens does hide plot plans and its label is accurate. What remains is a scope gap (the graph was built as an entity/paper map, not a work-in-flight map) plus one minor label/content mismatch in the Equipment lens, which is LOW, not MEDIUM."
  },
  {
    "id": "WIRE-5",
    "verdict": "REFUTED",
    "corrected_severity": null,
    "evidence": "The claim \"no query can answer 'which sheets carry E-22'\" and \"the only surface that can answer is /api/knowledge/locate\" are both false. lib/mentions.ts:126-134 `mentionsForAsset(orgId, assetId)` selects entity_mentions `.eq(\"asset_id\", assetId).order(\"mention_count\", ...)` — the table carries `page` (20260929_mention_engine.sql:35) and both document_id and knowledge_document_id — and it is mounted on the equipment hub itself: app/(protected)/assets/[tag]/page.tsx:20 `import MentionsPanel from \"@/components/assets/MentionsPanel\"` and :228 `<MentionsPanel orgId={activeOrgId} assetId={asset.id} tag={tag} />`, whose header comment reads \"the backlinks panel\". Independently, ask/route.ts:1660-1670 answers the question in the ask surface: \"Any tag the question or answer names that no citation covers gets a direct sheet citation from the entity index\" — `.from(\"knowledge_page_entities\").select(\"document_id, page, tag, raw\").eq(\"library_id\", libraryId).eq(\"kind\", \"equipment\").in(\"tag\", wanted)`. orchestrator/tools.ts:320-330 does the same join for the same-sheet check.",
    "note": "REFUTED: two surfaces outside the drawing viewer answer the owner's Q6 — the equipment hub's MentionsPanel (asset → document + page, via entity_mentions) and the knowledge ask route's sheet-citation path (tag → sheet + page, via knowledge_page_entities). The residual truth is that the three stores are not reconciled with each other and that the entity_mentions store is left empty by the separate 42P10 bug in WIRE-2 — but that is WIRE-2's finding, not an absence of any query or surface."
  },
  {
    "id": "WIRE-6",
    "verdict": "SURVIVES",
    "corrected_severity": null,
    "evidence": "flows/read/route.ts:58 `supabaseAdmin.from(\"assets\").select(\"id, tag\").eq(\"org_id\", orgId).eq(\"archived\", false).limit(4000)` — no `.order()`, so row order is whatever the heap returns. :68 `assets.slice(0, 300).forEach((a, i) => roster.push({ ref: \\`A${i+1}\\`, kind: \"asset\", id: a.id, label: a.tag }));` — only those 300 reach `byRef`, which is the sole mapping from the model's answer back to real ids. The empty result at :173-177 says only \"No new flows found — the drawing may not print flow arrows between known tags, or everything visible was already decided.\" — the truncation is never mentioned, and nothing in the response body reports assets.length vs 300.",
    "note": "Confirmed on every point: unordered fetch, hard slice at 300, no warning in the response, and the guard at :62 only fires when the registry is entirely empty. A 2,000-asset plant silently reads its PFD against an arbitrary 15% of its own registry. MEDIUM stands."
  },
  {
    "id": "WIRE-7",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "Two of the five entities named in the title DO carry an area reference. types/schema.ts:547 `unit: string;` on Ticket (required, not optional), and types/schema.ts:498-499 on Milestone: `/** Where the work happens — area / unit / equipment tag. */ location?: string | null;` plus :489 `workOrderRef`. The cited range 1106-1113 stops well above that field. The claim does hold for the other three: DocumentHold (types/schema.ts:452-466) has only documentId/reason/notes/opened*/released*; CheckoutSession (:911-940) has documentId/libraryId/userId/mode/projectId/episodeId and no scope field; 20260825_work_packages_acks.sql:21-35 declares work_packages with name/description/status/owner and no unit, asset or plant column.",
    "note": "SURVIVES_CORRECTED: the underlying observation — no FK from any in-flight work object to assets or units, so \"everything in flight on the crude unit\" needs a multi-hop join that mostly dead-ends — is fair, but the title's \"no reference to equipment or to an operating area at all\" is factually wrong for tickets (`unit`) and milestones (`location`, explicitly documented as area/unit/equipment tag). With that overstatement removed this is a schema-scope feature gap, not a defect: LOW."
  },
  {
    "id": "WIRE-8",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "Facts check out. transmittals.ts:396-402 `.from(\"transmittals\").select(\"*\").eq(\"org_id\", orgId).contains(\"items\", JSON.stringify([{ documentId }]))` against 20260717_transmittals.sql:40 `items JSONB NOT NULL DEFAULT '[]'::jsonb` whose only indexes are :53-54 `transmittals_org_number_idx (org_id, number)` and `transmittals_org_status_idx (org_id, status, created_at DESC)`. impact.ts:111-118 `.eq(\"metadata->source_document->>id\", documentId)`; a repo-wide grep of every migration and supabase/schema.sql for `USING GIN` shows tickets has GIN only on watchers, search_tsv and ticket_id trigram — none on metadata. The shape divergence is real: requests/new/page.tsx:290-296 writes `source_document: { id, document_number, title, rev, path }` while transitionIn.ts:321-322 writes `source_document: { id, number, title }`, and intake/resolve/route.ts:151-154 reads `meta.source_document?.number`.",
    "note": "SURVIVES on facts, downgraded on impact. Both queries stay org-scoped, and the finding's own scale (\"a few thousand transmittals\") is a sub-millisecond scan in Postgres — the performance claim does not carry MEDIUM. The shape mismatch is currently unreachable: intake/resolve pre-filters on `metadata->intake_collision->>intakeLinkId`, which only transitionIn writes, so it only ever sees the `number` shape. Latent inconsistency worth fixing, LOW."
  },
  {
    "id": "WIRE-9",
    "verdict": "SURVIVES_CORRECTED",
    "corrected_severity": "LOW",
    "evidence": "Both halves confirmed by exhaustive grep — `related_ticket_id|relatedTicketId` across all .ts/.tsx/.sql yields exactly six non-test hits: the DDL (supabase/schema.sql:334 `related_ticket_id UUID,`), the type (types/schema.ts:774), two reads (revisions.ts:958 and documents/[libraryId]/page.tsx:1893, both `relatedTicketId: r.related_ticket_id as string | undefined`), and the consumer (reviewControl.ts:60 `if (input.relatedTicketId) return \"none\";`). No INSERT or UPDATE anywhere sets the column. And the second layer holds too: reviewControl.ts:55-56 takes `relatedTicketId?: string | null` but RevUpModal.tsx:210 calls `effectiveModeForRevUp({ control: reviewControl ?? { mode: \"none\" }, changeType })` and setRevUp.ts:83 calls `effectiveModeForRevUp({ control, changeType })` — neither passes the argument.",
    "note": "SURVIVES on the facts — the escape hatch is dead twice over, so a ticket-derived rev never skips the gate as documented. Downgraded because the failure direction is safe: the consequence is an unnecessary trip through the reviewer queue, never a revision publishing without a review it should have had. Dead documented feature + extra friction is LOW, not MEDIUM."
  }
]